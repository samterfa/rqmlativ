
	#' List StateCrosswalks
	#'
	#' This function returns a dataframe or json object of StateCrosswalks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateCrosswalks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateCrosswalks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateCrosswalk') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateCrosswalks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateCrosswalks <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DimensionGroupID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateCrosswalkID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateCrosswalk", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateCrosswalk
	#'
	#' This function returns a dataframe or json object of a StateCrosswalk
	#' @param StateCrosswalkID The ID of the StateCrosswalk to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateCrosswalk. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateCrosswalk.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateCrosswalk') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateCrosswalk
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateCrosswalk <- function(StateCrosswalkID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DimensionGroupID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateCrosswalkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateCrosswalk", objectId = StateCrosswalkID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateCrosswalk
	#'
	#' This function deletes a StateCrosswalk
	#' @param StateCrosswalkID The ID of the StateCrosswalk to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateCrosswalkID of the deleted StateCrosswalk.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateCrosswalk <- function(StateCrosswalkID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateCrosswalk", objectId = StateCrosswalkID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateCrosswalk
	#'
	#' This function creates a StateCrosswalk
	#' @param fieldNames The field values to give the created StateCrosswalk. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateCrosswalk
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateCrosswalk <- function(Code = NULL, Description = NULL, DimensionGroupID = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateCrosswalk", body = list(DataObject = body), searchFields = append("StateCrosswalkID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateCrosswalk
	#'
	#' This function modifies a StateCrosswalk
	#' @param fieldNames The field values to give the modified StateCrosswalk. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateCrosswalk
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateCrosswalk <- function(StateCrosswalkID, Code = NULL, Description = NULL, DimensionGroupID = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateCrosswalk", objectId = StateCrosswalkID, body = list(DataObject = body), searchFields = append("StateCrosswalkID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSchoolAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of TeacherSchoolAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSchoolAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSchoolAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSchoolAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of TeacherSchoolAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSchoolAssociationDashboardV1s <- function(searchConditionsList = NULL, AcademicSubjects = F, CreatedTime = F, DashboardRunHistoryID = F, EntityID = F, GradeLevels = F, KeyHash = F, ModifiedTime = F, ProgramAssignment = F, SchoolCode = F, SchoolYearID = F, StaffID = F, StaffUniqueID = F, TeacherSchoolAssociationDashboardV1ID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TeacherSchoolAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSchoolAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a TeacherSchoolAssociationDashboardV1
	#' @param TeacherSchoolAssociationDashboardV1ID The ID of the TeacherSchoolAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSchoolAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSchoolAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSchoolAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of TeacherSchoolAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSchoolAssociationDashboardV1 <- function(TeacherSchoolAssociationDashboardV1ID, AcademicSubjects = F, CreatedTime = F, DashboardRunHistoryID = F, EntityID = F, GradeLevels = F, KeyHash = F, ModifiedTime = F, ProgramAssignment = F, SchoolCode = F, SchoolYearID = F, StaffID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSchoolAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TeacherSchoolAssociationDashboardV1", objectId = TeacherSchoolAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSchoolAssociationDashboardV1
	#'
	#' This function deletes a TeacherSchoolAssociationDashboardV1
	#' @param TeacherSchoolAssociationDashboardV1ID The ID of the TeacherSchoolAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The TeacherSchoolAssociationDashboardV1ID of the deleted TeacherSchoolAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSchoolAssociationDashboardV1 <- function(TeacherSchoolAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TeacherSchoolAssociationDashboardV1", objectId = TeacherSchoolAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSchoolAssociationDashboardV1
	#'
	#' This function creates a TeacherSchoolAssociationDashboardV1
	#' @param fieldNames The field values to give the created TeacherSchoolAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created TeacherSchoolAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSchoolAssociationDashboardV1 <- function(AcademicSubjects = NULL, DashboardRunHistoryID = NULL, EntityID = NULL, GradeLevels = NULL, ProgramAssignment = NULL, SchoolCode = NULL, SchoolYearID = NULL, StaffID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TeacherSchoolAssociationDashboardV1", body = list(DataObject = body), searchFields = append("TeacherSchoolAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSchoolAssociationDashboardV1
	#'
	#' This function modifies a TeacherSchoolAssociationDashboardV1
	#' @param fieldNames The field values to give the modified TeacherSchoolAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified TeacherSchoolAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSchoolAssociationDashboardV1 <- function(TeacherSchoolAssociationDashboardV1ID, AcademicSubjects = NULL, DashboardRunHistoryID = NULL, EntityID = NULL, GradeLevels = NULL, ProgramAssignment = NULL, SchoolCode = NULL, SchoolYearID = NULL, StaffID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TeacherSchoolAssociationDashboardV1", objectId = TeacherSchoolAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("TeacherSchoolAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateReportingConfigDistricts
	#'
	#' This function returns a dataframe or json object of StateReportingConfigDistricts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingConfigDistricts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingConfigDistricts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingConfigDistrict') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateReportingConfigDistricts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateReportingConfigDistricts <- function(searchConditionsList = NULL, ConfigDistrictID = F, ConfigDistrictMNID = F, CreatedTime = F, DistrictID = F, EarnedCreditsMethodIDDefaultOverride = F, EnableEdFiDashboard = F, ESSADistrictExpenditureStandardFilterData = F, ESSADistrictExpenditureXMLFilter = F, HistoricalTranscriptYears = F, ModifiedTime = F, NewHireDateType = F, PERAExclusionType = F, STARCreditType = F, UnemploymentCompensationTaxableWageBase = F, UnemploymentCompensationTaxRate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ConfigDistrict", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateReportingConfigDistrict
	#'
	#' This function returns a dataframe or json object of a StateReportingConfigDistrict
	#' @param StateReportingConfigDistrictID The ID of the StateReportingConfigDistrict to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingConfigDistrict. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingConfigDistrict.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingConfigDistrict') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateReportingConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateReportingConfigDistrict <- function(StateReportingConfigDistrictID, ConfigDistrictID = F, ConfigDistrictMNID = F, CreatedTime = F, DistrictID = F, EarnedCreditsMethodIDDefaultOverride = F, EnableEdFiDashboard = F, ESSADistrictExpenditureStandardFilterData = F, ESSADistrictExpenditureXMLFilter = F, HistoricalTranscriptYears = F, ModifiedTime = F, NewHireDateType = F, PERAExclusionType = F, STARCreditType = F, UnemploymentCompensationTaxableWageBase = F, UnemploymentCompensationTaxRate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateReportingConfigDistrictID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ConfigDistrict", objectId = StateReportingConfigDistrictID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateReportingConfigDistrict
	#'
	#' This function deletes a StateReportingConfigDistrict
	#' @param StateReportingConfigDistrictID The ID of the StateReportingConfigDistrict to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateReportingConfigDistrictID of the deleted StateReportingConfigDistrict.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateReportingConfigDistrict <- function(StateReportingConfigDistrictID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ConfigDistrict", objectId = StateReportingConfigDistrictID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateReportingConfigDistrict
	#'
	#' This function creates a StateReportingConfigDistrict
	#' @param fieldNames The field values to give the created StateReportingConfigDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateReportingConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateReportingConfigDistrict <- function(DistrictID = NULL, EarnedCreditsMethodIDDefaultOverride = NULL, EnableEdFiDashboard = NULL, HistoricalTranscriptYears = NULL, NewHireDateType = NULL, PERAExclusionType = NULL, STARCreditType = NULL, UnemploymentCompensationTaxableWageBase = NULL, UnemploymentCompensationTaxRate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ConfigDistrict", body = list(DataObject = body), searchFields = append("ConfigDistrictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateReportingConfigDistrict
	#'
	#' This function modifies a StateReportingConfigDistrict
	#' @param fieldNames The field values to give the modified StateReportingConfigDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateReportingConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateReportingConfigDistrict <- function(ConfigDistrictID, DistrictID = NULL, EarnedCreditsMethodIDDefaultOverride = NULL, EnableEdFiDashboard = NULL, HistoricalTranscriptYears = NULL, NewHireDateType = NULL, PERAExclusionType = NULL, STARCreditType = NULL, UnemploymentCompensationTaxableWageBase = NULL, UnemploymentCompensationTaxRate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ConfigDistrict", objectId = ConfigDistrictID, body = list(DataObject = body), searchFields = append("ConfigDistrictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AttendanceEventDashboardV1s
	#'
	#' This function returns a dataframe or json object of AttendanceEventDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AttendanceEventDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AttendanceEventDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AttendanceEventDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of AttendanceEventDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAttendanceEventDashboardV1s <- function(searchConditionsList = NULL, AttendanceEventDashboardV1ID = F, CategoryCode = F, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, Date = F, EdFiAttendanceEventCategoryID = F, EdFiAttendanceEventTypeID = F, EdFiEducationalEnvironmentID = F, EdFiTermTypeID = F, EducationalEnvironmentCode = F, EntityID = F, KeyHash = F, LocalCourseCode = F, Location = F, ModifiedTime = F, Reason = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, StudentID = F, StudentUniqueID = F, TermCode = F, TypeCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "AttendanceEventDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AttendanceEventDashboardV1
	#'
	#' This function returns a dataframe or json object of an AttendanceEventDashboardV1
	#' @param AttendanceEventDashboardV1ID The ID of the AttendanceEventDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AttendanceEventDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AttendanceEventDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AttendanceEventDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of AttendanceEventDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAttendanceEventDashboardV1 <- function(AttendanceEventDashboardV1ID, CategoryCode = F, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, Date = F, EdFiAttendanceEventCategoryID = F, EdFiAttendanceEventTypeID = F, EdFiEducationalEnvironmentID = F, EdFiTermTypeID = F, EducationalEnvironmentCode = F, EntityID = F, KeyHash = F, LocalCourseCode = F, Location = F, ModifiedTime = F, Reason = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, StudentID = F, StudentUniqueID = F, TermCode = F, TypeCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AttendanceEventDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "AttendanceEventDashboardV1", objectId = AttendanceEventDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AttendanceEventDashboardV1
	#'
	#' This function deletes an AttendanceEventDashboardV1
	#' @param AttendanceEventDashboardV1ID The ID of the AttendanceEventDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The AttendanceEventDashboardV1ID of the deleted AttendanceEventDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAttendanceEventDashboardV1 <- function(AttendanceEventDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "AttendanceEventDashboardV1", objectId = AttendanceEventDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AttendanceEventDashboardV1
	#'
	#' This function creates an AttendanceEventDashboardV1
	#' @param fieldNames The field values to give the created AttendanceEventDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created AttendanceEventDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAttendanceEventDashboardV1 <- function(CategoryCode = NULL, ClassPeriodName = NULL, DashboardRunHistoryID = NULL, Date = NULL, EdFiAttendanceEventCategoryID = NULL, EdFiAttendanceEventTypeID = NULL, EdFiEducationalEnvironmentID = NULL, EdFiTermTypeID = NULL, EducationalEnvironmentCode = NULL, EntityID = NULL, LocalCourseCode = NULL, Location = NULL, Reason = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, TypeCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "AttendanceEventDashboardV1", body = list(DataObject = body), searchFields = append("AttendanceEventDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AttendanceEventDashboardV1
	#'
	#' This function modifies an AttendanceEventDashboardV1
	#' @param fieldNames The field values to give the modified AttendanceEventDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified AttendanceEventDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAttendanceEventDashboardV1 <- function(AttendanceEventDashboardV1ID, CategoryCode = NULL, ClassPeriodName = NULL, DashboardRunHistoryID = NULL, Date = NULL, EdFiAttendanceEventCategoryID = NULL, EdFiAttendanceEventTypeID = NULL, EdFiEducationalEnvironmentID = NULL, EdFiTermTypeID = NULL, EducationalEnvironmentCode = NULL, EntityID = NULL, LocalCourseCode = NULL, Location = NULL, Reason = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, TypeCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "AttendanceEventDashboardV1", objectId = AttendanceEventDashboardV1ID, body = list(DataObject = body), searchFields = append("AttendanceEventDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List BehaviorDescriptorDashboardV1s
	#'
	#' This function returns a dataframe or json object of BehaviorDescriptorDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BehaviorDescriptorDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BehaviorDescriptorDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BehaviorDescriptorDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of BehaviorDescriptorDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listBehaviorDescriptorDashboardV1s <- function(searchConditionsList = NULL, BehaviorDescriptorDashboardV1ID = F, CategoryCode = F, Code = F, CreatedTime = F, DashboardRunHistoryID = F, Description = F, EdFiBehaviorCategoryTypeID = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, ShortDescription = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "BehaviorDescriptorDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a BehaviorDescriptorDashboardV1
	#'
	#' This function returns a dataframe or json object of a BehaviorDescriptorDashboardV1
	#' @param BehaviorDescriptorDashboardV1ID The ID of the BehaviorDescriptorDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BehaviorDescriptorDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BehaviorDescriptorDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BehaviorDescriptorDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of BehaviorDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getBehaviorDescriptorDashboardV1 <- function(BehaviorDescriptorDashboardV1ID, CategoryCode = F, Code = F, CreatedTime = F, DashboardRunHistoryID = F, Description = F, EdFiBehaviorCategoryTypeID = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, ShortDescription = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "BehaviorDescriptorDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "BehaviorDescriptorDashboardV1", objectId = BehaviorDescriptorDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a BehaviorDescriptorDashboardV1
	#'
	#' This function deletes a BehaviorDescriptorDashboardV1
	#' @param BehaviorDescriptorDashboardV1ID The ID of the BehaviorDescriptorDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The BehaviorDescriptorDashboardV1ID of the deleted BehaviorDescriptorDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteBehaviorDescriptorDashboardV1 <- function(BehaviorDescriptorDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "BehaviorDescriptorDashboardV1", objectId = BehaviorDescriptorDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a BehaviorDescriptorDashboardV1
	#'
	#' This function creates a BehaviorDescriptorDashboardV1
	#' @param fieldNames The field values to give the created BehaviorDescriptorDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created BehaviorDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createBehaviorDescriptorDashboardV1 <- function(CategoryCode = NULL, Code = NULL, DashboardRunHistoryID = NULL, Description = NULL, EdFiBehaviorCategoryTypeID = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearID = NULL, ShortDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "BehaviorDescriptorDashboardV1", body = list(DataObject = body), searchFields = append("BehaviorDescriptorDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a BehaviorDescriptorDashboardV1
	#'
	#' This function modifies a BehaviorDescriptorDashboardV1
	#' @param fieldNames The field values to give the modified BehaviorDescriptorDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified BehaviorDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyBehaviorDescriptorDashboardV1 <- function(BehaviorDescriptorDashboardV1ID, CategoryCode = NULL, Code = NULL, DashboardRunHistoryID = NULL, Description = NULL, EdFiBehaviorCategoryTypeID = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearID = NULL, ShortDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "BehaviorDescriptorDashboardV1", objectId = BehaviorDescriptorDashboardV1ID, body = list(DataObject = body), searchFields = append("BehaviorDescriptorDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalendarDateDashboardV1s
	#'
	#' This function returns a dataframe or json object of CalendarDateDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalendarDateDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalendarDateDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalendarDateDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of CalendarDateDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalendarDateDashboardV1s <- function(searchConditionsList = NULL, AssociatedCampusIDs = F, CalendarDateDashboardV1ID = F, CalendarEventCode = F, CreatedTime = F, DashboardRunHistoryID = F, Date = F, DistrictID = F, EdFiCalendarEventID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "CalendarDateDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalendarDateDashboardV1
	#'
	#' This function returns a dataframe or json object of a CalendarDateDashboardV1
	#' @param CalendarDateDashboardV1ID The ID of the CalendarDateDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalendarDateDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalendarDateDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalendarDateDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of CalendarDateDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalendarDateDashboardV1 <- function(CalendarDateDashboardV1ID, AssociatedCampusIDs = F, CalendarEventCode = F, CreatedTime = F, DashboardRunHistoryID = F, Date = F, DistrictID = F, EdFiCalendarEventID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalendarDateDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "CalendarDateDashboardV1", objectId = CalendarDateDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalendarDateDashboardV1
	#'
	#' This function deletes a CalendarDateDashboardV1
	#' @param CalendarDateDashboardV1ID The ID of the CalendarDateDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The CalendarDateDashboardV1ID of the deleted CalendarDateDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalendarDateDashboardV1 <- function(CalendarDateDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "CalendarDateDashboardV1", objectId = CalendarDateDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalendarDateDashboardV1
	#'
	#' This function creates a CalendarDateDashboardV1
	#' @param fieldNames The field values to give the created CalendarDateDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created CalendarDateDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalendarDateDashboardV1 <- function(AssociatedCampusIDs = NULL, CalendarEventCode = NULL, DashboardRunHistoryID = NULL, Date = NULL, DistrictID = NULL, EdFiCalendarEventID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "CalendarDateDashboardV1", body = list(DataObject = body), searchFields = append("CalendarDateDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalendarDateDashboardV1
	#'
	#' This function modifies a CalendarDateDashboardV1
	#' @param fieldNames The field values to give the modified CalendarDateDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified CalendarDateDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalendarDateDashboardV1 <- function(CalendarDateDashboardV1ID, AssociatedCampusIDs = NULL, CalendarEventCode = NULL, DashboardRunHistoryID = NULL, Date = NULL, DistrictID = NULL, EdFiCalendarEventID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "CalendarDateDashboardV1", objectId = CalendarDateDashboardV1ID, body = list(DataObject = body), searchFields = append("CalendarDateDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ClassPeriodDashboardV1s
	#'
	#' This function returns a dataframe or json object of ClassPeriodDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClassPeriodDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClassPeriodDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClassPeriodDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ClassPeriodDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listClassPeriodDashboardV1s <- function(searchConditionsList = NULL, ClassPeriodDashboardV1ID = F, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ClassPeriodDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ClassPeriodDashboardV1
	#'
	#' This function returns a dataframe or json object of a ClassPeriodDashboardV1
	#' @param ClassPeriodDashboardV1ID The ID of the ClassPeriodDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClassPeriodDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClassPeriodDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClassPeriodDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ClassPeriodDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getClassPeriodDashboardV1 <- function(ClassPeriodDashboardV1ID, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ClassPeriodDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ClassPeriodDashboardV1", objectId = ClassPeriodDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ClassPeriodDashboardV1
	#'
	#' This function deletes a ClassPeriodDashboardV1
	#' @param ClassPeriodDashboardV1ID The ID of the ClassPeriodDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ClassPeriodDashboardV1ID of the deleted ClassPeriodDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteClassPeriodDashboardV1 <- function(ClassPeriodDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ClassPeriodDashboardV1", objectId = ClassPeriodDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ClassPeriodDashboardV1
	#'
	#' This function creates a ClassPeriodDashboardV1
	#' @param fieldNames The field values to give the created ClassPeriodDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ClassPeriodDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createClassPeriodDashboardV1 <- function(ClassPeriodName = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ClassPeriodDashboardV1", body = list(DataObject = body), searchFields = append("ClassPeriodDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ClassPeriodDashboardV1
	#'
	#' This function modifies a ClassPeriodDashboardV1
	#' @param fieldNames The field values to give the modified ClassPeriodDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ClassPeriodDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyClassPeriodDashboardV1 <- function(ClassPeriodDashboardV1ID, ClassPeriodName = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ClassPeriodDashboardV1", objectId = ClassPeriodDashboardV1ID, body = list(DataObject = body), searchFields = append("ClassPeriodDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CohortDashboardV1s
	#'
	#' This function returns a dataframe or json object of CohortDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CohortDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CohortDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CohortDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of CohortDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCohortDashboardV1s <- function(searchConditionsList = NULL, AcademicSubject = F, CohortDashboardV1ID = F, CohortDescription = F, CohortIdentifier = F, CohortScope = F, CohortType = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiCohortID = F, EdFiCohortScopeID = F, EdFiSubjectTypeID = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "CohortDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CohortDashboardV1
	#'
	#' This function returns a dataframe or json object of a CohortDashboardV1
	#' @param CohortDashboardV1ID The ID of the CohortDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CohortDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CohortDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CohortDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of CohortDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCohortDashboardV1 <- function(CohortDashboardV1ID, AcademicSubject = F, CohortDescription = F, CohortIdentifier = F, CohortScope = F, CohortType = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiCohortID = F, EdFiCohortScopeID = F, EdFiSubjectTypeID = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CohortDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "CohortDashboardV1", objectId = CohortDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CohortDashboardV1
	#'
	#' This function deletes a CohortDashboardV1
	#' @param CohortDashboardV1ID The ID of the CohortDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The CohortDashboardV1ID of the deleted CohortDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCohortDashboardV1 <- function(CohortDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "CohortDashboardV1", objectId = CohortDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CohortDashboardV1
	#'
	#' This function creates a CohortDashboardV1
	#' @param fieldNames The field values to give the created CohortDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created CohortDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCohortDashboardV1 <- function(AcademicSubject = NULL, CohortDescription = NULL, CohortIdentifier = NULL, CohortScope = NULL, CohortType = NULL, DashboardRunHistoryID = NULL, EdFiCohortID = NULL, EdFiCohortScopeID = NULL, EdFiSubjectTypeID = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "CohortDashboardV1", body = list(DataObject = body), searchFields = append("CohortDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CohortDashboardV1
	#'
	#' This function modifies a CohortDashboardV1
	#' @param fieldNames The field values to give the modified CohortDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified CohortDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCohortDashboardV1 <- function(CohortDashboardV1ID, AcademicSubject = NULL, CohortDescription = NULL, CohortIdentifier = NULL, CohortScope = NULL, CohortType = NULL, DashboardRunHistoryID = NULL, EdFiCohortID = NULL, EdFiCohortScopeID = NULL, EdFiSubjectTypeID = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "CohortDashboardV1", objectId = CohortDashboardV1ID, body = list(DataObject = body), searchFields = append("CohortDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseDashboardV1s
	#'
	#' This function returns a dataframe or json object of CourseDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of CourseDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseDashboardV1s <- function(searchConditionsList = NULL, CourseDashboardV1ID = F, CourseID = F, CourseLevelCharacteristic = F, CourseTitle = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiCourseLevelCharacteristicID = F, EdFiSubjectTypeID = F, EntityID = F, KeyHash = F, LEACourseCode = F, ModifiedTime = F, NumberOfParts = F, SchoolCode = F, SchoolID = F, SchoolYearID = F, StateCourseCode = F, SubjectAreaCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "CourseDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseDashboardV1
	#'
	#' This function returns a dataframe or json object of a CourseDashboardV1
	#' @param CourseDashboardV1ID The ID of the CourseDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of CourseDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseDashboardV1 <- function(CourseDashboardV1ID, CourseID = F, CourseLevelCharacteristic = F, CourseTitle = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiCourseLevelCharacteristicID = F, EdFiSubjectTypeID = F, EntityID = F, KeyHash = F, LEACourseCode = F, ModifiedTime = F, NumberOfParts = F, SchoolCode = F, SchoolID = F, SchoolYearID = F, StateCourseCode = F, SubjectAreaCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "CourseDashboardV1", objectId = CourseDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseDashboardV1
	#'
	#' This function deletes a CourseDashboardV1
	#' @param CourseDashboardV1ID The ID of the CourseDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The CourseDashboardV1ID of the deleted CourseDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseDashboardV1 <- function(CourseDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "CourseDashboardV1", objectId = CourseDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseDashboardV1
	#'
	#' This function creates a CourseDashboardV1
	#' @param fieldNames The field values to give the created CourseDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created CourseDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseDashboardV1 <- function(CourseID = NULL, CourseLevelCharacteristic = NULL, CourseTitle = NULL, DashboardRunHistoryID = NULL, EdFiCourseLevelCharacteristicID = NULL, EdFiSubjectTypeID = NULL, EntityID = NULL, LEACourseCode = NULL, NumberOfParts = NULL, SchoolCode = NULL, SchoolID = NULL, SchoolYearID = NULL, StateCourseCode = NULL, SubjectAreaCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "CourseDashboardV1", body = list(DataObject = body), searchFields = append("CourseDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseDashboardV1
	#'
	#' This function modifies a CourseDashboardV1
	#' @param fieldNames The field values to give the modified CourseDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified CourseDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseDashboardV1 <- function(CourseDashboardV1ID, CourseID = NULL, CourseLevelCharacteristic = NULL, CourseTitle = NULL, DashboardRunHistoryID = NULL, EdFiCourseLevelCharacteristicID = NULL, EdFiSubjectTypeID = NULL, EntityID = NULL, LEACourseCode = NULL, NumberOfParts = NULL, SchoolCode = NULL, SchoolID = NULL, SchoolYearID = NULL, StateCourseCode = NULL, SubjectAreaCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "CourseDashboardV1", objectId = CourseDashboardV1ID, body = list(DataObject = body), searchFields = append("CourseDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseOfferingDashboardV1s
	#'
	#' This function returns a dataframe or json object of CourseOfferingDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseOfferingDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseOfferingDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseOfferingDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of CourseOfferingDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseOfferingDashboardV1s <- function(searchConditionsList = NULL, CourseID = F, CourseOfferingDashboardV1ID = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiTermTypeID = F, EntityID = F, KeyHash = F, LocalCourseCode = F, LocalCourseTitle = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "CourseOfferingDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseOfferingDashboardV1
	#'
	#' This function returns a dataframe or json object of a CourseOfferingDashboardV1
	#' @param CourseOfferingDashboardV1ID The ID of the CourseOfferingDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseOfferingDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseOfferingDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseOfferingDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of CourseOfferingDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseOfferingDashboardV1 <- function(CourseOfferingDashboardV1ID, CourseID = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiTermTypeID = F, EntityID = F, KeyHash = F, LocalCourseCode = F, LocalCourseTitle = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseOfferingDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "CourseOfferingDashboardV1", objectId = CourseOfferingDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseOfferingDashboardV1
	#'
	#' This function deletes a CourseOfferingDashboardV1
	#' @param CourseOfferingDashboardV1ID The ID of the CourseOfferingDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The CourseOfferingDashboardV1ID of the deleted CourseOfferingDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseOfferingDashboardV1 <- function(CourseOfferingDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "CourseOfferingDashboardV1", objectId = CourseOfferingDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseOfferingDashboardV1
	#'
	#' This function creates a CourseOfferingDashboardV1
	#' @param fieldNames The field values to give the created CourseOfferingDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created CourseOfferingDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseOfferingDashboardV1 <- function(CourseID = NULL, DashboardRunHistoryID = NULL, EdFiTermTypeID = NULL, EntityID = NULL, LocalCourseCode = NULL, LocalCourseTitle = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "CourseOfferingDashboardV1", body = list(DataObject = body), searchFields = append("CourseOfferingDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseOfferingDashboardV1
	#'
	#' This function modifies a CourseOfferingDashboardV1
	#' @param fieldNames The field values to give the modified CourseOfferingDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified CourseOfferingDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseOfferingDashboardV1 <- function(CourseOfferingDashboardV1ID, CourseID = NULL, DashboardRunHistoryID = NULL, EdFiTermTypeID = NULL, EntityID = NULL, LocalCourseCode = NULL, LocalCourseTitle = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "CourseOfferingDashboardV1", objectId = CourseOfferingDashboardV1ID, body = list(DataObject = body), searchFields = append("CourseOfferingDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseTranscriptDashboardV1s
	#'
	#' This function returns a dataframe or json object of CourseTranscriptDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseTranscriptDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseTranscriptDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseTranscriptDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of CourseTranscriptDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseTranscriptDashboardV1s <- function(searchConditionsList = NULL, AssigningOrganizationCode = F, CourseAttemptResult = F, CourseRepeatCode = F, CourseTranscriptDashboardV1ID = F, CreatedTime = F, CreditsAttempted = F, CreditsEarned = F, DashboardRunHistoryID = F, EdFiGradeLevelID = F, EntityID = F, FinalLetterGradeEarned = F, FinalNumericGradeEarned = F, GradeLevelWhenTaken = F, IdentificationSystem = F, KeyHash = F, LocalCourseCode = F, MethodCreditEarned = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, StudentID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "CourseTranscriptDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseTranscriptDashboardV1
	#'
	#' This function returns a dataframe or json object of a CourseTranscriptDashboardV1
	#' @param CourseTranscriptDashboardV1ID The ID of the CourseTranscriptDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseTranscriptDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseTranscriptDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseTranscriptDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of CourseTranscriptDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseTranscriptDashboardV1 <- function(CourseTranscriptDashboardV1ID, AssigningOrganizationCode = F, CourseAttemptResult = F, CourseRepeatCode = F, CreatedTime = F, CreditsAttempted = F, CreditsEarned = F, DashboardRunHistoryID = F, EdFiGradeLevelID = F, EntityID = F, FinalLetterGradeEarned = F, FinalNumericGradeEarned = F, GradeLevelWhenTaken = F, IdentificationSystem = F, KeyHash = F, LocalCourseCode = F, MethodCreditEarned = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, StudentID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseTranscriptDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "CourseTranscriptDashboardV1", objectId = CourseTranscriptDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseTranscriptDashboardV1
	#'
	#' This function deletes a CourseTranscriptDashboardV1
	#' @param CourseTranscriptDashboardV1ID The ID of the CourseTranscriptDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The CourseTranscriptDashboardV1ID of the deleted CourseTranscriptDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseTranscriptDashboardV1 <- function(CourseTranscriptDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "CourseTranscriptDashboardV1", objectId = CourseTranscriptDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseTranscriptDashboardV1
	#'
	#' This function creates a CourseTranscriptDashboardV1
	#' @param fieldNames The field values to give the created CourseTranscriptDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created CourseTranscriptDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseTranscriptDashboardV1 <- function(AssigningOrganizationCode = NULL, CourseAttemptResult = NULL, CourseRepeatCode = NULL, CreditsAttempted = NULL, CreditsEarned = NULL, DashboardRunHistoryID = NULL, EdFiGradeLevelID = NULL, EntityID = NULL, FinalLetterGradeEarned = NULL, FinalNumericGradeEarned = NULL, GradeLevelWhenTaken = NULL, IdentificationSystem = NULL, LocalCourseCode = NULL, MethodCreditEarned = NULL, SchoolCode = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "CourseTranscriptDashboardV1", body = list(DataObject = body), searchFields = append("CourseTranscriptDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseTranscriptDashboardV1
	#'
	#' This function modifies a CourseTranscriptDashboardV1
	#' @param fieldNames The field values to give the modified CourseTranscriptDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified CourseTranscriptDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseTranscriptDashboardV1 <- function(CourseTranscriptDashboardV1ID, AssigningOrganizationCode = NULL, CourseAttemptResult = NULL, CourseRepeatCode = NULL, CreditsAttempted = NULL, CreditsEarned = NULL, DashboardRunHistoryID = NULL, EdFiGradeLevelID = NULL, EntityID = NULL, FinalLetterGradeEarned = NULL, FinalNumericGradeEarned = NULL, GradeLevelWhenTaken = NULL, IdentificationSystem = NULL, LocalCourseCode = NULL, MethodCreditEarned = NULL, SchoolCode = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "CourseTranscriptDashboardV1", objectId = CourseTranscriptDashboardV1ID, body = list(DataObject = body), searchFields = append("CourseTranscriptDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DashboardRunHistories
	#'
	#' This function returns a dataframe or json object of DashboardRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DashboardRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DashboardRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DashboardRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of DashboardRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDashboardRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EndDateTime = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, SchoolYearID = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "DashboardRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DashboardRunHistory
	#'
	#' This function returns a dataframe or json object of a DashboardRunHistory
	#' @param DashboardRunHistoryID The ID of the DashboardRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DashboardRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DashboardRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DashboardRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of DashboardRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDashboardRunHistory <- function(DashboardRunHistoryID, CanAccessMedia = F, CreatedTime = F, DistrictID = F, EndDateTime = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, SchoolYearID = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DashboardRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "DashboardRunHistory", objectId = DashboardRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DashboardRunHistory
	#'
	#' This function deletes a DashboardRunHistory
	#' @param DashboardRunHistoryID The ID of the DashboardRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The DashboardRunHistoryID of the deleted DashboardRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDashboardRunHistory <- function(DashboardRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "DashboardRunHistory", objectId = DashboardRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DashboardRunHistory
	#'
	#' This function creates a DashboardRunHistory
	#' @param fieldNames The field values to give the created DashboardRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created DashboardRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDashboardRunHistory <- function(DistrictID = NULL, EndDateTime = NULL, MediaID = NULL, SchoolYearID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "DashboardRunHistory", body = list(DataObject = body), searchFields = append("DashboardRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DashboardRunHistory
	#'
	#' This function modifies a DashboardRunHistory
	#' @param fieldNames The field values to give the modified DashboardRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified DashboardRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDashboardRunHistory <- function(DashboardRunHistoryID, DistrictID = NULL, EndDateTime = NULL, MediaID = NULL, SchoolYearID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "DashboardRunHistory", objectId = DashboardRunHistoryID, body = list(DataObject = body), searchFields = append("DashboardRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisciplineActionDashboardV1s
	#'
	#' This function returns a dataframe or json object of DisciplineActionDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineActionDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineActionDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineActionDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of DisciplineActionDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisciplineActionDashboardV1s <- function(searchConditionsList = NULL, ActionIdentifier = F, ActualLength = F, CreatedTime = F, DashboardRunHistoryID = F, DifferenceReasonCode = F, DisciplineActionDashboardV1ID = F, DisciplineActionLength = F, DisciplineCode = F, DisciplineDate = F, DisciplineShortDescription = F, EdFiDisciplineActionLengthDifferenceReasonID = F, EntityID = F, IncidentIdentifier = F, IncidentOffenseNameActionID = F, KeyHash = F, ModifiedTime = F, SchoolDisciplinaryAssignment = F, SchoolDisciplinaryResponsibility = F, SchoolYearID = F, StaffUniqueID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "DisciplineActionDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisciplineActionDashboardV1
	#'
	#' This function returns a dataframe or json object of a DisciplineActionDashboardV1
	#' @param DisciplineActionDashboardV1ID The ID of the DisciplineActionDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineActionDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineActionDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineActionDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of DisciplineActionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisciplineActionDashboardV1 <- function(DisciplineActionDashboardV1ID, ActionIdentifier = F, ActualLength = F, CreatedTime = F, DashboardRunHistoryID = F, DifferenceReasonCode = F, DisciplineActionLength = F, DisciplineCode = F, DisciplineDate = F, DisciplineShortDescription = F, EdFiDisciplineActionLengthDifferenceReasonID = F, EntityID = F, IncidentIdentifier = F, IncidentOffenseNameActionID = F, KeyHash = F, ModifiedTime = F, SchoolDisciplinaryAssignment = F, SchoolDisciplinaryResponsibility = F, SchoolYearID = F, StaffUniqueID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisciplineActionDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "DisciplineActionDashboardV1", objectId = DisciplineActionDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisciplineActionDashboardV1
	#'
	#' This function deletes a DisciplineActionDashboardV1
	#' @param DisciplineActionDashboardV1ID The ID of the DisciplineActionDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The DisciplineActionDashboardV1ID of the deleted DisciplineActionDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisciplineActionDashboardV1 <- function(DisciplineActionDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "DisciplineActionDashboardV1", objectId = DisciplineActionDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisciplineActionDashboardV1
	#'
	#' This function creates a DisciplineActionDashboardV1
	#' @param fieldNames The field values to give the created DisciplineActionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created DisciplineActionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisciplineActionDashboardV1 <- function(ActionIdentifier = NULL, ActualLength = NULL, DashboardRunHistoryID = NULL, DifferenceReasonCode = NULL, DisciplineActionLength = NULL, DisciplineCode = NULL, DisciplineDate = NULL, DisciplineShortDescription = NULL, EdFiDisciplineActionLengthDifferenceReasonID = NULL, EntityID = NULL, IncidentIdentifier = NULL, IncidentOffenseNameActionID = NULL, SchoolDisciplinaryAssignment = NULL, SchoolDisciplinaryResponsibility = NULL, SchoolYearID = NULL, StaffUniqueID = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "DisciplineActionDashboardV1", body = list(DataObject = body), searchFields = append("DisciplineActionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisciplineActionDashboardV1
	#'
	#' This function modifies a DisciplineActionDashboardV1
	#' @param fieldNames The field values to give the modified DisciplineActionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified DisciplineActionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisciplineActionDashboardV1 <- function(DisciplineActionDashboardV1ID, ActionIdentifier = NULL, ActualLength = NULL, DashboardRunHistoryID = NULL, DifferenceReasonCode = NULL, DisciplineActionLength = NULL, DisciplineCode = NULL, DisciplineDate = NULL, DisciplineShortDescription = NULL, EdFiDisciplineActionLengthDifferenceReasonID = NULL, EntityID = NULL, IncidentIdentifier = NULL, IncidentOffenseNameActionID = NULL, SchoolDisciplinaryAssignment = NULL, SchoolDisciplinaryResponsibility = NULL, SchoolYearID = NULL, StaffUniqueID = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "DisciplineActionDashboardV1", objectId = DisciplineActionDashboardV1ID, body = list(DataObject = body), searchFields = append("DisciplineActionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisciplineDescriptorDashboardV1s
	#'
	#' This function returns a dataframe or json object of DisciplineDescriptorDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineDescriptorDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineDescriptorDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineDescriptorDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of DisciplineDescriptorDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisciplineDescriptorDashboardV1s <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DashboardRunHistoryID = F, Description = F, DisciplineDescriptorDashboardV1ID = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, ShortDescription = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "DisciplineDescriptorDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisciplineDescriptorDashboardV1
	#'
	#' This function returns a dataframe or json object of a DisciplineDescriptorDashboardV1
	#' @param DisciplineDescriptorDashboardV1ID The ID of the DisciplineDescriptorDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineDescriptorDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineDescriptorDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineDescriptorDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of DisciplineDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisciplineDescriptorDashboardV1 <- function(DisciplineDescriptorDashboardV1ID, Code = F, CreatedTime = F, DashboardRunHistoryID = F, Description = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, ShortDescription = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisciplineDescriptorDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "DisciplineDescriptorDashboardV1", objectId = DisciplineDescriptorDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisciplineDescriptorDashboardV1
	#'
	#' This function deletes a DisciplineDescriptorDashboardV1
	#' @param DisciplineDescriptorDashboardV1ID The ID of the DisciplineDescriptorDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The DisciplineDescriptorDashboardV1ID of the deleted DisciplineDescriptorDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisciplineDescriptorDashboardV1 <- function(DisciplineDescriptorDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "DisciplineDescriptorDashboardV1", objectId = DisciplineDescriptorDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisciplineDescriptorDashboardV1
	#'
	#' This function creates a DisciplineDescriptorDashboardV1
	#' @param fieldNames The field values to give the created DisciplineDescriptorDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created DisciplineDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisciplineDescriptorDashboardV1 <- function(Code = NULL, DashboardRunHistoryID = NULL, Description = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearID = NULL, ShortDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "DisciplineDescriptorDashboardV1", body = list(DataObject = body), searchFields = append("DisciplineDescriptorDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisciplineDescriptorDashboardV1
	#'
	#' This function modifies a DisciplineDescriptorDashboardV1
	#' @param fieldNames The field values to give the modified DisciplineDescriptorDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified DisciplineDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisciplineDescriptorDashboardV1 <- function(DisciplineDescriptorDashboardV1ID, Code = NULL, DashboardRunHistoryID = NULL, Description = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearID = NULL, ShortDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "DisciplineDescriptorDashboardV1", objectId = DisciplineDescriptorDashboardV1ID, body = list(DataObject = body), searchFields = append("DisciplineDescriptorDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisciplineIncidentAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of DisciplineIncidentAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineIncidentAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineIncidentAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineIncidentAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of DisciplineIncidentAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisciplineIncidentAssociationDashboardV1s <- function(searchConditionsList = NULL, CreatedTime = F, DashboardRunHistoryID = F, DisciplineIncidentAssociationDashboardV1ID = F, EntityID = F, IncidentID = F, IncidentIdentifier = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StudentID = F, StudentParticipationCode = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "DisciplineIncidentAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisciplineIncidentAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a DisciplineIncidentAssociationDashboardV1
	#' @param DisciplineIncidentAssociationDashboardV1ID The ID of the DisciplineIncidentAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineIncidentAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineIncidentAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineIncidentAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of DisciplineIncidentAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisciplineIncidentAssociationDashboardV1 <- function(DisciplineIncidentAssociationDashboardV1ID, CreatedTime = F, DashboardRunHistoryID = F, EntityID = F, IncidentID = F, IncidentIdentifier = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StudentID = F, StudentParticipationCode = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisciplineIncidentAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "DisciplineIncidentAssociationDashboardV1", objectId = DisciplineIncidentAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisciplineIncidentAssociationDashboardV1
	#'
	#' This function deletes a DisciplineIncidentAssociationDashboardV1
	#' @param DisciplineIncidentAssociationDashboardV1ID The ID of the DisciplineIncidentAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The DisciplineIncidentAssociationDashboardV1ID of the deleted DisciplineIncidentAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisciplineIncidentAssociationDashboardV1 <- function(DisciplineIncidentAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "DisciplineIncidentAssociationDashboardV1", objectId = DisciplineIncidentAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisciplineIncidentAssociationDashboardV1
	#'
	#' This function creates a DisciplineIncidentAssociationDashboardV1
	#' @param fieldNames The field values to give the created DisciplineIncidentAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created DisciplineIncidentAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisciplineIncidentAssociationDashboardV1 <- function(DashboardRunHistoryID = NULL, EntityID = NULL, IncidentID = NULL, IncidentIdentifier = NULL, SchoolYearID = NULL, StudentID = NULL, StudentParticipationCode = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "DisciplineIncidentAssociationDashboardV1", body = list(DataObject = body), searchFields = append("DisciplineIncidentAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisciplineIncidentAssociationDashboardV1
	#'
	#' This function modifies a DisciplineIncidentAssociationDashboardV1
	#' @param fieldNames The field values to give the modified DisciplineIncidentAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified DisciplineIncidentAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisciplineIncidentAssociationDashboardV1 <- function(DisciplineIncidentAssociationDashboardV1ID, DashboardRunHistoryID = NULL, EntityID = NULL, IncidentID = NULL, IncidentIdentifier = NULL, SchoolYearID = NULL, StudentID = NULL, StudentParticipationCode = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "DisciplineIncidentAssociationDashboardV1", objectId = DisciplineIncidentAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("DisciplineIncidentAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisciplineIncidentDashboardV1s
	#'
	#' This function returns a dataframe or json object of DisciplineIncidentDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineIncidentDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineIncidentDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineIncidentDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of DisciplineIncidentDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisciplineIncidentDashboardV1s <- function(searchConditionsList = NULL, BehaviorCode = F, BehaviorShortDescription = F, CreatedTime = F, DashboardRunHistoryID = F, DisciplineIncidentDashboardV1ID = F, EdFiIncidentLocationID = F, EdFiReporterDescriptionID = F, EntityID = F, IncidentDate = F, IncidentID = F, IncidentIdentifier = F, IncidentLocationCode = F, IncidentTime = F, KeyHash = F, ModifiedTime = F, ReporterDescriptionCode = F, ReporterName = F, SchoolCode = F, SchoolYearID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "DisciplineIncidentDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisciplineIncidentDashboardV1
	#'
	#' This function returns a dataframe or json object of a DisciplineIncidentDashboardV1
	#' @param DisciplineIncidentDashboardV1ID The ID of the DisciplineIncidentDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisciplineIncidentDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisciplineIncidentDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisciplineIncidentDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of DisciplineIncidentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisciplineIncidentDashboardV1 <- function(DisciplineIncidentDashboardV1ID, BehaviorCode = F, BehaviorShortDescription = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiIncidentLocationID = F, EdFiReporterDescriptionID = F, EntityID = F, IncidentDate = F, IncidentID = F, IncidentIdentifier = F, IncidentLocationCode = F, IncidentTime = F, KeyHash = F, ModifiedTime = F, ReporterDescriptionCode = F, ReporterName = F, SchoolCode = F, SchoolYearID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisciplineIncidentDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "DisciplineIncidentDashboardV1", objectId = DisciplineIncidentDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisciplineIncidentDashboardV1
	#'
	#' This function deletes a DisciplineIncidentDashboardV1
	#' @param DisciplineIncidentDashboardV1ID The ID of the DisciplineIncidentDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The DisciplineIncidentDashboardV1ID of the deleted DisciplineIncidentDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisciplineIncidentDashboardV1 <- function(DisciplineIncidentDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "DisciplineIncidentDashboardV1", objectId = DisciplineIncidentDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisciplineIncidentDashboardV1
	#'
	#' This function creates a DisciplineIncidentDashboardV1
	#' @param fieldNames The field values to give the created DisciplineIncidentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created DisciplineIncidentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisciplineIncidentDashboardV1 <- function(BehaviorCode = NULL, BehaviorShortDescription = NULL, DashboardRunHistoryID = NULL, EdFiIncidentLocationID = NULL, EdFiReporterDescriptionID = NULL, EntityID = NULL, IncidentDate = NULL, IncidentID = NULL, IncidentIdentifier = NULL, IncidentLocationCode = NULL, IncidentTime = NULL, ReporterDescriptionCode = NULL, ReporterName = NULL, SchoolCode = NULL, SchoolYearID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "DisciplineIncidentDashboardV1", body = list(DataObject = body), searchFields = append("DisciplineIncidentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisciplineIncidentDashboardV1
	#'
	#' This function modifies a DisciplineIncidentDashboardV1
	#' @param fieldNames The field values to give the modified DisciplineIncidentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified DisciplineIncidentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisciplineIncidentDashboardV1 <- function(DisciplineIncidentDashboardV1ID, BehaviorCode = NULL, BehaviorShortDescription = NULL, DashboardRunHistoryID = NULL, EdFiIncidentLocationID = NULL, EdFiReporterDescriptionID = NULL, EntityID = NULL, IncidentDate = NULL, IncidentID = NULL, IncidentIdentifier = NULL, IncidentLocationCode = NULL, IncidentTime = NULL, ReporterDescriptionCode = NULL, ReporterName = NULL, SchoolCode = NULL, SchoolYearID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "DisciplineIncidentDashboardV1", objectId = DisciplineIncidentDashboardV1ID, body = list(DataObject = body), searchFields = append("DisciplineIncidentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCohortScopes
	#'
	#' This function returns a dataframe or json object of EdFiCohortScopes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCohortScopes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCohortScopes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCohortScope') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCohortScopes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCohortScopes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiCohortScopeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCohortScope", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCohortScope
	#'
	#' This function returns a dataframe or json object of an EdFiCohortScope
	#' @param EdFiCohortScopeID The ID of the EdFiCohortScope to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCohortScope. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCohortScope.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCohortScope') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCohortScope
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCohortScope <- function(EdFiCohortScopeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCohortScopeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCohortScope", objectId = EdFiCohortScopeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCohortScope
	#'
	#' This function deletes an EdFiCohortScope
	#' @param EdFiCohortScopeID The ID of the EdFiCohortScope to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCohortScopeID of the deleted EdFiCohortScope.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCohortScope <- function(EdFiCohortScopeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCohortScope", objectId = EdFiCohortScopeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCohortScope
	#'
	#' This function creates an EdFiCohortScope
	#' @param fieldNames The field values to give the created EdFiCohortScope. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCohortScope
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCohortScope <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCohortScope", body = list(DataObject = body), searchFields = append("EdFiCohortScopeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCohortScope
	#'
	#' This function modifies an EdFiCohortScope
	#' @param fieldNames The field values to give the modified EdFiCohortScope. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCohortScope
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCohortScope <- function(EdFiCohortScopeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCohortScope", objectId = EdFiCohortScopeID, body = list(DataObject = body), searchFields = append("EdFiCohortScopeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingPeriodDashboardV1s
	#'
	#' This function returns a dataframe or json object of GradingPeriodDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriodDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriodDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriodDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GradingPeriodDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingPeriodDashboardV1s <- function(searchConditionsList = NULL, AssociatedCampusIDs = F, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EdFiGradingPeriodID = F, EndDate = F, GradingPeriodCode = F, GradingPeriodDashboardV1ID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, TotalInstructionalDays = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GradingPeriodDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingPeriodDashboardV1
	#'
	#' This function returns a dataframe or json object of a GradingPeriodDashboardV1
	#' @param GradingPeriodDashboardV1ID The ID of the GradingPeriodDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriodDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriodDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriodDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GradingPeriodDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingPeriodDashboardV1 <- function(GradingPeriodDashboardV1ID, AssociatedCampusIDs = F, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EdFiGradingPeriodID = F, EndDate = F, GradingPeriodCode = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, TotalInstructionalDays = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingPeriodDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GradingPeriodDashboardV1", objectId = GradingPeriodDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingPeriodDashboardV1
	#'
	#' This function deletes a GradingPeriodDashboardV1
	#' @param GradingPeriodDashboardV1ID The ID of the GradingPeriodDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GradingPeriodDashboardV1ID of the deleted GradingPeriodDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingPeriodDashboardV1 <- function(GradingPeriodDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GradingPeriodDashboardV1", objectId = GradingPeriodDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingPeriodDashboardV1
	#'
	#' This function creates a GradingPeriodDashboardV1
	#' @param fieldNames The field values to give the created GradingPeriodDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GradingPeriodDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingPeriodDashboardV1 <- function(AssociatedCampusIDs = NULL, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, EdFiGradingPeriodID = NULL, EndDate = NULL, GradingPeriodCode = NULL, SchoolYearID = NULL, TotalInstructionalDays = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GradingPeriodDashboardV1", body = list(DataObject = body), searchFields = append("GradingPeriodDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingPeriodDashboardV1
	#'
	#' This function modifies a GradingPeriodDashboardV1
	#' @param fieldNames The field values to give the modified GradingPeriodDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GradingPeriodDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingPeriodDashboardV1 <- function(GradingPeriodDashboardV1ID, AssociatedCampusIDs = NULL, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, EdFiGradingPeriodID = NULL, EndDate = NULL, GradingPeriodCode = NULL, SchoolYearID = NULL, TotalInstructionalDays = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GradingPeriodDashboardV1", objectId = GradingPeriodDashboardV1ID, body = list(DataObject = body), searchFields = append("GradingPeriodDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GraduationPlanDashboardV1s
	#'
	#' This function returns a dataframe or json object of GraduationPlanDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GraduationPlanDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GraduationPlanDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GraduationPlanDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GraduationPlanDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGraduationPlanDashboardV1s <- function(searchConditionsList = NULL, CreatedTime = F, DashboardRunHistoryID = F, EdFiGraduationPlanID = F, EntityID = F, GraduationPlanDashboardV1ID = F, GraduationPlanTypeCode = F, IsIndividualPlan = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, TotalCreditsRequired = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GraduationPlanDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GraduationPlanDashboardV1
	#'
	#' This function returns a dataframe or json object of a GraduationPlanDashboardV1
	#' @param GraduationPlanDashboardV1ID The ID of the GraduationPlanDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GraduationPlanDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GraduationPlanDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GraduationPlanDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GraduationPlanDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGraduationPlanDashboardV1 <- function(GraduationPlanDashboardV1ID, CreatedTime = F, DashboardRunHistoryID = F, EdFiGraduationPlanID = F, EntityID = F, GraduationPlanTypeCode = F, IsIndividualPlan = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, TotalCreditsRequired = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GraduationPlanDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GraduationPlanDashboardV1", objectId = GraduationPlanDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GraduationPlanDashboardV1
	#'
	#' This function deletes a GraduationPlanDashboardV1
	#' @param GraduationPlanDashboardV1ID The ID of the GraduationPlanDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GraduationPlanDashboardV1ID of the deleted GraduationPlanDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGraduationPlanDashboardV1 <- function(GraduationPlanDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GraduationPlanDashboardV1", objectId = GraduationPlanDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GraduationPlanDashboardV1
	#'
	#' This function creates a GraduationPlanDashboardV1
	#' @param fieldNames The field values to give the created GraduationPlanDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GraduationPlanDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGraduationPlanDashboardV1 <- function(DashboardRunHistoryID = NULL, EdFiGraduationPlanID = NULL, EntityID = NULL, GraduationPlanTypeCode = NULL, IsIndividualPlan = NULL, SchoolCode = NULL, SchoolYearID = NULL, TotalCreditsRequired = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GraduationPlanDashboardV1", body = list(DataObject = body), searchFields = append("GraduationPlanDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GraduationPlanDashboardV1
	#'
	#' This function modifies a GraduationPlanDashboardV1
	#' @param fieldNames The field values to give the modified GraduationPlanDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GraduationPlanDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGraduationPlanDashboardV1 <- function(GraduationPlanDashboardV1ID, DashboardRunHistoryID = NULL, EdFiGraduationPlanID = NULL, EntityID = NULL, GraduationPlanTypeCode = NULL, IsIndividualPlan = NULL, SchoolCode = NULL, SchoolYearID = NULL, TotalCreditsRequired = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GraduationPlanDashboardV1", objectId = GraduationPlanDashboardV1ID, body = list(DataObject = body), searchFields = append("GraduationPlanDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List LocalEducationAgencyDashboardV1s
	#'
	#' This function returns a dataframe or json object of LocalEducationAgencyDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given LocalEducationAgencyDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the LocalEducationAgencyDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('LocalEducationAgencyDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of LocalEducationAgencyDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listLocalEducationAgencyDashboardV1s <- function(searchConditionsList = NULL, AddressType = F, ApartmentRoomSuiteNumber = F, BuildingSiteNumber = F, City = F, CountryCode = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, DistrictID = F, DistrictName = F, EdFiCountryID = F, EdFiStateAbbreviationID = F, EducationServiceCenter = F, FaxNumber = F, KeyHash = F, LEACategory = F, LocalEducationAgencyDashboardV1ID = F, ModifiedTime = F, NameOfCounty = F, OrganizationCategory = F, PostalCode = F, SchoolYearID = F, StateAbbreviation = F, StreetNumberName = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Website = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "LocalEducationAgencyDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a LocalEducationAgencyDashboardV1
	#'
	#' This function returns a dataframe or json object of a LocalEducationAgencyDashboardV1
	#' @param LocalEducationAgencyDashboardV1ID The ID of the LocalEducationAgencyDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given LocalEducationAgencyDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the LocalEducationAgencyDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('LocalEducationAgencyDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of LocalEducationAgencyDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getLocalEducationAgencyDashboardV1 <- function(LocalEducationAgencyDashboardV1ID, AddressType = F, ApartmentRoomSuiteNumber = F, BuildingSiteNumber = F, City = F, CountryCode = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, DistrictID = F, DistrictName = F, EdFiCountryID = F, EdFiStateAbbreviationID = F, EducationServiceCenter = F, FaxNumber = F, KeyHash = F, LEACategory = F, ModifiedTime = F, NameOfCounty = F, OrganizationCategory = F, PostalCode = F, SchoolYearID = F, StateAbbreviation = F, StreetNumberName = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Website = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "LocalEducationAgencyDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "LocalEducationAgencyDashboardV1", objectId = LocalEducationAgencyDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a LocalEducationAgencyDashboardV1
	#'
	#' This function deletes a LocalEducationAgencyDashboardV1
	#' @param LocalEducationAgencyDashboardV1ID The ID of the LocalEducationAgencyDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The LocalEducationAgencyDashboardV1ID of the deleted LocalEducationAgencyDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteLocalEducationAgencyDashboardV1 <- function(LocalEducationAgencyDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "LocalEducationAgencyDashboardV1", objectId = LocalEducationAgencyDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a LocalEducationAgencyDashboardV1
	#'
	#' This function creates a LocalEducationAgencyDashboardV1
	#' @param fieldNames The field values to give the created LocalEducationAgencyDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created LocalEducationAgencyDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createLocalEducationAgencyDashboardV1 <- function(AddressType = NULL, ApartmentRoomSuiteNumber = NULL, BuildingSiteNumber = NULL, City = NULL, CountryCode = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, DistrictID = NULL, DistrictName = NULL, EdFiCountryID = NULL, EdFiStateAbbreviationID = NULL, EducationServiceCenter = NULL, FaxNumber = NULL, LEACategory = NULL, NameOfCounty = NULL, OrganizationCategory = NULL, PostalCode = NULL, SchoolYearID = NULL, StateAbbreviation = NULL, StreetNumberName = NULL, TelephoneNumber = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "LocalEducationAgencyDashboardV1", body = list(DataObject = body), searchFields = append("LocalEducationAgencyDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a LocalEducationAgencyDashboardV1
	#'
	#' This function modifies a LocalEducationAgencyDashboardV1
	#' @param fieldNames The field values to give the modified LocalEducationAgencyDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified LocalEducationAgencyDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyLocalEducationAgencyDashboardV1 <- function(LocalEducationAgencyDashboardV1ID, AddressType = NULL, ApartmentRoomSuiteNumber = NULL, BuildingSiteNumber = NULL, City = NULL, CountryCode = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, DistrictID = NULL, DistrictName = NULL, EdFiCountryID = NULL, EdFiStateAbbreviationID = NULL, EducationServiceCenter = NULL, FaxNumber = NULL, LEACategory = NULL, NameOfCounty = NULL, OrganizationCategory = NULL, PostalCode = NULL, SchoolYearID = NULL, StateAbbreviation = NULL, StreetNumberName = NULL, TelephoneNumber = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "LocalEducationAgencyDashboardV1", objectId = LocalEducationAgencyDashboardV1ID, body = list(DataObject = body), searchFields = append("LocalEducationAgencyDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List LocationDashboardV1s
	#'
	#' This function returns a dataframe or json object of LocationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given LocationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the LocationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('LocationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of LocationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listLocationDashboardV1s <- function(searchConditionsList = NULL, ClassroomIdentificationCode = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, KeyHash = F, LocationDashboardV1ID = F, MaximumNumberOfSeats = F, ModifiedTime = F, OptimalNumberOfSeats = F, SchoolID = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "LocationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a LocationDashboardV1
	#'
	#' This function returns a dataframe or json object of a LocationDashboardV1
	#' @param LocationDashboardV1ID The ID of the LocationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given LocationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the LocationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('LocationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of LocationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getLocationDashboardV1 <- function(LocationDashboardV1ID, ClassroomIdentificationCode = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, KeyHash = F, MaximumNumberOfSeats = F, ModifiedTime = F, OptimalNumberOfSeats = F, SchoolID = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "LocationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "LocationDashboardV1", objectId = LocationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a LocationDashboardV1
	#'
	#' This function deletes a LocationDashboardV1
	#' @param LocationDashboardV1ID The ID of the LocationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The LocationDashboardV1ID of the deleted LocationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteLocationDashboardV1 <- function(LocationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "LocationDashboardV1", objectId = LocationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a LocationDashboardV1
	#'
	#' This function creates a LocationDashboardV1
	#' @param fieldNames The field values to give the created LocationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created LocationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createLocationDashboardV1 <- function(ClassroomIdentificationCode = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, MaximumNumberOfSeats = NULL, OptimalNumberOfSeats = NULL, SchoolID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "LocationDashboardV1", body = list(DataObject = body), searchFields = append("LocationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a LocationDashboardV1
	#'
	#' This function modifies a LocationDashboardV1
	#' @param fieldNames The field values to give the modified LocationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified LocationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyLocationDashboardV1 <- function(LocationDashboardV1ID, ClassroomIdentificationCode = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, MaximumNumberOfSeats = NULL, OptimalNumberOfSeats = NULL, SchoolID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "LocationDashboardV1", objectId = LocationDashboardV1ID, body = list(DataObject = body), searchFields = append("LocationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ParentDashboardV1s
	#'
	#' This function returns a dataframe or json object of ParentDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ParentDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ParentDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ParentDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ParentDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listParentDashboardV1s <- function(searchConditionsList = NULL, ApartmentNumberHome = F, ApartmentNumberMailing = F, BuildingNumberHome = F, BuildingNumberMailing = F, CityHome = F, CityMailing = F, CountyHome = F, CountyMailing = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EdFiGenerationSuffixID = F, EdFiSexID = F, EdFiStateAbbreviationIDHome = F, EdFiStateAbbreviationIDMailing = F, EmailAddress = F, FirstName = F, GenerationSuffixCode = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, NameID = F, ParentDashboardV1ID = F, ParentUniqueID = F, PostalCodeHome = F, PostalCodeMailing = F, SchoolYearID = F, SexCode = F, StateHome = F, StateMailing = F, StreetNumberNameHome = F, StreetNumberNameMailing = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ParentDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ParentDashboardV1
	#'
	#' This function returns a dataframe or json object of a ParentDashboardV1
	#' @param ParentDashboardV1ID The ID of the ParentDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ParentDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ParentDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ParentDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ParentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getParentDashboardV1 <- function(ParentDashboardV1ID, ApartmentNumberHome = F, ApartmentNumberMailing = F, BuildingNumberHome = F, BuildingNumberMailing = F, CityHome = F, CityMailing = F, CountyHome = F, CountyMailing = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EdFiGenerationSuffixID = F, EdFiSexID = F, EdFiStateAbbreviationIDHome = F, EdFiStateAbbreviationIDMailing = F, EmailAddress = F, FirstName = F, GenerationSuffixCode = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, NameID = F, ParentUniqueID = F, PostalCodeHome = F, PostalCodeMailing = F, SchoolYearID = F, SexCode = F, StateHome = F, StateMailing = F, StreetNumberNameHome = F, StreetNumberNameMailing = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ParentDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ParentDashboardV1", objectId = ParentDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ParentDashboardV1
	#'
	#' This function deletes a ParentDashboardV1
	#' @param ParentDashboardV1ID The ID of the ParentDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ParentDashboardV1ID of the deleted ParentDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteParentDashboardV1 <- function(ParentDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ParentDashboardV1", objectId = ParentDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ParentDashboardV1
	#'
	#' This function creates a ParentDashboardV1
	#' @param fieldNames The field values to give the created ParentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ParentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createParentDashboardV1 <- function(ApartmentNumberHome = NULL, ApartmentNumberMailing = NULL, BuildingNumberHome = NULL, BuildingNumberMailing = NULL, CityHome = NULL, CityMailing = NULL, CountyHome = NULL, CountyMailing = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, EdFiGenerationSuffixID = NULL, EdFiSexID = NULL, EdFiStateAbbreviationIDHome = NULL, EdFiStateAbbreviationIDMailing = NULL, EmailAddress = NULL, FirstName = NULL, GenerationSuffixCode = NULL, LastName = NULL, MiddleName = NULL, NameID = NULL, ParentUniqueID = NULL, PostalCodeHome = NULL, PostalCodeMailing = NULL, SchoolYearID = NULL, SexCode = NULL, StateHome = NULL, StateMailing = NULL, StreetNumberNameHome = NULL, StreetNumberNameMailing = NULL, TelephoneNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ParentDashboardV1", body = list(DataObject = body), searchFields = append("ParentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ParentDashboardV1
	#'
	#' This function modifies a ParentDashboardV1
	#' @param fieldNames The field values to give the modified ParentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ParentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyParentDashboardV1 <- function(ParentDashboardV1ID, ApartmentNumberHome = NULL, ApartmentNumberMailing = NULL, BuildingNumberHome = NULL, BuildingNumberMailing = NULL, CityHome = NULL, CityMailing = NULL, CountyHome = NULL, CountyMailing = NULL, DashboardRunHistoryID = NULL, DistrictID = NULL, EdFiGenerationSuffixID = NULL, EdFiSexID = NULL, EdFiStateAbbreviationIDHome = NULL, EdFiStateAbbreviationIDMailing = NULL, EmailAddress = NULL, FirstName = NULL, GenerationSuffixCode = NULL, LastName = NULL, MiddleName = NULL, NameID = NULL, ParentUniqueID = NULL, PostalCodeHome = NULL, PostalCodeMailing = NULL, SchoolYearID = NULL, SexCode = NULL, StateHome = NULL, StateMailing = NULL, StreetNumberNameHome = NULL, StreetNumberNameMailing = NULL, TelephoneNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ParentDashboardV1", objectId = ParentDashboardV1ID, body = list(DataObject = body), searchFields = append("ParentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of ProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listProgramDashboardV1s <- function(searchConditionsList = NULL, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, KeyHash = F, ModifiedTime = F, ProgramDashboardV1ID = F, ProgramType = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a ProgramDashboardV1
	#' @param ProgramDashboardV1ID The ID of the ProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getProgramDashboardV1 <- function(ProgramDashboardV1ID, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, KeyHash = F, ModifiedTime = F, ProgramType = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ProgramDashboardV1", objectId = ProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ProgramDashboardV1
	#'
	#' This function deletes a ProgramDashboardV1
	#' @param ProgramDashboardV1ID The ID of the ProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ProgramDashboardV1ID of the deleted ProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteProgramDashboardV1 <- function(ProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ProgramDashboardV1", objectId = ProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ProgramDashboardV1
	#'
	#' This function creates a ProgramDashboardV1
	#' @param fieldNames The field values to give the created ProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createProgramDashboardV1 <- function(DashboardRunHistoryID = NULL, DistrictID = NULL, ProgramType = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ProgramDashboardV1", body = list(DataObject = body), searchFields = append("ProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ProgramDashboardV1
	#'
	#' This function modifies a ProgramDashboardV1
	#' @param fieldNames The field values to give the modified ProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyProgramDashboardV1 <- function(ProgramDashboardV1ID, DashboardRunHistoryID = NULL, DistrictID = NULL, ProgramType = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ProgramDashboardV1", objectId = ProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("ProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchoolExtensionDashboardV1s
	#'
	#' This function returns a dataframe or json object of SchoolExtensionDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchoolExtensionDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchoolExtensionDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchoolExtensionDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of SchoolExtensionDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchoolExtensionDashboardV1s <- function(searchConditionsList = NULL, AddressType = F, ApartmentRoomSuiteNumber = F, BuildingSiteNumber = F, City = F, ClassPeriodsOffered = F, CountryCode = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiCountryID = F, EdFiSchoolCategoryID = F, EdFiStateAbbreviationID = F, EntityID = F, GradesOffered = F, KeyHash = F, ModifiedTime = F, NameOfCounty = F, OrganizationCategory = F, PostalCode = F, SchoolCategoryCode = F, SchoolCode = F, SchoolExtensionDashboardV1ID = F, SchoolID = F, SchoolName = F, SchoolYearID = F, StateAbbreviation = F, StateDistrictCode = F, StreetNumberName = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Website = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "SchoolExtensionDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchoolExtensionDashboardV1
	#'
	#' This function returns a dataframe or json object of a SchoolExtensionDashboardV1
	#' @param SchoolExtensionDashboardV1ID The ID of the SchoolExtensionDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchoolExtensionDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchoolExtensionDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchoolExtensionDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of SchoolExtensionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchoolExtensionDashboardV1 <- function(SchoolExtensionDashboardV1ID, AddressType = F, ApartmentRoomSuiteNumber = F, BuildingSiteNumber = F, City = F, ClassPeriodsOffered = F, CountryCode = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiCountryID = F, EdFiSchoolCategoryID = F, EdFiStateAbbreviationID = F, EntityID = F, GradesOffered = F, KeyHash = F, ModifiedTime = F, NameOfCounty = F, OrganizationCategory = F, PostalCode = F, SchoolCategoryCode = F, SchoolCode = F, SchoolID = F, SchoolName = F, SchoolYearID = F, StateAbbreviation = F, StateDistrictCode = F, StreetNumberName = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Website = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchoolExtensionDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "SchoolExtensionDashboardV1", objectId = SchoolExtensionDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchoolExtensionDashboardV1
	#'
	#' This function deletes a SchoolExtensionDashboardV1
	#' @param SchoolExtensionDashboardV1ID The ID of the SchoolExtensionDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The SchoolExtensionDashboardV1ID of the deleted SchoolExtensionDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchoolExtensionDashboardV1 <- function(SchoolExtensionDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "SchoolExtensionDashboardV1", objectId = SchoolExtensionDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchoolExtensionDashboardV1
	#'
	#' This function creates a SchoolExtensionDashboardV1
	#' @param fieldNames The field values to give the created SchoolExtensionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created SchoolExtensionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchoolExtensionDashboardV1 <- function(AddressType = NULL, ApartmentRoomSuiteNumber = NULL, BuildingSiteNumber = NULL, City = NULL, ClassPeriodsOffered = NULL, CountryCode = NULL, DashboardRunHistoryID = NULL, EdFiCountryID = NULL, EdFiSchoolCategoryID = NULL, EdFiStateAbbreviationID = NULL, EntityID = NULL, GradesOffered = NULL, NameOfCounty = NULL, OrganizationCategory = NULL, PostalCode = NULL, SchoolCategoryCode = NULL, SchoolCode = NULL, SchoolID = NULL, SchoolName = NULL, SchoolYearID = NULL, StateAbbreviation = NULL, StateDistrictCode = NULL, StreetNumberName = NULL, TelephoneNumber = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "SchoolExtensionDashboardV1", body = list(DataObject = body), searchFields = append("SchoolExtensionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchoolExtensionDashboardV1
	#'
	#' This function modifies a SchoolExtensionDashboardV1
	#' @param fieldNames The field values to give the modified SchoolExtensionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified SchoolExtensionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchoolExtensionDashboardV1 <- function(SchoolExtensionDashboardV1ID, AddressType = NULL, ApartmentRoomSuiteNumber = NULL, BuildingSiteNumber = NULL, City = NULL, ClassPeriodsOffered = NULL, CountryCode = NULL, DashboardRunHistoryID = NULL, EdFiCountryID = NULL, EdFiSchoolCategoryID = NULL, EdFiStateAbbreviationID = NULL, EntityID = NULL, GradesOffered = NULL, NameOfCounty = NULL, OrganizationCategory = NULL, PostalCode = NULL, SchoolCategoryCode = NULL, SchoolCode = NULL, SchoolID = NULL, SchoolName = NULL, SchoolYearID = NULL, StateAbbreviation = NULL, StateDistrictCode = NULL, StreetNumberName = NULL, TelephoneNumber = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "SchoolExtensionDashboardV1", objectId = SchoolExtensionDashboardV1ID, body = list(DataObject = body), searchFields = append("SchoolExtensionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionDashboardV1s
	#'
	#' This function returns a dataframe or json object of SectionDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of SectionDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionDashboardV1s <- function(searchConditionsList = NULL, AvailableCredit = F, ClassPeriodName = F, ClassroomIdentificationCode = F, CreatedTime = F, DashboardRunHistoryID = F, DisplayPeriodID = F, EdFiEducationalEnvironmentID = F, EdFiTermTypeID = F, EducationalEnvironmentCode = F, EntityID = F, KeyHash = F, LocalCourseCode = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SectionDashboardV1ID = F, SectionID = F, SequenceOfCourse = F, TermCode = F, UniqueSectionCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "SectionDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionDashboardV1
	#'
	#' This function returns a dataframe or json object of a SectionDashboardV1
	#' @param SectionDashboardV1ID The ID of the SectionDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of SectionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionDashboardV1 <- function(SectionDashboardV1ID, AvailableCredit = F, ClassPeriodName = F, ClassroomIdentificationCode = F, CreatedTime = F, DashboardRunHistoryID = F, DisplayPeriodID = F, EdFiEducationalEnvironmentID = F, EdFiTermTypeID = F, EducationalEnvironmentCode = F, EntityID = F, KeyHash = F, LocalCourseCode = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SectionID = F, SequenceOfCourse = F, TermCode = F, UniqueSectionCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "SectionDashboardV1", objectId = SectionDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionDashboardV1
	#'
	#' This function deletes a SectionDashboardV1
	#' @param SectionDashboardV1ID The ID of the SectionDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The SectionDashboardV1ID of the deleted SectionDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionDashboardV1 <- function(SectionDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "SectionDashboardV1", objectId = SectionDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionDashboardV1
	#'
	#' This function creates a SectionDashboardV1
	#' @param fieldNames The field values to give the created SectionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created SectionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionDashboardV1 <- function(AvailableCredit = NULL, ClassPeriodName = NULL, ClassroomIdentificationCode = NULL, DashboardRunHistoryID = NULL, DisplayPeriodID = NULL, EdFiEducationalEnvironmentID = NULL, EdFiTermTypeID = NULL, EducationalEnvironmentCode = NULL, EntityID = NULL, LocalCourseCode = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SectionID = NULL, SequenceOfCourse = NULL, TermCode = NULL, UniqueSectionCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "SectionDashboardV1", body = list(DataObject = body), searchFields = append("SectionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionDashboardV1
	#'
	#' This function modifies a SectionDashboardV1
	#' @param fieldNames The field values to give the modified SectionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified SectionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionDashboardV1 <- function(SectionDashboardV1ID, AvailableCredit = NULL, ClassPeriodName = NULL, ClassroomIdentificationCode = NULL, DashboardRunHistoryID = NULL, DisplayPeriodID = NULL, EdFiEducationalEnvironmentID = NULL, EdFiTermTypeID = NULL, EducationalEnvironmentCode = NULL, EntityID = NULL, LocalCourseCode = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SectionID = NULL, SequenceOfCourse = NULL, TermCode = NULL, UniqueSectionCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "SectionDashboardV1", objectId = SectionDashboardV1ID, body = list(DataObject = body), searchFields = append("SectionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SessionDashboardV1s
	#'
	#' This function returns a dataframe or json object of SessionDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SessionDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SessionDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SessionDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of SessionDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSessionDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiTermTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SessionDashboardV1ID = F, SessionName = F, TermCode = F, TotalInstructionalDays = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "SessionDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SessionDashboardV1
	#'
	#' This function returns a dataframe or json object of a SessionDashboardV1
	#' @param SessionDashboardV1ID The ID of the SessionDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SessionDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SessionDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SessionDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of SessionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSessionDashboardV1 <- function(SessionDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiTermTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SessionName = F, TermCode = F, TotalInstructionalDays = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SessionDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "SessionDashboardV1", objectId = SessionDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SessionDashboardV1
	#'
	#' This function deletes a SessionDashboardV1
	#' @param SessionDashboardV1ID The ID of the SessionDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The SessionDashboardV1ID of the deleted SessionDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSessionDashboardV1 <- function(SessionDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "SessionDashboardV1", objectId = SessionDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SessionDashboardV1
	#'
	#' This function creates a SessionDashboardV1
	#' @param fieldNames The field values to give the created SessionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created SessionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSessionDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SessionName = NULL, TermCode = NULL, TotalInstructionalDays = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "SessionDashboardV1", body = list(DataObject = body), searchFields = append("SessionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SessionDashboardV1
	#'
	#' This function modifies a SessionDashboardV1
	#' @param fieldNames The field values to give the modified SessionDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified SessionDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySessionDashboardV1 <- function(SessionDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityID = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SessionName = NULL, TermCode = NULL, TotalInstructionalDays = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "SessionDashboardV1", objectId = SessionDashboardV1ID, body = list(DataObject = body), searchFields = append("SessionDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StaffCohortAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of StaffCohortAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffCohortAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffCohortAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffCohortAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StaffCohortAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStaffCohortAssociationDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CohortIdentifier = F, CreatedTime = F, DashboardRunHistoryID = F, EndDate = F, EntityID = F, HasStudentRecordAccess = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StaffCohortAssociationDashboardV1ID = F, StaffID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StaffCohortAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StaffCohortAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a StaffCohortAssociationDashboardV1
	#' @param StaffCohortAssociationDashboardV1ID The ID of the StaffCohortAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffCohortAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffCohortAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffCohortAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StaffCohortAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStaffCohortAssociationDashboardV1 <- function(StaffCohortAssociationDashboardV1ID, BeginDate = F, CohortIdentifier = F, CreatedTime = F, DashboardRunHistoryID = F, EndDate = F, EntityID = F, HasStudentRecordAccess = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StaffID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StaffCohortAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StaffCohortAssociationDashboardV1", objectId = StaffCohortAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StaffCohortAssociationDashboardV1
	#'
	#' This function deletes a StaffCohortAssociationDashboardV1
	#' @param StaffCohortAssociationDashboardV1ID The ID of the StaffCohortAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StaffCohortAssociationDashboardV1ID of the deleted StaffCohortAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStaffCohortAssociationDashboardV1 <- function(StaffCohortAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StaffCohortAssociationDashboardV1", objectId = StaffCohortAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StaffCohortAssociationDashboardV1
	#'
	#' This function creates a StaffCohortAssociationDashboardV1
	#' @param fieldNames The field values to give the created StaffCohortAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StaffCohortAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStaffCohortAssociationDashboardV1 <- function(BeginDate = NULL, CohortIdentifier = NULL, DashboardRunHistoryID = NULL, EndDate = NULL, EntityID = NULL, HasStudentRecordAccess = NULL, SchoolYearID = NULL, StaffID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StaffCohortAssociationDashboardV1", body = list(DataObject = body), searchFields = append("StaffCohortAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StaffCohortAssociationDashboardV1
	#'
	#' This function modifies a StaffCohortAssociationDashboardV1
	#' @param fieldNames The field values to give the modified StaffCohortAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StaffCohortAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStaffCohortAssociationDashboardV1 <- function(StaffCohortAssociationDashboardV1ID, BeginDate = NULL, CohortIdentifier = NULL, DashboardRunHistoryID = NULL, EndDate = NULL, EntityID = NULL, HasStudentRecordAccess = NULL, SchoolYearID = NULL, StaffID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StaffCohortAssociationDashboardV1", objectId = StaffCohortAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("StaffCohortAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StaffEdOrgAssignmentDashboardV1s
	#'
	#' This function returns a dataframe or json object of StaffEdOrgAssignmentDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffEdOrgAssignmentDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffEdOrgAssignmentDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffEdOrgAssignmentDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StaffEdOrgAssignmentDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStaffEdOrgAssignmentDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiStaffClassificationID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, PositionTitle = F, SchoolCode = F, SchoolYearID = F, StaffClassificationCode = F, StaffEdOrgAssignmentDashboardV1ID = F, StaffID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StaffEdOrgAssignmentDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StaffEdOrgAssignmentDashboardV1
	#'
	#' This function returns a dataframe or json object of a StaffEdOrgAssignmentDashboardV1
	#' @param StaffEdOrgAssignmentDashboardV1ID The ID of the StaffEdOrgAssignmentDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffEdOrgAssignmentDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffEdOrgAssignmentDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffEdOrgAssignmentDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StaffEdOrgAssignmentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStaffEdOrgAssignmentDashboardV1 <- function(StaffEdOrgAssignmentDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiStaffClassificationID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, PositionTitle = F, SchoolCode = F, SchoolYearID = F, StaffClassificationCode = F, StaffID = F, StaffUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StaffEdOrgAssignmentDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StaffEdOrgAssignmentDashboardV1", objectId = StaffEdOrgAssignmentDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StaffEdOrgAssignmentDashboardV1
	#'
	#' This function deletes a StaffEdOrgAssignmentDashboardV1
	#' @param StaffEdOrgAssignmentDashboardV1ID The ID of the StaffEdOrgAssignmentDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StaffEdOrgAssignmentDashboardV1ID of the deleted StaffEdOrgAssignmentDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStaffEdOrgAssignmentDashboardV1 <- function(StaffEdOrgAssignmentDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StaffEdOrgAssignmentDashboardV1", objectId = StaffEdOrgAssignmentDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StaffEdOrgAssignmentDashboardV1
	#'
	#' This function creates a StaffEdOrgAssignmentDashboardV1
	#' @param fieldNames The field values to give the created StaffEdOrgAssignmentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StaffEdOrgAssignmentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStaffEdOrgAssignmentDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiStaffClassificationID = NULL, EndDate = NULL, EntityID = NULL, PositionTitle = NULL, SchoolCode = NULL, SchoolYearID = NULL, StaffClassificationCode = NULL, StaffID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StaffEdOrgAssignmentDashboardV1", body = list(DataObject = body), searchFields = append("StaffEdOrgAssignmentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StaffEdOrgAssignmentDashboardV1
	#'
	#' This function modifies a StaffEdOrgAssignmentDashboardV1
	#' @param fieldNames The field values to give the modified StaffEdOrgAssignmentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StaffEdOrgAssignmentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStaffEdOrgAssignmentDashboardV1 <- function(StaffEdOrgAssignmentDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiStaffClassificationID = NULL, EndDate = NULL, EntityID = NULL, PositionTitle = NULL, SchoolCode = NULL, SchoolYearID = NULL, StaffClassificationCode = NULL, StaffID = NULL, StaffUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StaffEdOrgAssignmentDashboardV1", objectId = StaffEdOrgAssignmentDashboardV1ID, body = list(DataObject = body), searchFields = append("StaffEdOrgAssignmentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAcademicDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentAcademicDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAcademicDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAcademicDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAcademicDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentAcademicDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAcademicDashboardV1s <- function(searchConditionsList = NULL, CreatedTime = F, CumulativeCreditsAttempted = F, CumulativeCreditsEarned = F, CumulativeGPA = F, CumulativeGradePointsEarned = F, DashboardRunHistoryID = F, DistrictCode = F, DistrictID = F, EdFiTermTypeID = F, KeyHash = F, ModifiedTime = F, ProjectedGraduationDate = F, SchoolYearDescription = F, SchoolYearID = F, StudentAcademicDashboardV1ID = F, StudentID = F, StudentUniqueID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentAcademicDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAcademicDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentAcademicDashboardV1
	#' @param StudentAcademicDashboardV1ID The ID of the StudentAcademicDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAcademicDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAcademicDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAcademicDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentAcademicDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAcademicDashboardV1 <- function(StudentAcademicDashboardV1ID, CreatedTime = F, CumulativeCreditsAttempted = F, CumulativeCreditsEarned = F, CumulativeGPA = F, CumulativeGradePointsEarned = F, DashboardRunHistoryID = F, DistrictCode = F, DistrictID = F, EdFiTermTypeID = F, KeyHash = F, ModifiedTime = F, ProjectedGraduationDate = F, SchoolYearDescription = F, SchoolYearID = F, StudentID = F, StudentUniqueID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAcademicDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentAcademicDashboardV1", objectId = StudentAcademicDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAcademicDashboardV1
	#'
	#' This function deletes a StudentAcademicDashboardV1
	#' @param StudentAcademicDashboardV1ID The ID of the StudentAcademicDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentAcademicDashboardV1ID of the deleted StudentAcademicDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAcademicDashboardV1 <- function(StudentAcademicDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentAcademicDashboardV1", objectId = StudentAcademicDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAcademicDashboardV1
	#'
	#' This function creates a StudentAcademicDashboardV1
	#' @param fieldNames The field values to give the created StudentAcademicDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentAcademicDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAcademicDashboardV1 <- function(CumulativeCreditsAttempted = NULL, CumulativeCreditsEarned = NULL, CumulativeGPA = NULL, CumulativeGradePointsEarned = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, DistrictID = NULL, EdFiTermTypeID = NULL, ProjectedGraduationDate = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentAcademicDashboardV1", body = list(DataObject = body), searchFields = append("StudentAcademicDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAcademicDashboardV1
	#'
	#' This function modifies a StudentAcademicDashboardV1
	#' @param fieldNames The field values to give the modified StudentAcademicDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentAcademicDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAcademicDashboardV1 <- function(StudentAcademicDashboardV1ID, CumulativeCreditsAttempted = NULL, CumulativeCreditsEarned = NULL, CumulativeGPA = NULL, CumulativeGradePointsEarned = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, DistrictID = NULL, EdFiTermTypeID = NULL, ProjectedGraduationDate = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentAcademicDashboardV1", objectId = StudentAcademicDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentAcademicDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentCohortAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentCohortAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCohortAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCohortAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCohortAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentCohortAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentCohortAssociationDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CohortIdentifier = F, CreatedTime = F, DashboardRunHistoryID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StudentCohortAssociationDashboardV1ID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentCohortAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentCohortAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentCohortAssociationDashboardV1
	#' @param StudentCohortAssociationDashboardV1ID The ID of the StudentCohortAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCohortAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCohortAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCohortAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentCohortAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentCohortAssociationDashboardV1 <- function(StudentCohortAssociationDashboardV1ID, BeginDate = F, CohortIdentifier = F, CreatedTime = F, DashboardRunHistoryID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentCohortAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentCohortAssociationDashboardV1", objectId = StudentCohortAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentCohortAssociationDashboardV1
	#'
	#' This function deletes a StudentCohortAssociationDashboardV1
	#' @param StudentCohortAssociationDashboardV1ID The ID of the StudentCohortAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentCohortAssociationDashboardV1ID of the deleted StudentCohortAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentCohortAssociationDashboardV1 <- function(StudentCohortAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentCohortAssociationDashboardV1", objectId = StudentCohortAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentCohortAssociationDashboardV1
	#'
	#' This function creates a StudentCohortAssociationDashboardV1
	#' @param fieldNames The field values to give the created StudentCohortAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentCohortAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentCohortAssociationDashboardV1 <- function(BeginDate = NULL, CohortIdentifier = NULL, DashboardRunHistoryID = NULL, EndDate = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentCohortAssociationDashboardV1", body = list(DataObject = body), searchFields = append("StudentCohortAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentCohortAssociationDashboardV1
	#'
	#' This function modifies a StudentCohortAssociationDashboardV1
	#' @param fieldNames The field values to give the modified StudentCohortAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentCohortAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentCohortAssociationDashboardV1 <- function(StudentCohortAssociationDashboardV1ID, BeginDate = NULL, CohortIdentifier = NULL, DashboardRunHistoryID = NULL, EndDate = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentCohortAssociationDashboardV1", objectId = StudentCohortAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentCohortAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentDashboardV1s <- function(searchConditionsList = NULL, ApartmentNumberHome = F, ApartmentNumberMailing = F, BirthCity = F, BirthCountryCode = F, BirthDate = F, BirthStateCode = F, BuildingNumberHome = F, BuildingNumberMailing = F, CityHome = F, CityMailing = F, CountyHome = F, CountyMailing = F, CreatedTime = F, DashboardRunHistoryID = F, DisabilityTypeCode = F, DistrictCode = F, EdFiCountryID = F, EdFiDisabilityTypeID = F, EdFiGenerationSuffixID = F, EdFiLanguageID = F, EdFiLanguageIDHome = F, EdFiLimitedEnglishProficiencyID = F, EdFiSchoolFoodServicesEligibilityID = F, EdFiSexID = F, EdFiStateAbbreviationIDBirth = F, EdFiStateAbbreviationIDHome = F, EdFiStateAbbreviationIDMailing = F, EmailAddress = F, EntityID = F, EntryWithdrawalID = F, FirstName = F, GenerationSuffixCode = F, HomeLanguageCode = F, IsAlaskan = F, IsAsian = F, IsAtRisk = F, IsBlack = F, IsDisplacedHomemaker = F, IsEconomicDisadvantaged = F, IsHawaiian = F, IsHispanic = F, IsHomeless = F, IsImmigrant = F, IsMigrant = F, IsPregnantTeen = F, IsSingleParent = F, IsWhite = F, KeyHash = F, LanguageCode = F, LastName = F, LimitedEnglishProficiencyCode = F, LocalDistrictStudentID = F, LocalStateStudentID = F, MiddleName = F, ModifiedTime = F, PostalCodeHome = F, PostalCodeMailing = F, SchoolFoodServicesEligibilityCode = F, SchoolYearID = F, SexCode = F, StateCodeHome = F, StateCodeMailing = F, StreetNumberNameHome = F, StreetNumberNameMailing = F, StudentDashboardV1ID = F, StudentID = F, StudentUniqueID = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentDashboardV1
	#' @param StudentDashboardV1ID The ID of the StudentDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentDashboardV1 <- function(StudentDashboardV1ID, ApartmentNumberHome = F, ApartmentNumberMailing = F, BirthCity = F, BirthCountryCode = F, BirthDate = F, BirthStateCode = F, BuildingNumberHome = F, BuildingNumberMailing = F, CityHome = F, CityMailing = F, CountyHome = F, CountyMailing = F, CreatedTime = F, DashboardRunHistoryID = F, DisabilityTypeCode = F, DistrictCode = F, EdFiCountryID = F, EdFiDisabilityTypeID = F, EdFiGenerationSuffixID = F, EdFiLanguageID = F, EdFiLanguageIDHome = F, EdFiLimitedEnglishProficiencyID = F, EdFiSchoolFoodServicesEligibilityID = F, EdFiSexID = F, EdFiStateAbbreviationIDBirth = F, EdFiStateAbbreviationIDHome = F, EdFiStateAbbreviationIDMailing = F, EmailAddress = F, EntityID = F, EntryWithdrawalID = F, FirstName = F, GenerationSuffixCode = F, HomeLanguageCode = F, IsAlaskan = F, IsAsian = F, IsAtRisk = F, IsBlack = F, IsDisplacedHomemaker = F, IsEconomicDisadvantaged = F, IsHawaiian = F, IsHispanic = F, IsHomeless = F, IsImmigrant = F, IsMigrant = F, IsPregnantTeen = F, IsSingleParent = F, IsWhite = F, KeyHash = F, LanguageCode = F, LastName = F, LimitedEnglishProficiencyCode = F, LocalDistrictStudentID = F, LocalStateStudentID = F, MiddleName = F, ModifiedTime = F, PostalCodeHome = F, PostalCodeMailing = F, SchoolFoodServicesEligibilityCode = F, SchoolYearID = F, SexCode = F, StateCodeHome = F, StateCodeMailing = F, StreetNumberNameHome = F, StreetNumberNameMailing = F, StudentID = F, StudentUniqueID = F, TelephoneNumber = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentDashboardV1", objectId = StudentDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentDashboardV1
	#'
	#' This function deletes a StudentDashboardV1
	#' @param StudentDashboardV1ID The ID of the StudentDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentDashboardV1ID of the deleted StudentDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentDashboardV1 <- function(StudentDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentDashboardV1", objectId = StudentDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentDashboardV1
	#'
	#' This function creates a StudentDashboardV1
	#' @param fieldNames The field values to give the created StudentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentDashboardV1 <- function(ApartmentNumberHome = NULL, ApartmentNumberMailing = NULL, BirthCity = NULL, BirthCountryCode = NULL, BirthDate = NULL, BirthStateCode = NULL, BuildingNumberHome = NULL, BuildingNumberMailing = NULL, CityHome = NULL, CityMailing = NULL, CountyHome = NULL, CountyMailing = NULL, DashboardRunHistoryID = NULL, DisabilityTypeCode = NULL, DistrictCode = NULL, EdFiCountryID = NULL, EdFiDisabilityTypeID = NULL, EdFiGenerationSuffixID = NULL, EdFiLanguageID = NULL, EdFiLanguageIDHome = NULL, EdFiLimitedEnglishProficiencyID = NULL, EdFiSchoolFoodServicesEligibilityID = NULL, EdFiSexID = NULL, EdFiStateAbbreviationIDBirth = NULL, EdFiStateAbbreviationIDHome = NULL, EdFiStateAbbreviationIDMailing = NULL, EmailAddress = NULL, EntityID = NULL, EntryWithdrawalID = NULL, FirstName = NULL, GenerationSuffixCode = NULL, HomeLanguageCode = NULL, IsAlaskan = NULL, IsAsian = NULL, IsAtRisk = NULL, IsBlack = NULL, IsDisplacedHomemaker = NULL, IsEconomicDisadvantaged = NULL, IsHawaiian = NULL, IsHispanic = NULL, IsHomeless = NULL, IsImmigrant = NULL, IsMigrant = NULL, IsPregnantTeen = NULL, IsSingleParent = NULL, IsWhite = NULL, LanguageCode = NULL, LastName = NULL, LimitedEnglishProficiencyCode = NULL, LocalDistrictStudentID = NULL, LocalStateStudentID = NULL, MiddleName = NULL, PostalCodeHome = NULL, PostalCodeMailing = NULL, SchoolFoodServicesEligibilityCode = NULL, SchoolYearID = NULL, SexCode = NULL, StateCodeHome = NULL, StateCodeMailing = NULL, StreetNumberNameHome = NULL, StreetNumberNameMailing = NULL, StudentID = NULL, StudentUniqueID = NULL, TelephoneNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentDashboardV1", body = list(DataObject = body), searchFields = append("StudentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentDashboardV1
	#'
	#' This function modifies a StudentDashboardV1
	#' @param fieldNames The field values to give the modified StudentDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentDashboardV1 <- function(StudentDashboardV1ID, ApartmentNumberHome = NULL, ApartmentNumberMailing = NULL, BirthCity = NULL, BirthCountryCode = NULL, BirthDate = NULL, BirthStateCode = NULL, BuildingNumberHome = NULL, BuildingNumberMailing = NULL, CityHome = NULL, CityMailing = NULL, CountyHome = NULL, CountyMailing = NULL, DashboardRunHistoryID = NULL, DisabilityTypeCode = NULL, DistrictCode = NULL, EdFiCountryID = NULL, EdFiDisabilityTypeID = NULL, EdFiGenerationSuffixID = NULL, EdFiLanguageID = NULL, EdFiLanguageIDHome = NULL, EdFiLimitedEnglishProficiencyID = NULL, EdFiSchoolFoodServicesEligibilityID = NULL, EdFiSexID = NULL, EdFiStateAbbreviationIDBirth = NULL, EdFiStateAbbreviationIDHome = NULL, EdFiStateAbbreviationIDMailing = NULL, EmailAddress = NULL, EntityID = NULL, EntryWithdrawalID = NULL, FirstName = NULL, GenerationSuffixCode = NULL, HomeLanguageCode = NULL, IsAlaskan = NULL, IsAsian = NULL, IsAtRisk = NULL, IsBlack = NULL, IsDisplacedHomemaker = NULL, IsEconomicDisadvantaged = NULL, IsHawaiian = NULL, IsHispanic = NULL, IsHomeless = NULL, IsImmigrant = NULL, IsMigrant = NULL, IsPregnantTeen = NULL, IsSingleParent = NULL, IsWhite = NULL, LanguageCode = NULL, LastName = NULL, LimitedEnglishProficiencyCode = NULL, LocalDistrictStudentID = NULL, LocalStateStudentID = NULL, MiddleName = NULL, PostalCodeHome = NULL, PostalCodeMailing = NULL, SchoolFoodServicesEligibilityCode = NULL, SchoolYearID = NULL, SexCode = NULL, StateCodeHome = NULL, StateCodeMailing = NULL, StreetNumberNameHome = NULL, StreetNumberNameMailing = NULL, StudentID = NULL, StudentUniqueID = NULL, TelephoneNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentDashboardV1", objectId = StudentDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradeDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentGradeDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentGradeDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradeDashboardV1s <- function(searchConditionsList = NULL, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, DiagnosticStatement = F, EdFiGradeTypeID = F, EdFiGradingPeriodID = F, EdFiPerformanceBaseTypeID = F, EdFiTermTypeID = F, EntityID = F, GradeTypeCode = F, GradingPeriodCode = F, KeyHash = F, LetterGradeEarned = F, LocalCourseCode = F, Location = F, ModifiedTime = F, NumericGradeEarned = F, PerformanceBaseCode = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, StudentGradeDashboardV1ID = F, StudentID = F, StudentUniqueID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentGradeDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradeDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentGradeDashboardV1
	#' @param StudentGradeDashboardV1ID The ID of the StudentGradeDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentGradeDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradeDashboardV1 <- function(StudentGradeDashboardV1ID, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, DiagnosticStatement = F, EdFiGradeTypeID = F, EdFiGradingPeriodID = F, EdFiPerformanceBaseTypeID = F, EdFiTermTypeID = F, EntityID = F, GradeTypeCode = F, GradingPeriodCode = F, KeyHash = F, LetterGradeEarned = F, LocalCourseCode = F, Location = F, ModifiedTime = F, NumericGradeEarned = F, PerformanceBaseCode = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, StudentID = F, StudentUniqueID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradeDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentGradeDashboardV1", objectId = StudentGradeDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradeDashboardV1
	#'
	#' This function deletes a StudentGradeDashboardV1
	#' @param StudentGradeDashboardV1ID The ID of the StudentGradeDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentGradeDashboardV1ID of the deleted StudentGradeDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradeDashboardV1 <- function(StudentGradeDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentGradeDashboardV1", objectId = StudentGradeDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradeDashboardV1
	#'
	#' This function creates a StudentGradeDashboardV1
	#' @param fieldNames The field values to give the created StudentGradeDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentGradeDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradeDashboardV1 <- function(ClassPeriodName = NULL, DashboardRunHistoryID = NULL, DiagnosticStatement = NULL, EdFiGradeTypeID = NULL, EdFiGradingPeriodID = NULL, EdFiPerformanceBaseTypeID = NULL, EdFiTermTypeID = NULL, EntityID = NULL, GradeTypeCode = NULL, GradingPeriodCode = NULL, LetterGradeEarned = NULL, LocalCourseCode = NULL, Location = NULL, NumericGradeEarned = NULL, PerformanceBaseCode = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentGradeDashboardV1", body = list(DataObject = body), searchFields = append("StudentGradeDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradeDashboardV1
	#'
	#' This function modifies a StudentGradeDashboardV1
	#' @param fieldNames The field values to give the modified StudentGradeDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentGradeDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradeDashboardV1 <- function(StudentGradeDashboardV1ID, ClassPeriodName = NULL, DashboardRunHistoryID = NULL, DiagnosticStatement = NULL, EdFiGradeTypeID = NULL, EdFiGradingPeriodID = NULL, EdFiPerformanceBaseTypeID = NULL, EdFiTermTypeID = NULL, EntityID = NULL, GradeTypeCode = NULL, GradingPeriodCode = NULL, LetterGradeEarned = NULL, LocalCourseCode = NULL, Location = NULL, NumericGradeEarned = NULL, PerformanceBaseCode = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentGradeDashboardV1", objectId = StudentGradeDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentGradeDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentParentAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentParentAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentParentAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentParentAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentParentAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentParentAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentParentAssociationDashboardV1s <- function(searchConditionsList = NULL, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EdFiRelationTypeID = F, IsEmergencyContact = F, IsPrimaryContact = F, KeyHash = F, ModifiedTime = F, NameIDParent = F, RelationTypeCode = F, SchoolYearID = F, StudentID = F, StudentParentAssociationDashboardV1ID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentParentAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentParentAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentParentAssociationDashboardV1
	#' @param StudentParentAssociationDashboardV1ID The ID of the StudentParentAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentParentAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentParentAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentParentAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentParentAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentParentAssociationDashboardV1 <- function(StudentParentAssociationDashboardV1ID, CreatedTime = F, DashboardRunHistoryID = F, DistrictID = F, EdFiRelationTypeID = F, IsEmergencyContact = F, IsPrimaryContact = F, KeyHash = F, ModifiedTime = F, NameIDParent = F, RelationTypeCode = F, SchoolYearID = F, StudentID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentParentAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentParentAssociationDashboardV1", objectId = StudentParentAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentParentAssociationDashboardV1
	#'
	#' This function deletes a StudentParentAssociationDashboardV1
	#' @param StudentParentAssociationDashboardV1ID The ID of the StudentParentAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentParentAssociationDashboardV1ID of the deleted StudentParentAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentParentAssociationDashboardV1 <- function(StudentParentAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentParentAssociationDashboardV1", objectId = StudentParentAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentParentAssociationDashboardV1
	#'
	#' This function creates a StudentParentAssociationDashboardV1
	#' @param fieldNames The field values to give the created StudentParentAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentParentAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentParentAssociationDashboardV1 <- function(DashboardRunHistoryID = NULL, DistrictID = NULL, EdFiRelationTypeID = NULL, IsEmergencyContact = NULL, IsPrimaryContact = NULL, NameIDParent = NULL, RelationTypeCode = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentParentAssociationDashboardV1", body = list(DataObject = body), searchFields = append("StudentParentAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentParentAssociationDashboardV1
	#'
	#' This function modifies a StudentParentAssociationDashboardV1
	#' @param fieldNames The field values to give the modified StudentParentAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentParentAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentParentAssociationDashboardV1 <- function(StudentParentAssociationDashboardV1ID, DashboardRunHistoryID = NULL, DistrictID = NULL, EdFiRelationTypeID = NULL, IsEmergencyContact = NULL, IsPrimaryContact = NULL, NameIDParent = NULL, RelationTypeCode = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentParentAssociationDashboardV1", objectId = StudentParentAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentParentAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSchoolAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentSchoolAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSchoolAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSchoolAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSchoolAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentSchoolAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSchoolAssociationDashboardV1s <- function(searchConditionsList = NULL, ClassOf = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiExitWithdrawID = F, EdFiGradeLevelID = F, EntityID = F, EntryDate = F, ExitWithdrawDate = F, ExitWithdrawTypeCode = F, GradeReferenceID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, StateGradeLevelCode = F, StudentID = F, StudentSchoolAssociationDashboardV1ID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentSchoolAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSchoolAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentSchoolAssociationDashboardV1
	#' @param StudentSchoolAssociationDashboardV1ID The ID of the StudentSchoolAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSchoolAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSchoolAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSchoolAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentSchoolAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSchoolAssociationDashboardV1 <- function(StudentSchoolAssociationDashboardV1ID, ClassOf = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiExitWithdrawID = F, EdFiGradeLevelID = F, EntityID = F, EntryDate = F, ExitWithdrawDate = F, ExitWithdrawTypeCode = F, GradeReferenceID = F, KeyHash = F, ModifiedTime = F, SchoolCode = F, SchoolYearID = F, StateGradeLevelCode = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSchoolAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentSchoolAssociationDashboardV1", objectId = StudentSchoolAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSchoolAssociationDashboardV1
	#'
	#' This function deletes a StudentSchoolAssociationDashboardV1
	#' @param StudentSchoolAssociationDashboardV1ID The ID of the StudentSchoolAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentSchoolAssociationDashboardV1ID of the deleted StudentSchoolAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSchoolAssociationDashboardV1 <- function(StudentSchoolAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentSchoolAssociationDashboardV1", objectId = StudentSchoolAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSchoolAssociationDashboardV1
	#'
	#' This function creates a StudentSchoolAssociationDashboardV1
	#' @param fieldNames The field values to give the created StudentSchoolAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentSchoolAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSchoolAssociationDashboardV1 <- function(ClassOf = NULL, DashboardRunHistoryID = NULL, EdFiExitWithdrawID = NULL, EdFiGradeLevelID = NULL, EntityID = NULL, EntryDate = NULL, ExitWithdrawDate = NULL, ExitWithdrawTypeCode = NULL, GradeReferenceID = NULL, SchoolCode = NULL, SchoolYearID = NULL, StateGradeLevelCode = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentSchoolAssociationDashboardV1", body = list(DataObject = body), searchFields = append("StudentSchoolAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSchoolAssociationDashboardV1
	#'
	#' This function modifies a StudentSchoolAssociationDashboardV1
	#' @param fieldNames The field values to give the modified StudentSchoolAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentSchoolAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSchoolAssociationDashboardV1 <- function(StudentSchoolAssociationDashboardV1ID, ClassOf = NULL, DashboardRunHistoryID = NULL, EdFiExitWithdrawID = NULL, EdFiGradeLevelID = NULL, EntityID = NULL, EntryDate = NULL, ExitWithdrawDate = NULL, ExitWithdrawTypeCode = NULL, GradeReferenceID = NULL, SchoolCode = NULL, SchoolYearID = NULL, StateGradeLevelCode = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentSchoolAssociationDashboardV1", objectId = StudentSchoolAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentSchoolAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentSectionAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentSectionAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionAssociationDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiTermTypeID = F, EndDate = F, EntityID = F, IsHomeroom = F, IsRepeat = F, KeyHash = F, LocalCourseCode = F, Location = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SectionID = F, StudentID = F, StudentSectionAssociationDashboardV1ID = F, StudentUniqueID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentSectionAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentSectionAssociationDashboardV1
	#' @param StudentSectionAssociationDashboardV1ID The ID of the StudentSectionAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentSectionAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionAssociationDashboardV1 <- function(StudentSectionAssociationDashboardV1ID, BeginDate = F, ClassPeriodName = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiTermTypeID = F, EndDate = F, EntityID = F, IsHomeroom = F, IsRepeat = F, KeyHash = F, LocalCourseCode = F, Location = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SectionID = F, StudentID = F, StudentUniqueID = F, TermCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentSectionAssociationDashboardV1", objectId = StudentSectionAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionAssociationDashboardV1
	#'
	#' This function deletes a StudentSectionAssociationDashboardV1
	#' @param StudentSectionAssociationDashboardV1ID The ID of the StudentSectionAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentSectionAssociationDashboardV1ID of the deleted StudentSectionAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionAssociationDashboardV1 <- function(StudentSectionAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentSectionAssociationDashboardV1", objectId = StudentSectionAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionAssociationDashboardV1
	#'
	#' This function creates a StudentSectionAssociationDashboardV1
	#' @param fieldNames The field values to give the created StudentSectionAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentSectionAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionAssociationDashboardV1 <- function(BeginDate = NULL, ClassPeriodName = NULL, DashboardRunHistoryID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityID = NULL, IsHomeroom = NULL, IsRepeat = NULL, LocalCourseCode = NULL, Location = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SectionID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentSectionAssociationDashboardV1", body = list(DataObject = body), searchFields = append("StudentSectionAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionAssociationDashboardV1
	#'
	#' This function modifies a StudentSectionAssociationDashboardV1
	#' @param fieldNames The field values to give the modified StudentSectionAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentSectionAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionAssociationDashboardV1 <- function(StudentSectionAssociationDashboardV1ID, BeginDate = NULL, ClassPeriodName = NULL, DashboardRunHistoryID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityID = NULL, IsHomeroom = NULL, IsRepeat = NULL, LocalCourseCode = NULL, Location = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SectionID = NULL, StudentID = NULL, StudentUniqueID = NULL, TermCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentSectionAssociationDashboardV1", objectId = StudentSectionAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentSectionAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionAssociationDashboardV1s
	#'
	#' This function returns a dataframe or json object of TeacherSectionAssociationDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionAssociationDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionAssociationDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionAssociationDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of TeacherSectionAssociationDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionAssociationDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, ClassPeriodName = F, ClassroomPositionCode = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiClassroomPositionID = F, EdFiTermTypeID = F, EndDate = F, EntityID = F, IsHighlyQualifiedTeacher = F, KeyHash = F, LocalCourseCode = F, Location = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SectionID = F, StaffID = F, StaffUniqueID = F, TeacherSectionAssociationDashboardV1ID = F, TermCode = F, UniqueSectionCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TeacherSectionAssociationDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionAssociationDashboardV1
	#'
	#' This function returns a dataframe or json object of a TeacherSectionAssociationDashboardV1
	#' @param TeacherSectionAssociationDashboardV1ID The ID of the TeacherSectionAssociationDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionAssociationDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionAssociationDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionAssociationDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of TeacherSectionAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionAssociationDashboardV1 <- function(TeacherSectionAssociationDashboardV1ID, BeginDate = F, ClassPeriodName = F, ClassroomPositionCode = F, CreatedTime = F, DashboardRunHistoryID = F, EdFiClassroomPositionID = F, EdFiTermTypeID = F, EndDate = F, EntityID = F, IsHighlyQualifiedTeacher = F, KeyHash = F, LocalCourseCode = F, Location = F, ModifiedTime = F, SchoolCode = F, SchoolYearDescription = F, SchoolYearID = F, SectionID = F, StaffID = F, StaffUniqueID = F, TermCode = F, UniqueSectionCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionAssociationDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TeacherSectionAssociationDashboardV1", objectId = TeacherSectionAssociationDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionAssociationDashboardV1
	#'
	#' This function deletes a TeacherSectionAssociationDashboardV1
	#' @param TeacherSectionAssociationDashboardV1ID The ID of the TeacherSectionAssociationDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The TeacherSectionAssociationDashboardV1ID of the deleted TeacherSectionAssociationDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionAssociationDashboardV1 <- function(TeacherSectionAssociationDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TeacherSectionAssociationDashboardV1", objectId = TeacherSectionAssociationDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionAssociationDashboardV1
	#'
	#' This function creates a TeacherSectionAssociationDashboardV1
	#' @param fieldNames The field values to give the created TeacherSectionAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created TeacherSectionAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionAssociationDashboardV1 <- function(BeginDate = NULL, ClassPeriodName = NULL, ClassroomPositionCode = NULL, DashboardRunHistoryID = NULL, EdFiClassroomPositionID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityID = NULL, IsHighlyQualifiedTeacher = NULL, LocalCourseCode = NULL, Location = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SectionID = NULL, StaffID = NULL, StaffUniqueID = NULL, TermCode = NULL, UniqueSectionCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TeacherSectionAssociationDashboardV1", body = list(DataObject = body), searchFields = append("TeacherSectionAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionAssociationDashboardV1
	#'
	#' This function modifies a TeacherSectionAssociationDashboardV1
	#' @param fieldNames The field values to give the modified TeacherSectionAssociationDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified TeacherSectionAssociationDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionAssociationDashboardV1 <- function(TeacherSectionAssociationDashboardV1ID, BeginDate = NULL, ClassPeriodName = NULL, ClassroomPositionCode = NULL, DashboardRunHistoryID = NULL, EdFiClassroomPositionID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityID = NULL, IsHighlyQualifiedTeacher = NULL, LocalCourseCode = NULL, Location = NULL, SchoolCode = NULL, SchoolYearDescription = NULL, SchoolYearID = NULL, SectionID = NULL, StaffID = NULL, StaffUniqueID = NULL, TermCode = NULL, UniqueSectionCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TeacherSectionAssociationDashboardV1", objectId = TeacherSectionAssociationDashboardV1ID, body = list(DataObject = body), searchFields = append("TeacherSectionAssociationDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiAttendanceEventCategories
	#'
	#' This function returns a dataframe or json object of EdFiAttendanceEventCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiAttendanceEventCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiAttendanceEventCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiAttendanceEventCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiAttendanceEventCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiAttendanceEventCategories <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiAttendanceEventCategoryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiAttendanceEventCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiAttendanceEventCategory
	#'
	#' This function returns a dataframe or json object of an EdFiAttendanceEventCategory
	#' @param EdFiAttendanceEventCategoryID The ID of the EdFiAttendanceEventCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiAttendanceEventCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiAttendanceEventCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiAttendanceEventCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiAttendanceEventCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiAttendanceEventCategory <- function(EdFiAttendanceEventCategoryID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiAttendanceEventCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventCategory", objectId = EdFiAttendanceEventCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiAttendanceEventCategory
	#'
	#' This function deletes an EdFiAttendanceEventCategory
	#' @param EdFiAttendanceEventCategoryID The ID of the EdFiAttendanceEventCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiAttendanceEventCategoryID of the deleted EdFiAttendanceEventCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiAttendanceEventCategory <- function(EdFiAttendanceEventCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventCategory", objectId = EdFiAttendanceEventCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiAttendanceEventCategory
	#'
	#' This function creates an EdFiAttendanceEventCategory
	#' @param fieldNames The field values to give the created EdFiAttendanceEventCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiAttendanceEventCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiAttendanceEventCategory <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventCategory", body = list(DataObject = body), searchFields = append("EdFiAttendanceEventCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiAttendanceEventCategory
	#'
	#' This function modifies an EdFiAttendanceEventCategory
	#' @param fieldNames The field values to give the modified EdFiAttendanceEventCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiAttendanceEventCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiAttendanceEventCategory <- function(EdFiAttendanceEventCategoryID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventCategory", objectId = EdFiAttendanceEventCategoryID, body = list(DataObject = body), searchFields = append("EdFiAttendanceEventCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiAttendanceEventTypes
	#'
	#' This function returns a dataframe or json object of EdFiAttendanceEventTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiAttendanceEventTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiAttendanceEventTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiAttendanceEventType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiAttendanceEventTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiAttendanceEventTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiAttendanceEventTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiAttendanceEventType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiAttendanceEventType
	#'
	#' This function returns a dataframe or json object of an EdFiAttendanceEventType
	#' @param EdFiAttendanceEventTypeID The ID of the EdFiAttendanceEventType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiAttendanceEventType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiAttendanceEventType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiAttendanceEventType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiAttendanceEventType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiAttendanceEventType <- function(EdFiAttendanceEventTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiAttendanceEventTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventType", objectId = EdFiAttendanceEventTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiAttendanceEventType
	#'
	#' This function deletes an EdFiAttendanceEventType
	#' @param EdFiAttendanceEventTypeID The ID of the EdFiAttendanceEventType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiAttendanceEventTypeID of the deleted EdFiAttendanceEventType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiAttendanceEventType <- function(EdFiAttendanceEventTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventType", objectId = EdFiAttendanceEventTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiAttendanceEventType
	#'
	#' This function creates an EdFiAttendanceEventType
	#' @param fieldNames The field values to give the created EdFiAttendanceEventType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiAttendanceEventType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiAttendanceEventType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventType", body = list(DataObject = body), searchFields = append("EdFiAttendanceEventTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiAttendanceEventType
	#'
	#' This function modifies an EdFiAttendanceEventType
	#' @param fieldNames The field values to give the modified EdFiAttendanceEventType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiAttendanceEventType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiAttendanceEventType <- function(EdFiAttendanceEventTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiAttendanceEventType", objectId = EdFiAttendanceEventTypeID, body = list(DataObject = body), searchFields = append("EdFiAttendanceEventTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiBehaviorCategoryTypes
	#'
	#' This function returns a dataframe or json object of EdFiBehaviorCategoryTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiBehaviorCategoryTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiBehaviorCategoryTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiBehaviorCategoryType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiBehaviorCategoryTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiBehaviorCategoryTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiBehaviorCategoryTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiBehaviorCategoryType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiBehaviorCategoryType
	#'
	#' This function returns a dataframe or json object of an EdFiBehaviorCategoryType
	#' @param EdFiBehaviorCategoryTypeID The ID of the EdFiBehaviorCategoryType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiBehaviorCategoryType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiBehaviorCategoryType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiBehaviorCategoryType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiBehaviorCategoryType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiBehaviorCategoryType <- function(EdFiBehaviorCategoryTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiBehaviorCategoryTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiBehaviorCategoryType", objectId = EdFiBehaviorCategoryTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiBehaviorCategoryType
	#'
	#' This function deletes an EdFiBehaviorCategoryType
	#' @param EdFiBehaviorCategoryTypeID The ID of the EdFiBehaviorCategoryType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiBehaviorCategoryTypeID of the deleted EdFiBehaviorCategoryType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiBehaviorCategoryType <- function(EdFiBehaviorCategoryTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiBehaviorCategoryType", objectId = EdFiBehaviorCategoryTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiBehaviorCategoryType
	#'
	#' This function creates an EdFiBehaviorCategoryType
	#' @param fieldNames The field values to give the created EdFiBehaviorCategoryType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiBehaviorCategoryType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiBehaviorCategoryType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiBehaviorCategoryType", body = list(DataObject = body), searchFields = append("EdFiBehaviorCategoryTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiBehaviorCategoryType
	#'
	#' This function modifies an EdFiBehaviorCategoryType
	#' @param fieldNames The field values to give the modified EdFiBehaviorCategoryType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiBehaviorCategoryType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiBehaviorCategoryType <- function(EdFiBehaviorCategoryTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiBehaviorCategoryType", objectId = EdFiBehaviorCategoryTypeID, body = list(DataObject = body), searchFields = append("EdFiBehaviorCategoryTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCalendarEvents
	#'
	#' This function returns a dataframe or json object of EdFiCalendarEvents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCalendarEvents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCalendarEvents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCalendarEvent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCalendarEvents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCalendarEvents <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiCalendarEventID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCalendarEvent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCalendarEvent
	#'
	#' This function returns a dataframe or json object of an EdFiCalendarEvent
	#' @param EdFiCalendarEventID The ID of the EdFiCalendarEvent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCalendarEvent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCalendarEvent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCalendarEvent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCalendarEvent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCalendarEvent <- function(EdFiCalendarEventID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCalendarEventID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCalendarEvent", objectId = EdFiCalendarEventID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCalendarEvent
	#'
	#' This function deletes an EdFiCalendarEvent
	#' @param EdFiCalendarEventID The ID of the EdFiCalendarEvent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCalendarEventID of the deleted EdFiCalendarEvent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCalendarEvent <- function(EdFiCalendarEventID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCalendarEvent", objectId = EdFiCalendarEventID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCalendarEvent
	#'
	#' This function creates an EdFiCalendarEvent
	#' @param fieldNames The field values to give the created EdFiCalendarEvent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCalendarEvent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCalendarEvent <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCalendarEvent", body = list(DataObject = body), searchFields = append("EdFiCalendarEventID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCalendarEvent
	#'
	#' This function modifies an EdFiCalendarEvent
	#' @param fieldNames The field values to give the modified EdFiCalendarEvent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCalendarEvent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCalendarEvent <- function(EdFiCalendarEventID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCalendarEvent", objectId = EdFiCalendarEventID, body = list(DataObject = body), searchFields = append("EdFiCalendarEventID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiClassroomPositions
	#'
	#' This function returns a dataframe or json object of EdFiClassroomPositions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiClassroomPositions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiClassroomPositions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiClassroomPosition') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiClassroomPositions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiClassroomPositions <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiClassroomPositionID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiClassroomPosition", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiClassroomPosition
	#'
	#' This function returns a dataframe or json object of an EdFiClassroomPosition
	#' @param EdFiClassroomPositionID The ID of the EdFiClassroomPosition to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiClassroomPosition. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiClassroomPosition.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiClassroomPosition') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiClassroomPosition
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiClassroomPosition <- function(EdFiClassroomPositionID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiClassroomPositionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiClassroomPosition", objectId = EdFiClassroomPositionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiClassroomPosition
	#'
	#' This function deletes an EdFiClassroomPosition
	#' @param EdFiClassroomPositionID The ID of the EdFiClassroomPosition to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiClassroomPositionID of the deleted EdFiClassroomPosition.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiClassroomPosition <- function(EdFiClassroomPositionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiClassroomPosition", objectId = EdFiClassroomPositionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiClassroomPosition
	#'
	#' This function creates an EdFiClassroomPosition
	#' @param fieldNames The field values to give the created EdFiClassroomPosition. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiClassroomPosition
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiClassroomPosition <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiClassroomPosition", body = list(DataObject = body), searchFields = append("EdFiClassroomPositionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiClassroomPosition
	#'
	#' This function modifies an EdFiClassroomPosition
	#' @param fieldNames The field values to give the modified EdFiClassroomPosition. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiClassroomPosition
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiClassroomPosition <- function(EdFiClassroomPositionID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiClassroomPosition", objectId = EdFiClassroomPositionID, body = list(DataObject = body), searchFields = append("EdFiClassroomPositionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCohorts
	#'
	#' This function returns a dataframe or json object of EdFiCohorts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCohorts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCohorts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCohort') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCohorts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCohorts <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiCohortID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCohort", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCohort
	#'
	#' This function returns a dataframe or json object of an EdFiCohort
	#' @param EdFiCohortID The ID of the EdFiCohort to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCohort. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCohort.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCohort') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCohort
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCohort <- function(EdFiCohortID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCohortID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCohort", objectId = EdFiCohortID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCohort
	#'
	#' This function deletes an EdFiCohort
	#' @param EdFiCohortID The ID of the EdFiCohort to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCohortID of the deleted EdFiCohort.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCohort <- function(EdFiCohortID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCohort", objectId = EdFiCohortID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCohort
	#'
	#' This function creates an EdFiCohort
	#' @param fieldNames The field values to give the created EdFiCohort. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCohort
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCohort <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCohort", body = list(DataObject = body), searchFields = append("EdFiCohortID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCohort
	#'
	#' This function modifies an EdFiCohort
	#' @param fieldNames The field values to give the modified EdFiCohort. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCohort
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCohort <- function(EdFiCohortID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCohort", objectId = EdFiCohortID, body = list(DataObject = body), searchFields = append("EdFiCohortID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCountries
	#'
	#' This function returns a dataframe or json object of EdFiCountries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCountries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCountries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCountry') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCountries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCountries <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EdFiCountryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCountry", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCountry
	#'
	#' This function returns a dataframe or json object of an EdFiCountry
	#' @param EdFiCountryID The ID of the EdFiCountry to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCountry. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCountry.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCountry') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCountry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCountry <- function(EdFiCountryID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCountryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCountry", objectId = EdFiCountryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCountry
	#'
	#' This function deletes an EdFiCountry
	#' @param EdFiCountryID The ID of the EdFiCountry to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCountryID of the deleted EdFiCountry.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCountry <- function(EdFiCountryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCountry", objectId = EdFiCountryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCountry
	#'
	#' This function creates an EdFiCountry
	#' @param fieldNames The field values to give the created EdFiCountry. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCountry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCountry <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCountry", body = list(DataObject = body), searchFields = append("EdFiCountryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCountry
	#'
	#' This function modifies an EdFiCountry
	#' @param fieldNames The field values to give the modified EdFiCountry. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCountry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCountry <- function(EdFiCountryID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCountry", objectId = EdFiCountryID, body = list(DataObject = body), searchFields = append("EdFiCountryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCourseAttemptResults
	#'
	#' This function returns a dataframe or json object of EdFiCourseAttemptResults
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCourseAttemptResults. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCourseAttemptResults.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCourseAttemptResult') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCourseAttemptResults
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCourseAttemptResults <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiCourseAttemptResultID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCourseAttemptResult", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCourseAttemptResult
	#'
	#' This function returns a dataframe or json object of an EdFiCourseAttemptResult
	#' @param EdFiCourseAttemptResultID The ID of the EdFiCourseAttemptResult to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCourseAttemptResult. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCourseAttemptResult.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCourseAttemptResult') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCourseAttemptResult
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCourseAttemptResult <- function(EdFiCourseAttemptResultID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCourseAttemptResultID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCourseAttemptResult", objectId = EdFiCourseAttemptResultID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCourseAttemptResult
	#'
	#' This function deletes an EdFiCourseAttemptResult
	#' @param EdFiCourseAttemptResultID The ID of the EdFiCourseAttemptResult to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCourseAttemptResultID of the deleted EdFiCourseAttemptResult.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCourseAttemptResult <- function(EdFiCourseAttemptResultID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCourseAttemptResult", objectId = EdFiCourseAttemptResultID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCourseAttemptResult
	#'
	#' This function creates an EdFiCourseAttemptResult
	#' @param fieldNames The field values to give the created EdFiCourseAttemptResult. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCourseAttemptResult
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCourseAttemptResult <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCourseAttemptResult", body = list(DataObject = body), searchFields = append("EdFiCourseAttemptResultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCourseAttemptResult
	#'
	#' This function modifies an EdFiCourseAttemptResult
	#' @param fieldNames The field values to give the modified EdFiCourseAttemptResult. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCourseAttemptResult
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCourseAttemptResult <- function(EdFiCourseAttemptResultID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCourseAttemptResult", objectId = EdFiCourseAttemptResultID, body = list(DataObject = body), searchFields = append("EdFiCourseAttemptResultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCourseLevels
	#'
	#' This function returns a dataframe or json object of EdFiCourseLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCourseLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCourseLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCourseLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCourseLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCourseLevels <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiCourseLevelID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCourseLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCourseLevel
	#'
	#' This function returns a dataframe or json object of an EdFiCourseLevel
	#' @param EdFiCourseLevelID The ID of the EdFiCourseLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCourseLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCourseLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCourseLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCourseLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCourseLevel <- function(EdFiCourseLevelID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCourseLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCourseLevel", objectId = EdFiCourseLevelID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCourseLevel
	#'
	#' This function deletes an EdFiCourseLevel
	#' @param EdFiCourseLevelID The ID of the EdFiCourseLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCourseLevelID of the deleted EdFiCourseLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCourseLevel <- function(EdFiCourseLevelID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCourseLevel", objectId = EdFiCourseLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCourseLevel
	#'
	#' This function creates an EdFiCourseLevel
	#' @param fieldNames The field values to give the created EdFiCourseLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCourseLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCourseLevel <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCourseLevel", body = list(DataObject = body), searchFields = append("EdFiCourseLevelID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCourseLevel
	#'
	#' This function modifies an EdFiCourseLevel
	#' @param fieldNames The field values to give the modified EdFiCourseLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCourseLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCourseLevel <- function(EdFiCourseLevelID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCourseLevel", objectId = EdFiCourseLevelID, body = list(DataObject = body), searchFields = append("EdFiCourseLevelID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiCourseLevelCharacteristics
	#'
	#' This function returns a dataframe or json object of EdFiCourseLevelCharacteristics
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCourseLevelCharacteristics. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCourseLevelCharacteristics.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCourseLevelCharacteristic') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiCourseLevelCharacteristics
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiCourseLevelCharacteristics <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiCourseLevelCharacteristicID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiCourseLevelCharacteristic", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiCourseLevelCharacteristic
	#'
	#' This function returns a dataframe or json object of an EdFiCourseLevelCharacteristic
	#' @param EdFiCourseLevelCharacteristicID The ID of the EdFiCourseLevelCharacteristic to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiCourseLevelCharacteristic. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiCourseLevelCharacteristic.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiCourseLevelCharacteristic') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiCourseLevelCharacteristic
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiCourseLevelCharacteristic <- function(EdFiCourseLevelCharacteristicID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiCourseLevelCharacteristicID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiCourseLevelCharacteristic", objectId = EdFiCourseLevelCharacteristicID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiCourseLevelCharacteristic
	#'
	#' This function deletes an EdFiCourseLevelCharacteristic
	#' @param EdFiCourseLevelCharacteristicID The ID of the EdFiCourseLevelCharacteristic to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiCourseLevelCharacteristicID of the deleted EdFiCourseLevelCharacteristic.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiCourseLevelCharacteristic <- function(EdFiCourseLevelCharacteristicID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiCourseLevelCharacteristic", objectId = EdFiCourseLevelCharacteristicID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiCourseLevelCharacteristic
	#'
	#' This function creates an EdFiCourseLevelCharacteristic
	#' @param fieldNames The field values to give the created EdFiCourseLevelCharacteristic. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiCourseLevelCharacteristic
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiCourseLevelCharacteristic <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiCourseLevelCharacteristic", body = list(DataObject = body), searchFields = append("EdFiCourseLevelCharacteristicID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiCourseLevelCharacteristic
	#'
	#' This function modifies an EdFiCourseLevelCharacteristic
	#' @param fieldNames The field values to give the modified EdFiCourseLevelCharacteristic. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiCourseLevelCharacteristic
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiCourseLevelCharacteristic <- function(EdFiCourseLevelCharacteristicID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiCourseLevelCharacteristic", objectId = EdFiCourseLevelCharacteristicID, body = list(DataObject = body), searchFields = append("EdFiCourseLevelCharacteristicID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiDisabilityTypes
	#'
	#' This function returns a dataframe or json object of EdFiDisabilityTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiDisabilityTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiDisabilityTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiDisabilityType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiDisabilityTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiDisabilityTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiDisabilityTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiDisabilityType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiDisabilityType
	#'
	#' This function returns a dataframe or json object of an EdFiDisabilityType
	#' @param EdFiDisabilityTypeID The ID of the EdFiDisabilityType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiDisabilityType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiDisabilityType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiDisabilityType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiDisabilityType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiDisabilityType <- function(EdFiDisabilityTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiDisabilityTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiDisabilityType", objectId = EdFiDisabilityTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiDisabilityType
	#'
	#' This function deletes an EdFiDisabilityType
	#' @param EdFiDisabilityTypeID The ID of the EdFiDisabilityType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiDisabilityTypeID of the deleted EdFiDisabilityType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiDisabilityType <- function(EdFiDisabilityTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiDisabilityType", objectId = EdFiDisabilityTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiDisabilityType
	#'
	#' This function creates an EdFiDisabilityType
	#' @param fieldNames The field values to give the created EdFiDisabilityType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiDisabilityType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiDisabilityType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiDisabilityType", body = list(DataObject = body), searchFields = append("EdFiDisabilityTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiDisabilityType
	#'
	#' This function modifies an EdFiDisabilityType
	#' @param fieldNames The field values to give the modified EdFiDisabilityType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiDisabilityType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiDisabilityType <- function(EdFiDisabilityTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiDisabilityType", objectId = EdFiDisabilityTypeID, body = list(DataObject = body), searchFields = append("EdFiDisabilityTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiDisciplineActionLengthDifferenceReasons
	#'
	#' This function returns a dataframe or json object of EdFiDisciplineActionLengthDifferenceReasons
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiDisciplineActionLengthDifferenceReasons. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiDisciplineActionLengthDifferenceReasons.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiDisciplineActionLengthDifferenceReason') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiDisciplineActionLengthDifferenceReasons
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiDisciplineActionLengthDifferenceReasons <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiDisciplineActionLengthDifferenceReasonID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiDisciplineActionLengthDifferenceReason", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiDisciplineActionLengthDifferenceReason
	#'
	#' This function returns a dataframe or json object of an EdFiDisciplineActionLengthDifferenceReason
	#' @param EdFiDisciplineActionLengthDifferenceReasonID The ID of the EdFiDisciplineActionLengthDifferenceReason to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiDisciplineActionLengthDifferenceReason. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiDisciplineActionLengthDifferenceReason.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiDisciplineActionLengthDifferenceReason') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiDisciplineActionLengthDifferenceReason
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiDisciplineActionLengthDifferenceReason <- function(EdFiDisciplineActionLengthDifferenceReasonID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiDisciplineActionLengthDifferenceReasonID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiDisciplineActionLengthDifferenceReason", objectId = EdFiDisciplineActionLengthDifferenceReasonID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiDisciplineActionLengthDifferenceReason
	#'
	#' This function deletes an EdFiDisciplineActionLengthDifferenceReason
	#' @param EdFiDisciplineActionLengthDifferenceReasonID The ID of the EdFiDisciplineActionLengthDifferenceReason to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiDisciplineActionLengthDifferenceReasonID of the deleted EdFiDisciplineActionLengthDifferenceReason.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiDisciplineActionLengthDifferenceReason <- function(EdFiDisciplineActionLengthDifferenceReasonID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiDisciplineActionLengthDifferenceReason", objectId = EdFiDisciplineActionLengthDifferenceReasonID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiDisciplineActionLengthDifferenceReason
	#'
	#' This function creates an EdFiDisciplineActionLengthDifferenceReason
	#' @param fieldNames The field values to give the created EdFiDisciplineActionLengthDifferenceReason. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiDisciplineActionLengthDifferenceReason
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiDisciplineActionLengthDifferenceReason <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiDisciplineActionLengthDifferenceReason", body = list(DataObject = body), searchFields = append("EdFiDisciplineActionLengthDifferenceReasonID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiDisciplineActionLengthDifferenceReason
	#'
	#' This function modifies an EdFiDisciplineActionLengthDifferenceReason
	#' @param fieldNames The field values to give the modified EdFiDisciplineActionLengthDifferenceReason. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiDisciplineActionLengthDifferenceReason
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiDisciplineActionLengthDifferenceReason <- function(EdFiDisciplineActionLengthDifferenceReasonID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiDisciplineActionLengthDifferenceReason", objectId = EdFiDisciplineActionLengthDifferenceReasonID, body = list(DataObject = body), searchFields = append("EdFiDisciplineActionLengthDifferenceReasonID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiEducationalEnvironments
	#'
	#' This function returns a dataframe or json object of EdFiEducationalEnvironments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiEducationalEnvironments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiEducationalEnvironments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiEducationalEnvironment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiEducationalEnvironments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiEducationalEnvironments <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiEducationalEnvironmentID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiEducationalEnvironment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiEducationalEnvironment
	#'
	#' This function returns a dataframe or json object of an EdFiEducationalEnvironment
	#' @param EdFiEducationalEnvironmentID The ID of the EdFiEducationalEnvironment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiEducationalEnvironment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiEducationalEnvironment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiEducationalEnvironment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiEducationalEnvironment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiEducationalEnvironment <- function(EdFiEducationalEnvironmentID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiEducationalEnvironmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiEducationalEnvironment", objectId = EdFiEducationalEnvironmentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiEducationalEnvironment
	#'
	#' This function deletes an EdFiEducationalEnvironment
	#' @param EdFiEducationalEnvironmentID The ID of the EdFiEducationalEnvironment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiEducationalEnvironmentID of the deleted EdFiEducationalEnvironment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiEducationalEnvironment <- function(EdFiEducationalEnvironmentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiEducationalEnvironment", objectId = EdFiEducationalEnvironmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiEducationalEnvironment
	#'
	#' This function creates an EdFiEducationalEnvironment
	#' @param fieldNames The field values to give the created EdFiEducationalEnvironment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiEducationalEnvironment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiEducationalEnvironment <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiEducationalEnvironment", body = list(DataObject = body), searchFields = append("EdFiEducationalEnvironmentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiEducationalEnvironment
	#'
	#' This function modifies an EdFiEducationalEnvironment
	#' @param fieldNames The field values to give the modified EdFiEducationalEnvironment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiEducationalEnvironment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiEducationalEnvironment <- function(EdFiEducationalEnvironmentID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiEducationalEnvironment", objectId = EdFiEducationalEnvironmentID, body = list(DataObject = body), searchFields = append("EdFiEducationalEnvironmentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiEntries
	#'
	#' This function returns a dataframe or json object of EdFiEntries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiEntries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiEntries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiEntry') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiEntries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiEntries <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiEntryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiEntry", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiEntry
	#'
	#' This function returns a dataframe or json object of an EdFiEntry
	#' @param EdFiEntryID The ID of the EdFiEntry to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiEntry. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiEntry.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiEntry') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiEntry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiEntry <- function(EdFiEntryID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiEntryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiEntry", objectId = EdFiEntryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiEntry
	#'
	#' This function deletes an EdFiEntry
	#' @param EdFiEntryID The ID of the EdFiEntry to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiEntryID of the deleted EdFiEntry.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiEntry <- function(EdFiEntryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiEntry", objectId = EdFiEntryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiEntry
	#'
	#' This function creates an EdFiEntry
	#' @param fieldNames The field values to give the created EdFiEntry. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiEntry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiEntry <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiEntry", body = list(DataObject = body), searchFields = append("EdFiEntryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiEntry
	#'
	#' This function modifies an EdFiEntry
	#' @param fieldNames The field values to give the modified EdFiEntry. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiEntry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiEntry <- function(EdFiEntryID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiEntry", objectId = EdFiEntryID, body = list(DataObject = body), searchFields = append("EdFiEntryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiExitWithdraws
	#'
	#' This function returns a dataframe or json object of EdFiExitWithdraws
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiExitWithdraws. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiExitWithdraws.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiExitWithdraw') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiExitWithdraws
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiExitWithdraws <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiExitWithdrawID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiExitWithdraw", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiExitWithdraw
	#'
	#' This function returns a dataframe or json object of an EdFiExitWithdraw
	#' @param EdFiExitWithdrawID The ID of the EdFiExitWithdraw to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiExitWithdraw. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiExitWithdraw.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiExitWithdraw') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiExitWithdraw
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiExitWithdraw <- function(EdFiExitWithdrawID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiExitWithdrawID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiExitWithdraw", objectId = EdFiExitWithdrawID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiExitWithdraw
	#'
	#' This function deletes an EdFiExitWithdraw
	#' @param EdFiExitWithdrawID The ID of the EdFiExitWithdraw to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiExitWithdrawID of the deleted EdFiExitWithdraw.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiExitWithdraw <- function(EdFiExitWithdrawID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiExitWithdraw", objectId = EdFiExitWithdrawID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiExitWithdraw
	#'
	#' This function creates an EdFiExitWithdraw
	#' @param fieldNames The field values to give the created EdFiExitWithdraw. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiExitWithdraw
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiExitWithdraw <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiExitWithdraw", body = list(DataObject = body), searchFields = append("EdFiExitWithdrawID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiExitWithdraw
	#'
	#' This function modifies an EdFiExitWithdraw
	#' @param fieldNames The field values to give the modified EdFiExitWithdraw. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiExitWithdraw
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiExitWithdraw <- function(EdFiExitWithdrawID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiExitWithdraw", objectId = EdFiExitWithdrawID, body = list(DataObject = body), searchFields = append("EdFiExitWithdrawID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiGenerationSuffixes
	#'
	#' This function returns a dataframe or json object of EdFiGenerationSuffixes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGenerationSuffixes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGenerationSuffixes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGenerationSuffix') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiGenerationSuffixes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiGenerationSuffixes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiGenerationSuffixID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiGenerationSuffix", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiGenerationSuffix
	#'
	#' This function returns a dataframe or json object of an EdFiGenerationSuffix
	#' @param EdFiGenerationSuffixID The ID of the EdFiGenerationSuffix to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGenerationSuffix. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGenerationSuffix.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGenerationSuffix') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiGenerationSuffix
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiGenerationSuffix <- function(EdFiGenerationSuffixID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiGenerationSuffixID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiGenerationSuffix", objectId = EdFiGenerationSuffixID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiGenerationSuffix
	#'
	#' This function deletes an EdFiGenerationSuffix
	#' @param EdFiGenerationSuffixID The ID of the EdFiGenerationSuffix to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiGenerationSuffixID of the deleted EdFiGenerationSuffix.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiGenerationSuffix <- function(EdFiGenerationSuffixID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiGenerationSuffix", objectId = EdFiGenerationSuffixID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiGenerationSuffix
	#'
	#' This function creates an EdFiGenerationSuffix
	#' @param fieldNames The field values to give the created EdFiGenerationSuffix. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiGenerationSuffix
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiGenerationSuffix <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiGenerationSuffix", body = list(DataObject = body), searchFields = append("EdFiGenerationSuffixID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiGenerationSuffix
	#'
	#' This function modifies an EdFiGenerationSuffix
	#' @param fieldNames The field values to give the modified EdFiGenerationSuffix. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiGenerationSuffix
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiGenerationSuffix <- function(EdFiGenerationSuffixID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiGenerationSuffix", objectId = EdFiGenerationSuffixID, body = list(DataObject = body), searchFields = append("EdFiGenerationSuffixID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiGradeLevels
	#'
	#' This function returns a dataframe or json object of EdFiGradeLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradeLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradeLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradeLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiGradeLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiGradeLevels <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiGradeLevelID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiGradeLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiGradeLevel
	#'
	#' This function returns a dataframe or json object of an EdFiGradeLevel
	#' @param EdFiGradeLevelID The ID of the EdFiGradeLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradeLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradeLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradeLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiGradeLevel <- function(EdFiGradeLevelID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiGradeLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiGradeLevel", objectId = EdFiGradeLevelID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiGradeLevel
	#'
	#' This function deletes an EdFiGradeLevel
	#' @param EdFiGradeLevelID The ID of the EdFiGradeLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiGradeLevelID of the deleted EdFiGradeLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiGradeLevel <- function(EdFiGradeLevelID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiGradeLevel", objectId = EdFiGradeLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiGradeLevel
	#'
	#' This function creates an EdFiGradeLevel
	#' @param fieldNames The field values to give the created EdFiGradeLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiGradeLevel <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiGradeLevel", body = list(DataObject = body), searchFields = append("EdFiGradeLevelID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiGradeLevel
	#'
	#' This function modifies an EdFiGradeLevel
	#' @param fieldNames The field values to give the modified EdFiGradeLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiGradeLevel <- function(EdFiGradeLevelID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiGradeLevel", objectId = EdFiGradeLevelID, body = list(DataObject = body), searchFields = append("EdFiGradeLevelID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiGradeTypes
	#'
	#' This function returns a dataframe or json object of EdFiGradeTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradeTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradeTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradeType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiGradeTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiGradeTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiGradeTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiGradeType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiGradeType
	#'
	#' This function returns a dataframe or json object of an EdFiGradeType
	#' @param EdFiGradeTypeID The ID of the EdFiGradeType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradeType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradeType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradeType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiGradeType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiGradeType <- function(EdFiGradeTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiGradeTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiGradeType", objectId = EdFiGradeTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiGradeType
	#'
	#' This function deletes an EdFiGradeType
	#' @param EdFiGradeTypeID The ID of the EdFiGradeType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiGradeTypeID of the deleted EdFiGradeType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiGradeType <- function(EdFiGradeTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiGradeType", objectId = EdFiGradeTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiGradeType
	#'
	#' This function creates an EdFiGradeType
	#' @param fieldNames The field values to give the created EdFiGradeType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiGradeType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiGradeType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiGradeType", body = list(DataObject = body), searchFields = append("EdFiGradeTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiGradeType
	#'
	#' This function modifies an EdFiGradeType
	#' @param fieldNames The field values to give the modified EdFiGradeType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiGradeType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiGradeType <- function(EdFiGradeTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiGradeType", objectId = EdFiGradeTypeID, body = list(DataObject = body), searchFields = append("EdFiGradeTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiGradingPeriods
	#'
	#' This function returns a dataframe or json object of EdFiGradingPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradingPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradingPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradingPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiGradingPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiGradingPeriods <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiGradingPeriodID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiGradingPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiGradingPeriod
	#'
	#' This function returns a dataframe or json object of an EdFiGradingPeriod
	#' @param EdFiGradingPeriodID The ID of the EdFiGradingPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradingPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradingPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradingPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiGradingPeriod <- function(EdFiGradingPeriodID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiGradingPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiGradingPeriod", objectId = EdFiGradingPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiGradingPeriod
	#'
	#' This function deletes an EdFiGradingPeriod
	#' @param EdFiGradingPeriodID The ID of the EdFiGradingPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiGradingPeriodID of the deleted EdFiGradingPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiGradingPeriod <- function(EdFiGradingPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiGradingPeriod", objectId = EdFiGradingPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiGradingPeriod
	#'
	#' This function creates an EdFiGradingPeriod
	#' @param fieldNames The field values to give the created EdFiGradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiGradingPeriod <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiGradingPeriod", body = list(DataObject = body), searchFields = append("EdFiGradingPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiGradingPeriod
	#'
	#' This function modifies an EdFiGradingPeriod
	#' @param fieldNames The field values to give the modified EdFiGradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiGradingPeriod <- function(EdFiGradingPeriodID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiGradingPeriod", objectId = EdFiGradingPeriodID, body = list(DataObject = body), searchFields = append("EdFiGradingPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiGraduationPlans
	#'
	#' This function returns a dataframe or json object of EdFiGraduationPlans
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGraduationPlans. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGraduationPlans.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGraduationPlan') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiGraduationPlans
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiGraduationPlans <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiGraduationPlanID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiGraduationPlan", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiGraduationPlan
	#'
	#' This function returns a dataframe or json object of an EdFiGraduationPlan
	#' @param EdFiGraduationPlanID The ID of the EdFiGraduationPlan to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGraduationPlan. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGraduationPlan.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGraduationPlan') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiGraduationPlan
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiGraduationPlan <- function(EdFiGraduationPlanID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiGraduationPlanID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiGraduationPlan", objectId = EdFiGraduationPlanID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiGraduationPlan
	#'
	#' This function deletes an EdFiGraduationPlan
	#' @param EdFiGraduationPlanID The ID of the EdFiGraduationPlan to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiGraduationPlanID of the deleted EdFiGraduationPlan.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiGraduationPlan <- function(EdFiGraduationPlanID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiGraduationPlan", objectId = EdFiGraduationPlanID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiGraduationPlan
	#'
	#' This function creates an EdFiGraduationPlan
	#' @param fieldNames The field values to give the created EdFiGraduationPlan. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiGraduationPlan
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiGraduationPlan <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiGraduationPlan", body = list(DataObject = body), searchFields = append("EdFiGraduationPlanID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiGraduationPlan
	#'
	#' This function modifies an EdFiGraduationPlan
	#' @param fieldNames The field values to give the modified EdFiGraduationPlan. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiGraduationPlan
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiGraduationPlan <- function(EdFiGraduationPlanID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiGraduationPlan", objectId = EdFiGraduationPlanID, body = list(DataObject = body), searchFields = append("EdFiGraduationPlanID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiIncidentLocations
	#'
	#' This function returns a dataframe or json object of EdFiIncidentLocations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiIncidentLocations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiIncidentLocations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiIncidentLocation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiIncidentLocations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiIncidentLocations <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiIncidentLocationID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiIncidentLocation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiIncidentLocation
	#'
	#' This function returns a dataframe or json object of an EdFiIncidentLocation
	#' @param EdFiIncidentLocationID The ID of the EdFiIncidentLocation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiIncidentLocation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiIncidentLocation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiIncidentLocation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiIncidentLocation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiIncidentLocation <- function(EdFiIncidentLocationID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiIncidentLocationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiIncidentLocation", objectId = EdFiIncidentLocationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiIncidentLocation
	#'
	#' This function deletes an EdFiIncidentLocation
	#' @param EdFiIncidentLocationID The ID of the EdFiIncidentLocation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiIncidentLocationID of the deleted EdFiIncidentLocation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiIncidentLocation <- function(EdFiIncidentLocationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiIncidentLocation", objectId = EdFiIncidentLocationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiIncidentLocation
	#'
	#' This function creates an EdFiIncidentLocation
	#' @param fieldNames The field values to give the created EdFiIncidentLocation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiIncidentLocation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiIncidentLocation <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiIncidentLocation", body = list(DataObject = body), searchFields = append("EdFiIncidentLocationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiIncidentLocation
	#'
	#' This function modifies an EdFiIncidentLocation
	#' @param fieldNames The field values to give the modified EdFiIncidentLocation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiIncidentLocation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiIncidentLocation <- function(EdFiIncidentLocationID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiIncidentLocation", objectId = EdFiIncidentLocationID, body = list(DataObject = body), searchFields = append("EdFiIncidentLocationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiLanguages
	#'
	#' This function returns a dataframe or json object of EdFiLanguages
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLanguages. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLanguages.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLanguage') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiLanguages
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiLanguages <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiLanguageID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiLanguage", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiLanguage
	#'
	#' This function returns a dataframe or json object of an EdFiLanguage
	#' @param EdFiLanguageID The ID of the EdFiLanguage to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLanguage. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLanguage.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLanguage') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiLanguage
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiLanguage <- function(EdFiLanguageID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiLanguageID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiLanguage", objectId = EdFiLanguageID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiLanguage
	#'
	#' This function deletes an EdFiLanguage
	#' @param EdFiLanguageID The ID of the EdFiLanguage to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiLanguageID of the deleted EdFiLanguage.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiLanguage <- function(EdFiLanguageID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiLanguage", objectId = EdFiLanguageID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiLanguage
	#'
	#' This function creates an EdFiLanguage
	#' @param fieldNames The field values to give the created EdFiLanguage. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiLanguage
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiLanguage <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiLanguage", body = list(DataObject = body), searchFields = append("EdFiLanguageID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiLanguage
	#'
	#' This function modifies an EdFiLanguage
	#' @param fieldNames The field values to give the modified EdFiLanguage. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiLanguage
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiLanguage <- function(EdFiLanguageID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiLanguage", objectId = EdFiLanguageID, body = list(DataObject = body), searchFields = append("EdFiLanguageID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiLimitedEnglishProficiencies
	#'
	#' This function returns a dataframe or json object of EdFiLimitedEnglishProficiencies
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLimitedEnglishProficiencies. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLimitedEnglishProficiencies.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLimitedEnglishProficiency') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiLimitedEnglishProficiencies
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiLimitedEnglishProficiencies <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiLimitedEnglishProficiencyID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiLimitedEnglishProficiency", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiLimitedEnglishProficiency
	#'
	#' This function returns a dataframe or json object of an EdFiLimitedEnglishProficiency
	#' @param EdFiLimitedEnglishProficiencyID The ID of the EdFiLimitedEnglishProficiency to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLimitedEnglishProficiency. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLimitedEnglishProficiency.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLimitedEnglishProficiency') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiLimitedEnglishProficiency
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiLimitedEnglishProficiency <- function(EdFiLimitedEnglishProficiencyID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiLimitedEnglishProficiencyID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiLimitedEnglishProficiency", objectId = EdFiLimitedEnglishProficiencyID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiLimitedEnglishProficiency
	#'
	#' This function deletes an EdFiLimitedEnglishProficiency
	#' @param EdFiLimitedEnglishProficiencyID The ID of the EdFiLimitedEnglishProficiency to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiLimitedEnglishProficiencyID of the deleted EdFiLimitedEnglishProficiency.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiLimitedEnglishProficiency <- function(EdFiLimitedEnglishProficiencyID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiLimitedEnglishProficiency", objectId = EdFiLimitedEnglishProficiencyID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiLimitedEnglishProficiency
	#'
	#' This function creates an EdFiLimitedEnglishProficiency
	#' @param fieldNames The field values to give the created EdFiLimitedEnglishProficiency. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiLimitedEnglishProficiency
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiLimitedEnglishProficiency <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiLimitedEnglishProficiency", body = list(DataObject = body), searchFields = append("EdFiLimitedEnglishProficiencyID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiLimitedEnglishProficiency
	#'
	#' This function modifies an EdFiLimitedEnglishProficiency
	#' @param fieldNames The field values to give the modified EdFiLimitedEnglishProficiency. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiLimitedEnglishProficiency
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiLimitedEnglishProficiency <- function(EdFiLimitedEnglishProficiencyID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiLimitedEnglishProficiency", objectId = EdFiLimitedEnglishProficiencyID, body = list(DataObject = body), searchFields = append("EdFiLimitedEnglishProficiencyID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiPerformanceBaseTypes
	#'
	#' This function returns a dataframe or json object of EdFiPerformanceBaseTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiPerformanceBaseTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiPerformanceBaseTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiPerformanceBaseType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiPerformanceBaseTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiPerformanceBaseTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiPerformanceBaseTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiPerformanceBaseType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiPerformanceBaseType
	#'
	#' This function returns a dataframe or json object of an EdFiPerformanceBaseType
	#' @param EdFiPerformanceBaseTypeID The ID of the EdFiPerformanceBaseType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiPerformanceBaseType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiPerformanceBaseType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiPerformanceBaseType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiPerformanceBaseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiPerformanceBaseType <- function(EdFiPerformanceBaseTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiPerformanceBaseTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiPerformanceBaseType", objectId = EdFiPerformanceBaseTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiPerformanceBaseType
	#'
	#' This function deletes an EdFiPerformanceBaseType
	#' @param EdFiPerformanceBaseTypeID The ID of the EdFiPerformanceBaseType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiPerformanceBaseTypeID of the deleted EdFiPerformanceBaseType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiPerformanceBaseType <- function(EdFiPerformanceBaseTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiPerformanceBaseType", objectId = EdFiPerformanceBaseTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiPerformanceBaseType
	#'
	#' This function creates an EdFiPerformanceBaseType
	#' @param fieldNames The field values to give the created EdFiPerformanceBaseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiPerformanceBaseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiPerformanceBaseType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiPerformanceBaseType", body = list(DataObject = body), searchFields = append("EdFiPerformanceBaseTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiPerformanceBaseType
	#'
	#' This function modifies an EdFiPerformanceBaseType
	#' @param fieldNames The field values to give the modified EdFiPerformanceBaseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiPerformanceBaseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiPerformanceBaseType <- function(EdFiPerformanceBaseTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiPerformanceBaseType", objectId = EdFiPerformanceBaseTypeID, body = list(DataObject = body), searchFields = append("EdFiPerformanceBaseTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiPKSchoolTypes
	#'
	#' This function returns a dataframe or json object of EdFiPKSchoolTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiPKSchoolTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiPKSchoolTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiPKSchoolType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiPKSchoolTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiPKSchoolTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiPKSchoolTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiPKSchoolType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiPKSchoolType
	#'
	#' This function returns a dataframe or json object of an EdFiPKSchoolType
	#' @param EdFiPKSchoolTypeID The ID of the EdFiPKSchoolType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiPKSchoolType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiPKSchoolType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiPKSchoolType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiPKSchoolType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiPKSchoolType <- function(EdFiPKSchoolTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiPKSchoolTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiPKSchoolType", objectId = EdFiPKSchoolTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiPKSchoolType
	#'
	#' This function deletes an EdFiPKSchoolType
	#' @param EdFiPKSchoolTypeID The ID of the EdFiPKSchoolType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiPKSchoolTypeID of the deleted EdFiPKSchoolType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiPKSchoolType <- function(EdFiPKSchoolTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiPKSchoolType", objectId = EdFiPKSchoolTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiPKSchoolType
	#'
	#' This function creates an EdFiPKSchoolType
	#' @param fieldNames The field values to give the created EdFiPKSchoolType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiPKSchoolType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiPKSchoolType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiPKSchoolType", body = list(DataObject = body), searchFields = append("EdFiPKSchoolTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiPKSchoolType
	#'
	#' This function modifies an EdFiPKSchoolType
	#' @param fieldNames The field values to give the modified EdFiPKSchoolType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiPKSchoolType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiPKSchoolType <- function(EdFiPKSchoolTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiPKSchoolType", objectId = EdFiPKSchoolTypeID, body = list(DataObject = body), searchFields = append("EdFiPKSchoolTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiProgramTypes
	#'
	#' This function returns a dataframe or json object of EdFiProgramTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiProgramTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiProgramTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiProgramType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiProgramTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiProgramTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiProgramTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiProgramType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiProgramType
	#'
	#' This function returns a dataframe or json object of an EdFiProgramType
	#' @param EdFiProgramTypeID The ID of the EdFiProgramType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiProgramType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiProgramType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiProgramType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiProgramType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiProgramType <- function(EdFiProgramTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiProgramTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiProgramType", objectId = EdFiProgramTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiProgramType
	#'
	#' This function deletes an EdFiProgramType
	#' @param EdFiProgramTypeID The ID of the EdFiProgramType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiProgramTypeID of the deleted EdFiProgramType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiProgramType <- function(EdFiProgramTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiProgramType", objectId = EdFiProgramTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiProgramType
	#'
	#' This function creates an EdFiProgramType
	#' @param fieldNames The field values to give the created EdFiProgramType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiProgramType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiProgramType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiProgramType", body = list(DataObject = body), searchFields = append("EdFiProgramTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiProgramType
	#'
	#' This function modifies an EdFiProgramType
	#' @param fieldNames The field values to give the modified EdFiProgramType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiProgramType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiProgramType <- function(EdFiProgramTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiProgramType", objectId = EdFiProgramTypeID, body = list(DataObject = body), searchFields = append("EdFiProgramTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiRaces
	#'
	#' This function returns a dataframe or json object of EdFiRaces
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiRaces. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiRaces.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiRace') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiRaces
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiRaces <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiRaceID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiRace", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiRace
	#'
	#' This function returns a dataframe or json object of an EdFiRace
	#' @param EdFiRaceID The ID of the EdFiRace to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiRace. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiRace.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiRace') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiRace
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiRace <- function(EdFiRaceID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiRaceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiRace", objectId = EdFiRaceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiRace
	#'
	#' This function deletes an EdFiRace
	#' @param EdFiRaceID The ID of the EdFiRace to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiRaceID of the deleted EdFiRace.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiRace <- function(EdFiRaceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiRace", objectId = EdFiRaceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiRace
	#'
	#' This function creates an EdFiRace
	#' @param fieldNames The field values to give the created EdFiRace. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiRace
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiRace <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiRace", body = list(DataObject = body), searchFields = append("EdFiRaceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiRace
	#'
	#' This function modifies an EdFiRace
	#' @param fieldNames The field values to give the modified EdFiRace. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiRace
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiRace <- function(EdFiRaceID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiRace", objectId = EdFiRaceID, body = list(DataObject = body), searchFields = append("EdFiRaceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiRelationTypes
	#'
	#' This function returns a dataframe or json object of EdFiRelationTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiRelationTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiRelationTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiRelationType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiRelationTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiRelationTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiRelationTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiRelationType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiRelationType
	#'
	#' This function returns a dataframe or json object of an EdFiRelationType
	#' @param EdFiRelationTypeID The ID of the EdFiRelationType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiRelationType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiRelationType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiRelationType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiRelationType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiRelationType <- function(EdFiRelationTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiRelationTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiRelationType", objectId = EdFiRelationTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiRelationType
	#'
	#' This function deletes an EdFiRelationType
	#' @param EdFiRelationTypeID The ID of the EdFiRelationType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiRelationTypeID of the deleted EdFiRelationType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiRelationType <- function(EdFiRelationTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiRelationType", objectId = EdFiRelationTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiRelationType
	#'
	#' This function creates an EdFiRelationType
	#' @param fieldNames The field values to give the created EdFiRelationType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiRelationType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiRelationType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiRelationType", body = list(DataObject = body), searchFields = append("EdFiRelationTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiRelationType
	#'
	#' This function modifies an EdFiRelationType
	#' @param fieldNames The field values to give the modified EdFiRelationType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiRelationType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiRelationType <- function(EdFiRelationTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiRelationType", objectId = EdFiRelationTypeID, body = list(DataObject = body), searchFields = append("EdFiRelationTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiReporterDescriptions
	#'
	#' This function returns a dataframe or json object of EdFiReporterDescriptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiReporterDescriptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiReporterDescriptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiReporterDescription') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiReporterDescriptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiReporterDescriptions <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiReporterDescriptionID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiReporterDescription", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiReporterDescription
	#'
	#' This function returns a dataframe or json object of an EdFiReporterDescription
	#' @param EdFiReporterDescriptionID The ID of the EdFiReporterDescription to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiReporterDescription. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiReporterDescription.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiReporterDescription') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiReporterDescription
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiReporterDescription <- function(EdFiReporterDescriptionID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiReporterDescriptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiReporterDescription", objectId = EdFiReporterDescriptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiReporterDescription
	#'
	#' This function deletes an EdFiReporterDescription
	#' @param EdFiReporterDescriptionID The ID of the EdFiReporterDescription to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiReporterDescriptionID of the deleted EdFiReporterDescription.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiReporterDescription <- function(EdFiReporterDescriptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiReporterDescription", objectId = EdFiReporterDescriptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiReporterDescription
	#'
	#' This function creates an EdFiReporterDescription
	#' @param fieldNames The field values to give the created EdFiReporterDescription. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiReporterDescription
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiReporterDescription <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiReporterDescription", body = list(DataObject = body), searchFields = append("EdFiReporterDescriptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiReporterDescription
	#'
	#' This function modifies an EdFiReporterDescription
	#' @param fieldNames The field values to give the modified EdFiReporterDescription. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiReporterDescription
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiReporterDescription <- function(EdFiReporterDescriptionID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiReporterDescription", objectId = EdFiReporterDescriptionID, body = list(DataObject = body), searchFields = append("EdFiReporterDescriptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiSchoolCategories
	#'
	#' This function returns a dataframe or json object of EdFiSchoolCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSchoolCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSchoolCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSchoolCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiSchoolCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiSchoolCategories <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiSchoolCategoryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiSchoolCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiSchoolCategory
	#'
	#' This function returns a dataframe or json object of an EdFiSchoolCategory
	#' @param EdFiSchoolCategoryID The ID of the EdFiSchoolCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSchoolCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSchoolCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSchoolCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiSchoolCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiSchoolCategory <- function(EdFiSchoolCategoryID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiSchoolCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiSchoolCategory", objectId = EdFiSchoolCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiSchoolCategory
	#'
	#' This function deletes an EdFiSchoolCategory
	#' @param EdFiSchoolCategoryID The ID of the EdFiSchoolCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiSchoolCategoryID of the deleted EdFiSchoolCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiSchoolCategory <- function(EdFiSchoolCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiSchoolCategory", objectId = EdFiSchoolCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiSchoolCategory
	#'
	#' This function creates an EdFiSchoolCategory
	#' @param fieldNames The field values to give the created EdFiSchoolCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiSchoolCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiSchoolCategory <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiSchoolCategory", body = list(DataObject = body), searchFields = append("EdFiSchoolCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiSchoolCategory
	#'
	#' This function modifies an EdFiSchoolCategory
	#' @param fieldNames The field values to give the modified EdFiSchoolCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiSchoolCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiSchoolCategory <- function(EdFiSchoolCategoryID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiSchoolCategory", objectId = EdFiSchoolCategoryID, body = list(DataObject = body), searchFields = append("EdFiSchoolCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiSchoolFoodServicesEligibilities
	#'
	#' This function returns a dataframe or json object of EdFiSchoolFoodServicesEligibilities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSchoolFoodServicesEligibilities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSchoolFoodServicesEligibilities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSchoolFoodServicesEligibility') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiSchoolFoodServicesEligibilities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiSchoolFoodServicesEligibilities <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiSchoolFoodServicesEligibilityID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiSchoolFoodServicesEligibility", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiSchoolFoodServicesEligibility
	#'
	#' This function returns a dataframe or json object of an EdFiSchoolFoodServicesEligibility
	#' @param EdFiSchoolFoodServicesEligibilityID The ID of the EdFiSchoolFoodServicesEligibility to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSchoolFoodServicesEligibility. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSchoolFoodServicesEligibility.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSchoolFoodServicesEligibility') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiSchoolFoodServicesEligibility
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiSchoolFoodServicesEligibility <- function(EdFiSchoolFoodServicesEligibilityID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiSchoolFoodServicesEligibilityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiSchoolFoodServicesEligibility", objectId = EdFiSchoolFoodServicesEligibilityID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiSchoolFoodServicesEligibility
	#'
	#' This function deletes an EdFiSchoolFoodServicesEligibility
	#' @param EdFiSchoolFoodServicesEligibilityID The ID of the EdFiSchoolFoodServicesEligibility to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiSchoolFoodServicesEligibilityID of the deleted EdFiSchoolFoodServicesEligibility.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiSchoolFoodServicesEligibility <- function(EdFiSchoolFoodServicesEligibilityID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiSchoolFoodServicesEligibility", objectId = EdFiSchoolFoodServicesEligibilityID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiSchoolFoodServicesEligibility
	#'
	#' This function creates an EdFiSchoolFoodServicesEligibility
	#' @param fieldNames The field values to give the created EdFiSchoolFoodServicesEligibility. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiSchoolFoodServicesEligibility
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiSchoolFoodServicesEligibility <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiSchoolFoodServicesEligibility", body = list(DataObject = body), searchFields = append("EdFiSchoolFoodServicesEligibilityID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiSchoolFoodServicesEligibility
	#'
	#' This function modifies an EdFiSchoolFoodServicesEligibility
	#' @param fieldNames The field values to give the modified EdFiSchoolFoodServicesEligibility. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiSchoolFoodServicesEligibility
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiSchoolFoodServicesEligibility <- function(EdFiSchoolFoodServicesEligibilityID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiSchoolFoodServicesEligibility", objectId = EdFiSchoolFoodServicesEligibilityID, body = list(DataObject = body), searchFields = append("EdFiSchoolFoodServicesEligibilityID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiSexes
	#'
	#' This function returns a dataframe or json object of EdFiSexes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSexes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSexes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSex') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiSexes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiSexes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiSexID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiSex", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiSex
	#'
	#' This function returns a dataframe or json object of an EdFiSex
	#' @param EdFiSexID The ID of the EdFiSex to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSex. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSex.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSex') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiSex
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiSex <- function(EdFiSexID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiSexID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiSex", objectId = EdFiSexID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiSex
	#'
	#' This function deletes an EdFiSex
	#' @param EdFiSexID The ID of the EdFiSex to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiSexID of the deleted EdFiSex.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiSex <- function(EdFiSexID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiSex", objectId = EdFiSexID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiSex
	#'
	#' This function creates an EdFiSex
	#' @param fieldNames The field values to give the created EdFiSex. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiSex
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiSex <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiSex", body = list(DataObject = body), searchFields = append("EdFiSexID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiSex
	#'
	#' This function modifies an EdFiSex
	#' @param fieldNames The field values to give the modified EdFiSex. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiSex
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiSex <- function(EdFiSexID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiSex", objectId = EdFiSexID, body = list(DataObject = body), searchFields = append("EdFiSexID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiStaffClassifications
	#'
	#' This function returns a dataframe or json object of EdFiStaffClassifications
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStaffClassifications. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStaffClassifications.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStaffClassification') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiStaffClassifications
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiStaffClassifications <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiStaffClassificationID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiStaffClassification", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiStaffClassification
	#'
	#' This function returns a dataframe or json object of an EdFiStaffClassification
	#' @param EdFiStaffClassificationID The ID of the EdFiStaffClassification to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStaffClassification. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStaffClassification.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStaffClassification') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiStaffClassification
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiStaffClassification <- function(EdFiStaffClassificationID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiStaffClassificationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiStaffClassification", objectId = EdFiStaffClassificationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiStaffClassification
	#'
	#' This function deletes an EdFiStaffClassification
	#' @param EdFiStaffClassificationID The ID of the EdFiStaffClassification to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiStaffClassificationID of the deleted EdFiStaffClassification.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiStaffClassification <- function(EdFiStaffClassificationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiStaffClassification", objectId = EdFiStaffClassificationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiStaffClassification
	#'
	#' This function creates an EdFiStaffClassification
	#' @param fieldNames The field values to give the created EdFiStaffClassification. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiStaffClassification
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiStaffClassification <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiStaffClassification", body = list(DataObject = body), searchFields = append("EdFiStaffClassificationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiStaffClassification
	#'
	#' This function modifies an EdFiStaffClassification
	#' @param fieldNames The field values to give the modified EdFiStaffClassification. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiStaffClassification
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiStaffClassification <- function(EdFiStaffClassificationID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiStaffClassification", objectId = EdFiStaffClassificationID, body = list(DataObject = body), searchFields = append("EdFiStaffClassificationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiStateAbbreviations
	#'
	#' This function returns a dataframe or json object of EdFiStateAbbreviations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStateAbbreviations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStateAbbreviations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStateAbbreviation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiStateAbbreviations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiStateAbbreviations <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiStateAbbreviationID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiStateAbbreviation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiStateAbbreviation
	#'
	#' This function returns a dataframe or json object of an EdFiStateAbbreviation
	#' @param EdFiStateAbbreviationID The ID of the EdFiStateAbbreviation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStateAbbreviation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStateAbbreviation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStateAbbreviation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiStateAbbreviation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiStateAbbreviation <- function(EdFiStateAbbreviationID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiStateAbbreviationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiStateAbbreviation", objectId = EdFiStateAbbreviationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiStateAbbreviation
	#'
	#' This function deletes an EdFiStateAbbreviation
	#' @param EdFiStateAbbreviationID The ID of the EdFiStateAbbreviation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiStateAbbreviationID of the deleted EdFiStateAbbreviation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiStateAbbreviation <- function(EdFiStateAbbreviationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiStateAbbreviation", objectId = EdFiStateAbbreviationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiStateAbbreviation
	#'
	#' This function creates an EdFiStateAbbreviation
	#' @param fieldNames The field values to give the created EdFiStateAbbreviation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiStateAbbreviation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiStateAbbreviation <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiStateAbbreviation", body = list(DataObject = body), searchFields = append("EdFiStateAbbreviationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiStateAbbreviation
	#'
	#' This function modifies an EdFiStateAbbreviation
	#' @param fieldNames The field values to give the modified EdFiStateAbbreviation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiStateAbbreviation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiStateAbbreviation <- function(EdFiStateAbbreviationID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiStateAbbreviation", objectId = EdFiStateAbbreviationID, body = list(DataObject = body), searchFields = append("EdFiStateAbbreviationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiStudentCharacteristics
	#'
	#' This function returns a dataframe or json object of EdFiStudentCharacteristics
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStudentCharacteristics. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStudentCharacteristics.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStudentCharacteristic') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiStudentCharacteristics
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiStudentCharacteristics <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiStudentCharacteristicID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiStudentCharacteristic", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiStudentCharacteristic
	#'
	#' This function returns a dataframe or json object of an EdFiStudentCharacteristic
	#' @param EdFiStudentCharacteristicID The ID of the EdFiStudentCharacteristic to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStudentCharacteristic. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStudentCharacteristic.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStudentCharacteristic') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiStudentCharacteristic
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiStudentCharacteristic <- function(EdFiStudentCharacteristicID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiStudentCharacteristicID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiStudentCharacteristic", objectId = EdFiStudentCharacteristicID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiStudentCharacteristic
	#'
	#' This function deletes an EdFiStudentCharacteristic
	#' @param EdFiStudentCharacteristicID The ID of the EdFiStudentCharacteristic to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiStudentCharacteristicID of the deleted EdFiStudentCharacteristic.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiStudentCharacteristic <- function(EdFiStudentCharacteristicID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiStudentCharacteristic", objectId = EdFiStudentCharacteristicID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiStudentCharacteristic
	#'
	#' This function creates an EdFiStudentCharacteristic
	#' @param fieldNames The field values to give the created EdFiStudentCharacteristic. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiStudentCharacteristic
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiStudentCharacteristic <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiStudentCharacteristic", body = list(DataObject = body), searchFields = append("EdFiStudentCharacteristicID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiStudentCharacteristic
	#'
	#' This function modifies an EdFiStudentCharacteristic
	#' @param fieldNames The field values to give the modified EdFiStudentCharacteristic. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiStudentCharacteristic
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiStudentCharacteristic <- function(EdFiStudentCharacteristicID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiStudentCharacteristic", objectId = EdFiStudentCharacteristicID, body = list(DataObject = body), searchFields = append("EdFiStudentCharacteristicID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiStudentParticipations
	#'
	#' This function returns a dataframe or json object of EdFiStudentParticipations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStudentParticipations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStudentParticipations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStudentParticipation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiStudentParticipations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiStudentParticipations <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiStudentParticipationID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiStudentParticipation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiStudentParticipation
	#'
	#' This function returns a dataframe or json object of an EdFiStudentParticipation
	#' @param EdFiStudentParticipationID The ID of the EdFiStudentParticipation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStudentParticipation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStudentParticipation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStudentParticipation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiStudentParticipation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiStudentParticipation <- function(EdFiStudentParticipationID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiStudentParticipationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiStudentParticipation", objectId = EdFiStudentParticipationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiStudentParticipation
	#'
	#' This function deletes an EdFiStudentParticipation
	#' @param EdFiStudentParticipationID The ID of the EdFiStudentParticipation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiStudentParticipationID of the deleted EdFiStudentParticipation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiStudentParticipation <- function(EdFiStudentParticipationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiStudentParticipation", objectId = EdFiStudentParticipationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiStudentParticipation
	#'
	#' This function creates an EdFiStudentParticipation
	#' @param fieldNames The field values to give the created EdFiStudentParticipation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiStudentParticipation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiStudentParticipation <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiStudentParticipation", body = list(DataObject = body), searchFields = append("EdFiStudentParticipationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiStudentParticipation
	#'
	#' This function modifies an EdFiStudentParticipation
	#' @param fieldNames The field values to give the modified EdFiStudentParticipation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiStudentParticipation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiStudentParticipation <- function(EdFiStudentParticipationID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiStudentParticipation", objectId = EdFiStudentParticipationID, body = list(DataObject = body), searchFields = append("EdFiStudentParticipationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiSubjectTypes
	#'
	#' This function returns a dataframe or json object of EdFiSubjectTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSubjectTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSubjectTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSubjectType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiSubjectTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiSubjectTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiSubjectTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiSubjectType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiSubjectType
	#'
	#' This function returns a dataframe or json object of an EdFiSubjectType
	#' @param EdFiSubjectTypeID The ID of the EdFiSubjectType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSubjectType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSubjectType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSubjectType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiSubjectType <- function(EdFiSubjectTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiSubjectTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiSubjectType", objectId = EdFiSubjectTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiSubjectType
	#'
	#' This function deletes an EdFiSubjectType
	#' @param EdFiSubjectTypeID The ID of the EdFiSubjectType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiSubjectTypeID of the deleted EdFiSubjectType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiSubjectType <- function(EdFiSubjectTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiSubjectType", objectId = EdFiSubjectTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiSubjectType
	#'
	#' This function creates an EdFiSubjectType
	#' @param fieldNames The field values to give the created EdFiSubjectType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiSubjectType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiSubjectType", body = list(DataObject = body), searchFields = append("EdFiSubjectTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiSubjectType
	#'
	#' This function modifies an EdFiSubjectType
	#' @param fieldNames The field values to give the modified EdFiSubjectType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiSubjectType <- function(EdFiSubjectTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiSubjectType", objectId = EdFiSubjectTypeID, body = list(DataObject = body), searchFields = append("EdFiSubjectTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiTermTypes
	#'
	#' This function returns a dataframe or json object of EdFiTermTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiTermTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiTermTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiTermType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiTermTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiTermTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiTermTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiTermType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiTermType
	#'
	#' This function returns a dataframe or json object of an EdFiTermType
	#' @param EdFiTermTypeID The ID of the EdFiTermType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiTermType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiTermType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiTermType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiTermType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiTermType <- function(EdFiTermTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiTermTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiTermType", objectId = EdFiTermTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiTermType
	#'
	#' This function deletes an EdFiTermType
	#' @param EdFiTermTypeID The ID of the EdFiTermType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiTermTypeID of the deleted EdFiTermType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiTermType <- function(EdFiTermTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiTermType", objectId = EdFiTermTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiTermType
	#'
	#' This function creates an EdFiTermType
	#' @param fieldNames The field values to give the created EdFiTermType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiTermType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiTermType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiTermType", body = list(DataObject = body), searchFields = append("EdFiTermTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiTermType
	#'
	#' This function modifies an EdFiTermType
	#' @param fieldNames The field values to give the modified EdFiTermType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiTermType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiTermType <- function(EdFiTermTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiTermType", objectId = EdFiTermTypeID, body = list(DataObject = body), searchFields = append("EdFiTermTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ChildSupportExtractRunIncludedPayrollRuns
	#'
	#' This function returns a dataframe or json object of ChildSupportExtractRunIncludedPayrollRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExtractRunIncludedPayrollRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExtractRunIncludedPayrollRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportExtractRunIncludedPayrollRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ChildSupportExtractRunIncludedPayrollRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listChildSupportExtractRunIncludedPayrollRuns <- function(searchConditionsList = NULL, ChildSupportExtractRunID = F, ChildSupportExtractRunIncludedPayrollRunID = F, CreatedTime = F, ModifiedTime = F, PayrollRunID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ChildSupportExtractRunIncludedPayrollRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ChildSupportExtractRunIncludedPayrollRun
	#'
	#' This function returns a dataframe or json object of a ChildSupportExtractRunIncludedPayrollRun
	#' @param ChildSupportExtractRunIncludedPayrollRunID The ID of the ChildSupportExtractRunIncludedPayrollRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExtractRunIncludedPayrollRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExtractRunIncludedPayrollRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportExtractRunIncludedPayrollRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ChildSupportExtractRunIncludedPayrollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getChildSupportExtractRunIncludedPayrollRun <- function(ChildSupportExtractRunIncludedPayrollRunID, ChildSupportExtractRunID = F, CreatedTime = F, ModifiedTime = F, PayrollRunID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ChildSupportExtractRunIncludedPayrollRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ChildSupportExtractRunIncludedPayrollRun", objectId = ChildSupportExtractRunIncludedPayrollRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ChildSupportExtractRunIncludedPayrollRun
	#'
	#' This function deletes a ChildSupportExtractRunIncludedPayrollRun
	#' @param ChildSupportExtractRunIncludedPayrollRunID The ID of the ChildSupportExtractRunIncludedPayrollRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ChildSupportExtractRunIncludedPayrollRunID of the deleted ChildSupportExtractRunIncludedPayrollRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteChildSupportExtractRunIncludedPayrollRun <- function(ChildSupportExtractRunIncludedPayrollRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ChildSupportExtractRunIncludedPayrollRun", objectId = ChildSupportExtractRunIncludedPayrollRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ChildSupportExtractRunIncludedPayrollRun
	#'
	#' This function creates a ChildSupportExtractRunIncludedPayrollRun
	#' @param fieldNames The field values to give the created ChildSupportExtractRunIncludedPayrollRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ChildSupportExtractRunIncludedPayrollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createChildSupportExtractRunIncludedPayrollRun <- function(ChildSupportExtractRunID = NULL, PayrollRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ChildSupportExtractRunIncludedPayrollRun", body = list(DataObject = body), searchFields = append("ChildSupportExtractRunIncludedPayrollRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ChildSupportExtractRunIncludedPayrollRun
	#'
	#' This function modifies a ChildSupportExtractRunIncludedPayrollRun
	#' @param fieldNames The field values to give the modified ChildSupportExtractRunIncludedPayrollRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ChildSupportExtractRunIncludedPayrollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyChildSupportExtractRunIncludedPayrollRun <- function(ChildSupportExtractRunIncludedPayrollRunID, ChildSupportExtractRunID = NULL, PayrollRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ChildSupportExtractRunIncludedPayrollRun", objectId = ChildSupportExtractRunIncludedPayrollRunID, body = list(DataObject = body), searchFields = append("ChildSupportExtractRunIncludedPayrollRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ChildSupportDetailV1s
	#'
	#' This function returns a dataframe or json object of ChildSupportDetailV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportDetailV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportDetailV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportDetailV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ChildSupportDetailV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listChildSupportDetailV1s <- function(searchConditionsList = NULL, AmountWithheld = F, CaseNumber = F, CauseNumber = F, ChildSupportDetailV1ID = F, ChildSupportExtractRunID = F, CreatedTime = F, EmployeeID = F, FIPSCode = F, FirstName = F, HasMedicalCoverage = F, IsEmployed = F, LastName = F, ModifiedTime = F, SocialSecurityNumber = F, StateCode = F, StateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ChildSupportDetailV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ChildSupportDetailV1
	#'
	#' This function returns a dataframe or json object of a ChildSupportDetailV1
	#' @param ChildSupportDetailV1ID The ID of the ChildSupportDetailV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportDetailV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportDetailV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportDetailV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ChildSupportDetailV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getChildSupportDetailV1 <- function(ChildSupportDetailV1ID, AmountWithheld = F, CaseNumber = F, CauseNumber = F, ChildSupportExtractRunID = F, CreatedTime = F, EmployeeID = F, FIPSCode = F, FirstName = F, HasMedicalCoverage = F, IsEmployed = F, LastName = F, ModifiedTime = F, SocialSecurityNumber = F, StateCode = F, StateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ChildSupportDetailV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ChildSupportDetailV1", objectId = ChildSupportDetailV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ChildSupportDetailV1
	#'
	#' This function deletes a ChildSupportDetailV1
	#' @param ChildSupportDetailV1ID The ID of the ChildSupportDetailV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ChildSupportDetailV1ID of the deleted ChildSupportDetailV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteChildSupportDetailV1 <- function(ChildSupportDetailV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ChildSupportDetailV1", objectId = ChildSupportDetailV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ChildSupportDetailV1
	#'
	#' This function creates a ChildSupportDetailV1
	#' @param fieldNames The field values to give the created ChildSupportDetailV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ChildSupportDetailV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createChildSupportDetailV1 <- function(AmountWithheld = NULL, CaseNumber = NULL, CauseNumber = NULL, ChildSupportExtractRunID = NULL, EmployeeID = NULL, FIPSCode = NULL, FirstName = NULL, HasMedicalCoverage = NULL, IsEmployed = NULL, LastName = NULL, SocialSecurityNumber = NULL, StateCode = NULL, StateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ChildSupportDetailV1", body = list(DataObject = body), searchFields = append("ChildSupportDetailV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ChildSupportDetailV1
	#'
	#' This function modifies a ChildSupportDetailV1
	#' @param fieldNames The field values to give the modified ChildSupportDetailV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ChildSupportDetailV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyChildSupportDetailV1 <- function(ChildSupportDetailV1ID, AmountWithheld = NULL, CaseNumber = NULL, CauseNumber = NULL, ChildSupportExtractRunID = NULL, EmployeeID = NULL, FIPSCode = NULL, FirstName = NULL, HasMedicalCoverage = NULL, IsEmployed = NULL, LastName = NULL, SocialSecurityNumber = NULL, StateCode = NULL, StateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ChildSupportDetailV1", objectId = ChildSupportDetailV1ID, body = list(DataObject = body), searchFields = append("ChildSupportDetailV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ChildSupportExceptions
	#'
	#' This function returns a dataframe or json object of ChildSupportExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ChildSupportExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listChildSupportExceptions <- function(searchConditionsList = NULL, ChildSupportDetailV1ID = F, ChildSupportExceptionID = F, ChildSupportExtractRunID = F, CreatedTime = F, EmployeeID = F, FirstName = F, IsFatal = F, LastName = F, Message = F, ModifiedTime = F, PreserveException = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ChildSupportException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ChildSupportException
	#'
	#' This function returns a dataframe or json object of a ChildSupportException
	#' @param ChildSupportExceptionID The ID of the ChildSupportException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ChildSupportException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getChildSupportException <- function(ChildSupportExceptionID, ChildSupportDetailV1ID = F, ChildSupportExtractRunID = F, CreatedTime = F, EmployeeID = F, FirstName = F, IsFatal = F, LastName = F, Message = F, ModifiedTime = F, PreserveException = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ChildSupportExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ChildSupportException", objectId = ChildSupportExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ChildSupportException
	#'
	#' This function deletes a ChildSupportException
	#' @param ChildSupportExceptionID The ID of the ChildSupportException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ChildSupportExceptionID of the deleted ChildSupportException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteChildSupportException <- function(ChildSupportExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ChildSupportException", objectId = ChildSupportExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ChildSupportException
	#'
	#' This function creates a ChildSupportException
	#' @param fieldNames The field values to give the created ChildSupportException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ChildSupportException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createChildSupportException <- function(ChildSupportDetailV1ID = NULL, ChildSupportExtractRunID = NULL, EmployeeID = NULL, IsFatal = NULL, Message = NULL, PreserveException = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ChildSupportException", body = list(DataObject = body), searchFields = append("ChildSupportExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ChildSupportException
	#'
	#' This function modifies a ChildSupportException
	#' @param fieldNames The field values to give the modified ChildSupportException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ChildSupportException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyChildSupportException <- function(ChildSupportExceptionID, ChildSupportDetailV1ID = NULL, ChildSupportExtractRunID = NULL, EmployeeID = NULL, IsFatal = NULL, Message = NULL, PreserveException = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ChildSupportException", objectId = ChildSupportExceptionID, body = list(DataObject = body), searchFields = append("ChildSupportExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ChildSupportExtractRuns
	#'
	#' This function returns a dataframe or json object of ChildSupportExtractRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExtractRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExtractRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportExtractRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ChildSupportExtractRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listChildSupportExtractRuns <- function(searchConditionsList = NULL, ChildSupportExtractRunID = F, CreatedTime = F, DistrictID = F, ExtractedEmployeeCount = F, FatalExceptionCount = F, HasCauseNumber = F, LatestExport = F, LatestSentOrAcceptedExportTime = F, ModifiedTime = F, NonFatalExceptionCount = F, PostingDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ChildSupportExtractRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ChildSupportExtractRun
	#'
	#' This function returns a dataframe or json object of a ChildSupportExtractRun
	#' @param ChildSupportExtractRunID The ID of the ChildSupportExtractRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExtractRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExtractRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportExtractRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ChildSupportExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getChildSupportExtractRun <- function(ChildSupportExtractRunID, CreatedTime = F, DistrictID = F, ExtractedEmployeeCount = F, FatalExceptionCount = F, HasCauseNumber = F, LatestExport = F, LatestSentOrAcceptedExportTime = F, ModifiedTime = F, NonFatalExceptionCount = F, PostingDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ChildSupportExtractRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ChildSupportExtractRun", objectId = ChildSupportExtractRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ChildSupportExtractRun
	#'
	#' This function deletes a ChildSupportExtractRun
	#' @param ChildSupportExtractRunID The ID of the ChildSupportExtractRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ChildSupportExtractRunID of the deleted ChildSupportExtractRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteChildSupportExtractRun <- function(ChildSupportExtractRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ChildSupportExtractRun", objectId = ChildSupportExtractRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ChildSupportExtractRun
	#'
	#' This function creates a ChildSupportExtractRun
	#' @param fieldNames The field values to give the created ChildSupportExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ChildSupportExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createChildSupportExtractRun <- function(DistrictID = NULL, PostingDate = NULL, Version = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ChildSupportExtractRun", body = list(DataObject = body), searchFields = append("ChildSupportExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ChildSupportExtractRun
	#'
	#' This function modifies a ChildSupportExtractRun
	#' @param fieldNames The field values to give the modified ChildSupportExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ChildSupportExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyChildSupportExtractRun <- function(ChildSupportExtractRunID, DistrictID = NULL, PostingDate = NULL, Version = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ChildSupportExtractRun", objectId = ChildSupportExtractRunID, body = list(DataObject = body), searchFields = append("ChildSupportExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ChildSupportExports
	#'
	#' This function returns a dataframe or json object of ChildSupportExports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportExport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ChildSupportExports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listChildSupportExports <- function(searchConditionsList = NULL, ChildSupportExportID = F, ChildSupportExtractRunID = F, CreatedTime = F, MediaID = F, ModifiedTime = F, Status = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ChildSupportExport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ChildSupportExport
	#'
	#' This function returns a dataframe or json object of a ChildSupportExport
	#' @param ChildSupportExportID The ID of the ChildSupportExport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ChildSupportExport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ChildSupportExport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ChildSupportExport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ChildSupportExport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getChildSupportExport <- function(ChildSupportExportID, ChildSupportExtractRunID = F, CreatedTime = F, MediaID = F, ModifiedTime = F, Status = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ChildSupportExportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ChildSupportExport", objectId = ChildSupportExportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ChildSupportExport
	#'
	#' This function deletes a ChildSupportExport
	#' @param ChildSupportExportID The ID of the ChildSupportExport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ChildSupportExportID of the deleted ChildSupportExport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteChildSupportExport <- function(ChildSupportExportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ChildSupportExport", objectId = ChildSupportExportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ChildSupportExport
	#'
	#' This function creates a ChildSupportExport
	#' @param fieldNames The field values to give the created ChildSupportExport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ChildSupportExport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createChildSupportExport <- function(ChildSupportExtractRunID = NULL, MediaID = NULL, Status = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ChildSupportExport", body = list(DataObject = body), searchFields = append("ChildSupportExportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ChildSupportExport
	#'
	#' This function modifies a ChildSupportExport
	#' @param fieldNames The field values to give the modified ChildSupportExport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ChildSupportExport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyChildSupportExport <- function(ChildSupportExportID, ChildSupportExtractRunID = NULL, MediaID = NULL, Status = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ChildSupportExport", objectId = ChildSupportExportID, body = list(DataObject = body), searchFields = append("ChildSupportExportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ServiceDescriptorDashboardV1s
	#'
	#' This function returns a dataframe or json object of ServiceDescriptorDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ServiceDescriptorDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ServiceDescriptorDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ServiceDescriptorDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ServiceDescriptorDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listServiceDescriptorDashboardV1s <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DashboardRunHistoryID = F, Description = F, DistrictID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, ServiceDescriptorDashboardV1ID = F, ShortDescription = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ServiceDescriptorDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ServiceDescriptorDashboardV1
	#'
	#' This function returns a dataframe or json object of a ServiceDescriptorDashboardV1
	#' @param ServiceDescriptorDashboardV1ID The ID of the ServiceDescriptorDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ServiceDescriptorDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ServiceDescriptorDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ServiceDescriptorDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ServiceDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getServiceDescriptorDashboardV1 <- function(ServiceDescriptorDashboardV1ID, Code = F, CreatedTime = F, DashboardRunHistoryID = F, Description = F, DistrictID = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, ShortDescription = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ServiceDescriptorDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ServiceDescriptorDashboardV1", objectId = ServiceDescriptorDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ServiceDescriptorDashboardV1
	#'
	#' This function deletes a ServiceDescriptorDashboardV1
	#' @param ServiceDescriptorDashboardV1ID The ID of the ServiceDescriptorDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ServiceDescriptorDashboardV1ID of the deleted ServiceDescriptorDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteServiceDescriptorDashboardV1 <- function(ServiceDescriptorDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ServiceDescriptorDashboardV1", objectId = ServiceDescriptorDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ServiceDescriptorDashboardV1
	#'
	#' This function creates a ServiceDescriptorDashboardV1
	#' @param fieldNames The field values to give the created ServiceDescriptorDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ServiceDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createServiceDescriptorDashboardV1 <- function(Code = NULL, DashboardRunHistoryID = NULL, Description = NULL, DistrictID = NULL, SchoolYearID = NULL, ShortDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ServiceDescriptorDashboardV1", body = list(DataObject = body), searchFields = append("ServiceDescriptorDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ServiceDescriptorDashboardV1
	#'
	#' This function modifies a ServiceDescriptorDashboardV1
	#' @param fieldNames The field values to give the modified ServiceDescriptorDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ServiceDescriptorDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyServiceDescriptorDashboardV1 <- function(ServiceDescriptorDashboardV1ID, Code = NULL, DashboardRunHistoryID = NULL, Description = NULL, DistrictID = NULL, SchoolYearID = NULL, ShortDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ServiceDescriptorDashboardV1", objectId = ServiceDescriptorDashboardV1ID, body = list(DataObject = body), searchFields = append("ServiceDescriptorDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentBilingualProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentBilingualProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentBilingualProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentBilingualProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentBilingualProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentBilingualProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentBilingualProgramDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentBilingualProgramDashboardV1ID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentBilingualProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentBilingualProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentBilingualProgramDashboardV1
	#' @param StudentBilingualProgramDashboardV1ID The ID of the StudentBilingualProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentBilingualProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentBilingualProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentBilingualProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentBilingualProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentBilingualProgramDashboardV1 <- function(StudentBilingualProgramDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentBilingualProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentBilingualProgramDashboardV1", objectId = StudentBilingualProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentBilingualProgramDashboardV1
	#'
	#' This function deletes a StudentBilingualProgramDashboardV1
	#' @param StudentBilingualProgramDashboardV1ID The ID of the StudentBilingualProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentBilingualProgramDashboardV1ID of the deleted StudentBilingualProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentBilingualProgramDashboardV1 <- function(StudentBilingualProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentBilingualProgramDashboardV1", objectId = StudentBilingualProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentBilingualProgramDashboardV1
	#'
	#' This function creates a StudentBilingualProgramDashboardV1
	#' @param fieldNames The field values to give the created StudentBilingualProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentBilingualProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentBilingualProgramDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentBilingualProgramDashboardV1", body = list(DataObject = body), searchFields = append("StudentBilingualProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentBilingualProgramDashboardV1
	#'
	#' This function modifies a StudentBilingualProgramDashboardV1
	#' @param fieldNames The field values to give the modified StudentBilingualProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentBilingualProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentBilingualProgramDashboardV1 <- function(StudentBilingualProgramDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentBilingualProgramDashboardV1", objectId = StudentBilingualProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentBilingualProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentCTEProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentCTEProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCTEProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCTEProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCTEProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentCTEProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentCTEProgramDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentCTEProgramDashboardV1ID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentCTEProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentCTEProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentCTEProgramDashboardV1
	#' @param StudentCTEProgramDashboardV1ID The ID of the StudentCTEProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCTEProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCTEProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCTEProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentCTEProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentCTEProgramDashboardV1 <- function(StudentCTEProgramDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentCTEProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentCTEProgramDashboardV1", objectId = StudentCTEProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentCTEProgramDashboardV1
	#'
	#' This function deletes a StudentCTEProgramDashboardV1
	#' @param StudentCTEProgramDashboardV1ID The ID of the StudentCTEProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentCTEProgramDashboardV1ID of the deleted StudentCTEProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentCTEProgramDashboardV1 <- function(StudentCTEProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentCTEProgramDashboardV1", objectId = StudentCTEProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentCTEProgramDashboardV1
	#'
	#' This function creates a StudentCTEProgramDashboardV1
	#' @param fieldNames The field values to give the created StudentCTEProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentCTEProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentCTEProgramDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentCTEProgramDashboardV1", body = list(DataObject = body), searchFields = append("StudentCTEProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentCTEProgramDashboardV1
	#'
	#' This function modifies a StudentCTEProgramDashboardV1
	#' @param fieldNames The field values to give the modified StudentCTEProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentCTEProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentCTEProgramDashboardV1 <- function(StudentCTEProgramDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentCTEProgramDashboardV1", objectId = StudentCTEProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentCTEProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentESLProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentESLProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentESLProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentESLProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentESLProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentESLProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentESLProgramDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentESLProgramDashboardV1ID = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentESLProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentESLProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentESLProgramDashboardV1
	#' @param StudentESLProgramDashboardV1ID The ID of the StudentESLProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentESLProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentESLProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentESLProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentESLProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentESLProgramDashboardV1 <- function(StudentESLProgramDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentESLProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentESLProgramDashboardV1", objectId = StudentESLProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentESLProgramDashboardV1
	#'
	#' This function deletes a StudentESLProgramDashboardV1
	#' @param StudentESLProgramDashboardV1ID The ID of the StudentESLProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentESLProgramDashboardV1ID of the deleted StudentESLProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentESLProgramDashboardV1 <- function(StudentESLProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentESLProgramDashboardV1", objectId = StudentESLProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentESLProgramDashboardV1
	#'
	#' This function creates a StudentESLProgramDashboardV1
	#' @param fieldNames The field values to give the created StudentESLProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentESLProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentESLProgramDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentESLProgramDashboardV1", body = list(DataObject = body), searchFields = append("StudentESLProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentESLProgramDashboardV1
	#'
	#' This function modifies a StudentESLProgramDashboardV1
	#' @param fieldNames The field values to give the modified StudentESLProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentESLProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentESLProgramDashboardV1 <- function(StudentESLProgramDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentESLProgramDashboardV1", objectId = StudentESLProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentESLProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentProgramDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentProgramDashboardV1ID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentProgramDashboardV1
	#' @param StudentProgramDashboardV1ID The ID of the StudentProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentProgramDashboardV1 <- function(StudentProgramDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentProgramDashboardV1", objectId = StudentProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentProgramDashboardV1
	#'
	#' This function deletes a StudentProgramDashboardV1
	#' @param StudentProgramDashboardV1ID The ID of the StudentProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentProgramDashboardV1ID of the deleted StudentProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentProgramDashboardV1 <- function(StudentProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentProgramDashboardV1", objectId = StudentProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentProgramDashboardV1
	#'
	#' This function creates a StudentProgramDashboardV1
	#' @param fieldNames The field values to give the created StudentProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentProgramDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentProgramDashboardV1", body = list(DataObject = body), searchFields = append("StudentProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentProgramDashboardV1
	#'
	#' This function modifies a StudentProgramDashboardV1
	#' @param fieldNames The field values to give the modified StudentProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentProgramDashboardV1 <- function(StudentProgramDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentProgramDashboardV1", objectId = StudentProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSpecialEdProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentSpecialEdProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSpecialEdProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSpecialEdProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSpecialEdProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentSpecialEdProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSpecialEdProgramDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentSpecialEdProgramDashboardV1ID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentSpecialEdProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSpecialEdProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentSpecialEdProgramDashboardV1
	#' @param StudentSpecialEdProgramDashboardV1ID The ID of the StudentSpecialEdProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSpecialEdProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSpecialEdProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSpecialEdProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentSpecialEdProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSpecialEdProgramDashboardV1 <- function(StudentSpecialEdProgramDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentUniqueID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSpecialEdProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentSpecialEdProgramDashboardV1", objectId = StudentSpecialEdProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSpecialEdProgramDashboardV1
	#'
	#' This function deletes a StudentSpecialEdProgramDashboardV1
	#' @param StudentSpecialEdProgramDashboardV1ID The ID of the StudentSpecialEdProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentSpecialEdProgramDashboardV1ID of the deleted StudentSpecialEdProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSpecialEdProgramDashboardV1 <- function(StudentSpecialEdProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentSpecialEdProgramDashboardV1", objectId = StudentSpecialEdProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSpecialEdProgramDashboardV1
	#'
	#' This function creates a StudentSpecialEdProgramDashboardV1
	#' @param fieldNames The field values to give the created StudentSpecialEdProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentSpecialEdProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSpecialEdProgramDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentSpecialEdProgramDashboardV1", body = list(DataObject = body), searchFields = append("StudentSpecialEdProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSpecialEdProgramDashboardV1
	#'
	#' This function modifies a StudentSpecialEdProgramDashboardV1
	#' @param fieldNames The field values to give the modified StudentSpecialEdProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentSpecialEdProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSpecialEdProgramDashboardV1 <- function(StudentSpecialEdProgramDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentSpecialEdProgramDashboardV1", objectId = StudentSpecialEdProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentSpecialEdProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentTitleIPartAProgramDashboardV1s
	#'
	#' This function returns a dataframe or json object of StudentTitleIPartAProgramDashboardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentTitleIPartAProgramDashboardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentTitleIPartAProgramDashboardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentTitleIPartAProgramDashboardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StudentTitleIPartAProgramDashboardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentTitleIPartAProgramDashboardV1s <- function(searchConditionsList = NULL, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentTitleIPartAProgramDashboardV1ID = F, StudentUniqueID = F, TitleIPartAParticipant = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StudentTitleIPartAProgramDashboardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentTitleIPartAProgramDashboardV1
	#'
	#' This function returns a dataframe or json object of a StudentTitleIPartAProgramDashboardV1
	#' @param StudentTitleIPartAProgramDashboardV1ID The ID of the StudentTitleIPartAProgramDashboardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentTitleIPartAProgramDashboardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentTitleIPartAProgramDashboardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentTitleIPartAProgramDashboardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StudentTitleIPartAProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentTitleIPartAProgramDashboardV1 <- function(StudentTitleIPartAProgramDashboardV1ID, BeginDate = F, CreatedTime = F, DashboardRunHistoryID = F, DistrictCode = F, EdFiProgramTypeID = F, EndDate = F, EntityID = F, KeyHash = F, ModifiedTime = F, ProgramTypeCode = F, SchoolCode = F, SchoolYearID = F, ServiceShortDescriptions = F, StudentID = F, StudentUniqueID = F, TitleIPartAParticipant = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentTitleIPartAProgramDashboardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StudentTitleIPartAProgramDashboardV1", objectId = StudentTitleIPartAProgramDashboardV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentTitleIPartAProgramDashboardV1
	#'
	#' This function deletes a StudentTitleIPartAProgramDashboardV1
	#' @param StudentTitleIPartAProgramDashboardV1ID The ID of the StudentTitleIPartAProgramDashboardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StudentTitleIPartAProgramDashboardV1ID of the deleted StudentTitleIPartAProgramDashboardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentTitleIPartAProgramDashboardV1 <- function(StudentTitleIPartAProgramDashboardV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StudentTitleIPartAProgramDashboardV1", objectId = StudentTitleIPartAProgramDashboardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentTitleIPartAProgramDashboardV1
	#'
	#' This function creates a StudentTitleIPartAProgramDashboardV1
	#' @param fieldNames The field values to give the created StudentTitleIPartAProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StudentTitleIPartAProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentTitleIPartAProgramDashboardV1 <- function(BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, TitleIPartAParticipant = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StudentTitleIPartAProgramDashboardV1", body = list(DataObject = body), searchFields = append("StudentTitleIPartAProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentTitleIPartAProgramDashboardV1
	#'
	#' This function modifies a StudentTitleIPartAProgramDashboardV1
	#' @param fieldNames The field values to give the modified StudentTitleIPartAProgramDashboardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StudentTitleIPartAProgramDashboardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentTitleIPartAProgramDashboardV1 <- function(StudentTitleIPartAProgramDashboardV1ID, BeginDate = NULL, DashboardRunHistoryID = NULL, DistrictCode = NULL, EdFiProgramTypeID = NULL, EndDate = NULL, EntityID = NULL, ProgramTypeCode = NULL, SchoolCode = NULL, SchoolYearID = NULL, ServiceShortDescriptions = NULL, StudentID = NULL, StudentUniqueID = NULL, TitleIPartAParticipant = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StudentTitleIPartAProgramDashboardV1", objectId = StudentTitleIPartAProgramDashboardV1ID, body = list(DataObject = body), searchFields = append("StudentTitleIPartAProgramDashboardV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBColumns
	#'
	#' This function returns a dataframe or json object of GASBColumns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBColumns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBColumns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBColumn') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBColumns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBColumns <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DistrictID = F, FiscalYearID = F, GASBColumnID = F, GASBColumnType = F, GASBFundClassification = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBColumn", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBColumn
	#'
	#' This function returns a dataframe or json object of a GASBColumn
	#' @param GASBColumnID The ID of the GASBColumn to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBColumn. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBColumn.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBColumn') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBColumn <- function(GASBColumnID, CreatedTime = F, Description = F, DistrictID = F, FiscalYearID = F, GASBColumnType = F, GASBFundClassification = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBColumnID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBColumn", objectId = GASBColumnID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBColumn
	#'
	#' This function deletes a GASBColumn
	#' @param GASBColumnID The ID of the GASBColumn to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBColumnID of the deleted GASBColumn.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBColumn <- function(GASBColumnID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBColumn", objectId = GASBColumnID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBColumn
	#'
	#' This function creates a GASBColumn
	#' @param fieldNames The field values to give the created GASBColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBColumn <- function(Description = NULL, DistrictID = NULL, FiscalYearID = NULL, GASBColumnType = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBColumn", body = list(DataObject = body), searchFields = append("GASBColumnID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBColumn
	#'
	#' This function modifies a GASBColumn
	#' @param fieldNames The field values to give the modified GASBColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBColumn <- function(GASBColumnID, Description = NULL, DistrictID = NULL, FiscalYearID = NULL, GASBColumnType = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBColumn", objectId = GASBColumnID, body = list(DataObject = body), searchFields = append("GASBColumnID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRows
	#'
	#' This function returns a dataframe or json object of GASBRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRows <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DistrictID = F, FiscalYearID = F, GASBRowID = F, ModifiedTime = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRow
	#'
	#' This function returns a dataframe or json object of a GASBRow
	#' @param GASBRowID The ID of the GASBRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRow <- function(GASBRowID, CreatedTime = F, Description = F, DistrictID = F, FiscalYearID = F, ModifiedTime = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRow", objectId = GASBRowID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRow
	#'
	#' This function deletes a GASBRow
	#' @param GASBRowID The ID of the GASBRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRowID of the deleted GASBRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRow <- function(GASBRowID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRow", objectId = GASBRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRow
	#'
	#' This function creates a GASBRow
	#' @param fieldNames The field values to give the created GASBRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRow <- function(Description = NULL, DistrictID = NULL, FiscalYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRow", body = list(DataObject = body), searchFields = append("GASBRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRow
	#'
	#' This function modifies a GASBRow
	#' @param fieldNames The field values to give the modified GASBRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRow <- function(GASBRowID, Description = NULL, DistrictID = NULL, FiscalYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRow", objectId = GASBRowID, body = list(DataObject = body), searchFields = append("GASBRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateGASBReportTypes
	#'
	#' This function returns a dataframe or json object of StateGASBReportTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateGASBReportTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateGASBReportTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateGASBReportType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateGASBReportTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateGASBReportTypes <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, ModifiedTime = F, ReportType = F, SkywardHash = F, SkywardID = F, StateGASBReportTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateGASBReportType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateGASBReportType
	#'
	#' This function returns a dataframe or json object of a StateGASBReportType
	#' @param StateGASBReportTypeID The ID of the StateGASBReportType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateGASBReportType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateGASBReportType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateGASBReportType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateGASBReportType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateGASBReportType <- function(StateGASBReportTypeID, CreatedTime = F, Description = F, ModifiedTime = F, ReportType = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateGASBReportTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateGASBReportType", objectId = StateGASBReportTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateGASBReportType
	#'
	#' This function deletes a StateGASBReportType
	#' @param StateGASBReportTypeID The ID of the StateGASBReportType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateGASBReportTypeID of the deleted StateGASBReportType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateGASBReportType <- function(StateGASBReportTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateGASBReportType", objectId = StateGASBReportTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateGASBReportType
	#'
	#' This function creates a StateGASBReportType
	#' @param fieldNames The field values to give the created StateGASBReportType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateGASBReportType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateGASBReportType <- function(Description = NULL, ReportType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateGASBReportType", body = list(DataObject = body), searchFields = append("StateGASBReportTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateGASBReportType
	#'
	#' This function modifies a StateGASBReportType
	#' @param fieldNames The field values to give the modified StateGASBReportType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateGASBReportType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateGASBReportType <- function(StateGASBReportTypeID, Description = NULL, ReportType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateGASBReportType", objectId = StateGASBReportTypeID, body = list(DataObject = body), searchFields = append("StateGASBReportTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBExtractRuns
	#'
	#' This function returns a dataframe or json object of GASBExtractRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBExtractRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBExtractRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBExtractRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBExtractRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBExtractRuns <- function(searchConditionsList = NULL, CreatedTime = F, FiscalYearID = F, GASBExtractRunID = F, GASBFundClassification = F, HasBalanceSheet = F, HasRevenueAndExpenditure = F, HasRevenueAndExpenditureComparison = F, HasStatementOfActivities = F, HasStatementOfNetAssets = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBExtractRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBExtractRun
	#'
	#' This function returns a dataframe or json object of a GASBExtractRun
	#' @param GASBExtractRunID The ID of the GASBExtractRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBExtractRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBExtractRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBExtractRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBExtractRun <- function(GASBExtractRunID, CreatedTime = F, FiscalYearID = F, GASBFundClassification = F, HasBalanceSheet = F, HasRevenueAndExpenditure = F, HasRevenueAndExpenditureComparison = F, HasStatementOfActivities = F, HasStatementOfNetAssets = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBExtractRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBExtractRun", objectId = GASBExtractRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBExtractRun
	#'
	#' This function deletes a GASBExtractRun
	#' @param GASBExtractRunID The ID of the GASBExtractRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBExtractRunID of the deleted GASBExtractRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBExtractRun <- function(GASBExtractRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBExtractRun", objectId = GASBExtractRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBExtractRun
	#'
	#' This function creates a GASBExtractRun
	#' @param fieldNames The field values to give the created GASBExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBExtractRun <- function(FiscalYearID = NULL, GASBFundClassification = NULL, HasBalanceSheet = NULL, HasRevenueAndExpenditure = NULL, HasRevenueAndExpenditureComparison = NULL, HasStatementOfActivities = NULL, HasStatementOfNetAssets = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBExtractRun", body = list(DataObject = body), searchFields = append("GASBExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBExtractRun
	#'
	#' This function modifies a GASBExtractRun
	#' @param fieldNames The field values to give the modified GASBExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBExtractRun <- function(GASBExtractRunID, FiscalYearID = NULL, GASBFundClassification = NULL, HasBalanceSheet = NULL, HasRevenueAndExpenditure = NULL, HasRevenueAndExpenditureComparison = NULL, HasStatementOfActivities = NULL, HasStatementOfNetAssets = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBExtractRun", objectId = GASBExtractRunID, body = list(DataObject = body), searchFields = append("GASBExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityCells
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityCells
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityCells. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityCells.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityCell') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityCells
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityCells <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, GASBStatementOfActivityCellID = F, GASBStatementOfActivityRowID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityCell", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityCell
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityCell
	#' @param GASBStatementOfActivityCellID The ID of the GASBStatementOfActivityCell to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityCell. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityCell.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityCell') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityCell <- function(GASBStatementOfActivityCellID, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, GASBStatementOfActivityRowID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityCellID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityCell", objectId = GASBStatementOfActivityCellID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityCell
	#'
	#' This function deletes a GASBStatementOfActivityCell
	#' @param GASBStatementOfActivityCellID The ID of the GASBStatementOfActivityCell to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityCellID of the deleted GASBStatementOfActivityCell.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityCell <- function(GASBStatementOfActivityCellID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityCell", objectId = GASBStatementOfActivityCellID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityCell
	#'
	#' This function creates a GASBStatementOfActivityCell
	#' @param fieldNames The field values to give the created GASBStatementOfActivityCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityCell <- function(Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, GASBStatementOfActivityRowID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityCell", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityCell
	#'
	#' This function modifies a GASBStatementOfActivityCell
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityCell <- function(GASBStatementOfActivityCellID, Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, GASBStatementOfActivityRowID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityCell", objectId = GASBStatementOfActivityCellID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityRows
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityRows <- function(searchConditionsList = NULL, CreatedTime = F, Expenses = F, GASBExtractRunID = F, GASBStatementOfActivityReportID = F, GASBStatementOfActivityRowID = F, GovernmentalNetAmount = F, IsGovernmental = F, IsProprietary = F, ModifiedTime = F, ProprietaryNetAmount = F, RowDescription = F, TotalNetAmount = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityRow
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityRow
	#' @param GASBStatementOfActivityRowID The ID of the GASBStatementOfActivityRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityRow <- function(GASBStatementOfActivityRowID, CreatedTime = F, Expenses = F, GASBExtractRunID = F, GASBStatementOfActivityReportID = F, GovernmentalNetAmount = F, IsGovernmental = F, IsProprietary = F, ModifiedTime = F, ProprietaryNetAmount = F, RowDescription = F, TotalNetAmount = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityRow", objectId = GASBStatementOfActivityRowID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityRow
	#'
	#' This function deletes a GASBStatementOfActivityRow
	#' @param GASBStatementOfActivityRowID The ID of the GASBStatementOfActivityRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityRowID of the deleted GASBStatementOfActivityRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityRow <- function(GASBStatementOfActivityRowID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityRow", objectId = GASBStatementOfActivityRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityRow
	#'
	#' This function creates a GASBStatementOfActivityRow
	#' @param fieldNames The field values to give the created GASBStatementOfActivityRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityRow <- function(Expenses = NULL, GASBExtractRunID = NULL, GASBStatementOfActivityReportID = NULL, GovernmentalNetAmount = NULL, IsGovernmental = NULL, IsProprietary = NULL, ProprietaryNetAmount = NULL, RowDescription = NULL, TotalNetAmount = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityRow", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityRow
	#'
	#' This function modifies a GASBStatementOfActivityRow
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityRow <- function(GASBStatementOfActivityRowID, Expenses = NULL, GASBExtractRunID = NULL, GASBStatementOfActivityReportID = NULL, GovernmentalNetAmount = NULL, IsGovernmental = NULL, IsProprietary = NULL, ProprietaryNetAmount = NULL, RowDescription = NULL, TotalNetAmount = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityRow", objectId = GASBStatementOfActivityRowID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityIncludedAccounts <- function(searchConditionsList = NULL, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBStatementOfActivityIncludedAccountID = F, GASBStatementOfActivityRowID = F, IsGovernmental = F, IsProprietary = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityIncludedAccount
	#' @param GASBStatementOfActivityIncludedAccountID The ID of the GASBStatementOfActivityIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityIncludedAccount <- function(GASBStatementOfActivityIncludedAccountID, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBStatementOfActivityRowID = F, IsGovernmental = F, IsProprietary = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityIncludedAccount", objectId = GASBStatementOfActivityIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityIncludedAccount
	#'
	#' This function deletes a GASBStatementOfActivityIncludedAccount
	#' @param GASBStatementOfActivityIncludedAccountID The ID of the GASBStatementOfActivityIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityIncludedAccountID of the deleted GASBStatementOfActivityIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityIncludedAccount <- function(GASBStatementOfActivityIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityIncludedAccount", objectId = GASBStatementOfActivityIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityIncludedAccount
	#'
	#' This function creates a GASBStatementOfActivityIncludedAccount
	#' @param fieldNames The field values to give the created GASBStatementOfActivityIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityIncludedAccount <- function(AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBStatementOfActivityRowID = NULL, IsGovernmental = NULL, IsProprietary = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityIncludedAccount", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityIncludedAccount
	#'
	#' This function modifies a GASBStatementOfActivityIncludedAccount
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityIncludedAccount <- function(GASBStatementOfActivityIncludedAccountID, AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBStatementOfActivityRowID = NULL, IsGovernmental = NULL, IsProprietary = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityIncludedAccount", objectId = GASBStatementOfActivityIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfNetAssetsRows
	#'
	#' This function returns a dataframe or json object of GASBStatementOfNetAssetsRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfNetAssetsRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfNetAssetsRows <- function(searchConditionsList = NULL, CreatedTime = F, GASBExtractRunID = F, GASBStatementOfNetAssetsReportID = F, GASBStatementOfNetAssetsRowID = F, ModifiedTime = F, RowDescription = F, TotalFunds = F, TotalGovernmentalFunds = F, TotalProprietaryFunds = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfNetAssetsRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfNetAssetsRow
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfNetAssetsRow
	#' @param GASBStatementOfNetAssetsRowID The ID of the GASBStatementOfNetAssetsRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfNetAssetsRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfNetAssetsRow <- function(GASBStatementOfNetAssetsRowID, CreatedTime = F, GASBExtractRunID = F, GASBStatementOfNetAssetsReportID = F, ModifiedTime = F, RowDescription = F, TotalFunds = F, TotalGovernmentalFunds = F, TotalProprietaryFunds = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfNetAssetsRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsRow", objectId = GASBStatementOfNetAssetsRowID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfNetAssetsRow
	#'
	#' This function deletes a GASBStatementOfNetAssetsRow
	#' @param GASBStatementOfNetAssetsRowID The ID of the GASBStatementOfNetAssetsRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfNetAssetsRowID of the deleted GASBStatementOfNetAssetsRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfNetAssetsRow <- function(GASBStatementOfNetAssetsRowID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsRow", objectId = GASBStatementOfNetAssetsRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfNetAssetsRow
	#'
	#' This function creates a GASBStatementOfNetAssetsRow
	#' @param fieldNames The field values to give the created GASBStatementOfNetAssetsRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfNetAssetsRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfNetAssetsRow <- function(GASBExtractRunID = NULL, GASBStatementOfNetAssetsReportID = NULL, RowDescription = NULL, TotalFunds = NULL, TotalGovernmentalFunds = NULL, TotalProprietaryFunds = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsRow", body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfNetAssetsRow
	#'
	#' This function modifies a GASBStatementOfNetAssetsRow
	#' @param fieldNames The field values to give the modified GASBStatementOfNetAssetsRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfNetAssetsRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfNetAssetsRow <- function(GASBStatementOfNetAssetsRowID, GASBExtractRunID = NULL, GASBStatementOfNetAssetsReportID = NULL, RowDescription = NULL, TotalFunds = NULL, TotalGovernmentalFunds = NULL, TotalProprietaryFunds = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsRow", objectId = GASBStatementOfNetAssetsRowID, body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfNetAssetsIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBStatementOfNetAssetsIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfNetAssetsIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfNetAssetsIncludedAccounts <- function(searchConditionsList = NULL, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBStatementOfNetAssetsIncludedAccountID = F, GASBStatementOfNetAssetsRowID = F, IsGovernmental = F, IsProprietary = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfNetAssetsIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfNetAssetsIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfNetAssetsIncludedAccount
	#' @param GASBStatementOfNetAssetsIncludedAccountID The ID of the GASBStatementOfNetAssetsIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfNetAssetsIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfNetAssetsIncludedAccount <- function(GASBStatementOfNetAssetsIncludedAccountID, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBStatementOfNetAssetsRowID = F, IsGovernmental = F, IsProprietary = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfNetAssetsIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsIncludedAccount", objectId = GASBStatementOfNetAssetsIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfNetAssetsIncludedAccount
	#'
	#' This function deletes a GASBStatementOfNetAssetsIncludedAccount
	#' @param GASBStatementOfNetAssetsIncludedAccountID The ID of the GASBStatementOfNetAssetsIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfNetAssetsIncludedAccountID of the deleted GASBStatementOfNetAssetsIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfNetAssetsIncludedAccount <- function(GASBStatementOfNetAssetsIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsIncludedAccount", objectId = GASBStatementOfNetAssetsIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfNetAssetsIncludedAccount
	#'
	#' This function creates a GASBStatementOfNetAssetsIncludedAccount
	#' @param fieldNames The field values to give the created GASBStatementOfNetAssetsIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfNetAssetsIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfNetAssetsIncludedAccount <- function(AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBStatementOfNetAssetsRowID = NULL, IsGovernmental = NULL, IsProprietary = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsIncludedAccount", body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfNetAssetsIncludedAccount
	#'
	#' This function modifies a GASBStatementOfNetAssetsIncludedAccount
	#' @param fieldNames The field values to give the modified GASBStatementOfNetAssetsIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfNetAssetsIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfNetAssetsIncludedAccount <- function(GASBStatementOfNetAssetsIncludedAccountID, AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBStatementOfNetAssetsRowID = NULL, IsGovernmental = NULL, IsProprietary = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsIncludedAccount", objectId = GASBStatementOfNetAssetsIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateReportingTempExceptions
	#'
	#' This function returns a dataframe or json object of StateReportingTempExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingTempExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingTempExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingTempException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateReportingTempExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateReportingTempExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Message = F, ModifiedTime = F, RecordIdentifier = F, TempExceptionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TempException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateReportingTempException
	#'
	#' This function returns a dataframe or json object of a StateReportingTempException
	#' @param StateReportingTempExceptionID The ID of the StateReportingTempException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingTempException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingTempException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingTempException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateReportingTempException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateReportingTempException <- function(StateReportingTempExceptionID, CreatedTime = F, Message = F, ModifiedTime = F, RecordIdentifier = F, TempExceptionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateReportingTempExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TempException", objectId = StateReportingTempExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateReportingTempException
	#'
	#' This function deletes a StateReportingTempException
	#' @param StateReportingTempExceptionID The ID of the StateReportingTempException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateReportingTempExceptionID of the deleted StateReportingTempException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateReportingTempException <- function(StateReportingTempExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TempException", objectId = StateReportingTempExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateReportingTempException
	#'
	#' This function creates a StateReportingTempException
	#' @param fieldNames The field values to give the created StateReportingTempException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateReportingTempException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateReportingTempException <- function(Message = NULL, RecordIdentifier = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TempException", body = list(DataObject = body), searchFields = append("TempExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateReportingTempException
	#'
	#' This function modifies a StateReportingTempException
	#' @param fieldNames The field values to give the modified StateReportingTempException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateReportingTempException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateReportingTempException <- function(TempExceptionID, Message = NULL, RecordIdentifier = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TempException", objectId = TempExceptionID, body = list(DataObject = body), searchFields = append("TempExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SubmissionSummaries
	#'
	#' This function returns a dataframe or json object of SubmissionSummaries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SubmissionSummaries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SubmissionSummaries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SubmissionSummary') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of SubmissionSummaries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSubmissionSummaries <- function(searchConditionsList = NULL, ComplexType = F, FatalErrorCount = F, Interchange = F, RecordCount = F, SecondaryRecordCount = F, SkywardFatalErrorCount = F, SpecialWarningCount = F, TotalErrorCount = F, WarningCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "SubmissionSummary", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SubmissionSummary
	#'
	#' This function returns a dataframe or json object of a SubmissionSummary
	#' @param SubmissionSummaryID The ID of the SubmissionSummary to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SubmissionSummary. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SubmissionSummary.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SubmissionSummary') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of SubmissionSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSubmissionSummary <- function(SubmissionSummaryID, ComplexType = F, FatalErrorCount = F, Interchange = F, RecordCount = F, SecondaryRecordCount = F, SkywardFatalErrorCount = F, SpecialWarningCount = F, TotalErrorCount = F, WarningCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SubmissionSummaryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "SubmissionSummary", objectId = SubmissionSummaryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SubmissionSummary
	#'
	#' This function deletes a SubmissionSummary
	#' @param SubmissionSummaryID The ID of the SubmissionSummary to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The SubmissionSummaryID of the deleted SubmissionSummary.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSubmissionSummary <- function(SubmissionSummaryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "SubmissionSummary", objectId = SubmissionSummaryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SubmissionSummary
	#'
	#' This function creates a SubmissionSummary
	#' @param fieldNames The field values to give the created SubmissionSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created SubmissionSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSubmissionSummary <- function(ComplexType = NULL, FatalErrorCount = NULL, Interchange = NULL, SecondaryRecordCount = NULL, SkywardFatalErrorCount = NULL, SpecialWarningCount = NULL, TotalErrorCount = NULL, WarningCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "SubmissionSummary", body = list(DataObject = body), searchFields = append("SubmissionSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SubmissionSummary
	#'
	#' This function modifies a SubmissionSummary
	#' @param fieldNames The field values to give the modified SubmissionSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified SubmissionSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySubmissionSummary <- function(SubmissionSummaryID, ComplexType = NULL, FatalErrorCount = NULL, Interchange = NULL, SecondaryRecordCount = NULL, SkywardFatalErrorCount = NULL, SpecialWarningCount = NULL, TotalErrorCount = NULL, WarningCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "SubmissionSummary", objectId = SubmissionSummaryID, body = list(DataObject = body), searchFields = append("SubmissionSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAVerificationReports
	#'
	#' This function returns a dataframe or json object of ACAVerificationReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAVerificationReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAVerificationReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAVerificationReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ACAVerificationReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAVerificationReports <- function(searchConditionsList = NULL, ACAEmployeeRequiredContribution = F, ACAOfferOfCoverageCode = F, ACASafeHarborCode = F, DistrictID = F, EmployeeACAOfferAndCoverageID = F, EmployeeID = F, EmployerOfferedSelfInsuredCoverage = F, FullNameLFM = F, PlanStartMonthOverride = F, ReportEmployeeAsCoveredIndividual = F, ReportingMonth = F, ReportingMonthCode = F, ReportingYear = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ACAVerificationReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAVerificationReport
	#'
	#' This function returns a dataframe or json object of an ACAVerificationReport
	#' @param ACAVerificationReportID The ID of the ACAVerificationReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAVerificationReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAVerificationReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAVerificationReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ACAVerificationReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAVerificationReport <- function(ACAVerificationReportID, ACAEmployeeRequiredContribution = F, ACAOfferOfCoverageCode = F, ACASafeHarborCode = F, DistrictID = F, EmployeeACAOfferAndCoverageID = F, EmployeeID = F, EmployerOfferedSelfInsuredCoverage = F, FullNameLFM = F, PlanStartMonthOverride = F, ReportEmployeeAsCoveredIndividual = F, ReportingMonth = F, ReportingMonthCode = F, ReportingYear = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAVerificationReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ACAVerificationReport", objectId = ACAVerificationReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAVerificationReport
	#'
	#' This function deletes an ACAVerificationReport
	#' @param ACAVerificationReportID The ID of the ACAVerificationReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ACAVerificationReportID of the deleted ACAVerificationReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAVerificationReport <- function(ACAVerificationReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ACAVerificationReport", objectId = ACAVerificationReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAVerificationReport
	#'
	#' This function creates an ACAVerificationReport
	#' @param fieldNames The field values to give the created ACAVerificationReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ACAVerificationReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAVerificationReport <- function(ACAEmployeeRequiredContribution = NULL, ACAOfferOfCoverageCode = NULL, ACASafeHarborCode = NULL, DistrictID = NULL, EmployeeACAOfferAndCoverageID = NULL, EmployerOfferedSelfInsuredCoverage = NULL, FullNameLFM = NULL, PlanStartMonthOverride = NULL, ReportEmployeeAsCoveredIndividual = NULL, ReportingMonth = NULL, ReportingMonthCode = NULL, ReportingYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ACAVerificationReport", body = list(DataObject = body), searchFields = append("ACAVerificationReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAVerificationReport
	#'
	#' This function modifies an ACAVerificationReport
	#' @param fieldNames The field values to give the modified ACAVerificationReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ACAVerificationReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAVerificationReport <- function(ACAVerificationReportID, ACAEmployeeRequiredContribution = NULL, ACAOfferOfCoverageCode = NULL, ACASafeHarborCode = NULL, DistrictID = NULL, EmployeeACAOfferAndCoverageID = NULL, EmployerOfferedSelfInsuredCoverage = NULL, FullNameLFM = NULL, PlanStartMonthOverride = NULL, ReportEmployeeAsCoveredIndividual = NULL, ReportingMonth = NULL, ReportingMonthCode = NULL, ReportingYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ACAVerificationReport", objectId = ACAVerificationReportID, body = list(DataObject = body), searchFields = append("ACAVerificationReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiReportMonths
	#'
	#' This function returns a dataframe or json object of EdFiReportMonths
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiReportMonths. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiReportMonths.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiReportMonth') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiReportMonths
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiReportMonths <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EdFiReportMonthID = F, IsInvalid = F, ModifiedTime = F, NumericMonth = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiReportMonth", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiReportMonth
	#'
	#' This function returns a dataframe or json object of an EdFiReportMonth
	#' @param EdFiReportMonthID The ID of the EdFiReportMonth to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiReportMonth. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiReportMonth.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiReportMonth') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiReportMonth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiReportMonth <- function(EdFiReportMonthID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, NumericMonth = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiReportMonthID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiReportMonth", objectId = EdFiReportMonthID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiReportMonth
	#'
	#' This function deletes an EdFiReportMonth
	#' @param EdFiReportMonthID The ID of the EdFiReportMonth to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiReportMonthID of the deleted EdFiReportMonth.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiReportMonth <- function(EdFiReportMonthID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiReportMonth", objectId = EdFiReportMonthID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiReportMonth
	#'
	#' This function creates an EdFiReportMonth
	#' @param fieldNames The field values to give the created EdFiReportMonth. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiReportMonth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiReportMonth <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiReportMonth", body = list(DataObject = body), searchFields = append("EdFiReportMonthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiReportMonth
	#'
	#' This function modifies an EdFiReportMonth
	#' @param fieldNames The field values to give the modified EdFiReportMonth. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiReportMonth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiReportMonth <- function(EdFiReportMonthID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiReportMonth", objectId = EdFiReportMonthID, body = list(DataObject = body), searchFields = append("EdFiReportMonthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRowDimensionFilterRanges
	#'
	#' This function returns a dataframe or json object of GASBRowDimensionFilterRanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRowDimensionFilterRanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRowDimensionFilterRanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRowDimensionFilterRange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRowDimensionFilterRanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRowDimensionFilterRanges <- function(searchConditionsList = NULL, CreatedTime = F, FilterHigh = F, FilterLow = F, GASBRowDimensionFilterRangeID = F, GASBRowID = F, ModifiedTime = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRowDimensionFilterRange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRowDimensionFilterRange
	#'
	#' This function returns a dataframe or json object of a GASBRowDimensionFilterRange
	#' @param GASBRowDimensionFilterRangeID The ID of the GASBRowDimensionFilterRange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRowDimensionFilterRange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRowDimensionFilterRange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRowDimensionFilterRange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRowDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRowDimensionFilterRange <- function(GASBRowDimensionFilterRangeID, CreatedTime = F, FilterHigh = F, FilterLow = F, GASBRowID = F, ModifiedTime = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRowDimensionFilterRangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRowDimensionFilterRange", objectId = GASBRowDimensionFilterRangeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRowDimensionFilterRange
	#'
	#' This function deletes a GASBRowDimensionFilterRange
	#' @param GASBRowDimensionFilterRangeID The ID of the GASBRowDimensionFilterRange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRowDimensionFilterRangeID of the deleted GASBRowDimensionFilterRange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRowDimensionFilterRange <- function(GASBRowDimensionFilterRangeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRowDimensionFilterRange", objectId = GASBRowDimensionFilterRangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRowDimensionFilterRange
	#'
	#' This function creates a GASBRowDimensionFilterRange
	#' @param fieldNames The field values to give the created GASBRowDimensionFilterRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRowDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRowDimensionFilterRange <- function(FilterHigh = NULL, FilterLow = NULL, GASBRowID = NULL, StructureDimensionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRowDimensionFilterRange", body = list(DataObject = body), searchFields = append("GASBRowDimensionFilterRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRowDimensionFilterRange
	#'
	#' This function modifies a GASBRowDimensionFilterRange
	#' @param fieldNames The field values to give the modified GASBRowDimensionFilterRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRowDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRowDimensionFilterRange <- function(GASBRowDimensionFilterRangeID, FilterHigh = NULL, FilterLow = NULL, GASBRowID = NULL, StructureDimensionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRowDimensionFilterRange", objectId = GASBRowDimensionFilterRangeID, body = list(DataObject = body), searchFields = append("GASBRowDimensionFilterRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBColumnDimensionFilterRanges
	#'
	#' This function returns a dataframe or json object of GASBColumnDimensionFilterRanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBColumnDimensionFilterRanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBColumnDimensionFilterRanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBColumnDimensionFilterRange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBColumnDimensionFilterRanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBColumnDimensionFilterRanges <- function(searchConditionsList = NULL, CreatedTime = F, FilterHigh = F, FilterLow = F, GASBColumnDimensionFilterRangeID = F, GASBColumnID = F, ModifiedTime = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBColumnDimensionFilterRange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBColumnDimensionFilterRange
	#'
	#' This function returns a dataframe or json object of a GASBColumnDimensionFilterRange
	#' @param GASBColumnDimensionFilterRangeID The ID of the GASBColumnDimensionFilterRange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBColumnDimensionFilterRange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBColumnDimensionFilterRange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBColumnDimensionFilterRange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBColumnDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBColumnDimensionFilterRange <- function(GASBColumnDimensionFilterRangeID, CreatedTime = F, FilterHigh = F, FilterLow = F, GASBColumnID = F, ModifiedTime = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBColumnDimensionFilterRangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBColumnDimensionFilterRange", objectId = GASBColumnDimensionFilterRangeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBColumnDimensionFilterRange
	#'
	#' This function deletes a GASBColumnDimensionFilterRange
	#' @param GASBColumnDimensionFilterRangeID The ID of the GASBColumnDimensionFilterRange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBColumnDimensionFilterRangeID of the deleted GASBColumnDimensionFilterRange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBColumnDimensionFilterRange <- function(GASBColumnDimensionFilterRangeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBColumnDimensionFilterRange", objectId = GASBColumnDimensionFilterRangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBColumnDimensionFilterRange
	#'
	#' This function creates a GASBColumnDimensionFilterRange
	#' @param fieldNames The field values to give the created GASBColumnDimensionFilterRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBColumnDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBColumnDimensionFilterRange <- function(FilterHigh = NULL, FilterLow = NULL, GASBColumnID = NULL, StructureDimensionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBColumnDimensionFilterRange", body = list(DataObject = body), searchFields = append("GASBColumnDimensionFilterRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBColumnDimensionFilterRange
	#'
	#' This function modifies a GASBColumnDimensionFilterRange
	#' @param fieldNames The field values to give the modified GASBColumnDimensionFilterRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBColumnDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBColumnDimensionFilterRange <- function(GASBColumnDimensionFilterRangeID, FilterHigh = NULL, FilterLow = NULL, GASBColumnID = NULL, StructureDimensionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBColumnDimensionFilterRange", objectId = GASBColumnDimensionFilterRangeID, body = list(DataObject = body), searchFields = append("GASBColumnDimensionFilterRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityReports
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityReports <- function(searchConditionsList = NULL, CreatedTime = F, GASBExtractRunID = F, GASBStatementOfActivityReportID = F, ModifiedTime = F, NetAssetsGovernmentalBeginningBalance = F, NetAssetsGovernmentalEndingBalance = F, NetAssetsProprietaryBeginningBalance = F, NetAssetsProprietaryEndingBalance = F, NetAssetsTotalBeginningBalance = F, NetAssetsTotalEndingBalance = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityReport
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityReport
	#' @param GASBStatementOfActivityReportID The ID of the GASBStatementOfActivityReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityReport <- function(GASBStatementOfActivityReportID, CreatedTime = F, GASBExtractRunID = F, ModifiedTime = F, NetAssetsGovernmentalBeginningBalance = F, NetAssetsGovernmentalEndingBalance = F, NetAssetsProprietaryBeginningBalance = F, NetAssetsProprietaryEndingBalance = F, NetAssetsTotalBeginningBalance = F, NetAssetsTotalEndingBalance = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityReport", objectId = GASBStatementOfActivityReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityReport
	#'
	#' This function deletes a GASBStatementOfActivityReport
	#' @param GASBStatementOfActivityReportID The ID of the GASBStatementOfActivityReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityReportID of the deleted GASBStatementOfActivityReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityReport <- function(GASBStatementOfActivityReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityReport", objectId = GASBStatementOfActivityReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityReport
	#'
	#' This function creates a GASBStatementOfActivityReport
	#' @param fieldNames The field values to give the created GASBStatementOfActivityReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityReport <- function(GASBExtractRunID = NULL, NetAssetsGovernmentalBeginningBalance = NULL, NetAssetsGovernmentalEndingBalance = NULL, NetAssetsProprietaryBeginningBalance = NULL, NetAssetsProprietaryEndingBalance = NULL, NetAssetsTotalBeginningBalance = NULL, NetAssetsTotalEndingBalance = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityReport", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityReport
	#'
	#' This function modifies a GASBStatementOfActivityReport
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityReport <- function(GASBStatementOfActivityReportID, GASBExtractRunID = NULL, NetAssetsGovernmentalBeginningBalance = NULL, NetAssetsGovernmentalEndingBalance = NULL, NetAssetsProprietaryBeginningBalance = NULL, NetAssetsProprietaryEndingBalance = NULL, NetAssetsTotalBeginningBalance = NULL, NetAssetsTotalEndingBalance = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityReport", objectId = GASBStatementOfActivityReportID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityColumnIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityColumnIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityColumnIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityColumnIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityColumnIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityColumnIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityColumnIncludedAccounts <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, GASBColumnID = F, GASBStatementOfActivityColumnIncludedAccountID = F, GASBStatementOfActivityIncludedAccountID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityColumnIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityColumnIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityColumnIncludedAccount
	#' @param GASBStatementOfActivityColumnIncludedAccountID The ID of the GASBStatementOfActivityColumnIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityColumnIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityColumnIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityColumnIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityColumnIncludedAccount <- function(GASBStatementOfActivityColumnIncludedAccountID, CreatedTime = F, Description = F, GASBColumnID = F, GASBStatementOfActivityIncludedAccountID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityColumnIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityColumnIncludedAccount", objectId = GASBStatementOfActivityColumnIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityColumnIncludedAccount
	#'
	#' This function deletes a GASBStatementOfActivityColumnIncludedAccount
	#' @param GASBStatementOfActivityColumnIncludedAccountID The ID of the GASBStatementOfActivityColumnIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityColumnIncludedAccountID of the deleted GASBStatementOfActivityColumnIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityColumnIncludedAccount <- function(GASBStatementOfActivityColumnIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityColumnIncludedAccount", objectId = GASBStatementOfActivityColumnIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityColumnIncludedAccount
	#'
	#' This function creates a GASBStatementOfActivityColumnIncludedAccount
	#' @param fieldNames The field values to give the created GASBStatementOfActivityColumnIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityColumnIncludedAccount <- function(Description = NULL, GASBColumnID = NULL, GASBStatementOfActivityIncludedAccountID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityColumnIncludedAccount", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityColumnIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityColumnIncludedAccount
	#'
	#' This function modifies a GASBStatementOfActivityColumnIncludedAccount
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityColumnIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityColumnIncludedAccount <- function(GASBStatementOfActivityColumnIncludedAccountID, Description = NULL, GASBColumnID = NULL, GASBStatementOfActivityIncludedAccountID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityColumnIncludedAccount", objectId = GASBStatementOfActivityColumnIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityColumnIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfNetAssetsReports
	#'
	#' This function returns a dataframe or json object of GASBStatementOfNetAssetsReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfNetAssetsReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfNetAssetsReports <- function(searchConditionsList = NULL, CreatedTime = F, GASBExtractRunID = F, GASBStatementOfNetAssetsReportID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfNetAssetsReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfNetAssetsReport
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfNetAssetsReport
	#' @param GASBStatementOfNetAssetsReportID The ID of the GASBStatementOfNetAssetsReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfNetAssetsReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfNetAssetsReport <- function(GASBStatementOfNetAssetsReportID, CreatedTime = F, GASBExtractRunID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfNetAssetsReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsReport", objectId = GASBStatementOfNetAssetsReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfNetAssetsReport
	#'
	#' This function deletes a GASBStatementOfNetAssetsReport
	#' @param GASBStatementOfNetAssetsReportID The ID of the GASBStatementOfNetAssetsReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfNetAssetsReportID of the deleted GASBStatementOfNetAssetsReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfNetAssetsReport <- function(GASBStatementOfNetAssetsReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsReport", objectId = GASBStatementOfNetAssetsReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfNetAssetsReport
	#'
	#' This function creates a GASBStatementOfNetAssetsReport
	#' @param fieldNames The field values to give the created GASBStatementOfNetAssetsReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfNetAssetsReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfNetAssetsReport <- function(GASBExtractRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsReport", body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfNetAssetsReport
	#'
	#' This function modifies a GASBStatementOfNetAssetsReport
	#' @param fieldNames The field values to give the modified GASBStatementOfNetAssetsReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfNetAssetsReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfNetAssetsReport <- function(GASBStatementOfNetAssetsReportID, GASBExtractRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsReport", objectId = GASBStatementOfNetAssetsReportID, body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityTotalViews
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityTotalViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityTotalViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityTotalViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityTotalView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityTotalViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityTotalViews <- function(searchConditionsList = NULL, DisplayOrder = F, Expenses = F, GASBExtractRunID = F, GASBStatementOfActivityTotalViewID = F, GovernmentalNetAmount = F, ProprietaryNetAmount = F, StatementOfActivityTotalType = F, TotalNetAmount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityTotalView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityTotalView
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityTotalView
	#' @param GASBStatementOfActivityTotalViewID The ID of the GASBStatementOfActivityTotalView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityTotalView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityTotalView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityTotalView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityTotalView <- function(GASBStatementOfActivityTotalViewID, DisplayOrder = F, Expenses = F, GASBExtractRunID = F, GovernmentalNetAmount = F, ProprietaryNetAmount = F, StatementOfActivityTotalType = F, TotalNetAmount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityTotalViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityTotalView", objectId = GASBStatementOfActivityTotalViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityTotalView
	#'
	#' This function deletes a GASBStatementOfActivityTotalView
	#' @param GASBStatementOfActivityTotalViewID The ID of the GASBStatementOfActivityTotalView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityTotalViewID of the deleted GASBStatementOfActivityTotalView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityTotalView <- function(GASBStatementOfActivityTotalViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityTotalView", objectId = GASBStatementOfActivityTotalViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityTotalView
	#'
	#' This function creates a GASBStatementOfActivityTotalView
	#' @param fieldNames The field values to give the created GASBStatementOfActivityTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityTotalView <- function(DisplayOrder = NULL, Expenses = NULL, GASBExtractRunID = NULL, GovernmentalNetAmount = NULL, ProprietaryNetAmount = NULL, StatementOfActivityTotalType = NULL, TotalNetAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityTotalView", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityTotalView
	#'
	#' This function modifies a GASBStatementOfActivityTotalView
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityTotalView <- function(GASBStatementOfActivityTotalViewID, DisplayOrder = NULL, Expenses = NULL, GASBExtractRunID = NULL, GovernmentalNetAmount = NULL, ProprietaryNetAmount = NULL, StatementOfActivityTotalType = NULL, TotalNetAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityTotalView", objectId = GASBStatementOfActivityTotalViewID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfActivityViewCells
	#'
	#' This function returns a dataframe or json object of GASBStatementOfActivityViewCells
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityViewCells. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityViewCells.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityViewCell') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfActivityViewCells
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfActivityViewCells <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, GASBStatementOfActivityViewCellID = F, ModifiedTime = F, StatementOfActivityTotalType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfActivityViewCell", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfActivityViewCell
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfActivityViewCell
	#' @param GASBStatementOfActivityViewCellID The ID of the GASBStatementOfActivityViewCell to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfActivityViewCell. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfActivityViewCell.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfActivityViewCell') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfActivityViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfActivityViewCell <- function(GASBStatementOfActivityViewCellID, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, ModifiedTime = F, StatementOfActivityTotalType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfActivityViewCellID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityViewCell", objectId = GASBStatementOfActivityViewCellID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfActivityViewCell
	#'
	#' This function deletes a GASBStatementOfActivityViewCell
	#' @param GASBStatementOfActivityViewCellID The ID of the GASBStatementOfActivityViewCell to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfActivityViewCellID of the deleted GASBStatementOfActivityViewCell.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfActivityViewCell <- function(GASBStatementOfActivityViewCellID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityViewCell", objectId = GASBStatementOfActivityViewCellID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfActivityViewCell
	#'
	#' This function creates a GASBStatementOfActivityViewCell
	#' @param fieldNames The field values to give the created GASBStatementOfActivityViewCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfActivityViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfActivityViewCell <- function(Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, StatementOfActivityTotalType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityViewCell", body = list(DataObject = body), searchFields = append("GASBStatementOfActivityViewCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfActivityViewCell
	#'
	#' This function modifies a GASBStatementOfActivityViewCell
	#' @param fieldNames The field values to give the modified GASBStatementOfActivityViewCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfActivityViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfActivityViewCell <- function(GASBStatementOfActivityViewCellID, Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, StatementOfActivityTotalType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfActivityViewCell", objectId = GASBStatementOfActivityViewCellID, body = list(DataObject = body), searchFields = append("GASBStatementOfActivityViewCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBStatementOfNetAssetsTotalViews
	#'
	#' This function returns a dataframe or json object of GASBStatementOfNetAssetsTotalViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsTotalViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsTotalViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsTotalView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBStatementOfNetAssetsTotalViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBStatementOfNetAssetsTotalViews <- function(searchConditionsList = NULL, DisplayOrder = F, GASBExtractRunID = F, GovernmentalNetAmount = F, ProprietaryNetAmount = F, StatementOfNetAssetsTotalType = F, TotalNetAmount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBStatementOfNetAssetsTotalView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBStatementOfNetAssetsTotalView
	#'
	#' This function returns a dataframe or json object of a GASBStatementOfNetAssetsTotalView
	#' @param GASBStatementOfNetAssetsTotalViewID The ID of the GASBStatementOfNetAssetsTotalView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBStatementOfNetAssetsTotalView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBStatementOfNetAssetsTotalView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBStatementOfNetAssetsTotalView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBStatementOfNetAssetsTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBStatementOfNetAssetsTotalView <- function(GASBStatementOfNetAssetsTotalViewID, DisplayOrder = F, GASBExtractRunID = F, GovernmentalNetAmount = F, ProprietaryNetAmount = F, StatementOfNetAssetsTotalType = F, TotalNetAmount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBStatementOfNetAssetsTotalViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsTotalView", objectId = GASBStatementOfNetAssetsTotalViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBStatementOfNetAssetsTotalView
	#'
	#' This function deletes a GASBStatementOfNetAssetsTotalView
	#' @param GASBStatementOfNetAssetsTotalViewID The ID of the GASBStatementOfNetAssetsTotalView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBStatementOfNetAssetsTotalViewID of the deleted GASBStatementOfNetAssetsTotalView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBStatementOfNetAssetsTotalView <- function(GASBStatementOfNetAssetsTotalViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsTotalView", objectId = GASBStatementOfNetAssetsTotalViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBStatementOfNetAssetsTotalView
	#'
	#' This function creates a GASBStatementOfNetAssetsTotalView
	#' @param fieldNames The field values to give the created GASBStatementOfNetAssetsTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBStatementOfNetAssetsTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBStatementOfNetAssetsTotalView <- function(DisplayOrder = NULL, GovernmentalNetAmount = NULL, ProprietaryNetAmount = NULL, StatementOfNetAssetsTotalType = NULL, TotalNetAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsTotalView", body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBStatementOfNetAssetsTotalView
	#'
	#' This function modifies a GASBStatementOfNetAssetsTotalView
	#' @param fieldNames The field values to give the modified GASBStatementOfNetAssetsTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBStatementOfNetAssetsTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBStatementOfNetAssetsTotalView <- function(GASBStatementOfNetAssetsTotalViewID, DisplayOrder = NULL, GovernmentalNetAmount = NULL, ProprietaryNetAmount = NULL, StatementOfNetAssetsTotalType = NULL, TotalNetAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBStatementOfNetAssetsTotalView", objectId = GASBStatementOfNetAssetsTotalViewID, body = list(DataObject = body), searchFields = append("GASBStatementOfNetAssetsTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetColumnIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetColumnIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetColumnIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetColumnIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetColumnIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetColumnIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetColumnIncludedAccounts <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, GASBBalanceSheetColumnIncludedAccountID = F, GASBBalanceSheetIncludedAccountID = F, GASBColumnID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetColumnIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetColumnIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetColumnIncludedAccount
	#' @param GASBBalanceSheetColumnIncludedAccountID The ID of the GASBBalanceSheetColumnIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetColumnIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetColumnIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetColumnIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetColumnIncludedAccount <- function(GASBBalanceSheetColumnIncludedAccountID, CreatedTime = F, Description = F, GASBBalanceSheetIncludedAccountID = F, GASBColumnID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetColumnIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetColumnIncludedAccount", objectId = GASBBalanceSheetColumnIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetColumnIncludedAccount
	#'
	#' This function deletes a GASBBalanceSheetColumnIncludedAccount
	#' @param GASBBalanceSheetColumnIncludedAccountID The ID of the GASBBalanceSheetColumnIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetColumnIncludedAccountID of the deleted GASBBalanceSheetColumnIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetColumnIncludedAccount <- function(GASBBalanceSheetColumnIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetColumnIncludedAccount", objectId = GASBBalanceSheetColumnIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetColumnIncludedAccount
	#'
	#' This function creates a GASBBalanceSheetColumnIncludedAccount
	#' @param fieldNames The field values to give the created GASBBalanceSheetColumnIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetColumnIncludedAccount <- function(Description = NULL, GASBBalanceSheetIncludedAccountID = NULL, GASBColumnID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetColumnIncludedAccount", body = list(DataObject = body), searchFields = append("GASBBalanceSheetColumnIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetColumnIncludedAccount
	#'
	#' This function modifies a GASBBalanceSheetColumnIncludedAccount
	#' @param fieldNames The field values to give the modified GASBBalanceSheetColumnIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetColumnIncludedAccount <- function(GASBBalanceSheetColumnIncludedAccountID, Description = NULL, GASBBalanceSheetIncludedAccountID = NULL, GASBColumnID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetColumnIncludedAccount", objectId = GASBBalanceSheetColumnIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetColumnIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetReports
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetReports <- function(searchConditionsList = NULL, CreatedTime = F, GASBBalanceSheetReportID = F, GASBExtractRunID = F, GASBFundClassification = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetReport
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetReport
	#' @param GASBBalanceSheetReportID The ID of the GASBBalanceSheetReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetReport <- function(GASBBalanceSheetReportID, CreatedTime = F, GASBExtractRunID = F, GASBFundClassification = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetReport", objectId = GASBBalanceSheetReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetReport
	#'
	#' This function deletes a GASBBalanceSheetReport
	#' @param GASBBalanceSheetReportID The ID of the GASBBalanceSheetReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetReportID of the deleted GASBBalanceSheetReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetReport <- function(GASBBalanceSheetReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetReport", objectId = GASBBalanceSheetReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetReport
	#'
	#' This function creates a GASBBalanceSheetReport
	#' @param fieldNames The field values to give the created GASBBalanceSheetReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetReport <- function(GASBExtractRunID = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetReport", body = list(DataObject = body), searchFields = append("GASBBalanceSheetReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetReport
	#'
	#' This function modifies a GASBBalanceSheetReport
	#' @param fieldNames The field values to give the modified GASBBalanceSheetReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetReport <- function(GASBBalanceSheetReportID, GASBExtractRunID = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetReport", objectId = GASBBalanceSheetReportID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetRows
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetRows <- function(searchConditionsList = NULL, CreatedTime = F, GASBBalanceSheetReportID = F, GASBBalanceSheetRowID = F, GASBExtractRunID = F, ModifiedTime = F, RowDescription = F, TotalFunds = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetRow
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetRow
	#' @param GASBBalanceSheetRowID The ID of the GASBBalanceSheetRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetRow <- function(GASBBalanceSheetRowID, CreatedTime = F, GASBBalanceSheetReportID = F, GASBExtractRunID = F, ModifiedTime = F, RowDescription = F, TotalFunds = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetRow", objectId = GASBBalanceSheetRowID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetRow
	#'
	#' This function deletes a GASBBalanceSheetRow
	#' @param GASBBalanceSheetRowID The ID of the GASBBalanceSheetRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetRowID of the deleted GASBBalanceSheetRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetRow <- function(GASBBalanceSheetRowID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetRow", objectId = GASBBalanceSheetRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetRow
	#'
	#' This function creates a GASBBalanceSheetRow
	#' @param fieldNames The field values to give the created GASBBalanceSheetRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetRow <- function(GASBBalanceSheetReportID = NULL, GASBExtractRunID = NULL, RowDescription = NULL, TotalFunds = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetRow", body = list(DataObject = body), searchFields = append("GASBBalanceSheetRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetRow
	#'
	#' This function modifies a GASBBalanceSheetRow
	#' @param fieldNames The field values to give the modified GASBBalanceSheetRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetRow <- function(GASBBalanceSheetRowID, GASBBalanceSheetReportID = NULL, GASBExtractRunID = NULL, RowDescription = NULL, TotalFunds = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetRow", objectId = GASBBalanceSheetRowID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetCells
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetCells
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetCells. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetCells.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetCell') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetCells
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetCells <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBBalanceSheetCellID = F, GASBBalanceSheetRowID = F, GASBColumnID = F, GASBExtractRunID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetCell", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetCell
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetCell
	#' @param GASBBalanceSheetCellID The ID of the GASBBalanceSheetCell to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetCell. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetCell.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetCell') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetCell <- function(GASBBalanceSheetCellID, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBBalanceSheetRowID = F, GASBColumnID = F, GASBExtractRunID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetCellID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetCell", objectId = GASBBalanceSheetCellID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetCell
	#'
	#' This function deletes a GASBBalanceSheetCell
	#' @param GASBBalanceSheetCellID The ID of the GASBBalanceSheetCell to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetCellID of the deleted GASBBalanceSheetCell.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetCell <- function(GASBBalanceSheetCellID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetCell", objectId = GASBBalanceSheetCellID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetCell
	#'
	#' This function creates a GASBBalanceSheetCell
	#' @param fieldNames The field values to give the created GASBBalanceSheetCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetCell <- function(Description = NULL, GASBBalanceSheetRowID = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetCell", body = list(DataObject = body), searchFields = append("GASBBalanceSheetCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetCell
	#'
	#' This function modifies a GASBBalanceSheetCell
	#' @param fieldNames The field values to give the modified GASBBalanceSheetCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetCell <- function(GASBBalanceSheetCellID, Description = NULL, GASBBalanceSheetRowID = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetCell", objectId = GASBBalanceSheetCellID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetIncludedAccounts <- function(searchConditionsList = NULL, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBBalanceSheetIncludedAccountID = F, GASBBalanceSheetRowID = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetIncludedAccount
	#' @param GASBBalanceSheetIncludedAccountID The ID of the GASBBalanceSheetIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetIncludedAccount <- function(GASBBalanceSheetIncludedAccountID, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBBalanceSheetRowID = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetIncludedAccount", objectId = GASBBalanceSheetIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetIncludedAccount
	#'
	#' This function deletes a GASBBalanceSheetIncludedAccount
	#' @param GASBBalanceSheetIncludedAccountID The ID of the GASBBalanceSheetIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetIncludedAccountID of the deleted GASBBalanceSheetIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetIncludedAccount <- function(GASBBalanceSheetIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetIncludedAccount", objectId = GASBBalanceSheetIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetIncludedAccount
	#'
	#' This function creates a GASBBalanceSheetIncludedAccount
	#' @param fieldNames The field values to give the created GASBBalanceSheetIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetIncludedAccount <- function(AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBBalanceSheetRowID = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetIncludedAccount", body = list(DataObject = body), searchFields = append("GASBBalanceSheetIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetIncludedAccount
	#'
	#' This function modifies a GASBBalanceSheetIncludedAccount
	#' @param fieldNames The field values to give the modified GASBBalanceSheetIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetIncludedAccount <- function(GASBBalanceSheetIncludedAccountID, AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBBalanceSheetRowID = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetIncludedAccount", objectId = GASBBalanceSheetIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetTotalViews
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetTotalViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetTotalViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetTotalViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetTotalView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetTotalViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetTotalViews <- function(searchConditionsList = NULL, BalanceSheetTotalType = F, DisplayOrder = F, GASBBalanceSheetTotalViewID = F, GASBExtractRunID = F, TotalFunds = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetTotalView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetTotalView
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetTotalView
	#' @param GASBBalanceSheetTotalViewID The ID of the GASBBalanceSheetTotalView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetTotalView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetTotalView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetTotalView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetTotalView <- function(GASBBalanceSheetTotalViewID, BalanceSheetTotalType = F, DisplayOrder = F, GASBExtractRunID = F, TotalFunds = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetTotalViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetTotalView", objectId = GASBBalanceSheetTotalViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetTotalView
	#'
	#' This function deletes a GASBBalanceSheetTotalView
	#' @param GASBBalanceSheetTotalViewID The ID of the GASBBalanceSheetTotalView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetTotalViewID of the deleted GASBBalanceSheetTotalView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetTotalView <- function(GASBBalanceSheetTotalViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetTotalView", objectId = GASBBalanceSheetTotalViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetTotalView
	#'
	#' This function creates a GASBBalanceSheetTotalView
	#' @param fieldNames The field values to give the created GASBBalanceSheetTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetTotalView <- function(BalanceSheetTotalType = NULL, DisplayOrder = NULL, GASBExtractRunID = NULL, TotalFunds = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetTotalView", body = list(DataObject = body), searchFields = append("GASBBalanceSheetTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetTotalView
	#'
	#' This function modifies a GASBBalanceSheetTotalView
	#' @param fieldNames The field values to give the modified GASBBalanceSheetTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetTotalView <- function(GASBBalanceSheetTotalViewID, BalanceSheetTotalType = NULL, DisplayOrder = NULL, GASBExtractRunID = NULL, TotalFunds = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetTotalView", objectId = GASBBalanceSheetTotalViewID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBBalanceSheetViewCells
	#'
	#' This function returns a dataframe or json object of GASBBalanceSheetViewCells
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetViewCells. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetViewCells.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetViewCell') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBBalanceSheetViewCells
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBBalanceSheetViewCells <- function(searchConditionsList = NULL, BalanceSheetTotalType = F, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBBalanceSheetViewCellID = F, GASBColumnID = F, GASBExtractRunID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBBalanceSheetViewCell", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBBalanceSheetViewCell
	#'
	#' This function returns a dataframe or json object of a GASBBalanceSheetViewCell
	#' @param GASBBalanceSheetViewCellID The ID of the GASBBalanceSheetViewCell to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBBalanceSheetViewCell. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBBalanceSheetViewCell.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBBalanceSheetViewCell') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBBalanceSheetViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBBalanceSheetViewCell <- function(GASBBalanceSheetViewCellID, BalanceSheetTotalType = F, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBBalanceSheetViewCellID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetViewCell", objectId = GASBBalanceSheetViewCellID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBBalanceSheetViewCell
	#'
	#' This function deletes a GASBBalanceSheetViewCell
	#' @param GASBBalanceSheetViewCellID The ID of the GASBBalanceSheetViewCell to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBBalanceSheetViewCellID of the deleted GASBBalanceSheetViewCell.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBBalanceSheetViewCell <- function(GASBBalanceSheetViewCellID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetViewCell", objectId = GASBBalanceSheetViewCellID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBBalanceSheetViewCell
	#'
	#' This function creates a GASBBalanceSheetViewCell
	#' @param fieldNames The field values to give the created GASBBalanceSheetViewCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBBalanceSheetViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBBalanceSheetViewCell <- function(BalanceSheetTotalType = NULL, Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBBalanceSheetViewCell", body = list(DataObject = body), searchFields = append("GASBBalanceSheetViewCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBBalanceSheetViewCell
	#'
	#' This function modifies a GASBBalanceSheetViewCell
	#' @param fieldNames The field values to give the modified GASBBalanceSheetViewCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBBalanceSheetViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBBalanceSheetViewCell <- function(GASBBalanceSheetViewCellID, BalanceSheetTotalType = NULL, Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBBalanceSheetViewCell", objectId = GASBBalanceSheetViewCellID, body = list(DataObject = body), searchFields = append("GASBBalanceSheetViewCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiDistricts
	#'
	#' This function returns a dataframe or json object of EdFiDistricts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiDistricts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiDistricts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiDistrict') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiDistricts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiDistricts <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiDistrictID = F, EdFiID = F, EdFiLocalEducationAgencyCategoryDescriptorID = F, IsInvalid = F, ModifiedTime = F, Name = F, SchoolYearID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiDistrict", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiDistrict
	#'
	#' This function returns a dataframe or json object of an EdFiDistrict
	#' @param EdFiDistrictID The ID of the EdFiDistrict to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiDistrict. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiDistrict.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiDistrict') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiDistrict <- function(EdFiDistrictID, Code = F, CreatedTime = F, EdFiID = F, EdFiLocalEducationAgencyCategoryDescriptorID = F, IsInvalid = F, ModifiedTime = F, Name = F, SchoolYearID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiDistrictID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiDistrict", objectId = EdFiDistrictID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiDistrict
	#'
	#' This function deletes an EdFiDistrict
	#' @param EdFiDistrictID The ID of the EdFiDistrict to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiDistrictID of the deleted EdFiDistrict.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiDistrict <- function(EdFiDistrictID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiDistrict", objectId = EdFiDistrictID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiDistrict
	#'
	#' This function creates an EdFiDistrict
	#' @param fieldNames The field values to give the created EdFiDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiDistrict <- function(Code = NULL, EdFiID = NULL, EdFiLocalEducationAgencyCategoryDescriptorID = NULL, IsInvalid = NULL, Name = NULL, SchoolYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiDistrict", body = list(DataObject = body), searchFields = append("EdFiDistrictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiDistrict
	#'
	#' This function modifies an EdFiDistrict
	#' @param fieldNames The field values to give the modified EdFiDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiDistrict <- function(EdFiDistrictID, Code = NULL, EdFiID = NULL, EdFiLocalEducationAgencyCategoryDescriptorID = NULL, IsInvalid = NULL, Name = NULL, SchoolYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiDistrict", objectId = EdFiDistrictID, body = list(DataObject = body), searchFields = append("EdFiDistrictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiSchools
	#'
	#' This function returns a dataframe or json object of EdFiSchools
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSchools. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSchools.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSchool') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiSchools
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiSchools <- function(searchConditionsList = NULL, Code = F, CodeName = F, CreatedTime = F, EdFiDistrictID = F, EdFiID = F, EdFiSchoolID = F, IsInvalid = F, ModifiedTime = F, Name = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiSchool", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiSchool
	#'
	#' This function returns a dataframe or json object of an EdFiSchool
	#' @param EdFiSchoolID The ID of the EdFiSchool to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiSchool. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiSchool.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiSchool') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiSchool
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiSchool <- function(EdFiSchoolID, Code = F, CodeName = F, CreatedTime = F, EdFiDistrictID = F, EdFiID = F, IsInvalid = F, ModifiedTime = F, Name = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiSchoolID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiSchool", objectId = EdFiSchoolID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiSchool
	#'
	#' This function deletes an EdFiSchool
	#' @param EdFiSchoolID The ID of the EdFiSchool to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiSchoolID of the deleted EdFiSchool.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiSchool <- function(EdFiSchoolID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiSchool", objectId = EdFiSchoolID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiSchool
	#'
	#' This function creates an EdFiSchool
	#' @param fieldNames The field values to give the created EdFiSchool. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiSchool
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiSchool <- function(Code = NULL, EdFiDistrictID = NULL, EdFiID = NULL, IsInvalid = NULL, Name = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiSchool", body = list(DataObject = body), searchFields = append("EdFiSchoolID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiSchool
	#'
	#' This function modifies an EdFiSchool
	#' @param fieldNames The field values to give the modified EdFiSchool. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiSchool
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiSchool <- function(EdFiSchoolID, Code = NULL, EdFiDistrictID = NULL, EdFiID = NULL, IsInvalid = NULL, Name = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiSchool", objectId = EdFiSchoolID, body = list(DataObject = body), searchFields = append("EdFiSchoolID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateGASBRowDefaults
	#'
	#' This function returns a dataframe or json object of StateGASBRowDefaults
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateGASBRowDefaults. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateGASBRowDefaults.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateGASBRowDefault') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateGASBRowDefaults
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateGASBRowDefaults <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, FilterHigh = F, FilterLow = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateGASBRowDefaultID = F, StructureDimensionID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateGASBRowDefault", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateGASBRowDefault
	#'
	#' This function returns a dataframe or json object of a StateGASBRowDefault
	#' @param StateGASBRowDefaultID The ID of the StateGASBRowDefault to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateGASBRowDefault. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateGASBRowDefault.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateGASBRowDefault') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateGASBRowDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateGASBRowDefault <- function(StateGASBRowDefaultID, CreatedTime = F, Description = F, FilterHigh = F, FilterLow = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StructureDimensionID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateGASBRowDefaultID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateGASBRowDefault", objectId = StateGASBRowDefaultID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateGASBRowDefault
	#'
	#' This function deletes a StateGASBRowDefault
	#' @param StateGASBRowDefaultID The ID of the StateGASBRowDefault to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateGASBRowDefaultID of the deleted StateGASBRowDefault.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateGASBRowDefault <- function(StateGASBRowDefaultID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateGASBRowDefault", objectId = StateGASBRowDefaultID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateGASBRowDefault
	#'
	#' This function creates a StateGASBRowDefault
	#' @param fieldNames The field values to give the created StateGASBRowDefault. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateGASBRowDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateGASBRowDefault <- function(Description = NULL, FilterHigh = NULL, FilterLow = NULL, IsInvalid = NULL, StructureDimensionID = NULL, Type = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateGASBRowDefault", body = list(DataObject = body), searchFields = append("StateGASBRowDefaultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateGASBRowDefault
	#'
	#' This function modifies a StateGASBRowDefault
	#' @param fieldNames The field values to give the modified StateGASBRowDefault. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateGASBRowDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateGASBRowDefault <- function(StateGASBRowDefaultID, Description = NULL, FilterHigh = NULL, FilterLow = NULL, IsInvalid = NULL, StructureDimensionID = NULL, Type = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateGASBRowDefault", objectId = StateGASBRowDefaultID, body = list(DataObject = body), searchFields = append("StateGASBRowDefaultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateGASBColumnDefaults
	#'
	#' This function returns a dataframe or json object of StateGASBColumnDefaults
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateGASBColumnDefaults. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateGASBColumnDefaults.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateGASBColumnDefault') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateGASBColumnDefaults
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateGASBColumnDefaults <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, FilterHigh = F, FilterLow = F, GASBColumnType = F, GASBFundClassification = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateGASBColumnDefaultID = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateGASBColumnDefault", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateGASBColumnDefault
	#'
	#' This function returns a dataframe or json object of a StateGASBColumnDefault
	#' @param StateGASBColumnDefaultID The ID of the StateGASBColumnDefault to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateGASBColumnDefault. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateGASBColumnDefault.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateGASBColumnDefault') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateGASBColumnDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateGASBColumnDefault <- function(StateGASBColumnDefaultID, CreatedTime = F, Description = F, FilterHigh = F, FilterLow = F, GASBColumnType = F, GASBFundClassification = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateGASBColumnDefaultID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateGASBColumnDefault", objectId = StateGASBColumnDefaultID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateGASBColumnDefault
	#'
	#' This function deletes a StateGASBColumnDefault
	#' @param StateGASBColumnDefaultID The ID of the StateGASBColumnDefault to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateGASBColumnDefaultID of the deleted StateGASBColumnDefault.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateGASBColumnDefault <- function(StateGASBColumnDefaultID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateGASBColumnDefault", objectId = StateGASBColumnDefaultID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateGASBColumnDefault
	#'
	#' This function creates a StateGASBColumnDefault
	#' @param fieldNames The field values to give the created StateGASBColumnDefault. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateGASBColumnDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateGASBColumnDefault <- function(Description = NULL, FilterHigh = NULL, FilterLow = NULL, GASBColumnType = NULL, GASBFundClassification = NULL, IsInvalid = NULL, StructureDimensionID = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateGASBColumnDefault", body = list(DataObject = body), searchFields = append("StateGASBColumnDefaultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateGASBColumnDefault
	#'
	#' This function modifies a StateGASBColumnDefault
	#' @param fieldNames The field values to give the modified StateGASBColumnDefault. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateGASBColumnDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateGASBColumnDefault <- function(StateGASBColumnDefaultID, Description = NULL, FilterHigh = NULL, FilterLow = NULL, GASBColumnType = NULL, GASBFundClassification = NULL, IsInvalid = NULL, StructureDimensionID = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateGASBColumnDefault", objectId = StateGASBColumnDefaultID, body = list(DataObject = body), searchFields = append("StateGASBColumnDefaultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureComparisonIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureComparisonIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureComparisonIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureComparisonIncludedAccounts <- function(searchConditionsList = NULL, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBRevenueAndExpenditureComparisonIncludedAccountID = F, GASBRevenueAndExpenditureComparisonRowID = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureComparisonIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureComparisonIncludedAccount
	#' @param GASBRevenueAndExpenditureComparisonIncludedAccountID The ID of the GASBRevenueAndExpenditureComparisonIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureComparisonIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureComparisonIncludedAccount <- function(GASBRevenueAndExpenditureComparisonIncludedAccountID, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBRevenueAndExpenditureComparisonRowID = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureComparisonIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonIncludedAccount", objectId = GASBRevenueAndExpenditureComparisonIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureComparisonIncludedAccount
	#'
	#' This function deletes a GASBRevenueAndExpenditureComparisonIncludedAccount
	#' @param GASBRevenueAndExpenditureComparisonIncludedAccountID The ID of the GASBRevenueAndExpenditureComparisonIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureComparisonIncludedAccountID of the deleted GASBRevenueAndExpenditureComparisonIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureComparisonIncludedAccount <- function(GASBRevenueAndExpenditureComparisonIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonIncludedAccount", objectId = GASBRevenueAndExpenditureComparisonIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureComparisonIncludedAccount
	#'
	#' This function creates a GASBRevenueAndExpenditureComparisonIncludedAccount
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureComparisonIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureComparisonIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureComparisonIncludedAccount <- function(AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBRevenueAndExpenditureComparisonRowID = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonIncludedAccount", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureComparisonIncludedAccount
	#'
	#' This function modifies a GASBRevenueAndExpenditureComparisonIncludedAccount
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureComparisonIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureComparisonIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureComparisonIncludedAccount <- function(GASBRevenueAndExpenditureComparisonIncludedAccountID, AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBRevenueAndExpenditureComparisonRowID = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonIncludedAccount", objectId = GASBRevenueAndExpenditureComparisonIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureComparisonReports
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureComparisonReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureComparisonReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureComparisonReports <- function(searchConditionsList = NULL, CreatedTime = F, FundBeginningBalance = F, FundEndingBalance = F, GASBExtractRunID = F, GASBFundClassification = F, GASBRevenueAndExpenditureComparisonReportID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureComparisonReport
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureComparisonReport
	#' @param GASBRevenueAndExpenditureComparisonReportID The ID of the GASBRevenueAndExpenditureComparisonReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureComparisonReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureComparisonReport <- function(GASBRevenueAndExpenditureComparisonReportID, CreatedTime = F, FundBeginningBalance = F, FundEndingBalance = F, GASBExtractRunID = F, GASBFundClassification = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureComparisonReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonReport", objectId = GASBRevenueAndExpenditureComparisonReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureComparisonReport
	#'
	#' This function deletes a GASBRevenueAndExpenditureComparisonReport
	#' @param GASBRevenueAndExpenditureComparisonReportID The ID of the GASBRevenueAndExpenditureComparisonReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureComparisonReportID of the deleted GASBRevenueAndExpenditureComparisonReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureComparisonReport <- function(GASBRevenueAndExpenditureComparisonReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonReport", objectId = GASBRevenueAndExpenditureComparisonReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureComparisonReport
	#'
	#' This function creates a GASBRevenueAndExpenditureComparisonReport
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureComparisonReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureComparisonReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureComparisonReport <- function(FundBeginningBalance = NULL, FundEndingBalance = NULL, GASBExtractRunID = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonReport", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureComparisonReport
	#'
	#' This function modifies a GASBRevenueAndExpenditureComparisonReport
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureComparisonReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureComparisonReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureComparisonReport <- function(GASBRevenueAndExpenditureComparisonReportID, FundBeginningBalance = NULL, FundEndingBalance = NULL, GASBExtractRunID = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonReport", objectId = GASBRevenueAndExpenditureComparisonReportID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureComparisonRows
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureComparisonRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureComparisonRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureComparisonRows <- function(searchConditionsList = NULL, ActualBudget = F, CreatedTime = F, FinalBudget = F, FinalToActualVariance = F, GASBExtractRunID = F, GASBRevenueAndExpenditureComparisonReportID = F, GASBRevenueAndExpenditureComparisonRowID = F, ModifiedTime = F, OriginalBudget = F, OriginalToFinalVariance = F, RowDescription = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureComparisonRow
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureComparisonRow
	#' @param GASBRevenueAndExpenditureComparisonRowID The ID of the GASBRevenueAndExpenditureComparisonRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureComparisonRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureComparisonRow <- function(GASBRevenueAndExpenditureComparisonRowID, ActualBudget = F, CreatedTime = F, FinalBudget = F, FinalToActualVariance = F, GASBExtractRunID = F, GASBRevenueAndExpenditureComparisonReportID = F, ModifiedTime = F, OriginalBudget = F, OriginalToFinalVariance = F, RowDescription = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureComparisonRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonRow", objectId = GASBRevenueAndExpenditureComparisonRowID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureComparisonRow
	#'
	#' This function deletes a GASBRevenueAndExpenditureComparisonRow
	#' @param GASBRevenueAndExpenditureComparisonRowID The ID of the GASBRevenueAndExpenditureComparisonRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureComparisonRowID of the deleted GASBRevenueAndExpenditureComparisonRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureComparisonRow <- function(GASBRevenueAndExpenditureComparisonRowID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonRow", objectId = GASBRevenueAndExpenditureComparisonRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureComparisonRow
	#'
	#' This function creates a GASBRevenueAndExpenditureComparisonRow
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureComparisonRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureComparisonRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureComparisonRow <- function(ActualBudget = NULL, FinalBudget = NULL, GASBExtractRunID = NULL, GASBRevenueAndExpenditureComparisonReportID = NULL, OriginalBudget = NULL, RowDescription = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonRow", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureComparisonRow
	#'
	#' This function modifies a GASBRevenueAndExpenditureComparisonRow
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureComparisonRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureComparisonRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureComparisonRow <- function(GASBRevenueAndExpenditureComparisonRowID, ActualBudget = NULL, FinalBudget = NULL, GASBExtractRunID = NULL, GASBRevenueAndExpenditureComparisonReportID = NULL, OriginalBudget = NULL, RowDescription = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonRow", objectId = GASBRevenueAndExpenditureComparisonRowID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureComparisonTotalViews
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureComparisonTotalViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonTotalViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonTotalViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonTotalView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureComparisonTotalViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureComparisonTotalViews <- function(searchConditionsList = NULL, ActualBudget = F, DisplayOrder = F, FinalBudget = F, FinalToActualVariance = F, GASBExtractRunID = F, GASBRevenueAndExpenditureComparisonTotalViewID = F, OriginalBudget = F, OriginalToFinalVariance = F, RevenueAndExpenditureComparisonTotalType = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonTotalView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureComparisonTotalView
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureComparisonTotalView
	#' @param GASBRevenueAndExpenditureComparisonTotalViewID The ID of the GASBRevenueAndExpenditureComparisonTotalView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureComparisonTotalView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureComparisonTotalView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureComparisonTotalView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureComparisonTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureComparisonTotalView <- function(GASBRevenueAndExpenditureComparisonTotalViewID, ActualBudget = F, DisplayOrder = F, FinalBudget = F, FinalToActualVariance = F, GASBExtractRunID = F, OriginalBudget = F, OriginalToFinalVariance = F, RevenueAndExpenditureComparisonTotalType = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureComparisonTotalViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonTotalView", objectId = GASBRevenueAndExpenditureComparisonTotalViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureComparisonTotalView
	#'
	#' This function deletes a GASBRevenueAndExpenditureComparisonTotalView
	#' @param GASBRevenueAndExpenditureComparisonTotalViewID The ID of the GASBRevenueAndExpenditureComparisonTotalView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureComparisonTotalViewID of the deleted GASBRevenueAndExpenditureComparisonTotalView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureComparisonTotalView <- function(GASBRevenueAndExpenditureComparisonTotalViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonTotalView", objectId = GASBRevenueAndExpenditureComparisonTotalViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureComparisonTotalView
	#'
	#' This function creates a GASBRevenueAndExpenditureComparisonTotalView
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureComparisonTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureComparisonTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureComparisonTotalView <- function(ActualBudget = NULL, DisplayOrder = NULL, FinalBudget = NULL, FinalToActualVariance = NULL, GASBExtractRunID = NULL, OriginalBudget = NULL, OriginalToFinalVariance = NULL, RevenueAndExpenditureComparisonTotalType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonTotalView", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureComparisonTotalView
	#'
	#' This function modifies a GASBRevenueAndExpenditureComparisonTotalView
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureComparisonTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureComparisonTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureComparisonTotalView <- function(GASBRevenueAndExpenditureComparisonTotalViewID, ActualBudget = NULL, DisplayOrder = NULL, FinalBudget = NULL, FinalToActualVariance = NULL, GASBExtractRunID = NULL, OriginalBudget = NULL, OriginalToFinalVariance = NULL, RevenueAndExpenditureComparisonTotalType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureComparisonTotalView", objectId = GASBRevenueAndExpenditureComparisonTotalViewID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureComparisonTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureCells
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureCells
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureCells. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureCells.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureCell') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureCells
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureCells <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, GASBRevenueAndExpenditureCellID = F, GASBRevenueAndExpenditureRowID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureCell", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureCell
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureCell
	#' @param GASBRevenueAndExpenditureCellID The ID of the GASBRevenueAndExpenditureCell to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureCell. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureCell.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureCell') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureCell <- function(GASBRevenueAndExpenditureCellID, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, GASBRevenueAndExpenditureRowID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureCellID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureCell", objectId = GASBRevenueAndExpenditureCellID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureCell
	#'
	#' This function deletes a GASBRevenueAndExpenditureCell
	#' @param GASBRevenueAndExpenditureCellID The ID of the GASBRevenueAndExpenditureCell to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureCellID of the deleted GASBRevenueAndExpenditureCell.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureCell <- function(GASBRevenueAndExpenditureCellID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureCell", objectId = GASBRevenueAndExpenditureCellID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureCell
	#'
	#' This function creates a GASBRevenueAndExpenditureCell
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureCell <- function(Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, GASBRevenueAndExpenditureRowID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureCell", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureCell
	#'
	#' This function modifies a GASBRevenueAndExpenditureCell
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureCell <- function(GASBRevenueAndExpenditureCellID, Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, GASBRevenueAndExpenditureRowID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureCell", objectId = GASBRevenueAndExpenditureCellID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureColumnIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureColumnIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureColumnIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureColumnIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureColumnIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureColumnIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureColumnIncludedAccounts <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, GASBColumnID = F, GASBRevenueAndExpenditureColumnIncludedAccountID = F, GASBRevenueAndExpenditureIncludedAccountID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureColumnIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureColumnIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureColumnIncludedAccount
	#' @param GASBRevenueAndExpenditureColumnIncludedAccountID The ID of the GASBRevenueAndExpenditureColumnIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureColumnIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureColumnIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureColumnIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureColumnIncludedAccount <- function(GASBRevenueAndExpenditureColumnIncludedAccountID, CreatedTime = F, Description = F, GASBColumnID = F, GASBRevenueAndExpenditureIncludedAccountID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureColumnIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureColumnIncludedAccount", objectId = GASBRevenueAndExpenditureColumnIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureColumnIncludedAccount
	#'
	#' This function deletes a GASBRevenueAndExpenditureColumnIncludedAccount
	#' @param GASBRevenueAndExpenditureColumnIncludedAccountID The ID of the GASBRevenueAndExpenditureColumnIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureColumnIncludedAccountID of the deleted GASBRevenueAndExpenditureColumnIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureColumnIncludedAccount <- function(GASBRevenueAndExpenditureColumnIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureColumnIncludedAccount", objectId = GASBRevenueAndExpenditureColumnIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureColumnIncludedAccount
	#'
	#' This function creates a GASBRevenueAndExpenditureColumnIncludedAccount
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureColumnIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureColumnIncludedAccount <- function(Description = NULL, GASBColumnID = NULL, GASBRevenueAndExpenditureIncludedAccountID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureColumnIncludedAccount", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureColumnIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureColumnIncludedAccount
	#'
	#' This function modifies a GASBRevenueAndExpenditureColumnIncludedAccount
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureColumnIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureColumnIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureColumnIncludedAccount <- function(GASBRevenueAndExpenditureColumnIncludedAccountID, Description = NULL, GASBColumnID = NULL, GASBRevenueAndExpenditureIncludedAccountID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureColumnIncludedAccount", objectId = GASBRevenueAndExpenditureColumnIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureColumnIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureIncludedAccounts
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureIncludedAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureIncludedAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureIncludedAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureIncludedAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureIncludedAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureIncludedAccounts <- function(searchConditionsList = NULL, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBRevenueAndExpenditureIncludedAccountID = F, GASBRevenueAndExpenditureRowID = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureIncludedAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureIncludedAccount
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureIncludedAccount
	#' @param GASBRevenueAndExpenditureIncludedAccountID The ID of the GASBRevenueAndExpenditureIncludedAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureIncludedAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureIncludedAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureIncludedAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureIncludedAccount <- function(GASBRevenueAndExpenditureIncludedAccountID, AccountFiscalYearID = F, BeginningBalance = F, CreatedTime = F, Credit = F, Debit = F, EndingBalance = F, GASBRevenueAndExpenditureRowID = F, ModifiedTime = F, OriginalBudget = F, UserIDCreatedBy = F, UserIDModifiedBy = F, YearToDateActivity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureIncludedAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureIncludedAccount", objectId = GASBRevenueAndExpenditureIncludedAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureIncludedAccount
	#'
	#' This function deletes a GASBRevenueAndExpenditureIncludedAccount
	#' @param GASBRevenueAndExpenditureIncludedAccountID The ID of the GASBRevenueAndExpenditureIncludedAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureIncludedAccountID of the deleted GASBRevenueAndExpenditureIncludedAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureIncludedAccount <- function(GASBRevenueAndExpenditureIncludedAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureIncludedAccount", objectId = GASBRevenueAndExpenditureIncludedAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureIncludedAccount
	#'
	#' This function creates a GASBRevenueAndExpenditureIncludedAccount
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureIncludedAccount <- function(AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBRevenueAndExpenditureRowID = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureIncludedAccount", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureIncludedAccount
	#'
	#' This function modifies a GASBRevenueAndExpenditureIncludedAccount
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureIncludedAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureIncludedAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureIncludedAccount <- function(GASBRevenueAndExpenditureIncludedAccountID, AccountFiscalYearID = NULL, BeginningBalance = NULL, Credit = NULL, Debit = NULL, EndingBalance = NULL, GASBRevenueAndExpenditureRowID = NULL, OriginalBudget = NULL, YearToDateActivity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureIncludedAccount", objectId = GASBRevenueAndExpenditureIncludedAccountID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureIncludedAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureReports
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureReports <- function(searchConditionsList = NULL, CreatedTime = F, GASBExtractRunID = F, GASBFundClassification = F, GASBRevenueAndExpenditureReportID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureReport
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureReport
	#' @param GASBRevenueAndExpenditureReportID The ID of the GASBRevenueAndExpenditureReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureReport <- function(GASBRevenueAndExpenditureReportID, CreatedTime = F, GASBExtractRunID = F, GASBFundClassification = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureReport", objectId = GASBRevenueAndExpenditureReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureReport
	#'
	#' This function deletes a GASBRevenueAndExpenditureReport
	#' @param GASBRevenueAndExpenditureReportID The ID of the GASBRevenueAndExpenditureReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureReportID of the deleted GASBRevenueAndExpenditureReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureReport <- function(GASBRevenueAndExpenditureReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureReport", objectId = GASBRevenueAndExpenditureReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureReport
	#'
	#' This function creates a GASBRevenueAndExpenditureReport
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureReport <- function(GASBExtractRunID = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureReport", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureReport
	#'
	#' This function modifies a GASBRevenueAndExpenditureReport
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureReport <- function(GASBRevenueAndExpenditureReportID, GASBExtractRunID = NULL, GASBFundClassification = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureReport", objectId = GASBRevenueAndExpenditureReportID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureRows
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureRows <- function(searchConditionsList = NULL, CreatedTime = F, FundBalanceType = F, GASBExtractRunID = F, GASBRevenueAndExpenditureReportID = F, GASBRevenueAndExpenditureRowID = F, ModifiedTime = F, RowDescription = F, TotalFunds = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureRow
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureRow
	#' @param GASBRevenueAndExpenditureRowID The ID of the GASBRevenueAndExpenditureRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureRow <- function(GASBRevenueAndExpenditureRowID, CreatedTime = F, FundBalanceType = F, GASBExtractRunID = F, GASBRevenueAndExpenditureReportID = F, ModifiedTime = F, RowDescription = F, TotalFunds = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureRow", objectId = GASBRevenueAndExpenditureRowID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureRow
	#'
	#' This function deletes a GASBRevenueAndExpenditureRow
	#' @param GASBRevenueAndExpenditureRowID The ID of the GASBRevenueAndExpenditureRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureRowID of the deleted GASBRevenueAndExpenditureRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureRow <- function(GASBRevenueAndExpenditureRowID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureRow", objectId = GASBRevenueAndExpenditureRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureRow
	#'
	#' This function creates a GASBRevenueAndExpenditureRow
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureRow <- function(FundBalanceType = NULL, GASBExtractRunID = NULL, GASBRevenueAndExpenditureReportID = NULL, RowDescription = NULL, TotalFunds = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureRow", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureRow
	#'
	#' This function modifies a GASBRevenueAndExpenditureRow
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureRow <- function(GASBRevenueAndExpenditureRowID, FundBalanceType = NULL, GASBExtractRunID = NULL, GASBRevenueAndExpenditureReportID = NULL, RowDescription = NULL, TotalFunds = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureRow", objectId = GASBRevenueAndExpenditureRowID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureRowID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureTotalViews
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureTotalViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureTotalViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureTotalViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureTotalView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureTotalViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureTotalViews <- function(searchConditionsList = NULL, DisplayOrder = F, GASBExtractRunID = F, GASBRevenueAndExpenditureTotalViewID = F, RevenueAndExpenditureTotalType = F, TotalFunds = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureTotalView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureTotalView
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureTotalView
	#' @param GASBRevenueAndExpenditureTotalViewID The ID of the GASBRevenueAndExpenditureTotalView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureTotalView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureTotalView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureTotalView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureTotalView <- function(GASBRevenueAndExpenditureTotalViewID, DisplayOrder = F, GASBExtractRunID = F, RevenueAndExpenditureTotalType = F, TotalFunds = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureTotalViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureTotalView", objectId = GASBRevenueAndExpenditureTotalViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureTotalView
	#'
	#' This function deletes a GASBRevenueAndExpenditureTotalView
	#' @param GASBRevenueAndExpenditureTotalViewID The ID of the GASBRevenueAndExpenditureTotalView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureTotalViewID of the deleted GASBRevenueAndExpenditureTotalView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureTotalView <- function(GASBRevenueAndExpenditureTotalViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureTotalView", objectId = GASBRevenueAndExpenditureTotalViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureTotalView
	#'
	#' This function creates a GASBRevenueAndExpenditureTotalView
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureTotalView <- function(DisplayOrder = NULL, GASBExtractRunID = NULL, RevenueAndExpenditureTotalType = NULL, TotalFunds = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureTotalView", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureTotalView
	#'
	#' This function modifies a GASBRevenueAndExpenditureTotalView
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureTotalView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureTotalView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureTotalView <- function(GASBRevenueAndExpenditureTotalViewID, DisplayOrder = NULL, GASBExtractRunID = NULL, RevenueAndExpenditureTotalType = NULL, TotalFunds = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureTotalView", objectId = GASBRevenueAndExpenditureTotalViewID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureTotalViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GASBRevenueAndExpenditureViewCells
	#'
	#' This function returns a dataframe or json object of GASBRevenueAndExpenditureViewCells
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureViewCells. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureViewCells.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureViewCell') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of GASBRevenueAndExpenditureViewCells
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGASBRevenueAndExpenditureViewCells <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, GASBRevenueAndExpenditureViewCellID = F, ModifiedTime = F, RevenueAndExpenditureTotalType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "GASBRevenueAndExpenditureViewCell", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GASBRevenueAndExpenditureViewCell
	#'
	#' This function returns a dataframe or json object of a GASBRevenueAndExpenditureViewCell
	#' @param GASBRevenueAndExpenditureViewCellID The ID of the GASBRevenueAndExpenditureViewCell to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GASBRevenueAndExpenditureViewCell. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GASBRevenueAndExpenditureViewCell.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GASBRevenueAndExpenditureViewCell') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of GASBRevenueAndExpenditureViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGASBRevenueAndExpenditureViewCell <- function(GASBRevenueAndExpenditureViewCellID, CreatedTime = F, Description = F, DynamicRelationshipID = F, GASBColumnID = F, GASBExtractRunID = F, ModifiedTime = F, RevenueAndExpenditureTotalType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GASBRevenueAndExpenditureViewCellID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureViewCell", objectId = GASBRevenueAndExpenditureViewCellID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GASBRevenueAndExpenditureViewCell
	#'
	#' This function deletes a GASBRevenueAndExpenditureViewCell
	#' @param GASBRevenueAndExpenditureViewCellID The ID of the GASBRevenueAndExpenditureViewCell to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The GASBRevenueAndExpenditureViewCellID of the deleted GASBRevenueAndExpenditureViewCell.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGASBRevenueAndExpenditureViewCell <- function(GASBRevenueAndExpenditureViewCellID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureViewCell", objectId = GASBRevenueAndExpenditureViewCellID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GASBRevenueAndExpenditureViewCell
	#'
	#' This function creates a GASBRevenueAndExpenditureViewCell
	#' @param fieldNames The field values to give the created GASBRevenueAndExpenditureViewCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created GASBRevenueAndExpenditureViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGASBRevenueAndExpenditureViewCell <- function(Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, RevenueAndExpenditureTotalType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureViewCell", body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureViewCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GASBRevenueAndExpenditureViewCell
	#'
	#' This function modifies a GASBRevenueAndExpenditureViewCell
	#' @param fieldNames The field values to give the modified GASBRevenueAndExpenditureViewCell. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified GASBRevenueAndExpenditureViewCell
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGASBRevenueAndExpenditureViewCell <- function(GASBRevenueAndExpenditureViewCellID, Description = NULL, GASBColumnID = NULL, GASBExtractRunID = NULL, RevenueAndExpenditureTotalType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "GASBRevenueAndExpenditureViewCell", objectId = GASBRevenueAndExpenditureViewCellID, body = list(DataObject = body), searchFields = append("GASBRevenueAndExpenditureViewCellID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateIdentifierNames
	#'
	#' This function returns a dataframe or json object of StateIdentifierNames
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateIdentifierNames. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateIdentifierNames.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateIdentifierName') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateIdentifierNames
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateIdentifierNames <- function(searchConditionsList = NULL, BirthDate = F, CreatedTime = F, DistrictNumber = F, DistrictTypeCode = F, Gender = F, HasErrors = F, LegalFirstName = F, LegalLastName = F, LegalMiddleName = F, LegalSuffix = F, LocalID = F, ModifiedTime = F, NameID = F, RequiresAPIMatchReview = F, StateIdentifierNameID = F, StateIdentifierNameMNID = F, StateIdentifierRunHistoryID = F, StudentStateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateIdentifierName", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateIdentifierName
	#'
	#' This function returns a dataframe or json object of a StateIdentifierName
	#' @param StateIdentifierNameID The ID of the StateIdentifierName to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateIdentifierName. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateIdentifierName.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateIdentifierName') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateIdentifierName
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateIdentifierName <- function(StateIdentifierNameID, BirthDate = F, CreatedTime = F, DistrictNumber = F, DistrictTypeCode = F, Gender = F, HasErrors = F, LegalFirstName = F, LegalLastName = F, LegalMiddleName = F, LegalSuffix = F, LocalID = F, ModifiedTime = F, NameID = F, RequiresAPIMatchReview = F, StateIdentifierNameMNID = F, StateIdentifierRunHistoryID = F, StudentStateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateIdentifierNameID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateIdentifierName", objectId = StateIdentifierNameID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateIdentifierName
	#'
	#' This function deletes a StateIdentifierName
	#' @param StateIdentifierNameID The ID of the StateIdentifierName to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateIdentifierNameID of the deleted StateIdentifierName.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateIdentifierName <- function(StateIdentifierNameID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateIdentifierName", objectId = StateIdentifierNameID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateIdentifierName
	#'
	#' This function creates a StateIdentifierName
	#' @param fieldNames The field values to give the created StateIdentifierName. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateIdentifierName
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateIdentifierName <- function(BirthDate = NULL, DistrictNumber = NULL, DistrictTypeCode = NULL, Gender = NULL, LegalFirstName = NULL, LegalLastName = NULL, LegalMiddleName = NULL, LegalSuffix = NULL, LocalID = NULL, NameID = NULL, RequiresAPIMatchReview = NULL, StateIdentifierRunHistoryID = NULL, StudentStateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateIdentifierName", body = list(DataObject = body), searchFields = append("StateIdentifierNameID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateIdentifierName
	#'
	#' This function modifies a StateIdentifierName
	#' @param fieldNames The field values to give the modified StateIdentifierName. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateIdentifierName
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateIdentifierName <- function(StateIdentifierNameID, BirthDate = NULL, DistrictNumber = NULL, DistrictTypeCode = NULL, Gender = NULL, LegalFirstName = NULL, LegalLastName = NULL, LegalMiddleName = NULL, LegalSuffix = NULL, LocalID = NULL, NameID = NULL, RequiresAPIMatchReview = NULL, StateIdentifierRunHistoryID = NULL, StudentStateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateIdentifierName", objectId = StateIdentifierNameID, body = list(DataObject = body), searchFields = append("StateIdentifierNameID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateIdentifierNameExceptions
	#'
	#' This function returns a dataframe or json object of StateIdentifierNameExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateIdentifierNameExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateIdentifierNameExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateIdentifierNameException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateIdentifierNameExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateIdentifierNameExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, StateIdentifierNameExceptionID = F, StateIdentifierNameID = F, StateIdentifierRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateIdentifierNameException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateIdentifierNameException
	#'
	#' This function returns a dataframe or json object of a StateIdentifierNameException
	#' @param StateIdentifierNameExceptionID The ID of the StateIdentifierNameException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateIdentifierNameException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateIdentifierNameException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateIdentifierNameException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateIdentifierNameException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateIdentifierNameException <- function(StateIdentifierNameExceptionID, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, StateIdentifierNameID = F, StateIdentifierRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateIdentifierNameExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateIdentifierNameException", objectId = StateIdentifierNameExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateIdentifierNameException
	#'
	#' This function deletes a StateIdentifierNameException
	#' @param StateIdentifierNameExceptionID The ID of the StateIdentifierNameException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateIdentifierNameExceptionID of the deleted StateIdentifierNameException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateIdentifierNameException <- function(StateIdentifierNameExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateIdentifierNameException", objectId = StateIdentifierNameExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateIdentifierNameException
	#'
	#' This function creates a StateIdentifierNameException
	#' @param fieldNames The field values to give the created StateIdentifierNameException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateIdentifierNameException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateIdentifierNameException <- function(Message = NULL, MessageType = NULL, StateIdentifierNameID = NULL, StateIdentifierRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateIdentifierNameException", body = list(DataObject = body), searchFields = append("StateIdentifierNameExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateIdentifierNameException
	#'
	#' This function modifies a StateIdentifierNameException
	#' @param fieldNames The field values to give the modified StateIdentifierNameException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateIdentifierNameException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateIdentifierNameException <- function(StateIdentifierNameExceptionID, Message = NULL, MessageType = NULL, StateIdentifierNameID = NULL, StateIdentifierRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateIdentifierNameException", objectId = StateIdentifierNameExceptionID, body = list(DataObject = body), searchFields = append("StateIdentifierNameExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateIdentifierRunHistories
	#'
	#' This function returns a dataframe or json object of StateIdentifierRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateIdentifierRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateIdentifierRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateIdentifierRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateIdentifierRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateIdentifierRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, DistrictID = F, EndDateTime = F, HasErrors = F, HasImportFileErrors = F, HasMediaID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, NumericYear = F, PersonTypeRunFor = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, StateIdentifierRunHistoryID = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateIdentifierRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateIdentifierRunHistory
	#'
	#' This function returns a dataframe or json object of a StateIdentifierRunHistory
	#' @param StateIdentifierRunHistoryID The ID of the StateIdentifierRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateIdentifierRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateIdentifierRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateIdentifierRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateIdentifierRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateIdentifierRunHistory <- function(StateIdentifierRunHistoryID, CanAccessMedia = F, CreatedTime = F, DistrictID = F, EndDateTime = F, HasErrors = F, HasImportFileErrors = F, HasMediaID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, NumericYear = F, PersonTypeRunFor = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateIdentifierRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateIdentifierRunHistory", objectId = StateIdentifierRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateIdentifierRunHistory
	#'
	#' This function deletes a StateIdentifierRunHistory
	#' @param StateIdentifierRunHistoryID The ID of the StateIdentifierRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateIdentifierRunHistoryID of the deleted StateIdentifierRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateIdentifierRunHistory <- function(StateIdentifierRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateIdentifierRunHistory", objectId = StateIdentifierRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateIdentifierRunHistory
	#'
	#' This function creates a StateIdentifierRunHistory
	#' @param fieldNames The field values to give the created StateIdentifierRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateIdentifierRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateIdentifierRunHistory <- function(DistrictID = NULL, EndDateTime = NULL, MediaID = NULL, NumericYear = NULL, PersonTypeRunFor = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateIdentifierRunHistory", body = list(DataObject = body), searchFields = append("StateIdentifierRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateIdentifierRunHistory
	#'
	#' This function modifies a StateIdentifierRunHistory
	#' @param fieldNames The field values to give the modified StateIdentifierRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateIdentifierRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateIdentifierRunHistory <- function(StateIdentifierRunHistoryID, DistrictID = NULL, EndDateTime = NULL, MediaID = NULL, NumericYear = NULL, PersonTypeRunFor = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateIdentifierRunHistory", objectId = StateIdentifierRunHistoryID, body = list(DataObject = body), searchFields = append("StateIdentifierRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiGradingPeriodDescriptors
	#'
	#' This function returns a dataframe or json object of EdFiGradingPeriodDescriptors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradingPeriodDescriptors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradingPeriodDescriptors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradingPeriodDescriptor') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiGradingPeriodDescriptors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiGradingPeriodDescriptors <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EdFiGradingPeriodDescriptorID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiGradingPeriodDescriptor", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiGradingPeriodDescriptor
	#'
	#' This function returns a dataframe or json object of an EdFiGradingPeriodDescriptor
	#' @param EdFiGradingPeriodDescriptorID The ID of the EdFiGradingPeriodDescriptor to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiGradingPeriodDescriptor. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiGradingPeriodDescriptor.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiGradingPeriodDescriptor') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiGradingPeriodDescriptor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiGradingPeriodDescriptor <- function(EdFiGradingPeriodDescriptorID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiGradingPeriodDescriptorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiGradingPeriodDescriptor", objectId = EdFiGradingPeriodDescriptorID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiGradingPeriodDescriptor
	#'
	#' This function deletes an EdFiGradingPeriodDescriptor
	#' @param EdFiGradingPeriodDescriptorID The ID of the EdFiGradingPeriodDescriptor to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiGradingPeriodDescriptorID of the deleted EdFiGradingPeriodDescriptor.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiGradingPeriodDescriptor <- function(EdFiGradingPeriodDescriptorID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiGradingPeriodDescriptor", objectId = EdFiGradingPeriodDescriptorID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiGradingPeriodDescriptor
	#'
	#' This function creates an EdFiGradingPeriodDescriptor
	#' @param fieldNames The field values to give the created EdFiGradingPeriodDescriptor. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiGradingPeriodDescriptor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiGradingPeriodDescriptor <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiGradingPeriodDescriptor", body = list(DataObject = body), searchFields = append("EdFiGradingPeriodDescriptorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiGradingPeriodDescriptor
	#'
	#' This function modifies an EdFiGradingPeriodDescriptor
	#' @param fieldNames The field values to give the modified EdFiGradingPeriodDescriptor. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiGradingPeriodDescriptor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiGradingPeriodDescriptor <- function(EdFiGradingPeriodDescriptorID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiGradingPeriodDescriptor", objectId = EdFiGradingPeriodDescriptorID, body = list(DataObject = body), searchFields = append("EdFiGradingPeriodDescriptorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAExclusions
	#'
	#' This function returns a dataframe or json object of ESSAExclusions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusion') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAExclusions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAExclusions <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DistrictID = F, ESSAExclusionID = F, ModifiedTime = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAExclusion", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAExclusion
	#'
	#' This function returns a dataframe or json object of an ESSAExclusion
	#' @param ESSAExclusionID The ID of the ESSAExclusion to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusion. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusion.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusion') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAExclusion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAExclusion <- function(ESSAExclusionID, CreatedTime = F, Description = F, DistrictID = F, ModifiedTime = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAExclusionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAExclusion", objectId = ESSAExclusionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAExclusion
	#'
	#' This function deletes an ESSAExclusion
	#' @param ESSAExclusionID The ID of the ESSAExclusion to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAExclusionID of the deleted ESSAExclusion.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAExclusion <- function(ESSAExclusionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAExclusion", objectId = ESSAExclusionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAExclusion
	#'
	#' This function creates an ESSAExclusion
	#' @param fieldNames The field values to give the created ESSAExclusion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAExclusion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAExclusion <- function(Description = NULL, DistrictID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAExclusion", body = list(DataObject = body), searchFields = append("ESSAExclusionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAExclusion
	#'
	#' This function modifies an ESSAExclusion
	#' @param fieldNames The field values to give the modified ESSAExclusion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAExclusion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAExclusion <- function(ESSAExclusionID, Description = NULL, DistrictID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAExclusion", objectId = ESSAExclusionID, body = list(DataObject = body), searchFields = append("ESSAExclusionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAExclusionDimensionFilterRanges
	#'
	#' This function returns a dataframe or json object of ESSAExclusionDimensionFilterRanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusionDimensionFilterRanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusionDimensionFilterRanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusionDimensionFilterRange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAExclusionDimensionFilterRanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAExclusionDimensionFilterRanges <- function(searchConditionsList = NULL, CreatedTime = F, ESSAExclusionDimensionFilterRangeID = F, ESSAExclusionID = F, FilterHigh = F, FilterLow = F, ModifiedTime = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAExclusionDimensionFilterRange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAExclusionDimensionFilterRange
	#'
	#' This function returns a dataframe or json object of an ESSAExclusionDimensionFilterRange
	#' @param ESSAExclusionDimensionFilterRangeID The ID of the ESSAExclusionDimensionFilterRange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusionDimensionFilterRange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusionDimensionFilterRange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusionDimensionFilterRange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAExclusionDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAExclusionDimensionFilterRange <- function(ESSAExclusionDimensionFilterRangeID, CreatedTime = F, ESSAExclusionID = F, FilterHigh = F, FilterLow = F, ModifiedTime = F, StructureDimensionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAExclusionDimensionFilterRangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAExclusionDimensionFilterRange", objectId = ESSAExclusionDimensionFilterRangeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAExclusionDimensionFilterRange
	#'
	#' This function deletes an ESSAExclusionDimensionFilterRange
	#' @param ESSAExclusionDimensionFilterRangeID The ID of the ESSAExclusionDimensionFilterRange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAExclusionDimensionFilterRangeID of the deleted ESSAExclusionDimensionFilterRange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAExclusionDimensionFilterRange <- function(ESSAExclusionDimensionFilterRangeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAExclusionDimensionFilterRange", objectId = ESSAExclusionDimensionFilterRangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAExclusionDimensionFilterRange
	#'
	#' This function creates an ESSAExclusionDimensionFilterRange
	#' @param fieldNames The field values to give the created ESSAExclusionDimensionFilterRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAExclusionDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAExclusionDimensionFilterRange <- function(ESSAExclusionID = NULL, FilterHigh = NULL, FilterLow = NULL, StructureDimensionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAExclusionDimensionFilterRange", body = list(DataObject = body), searchFields = append("ESSAExclusionDimensionFilterRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAExclusionDimensionFilterRange
	#'
	#' This function modifies an ESSAExclusionDimensionFilterRange
	#' @param fieldNames The field values to give the modified ESSAExclusionDimensionFilterRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAExclusionDimensionFilterRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAExclusionDimensionFilterRange <- function(ESSAExclusionDimensionFilterRangeID, ESSAExclusionID = NULL, FilterHigh = NULL, FilterLow = NULL, StructureDimensionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAExclusionDimensionFilterRange", objectId = ESSAExclusionDimensionFilterRangeID, body = list(DataObject = body), searchFields = append("ESSAExclusionDimensionFilterRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAExpenditures
	#'
	#' This function returns a dataframe or json object of ESSAExpenditures
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExpenditures. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExpenditures.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExpenditure') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAExpenditures
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAExpenditures <- function(searchConditionsList = NULL, CreatedTime = F, DistrictID = F, Enrollment = F, ESSAExpenditureAccountStandardFilterData = F, ESSAExpenditureAccountXMLFilter = F, ESSAExpenditureID = F, ModifiedTime = F, SchoolName = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAExpenditure", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAExpenditure
	#'
	#' This function returns a dataframe or json object of an ESSAExpenditure
	#' @param ESSAExpenditureID The ID of the ESSAExpenditure to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExpenditure. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExpenditure.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExpenditure') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAExpenditure
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAExpenditure <- function(ESSAExpenditureID, CreatedTime = F, DistrictID = F, Enrollment = F, ESSAExpenditureAccountStandardFilterData = F, ESSAExpenditureAccountXMLFilter = F, ModifiedTime = F, SchoolName = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAExpenditureID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAExpenditure", objectId = ESSAExpenditureID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAExpenditure
	#'
	#' This function deletes an ESSAExpenditure
	#' @param ESSAExpenditureID The ID of the ESSAExpenditure to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAExpenditureID of the deleted ESSAExpenditure.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAExpenditure <- function(ESSAExpenditureID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAExpenditure", objectId = ESSAExpenditureID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAExpenditure
	#'
	#' This function creates an ESSAExpenditure
	#' @param fieldNames The field values to give the created ESSAExpenditure. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAExpenditure
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAExpenditure <- function(DistrictID = NULL, Enrollment = NULL, SchoolName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAExpenditure", body = list(DataObject = body), searchFields = append("ESSAExpenditureID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAExpenditure
	#'
	#' This function modifies an ESSAExpenditure
	#' @param fieldNames The field values to give the modified ESSAExpenditure. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAExpenditure
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAExpenditure <- function(ESSAExpenditureID, DistrictID = NULL, Enrollment = NULL, SchoolName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAExpenditure", objectId = ESSAExpenditureID, body = list(DataObject = body), searchFields = append("ESSAExpenditureID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAIncludedAccountExceptions
	#'
	#' This function returns a dataframe or json object of ESSAIncludedAccountExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAIncludedAccountExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAIncludedAccountExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAIncludedAccountException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAIncludedAccountExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAIncludedAccountExceptions <- function(searchConditionsList = NULL, AccountFiscalYearID = F, CreatedTime = F, DisplayAccount = F, ESSAIncludedAccountExceptionID = F, ESSAIncludedAccountV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAIncludedAccountException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAIncludedAccountException
	#'
	#' This function returns a dataframe or json object of an ESSAIncludedAccountException
	#' @param ESSAIncludedAccountExceptionID The ID of the ESSAIncludedAccountException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAIncludedAccountException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAIncludedAccountException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAIncludedAccountException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAIncludedAccountException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAIncludedAccountException <- function(ESSAIncludedAccountExceptionID, AccountFiscalYearID = F, CreatedTime = F, DisplayAccount = F, ESSAIncludedAccountV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAIncludedAccountExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountException", objectId = ESSAIncludedAccountExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAIncludedAccountException
	#'
	#' This function deletes an ESSAIncludedAccountException
	#' @param ESSAIncludedAccountExceptionID The ID of the ESSAIncludedAccountException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAIncludedAccountExceptionID of the deleted ESSAIncludedAccountException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAIncludedAccountException <- function(ESSAIncludedAccountExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountException", objectId = ESSAIncludedAccountExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAIncludedAccountException
	#'
	#' This function creates an ESSAIncludedAccountException
	#' @param fieldNames The field values to give the created ESSAIncludedAccountException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAIncludedAccountException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAIncludedAccountException <- function(AccountFiscalYearID = NULL, DisplayAccount = NULL, ESSAIncludedAccountV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountException", body = list(DataObject = body), searchFields = append("ESSAIncludedAccountExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAIncludedAccountException
	#'
	#' This function modifies an ESSAIncludedAccountException
	#' @param fieldNames The field values to give the modified ESSAIncludedAccountException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAIncludedAccountException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAIncludedAccountException <- function(ESSAIncludedAccountExceptionID, AccountFiscalYearID = NULL, DisplayAccount = NULL, ESSAIncludedAccountV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountException", objectId = ESSAIncludedAccountExceptionID, body = list(DataObject = body), searchFields = append("ESSAIncludedAccountExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAIncludedAccountV1s
	#'
	#' This function returns a dataframe or json object of ESSAIncludedAccountV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAIncludedAccountV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAIncludedAccountV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAIncludedAccountV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAIncludedAccountV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAIncludedAccountV1s <- function(searchConditionsList = NULL, AccountFiscalYearID = F, Activity = F, CreatedTime = F, DisplayAccount = F, DistrictName = F, ESSAIncludedAccountV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, ExclusionName = F, FundingSource = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, SchoolName = F, Type = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAIncludedAccountV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAIncludedAccountV1
	#'
	#' This function returns a dataframe or json object of an ESSAIncludedAccountV1
	#' @param ESSAIncludedAccountV1ID The ID of the ESSAIncludedAccountV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAIncludedAccountV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAIncludedAccountV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAIncludedAccountV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAIncludedAccountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAIncludedAccountV1 <- function(ESSAIncludedAccountV1ID, AccountFiscalYearID = F, Activity = F, CreatedTime = F, DisplayAccount = F, DistrictName = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, ExclusionName = F, FundingSource = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, SchoolName = F, Type = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAIncludedAccountV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountV1", objectId = ESSAIncludedAccountV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAIncludedAccountV1
	#'
	#' This function deletes an ESSAIncludedAccountV1
	#' @param ESSAIncludedAccountV1ID The ID of the ESSAIncludedAccountV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAIncludedAccountV1ID of the deleted ESSAIncludedAccountV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAIncludedAccountV1 <- function(ESSAIncludedAccountV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountV1", objectId = ESSAIncludedAccountV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAIncludedAccountV1
	#'
	#' This function creates an ESSAIncludedAccountV1
	#' @param fieldNames The field values to give the created ESSAIncludedAccountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAIncludedAccountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAIncludedAccountV1 <- function(AccountFiscalYearID = NULL, Activity = NULL, DisplayAccount = NULL, DistrictName = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, ExclusionName = NULL, FundingSource = NULL, SchoolName = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountV1", body = list(DataObject = body), searchFields = append("ESSAIncludedAccountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAIncludedAccountV1
	#'
	#' This function modifies an ESSAIncludedAccountV1
	#' @param fieldNames The field values to give the modified ESSAIncludedAccountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAIncludedAccountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAIncludedAccountV1 <- function(ESSAIncludedAccountV1ID, AccountFiscalYearID = NULL, Activity = NULL, DisplayAccount = NULL, DistrictName = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, ExclusionName = NULL, FundingSource = NULL, SchoolName = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAIncludedAccountV1", objectId = ESSAIncludedAccountV1ID, body = list(DataObject = body), searchFields = append("ESSAIncludedAccountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSASummaryViews
	#'
	#' This function returns a dataframe or json object of ESSASummaryViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASummaryViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASummaryViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASummaryView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSASummaryViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSASummaryViews <- function(searchConditionsList = NULL, Description = F, DistrictLevelPerPupilFederal = F, DistrictLevelPerPupilStateLocal = F, Enrollment = F, ESSASubmissionID = F, IsDistrictWide = F, SchoolLevelPerPupilFederal = F, SchoolLevelPerPupilStateLocal = F, TotalCosts = F, TotalCostsLessExclusions = F, TotalCostsPerPupil = F, TotalDistrictLevelCostsPerPupil = F, TotalExclusions = F, TotalSchoolLevelCostsPerPupil = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSASummaryView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSASummaryView
	#'
	#' This function returns a dataframe or json object of an ESSASummaryView
	#' @param ESSASummaryViewID The ID of the ESSASummaryView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASummaryView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASummaryView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASummaryView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSASummaryView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSASummaryView <- function(ESSASummaryViewID, Description = F, DistrictLevelPerPupilFederal = F, DistrictLevelPerPupilStateLocal = F, Enrollment = F, ESSASubmissionID = F, IsDistrictWide = F, SchoolLevelPerPupilFederal = F, SchoolLevelPerPupilStateLocal = F, TotalCosts = F, TotalCostsLessExclusions = F, TotalCostsPerPupil = F, TotalDistrictLevelCostsPerPupil = F, TotalExclusions = F, TotalSchoolLevelCostsPerPupil = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSASummaryViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSASummaryView", objectId = ESSASummaryViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSASummaryView
	#'
	#' This function deletes an ESSASummaryView
	#' @param ESSASummaryViewID The ID of the ESSASummaryView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSASummaryViewID of the deleted ESSASummaryView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSASummaryView <- function(ESSASummaryViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSASummaryView", objectId = ESSASummaryViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSASummaryView
	#'
	#' This function creates an ESSASummaryView
	#' @param fieldNames The field values to give the created ESSASummaryView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSASummaryView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSASummaryView <- function(Description = NULL, DistrictLevelPerPupilFederal = NULL, DistrictLevelPerPupilStateLocal = NULL, Enrollment = NULL, IsDistrictWide = NULL, SchoolLevelPerPupilFederal = NULL, SchoolLevelPerPupilStateLocal = NULL, TotalCosts = NULL, TotalCostsLessExclusions = NULL, TotalCostsPerPupil = NULL, TotalDistrictLevelCostsPerPupil = NULL, TotalExclusions = NULL, TotalSchoolLevelCostsPerPupil = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSASummaryView", body = list(DataObject = body), searchFields = append("ESSASummaryViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSASummaryView
	#'
	#' This function modifies an ESSASummaryView
	#' @param fieldNames The field values to give the modified ESSASummaryView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSASummaryView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSASummaryView <- function(ESSASummaryViewID, Description = NULL, DistrictLevelPerPupilFederal = NULL, DistrictLevelPerPupilStateLocal = NULL, Enrollment = NULL, IsDistrictWide = NULL, SchoolLevelPerPupilFederal = NULL, SchoolLevelPerPupilStateLocal = NULL, TotalCosts = NULL, TotalCostsLessExclusions = NULL, TotalCostsPerPupil = NULL, TotalDistrictLevelCostsPerPupil = NULL, TotalExclusions = NULL, TotalSchoolLevelCostsPerPupil = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSASummaryView", objectId = ESSASummaryViewID, body = list(DataObject = body), searchFields = append("ESSASummaryViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAExclusionExceptions
	#'
	#' This function returns a dataframe or json object of ESSAExclusionExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusionExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusionExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusionException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAExclusionExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAExclusionExceptions <- function(searchConditionsList = NULL, CreatedTime = F, ESSAExclusionExceptionID = F, ESSAExclusionID = F, ESSAExclusionV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAExclusionException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAExclusionException
	#'
	#' This function returns a dataframe or json object of an ESSAExclusionException
	#' @param ESSAExclusionExceptionID The ID of the ESSAExclusionException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusionException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusionException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusionException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAExclusionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAExclusionException <- function(ESSAExclusionExceptionID, CreatedTime = F, ESSAExclusionID = F, ESSAExclusionV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAExclusionExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAExclusionException", objectId = ESSAExclusionExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAExclusionException
	#'
	#' This function deletes an ESSAExclusionException
	#' @param ESSAExclusionExceptionID The ID of the ESSAExclusionException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAExclusionExceptionID of the deleted ESSAExclusionException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAExclusionException <- function(ESSAExclusionExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAExclusionException", objectId = ESSAExclusionExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAExclusionException
	#'
	#' This function creates an ESSAExclusionException
	#' @param fieldNames The field values to give the created ESSAExclusionException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAExclusionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAExclusionException <- function(ESSAExclusionID = NULL, ESSAExclusionV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAExclusionException", body = list(DataObject = body), searchFields = append("ESSAExclusionExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAExclusionException
	#'
	#' This function modifies an ESSAExclusionException
	#' @param fieldNames The field values to give the modified ESSAExclusionException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAExclusionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAExclusionException <- function(ESSAExclusionExceptionID, ESSAExclusionID = NULL, ESSAExclusionV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAExclusionException", objectId = ESSAExclusionExceptionID, body = list(DataObject = body), searchFields = append("ESSAExclusionExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSAExclusionV1s
	#'
	#' This function returns a dataframe or json object of ESSAExclusionV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusionV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusionV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusionV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSAExclusionV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSAExclusionV1s <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, ESSAExclusionID = F, ESSAExclusionV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, TotalFederalFunds = F, TotalStateLocalFunds = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSAExclusionV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSAExclusionV1
	#'
	#' This function returns a dataframe or json object of an ESSAExclusionV1
	#' @param ESSAExclusionV1ID The ID of the ESSAExclusionV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSAExclusionV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSAExclusionV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSAExclusionV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSAExclusionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSAExclusionV1 <- function(ESSAExclusionV1ID, CreatedTime = F, Description = F, ESSAExclusionID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, TotalFederalFunds = F, TotalStateLocalFunds = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSAExclusionV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSAExclusionV1", objectId = ESSAExclusionV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSAExclusionV1
	#'
	#' This function deletes an ESSAExclusionV1
	#' @param ESSAExclusionV1ID The ID of the ESSAExclusionV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSAExclusionV1ID of the deleted ESSAExclusionV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSAExclusionV1 <- function(ESSAExclusionV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSAExclusionV1", objectId = ESSAExclusionV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSAExclusionV1
	#'
	#' This function creates an ESSAExclusionV1
	#' @param fieldNames The field values to give the created ESSAExclusionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSAExclusionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSAExclusionV1 <- function(Description = NULL, ESSAExclusionID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSAExclusionV1", body = list(DataObject = body), searchFields = append("ESSAExclusionV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSAExclusionV1
	#'
	#' This function modifies an ESSAExclusionV1
	#' @param fieldNames The field values to give the modified ESSAExclusionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSAExclusionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSAExclusionV1 <- function(ESSAExclusionV1ID, Description = NULL, ESSAExclusionID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSAExclusionV1", objectId = ESSAExclusionV1ID, body = list(DataObject = body), searchFields = append("ESSAExclusionV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSADistrictExceptions
	#'
	#' This function returns a dataframe or json object of ESSADistrictExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSADistrictExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSADistrictExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSADistrictException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSADistrictExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSADistrictExceptions <- function(searchConditionsList = NULL, CreatedTime = F, DistrictName = F, ESSADistrictExceptionID = F, ESSADistrictV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSADistrictException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSADistrictException
	#'
	#' This function returns a dataframe or json object of an ESSADistrictException
	#' @param ESSADistrictExceptionID The ID of the ESSADistrictException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSADistrictException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSADistrictException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSADistrictException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSADistrictException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSADistrictException <- function(ESSADistrictExceptionID, CreatedTime = F, DistrictName = F, ESSADistrictV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSADistrictExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSADistrictException", objectId = ESSADistrictExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSADistrictException
	#'
	#' This function deletes an ESSADistrictException
	#' @param ESSADistrictExceptionID The ID of the ESSADistrictException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSADistrictExceptionID of the deleted ESSADistrictException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSADistrictException <- function(ESSADistrictExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSADistrictException", objectId = ESSADistrictExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSADistrictException
	#'
	#' This function creates an ESSADistrictException
	#' @param fieldNames The field values to give the created ESSADistrictException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSADistrictException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSADistrictException <- function(DistrictName = NULL, ESSADistrictV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSADistrictException", body = list(DataObject = body), searchFields = append("ESSADistrictExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSADistrictException
	#'
	#' This function modifies an ESSADistrictException
	#' @param fieldNames The field values to give the modified ESSADistrictException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSADistrictException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSADistrictException <- function(ESSADistrictExceptionID, DistrictName = NULL, ESSADistrictV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSADistrictException", objectId = ESSADistrictExceptionID, body = list(DataObject = body), searchFields = append("ESSADistrictExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSADistrictV1s
	#'
	#' This function returns a dataframe or json object of ESSADistrictV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSADistrictV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSADistrictV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSADistrictV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSADistrictV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSADistrictV1s <- function(searchConditionsList = NULL, CreatedTime = F, DistrictName = F, Enrollment = F, ESSADistrictV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, TotalExclusions = F, TotalFederalFunds = F, TotalStateLocalFunds = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSADistrictV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSADistrictV1
	#'
	#' This function returns a dataframe or json object of an ESSADistrictV1
	#' @param ESSADistrictV1ID The ID of the ESSADistrictV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSADistrictV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSADistrictV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSADistrictV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSADistrictV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSADistrictV1 <- function(ESSADistrictV1ID, CreatedTime = F, DistrictName = F, Enrollment = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, TotalExclusions = F, TotalFederalFunds = F, TotalStateLocalFunds = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSADistrictV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSADistrictV1", objectId = ESSADistrictV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSADistrictV1
	#'
	#' This function deletes an ESSADistrictV1
	#' @param ESSADistrictV1ID The ID of the ESSADistrictV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSADistrictV1ID of the deleted ESSADistrictV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSADistrictV1 <- function(ESSADistrictV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSADistrictV1", objectId = ESSADistrictV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSADistrictV1
	#'
	#' This function creates an ESSADistrictV1
	#' @param fieldNames The field values to give the created ESSADistrictV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSADistrictV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSADistrictV1 <- function(DistrictName = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSADistrictV1", body = list(DataObject = body), searchFields = append("ESSADistrictV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSADistrictV1
	#'
	#' This function modifies an ESSADistrictV1
	#' @param fieldNames The field values to give the modified ESSADistrictV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSADistrictV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSADistrictV1 <- function(ESSADistrictV1ID, DistrictName = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSADistrictV1", objectId = ESSADistrictV1ID, body = list(DataObject = body), searchFields = append("ESSADistrictV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSASchoolExceptions
	#'
	#' This function returns a dataframe or json object of ESSASchoolExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASchoolExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASchoolExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASchoolException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSASchoolExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSASchoolExceptions <- function(searchConditionsList = NULL, CreatedTime = F, ESSAExpenditureID = F, ESSASchoolExceptionID = F, ESSASchoolV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSASchoolException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSASchoolException
	#'
	#' This function returns a dataframe or json object of an ESSASchoolException
	#' @param ESSASchoolExceptionID The ID of the ESSASchoolException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASchoolException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASchoolException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASchoolException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSASchoolException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSASchoolException <- function(ESSASchoolExceptionID, CreatedTime = F, ESSAExpenditureID = F, ESSASchoolV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSASchoolExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSASchoolException", objectId = ESSASchoolExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSASchoolException
	#'
	#' This function deletes an ESSASchoolException
	#' @param ESSASchoolExceptionID The ID of the ESSASchoolException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSASchoolExceptionID of the deleted ESSASchoolException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSASchoolException <- function(ESSASchoolExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSASchoolException", objectId = ESSASchoolExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSASchoolException
	#'
	#' This function creates an ESSASchoolException
	#' @param fieldNames The field values to give the created ESSASchoolException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSASchoolException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSASchoolException <- function(ESSAExpenditureID = NULL, ESSASchoolV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSASchoolException", body = list(DataObject = body), searchFields = append("ESSASchoolExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSASchoolException
	#'
	#' This function modifies an ESSASchoolException
	#' @param fieldNames The field values to give the modified ESSASchoolException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSASchoolException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSASchoolException <- function(ESSASchoolExceptionID, ESSAExpenditureID = NULL, ESSASchoolV1ID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSASchoolException", objectId = ESSASchoolExceptionID, body = list(DataObject = body), searchFields = append("ESSASchoolExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSASchoolV1s
	#'
	#' This function returns a dataframe or json object of ESSASchoolV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASchoolV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASchoolV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASchoolV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSASchoolV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSASchoolV1s <- function(searchConditionsList = NULL, CreatedTime = F, Enrollment = F, ESSAExpenditureID = F, ESSASchoolV1ID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, SchoolName = F, TotalFederalFunds = F, TotalStateLocalFunds = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSASchoolV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSASchoolV1
	#'
	#' This function returns a dataframe or json object of an ESSASchoolV1
	#' @param ESSASchoolV1ID The ID of the ESSASchoolV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASchoolV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASchoolV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASchoolV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSASchoolV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSASchoolV1 <- function(ESSASchoolV1ID, CreatedTime = F, Enrollment = F, ESSAExpenditureID = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, SchoolName = F, TotalFederalFunds = F, TotalStateLocalFunds = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSASchoolV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSASchoolV1", objectId = ESSASchoolV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSASchoolV1
	#'
	#' This function deletes an ESSASchoolV1
	#' @param ESSASchoolV1ID The ID of the ESSASchoolV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSASchoolV1ID of the deleted ESSASchoolV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSASchoolV1 <- function(ESSASchoolV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSASchoolV1", objectId = ESSASchoolV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSASchoolV1
	#'
	#' This function creates an ESSASchoolV1
	#' @param fieldNames The field values to give the created ESSASchoolV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSASchoolV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSASchoolV1 <- function(Enrollment = NULL, ESSAExpenditureID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, SchoolName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSASchoolV1", body = list(DataObject = body), searchFields = append("ESSASchoolV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSASchoolV1
	#'
	#' This function modifies an ESSASchoolV1
	#' @param fieldNames The field values to give the modified ESSASchoolV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSASchoolV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSASchoolV1 <- function(ESSASchoolV1ID, Enrollment = NULL, ESSAExpenditureID = NULL, ESSASubmissionID = NULL, ESSASubmissionRunHistoryID = NULL, SchoolName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSASchoolV1", objectId = ESSASchoolV1ID, body = list(DataObject = body), searchFields = append("ESSASchoolV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSASubmissions
	#'
	#' This function returns a dataframe or json object of ESSASubmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASubmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASubmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASubmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSASubmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSASubmissions <- function(searchConditionsList = NULL, CreatedTime = F, DistrictID = F, ESSASubmissionID = F, FiscalYearID = F, HasAnExport = F, HasAnExtract = F, ModifiedTime = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSASubmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSASubmission
	#'
	#' This function returns a dataframe or json object of an ESSASubmission
	#' @param ESSASubmissionID The ID of the ESSASubmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASubmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASubmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASubmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSASubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSASubmission <- function(ESSASubmissionID, CreatedTime = F, DistrictID = F, FiscalYearID = F, HasAnExport = F, HasAnExtract = F, ModifiedTime = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSASubmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSASubmission", objectId = ESSASubmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSASubmission
	#'
	#' This function deletes an ESSASubmission
	#' @param ESSASubmissionID The ID of the ESSASubmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSASubmissionID of the deleted ESSASubmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSASubmission <- function(ESSASubmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSASubmission", objectId = ESSASubmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSASubmission
	#'
	#' This function creates an ESSASubmission
	#' @param fieldNames The field values to give the created ESSASubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSASubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSASubmission <- function(DistrictID = NULL, FiscalYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSASubmission", body = list(DataObject = body), searchFields = append("ESSASubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSASubmission
	#'
	#' This function modifies an ESSASubmission
	#' @param fieldNames The field values to give the modified ESSASubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSASubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSASubmission <- function(ESSASubmissionID, DistrictID = NULL, FiscalYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSASubmission", objectId = ESSASubmissionID, body = list(DataObject = body), searchFields = append("ESSASubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSASubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of ESSASubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSASubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSASubmissionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ESSASubmissionID = F, ESSASubmissionRunHistoryID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSASubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSASubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of an ESSASubmissionRunHistory
	#' @param ESSASubmissionRunHistoryID The ID of the ESSASubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSASubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSASubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSASubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSASubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSASubmissionRunHistory <- function(ESSASubmissionRunHistoryID, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ESSASubmissionID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSASubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSASubmissionRunHistory", objectId = ESSASubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSASubmissionRunHistory
	#'
	#' This function deletes an ESSASubmissionRunHistory
	#' @param ESSASubmissionRunHistoryID The ID of the ESSASubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSASubmissionRunHistoryID of the deleted ESSASubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSASubmissionRunHistory <- function(ESSASubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSASubmissionRunHistory", objectId = ESSASubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSASubmissionRunHistory
	#'
	#' This function creates an ESSASubmissionRunHistory
	#' @param fieldNames The field values to give the created ESSASubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSASubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSASubmissionRunHistory <- function(EndDateTime = NULL, ESSASubmissionID = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSASubmissionRunHistory", body = list(DataObject = body), searchFields = append("ESSASubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSASubmissionRunHistory
	#'
	#' This function modifies an ESSASubmissionRunHistory
	#' @param fieldNames The field values to give the modified ESSASubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSASubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSASubmissionRunHistory <- function(ESSASubmissionRunHistoryID, EndDateTime = NULL, ESSASubmissionID = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSASubmissionRunHistory", objectId = ESSASubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("ESSASubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MassUpdateStateReportingSourceRunHistories
	#'
	#' This function returns a dataframe or json object of MassUpdateStateReportingSourceRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateStateReportingSourceRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateStateReportingSourceRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateStateReportingSourceRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of MassUpdateStateReportingSourceRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMassUpdateStateReportingSourceRunHistories <- function(searchConditionsList = NULL, AccountFiscalYearStandardFilterData = F, AccountStandardFilterData = F, AssignmentPayTypeStandardFilterData = F, AssignmentStandardFilterData = F, CreatedTime = F, DistrictID = F, EmployeeDistrictStandardFilterData = F, EmployeeFiscalYearStandardFilterData = F, EmployeeRetirementStandardFilterData = F, EmployeeRetirementStatusStandardFilterData = F, EmployeeStandardFilterData = F, EndTime = F, FilterParameters = F, IsLocked = F, MassUpdateStateReportingSourceRunHistoryID = F, ModifiedTime = F, NameStandardFilterData = F, RecordParameters = F, RunData = F, RunParameters = F, RunReason = F, STAREmploymentPeriodFilterData = F, StartTime = F, Status = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "MassUpdateStateReportingSourceRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MassUpdateStateReportingSourceRunHistory
	#'
	#' This function returns a dataframe or json object of a MassUpdateStateReportingSourceRunHistory
	#' @param MassUpdateStateReportingSourceRunHistoryID The ID of the MassUpdateStateReportingSourceRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateStateReportingSourceRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateStateReportingSourceRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateStateReportingSourceRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of MassUpdateStateReportingSourceRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMassUpdateStateReportingSourceRunHistory <- function(MassUpdateStateReportingSourceRunHistoryID, AccountFiscalYearStandardFilterData = F, AccountStandardFilterData = F, AssignmentPayTypeStandardFilterData = F, AssignmentStandardFilterData = F, CreatedTime = F, DistrictID = F, EmployeeDistrictStandardFilterData = F, EmployeeFiscalYearStandardFilterData = F, EmployeeRetirementStandardFilterData = F, EmployeeRetirementStatusStandardFilterData = F, EmployeeStandardFilterData = F, EndTime = F, FilterParameters = F, IsLocked = F, ModifiedTime = F, NameStandardFilterData = F, RecordParameters = F, RunData = F, RunParameters = F, RunReason = F, STAREmploymentPeriodFilterData = F, StartTime = F, Status = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MassUpdateStateReportingSourceRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "MassUpdateStateReportingSourceRunHistory", objectId = MassUpdateStateReportingSourceRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MassUpdateStateReportingSourceRunHistory
	#'
	#' This function deletes a MassUpdateStateReportingSourceRunHistory
	#' @param MassUpdateStateReportingSourceRunHistoryID The ID of the MassUpdateStateReportingSourceRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The MassUpdateStateReportingSourceRunHistoryID of the deleted MassUpdateStateReportingSourceRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMassUpdateStateReportingSourceRunHistory <- function(MassUpdateStateReportingSourceRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "MassUpdateStateReportingSourceRunHistory", objectId = MassUpdateStateReportingSourceRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MassUpdateStateReportingSourceRunHistory
	#'
	#' This function creates a MassUpdateStateReportingSourceRunHistory
	#' @param fieldNames The field values to give the created MassUpdateStateReportingSourceRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created MassUpdateStateReportingSourceRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMassUpdateStateReportingSourceRunHistory <- function(DistrictID = NULL, EndTime = NULL, FilterParameters = NULL, RecordParameters = NULL, RunReason = NULL, StartTime = NULL, Status = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "MassUpdateStateReportingSourceRunHistory", body = list(DataObject = body), searchFields = append("MassUpdateStateReportingSourceRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MassUpdateStateReportingSourceRunHistory
	#'
	#' This function modifies a MassUpdateStateReportingSourceRunHistory
	#' @param fieldNames The field values to give the modified MassUpdateStateReportingSourceRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified MassUpdateStateReportingSourceRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMassUpdateStateReportingSourceRunHistory <- function(MassUpdateStateReportingSourceRunHistoryID, DistrictID = NULL, EndTime = NULL, FilterParameters = NULL, RecordParameters = NULL, RunReason = NULL, StartTime = NULL, Status = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "MassUpdateStateReportingSourceRunHistory", objectId = MassUpdateStateReportingSourceRunHistoryID, body = list(DataObject = body), searchFields = append("MassUpdateStateReportingSourceRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempMassUpdateStateReportingSourceUtilityErrors
	#'
	#' This function returns a dataframe or json object of TempMassUpdateStateReportingSourceUtilityErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMassUpdateStateReportingSourceUtilityErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMassUpdateStateReportingSourceUtilityErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMassUpdateStateReportingSourceUtilityError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of TempMassUpdateStateReportingSourceUtilityErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempMassUpdateStateReportingSourceUtilityErrors <- function(searchConditionsList = NULL, CreatedTime = F, FailureReason = F, ModifiedTime = F, RecordIdentifier = F, TempMassUpdateStateReportingSourceUtilityErrorID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempMassUpdateStateReportingSourceUtilityError
	#'
	#' This function returns a dataframe or json object of a TempMassUpdateStateReportingSourceUtilityError
	#' @param TempMassUpdateStateReportingSourceUtilityErrorID The ID of the TempMassUpdateStateReportingSourceUtilityError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMassUpdateStateReportingSourceUtilityError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMassUpdateStateReportingSourceUtilityError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMassUpdateStateReportingSourceUtilityError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of TempMassUpdateStateReportingSourceUtilityError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempMassUpdateStateReportingSourceUtilityError <- function(TempMassUpdateStateReportingSourceUtilityErrorID, CreatedTime = F, FailureReason = F, ModifiedTime = F, RecordIdentifier = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempMassUpdateStateReportingSourceUtilityErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityError", objectId = TempMassUpdateStateReportingSourceUtilityErrorID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempMassUpdateStateReportingSourceUtilityError
	#'
	#' This function deletes a TempMassUpdateStateReportingSourceUtilityError
	#' @param TempMassUpdateStateReportingSourceUtilityErrorID The ID of the TempMassUpdateStateReportingSourceUtilityError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The TempMassUpdateStateReportingSourceUtilityErrorID of the deleted TempMassUpdateStateReportingSourceUtilityError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempMassUpdateStateReportingSourceUtilityError <- function(TempMassUpdateStateReportingSourceUtilityErrorID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityError", objectId = TempMassUpdateStateReportingSourceUtilityErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempMassUpdateStateReportingSourceUtilityError
	#'
	#' This function creates a TempMassUpdateStateReportingSourceUtilityError
	#' @param fieldNames The field values to give the created TempMassUpdateStateReportingSourceUtilityError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created TempMassUpdateStateReportingSourceUtilityError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempMassUpdateStateReportingSourceUtilityError <- function(FailureReason = NULL, RecordIdentifier = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityError", body = list(DataObject = body), searchFields = append("TempMassUpdateStateReportingSourceUtilityErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempMassUpdateStateReportingSourceUtilityError
	#'
	#' This function modifies a TempMassUpdateStateReportingSourceUtilityError
	#' @param fieldNames The field values to give the modified TempMassUpdateStateReportingSourceUtilityError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified TempMassUpdateStateReportingSourceUtilityError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempMassUpdateStateReportingSourceUtilityError <- function(TempMassUpdateStateReportingSourceUtilityErrorID, FailureReason = NULL, RecordIdentifier = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityError", objectId = TempMassUpdateStateReportingSourceUtilityErrorID, body = list(DataObject = body), searchFields = append("TempMassUpdateStateReportingSourceUtilityErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempMassUpdateStateReportingSourceUtilityRecordTypes
	#'
	#' This function returns a dataframe or json object of TempMassUpdateStateReportingSourceUtilityRecordTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMassUpdateStateReportingSourceUtilityRecordTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMassUpdateStateReportingSourceUtilityRecordTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMassUpdateStateReportingSourceUtilityRecordType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of TempMassUpdateStateReportingSourceUtilityRecordTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempMassUpdateStateReportingSourceUtilityRecordTypes <- function(searchConditionsList = NULL, CreatedTime = F, FilterType = F, ModifiedTime = F, ModuleFriendlyName = F, RecordType = F, RecordTypeFriendlyName = F, StateReportingSourceDBTableName = F, StateReportingSourceID = F, TempMassUpdateStateReportingSourceUtilityRecordTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityRecordType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempMassUpdateStateReportingSourceUtilityRecordType
	#'
	#' This function returns a dataframe or json object of a TempMassUpdateStateReportingSourceUtilityRecordType
	#' @param TempMassUpdateStateReportingSourceUtilityRecordTypeID The ID of the TempMassUpdateStateReportingSourceUtilityRecordType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMassUpdateStateReportingSourceUtilityRecordType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMassUpdateStateReportingSourceUtilityRecordType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMassUpdateStateReportingSourceUtilityRecordType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of TempMassUpdateStateReportingSourceUtilityRecordType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempMassUpdateStateReportingSourceUtilityRecordType <- function(TempMassUpdateStateReportingSourceUtilityRecordTypeID, CreatedTime = F, FilterType = F, ModifiedTime = F, ModuleFriendlyName = F, RecordType = F, RecordTypeFriendlyName = F, StateReportingSourceDBTableName = F, StateReportingSourceID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempMassUpdateStateReportingSourceUtilityRecordTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityRecordType", objectId = TempMassUpdateStateReportingSourceUtilityRecordTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempMassUpdateStateReportingSourceUtilityRecordType
	#'
	#' This function deletes a TempMassUpdateStateReportingSourceUtilityRecordType
	#' @param TempMassUpdateStateReportingSourceUtilityRecordTypeID The ID of the TempMassUpdateStateReportingSourceUtilityRecordType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The TempMassUpdateStateReportingSourceUtilityRecordTypeID of the deleted TempMassUpdateStateReportingSourceUtilityRecordType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempMassUpdateStateReportingSourceUtilityRecordType <- function(TempMassUpdateStateReportingSourceUtilityRecordTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityRecordType", objectId = TempMassUpdateStateReportingSourceUtilityRecordTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempMassUpdateStateReportingSourceUtilityRecordType
	#'
	#' This function creates a TempMassUpdateStateReportingSourceUtilityRecordType
	#' @param fieldNames The field values to give the created TempMassUpdateStateReportingSourceUtilityRecordType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created TempMassUpdateStateReportingSourceUtilityRecordType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempMassUpdateStateReportingSourceUtilityRecordType <- function(FilterType = NULL, ModuleFriendlyName = NULL, RecordType = NULL, RecordTypeFriendlyName = NULL, StateReportingSourceDBTableName = NULL, StateReportingSourceID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityRecordType", body = list(DataObject = body), searchFields = append("TempMassUpdateStateReportingSourceUtilityRecordTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempMassUpdateStateReportingSourceUtilityRecordType
	#'
	#' This function modifies a TempMassUpdateStateReportingSourceUtilityRecordType
	#' @param fieldNames The field values to give the modified TempMassUpdateStateReportingSourceUtilityRecordType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified TempMassUpdateStateReportingSourceUtilityRecordType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempMassUpdateStateReportingSourceUtilityRecordType <- function(TempMassUpdateStateReportingSourceUtilityRecordTypeID, FilterType = NULL, ModuleFriendlyName = NULL, RecordType = NULL, RecordTypeFriendlyName = NULL, StateReportingSourceDBTableName = NULL, StateReportingSourceID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TempMassUpdateStateReportingSourceUtilityRecordType", objectId = TempMassUpdateStateReportingSourceUtilityRecordTypeID, body = list(DataObject = body), searchFields = append("TempMassUpdateStateReportingSourceUtilityRecordTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ESSADefaultExclusions
	#'
	#' This function returns a dataframe or json object of ESSADefaultExclusions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSADefaultExclusions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSADefaultExclusions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSADefaultExclusion') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of ESSADefaultExclusions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listESSADefaultExclusions <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, ESSADefaultExclusionID = F, FilterHigh = F, FilterLow = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StructureDimensionID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "ESSADefaultExclusion", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ESSADefaultExclusion
	#'
	#' This function returns a dataframe or json object of an ESSADefaultExclusion
	#' @param ESSADefaultExclusionID The ID of the ESSADefaultExclusion to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ESSADefaultExclusion. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ESSADefaultExclusion.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ESSADefaultExclusion') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of ESSADefaultExclusion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getESSADefaultExclusion <- function(ESSADefaultExclusionID, CreatedTime = F, Description = F, FilterHigh = F, FilterLow = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StructureDimensionID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ESSADefaultExclusionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "ESSADefaultExclusion", objectId = ESSADefaultExclusionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ESSADefaultExclusion
	#'
	#' This function deletes an ESSADefaultExclusion
	#' @param ESSADefaultExclusionID The ID of the ESSADefaultExclusion to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The ESSADefaultExclusionID of the deleted ESSADefaultExclusion.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteESSADefaultExclusion <- function(ESSADefaultExclusionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "ESSADefaultExclusion", objectId = ESSADefaultExclusionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ESSADefaultExclusion
	#'
	#' This function creates an ESSADefaultExclusion
	#' @param fieldNames The field values to give the created ESSADefaultExclusion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created ESSADefaultExclusion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createESSADefaultExclusion <- function(Description = NULL, FilterHigh = NULL, FilterLow = NULL, IsInvalid = NULL, StructureDimensionID = NULL, Type = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "ESSADefaultExclusion", body = list(DataObject = body), searchFields = append("ESSADefaultExclusionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ESSADefaultExclusion
	#'
	#' This function modifies an ESSADefaultExclusion
	#' @param fieldNames The field values to give the modified ESSADefaultExclusion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified ESSADefaultExclusion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyESSADefaultExclusion <- function(ESSADefaultExclusionID, Description = NULL, FilterHigh = NULL, FilterLow = NULL, IsInvalid = NULL, StructureDimensionID = NULL, Type = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "ESSADefaultExclusion", objectId = ESSADefaultExclusionID, body = list(DataObject = body), searchFields = append("ESSADefaultExclusionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NewHireEmployerExceptions
	#'
	#' This function returns a dataframe or json object of NewHireEmployerExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireEmployerExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireEmployerExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireEmployerException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of NewHireEmployerExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNewHireEmployerExceptions <- function(searchConditionsList = NULL, CreatedTime = F, EmployerName = F, Message = F, MessageType = F, ModifiedTime = F, NewHireEmployerExceptionID = F, NewHireEmployerV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "NewHireEmployerException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NewHireEmployerException
	#'
	#' This function returns a dataframe or json object of a NewHireEmployerException
	#' @param NewHireEmployerExceptionID The ID of the NewHireEmployerException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireEmployerException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireEmployerException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireEmployerException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of NewHireEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNewHireEmployerException <- function(NewHireEmployerExceptionID, CreatedTime = F, EmployerName = F, Message = F, MessageType = F, ModifiedTime = F, NewHireEmployerV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NewHireEmployerExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "NewHireEmployerException", objectId = NewHireEmployerExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NewHireEmployerException
	#'
	#' This function deletes a NewHireEmployerException
	#' @param NewHireEmployerExceptionID The ID of the NewHireEmployerException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The NewHireEmployerExceptionID of the deleted NewHireEmployerException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNewHireEmployerException <- function(NewHireEmployerExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "NewHireEmployerException", objectId = NewHireEmployerExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NewHireEmployerException
	#'
	#' This function creates a NewHireEmployerException
	#' @param fieldNames The field values to give the created NewHireEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created NewHireEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNewHireEmployerException <- function(EmployerName = NULL, Message = NULL, MessageType = NULL, NewHireEmployerV1ID = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "NewHireEmployerException", body = list(DataObject = body), searchFields = append("NewHireEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NewHireEmployerException
	#'
	#' This function modifies a NewHireEmployerException
	#' @param fieldNames The field values to give the modified NewHireEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified NewHireEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNewHireEmployerException <- function(NewHireEmployerExceptionID, EmployerName = NULL, Message = NULL, MessageType = NULL, NewHireEmployerV1ID = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "NewHireEmployerException", objectId = NewHireEmployerExceptionID, body = list(DataObject = body), searchFields = append("NewHireEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NewHireEmployeeExceptions
	#'
	#' This function returns a dataframe or json object of NewHireEmployeeExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireEmployeeExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireEmployeeExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireEmployeeException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of NewHireEmployeeExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNewHireEmployeeExceptions <- function(searchConditionsList = NULL, CreatedTime = F, FirstName = F, LastName = F, Message = F, MessageType = F, MiddleName = F, ModifiedTime = F, NewHireEmployeeExceptionID = F, NewHireEmployeeV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "NewHireEmployeeException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NewHireEmployeeException
	#'
	#' This function returns a dataframe or json object of a NewHireEmployeeException
	#' @param NewHireEmployeeExceptionID The ID of the NewHireEmployeeException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireEmployeeException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireEmployeeException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireEmployeeException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of NewHireEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNewHireEmployeeException <- function(NewHireEmployeeExceptionID, CreatedTime = F, FirstName = F, LastName = F, Message = F, MessageType = F, MiddleName = F, ModifiedTime = F, NewHireEmployeeV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NewHireEmployeeExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "NewHireEmployeeException", objectId = NewHireEmployeeExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NewHireEmployeeException
	#'
	#' This function deletes a NewHireEmployeeException
	#' @param NewHireEmployeeExceptionID The ID of the NewHireEmployeeException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The NewHireEmployeeExceptionID of the deleted NewHireEmployeeException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNewHireEmployeeException <- function(NewHireEmployeeExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "NewHireEmployeeException", objectId = NewHireEmployeeExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NewHireEmployeeException
	#'
	#' This function creates a NewHireEmployeeException
	#' @param fieldNames The field values to give the created NewHireEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created NewHireEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNewHireEmployeeException <- function(FirstName = NULL, LastName = NULL, Message = NULL, MessageType = NULL, MiddleName = NULL, NewHireEmployeeV1ID = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "NewHireEmployeeException", body = list(DataObject = body), searchFields = append("NewHireEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NewHireEmployeeException
	#'
	#' This function modifies a NewHireEmployeeException
	#' @param fieldNames The field values to give the modified NewHireEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified NewHireEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNewHireEmployeeException <- function(NewHireEmployeeExceptionID, FirstName = NULL, LastName = NULL, Message = NULL, MessageType = NULL, MiddleName = NULL, NewHireEmployeeV1ID = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "NewHireEmployeeException", objectId = NewHireEmployeeExceptionID, body = list(DataObject = body), searchFields = append("NewHireEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateReportingNewHireEmployerV1s
	#'
	#' This function returns a dataframe or json object of StateReportingNewHireEmployerV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingNewHireEmployerV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingNewHireEmployerV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingNewHireEmployerV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateReportingNewHireEmployerV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateReportingNewHireEmployerV1s <- function(searchConditionsList = NULL, AddressLine1 = F, AddressLine2 = F, AddressLine3 = F, AddressType = F, City = F, CreatedTime = F, EmployerID = F, EmployerName = F, FederalEIN = F, GECCountryCode = F, GECCountryID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, NewHireEmployerV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, PostalCode = F, StateCode = F, StateID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipCode = F, ZipCodePlusFour = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "NewHireEmployerV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateReportingNewHireEmployerV1
	#'
	#' This function returns a dataframe or json object of a StateReportingNewHireEmployerV1
	#' @param StateReportingNewHireEmployerV1ID The ID of the StateReportingNewHireEmployerV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingNewHireEmployerV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingNewHireEmployerV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingNewHireEmployerV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateReportingNewHireEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateReportingNewHireEmployerV1 <- function(StateReportingNewHireEmployerV1ID, AddressLine1 = F, AddressLine2 = F, AddressLine3 = F, AddressType = F, City = F, CreatedTime = F, EmployerID = F, EmployerName = F, FederalEIN = F, GECCountryCode = F, GECCountryID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, NewHireEmployerV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, PostalCode = F, StateCode = F, StateID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipCode = F, ZipCodePlusFour = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateReportingNewHireEmployerV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "NewHireEmployerV1", objectId = StateReportingNewHireEmployerV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateReportingNewHireEmployerV1
	#'
	#' This function deletes a StateReportingNewHireEmployerV1
	#' @param StateReportingNewHireEmployerV1ID The ID of the StateReportingNewHireEmployerV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateReportingNewHireEmployerV1ID of the deleted StateReportingNewHireEmployerV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateReportingNewHireEmployerV1 <- function(StateReportingNewHireEmployerV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "NewHireEmployerV1", objectId = StateReportingNewHireEmployerV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateReportingNewHireEmployerV1
	#'
	#' This function creates a StateReportingNewHireEmployerV1
	#' @param fieldNames The field values to give the created StateReportingNewHireEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateReportingNewHireEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateReportingNewHireEmployerV1 <- function(AddressLine1 = NULL, AddressLine2 = NULL, AddressLine3 = NULL, AddressType = NULL, City = NULL, EmployerID = NULL, EmployerName = NULL, FederalEIN = NULL, GECCountryCode = NULL, GECCountryID = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, PostalCode = NULL, StateCode = NULL, StateID = NULL, ZipCode = NULL, ZipCodePlusFour = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "NewHireEmployerV1", body = list(DataObject = body), searchFields = append("NewHireEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateReportingNewHireEmployerV1
	#'
	#' This function modifies a StateReportingNewHireEmployerV1
	#' @param fieldNames The field values to give the modified StateReportingNewHireEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateReportingNewHireEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateReportingNewHireEmployerV1 <- function(NewHireEmployerV1ID, AddressLine1 = NULL, AddressLine2 = NULL, AddressLine3 = NULL, AddressType = NULL, City = NULL, EmployerID = NULL, EmployerName = NULL, FederalEIN = NULL, GECCountryCode = NULL, GECCountryID = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, PostalCode = NULL, StateCode = NULL, StateID = NULL, ZipCode = NULL, ZipCodePlusFour = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "NewHireEmployerV1", objectId = NewHireEmployerV1ID, body = list(DataObject = body), searchFields = append("NewHireEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateReportingNewHireEmployeeV1s
	#'
	#' This function returns a dataframe or json object of StateReportingNewHireEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingNewHireEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingNewHireEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingNewHireEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateReportingNewHireEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateReportingNewHireEmployeeV1s <- function(searchConditionsList = NULL, AddressLine1 = F, AddressLine2 = F, AddressLine3 = F, AddressType = F, BirthDate = F, City = F, CountryName = F, CreatedTime = F, EmployeeID = F, FirstName = F, GECCountryCode = F, GECCountryID = F, HasErrors = F, HireDate = F, IsRehire = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, NewHireEmployeeV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, PostalCode = F, SocialSecurityNumber = F, StateCode = F, StateID = F, StateIDWork = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkStateCode = F, ZipCode = F, ZipCodePlusFour = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "NewHireEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateReportingNewHireEmployeeV1
	#'
	#' This function returns a dataframe or json object of a StateReportingNewHireEmployeeV1
	#' @param StateReportingNewHireEmployeeV1ID The ID of the StateReportingNewHireEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingNewHireEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingNewHireEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingNewHireEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateReportingNewHireEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateReportingNewHireEmployeeV1 <- function(StateReportingNewHireEmployeeV1ID, AddressLine1 = F, AddressLine2 = F, AddressLine3 = F, AddressType = F, BirthDate = F, City = F, CountryName = F, CreatedTime = F, EmployeeID = F, FirstName = F, GECCountryCode = F, GECCountryID = F, HasErrors = F, HireDate = F, IsRehire = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, NewHireEmployeeV1ID = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, PostalCode = F, SocialSecurityNumber = F, StateCode = F, StateID = F, StateIDWork = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkStateCode = F, ZipCode = F, ZipCodePlusFour = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateReportingNewHireEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "NewHireEmployeeV1", objectId = StateReportingNewHireEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateReportingNewHireEmployeeV1
	#'
	#' This function deletes a StateReportingNewHireEmployeeV1
	#' @param StateReportingNewHireEmployeeV1ID The ID of the StateReportingNewHireEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateReportingNewHireEmployeeV1ID of the deleted StateReportingNewHireEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateReportingNewHireEmployeeV1 <- function(StateReportingNewHireEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "NewHireEmployeeV1", objectId = StateReportingNewHireEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateReportingNewHireEmployeeV1
	#'
	#' This function creates a StateReportingNewHireEmployeeV1
	#' @param fieldNames The field values to give the created StateReportingNewHireEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateReportingNewHireEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateReportingNewHireEmployeeV1 <- function(AddressLine1 = NULL, AddressLine2 = NULL, AddressLine3 = NULL, AddressType = NULL, BirthDate = NULL, City = NULL, CountryName = NULL, EmployeeID = NULL, FirstName = NULL, GECCountryCode = NULL, GECCountryID = NULL, HireDate = NULL, IsRehire = NULL, LastName = NULL, MiddleName = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, PostalCode = NULL, SocialSecurityNumber = NULL, StateCode = NULL, StateID = NULL, StateIDWork = NULL, WorkStateCode = NULL, ZipCode = NULL, ZipCodePlusFour = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "NewHireEmployeeV1", body = list(DataObject = body), searchFields = append("NewHireEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateReportingNewHireEmployeeV1
	#'
	#' This function modifies a StateReportingNewHireEmployeeV1
	#' @param fieldNames The field values to give the modified StateReportingNewHireEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateReportingNewHireEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateReportingNewHireEmployeeV1 <- function(NewHireEmployeeV1ID, AddressLine1 = NULL, AddressLine2 = NULL, AddressLine3 = NULL, AddressType = NULL, BirthDate = NULL, City = NULL, CountryName = NULL, EmployeeID = NULL, FirstName = NULL, GECCountryCode = NULL, GECCountryID = NULL, HireDate = NULL, IsRehire = NULL, LastName = NULL, MiddleName = NULL, NewHireSubmissionID = NULL, NewHireSubmissionRunHistoryID = NULL, PostalCode = NULL, SocialSecurityNumber = NULL, StateCode = NULL, StateID = NULL, StateIDWork = NULL, WorkStateCode = NULL, ZipCode = NULL, ZipCodePlusFour = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "NewHireEmployeeV1", objectId = NewHireEmployeeV1ID, body = list(DataObject = body), searchFields = append("NewHireEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NewHireSubmissions
	#'
	#' This function returns a dataframe or json object of NewHireSubmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireSubmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireSubmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireSubmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of NewHireSubmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNewHireSubmissions <- function(searchConditionsList = NULL, CreatedTime = F, DistrictID = F, FiscalYearID = F, HasAnExport = F, HasAnExtract = F, HireDateHigh = F, HireDateLow = F, IsEditable = F, ModifiedTime = F, MostRecentExportTime = F, NewHireSubmissionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "NewHireSubmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NewHireSubmission
	#'
	#' This function returns a dataframe or json object of a NewHireSubmission
	#' @param NewHireSubmissionID The ID of the NewHireSubmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireSubmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireSubmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireSubmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of NewHireSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNewHireSubmission <- function(NewHireSubmissionID, CreatedTime = F, DistrictID = F, FiscalYearID = F, HasAnExport = F, HasAnExtract = F, HireDateHigh = F, HireDateLow = F, IsEditable = F, ModifiedTime = F, MostRecentExportTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NewHireSubmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "NewHireSubmission", objectId = NewHireSubmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NewHireSubmission
	#'
	#' This function deletes a NewHireSubmission
	#' @param NewHireSubmissionID The ID of the NewHireSubmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The NewHireSubmissionID of the deleted NewHireSubmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNewHireSubmission <- function(NewHireSubmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "NewHireSubmission", objectId = NewHireSubmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NewHireSubmission
	#'
	#' This function creates a NewHireSubmission
	#' @param fieldNames The field values to give the created NewHireSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created NewHireSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNewHireSubmission <- function(DistrictID = NULL, FiscalYearID = NULL, HireDateHigh = NULL, HireDateLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "NewHireSubmission", body = list(DataObject = body), searchFields = append("NewHireSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NewHireSubmission
	#'
	#' This function modifies a NewHireSubmission
	#' @param fieldNames The field values to give the modified NewHireSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified NewHireSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNewHireSubmission <- function(NewHireSubmissionID, DistrictID = NULL, FiscalYearID = NULL, HireDateHigh = NULL, HireDateLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "NewHireSubmission", objectId = NewHireSubmissionID, body = list(DataObject = body), searchFields = append("NewHireSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NewHireSubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of NewHireSubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireSubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireSubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireSubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of NewHireSubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNewHireSubmissionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, NewHireSubmissionID = F, NewHireSubmissionRunHistoryID = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "NewHireSubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NewHireSubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of a NewHireSubmissionRunHistory
	#' @param NewHireSubmissionRunHistoryID The ID of the NewHireSubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NewHireSubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NewHireSubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NewHireSubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of NewHireSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNewHireSubmissionRunHistory <- function(NewHireSubmissionRunHistoryID, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, NewHireSubmissionID = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NewHireSubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "NewHireSubmissionRunHistory", objectId = NewHireSubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NewHireSubmissionRunHistory
	#'
	#' This function deletes a NewHireSubmissionRunHistory
	#' @param NewHireSubmissionRunHistoryID The ID of the NewHireSubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The NewHireSubmissionRunHistoryID of the deleted NewHireSubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNewHireSubmissionRunHistory <- function(NewHireSubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "NewHireSubmissionRunHistory", objectId = NewHireSubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NewHireSubmissionRunHistory
	#'
	#' This function creates a NewHireSubmissionRunHistory
	#' @param fieldNames The field values to give the created NewHireSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created NewHireSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNewHireSubmissionRunHistory <- function(EndDateTime = NULL, MediaID = NULL, NewHireSubmissionID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "NewHireSubmissionRunHistory", body = list(DataObject = body), searchFields = append("NewHireSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NewHireSubmissionRunHistory
	#'
	#' This function modifies a NewHireSubmissionRunHistory
	#' @param fieldNames The field values to give the modified NewHireSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified NewHireSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNewHireSubmissionRunHistory <- function(NewHireSubmissionRunHistoryID, EndDateTime = NULL, MediaID = NULL, NewHireSubmissionID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "NewHireSubmissionRunHistory", objectId = NewHireSubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("NewHireSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationSubmissionMultipleWorksiteViews
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationSubmissionMultipleWorksiteViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationSubmissionMultipleWorksiteViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationSubmissionMultipleWorksiteViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationSubmissionMultipleWorksiteView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationSubmissionMultipleWorksiteViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationSubmissionMultipleWorksiteViews <- function(searchConditionsList = NULL, BuildingID = F, PaidFor12thDayOfMonth1EmployeeCount = F, PaidFor12thDayOfMonth2EmployeeCount = F, PaidFor12thDayOfMonth3EmployeeCount = F, TotalQuarterlyWages = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionMultipleWorksiteViewID = F, Worksite = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionMultipleWorksiteView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationSubmissionMultipleWorksiteView
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationSubmissionMultipleWorksiteView
	#' @param UnemploymentCompensationSubmissionMultipleWorksiteViewID The ID of the UnemploymentCompensationSubmissionMultipleWorksiteView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationSubmissionMultipleWorksiteView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationSubmissionMultipleWorksiteView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationSubmissionMultipleWorksiteView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationSubmissionMultipleWorksiteView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationSubmissionMultipleWorksiteView <- function(UnemploymentCompensationSubmissionMultipleWorksiteViewID, BuildingID = F, PaidFor12thDayOfMonth1EmployeeCount = F, PaidFor12thDayOfMonth2EmployeeCount = F, PaidFor12thDayOfMonth3EmployeeCount = F, TotalQuarterlyWages = F, UnemploymentCompensationSubmissionID = F, Worksite = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationSubmissionMultipleWorksiteViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionMultipleWorksiteView", objectId = UnemploymentCompensationSubmissionMultipleWorksiteViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationSubmissionMultipleWorksiteView
	#'
	#' This function deletes an UnemploymentCompensationSubmissionMultipleWorksiteView
	#' @param UnemploymentCompensationSubmissionMultipleWorksiteViewID The ID of the UnemploymentCompensationSubmissionMultipleWorksiteView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationSubmissionMultipleWorksiteViewID of the deleted UnemploymentCompensationSubmissionMultipleWorksiteView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationSubmissionMultipleWorksiteView <- function(UnemploymentCompensationSubmissionMultipleWorksiteViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionMultipleWorksiteView", objectId = UnemploymentCompensationSubmissionMultipleWorksiteViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationSubmissionMultipleWorksiteView
	#'
	#' This function creates an UnemploymentCompensationSubmissionMultipleWorksiteView
	#' @param fieldNames The field values to give the created UnemploymentCompensationSubmissionMultipleWorksiteView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationSubmissionMultipleWorksiteView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationSubmissionMultipleWorksiteView <- function(BuildingID = NULL, UnemploymentCompensationSubmissionID = NULL, Worksite = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionMultipleWorksiteView", body = list(DataObject = body), searchFields = append("UnemploymentCompensationSubmissionMultipleWorksiteViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationSubmissionMultipleWorksiteView
	#'
	#' This function modifies an UnemploymentCompensationSubmissionMultipleWorksiteView
	#' @param fieldNames The field values to give the modified UnemploymentCompensationSubmissionMultipleWorksiteView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationSubmissionMultipleWorksiteView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationSubmissionMultipleWorksiteView <- function(UnemploymentCompensationSubmissionMultipleWorksiteViewID, BuildingID = NULL, UnemploymentCompensationSubmissionID = NULL, Worksite = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionMultipleWorksiteView", objectId = UnemploymentCompensationSubmissionMultipleWorksiteViewID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationSubmissionMultipleWorksiteViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationEmployeeExceptions
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationEmployeeExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployeeExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployeeExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployeeException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationEmployeeExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationEmployeeExceptions <- function(searchConditionsList = NULL, CreatedTime = F, FirstName = F, LastName = F, Message = F, MessageType = F, MiddleName = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationEmployeeExceptionID = F, UnemploymentCompensationEmployeeV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationEmployeeException
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationEmployeeException
	#' @param UnemploymentCompensationEmployeeExceptionID The ID of the UnemploymentCompensationEmployeeException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployeeException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployeeException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployeeException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationEmployeeException <- function(UnemploymentCompensationEmployeeExceptionID, CreatedTime = F, FirstName = F, LastName = F, Message = F, MessageType = F, MiddleName = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationEmployeeV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationEmployeeExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeException", objectId = UnemploymentCompensationEmployeeExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationEmployeeException
	#'
	#' This function deletes an UnemploymentCompensationEmployeeException
	#' @param UnemploymentCompensationEmployeeExceptionID The ID of the UnemploymentCompensationEmployeeException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationEmployeeExceptionID of the deleted UnemploymentCompensationEmployeeException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationEmployeeException <- function(UnemploymentCompensationEmployeeExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeException", objectId = UnemploymentCompensationEmployeeExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationEmployeeException
	#'
	#' This function creates an UnemploymentCompensationEmployeeException
	#' @param fieldNames The field values to give the created UnemploymentCompensationEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationEmployeeException <- function(FirstName = NULL, LastName = NULL, Message = NULL, MessageType = NULL, MiddleName = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationEmployeeV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeException", body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationEmployeeException
	#'
	#' This function modifies an UnemploymentCompensationEmployeeException
	#' @param fieldNames The field values to give the modified UnemploymentCompensationEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationEmployeeException <- function(UnemploymentCompensationEmployeeExceptionID, FirstName = NULL, LastName = NULL, Message = NULL, MessageType = NULL, MiddleName = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationEmployeeV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeException", objectId = UnemploymentCompensationEmployeeExceptionID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationEmployerExceptions
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationEmployerExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployerExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployerExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployerException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationEmployerExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationEmployerExceptions <- function(searchConditionsList = NULL, CreatedTime = F, EmployerName = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationEmployerExceptionID = F, UnemploymentCompensationEmployerV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationEmployerException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationEmployerException
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationEmployerException
	#' @param UnemploymentCompensationEmployerExceptionID The ID of the UnemploymentCompensationEmployerException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployerException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployerException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployerException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationEmployerException <- function(UnemploymentCompensationEmployerExceptionID, CreatedTime = F, EmployerName = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationEmployerV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationEmployerExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerException", objectId = UnemploymentCompensationEmployerExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationEmployerException
	#'
	#' This function deletes an UnemploymentCompensationEmployerException
	#' @param UnemploymentCompensationEmployerExceptionID The ID of the UnemploymentCompensationEmployerException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationEmployerExceptionID of the deleted UnemploymentCompensationEmployerException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationEmployerException <- function(UnemploymentCompensationEmployerExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerException", objectId = UnemploymentCompensationEmployerExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationEmployerException
	#'
	#' This function creates an UnemploymentCompensationEmployerException
	#' @param fieldNames The field values to give the created UnemploymentCompensationEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationEmployerException <- function(EmployerName = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationEmployerV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerException", body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationEmployerException
	#'
	#' This function modifies an UnemploymentCompensationEmployerException
	#' @param fieldNames The field values to give the modified UnemploymentCompensationEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationEmployerException <- function(UnemploymentCompensationEmployerExceptionID, EmployerName = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationEmployerV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerException", objectId = UnemploymentCompensationEmployerExceptionID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationOtherStateEmployeeExceptions
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationOtherStateEmployeeExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationOtherStateEmployeeExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationOtherStateEmployeeExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationOtherStateEmployeeException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationOtherStateEmployeeExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationOtherStateEmployeeExceptions <- function(searchConditionsList = NULL, CreatedTime = F, FirstName = F, LastName = F, Message = F, MessageType = F, MiddleName = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationOtherStateEmployeeExceptionID = F, UnemploymentCompensationOtherStateEmployeeV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationOtherStateEmployeeException
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationOtherStateEmployeeException
	#' @param UnemploymentCompensationOtherStateEmployeeExceptionID The ID of the UnemploymentCompensationOtherStateEmployeeException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationOtherStateEmployeeException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationOtherStateEmployeeException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationOtherStateEmployeeException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationOtherStateEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationOtherStateEmployeeException <- function(UnemploymentCompensationOtherStateEmployeeExceptionID, CreatedTime = F, FirstName = F, LastName = F, Message = F, MessageType = F, MiddleName = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationOtherStateEmployeeV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationOtherStateEmployeeExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeException", objectId = UnemploymentCompensationOtherStateEmployeeExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationOtherStateEmployeeException
	#'
	#' This function deletes an UnemploymentCompensationOtherStateEmployeeException
	#' @param UnemploymentCompensationOtherStateEmployeeExceptionID The ID of the UnemploymentCompensationOtherStateEmployeeException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationOtherStateEmployeeExceptionID of the deleted UnemploymentCompensationOtherStateEmployeeException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationOtherStateEmployeeException <- function(UnemploymentCompensationOtherStateEmployeeExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeException", objectId = UnemploymentCompensationOtherStateEmployeeExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationOtherStateEmployeeException
	#'
	#' This function creates an UnemploymentCompensationOtherStateEmployeeException
	#' @param fieldNames The field values to give the created UnemploymentCompensationOtherStateEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationOtherStateEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationOtherStateEmployeeException <- function(FirstName = NULL, LastName = NULL, Message = NULL, MessageType = NULL, MiddleName = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationOtherStateEmployeeV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeException", body = list(DataObject = body), searchFields = append("UnemploymentCompensationOtherStateEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationOtherStateEmployeeException
	#'
	#' This function modifies an UnemploymentCompensationOtherStateEmployeeException
	#' @param fieldNames The field values to give the modified UnemploymentCompensationOtherStateEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationOtherStateEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationOtherStateEmployeeException <- function(UnemploymentCompensationOtherStateEmployeeExceptionID, FirstName = NULL, LastName = NULL, Message = NULL, MessageType = NULL, MiddleName = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationOtherStateEmployeeV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeException", objectId = UnemploymentCompensationOtherStateEmployeeExceptionID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationOtherStateEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationPayrollRunExceptions
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationPayrollRunExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationPayrollRunExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationPayrollRunExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationPayrollRunException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationPayrollRunExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationPayrollRunExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationPayrollRunExceptionID = F, UnemploymentCompensationPayrollRunV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationPayrollRunException
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationPayrollRunException
	#' @param UnemploymentCompensationPayrollRunExceptionID The ID of the UnemploymentCompensationPayrollRunException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationPayrollRunException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationPayrollRunException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationPayrollRunException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationPayrollRunException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationPayrollRunException <- function(UnemploymentCompensationPayrollRunExceptionID, CreatedTime = F, Description = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UnemploymentCompensationPayrollRunV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationPayrollRunExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunException", objectId = UnemploymentCompensationPayrollRunExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationPayrollRunException
	#'
	#' This function deletes an UnemploymentCompensationPayrollRunException
	#' @param UnemploymentCompensationPayrollRunExceptionID The ID of the UnemploymentCompensationPayrollRunException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationPayrollRunExceptionID of the deleted UnemploymentCompensationPayrollRunException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationPayrollRunException <- function(UnemploymentCompensationPayrollRunExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunException", objectId = UnemploymentCompensationPayrollRunExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationPayrollRunException
	#'
	#' This function creates an UnemploymentCompensationPayrollRunException
	#' @param fieldNames The field values to give the created UnemploymentCompensationPayrollRunException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationPayrollRunException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationPayrollRunException <- function(Description = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationPayrollRunV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunException", body = list(DataObject = body), searchFields = append("UnemploymentCompensationPayrollRunExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationPayrollRunException
	#'
	#' This function modifies an UnemploymentCompensationPayrollRunException
	#' @param fieldNames The field values to give the modified UnemploymentCompensationPayrollRunException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationPayrollRunException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationPayrollRunException <- function(UnemploymentCompensationPayrollRunExceptionID, Description = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, UnemploymentCompensationPayrollRunV1ID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunException", objectId = UnemploymentCompensationPayrollRunExceptionID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationPayrollRunExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationEmployeeV1s
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationEmployeeV1s <- function(searchConditionsList = NULL, AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = F, AccumulatedGrossWagesForCurrentYear = F, AccumulatedGrossWagesForPriorQuarters = F, AccumulatedTaxableWagesForPriorQuarters = F, BuildingID = F, CreatedTime = F, EmployeeID = F, FirstName = F, GrossWagesMonth1 = F, GrossWagesMonth2 = F, GrossWagesMonth3 = F, HasErrors = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, PaidFor12thDayOfMonth1 = F, PaidFor12thDayOfMonth2 = F, PaidFor12thDayOfMonth3 = F, QuarterlyDeductionsReducingTaxableWages = F, QuarterlyGrossWagesEarned = F, QuarterlyTaxableWagesEarned = F, SocialSecurityNumber = F, UnemploymentCompensationEmployeeV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Worksite = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationEmployeeV1
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationEmployeeV1
	#' @param UnemploymentCompensationEmployeeV1ID The ID of the UnemploymentCompensationEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationEmployeeV1 <- function(UnemploymentCompensationEmployeeV1ID, AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = F, AccumulatedGrossWagesForCurrentYear = F, AccumulatedGrossWagesForPriorQuarters = F, AccumulatedTaxableWagesForPriorQuarters = F, BuildingID = F, CreatedTime = F, EmployeeID = F, FirstName = F, GrossWagesMonth1 = F, GrossWagesMonth2 = F, GrossWagesMonth3 = F, HasErrors = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, PaidFor12thDayOfMonth1 = F, PaidFor12thDayOfMonth2 = F, PaidFor12thDayOfMonth3 = F, QuarterlyDeductionsReducingTaxableWages = F, QuarterlyGrossWagesEarned = F, QuarterlyTaxableWagesEarned = F, SocialSecurityNumber = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Worksite = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeV1", objectId = UnemploymentCompensationEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationEmployeeV1
	#'
	#' This function deletes an UnemploymentCompensationEmployeeV1
	#' @param UnemploymentCompensationEmployeeV1ID The ID of the UnemploymentCompensationEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationEmployeeV1ID of the deleted UnemploymentCompensationEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationEmployeeV1 <- function(UnemploymentCompensationEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeV1", objectId = UnemploymentCompensationEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationEmployeeV1
	#'
	#' This function creates an UnemploymentCompensationEmployeeV1
	#' @param fieldNames The field values to give the created UnemploymentCompensationEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationEmployeeV1 <- function(AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = NULL, AccumulatedGrossWagesForPriorQuarters = NULL, BuildingID = NULL, EmployeeID = NULL, FirstName = NULL, GrossWagesMonth1 = NULL, GrossWagesMonth2 = NULL, GrossWagesMonth3 = NULL, LastName = NULL, MiddleName = NULL, PaidFor12thDayOfMonth1 = NULL, PaidFor12thDayOfMonth2 = NULL, PaidFor12thDayOfMonth3 = NULL, QuarterlyDeductionsReducingTaxableWages = NULL, SocialSecurityNumber = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, Worksite = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeV1", body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationEmployeeV1
	#'
	#' This function modifies an UnemploymentCompensationEmployeeV1
	#' @param fieldNames The field values to give the modified UnemploymentCompensationEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationEmployeeV1 <- function(UnemploymentCompensationEmployeeV1ID, AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = NULL, AccumulatedGrossWagesForPriorQuarters = NULL, BuildingID = NULL, EmployeeID = NULL, FirstName = NULL, GrossWagesMonth1 = NULL, GrossWagesMonth2 = NULL, GrossWagesMonth3 = NULL, LastName = NULL, MiddleName = NULL, PaidFor12thDayOfMonth1 = NULL, PaidFor12thDayOfMonth2 = NULL, PaidFor12thDayOfMonth3 = NULL, QuarterlyDeductionsReducingTaxableWages = NULL, SocialSecurityNumber = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, Worksite = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployeeV1", objectId = UnemploymentCompensationEmployeeV1ID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationEmployerV1s
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationEmployerV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployerV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployerV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployerV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationEmployerV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationEmployerV1s <- function(searchConditionsList = NULL, CreatedTime = F, EmployerID = F, EmployerName = F, FederalEIN = F, HasErrors = F, InterestDue = F, KeyHash = F, ModifiedTime = F, NumberOfEmployees1stMonth = F, NumberOfEmployees2ndMonth = F, NumberOfEmployees3rdMonth = F, PenaltyDue = F, PreviousOverpayment = F, PreviousUnderpayment = F, TaxableWageBase = F, TaxRate = F, TotalEmployerContributions = F, TotalGrossWages = F, TotalNumberOfEmployees = F, TotalPaymentDue = F, TotalTaxableWages = F, UnemploymentCompensationEmployerV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationEmployerV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationEmployerV1
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationEmployerV1
	#' @param UnemploymentCompensationEmployerV1ID The ID of the UnemploymentCompensationEmployerV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationEmployerV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationEmployerV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationEmployerV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationEmployerV1 <- function(UnemploymentCompensationEmployerV1ID, CreatedTime = F, EmployerID = F, EmployerName = F, FederalEIN = F, HasErrors = F, InterestDue = F, KeyHash = F, ModifiedTime = F, NumberOfEmployees1stMonth = F, NumberOfEmployees2ndMonth = F, NumberOfEmployees3rdMonth = F, PenaltyDue = F, PreviousOverpayment = F, PreviousUnderpayment = F, TaxableWageBase = F, TaxRate = F, TotalEmployerContributions = F, TotalGrossWages = F, TotalNumberOfEmployees = F, TotalPaymentDue = F, TotalTaxableWages = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationEmployerV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerV1", objectId = UnemploymentCompensationEmployerV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationEmployerV1
	#'
	#' This function deletes an UnemploymentCompensationEmployerV1
	#' @param UnemploymentCompensationEmployerV1ID The ID of the UnemploymentCompensationEmployerV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationEmployerV1ID of the deleted UnemploymentCompensationEmployerV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationEmployerV1 <- function(UnemploymentCompensationEmployerV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerV1", objectId = UnemploymentCompensationEmployerV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationEmployerV1
	#'
	#' This function creates an UnemploymentCompensationEmployerV1
	#' @param fieldNames The field values to give the created UnemploymentCompensationEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationEmployerV1 <- function(EmployerID = NULL, EmployerName = NULL, FederalEIN = NULL, InterestDue = NULL, PenaltyDue = NULL, PreviousOverpayment = NULL, PreviousUnderpayment = NULL, TaxableWageBase = NULL, TaxRate = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerV1", body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationEmployerV1
	#'
	#' This function modifies an UnemploymentCompensationEmployerV1
	#' @param fieldNames The field values to give the modified UnemploymentCompensationEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationEmployerV1 <- function(UnemploymentCompensationEmployerV1ID, EmployerID = NULL, EmployerName = NULL, FederalEIN = NULL, InterestDue = NULL, PenaltyDue = NULL, PreviousOverpayment = NULL, PreviousUnderpayment = NULL, TaxableWageBase = NULL, TaxRate = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationEmployerV1", objectId = UnemploymentCompensationEmployerV1ID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationOtherStateEmployeeV1s
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationOtherStateEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationOtherStateEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationOtherStateEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationOtherStateEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationOtherStateEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationOtherStateEmployeeV1s <- function(searchConditionsList = NULL, AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = F, AccumulatedGrossWagesForCurrentYear = F, AccumulatedGrossWagesForPriorQuarters = F, AccumulatedTaxableWagesForPriorQuarters = F, BuildingID = F, CreatedTime = F, EmployeeID = F, FirstName = F, GrossWagesMonth1 = F, GrossWagesMonth2 = F, GrossWagesMonth3 = F, HasErrors = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, PaidFor12thDayOfMonth1 = F, PaidFor12thDayOfMonth2 = F, PaidFor12thDayOfMonth3 = F, QuarterlyDeductionsReducingTaxableWages = F, QuarterlyGrossWagesEarned = F, QuarterlyTaxableWagesEarned = F, SocialSecurityNumber = F, TaxStateID = F, UnemploymentCompensationOtherStateEmployeeV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Worksite = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationOtherStateEmployeeV1
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationOtherStateEmployeeV1
	#' @param UnemploymentCompensationOtherStateEmployeeV1ID The ID of the UnemploymentCompensationOtherStateEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationOtherStateEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationOtherStateEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationOtherStateEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationOtherStateEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationOtherStateEmployeeV1 <- function(UnemploymentCompensationOtherStateEmployeeV1ID, AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = F, AccumulatedGrossWagesForCurrentYear = F, AccumulatedGrossWagesForPriorQuarters = F, AccumulatedTaxableWagesForPriorQuarters = F, BuildingID = F, CreatedTime = F, EmployeeID = F, FirstName = F, GrossWagesMonth1 = F, GrossWagesMonth2 = F, GrossWagesMonth3 = F, HasErrors = F, KeyHash = F, LastName = F, MiddleName = F, ModifiedTime = F, PaidFor12thDayOfMonth1 = F, PaidFor12thDayOfMonth2 = F, PaidFor12thDayOfMonth3 = F, QuarterlyDeductionsReducingTaxableWages = F, QuarterlyGrossWagesEarned = F, QuarterlyTaxableWagesEarned = F, SocialSecurityNumber = F, TaxStateID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Worksite = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationOtherStateEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeV1", objectId = UnemploymentCompensationOtherStateEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationOtherStateEmployeeV1
	#'
	#' This function deletes an UnemploymentCompensationOtherStateEmployeeV1
	#' @param UnemploymentCompensationOtherStateEmployeeV1ID The ID of the UnemploymentCompensationOtherStateEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationOtherStateEmployeeV1ID of the deleted UnemploymentCompensationOtherStateEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationOtherStateEmployeeV1 <- function(UnemploymentCompensationOtherStateEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeV1", objectId = UnemploymentCompensationOtherStateEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationOtherStateEmployeeV1
	#'
	#' This function creates an UnemploymentCompensationOtherStateEmployeeV1
	#' @param fieldNames The field values to give the created UnemploymentCompensationOtherStateEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationOtherStateEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationOtherStateEmployeeV1 <- function(AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = NULL, AccumulatedGrossWagesForPriorQuarters = NULL, BuildingID = NULL, EmployeeID = NULL, FirstName = NULL, GrossWagesMonth1 = NULL, GrossWagesMonth2 = NULL, GrossWagesMonth3 = NULL, LastName = NULL, MiddleName = NULL, PaidFor12thDayOfMonth1 = NULL, PaidFor12thDayOfMonth2 = NULL, PaidFor12thDayOfMonth3 = NULL, QuarterlyDeductionsReducingTaxableWages = NULL, SocialSecurityNumber = NULL, TaxStateID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, Worksite = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeV1", body = list(DataObject = body), searchFields = append("UnemploymentCompensationOtherStateEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationOtherStateEmployeeV1
	#'
	#' This function modifies an UnemploymentCompensationOtherStateEmployeeV1
	#' @param fieldNames The field values to give the modified UnemploymentCompensationOtherStateEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationOtherStateEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationOtherStateEmployeeV1 <- function(UnemploymentCompensationOtherStateEmployeeV1ID, AccumulatedDeductionsReducingTaxableWagesForPriorQuarters = NULL, AccumulatedGrossWagesForPriorQuarters = NULL, BuildingID = NULL, EmployeeID = NULL, FirstName = NULL, GrossWagesMonth1 = NULL, GrossWagesMonth2 = NULL, GrossWagesMonth3 = NULL, LastName = NULL, MiddleName = NULL, PaidFor12thDayOfMonth1 = NULL, PaidFor12thDayOfMonth2 = NULL, PaidFor12thDayOfMonth3 = NULL, QuarterlyDeductionsReducingTaxableWages = NULL, SocialSecurityNumber = NULL, TaxStateID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, Worksite = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationOtherStateEmployeeV1", objectId = UnemploymentCompensationOtherStateEmployeeV1ID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationOtherStateEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationPayrollRunV1s
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationPayrollRunV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationPayrollRunV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationPayrollRunV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationPayrollRunV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationPayrollRunV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationPayrollRunV1s <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, HasErrors = F, KeyHash = F, ModifiedTime = F, PayrollRunID = F, UnemploymentCompensationPayrollRunV1ID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationPayrollRunV1
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationPayrollRunV1
	#' @param UnemploymentCompensationPayrollRunV1ID The ID of the UnemploymentCompensationPayrollRunV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationPayrollRunV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationPayrollRunV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationPayrollRunV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationPayrollRunV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationPayrollRunV1 <- function(UnemploymentCompensationPayrollRunV1ID, CreatedTime = F, Description = F, HasErrors = F, KeyHash = F, ModifiedTime = F, PayrollRunID = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationPayrollRunV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunV1", objectId = UnemploymentCompensationPayrollRunV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationPayrollRunV1
	#'
	#' This function deletes an UnemploymentCompensationPayrollRunV1
	#' @param UnemploymentCompensationPayrollRunV1ID The ID of the UnemploymentCompensationPayrollRunV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationPayrollRunV1ID of the deleted UnemploymentCompensationPayrollRunV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationPayrollRunV1 <- function(UnemploymentCompensationPayrollRunV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunV1", objectId = UnemploymentCompensationPayrollRunV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationPayrollRunV1
	#'
	#' This function creates an UnemploymentCompensationPayrollRunV1
	#' @param fieldNames The field values to give the created UnemploymentCompensationPayrollRunV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationPayrollRunV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationPayrollRunV1 <- function(Description = NULL, PayrollRunID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunV1", body = list(DataObject = body), searchFields = append("UnemploymentCompensationPayrollRunV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationPayrollRunV1
	#'
	#' This function modifies an UnemploymentCompensationPayrollRunV1
	#' @param fieldNames The field values to give the modified UnemploymentCompensationPayrollRunV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationPayrollRunV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationPayrollRunV1 <- function(UnemploymentCompensationPayrollRunV1ID, Description = NULL, PayrollRunID = NULL, UnemploymentCompensationSubmissionID = NULL, UnemploymentCompensationSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationPayrollRunV1", objectId = UnemploymentCompensationPayrollRunV1ID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationPayrollRunV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationSubmissions
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationSubmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationSubmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationSubmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationSubmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationSubmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationSubmissions <- function(searchConditionsList = NULL, CalendarYearID = F, CreatedTime = F, DistrictID = F, HasAnExport = F, HasAnExtract = F, ModifiedTime = F, Quarter = F, UnemploymentCompensationSubmissionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationSubmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationSubmission
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationSubmission
	#' @param UnemploymentCompensationSubmissionID The ID of the UnemploymentCompensationSubmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationSubmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationSubmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationSubmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationSubmission <- function(UnemploymentCompensationSubmissionID, CalendarYearID = F, CreatedTime = F, DistrictID = F, HasAnExport = F, HasAnExtract = F, ModifiedTime = F, Quarter = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationSubmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmission", objectId = UnemploymentCompensationSubmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationSubmission
	#'
	#' This function deletes an UnemploymentCompensationSubmission
	#' @param UnemploymentCompensationSubmissionID The ID of the UnemploymentCompensationSubmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationSubmissionID of the deleted UnemploymentCompensationSubmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationSubmission <- function(UnemploymentCompensationSubmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmission", objectId = UnemploymentCompensationSubmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationSubmission
	#'
	#' This function creates an UnemploymentCompensationSubmission
	#' @param fieldNames The field values to give the created UnemploymentCompensationSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationSubmission <- function(CalendarYearID = NULL, DistrictID = NULL, Quarter = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmission", body = list(DataObject = body), searchFields = append("UnemploymentCompensationSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationSubmission
	#'
	#' This function modifies an UnemploymentCompensationSubmission
	#' @param fieldNames The field values to give the modified UnemploymentCompensationSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationSubmission <- function(UnemploymentCompensationSubmissionID, CalendarYearID = NULL, DistrictID = NULL, Quarter = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmission", objectId = UnemploymentCompensationSubmissionID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnemploymentCompensationSubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of UnemploymentCompensationSubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationSubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationSubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationSubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of UnemploymentCompensationSubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnemploymentCompensationSubmissionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportType = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UnemploymentCompensationSubmissionID = F, UnemploymentCompensationSubmissionRunHistoryID = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnemploymentCompensationSubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of an UnemploymentCompensationSubmissionRunHistory
	#' @param UnemploymentCompensationSubmissionRunHistoryID The ID of the UnemploymentCompensationSubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnemploymentCompensationSubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnemploymentCompensationSubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnemploymentCompensationSubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of UnemploymentCompensationSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnemploymentCompensationSubmissionRunHistory <- function(UnemploymentCompensationSubmissionRunHistoryID, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportType = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UnemploymentCompensationSubmissionID = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnemploymentCompensationSubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionRunHistory", objectId = UnemploymentCompensationSubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnemploymentCompensationSubmissionRunHistory
	#'
	#' This function deletes an UnemploymentCompensationSubmissionRunHistory
	#' @param UnemploymentCompensationSubmissionRunHistoryID The ID of the UnemploymentCompensationSubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The UnemploymentCompensationSubmissionRunHistoryID of the deleted UnemploymentCompensationSubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnemploymentCompensationSubmissionRunHistory <- function(UnemploymentCompensationSubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionRunHistory", objectId = UnemploymentCompensationSubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnemploymentCompensationSubmissionRunHistory
	#'
	#' This function creates an UnemploymentCompensationSubmissionRunHistory
	#' @param fieldNames The field values to give the created UnemploymentCompensationSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created UnemploymentCompensationSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnemploymentCompensationSubmissionRunHistory <- function(EndDateTime = NULL, ExportType = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UnemploymentCompensationSubmissionID = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionRunHistory", body = list(DataObject = body), searchFields = append("UnemploymentCompensationSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnemploymentCompensationSubmissionRunHistory
	#'
	#' This function modifies an UnemploymentCompensationSubmissionRunHistory
	#' @param fieldNames The field values to give the modified UnemploymentCompensationSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified UnemploymentCompensationSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnemploymentCompensationSubmissionRunHistory <- function(UnemploymentCompensationSubmissionRunHistoryID, EndDateTime = NULL, ExportType = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UnemploymentCompensationSubmissionID = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "UnemploymentCompensationSubmissionRunHistory", objectId = UnemploymentCompensationSubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("UnemploymentCompensationSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStateIdentifierNames
	#'
	#' This function returns a dataframe or json object of TempStateIdentifierNames
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStateIdentifierNames. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStateIdentifierNames.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStateIdentifierName') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of TempStateIdentifierNames
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStateIdentifierNames <- function(searchConditionsList = NULL, BirthCity = F, BirthCountry = F, BirthDate = F, BirthState = F, CreatedTime = F, Enrollment1EndDate = F, Enrollment1EntryGradeLevelDescriptor = F, Enrollment1LocalEducationAgencyID = F, Enrollment1LocalEducationAgencyNameOfInstitution = F, Enrollment1SchoolID = F, Enrollment1SchoolNameOfInstitution = F, Enrollment1SchoolYear = F, Enrollment1StartDate = F, Enrollment2EndDate = F, Enrollment2EntryGradeLevelDescriptor = F, Enrollment2LocalEducationAgencyID = F, Enrollment2LocalEducationAgencyNameOfInstitution = F, Enrollment2SchoolID = F, Enrollment2SchoolNameOfInstitution = F, Enrollment2SchoolYear = F, Enrollment2StartDate = F, Gender = F, LegalFirstName = F, LegalLastName = F, LegalMiddleName = F, LegalSuffix = F, LocalID = F, ModifiedTime = F, MultipleBirthIndicator = F, OtherFirstName = F, OtherLastName = F, OtherMiddleName = F, Parent1FirstName = F, Parent1LastName = F, Parent1MiddleName = F, Parent2FirstName = F, Parent2LastName = F, Parent2MiddleName = F, RaceKey = F, Score = F, StateIdentifierNameID = F, TempStateIdentifierNameID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WISEIDNumber = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TempStateIdentifierName", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStateIdentifierName
	#'
	#' This function returns a dataframe or json object of a TempStateIdentifierName
	#' @param TempStateIdentifierNameID The ID of the TempStateIdentifierName to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStateIdentifierName. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStateIdentifierName.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStateIdentifierName') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of TempStateIdentifierName
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStateIdentifierName <- function(TempStateIdentifierNameID, BirthCity = F, BirthCountry = F, BirthDate = F, BirthState = F, CreatedTime = F, Enrollment1EndDate = F, Enrollment1EntryGradeLevelDescriptor = F, Enrollment1LocalEducationAgencyID = F, Enrollment1LocalEducationAgencyNameOfInstitution = F, Enrollment1SchoolID = F, Enrollment1SchoolNameOfInstitution = F, Enrollment1SchoolYear = F, Enrollment1StartDate = F, Enrollment2EndDate = F, Enrollment2EntryGradeLevelDescriptor = F, Enrollment2LocalEducationAgencyID = F, Enrollment2LocalEducationAgencyNameOfInstitution = F, Enrollment2SchoolID = F, Enrollment2SchoolNameOfInstitution = F, Enrollment2SchoolYear = F, Enrollment2StartDate = F, Gender = F, LegalFirstName = F, LegalLastName = F, LegalMiddleName = F, LegalSuffix = F, LocalID = F, ModifiedTime = F, MultipleBirthIndicator = F, OtherFirstName = F, OtherLastName = F, OtherMiddleName = F, Parent1FirstName = F, Parent1LastName = F, Parent1MiddleName = F, Parent2FirstName = F, Parent2LastName = F, Parent2MiddleName = F, RaceKey = F, Score = F, StateIdentifierNameID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WISEIDNumber = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStateIdentifierNameID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TempStateIdentifierName", objectId = TempStateIdentifierNameID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStateIdentifierName
	#'
	#' This function deletes a TempStateIdentifierName
	#' @param TempStateIdentifierNameID The ID of the TempStateIdentifierName to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The TempStateIdentifierNameID of the deleted TempStateIdentifierName.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStateIdentifierName <- function(TempStateIdentifierNameID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TempStateIdentifierName", objectId = TempStateIdentifierNameID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStateIdentifierName
	#'
	#' This function creates a TempStateIdentifierName
	#' @param fieldNames The field values to give the created TempStateIdentifierName. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created TempStateIdentifierName
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStateIdentifierName <- function(BirthCity = NULL, BirthCountry = NULL, BirthDate = NULL, BirthState = NULL, Enrollment1EndDate = NULL, Enrollment1EntryGradeLevelDescriptor = NULL, Enrollment1LocalEducationAgencyID = NULL, Enrollment1LocalEducationAgencyNameOfInstitution = NULL, Enrollment1SchoolID = NULL, Enrollment1SchoolNameOfInstitution = NULL, Enrollment1SchoolYear = NULL, Enrollment1StartDate = NULL, Enrollment2EndDate = NULL, Enrollment2EntryGradeLevelDescriptor = NULL, Enrollment2LocalEducationAgencyID = NULL, Enrollment2LocalEducationAgencyNameOfInstitution = NULL, Enrollment2SchoolID = NULL, Enrollment2SchoolNameOfInstitution = NULL, Enrollment2SchoolYear = NULL, Enrollment2StartDate = NULL, Gender = NULL, LegalFirstName = NULL, LegalLastName = NULL, LegalMiddleName = NULL, LegalSuffix = NULL, LocalID = NULL, MultipleBirthIndicator = NULL, OtherFirstName = NULL, OtherLastName = NULL, OtherMiddleName = NULL, Parent1FirstName = NULL, Parent1LastName = NULL, Parent1MiddleName = NULL, Parent2FirstName = NULL, Parent2LastName = NULL, Parent2MiddleName = NULL, RaceKey = NULL, Score = NULL, StateIdentifierNameID = NULL, WISEIDNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TempStateIdentifierName", body = list(DataObject = body), searchFields = append("TempStateIdentifierNameID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStateIdentifierName
	#'
	#' This function modifies a TempStateIdentifierName
	#' @param fieldNames The field values to give the modified TempStateIdentifierName. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified TempStateIdentifierName
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStateIdentifierName <- function(TempStateIdentifierNameID, BirthCity = NULL, BirthCountry = NULL, BirthDate = NULL, BirthState = NULL, Enrollment1EndDate = NULL, Enrollment1EntryGradeLevelDescriptor = NULL, Enrollment1LocalEducationAgencyID = NULL, Enrollment1LocalEducationAgencyNameOfInstitution = NULL, Enrollment1SchoolID = NULL, Enrollment1SchoolNameOfInstitution = NULL, Enrollment1SchoolYear = NULL, Enrollment1StartDate = NULL, Enrollment2EndDate = NULL, Enrollment2EntryGradeLevelDescriptor = NULL, Enrollment2LocalEducationAgencyID = NULL, Enrollment2LocalEducationAgencyNameOfInstitution = NULL, Enrollment2SchoolID = NULL, Enrollment2SchoolNameOfInstitution = NULL, Enrollment2SchoolYear = NULL, Enrollment2StartDate = NULL, Gender = NULL, LegalFirstName = NULL, LegalLastName = NULL, LegalMiddleName = NULL, LegalSuffix = NULL, LocalID = NULL, MultipleBirthIndicator = NULL, OtherFirstName = NULL, OtherLastName = NULL, OtherMiddleName = NULL, Parent1FirstName = NULL, Parent1LastName = NULL, Parent1MiddleName = NULL, Parent2FirstName = NULL, Parent2LastName = NULL, Parent2MiddleName = NULL, RaceKey = NULL, Score = NULL, StateIdentifierNameID = NULL, WISEIDNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TempStateIdentifierName", objectId = TempStateIdentifierNameID, body = list(DataObject = body), searchFields = append("TempStateIdentifierNameID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStateIdentifierNameErrors
	#'
	#' This function returns a dataframe or json object of TempStateIdentifierNameErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStateIdentifierNameErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStateIdentifierNameErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStateIdentifierNameError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of TempStateIdentifierNameErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStateIdentifierNameErrors <- function(searchConditionsList = NULL, CreatedTime = F, FailureReason = F, LegalFirstName = F, LegalLastName = F, LegalMiddleName = F, ModifiedTime = F, StateIdentifierNameID = F, TempStateIdentifierNameErrorID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "TempStateIdentifierNameError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStateIdentifierNameError
	#'
	#' This function returns a dataframe or json object of a TempStateIdentifierNameError
	#' @param TempStateIdentifierNameErrorID The ID of the TempStateIdentifierNameError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStateIdentifierNameError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStateIdentifierNameError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStateIdentifierNameError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of TempStateIdentifierNameError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStateIdentifierNameError <- function(TempStateIdentifierNameErrorID, CreatedTime = F, FailureReason = F, LegalFirstName = F, LegalLastName = F, LegalMiddleName = F, ModifiedTime = F, StateIdentifierNameID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStateIdentifierNameErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "TempStateIdentifierNameError", objectId = TempStateIdentifierNameErrorID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStateIdentifierNameError
	#'
	#' This function deletes a TempStateIdentifierNameError
	#' @param TempStateIdentifierNameErrorID The ID of the TempStateIdentifierNameError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The TempStateIdentifierNameErrorID of the deleted TempStateIdentifierNameError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStateIdentifierNameError <- function(TempStateIdentifierNameErrorID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "TempStateIdentifierNameError", objectId = TempStateIdentifierNameErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStateIdentifierNameError
	#'
	#' This function creates a TempStateIdentifierNameError
	#' @param fieldNames The field values to give the created TempStateIdentifierNameError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created TempStateIdentifierNameError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStateIdentifierNameError <- function(FailureReason = NULL, LegalFirstName = NULL, LegalLastName = NULL, LegalMiddleName = NULL, StateIdentifierNameID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "TempStateIdentifierNameError", body = list(DataObject = body), searchFields = append("TempStateIdentifierNameErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStateIdentifierNameError
	#'
	#' This function modifies a TempStateIdentifierNameError
	#' @param fieldNames The field values to give the modified TempStateIdentifierNameError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified TempStateIdentifierNameError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStateIdentifierNameError <- function(TempStateIdentifierNameErrorID, FailureReason = NULL, LegalFirstName = NULL, LegalLastName = NULL, LegalMiddleName = NULL, StateIdentifierNameID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "TempStateIdentifierNameError", objectId = TempStateIdentifierNameErrorID, body = list(DataObject = body), searchFields = append("TempStateIdentifierNameErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiChartOfAccounts
	#'
	#' This function returns a dataframe or json object of EdFiChartOfAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiChartOfAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiChartOfAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiChartOfAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiChartOfAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiChartOfAccounts <- function(searchConditionsList = NULL, AccountType = F, CreatedTime = F, EdFiChartOfAccountID = F, EdFiID = F, EducationOrganizationID = F, FiscalYearID = F, FunctionCode = F, FundCode = F, IsInvalid = F, ModifiedTime = F, Name = F, ObjectCode = F, OperationalUnitCode = F, ProgramCode = F, ProjectCode = F, SourceCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiChartOfAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiChartOfAccount
	#'
	#' This function returns a dataframe or json object of an EdFiChartOfAccount
	#' @param EdFiChartOfAccountID The ID of the EdFiChartOfAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiChartOfAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiChartOfAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiChartOfAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiChartOfAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiChartOfAccount <- function(EdFiChartOfAccountID, AccountType = F, CreatedTime = F, EdFiID = F, EducationOrganizationID = F, FiscalYearID = F, FunctionCode = F, FundCode = F, IsInvalid = F, ModifiedTime = F, Name = F, ObjectCode = F, OperationalUnitCode = F, ProgramCode = F, ProjectCode = F, SourceCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiChartOfAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiChartOfAccount", objectId = EdFiChartOfAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiChartOfAccount
	#'
	#' This function deletes an EdFiChartOfAccount
	#' @param EdFiChartOfAccountID The ID of the EdFiChartOfAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiChartOfAccountID of the deleted EdFiChartOfAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiChartOfAccount <- function(EdFiChartOfAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiChartOfAccount", objectId = EdFiChartOfAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiChartOfAccount
	#'
	#' This function creates an EdFiChartOfAccount
	#' @param fieldNames The field values to give the created EdFiChartOfAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiChartOfAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiChartOfAccount <- function(AccountType = NULL, EdFiID = NULL, EducationOrganizationID = NULL, FiscalYearID = NULL, FunctionCode = NULL, FundCode = NULL, IsInvalid = NULL, Name = NULL, ObjectCode = NULL, OperationalUnitCode = NULL, ProgramCode = NULL, ProjectCode = NULL, SourceCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiChartOfAccount", body = list(DataObject = body), searchFields = append("EdFiChartOfAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiChartOfAccount
	#'
	#' This function modifies an EdFiChartOfAccount
	#' @param fieldNames The field values to give the modified EdFiChartOfAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiChartOfAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiChartOfAccount <- function(EdFiChartOfAccountID, AccountType = NULL, EdFiID = NULL, EducationOrganizationID = NULL, FiscalYearID = NULL, FunctionCode = NULL, FundCode = NULL, IsInvalid = NULL, Name = NULL, ObjectCode = NULL, OperationalUnitCode = NULL, ProgramCode = NULL, ProjectCode = NULL, SourceCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiChartOfAccount", objectId = EdFiChartOfAccountID, body = list(DataObject = body), searchFields = append("EdFiChartOfAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiLevelOfEducationTypes
	#'
	#' This function returns a dataframe or json object of EdFiLevelOfEducationTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLevelOfEducationTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLevelOfEducationTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLevelOfEducationType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiLevelOfEducationTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiLevelOfEducationTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiLevelOfEducationTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiLevelOfEducationType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiLevelOfEducationType
	#'
	#' This function returns a dataframe or json object of an EdFiLevelOfEducationType
	#' @param EdFiLevelOfEducationTypeID The ID of the EdFiLevelOfEducationType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLevelOfEducationType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLevelOfEducationType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLevelOfEducationType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiLevelOfEducationType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiLevelOfEducationType <- function(EdFiLevelOfEducationTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiLevelOfEducationTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiLevelOfEducationType", objectId = EdFiLevelOfEducationTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiLevelOfEducationType
	#'
	#' This function deletes an EdFiLevelOfEducationType
	#' @param EdFiLevelOfEducationTypeID The ID of the EdFiLevelOfEducationType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiLevelOfEducationTypeID of the deleted EdFiLevelOfEducationType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiLevelOfEducationType <- function(EdFiLevelOfEducationTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiLevelOfEducationType", objectId = EdFiLevelOfEducationTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiLevelOfEducationType
	#'
	#' This function creates an EdFiLevelOfEducationType
	#' @param fieldNames The field values to give the created EdFiLevelOfEducationType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiLevelOfEducationType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiLevelOfEducationType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiLevelOfEducationType", body = list(DataObject = body), searchFields = append("EdFiLevelOfEducationTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiLevelOfEducationType
	#'
	#' This function modifies an EdFiLevelOfEducationType
	#' @param fieldNames The field values to give the modified EdFiLevelOfEducationType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiLevelOfEducationType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiLevelOfEducationType <- function(EdFiLevelOfEducationTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiLevelOfEducationType", objectId = EdFiLevelOfEducationTypeID, body = list(DataObject = body), searchFields = append("EdFiLevelOfEducationTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiLocalAccountReportingTags
	#'
	#' This function returns a dataframe or json object of EdFiLocalAccountReportingTags
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLocalAccountReportingTags. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLocalAccountReportingTags.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLocalAccountReportingTag') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiLocalAccountReportingTags
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiLocalAccountReportingTags <- function(searchConditionsList = NULL, CreatedTime = F, Descriptor = F, EdFiLocalAccountID = F, EdFiLocalAccountReportingTagID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiLocalAccountReportingTag", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiLocalAccountReportingTag
	#'
	#' This function returns a dataframe or json object of an EdFiLocalAccountReportingTag
	#' @param EdFiLocalAccountReportingTagID The ID of the EdFiLocalAccountReportingTag to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLocalAccountReportingTag. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLocalAccountReportingTag.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLocalAccountReportingTag') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiLocalAccountReportingTag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiLocalAccountReportingTag <- function(EdFiLocalAccountReportingTagID, CreatedTime = F, Descriptor = F, EdFiLocalAccountID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Value = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiLocalAccountReportingTagID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiLocalAccountReportingTag", objectId = EdFiLocalAccountReportingTagID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiLocalAccountReportingTag
	#'
	#' This function deletes an EdFiLocalAccountReportingTag
	#' @param EdFiLocalAccountReportingTagID The ID of the EdFiLocalAccountReportingTag to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiLocalAccountReportingTagID of the deleted EdFiLocalAccountReportingTag.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiLocalAccountReportingTag <- function(EdFiLocalAccountReportingTagID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiLocalAccountReportingTag", objectId = EdFiLocalAccountReportingTagID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiLocalAccountReportingTag
	#'
	#' This function creates an EdFiLocalAccountReportingTag
	#' @param fieldNames The field values to give the created EdFiLocalAccountReportingTag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiLocalAccountReportingTag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiLocalAccountReportingTag <- function(Descriptor = NULL, EdFiLocalAccountID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiLocalAccountReportingTag", body = list(DataObject = body), searchFields = append("EdFiLocalAccountReportingTagID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiLocalAccountReportingTag
	#'
	#' This function modifies an EdFiLocalAccountReportingTag
	#' @param fieldNames The field values to give the modified EdFiLocalAccountReportingTag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiLocalAccountReportingTag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiLocalAccountReportingTag <- function(EdFiLocalAccountReportingTagID, Descriptor = NULL, EdFiLocalAccountID = NULL, Value = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiLocalAccountReportingTag", objectId = EdFiLocalAccountReportingTagID, body = list(DataObject = body), searchFields = append("EdFiLocalAccountReportingTagID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiLocalAccounts
	#'
	#' This function returns a dataframe or json object of EdFiLocalAccounts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLocalAccounts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLocalAccounts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLocalAccount') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiLocalAccounts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiLocalAccounts <- function(searchConditionsList = NULL, AccountFiscalYearID = F, CreatedTime = F, EdFiChartOfAccountID = F, EdFiID = F, EdFiLocalAccountID = F, EducationOrganizationID = F, LocalActualAmount = F, LocalBudgetAmount = F, ModifiedTime = F, Name = F, ReportingTagCount = F, ReportingTags = F, ReportingTagsHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiLocalAccount", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiLocalAccount
	#'
	#' This function returns a dataframe or json object of an EdFiLocalAccount
	#' @param EdFiLocalAccountID The ID of the EdFiLocalAccount to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiLocalAccount. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiLocalAccount.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiLocalAccount') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiLocalAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiLocalAccount <- function(EdFiLocalAccountID, AccountFiscalYearID = F, CreatedTime = F, EdFiChartOfAccountID = F, EdFiID = F, EducationOrganizationID = F, LocalActualAmount = F, LocalBudgetAmount = F, ModifiedTime = F, Name = F, ReportingTagCount = F, ReportingTags = F, ReportingTagsHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiLocalAccountID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiLocalAccount", objectId = EdFiLocalAccountID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiLocalAccount
	#'
	#' This function deletes an EdFiLocalAccount
	#' @param EdFiLocalAccountID The ID of the EdFiLocalAccount to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiLocalAccountID of the deleted EdFiLocalAccount.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiLocalAccount <- function(EdFiLocalAccountID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiLocalAccount", objectId = EdFiLocalAccountID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiLocalAccount
	#'
	#' This function creates an EdFiLocalAccount
	#' @param fieldNames The field values to give the created EdFiLocalAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiLocalAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiLocalAccount <- function(AccountFiscalYearID = NULL, EdFiChartOfAccountID = NULL, EdFiID = NULL, EducationOrganizationID = NULL, LocalActualAmount = NULL, LocalBudgetAmount = NULL, Name = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiLocalAccount", body = list(DataObject = body), searchFields = append("EdFiLocalAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiLocalAccount
	#'
	#' This function modifies an EdFiLocalAccount
	#' @param fieldNames The field values to give the modified EdFiLocalAccount. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiLocalAccount
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiLocalAccount <- function(EdFiLocalAccountID, AccountFiscalYearID = NULL, EdFiChartOfAccountID = NULL, EdFiID = NULL, EducationOrganizationID = NULL, LocalActualAmount = NULL, LocalBudgetAmount = NULL, Name = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiLocalAccount", objectId = EdFiLocalAccountID, body = list(DataObject = body), searchFields = append("EdFiLocalAccountID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateReportingExclusionTypes
	#'
	#' This function returns a dataframe or json object of StateReportingExclusionTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingExclusionTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingExclusionTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingExclusionType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateReportingExclusionTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateReportingExclusionTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateReportingExclusionTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateReportingExclusionType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateReportingExclusionType
	#'
	#' This function returns a dataframe or json object of a StateReportingExclusionType
	#' @param StateReportingExclusionTypeID The ID of the StateReportingExclusionType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingExclusionType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingExclusionType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingExclusionType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateReportingExclusionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateReportingExclusionType <- function(StateReportingExclusionTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateReportingExclusionTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateReportingExclusionType", objectId = StateReportingExclusionTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateReportingExclusionType
	#'
	#' This function deletes a StateReportingExclusionType
	#' @param StateReportingExclusionTypeID The ID of the StateReportingExclusionType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateReportingExclusionTypeID of the deleted StateReportingExclusionType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateReportingExclusionType <- function(StateReportingExclusionTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateReportingExclusionType", objectId = StateReportingExclusionTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateReportingExclusionType
	#'
	#' This function creates a StateReportingExclusionType
	#' @param fieldNames The field values to give the created StateReportingExclusionType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateReportingExclusionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateReportingExclusionType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateReportingExclusionType", body = list(DataObject = body), searchFields = append("StateReportingExclusionTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateReportingExclusionType
	#'
	#' This function modifies a StateReportingExclusionType
	#' @param fieldNames The field values to give the modified StateReportingExclusionType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateReportingExclusionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateReportingExclusionType <- function(StateReportingExclusionTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateReportingExclusionType", objectId = StateReportingExclusionTypeID, body = list(DataObject = body), searchFields = append("StateReportingExclusionTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateETranscriptSessionTypes
	#'
	#' This function returns a dataframe or json object of StateETranscriptSessionTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateETranscriptSessionTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateETranscriptSessionTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateETranscriptSessionType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateETranscriptSessionTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateETranscriptSessionTypes <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateETranscriptSessionTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateETranscriptSessionType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateETranscriptSessionType
	#'
	#' This function returns a dataframe or json object of a StateETranscriptSessionType
	#' @param StateETranscriptSessionTypeID The ID of the StateETranscriptSessionType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateETranscriptSessionType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateETranscriptSessionType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateETranscriptSessionType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateETranscriptSessionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateETranscriptSessionType <- function(StateETranscriptSessionTypeID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateETranscriptSessionTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateETranscriptSessionType", objectId = StateETranscriptSessionTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateETranscriptSessionType
	#'
	#' This function deletes a StateETranscriptSessionType
	#' @param StateETranscriptSessionTypeID The ID of the StateETranscriptSessionType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateETranscriptSessionTypeID of the deleted StateETranscriptSessionType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateETranscriptSessionType <- function(StateETranscriptSessionTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateETranscriptSessionType", objectId = StateETranscriptSessionTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateETranscriptSessionType
	#'
	#' This function creates a StateETranscriptSessionType
	#' @param fieldNames The field values to give the created StateETranscriptSessionType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateETranscriptSessionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateETranscriptSessionType <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateETranscriptSessionType", body = list(DataObject = body), searchFields = append("StateETranscriptSessionTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateETranscriptSessionType
	#'
	#' This function modifies a StateETranscriptSessionType
	#' @param fieldNames The field values to give the modified StateETranscriptSessionType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateETranscriptSessionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateETranscriptSessionType <- function(StateETranscriptSessionTypeID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateETranscriptSessionType", objectId = StateETranscriptSessionTypeID, body = list(DataObject = body), searchFields = append("StateETranscriptSessionTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiStudentInstructionTypes
	#'
	#' This function returns a dataframe or json object of EdFiStudentInstructionTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStudentInstructionTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStudentInstructionTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStudentInstructionType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiStudentInstructionTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiStudentInstructionTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiStudentInstructionTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiStudentInstructionType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiStudentInstructionType
	#'
	#' This function returns a dataframe or json object of an EdFiStudentInstructionType
	#' @param EdFiStudentInstructionTypeID The ID of the EdFiStudentInstructionType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiStudentInstructionType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiStudentInstructionType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiStudentInstructionType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiStudentInstructionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiStudentInstructionType <- function(EdFiStudentInstructionTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiStudentInstructionTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiStudentInstructionType", objectId = EdFiStudentInstructionTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiStudentInstructionType
	#'
	#' This function deletes an EdFiStudentInstructionType
	#' @param EdFiStudentInstructionTypeID The ID of the EdFiStudentInstructionType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiStudentInstructionTypeID of the deleted EdFiStudentInstructionType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiStudentInstructionType <- function(EdFiStudentInstructionTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiStudentInstructionType", objectId = EdFiStudentInstructionTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiStudentInstructionType
	#'
	#' This function creates an EdFiStudentInstructionType
	#' @param fieldNames The field values to give the created EdFiStudentInstructionType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiStudentInstructionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiStudentInstructionType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiStudentInstructionType", body = list(DataObject = body), searchFields = append("EdFiStudentInstructionTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiStudentInstructionType
	#'
	#' This function modifies an EdFiStudentInstructionType
	#' @param fieldNames The field values to give the modified EdFiStudentInstructionType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiStudentInstructionType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiStudentInstructionType <- function(EdFiStudentInstructionTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiStudentInstructionType", objectId = EdFiStudentInstructionTypeID, body = list(DataObject = body), searchFields = append("EdFiStudentInstructionTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateDeviceAccesses
	#'
	#' This function returns a dataframe or json object of StateDeviceAccesses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateDeviceAccesses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateDeviceAccesses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateDeviceAccess') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateDeviceAccesses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateDeviceAccesses <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateDeviceAccessID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateDeviceAccess", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateDeviceAccess
	#'
	#' This function returns a dataframe or json object of a StateDeviceAccess
	#' @param StateDeviceAccessID The ID of the StateDeviceAccess to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateDeviceAccess. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateDeviceAccess.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateDeviceAccess') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateDeviceAccess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateDeviceAccess <- function(StateDeviceAccessID, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateDeviceAccessID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateDeviceAccess", objectId = StateDeviceAccessID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateDeviceAccess
	#'
	#' This function deletes a StateDeviceAccess
	#' @param StateDeviceAccessID The ID of the StateDeviceAccess to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateDeviceAccessID of the deleted StateDeviceAccess.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateDeviceAccess <- function(StateDeviceAccessID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateDeviceAccess", objectId = StateDeviceAccessID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateDeviceAccess
	#'
	#' This function creates a StateDeviceAccess
	#' @param fieldNames The field values to give the created StateDeviceAccess. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateDeviceAccess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateDeviceAccess <- function(Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateDeviceAccess", body = list(DataObject = body), searchFields = append("StateDeviceAccessID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateDeviceAccess
	#'
	#' This function modifies a StateDeviceAccess
	#' @param fieldNames The field values to give the modified StateDeviceAccess. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateDeviceAccess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateDeviceAccess <- function(StateDeviceAccessID, Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateDeviceAccess", objectId = StateDeviceAccessID, body = list(DataObject = body), searchFields = append("StateDeviceAccessID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateDeviceAccessSources
	#'
	#' This function returns a dataframe or json object of StateDeviceAccessSources
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateDeviceAccessSources. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateDeviceAccessSources.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateDeviceAccessSource') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateDeviceAccessSources
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateDeviceAccessSources <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateDeviceAccessSourceID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateDeviceAccessSource", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateDeviceAccessSource
	#'
	#' This function returns a dataframe or json object of a StateDeviceAccessSource
	#' @param StateDeviceAccessSourceID The ID of the StateDeviceAccessSource to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateDeviceAccessSource. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateDeviceAccessSource.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateDeviceAccessSource') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateDeviceAccessSource
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateDeviceAccessSource <- function(StateDeviceAccessSourceID, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateDeviceAccessSourceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateDeviceAccessSource", objectId = StateDeviceAccessSourceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateDeviceAccessSource
	#'
	#' This function deletes a StateDeviceAccessSource
	#' @param StateDeviceAccessSourceID The ID of the StateDeviceAccessSource to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateDeviceAccessSourceID of the deleted StateDeviceAccessSource.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateDeviceAccessSource <- function(StateDeviceAccessSourceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateDeviceAccessSource", objectId = StateDeviceAccessSourceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateDeviceAccessSource
	#'
	#' This function creates a StateDeviceAccessSource
	#' @param fieldNames The field values to give the created StateDeviceAccessSource. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateDeviceAccessSource
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateDeviceAccessSource <- function(Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateDeviceAccessSource", body = list(DataObject = body), searchFields = append("StateDeviceAccessSourceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateDeviceAccessSource
	#'
	#' This function modifies a StateDeviceAccessSource
	#' @param fieldNames The field values to give the modified StateDeviceAccessSource. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateDeviceAccessSource
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateDeviceAccessSource <- function(StateDeviceAccessSourceID, Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateDeviceAccessSource", objectId = StateDeviceAccessSourceID, body = list(DataObject = body), searchFields = append("StateDeviceAccessSourceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateDevices
	#'
	#' This function returns a dataframe or json object of StateDevices
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateDevices. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateDevices.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateDevice') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateDevices
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateDevices <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateDeviceID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateDevice", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateDevice
	#'
	#' This function returns a dataframe or json object of a StateDevice
	#' @param StateDeviceID The ID of the StateDevice to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateDevice. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateDevice.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateDevice') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateDevice
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateDevice <- function(StateDeviceID, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateDeviceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateDevice", objectId = StateDeviceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateDevice
	#'
	#' This function deletes a StateDevice
	#' @param StateDeviceID The ID of the StateDevice to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateDeviceID of the deleted StateDevice.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateDevice <- function(StateDeviceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateDevice", objectId = StateDeviceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateDevice
	#'
	#' This function creates a StateDevice
	#' @param fieldNames The field values to give the created StateDevice. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateDevice
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateDevice <- function(Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateDevice", body = list(DataObject = body), searchFields = append("StateDeviceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateDevice
	#'
	#' This function modifies a StateDevice
	#' @param fieldNames The field values to give the modified StateDevice. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateDevice
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateDevice <- function(StateDeviceID, Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateDevice", objectId = StateDeviceID, body = list(DataObject = body), searchFields = append("StateDeviceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateInternetPerformances
	#'
	#' This function returns a dataframe or json object of StateInternetPerformances
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateInternetPerformances. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateInternetPerformances.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateInternetPerformance') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateInternetPerformances
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateInternetPerformances <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateInternetPerformanceID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateInternetPerformance", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateInternetPerformance
	#'
	#' This function returns a dataframe or json object of a StateInternetPerformance
	#' @param StateInternetPerformanceID The ID of the StateInternetPerformance to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateInternetPerformance. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateInternetPerformance.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateInternetPerformance') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateInternetPerformance
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateInternetPerformance <- function(StateInternetPerformanceID, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateInternetPerformanceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateInternetPerformance", objectId = StateInternetPerformanceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateInternetPerformance
	#'
	#' This function deletes a StateInternetPerformance
	#' @param StateInternetPerformanceID The ID of the StateInternetPerformance to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateInternetPerformanceID of the deleted StateInternetPerformance.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateInternetPerformance <- function(StateInternetPerformanceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateInternetPerformance", objectId = StateInternetPerformanceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateInternetPerformance
	#'
	#' This function creates a StateInternetPerformance
	#' @param fieldNames The field values to give the created StateInternetPerformance. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateInternetPerformance
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateInternetPerformance <- function(Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateInternetPerformance", body = list(DataObject = body), searchFields = append("StateInternetPerformanceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateInternetPerformance
	#'
	#' This function modifies a StateInternetPerformance
	#' @param fieldNames The field values to give the modified StateInternetPerformance. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateInternetPerformance
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateInternetPerformance <- function(StateInternetPerformanceID, Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateInternetPerformance", objectId = StateInternetPerformanceID, body = list(DataObject = body), searchFields = append("StateInternetPerformanceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateInternetAccesses
	#'
	#' This function returns a dataframe or json object of StateInternetAccesses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateInternetAccesses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateInternetAccesses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateInternetAccess') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateInternetAccesses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateInternetAccesses <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateInternetAccessID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateInternetAccess", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateInternetAccess
	#'
	#' This function returns a dataframe or json object of a StateInternetAccess
	#' @param StateInternetAccessID The ID of the StateInternetAccess to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateInternetAccess. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateInternetAccess.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateInternetAccess') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateInternetAccess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateInternetAccess <- function(StateInternetAccessID, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateInternetAccessID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateInternetAccess", objectId = StateInternetAccessID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateInternetAccess
	#'
	#' This function deletes a StateInternetAccess
	#' @param StateInternetAccessID The ID of the StateInternetAccess to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateInternetAccessID of the deleted StateInternetAccess.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateInternetAccess <- function(StateInternetAccessID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateInternetAccess", objectId = StateInternetAccessID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateInternetAccess
	#'
	#' This function creates a StateInternetAccess
	#' @param fieldNames The field values to give the created StateInternetAccess. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateInternetAccess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateInternetAccess <- function(Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateInternetAccess", body = list(DataObject = body), searchFields = append("StateInternetAccessID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateInternetAccess
	#'
	#' This function modifies a StateInternetAccess
	#' @param fieldNames The field values to give the modified StateInternetAccess. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateInternetAccess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateInternetAccess <- function(StateInternetAccessID, Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateInternetAccess", objectId = StateInternetAccessID, body = list(DataObject = body), searchFields = append("StateInternetAccessID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateInternetAccessTypes
	#'
	#' This function returns a dataframe or json object of StateInternetAccessTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateInternetAccessTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateInternetAccessTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateInternetAccessType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of StateInternetAccessTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateInternetAccessTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateInternetAccessTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "StateInternetAccessType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateInternetAccessType
	#'
	#' This function returns a dataframe or json object of a StateInternetAccessType
	#' @param StateInternetAccessTypeID The ID of the StateInternetAccessType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateInternetAccessType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateInternetAccessType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateInternetAccessType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of StateInternetAccessType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateInternetAccessType <- function(StateInternetAccessTypeID, Code = F, CreatedTime = F, DisplayCode = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateInternetAccessTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "StateInternetAccessType", objectId = StateInternetAccessTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateInternetAccessType
	#'
	#' This function deletes a StateInternetAccessType
	#' @param StateInternetAccessTypeID The ID of the StateInternetAccessType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The StateInternetAccessTypeID of the deleted StateInternetAccessType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateInternetAccessType <- function(StateInternetAccessTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "StateInternetAccessType", objectId = StateInternetAccessTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateInternetAccessType
	#'
	#' This function creates a StateInternetAccessType
	#' @param fieldNames The field values to give the created StateInternetAccessType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created StateInternetAccessType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateInternetAccessType <- function(Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "StateInternetAccessType", body = list(DataObject = body), searchFields = append("StateInternetAccessTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateInternetAccessType
	#'
	#' This function modifies a StateInternetAccessType
	#' @param fieldNames The field values to give the modified StateInternetAccessType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified StateInternetAccessType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateInternetAccessType <- function(StateInternetAccessTypeID, Code = NULL, DisplayCode = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "StateInternetAccessType", objectId = StateInternetAccessTypeID, body = list(DataObject = body), searchFields = append("StateInternetAccessTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EdFiPopulationServedTypes
	#'
	#' This function returns a dataframe or json object of EdFiPopulationServedTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiPopulationServedTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiPopulationServedTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiPopulationServedType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A list of EdFiPopulationServedTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEdFiPopulationServedTypes <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, EdFiPopulationServedTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReporting", objectName = "EdFiPopulationServedType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EdFiPopulationServedType
	#'
	#' This function returns a dataframe or json object of an EdFiPopulationServedType
	#' @param EdFiPopulationServedTypeID The ID of the EdFiPopulationServedType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EdFiPopulationServedType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EdFiPopulationServedType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EdFiPopulationServedType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A dataframe or of EdFiPopulationServedType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEdFiPopulationServedType <- function(EdFiPopulationServedTypeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EdFiPopulationServedTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReporting", objectName = "EdFiPopulationServedType", objectId = EdFiPopulationServedTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EdFiPopulationServedType
	#'
	#' This function deletes an EdFiPopulationServedType
	#' @param EdFiPopulationServedTypeID The ID of the EdFiPopulationServedType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The EdFiPopulationServedTypeID of the deleted EdFiPopulationServedType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEdFiPopulationServedType <- function(EdFiPopulationServedTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReporting", objectName = "EdFiPopulationServedType", objectId = EdFiPopulationServedTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EdFiPopulationServedType
	#'
	#' This function creates an EdFiPopulationServedType
	#' @param fieldNames The field values to give the created EdFiPopulationServedType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return A newly created EdFiPopulationServedType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEdFiPopulationServedType <- function(Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReporting", objectName = "EdFiPopulationServedType", body = list(DataObject = body), searchFields = append("EdFiPopulationServedTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EdFiPopulationServedType
	#'
	#' This function modifies an EdFiPopulationServedType
	#' @param fieldNames The field values to give the modified EdFiPopulationServedType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReporting
	#' @return The modified EdFiPopulationServedType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEdFiPopulationServedType <- function(EdFiPopulationServedTypeID, Code = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReporting", objectName = "EdFiPopulationServedType", objectId = EdFiPopulationServedTypeID, body = list(DataObject = body), searchFields = append("EdFiPopulationServedTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}
