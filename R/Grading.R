
	#' List TempTransferCourses
	#'
	#' This function returns a dataframe or json object of TempTransferCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempTransferCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempTransferCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempTransferCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempTransferCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempTransferCourses <- function(searchConditionsList = NULL, TempTransferCourseID = F, EntityID = F, SchoolYearID = F, CourseID = F, SectionID = F, SectionLengthSubsetID = F, GradeReferenceID = F, CourseDescription = F, StartDate = F, EndDate = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLinking = F, CourseSection = F, GradedCourse = F, EntityCode = F, SchoolYear = F, StudentID = F, SectionLengthID = F, GradingPeriodSetID = F, UseEarnedCreditOverride = F, UseEarnedCreditPercentOverride = F, UseAddOnGPA = F, CourseCredits = F, EarnedCreditOverride = F, TotalEarnedCredits = F, TotalFailedCredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradYear = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempTransferCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempTransferCourse
	#'
	#' This function returns a dataframe or json object of a TempTransferCourse
	#' @param TempTransferCourseID The ID of the TempTransferCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempTransferCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempTransferCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempTransferCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempTransferCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempTransferCourse <- function(TempTransferCourseID, EntityID = F, SchoolYearID = F, CourseID = F, SectionID = F, SectionLengthSubsetID = F, GradeReferenceID = F, CourseDescription = F, StartDate = F, EndDate = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLinking = F, CourseSection = F, GradedCourse = F, EntityCode = F, SchoolYear = F, StudentID = F, SectionLengthID = F, GradingPeriodSetID = F, UseEarnedCreditOverride = F, UseEarnedCreditPercentOverride = F, UseAddOnGPA = F, CourseCredits = F, EarnedCreditOverride = F, TotalEarnedCredits = F, TotalFailedCredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradYear = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempTransferCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempTransferCourse", objectId = TempTransferCourseID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempTransferCourse
	#'
	#' This function deletes a TempTransferCourse
	#' @param TempTransferCourseID The ID of the TempTransferCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempTransferCourseID of the deleted TempTransferCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempTransferCourse <- function(TempTransferCourseID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempTransferCourse", objectId = TempTransferCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempTransferCourse
	#'
	#' This function creates a TempTransferCourse
	#' @param fieldNames The field values to give the created TempTransferCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempTransferCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempTransferCourse <- function(EntityID = NULL, SchoolYearID = NULL, CourseID = NULL, SectionID = NULL, SectionLengthSubsetID = NULL, GradeReferenceID = NULL, CourseDescription = NULL, StartDate = NULL, EndDate = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLinking = NULL, CourseSection = NULL, GradedCourse = NULL, EntityCode = NULL, SchoolYear = NULL, StudentID = NULL, SectionLengthID = NULL, GradingPeriodSetID = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditPercentOverride = NULL, UseAddOnGPA = NULL, CourseCredits = NULL, EarnedCreditOverride = NULL, TotalEarnedCredits = NULL, TotalFailedCredits = NULL, GradYear = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempTransferCourse", body = list(DataObject = body), searchFields = append("TempTransferCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempTransferCourse
	#'
	#' This function modifies a TempTransferCourse
	#' @param fieldNames The field values to give the modified TempTransferCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempTransferCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempTransferCourse <- function(TempTransferCourseID, EntityID = NULL, SchoolYearID = NULL, CourseID = NULL, SectionID = NULL, SectionLengthSubsetID = NULL, GradeReferenceID = NULL, CourseDescription = NULL, StartDate = NULL, EndDate = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLinking = NULL, CourseSection = NULL, GradedCourse = NULL, EntityCode = NULL, SchoolYear = NULL, StudentID = NULL, SectionLengthID = NULL, GradingPeriodSetID = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditPercentOverride = NULL, UseAddOnGPA = NULL, CourseCredits = NULL, EarnedCreditOverride = NULL, TotalEarnedCredits = NULL, TotalFailedCredits = NULL, GradYear = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempTransferCourse", objectId = TempTransferCourseID, body = list(DataObject = body), searchFields = append("TempTransferCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List RankRunHistories
	#'
	#' This function returns a dataframe or json object of RankRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RankRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RankRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RankRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of RankRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listRankRunHistories <- function(searchConditionsList = NULL, RankRunHistoryID = F, RankMethodID = F, Date = F, GPAAsOfDate = F, Description = F, Calculation = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentGroup = F, GradeLevelIDCohort = F, FullGroupingDescription = F, ParameterDescription = F, EntityID = F, SchoolYearID = F, StudentActiveAsOfDate = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "RankRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a RankRunHistory
	#'
	#' This function returns a dataframe or json object of a RankRunHistory
	#' @param RankRunHistoryID The ID of the RankRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RankRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RankRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RankRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of RankRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getRankRunHistory <- function(RankRunHistoryID, RankMethodID = F, Date = F, GPAAsOfDate = F, Description = F, Calculation = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentGroup = F, GradeLevelIDCohort = F, FullGroupingDescription = F, ParameterDescription = F, EntityID = F, SchoolYearID = F, StudentActiveAsOfDate = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "RankRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "RankRunHistory", objectId = RankRunHistoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a RankRunHistory
	#'
	#' This function deletes a RankRunHistory
	#' @param RankRunHistoryID The ID of the RankRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The RankRunHistoryID of the deleted RankRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteRankRunHistory <- function(RankRunHistoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "RankRunHistory", objectId = RankRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a RankRunHistory
	#'
	#' This function creates a RankRunHistory
	#' @param fieldNames The field values to give the created RankRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created RankRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createRankRunHistory <- function(RankMethodID = NULL, Date = NULL, GPAAsOfDate = NULL, Description = NULL, Calculation = NULL, StudentGroup = NULL, GradeLevelIDCohort = NULL, EntityID = NULL, SchoolYearID = NULL, StudentActiveAsOfDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "RankRunHistory", body = list(DataObject = body), searchFields = append("RankRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a RankRunHistory
	#'
	#' This function modifies a RankRunHistory
	#' @param fieldNames The field values to give the modified RankRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified RankRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyRankRunHistory <- function(RankRunHistoryID, RankMethodID = NULL, Date = NULL, GPAAsOfDate = NULL, Description = NULL, Calculation = NULL, StudentGroup = NULL, GradeLevelIDCohort = NULL, EntityID = NULL, SchoolYearID = NULL, StudentActiveAsOfDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "RankRunHistory", objectId = RankRunHistoryID, body = list(DataObject = body), searchFields = append("RankRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedGradingPeriods
	#'
	#' This function returns a dataframe or json object of TempFailedGradingPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedGradingPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedGradingPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedGradingPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempFailedGradingPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedGradingPeriods <- function(searchConditionsList = NULL, TempFailedGradingPeriodID = F, TempGradingPeriodID = F, Note = F, GradingPeriodSetID = F, GradingPeriodID = F, GradingPeriodSetCode = F, SectionLengthCode = F, Number = F, Description = F, StartDate = F, EndDate = F, OriginalStartDate = F, OriginalEndDate = F, IsUpdated = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingPeriodSetCodeDescription = F, SectionLengthID = F, SectionLengthCodeDescription = F, ProcessAction = F, SectionLengthStartDate = F, SectionLengthEndDate = F, CourseLengthCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempFailedGradingPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedGradingPeriod
	#'
	#' This function returns a dataframe or json object of a TempFailedGradingPeriod
	#' @param TempFailedGradingPeriodID The ID of the TempFailedGradingPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedGradingPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedGradingPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedGradingPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempFailedGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedGradingPeriod <- function(TempFailedGradingPeriodID, TempGradingPeriodID = F, Note = F, GradingPeriodSetID = F, GradingPeriodID = F, GradingPeriodSetCode = F, SectionLengthCode = F, Number = F, Description = F, StartDate = F, EndDate = F, OriginalStartDate = F, OriginalEndDate = F, IsUpdated = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingPeriodSetCodeDescription = F, SectionLengthID = F, SectionLengthCodeDescription = F, ProcessAction = F, SectionLengthStartDate = F, SectionLengthEndDate = F, CourseLengthCode = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedGradingPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempFailedGradingPeriod", objectId = TempFailedGradingPeriodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedGradingPeriod
	#'
	#' This function deletes a TempFailedGradingPeriod
	#' @param TempFailedGradingPeriodID The ID of the TempFailedGradingPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempFailedGradingPeriodID of the deleted TempFailedGradingPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedGradingPeriod <- function(TempFailedGradingPeriodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempFailedGradingPeriod", objectId = TempFailedGradingPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedGradingPeriod
	#'
	#' This function creates a TempFailedGradingPeriod
	#' @param fieldNames The field values to give the created TempFailedGradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempFailedGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedGradingPeriod <- function(TempGradingPeriodID = NULL, Note = NULL, GradingPeriodSetID = NULL, GradingPeriodID = NULL, GradingPeriodSetCode = NULL, SectionLengthCode = NULL, Number = NULL, Description = NULL, StartDate = NULL, EndDate = NULL, OriginalStartDate = NULL, OriginalEndDate = NULL, IsUpdated = NULL, GradingPeriodSetCodeDescription = NULL, SectionLengthID = NULL, SectionLengthCodeDescription = NULL, ProcessAction = NULL, SectionLengthStartDate = NULL, SectionLengthEndDate = NULL, CourseLengthCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempFailedGradingPeriod", body = list(DataObject = body), searchFields = append("TempFailedGradingPeriodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedGradingPeriod
	#'
	#' This function modifies a TempFailedGradingPeriod
	#' @param fieldNames The field values to give the modified TempFailedGradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempFailedGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedGradingPeriod <- function(TempFailedGradingPeriodID, TempGradingPeriodID = NULL, Note = NULL, GradingPeriodSetID = NULL, GradingPeriodID = NULL, GradingPeriodSetCode = NULL, SectionLengthCode = NULL, Number = NULL, Description = NULL, StartDate = NULL, EndDate = NULL, OriginalStartDate = NULL, OriginalEndDate = NULL, IsUpdated = NULL, GradingPeriodSetCodeDescription = NULL, SectionLengthID = NULL, SectionLengthCodeDescription = NULL, ProcessAction = NULL, SectionLengthStartDate = NULL, SectionLengthEndDate = NULL, CourseLengthCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempFailedGradingPeriod", objectId = TempFailedGradingPeriodID, body = list(DataObject = body), searchFields = append("TempFailedGradingPeriodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingConfigDistrictYears
	#'
	#' This function returns a dataframe or json object of GradingConfigDistrictYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigDistrictYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigDistrictYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigDistrictYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingConfigDistrictYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingConfigDistrictYears <- function(searchConditionsList = NULL, ConfigDistrictYearID = F, DistrictID = F, SchoolYearID = F, ConfigDistrictYearIDClonedFrom = F, UseStudentSectionLinkCourseType = F, StudentSectionLinkOption = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, UseStudentSectionLinking = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ConfigDistrictYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingConfigDistrictYear
	#'
	#' This function returns a dataframe or json object of a GradingConfigDistrictYear
	#' @param GradingConfigDistrictYearID The ID of the GradingConfigDistrictYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigDistrictYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigDistrictYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigDistrictYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingConfigDistrictYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingConfigDistrictYear <- function(GradingConfigDistrictYearID, ConfigDistrictYearID = F, DistrictID = F, SchoolYearID = F, ConfigDistrictYearIDClonedFrom = F, UseStudentSectionLinkCourseType = F, StudentSectionLinkOption = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, UseStudentSectionLinking = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingConfigDistrictYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ConfigDistrictYear", objectId = GradingConfigDistrictYearID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingConfigDistrictYear
	#'
	#' This function deletes a GradingConfigDistrictYear
	#' @param GradingConfigDistrictYearID The ID of the GradingConfigDistrictYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingConfigDistrictYearID of the deleted GradingConfigDistrictYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingConfigDistrictYear <- function(GradingConfigDistrictYearID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ConfigDistrictYear", objectId = GradingConfigDistrictYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingConfigDistrictYear
	#'
	#' This function creates a GradingConfigDistrictYear
	#' @param fieldNames The field values to give the created GradingConfigDistrictYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingConfigDistrictYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingConfigDistrictYear <- function(DistrictID = NULL, SchoolYearID = NULL, ConfigDistrictYearIDClonedFrom = NULL, UseStudentSectionLinkCourseType = NULL, StudentSectionLinkOption = NULL, UseStudentSectionLinking = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ConfigDistrictYear", body = list(DataObject = body), searchFields = append("ConfigDistrictYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingConfigDistrictYear
	#'
	#' This function modifies a GradingConfigDistrictYear
	#' @param fieldNames The field values to give the modified GradingConfigDistrictYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingConfigDistrictYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingConfigDistrictYear <- function(ConfigDistrictYearID, DistrictID = NULL, SchoolYearID = NULL, ConfigDistrictYearIDClonedFrom = NULL, UseStudentSectionLinkCourseType = NULL, StudentSectionLinkOption = NULL, UseStudentSectionLinking = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ConfigDistrictYear", objectId = ConfigDistrictYearID, body = list(DataObject = body), searchFields = append("ConfigDistrictYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookLockGradeBuckets
	#'
	#' This function returns a dataframe or json object of GradebookLockGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookLockGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookLockGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookLockGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradebookLockGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookLockGradeBuckets <- function(searchConditionsList = NULL, GradebookLockGradeBucketID = F, ConfigEntityGroupYearID = F, GradingPeriodGradeBucketID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradebookLockGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookLockGradeBucket
	#'
	#' This function returns a dataframe or json object of a GradebookLockGradeBucket
	#' @param GradebookLockGradeBucketID The ID of the GradebookLockGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookLockGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookLockGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookLockGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradebookLockGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookLockGradeBucket <- function(GradebookLockGradeBucketID, ConfigEntityGroupYearID = F, GradingPeriodGradeBucketID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookLockGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradebookLockGradeBucket", objectId = GradebookLockGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookLockGradeBucket
	#'
	#' This function deletes a GradebookLockGradeBucket
	#' @param GradebookLockGradeBucketID The ID of the GradebookLockGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradebookLockGradeBucketID of the deleted GradebookLockGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookLockGradeBucket <- function(GradebookLockGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradebookLockGradeBucket", objectId = GradebookLockGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookLockGradeBucket
	#'
	#' This function creates a GradebookLockGradeBucket
	#' @param fieldNames The field values to give the created GradebookLockGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradebookLockGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookLockGradeBucket <- function(ConfigEntityGroupYearID = NULL, GradingPeriodGradeBucketID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradebookLockGradeBucket", body = list(DataObject = body), searchFields = append("GradebookLockGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookLockGradeBucket
	#'
	#' This function modifies a GradebookLockGradeBucket
	#' @param fieldNames The field values to give the modified GradebookLockGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradebookLockGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookLockGradeBucket <- function(GradebookLockGradeBucketID, ConfigEntityGroupYearID = NULL, GradingPeriodGradeBucketID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradebookLockGradeBucket", objectId = GradebookLockGradeBucketID, body = list(DataObject = body), searchFields = append("GradebookLockGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookLockGradeReferences
	#'
	#' This function returns a dataframe or json object of GradebookLockGradeReferences
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookLockGradeReferences. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookLockGradeReferences.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookLockGradeReference') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradebookLockGradeReferences
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookLockGradeReferences <- function(searchConditionsList = NULL, GradebookLockGradeReferenceID = F, ConfigEntityGroupYearID = F, GradeReferenceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradebookLockGradeReference", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookLockGradeReference
	#'
	#' This function returns a dataframe or json object of a GradebookLockGradeReference
	#' @param GradebookLockGradeReferenceID The ID of the GradebookLockGradeReference to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookLockGradeReference. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookLockGradeReference.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookLockGradeReference') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradebookLockGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookLockGradeReference <- function(GradebookLockGradeReferenceID, ConfigEntityGroupYearID = F, GradeReferenceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookLockGradeReferenceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradebookLockGradeReference", objectId = GradebookLockGradeReferenceID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookLockGradeReference
	#'
	#' This function deletes a GradebookLockGradeReference
	#' @param GradebookLockGradeReferenceID The ID of the GradebookLockGradeReference to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradebookLockGradeReferenceID of the deleted GradebookLockGradeReference.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookLockGradeReference <- function(GradebookLockGradeReferenceID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradebookLockGradeReference", objectId = GradebookLockGradeReferenceID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookLockGradeReference
	#'
	#' This function creates a GradebookLockGradeReference
	#' @param fieldNames The field values to give the created GradebookLockGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradebookLockGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookLockGradeReference <- function(ConfigEntityGroupYearID = NULL, GradeReferenceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradebookLockGradeReference", body = list(DataObject = body), searchFields = append("GradebookLockGradeReferenceID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookLockGradeReference
	#'
	#' This function modifies a GradebookLockGradeReference
	#' @param fieldNames The field values to give the modified GradebookLockGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradebookLockGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookLockGradeReference <- function(GradebookLockGradeReferenceID, ConfigEntityGroupYearID = NULL, GradeReferenceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradebookLockGradeReference", objectId = GradebookLockGradeReferenceID, body = list(DataObject = body), searchFields = append("GradebookLockGradeReferenceID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeBucketFlags
	#'
	#' This function returns a dataframe or json object of GradeBucketFlags
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketFlags. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketFlags.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketFlag') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeBucketFlags
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeBucketFlags <- function(searchConditionsList = NULL, GradeBucketFlagID = F, DistrictID = F, DistrictGroupKey = F, Code = F, Description = F, DisplayOrder = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeBucketFlag", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeBucketFlag
	#'
	#' This function returns a dataframe or json object of a GradeBucketFlag
	#' @param GradeBucketFlagID The ID of the GradeBucketFlag to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketFlag. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketFlag.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketFlag') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeBucketFlag <- function(GradeBucketFlagID, DistrictID = F, DistrictGroupKey = F, Code = F, Description = F, DisplayOrder = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeBucketFlagID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeBucketFlag", objectId = GradeBucketFlagID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeBucketFlag
	#'
	#' This function deletes a GradeBucketFlag
	#' @param GradeBucketFlagID The ID of the GradeBucketFlag to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeBucketFlagID of the deleted GradeBucketFlag.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeBucketFlag <- function(GradeBucketFlagID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeBucketFlag", objectId = GradeBucketFlagID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeBucketFlag
	#'
	#' This function creates a GradeBucketFlag
	#' @param fieldNames The field values to give the created GradeBucketFlag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeBucketFlag <- function(DistrictID = NULL, DistrictGroupKey = NULL, Code = NULL, Description = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeBucketFlag", body = list(DataObject = body), searchFields = append("GradeBucketFlagID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeBucketFlag
	#'
	#' This function modifies a GradeBucketFlag
	#' @param fieldNames The field values to give the modified GradeBucketFlag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeBucketFlag <- function(GradeBucketFlagID, DistrictID = NULL, DistrictGroupKey = NULL, Code = NULL, Description = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeBucketFlag", objectId = GradeBucketFlagID, body = list(DataObject = body), searchFields = append("GradeBucketFlagID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeBucketFlagDetails
	#'
	#' This function returns a dataframe or json object of GradeBucketFlagDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketFlagDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketFlagDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketFlagDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeBucketFlagDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeBucketFlagDetails <- function(searchConditionsList = NULL, GradeBucketFlagDetailID = F, GradeBucketFlagID = F, SchoolYearID = F, EntityID = F, EntityGroupKey = F, UseEarnedOverride = F, UseInEarned = F, UseInFailed = F, UseGPAOverride = F, UseInGPA = F, IsActive = F, PrintWithGradeMark = F, GradeBucketFlagDetailIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, UseGPAPointsOverride = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeBucketFlagDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeBucketFlagDetail
	#'
	#' This function returns a dataframe or json object of a GradeBucketFlagDetail
	#' @param GradeBucketFlagDetailID The ID of the GradeBucketFlagDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketFlagDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketFlagDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketFlagDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeBucketFlagDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeBucketFlagDetail <- function(GradeBucketFlagDetailID, GradeBucketFlagID = F, SchoolYearID = F, EntityID = F, EntityGroupKey = F, UseEarnedOverride = F, UseInEarned = F, UseInFailed = F, UseGPAOverride = F, UseInGPA = F, IsActive = F, PrintWithGradeMark = F, GradeBucketFlagDetailIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, UseGPAPointsOverride = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeBucketFlagDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeBucketFlagDetail", objectId = GradeBucketFlagDetailID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeBucketFlagDetail
	#'
	#' This function deletes a GradeBucketFlagDetail
	#' @param GradeBucketFlagDetailID The ID of the GradeBucketFlagDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeBucketFlagDetailID of the deleted GradeBucketFlagDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeBucketFlagDetail <- function(GradeBucketFlagDetailID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeBucketFlagDetail", objectId = GradeBucketFlagDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeBucketFlagDetail
	#'
	#' This function creates a GradeBucketFlagDetail
	#' @param fieldNames The field values to give the created GradeBucketFlagDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeBucketFlagDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeBucketFlagDetail <- function(GradeBucketFlagID = NULL, SchoolYearID = NULL, EntityID = NULL, EntityGroupKey = NULL, UseEarnedOverride = NULL, UseInEarned = NULL, UseInFailed = NULL, UseGPAOverride = NULL, UseInGPA = NULL, IsActive = NULL, PrintWithGradeMark = NULL, GradeBucketFlagDetailIDClonedFrom = NULL, UseGPAPointsOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeBucketFlagDetail", body = list(DataObject = body), searchFields = append("GradeBucketFlagDetailID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeBucketFlagDetail
	#'
	#' This function modifies a GradeBucketFlagDetail
	#' @param fieldNames The field values to give the modified GradeBucketFlagDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeBucketFlagDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeBucketFlagDetail <- function(GradeBucketFlagDetailID, GradeBucketFlagID = NULL, SchoolYearID = NULL, EntityID = NULL, EntityGroupKey = NULL, UseEarnedOverride = NULL, UseInEarned = NULL, UseInFailed = NULL, UseGPAOverride = NULL, UseInGPA = NULL, IsActive = NULL, PrintWithGradeMark = NULL, GradeBucketFlagDetailIDClonedFrom = NULL, UseGPAPointsOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeBucketFlagDetail", objectId = GradeBucketFlagDetailID, body = list(DataObject = body), searchFields = append("GradeBucketFlagDetailID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradeBucketFlags
	#'
	#' This function returns a dataframe or json object of StudentGradeBucketFlags
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketFlags. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketFlags.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketFlag') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentGradeBucketFlags
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradeBucketFlags <- function(searchConditionsList = NULL, StudentGradeBucketFlagID = F, StudentGradeBucketID = F, GradeBucketFlagID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsManual = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentGradeBucketFlag", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradeBucketFlag
	#'
	#' This function returns a dataframe or json object of a StudentGradeBucketFlag
	#' @param StudentGradeBucketFlagID The ID of the StudentGradeBucketFlag to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketFlag. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketFlag.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketFlag') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentGradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradeBucketFlag <- function(StudentGradeBucketFlagID, StudentGradeBucketID = F, GradeBucketFlagID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsManual = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradeBucketFlagID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentGradeBucketFlag", objectId = StudentGradeBucketFlagID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradeBucketFlag
	#'
	#' This function deletes a StudentGradeBucketFlag
	#' @param StudentGradeBucketFlagID The ID of the StudentGradeBucketFlag to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentGradeBucketFlagID of the deleted StudentGradeBucketFlag.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradeBucketFlag <- function(StudentGradeBucketFlagID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentGradeBucketFlag", objectId = StudentGradeBucketFlagID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradeBucketFlag
	#'
	#' This function creates a StudentGradeBucketFlag
	#' @param fieldNames The field values to give the created StudentGradeBucketFlag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentGradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradeBucketFlag <- function(StudentGradeBucketID = NULL, GradeBucketFlagID = NULL, IsManual = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentGradeBucketFlag", body = list(DataObject = body), searchFields = append("StudentGradeBucketFlagID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradeBucketFlag
	#'
	#' This function modifies a StudentGradeBucketFlag
	#' @param fieldNames The field values to give the modified StudentGradeBucketFlag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentGradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradeBucketFlag <- function(StudentGradeBucketFlagID, StudentGradeBucketID = NULL, GradeBucketFlagID = NULL, IsManual = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentGradeBucketFlag", objectId = StudentGradeBucketFlagID, body = list(DataObject = body), searchFields = append("StudentGradeBucketFlagID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempHonorRollGradeMarkMethodRanges
	#'
	#' This function returns a dataframe or json object of TempHonorRollGradeMarkMethodRanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempHonorRollGradeMarkMethodRanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempHonorRollGradeMarkMethodRanges <- function(searchConditionsList = NULL, TempHonorRollGradeMarkMethodRangeID = F, HonorRollGradeMarkMethodRangeID = F, HonorRollGradeMarkMethodID = F, Name = F, IsActive = F, PriorityOrder = F, TotalAllowableExceptions = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempHonorRollGradeMarkMethodRange
	#'
	#' This function returns a dataframe or json object of a TempHonorRollGradeMarkMethodRange
	#' @param TempHonorRollGradeMarkMethodRangeID The ID of the TempHonorRollGradeMarkMethodRange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempHonorRollGradeMarkMethodRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempHonorRollGradeMarkMethodRange <- function(TempHonorRollGradeMarkMethodRangeID, HonorRollGradeMarkMethodRangeID = F, HonorRollGradeMarkMethodID = F, Name = F, IsActive = F, PriorityOrder = F, TotalAllowableExceptions = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempHonorRollGradeMarkMethodRangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRange", objectId = TempHonorRollGradeMarkMethodRangeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempHonorRollGradeMarkMethodRange
	#'
	#' This function deletes a TempHonorRollGradeMarkMethodRange
	#' @param TempHonorRollGradeMarkMethodRangeID The ID of the TempHonorRollGradeMarkMethodRange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempHonorRollGradeMarkMethodRangeID of the deleted TempHonorRollGradeMarkMethodRange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempHonorRollGradeMarkMethodRange <- function(TempHonorRollGradeMarkMethodRangeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRange", objectId = TempHonorRollGradeMarkMethodRangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempHonorRollGradeMarkMethodRange
	#'
	#' This function creates a TempHonorRollGradeMarkMethodRange
	#' @param fieldNames The field values to give the created TempHonorRollGradeMarkMethodRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempHonorRollGradeMarkMethodRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempHonorRollGradeMarkMethodRange <- function(HonorRollGradeMarkMethodRangeID = NULL, HonorRollGradeMarkMethodID = NULL, Name = NULL, IsActive = NULL, PriorityOrder = NULL, TotalAllowableExceptions = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRange", body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempHonorRollGradeMarkMethodRange
	#'
	#' This function modifies a TempHonorRollGradeMarkMethodRange
	#' @param fieldNames The field values to give the modified TempHonorRollGradeMarkMethodRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempHonorRollGradeMarkMethodRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempHonorRollGradeMarkMethodRange <- function(TempHonorRollGradeMarkMethodRangeID, HonorRollGradeMarkMethodRangeID = NULL, HonorRollGradeMarkMethodID = NULL, Name = NULL, IsActive = NULL, PriorityOrder = NULL, TotalAllowableExceptions = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRange", objectId = TempHonorRollGradeMarkMethodRangeID, body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempHonorRollGradeMarkMethodRangeCourseGroups
	#'
	#' This function returns a dataframe or json object of TempHonorRollGradeMarkMethodRangeCourseGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRangeCourseGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRangeCourseGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRangeCourseGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempHonorRollGradeMarkMethodRangeCourseGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempHonorRollGradeMarkMethodRangeCourseGroups <- function(searchConditionsList = NULL, TempHonorRollGradeMarkMethodRangeCourseGroupID = F, HonorRollGradeMarkMethodRangeCourseGroupID = F, HonorRollGradeMarkMethodRangeID = F, TempHonorRollGradeMarkMethodRangeID = F, CourseGroupID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeCourseGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempHonorRollGradeMarkMethodRangeCourseGroup
	#'
	#' This function returns a dataframe or json object of a TempHonorRollGradeMarkMethodRangeCourseGroup
	#' @param TempHonorRollGradeMarkMethodRangeCourseGroupID The ID of the TempHonorRollGradeMarkMethodRangeCourseGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRangeCourseGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRangeCourseGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRangeCourseGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempHonorRollGradeMarkMethodRangeCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempHonorRollGradeMarkMethodRangeCourseGroup <- function(TempHonorRollGradeMarkMethodRangeCourseGroupID, HonorRollGradeMarkMethodRangeCourseGroupID = F, HonorRollGradeMarkMethodRangeID = F, TempHonorRollGradeMarkMethodRangeID = F, CourseGroupID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempHonorRollGradeMarkMethodRangeCourseGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeCourseGroup", objectId = TempHonorRollGradeMarkMethodRangeCourseGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempHonorRollGradeMarkMethodRangeCourseGroup
	#'
	#' This function deletes a TempHonorRollGradeMarkMethodRangeCourseGroup
	#' @param TempHonorRollGradeMarkMethodRangeCourseGroupID The ID of the TempHonorRollGradeMarkMethodRangeCourseGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempHonorRollGradeMarkMethodRangeCourseGroupID of the deleted TempHonorRollGradeMarkMethodRangeCourseGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempHonorRollGradeMarkMethodRangeCourseGroup <- function(TempHonorRollGradeMarkMethodRangeCourseGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeCourseGroup", objectId = TempHonorRollGradeMarkMethodRangeCourseGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempHonorRollGradeMarkMethodRangeCourseGroup
	#'
	#' This function creates a TempHonorRollGradeMarkMethodRangeCourseGroup
	#' @param fieldNames The field values to give the created TempHonorRollGradeMarkMethodRangeCourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempHonorRollGradeMarkMethodRangeCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempHonorRollGradeMarkMethodRangeCourseGroup <- function(HonorRollGradeMarkMethodRangeCourseGroupID = NULL, HonorRollGradeMarkMethodRangeID = NULL, TempHonorRollGradeMarkMethodRangeID = NULL, CourseGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeCourseGroup", body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeCourseGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempHonorRollGradeMarkMethodRangeCourseGroup
	#'
	#' This function modifies a TempHonorRollGradeMarkMethodRangeCourseGroup
	#' @param fieldNames The field values to give the modified TempHonorRollGradeMarkMethodRangeCourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempHonorRollGradeMarkMethodRangeCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempHonorRollGradeMarkMethodRangeCourseGroup <- function(TempHonorRollGradeMarkMethodRangeCourseGroupID, HonorRollGradeMarkMethodRangeCourseGroupID = NULL, HonorRollGradeMarkMethodRangeID = NULL, TempHonorRollGradeMarkMethodRangeID = NULL, CourseGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeCourseGroup", objectId = TempHonorRollGradeMarkMethodRangeCourseGroupID, body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeCourseGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempHonorRollGradeMarkMethodRangeGradeMarks
	#'
	#' This function returns a dataframe or json object of TempHonorRollGradeMarkMethodRangeGradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRangeGradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRangeGradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRangeGradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempHonorRollGradeMarkMethodRangeGradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempHonorRollGradeMarkMethodRangeGradeMarks <- function(searchConditionsList = NULL, TempHonorRollGradeMarkMethodRangeGradeMarkID = F, HonorRollGradeMarkMethodRangeGradeMarkID = F, TempHonorRollGradeMarkMethodRangeID = F, GradeMarkID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempHonorRollGradeMarkMethodRangeGradeMark
	#'
	#' This function returns a dataframe or json object of a TempHonorRollGradeMarkMethodRangeGradeMark
	#' @param TempHonorRollGradeMarkMethodRangeGradeMarkID The ID of the TempHonorRollGradeMarkMethodRangeGradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRangeGradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRangeGradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRangeGradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempHonorRollGradeMarkMethodRangeGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempHonorRollGradeMarkMethodRangeGradeMark <- function(TempHonorRollGradeMarkMethodRangeGradeMarkID, HonorRollGradeMarkMethodRangeGradeMarkID = F, TempHonorRollGradeMarkMethodRangeID = F, GradeMarkID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempHonorRollGradeMarkMethodRangeGradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradeMark", objectId = TempHonorRollGradeMarkMethodRangeGradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempHonorRollGradeMarkMethodRangeGradeMark
	#'
	#' This function deletes a TempHonorRollGradeMarkMethodRangeGradeMark
	#' @param TempHonorRollGradeMarkMethodRangeGradeMarkID The ID of the TempHonorRollGradeMarkMethodRangeGradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempHonorRollGradeMarkMethodRangeGradeMarkID of the deleted TempHonorRollGradeMarkMethodRangeGradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempHonorRollGradeMarkMethodRangeGradeMark <- function(TempHonorRollGradeMarkMethodRangeGradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradeMark", objectId = TempHonorRollGradeMarkMethodRangeGradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempHonorRollGradeMarkMethodRangeGradeMark
	#'
	#' This function creates a TempHonorRollGradeMarkMethodRangeGradeMark
	#' @param fieldNames The field values to give the created TempHonorRollGradeMarkMethodRangeGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempHonorRollGradeMarkMethodRangeGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempHonorRollGradeMarkMethodRangeGradeMark <- function(HonorRollGradeMarkMethodRangeGradeMarkID = NULL, TempHonorRollGradeMarkMethodRangeID = NULL, GradeMarkID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradeMark", body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempHonorRollGradeMarkMethodRangeGradeMark
	#'
	#' This function modifies a TempHonorRollGradeMarkMethodRangeGradeMark
	#' @param fieldNames The field values to give the modified TempHonorRollGradeMarkMethodRangeGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempHonorRollGradeMarkMethodRangeGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempHonorRollGradeMarkMethodRangeGradeMark <- function(TempHonorRollGradeMarkMethodRangeGradeMarkID, HonorRollGradeMarkMethodRangeGradeMarkID = NULL, TempHonorRollGradeMarkMethodRangeID = NULL, GradeMarkID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradeMark", objectId = TempHonorRollGradeMarkMethodRangeGradeMarkID, body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBuckets
	#'
	#' This function returns a dataframe or json object of TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempHonorRollGradeMarkMethodRangeGradingPeriodGradeBuckets <- function(searchConditionsList = NULL, TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID = F, HonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID = F, TempHonorRollGradeMarkMethodRangeID = F, GradingPeriodGradeBucketID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#'
	#' This function returns a dataframe or json object of a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' @param TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID The ID of the TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket <- function(TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID, HonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID = F, TempHonorRollGradeMarkMethodRangeID = F, GradingPeriodGradeBucketID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket", objectId = TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#'
	#' This function deletes a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' @param TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID The ID of the TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID of the deleted TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket <- function(TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket", objectId = TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#'
	#' This function creates a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' @param fieldNames The field values to give the created TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket <- function(HonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID = NULL, TempHonorRollGradeMarkMethodRangeID = NULL, GradingPeriodGradeBucketID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket", body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#'
	#' This function modifies a TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' @param fieldNames The field values to give the modified TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket <- function(TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID, HonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID = NULL, TempHonorRollGradeMarkMethodRangeID = NULL, GradingPeriodGradeBucketID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucket", objectId = TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID, body = list(DataObject = body), searchFields = append("TempHonorRollGradeMarkMethodRangeGradingPeriodGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentCommentBuckets
	#'
	#' This function returns a dataframe or json object of TempStudentCommentBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCommentBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCommentBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCommentBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentCommentBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentCommentBuckets <- function(searchConditionsList = NULL, TempStudentCommentBucketID = F, StudentCommentBucketID = F, StudentSectionID = F, CommentBucketID = F, GradingPeriodID = F, NewCommentCodeID = F, CommentBucketName = F, NewCommentCode = F, CurrentCommentCode = F, StudentName = F, StudentSectionDescription = F, GradingPeriodDescription = F, SectionLengthCode = F, DisplayOrder = F, WorkflowAction = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentCommentBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentCommentBucket
	#'
	#' This function returns a dataframe or json object of a TempStudentCommentBucket
	#' @param TempStudentCommentBucketID The ID of the TempStudentCommentBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCommentBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCommentBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCommentBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentCommentBucket <- function(TempStudentCommentBucketID, StudentCommentBucketID = F, StudentSectionID = F, CommentBucketID = F, GradingPeriodID = F, NewCommentCodeID = F, CommentBucketName = F, NewCommentCode = F, CurrentCommentCode = F, StudentName = F, StudentSectionDescription = F, GradingPeriodDescription = F, SectionLengthCode = F, DisplayOrder = F, WorkflowAction = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentCommentBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentCommentBucket", objectId = TempStudentCommentBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentCommentBucket
	#'
	#' This function deletes a TempStudentCommentBucket
	#' @param TempStudentCommentBucketID The ID of the TempStudentCommentBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentCommentBucketID of the deleted TempStudentCommentBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentCommentBucket <- function(TempStudentCommentBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentCommentBucket", objectId = TempStudentCommentBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentCommentBucket
	#'
	#' This function creates a TempStudentCommentBucket
	#' @param fieldNames The field values to give the created TempStudentCommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentCommentBucket <- function(StudentCommentBucketID = NULL, StudentSectionID = NULL, CommentBucketID = NULL, GradingPeriodID = NULL, NewCommentCodeID = NULL, CommentBucketName = NULL, NewCommentCode = NULL, CurrentCommentCode = NULL, StudentName = NULL, StudentSectionDescription = NULL, GradingPeriodDescription = NULL, SectionLengthCode = NULL, DisplayOrder = NULL, WorkflowAction = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentCommentBucket", body = list(DataObject = body), searchFields = append("TempStudentCommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentCommentBucket
	#'
	#' This function modifies a TempStudentCommentBucket
	#' @param fieldNames The field values to give the modified TempStudentCommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentCommentBucket <- function(TempStudentCommentBucketID, StudentCommentBucketID = NULL, StudentSectionID = NULL, CommentBucketID = NULL, GradingPeriodID = NULL, NewCommentCodeID = NULL, CommentBucketName = NULL, NewCommentCode = NULL, CurrentCommentCode = NULL, StudentName = NULL, StudentSectionDescription = NULL, GradingPeriodDescription = NULL, SectionLengthCode = NULL, DisplayOrder = NULL, WorkflowAction = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentCommentBucket", objectId = TempStudentCommentBucketID, body = list(DataObject = body), searchFields = append("TempStudentCommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentGradeBucketFlags
	#'
	#' This function returns a dataframe or json object of TempStudentGradeBucketFlags
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGradeBucketFlags. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGradeBucketFlags.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGradeBucketFlag') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentGradeBucketFlags
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentGradeBucketFlags <- function(searchConditionsList = NULL, TempStudentGradeBucketFlagID = F, StudentGradeBucketID = F, GradeBucketFlagID = F, GradeBucketFlagCode = F, StudentName = F, StudentSectionDescription = F, CourseSectionCode = F, SectionLengthCode = F, GradeBucketLabel = F, GradeMarkCode = F, IsException = F, ExceptionReason = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsError = F, IsDelete = F, IsManual = F, StudentGradeBucketFlagIDToDelete = F, StudentSectionID = F, SchoolYearDescription = F, SelectedToProcess = F, StudentNumber = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentGradeBucketFlag", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentGradeBucketFlag
	#'
	#' This function returns a dataframe or json object of a TempStudentGradeBucketFlag
	#' @param TempStudentGradeBucketFlagID The ID of the TempStudentGradeBucketFlag to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGradeBucketFlag. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGradeBucketFlag.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGradeBucketFlag') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentGradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentGradeBucketFlag <- function(TempStudentGradeBucketFlagID, StudentGradeBucketID = F, GradeBucketFlagID = F, GradeBucketFlagCode = F, StudentName = F, StudentSectionDescription = F, CourseSectionCode = F, SectionLengthCode = F, GradeBucketLabel = F, GradeMarkCode = F, IsException = F, ExceptionReason = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsError = F, IsDelete = F, IsManual = F, StudentGradeBucketFlagIDToDelete = F, StudentSectionID = F, SchoolYearDescription = F, SelectedToProcess = F, StudentNumber = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentGradeBucketFlagID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentGradeBucketFlag", objectId = TempStudentGradeBucketFlagID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentGradeBucketFlag
	#'
	#' This function deletes a TempStudentGradeBucketFlag
	#' @param TempStudentGradeBucketFlagID The ID of the TempStudentGradeBucketFlag to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentGradeBucketFlagID of the deleted TempStudentGradeBucketFlag.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentGradeBucketFlag <- function(TempStudentGradeBucketFlagID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentGradeBucketFlag", objectId = TempStudentGradeBucketFlagID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentGradeBucketFlag
	#'
	#' This function creates a TempStudentGradeBucketFlag
	#' @param fieldNames The field values to give the created TempStudentGradeBucketFlag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentGradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentGradeBucketFlag <- function(StudentGradeBucketID = NULL, GradeBucketFlagID = NULL, GradeBucketFlagCode = NULL, StudentName = NULL, StudentSectionDescription = NULL, CourseSectionCode = NULL, SectionLengthCode = NULL, GradeBucketLabel = NULL, GradeMarkCode = NULL, IsException = NULL, ExceptionReason = NULL, IsError = NULL, IsDelete = NULL, IsManual = NULL, StudentGradeBucketFlagIDToDelete = NULL, StudentSectionID = NULL, SchoolYearDescription = NULL, SelectedToProcess = NULL, StudentNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentGradeBucketFlag", body = list(DataObject = body), searchFields = append("TempStudentGradeBucketFlagID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentGradeBucketFlag
	#'
	#' This function modifies a TempStudentGradeBucketFlag
	#' @param fieldNames The field values to give the modified TempStudentGradeBucketFlag. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentGradeBucketFlag
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentGradeBucketFlag <- function(TempStudentGradeBucketFlagID, StudentGradeBucketID = NULL, GradeBucketFlagID = NULL, GradeBucketFlagCode = NULL, StudentName = NULL, StudentSectionDescription = NULL, CourseSectionCode = NULL, SectionLengthCode = NULL, GradeBucketLabel = NULL, GradeMarkCode = NULL, IsException = NULL, ExceptionReason = NULL, IsError = NULL, IsDelete = NULL, IsManual = NULL, StudentGradeBucketFlagIDToDelete = NULL, StudentSectionID = NULL, SchoolYearDescription = NULL, SelectedToProcess = NULL, StudentNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentGradeBucketFlag", objectId = TempStudentGradeBucketFlagID, body = list(DataObject = body), searchFields = append("TempStudentGradeBucketFlagID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TranscriptSents
	#'
	#' This function returns a dataframe or json object of TranscriptSents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TranscriptSents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TranscriptSents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TranscriptSent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TranscriptSents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTranscriptSents <- function(searchConditionsList = NULL, TranscriptSentID = F, StudentID = F, InstitutionID = F, DateSent = F, Comment = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TranscriptSent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TranscriptSent
	#'
	#' This function returns a dataframe or json object of a TranscriptSent
	#' @param TranscriptSentID The ID of the TranscriptSent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TranscriptSent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TranscriptSent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TranscriptSent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TranscriptSent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTranscriptSent <- function(TranscriptSentID, StudentID = F, InstitutionID = F, DateSent = F, Comment = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TranscriptSentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TranscriptSent", objectId = TranscriptSentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TranscriptSent
	#'
	#' This function deletes a TranscriptSent
	#' @param TranscriptSentID The ID of the TranscriptSent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TranscriptSentID of the deleted TranscriptSent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTranscriptSent <- function(TranscriptSentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TranscriptSent", objectId = TranscriptSentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TranscriptSent
	#'
	#' This function creates a TranscriptSent
	#' @param fieldNames The field values to give the created TranscriptSent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TranscriptSent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTranscriptSent <- function(StudentID = NULL, InstitutionID = NULL, DateSent = NULL, Comment = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TranscriptSent", body = list(DataObject = body), searchFields = append("TranscriptSentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TranscriptSent
	#'
	#' This function modifies a TranscriptSent
	#' @param fieldNames The field values to give the modified TranscriptSent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TranscriptSent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTranscriptSent <- function(TranscriptSentID, StudentID = NULL, InstitutionID = NULL, DateSent = NULL, Comment = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TranscriptSent", objectId = TranscriptSentID, body = list(DataObject = body), searchFields = append("TranscriptSentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGPAMethodGradeReferenceOverrides
	#'
	#' This function returns a dataframe or json object of CourseGPAMethodGradeReferenceOverrides
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGPAMethodGradeReferenceOverrides. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGPAMethodGradeReferenceOverrides.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGPAMethodGradeReferenceOverride') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CourseGPAMethodGradeReferenceOverrides
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGPAMethodGradeReferenceOverrides <- function(searchConditionsList = NULL, CourseGPAMethodGradeReferenceOverrideID = F, CourseGPAMethodID = F, GradeReferenceID = F, PointSetEntityID = F, GPACredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CourseGPAMethodGradeReferenceOverride", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGPAMethodGradeReferenceOverride
	#'
	#' This function returns a dataframe or json object of a CourseGPAMethodGradeReferenceOverride
	#' @param CourseGPAMethodGradeReferenceOverrideID The ID of the CourseGPAMethodGradeReferenceOverride to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGPAMethodGradeReferenceOverride. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGPAMethodGradeReferenceOverride.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGPAMethodGradeReferenceOverride') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CourseGPAMethodGradeReferenceOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGPAMethodGradeReferenceOverride <- function(CourseGPAMethodGradeReferenceOverrideID, CourseGPAMethodID = F, GradeReferenceID = F, PointSetEntityID = F, GPACredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGPAMethodGradeReferenceOverrideID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CourseGPAMethodGradeReferenceOverride", objectId = CourseGPAMethodGradeReferenceOverrideID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGPAMethodGradeReferenceOverride
	#'
	#' This function deletes a CourseGPAMethodGradeReferenceOverride
	#' @param CourseGPAMethodGradeReferenceOverrideID The ID of the CourseGPAMethodGradeReferenceOverride to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CourseGPAMethodGradeReferenceOverrideID of the deleted CourseGPAMethodGradeReferenceOverride.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGPAMethodGradeReferenceOverride <- function(CourseGPAMethodGradeReferenceOverrideID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CourseGPAMethodGradeReferenceOverride", objectId = CourseGPAMethodGradeReferenceOverrideID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGPAMethodGradeReferenceOverride
	#'
	#' This function creates a CourseGPAMethodGradeReferenceOverride
	#' @param fieldNames The field values to give the created CourseGPAMethodGradeReferenceOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CourseGPAMethodGradeReferenceOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGPAMethodGradeReferenceOverride <- function(CourseGPAMethodID = NULL, GradeReferenceID = NULL, PointSetEntityID = NULL, GPACredits = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CourseGPAMethodGradeReferenceOverride", body = list(DataObject = body), searchFields = append("CourseGPAMethodGradeReferenceOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGPAMethodGradeReferenceOverride
	#'
	#' This function modifies a CourseGPAMethodGradeReferenceOverride
	#' @param fieldNames The field values to give the modified CourseGPAMethodGradeReferenceOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CourseGPAMethodGradeReferenceOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGPAMethodGradeReferenceOverride <- function(CourseGPAMethodGradeReferenceOverrideID, CourseGPAMethodID = NULL, GradeReferenceID = NULL, PointSetEntityID = NULL, GPACredits = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CourseGPAMethodGradeReferenceOverride", objectId = CourseGPAMethodGradeReferenceOverrideID, body = list(DataObject = body), searchFields = append("CourseGPAMethodGradeReferenceOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFactorBasedAddOns
	#'
	#' This function returns a dataframe or json object of TempFactorBasedAddOns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFactorBasedAddOns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFactorBasedAddOns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFactorBasedAddOn') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempFactorBasedAddOns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFactorBasedAddOns <- function(searchConditionsList = NULL, TempFactorBasedAddOnID = F, TempFactorBasedAddOnClonedFromID = F, OriginalGradingEndDateCutoffForCumulative = F, GradingEndDateCutoffForCumulative = F, GradeReferenceDisplayName = F, GPABucketEntityDisplayName = F, Factor = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempFactorBasedAddOn", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFactorBasedAddOn
	#'
	#' This function returns a dataframe or json object of a TempFactorBasedAddOn
	#' @param TempFactorBasedAddOnID The ID of the TempFactorBasedAddOn to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFactorBasedAddOn. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFactorBasedAddOn.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFactorBasedAddOn') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempFactorBasedAddOn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFactorBasedAddOn <- function(TempFactorBasedAddOnID, TempFactorBasedAddOnClonedFromID = F, OriginalGradingEndDateCutoffForCumulative = F, GradingEndDateCutoffForCumulative = F, GradeReferenceDisplayName = F, GPABucketEntityDisplayName = F, Factor = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFactorBasedAddOnID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempFactorBasedAddOn", objectId = TempFactorBasedAddOnID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFactorBasedAddOn
	#'
	#' This function deletes a TempFactorBasedAddOn
	#' @param TempFactorBasedAddOnID The ID of the TempFactorBasedAddOn to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempFactorBasedAddOnID of the deleted TempFactorBasedAddOn.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFactorBasedAddOn <- function(TempFactorBasedAddOnID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempFactorBasedAddOn", objectId = TempFactorBasedAddOnID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFactorBasedAddOn
	#'
	#' This function creates a TempFactorBasedAddOn
	#' @param fieldNames The field values to give the created TempFactorBasedAddOn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempFactorBasedAddOn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFactorBasedAddOn <- function(TempFactorBasedAddOnClonedFromID = NULL, OriginalGradingEndDateCutoffForCumulative = NULL, GradingEndDateCutoffForCumulative = NULL, GradeReferenceDisplayName = NULL, GPABucketEntityDisplayName = NULL, Factor = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempFactorBasedAddOn", body = list(DataObject = body), searchFields = append("TempFactorBasedAddOnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFactorBasedAddOn
	#'
	#' This function modifies a TempFactorBasedAddOn
	#' @param fieldNames The field values to give the modified TempFactorBasedAddOn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempFactorBasedAddOn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFactorBasedAddOn <- function(TempFactorBasedAddOnID, TempFactorBasedAddOnClonedFromID = NULL, OriginalGradingEndDateCutoffForCumulative = NULL, GradingEndDateCutoffForCumulative = NULL, GradeReferenceDisplayName = NULL, GPABucketEntityDisplayName = NULL, Factor = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempFactorBasedAddOn", objectId = TempFactorBasedAddOnID, body = list(DataObject = body), searchFields = append("TempFactorBasedAddOnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List QueuedGPACalculations
	#'
	#' This function returns a dataframe or json object of QueuedGPACalculations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QueuedGPACalculations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QueuedGPACalculations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QueuedGPACalculation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of QueuedGPACalculations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listQueuedGPACalculations <- function(searchConditionsList = NULL, QueuedGPACalculationID = F, SkipCredits = F, SourceTypeCode = F, SourceType = F, SourcePrimaryKey = F, StatusCode = F, Status = F, StartTime = F, EndTime = F, HostName = F, ThreadName = F, ProcessID = F, UserIDImpersonator = F, EntityID = F, SchoolYearID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "QueuedGPACalculation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a QueuedGPACalculation
	#'
	#' This function returns a dataframe or json object of a QueuedGPACalculation
	#' @param QueuedGPACalculationID The ID of the QueuedGPACalculation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QueuedGPACalculation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QueuedGPACalculation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QueuedGPACalculation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of QueuedGPACalculation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getQueuedGPACalculation <- function(QueuedGPACalculationID, SkipCredits = F, SourceTypeCode = F, SourceType = F, SourcePrimaryKey = F, StatusCode = F, Status = F, StartTime = F, EndTime = F, HostName = F, ThreadName = F, ProcessID = F, UserIDImpersonator = F, EntityID = F, SchoolYearID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "QueuedGPACalculationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "QueuedGPACalculation", objectId = QueuedGPACalculationID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a QueuedGPACalculation
	#'
	#' This function deletes a QueuedGPACalculation
	#' @param QueuedGPACalculationID The ID of the QueuedGPACalculation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The QueuedGPACalculationID of the deleted QueuedGPACalculation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteQueuedGPACalculation <- function(QueuedGPACalculationID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "QueuedGPACalculation", objectId = QueuedGPACalculationID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a QueuedGPACalculation
	#'
	#' This function creates a QueuedGPACalculation
	#' @param fieldNames The field values to give the created QueuedGPACalculation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created QueuedGPACalculation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createQueuedGPACalculation <- function(SkipCredits = NULL, SourceType = NULL, SourcePrimaryKey = NULL, Status = NULL, StartTime = NULL, EndTime = NULL, HostName = NULL, ThreadName = NULL, ProcessID = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "QueuedGPACalculation", body = list(DataObject = body), searchFields = append("QueuedGPACalculationID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a QueuedGPACalculation
	#'
	#' This function modifies a QueuedGPACalculation
	#' @param fieldNames The field values to give the modified QueuedGPACalculation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified QueuedGPACalculation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyQueuedGPACalculation <- function(QueuedGPACalculationID, SkipCredits = NULL, SourceType = NULL, SourcePrimaryKey = NULL, Status = NULL, StartTime = NULL, EndTime = NULL, HostName = NULL, ThreadName = NULL, ProcessID = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "QueuedGPACalculation", objectId = QueuedGPACalculationID, body = list(DataObject = body), searchFields = append("QueuedGPACalculationID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradingUtilityErrors
	#'
	#' This function returns a dataframe or json object of TempGradingUtilityErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingUtilityErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingUtilityErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingUtilityError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradingUtilityErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradingUtilityErrors <- function(searchConditionsList = NULL, TempGradingUtilityErrorID = F, HonorRollName = F, StudentName = F, Grade = F, Value = F, Error = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradingUtilityError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradingUtilityError
	#'
	#' This function returns a dataframe or json object of a TempGradingUtilityError
	#' @param TempGradingUtilityErrorID The ID of the TempGradingUtilityError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingUtilityError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingUtilityError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingUtilityError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradingUtilityError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradingUtilityError <- function(TempGradingUtilityErrorID, HonorRollName = F, StudentName = F, Grade = F, Value = F, Error = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradingUtilityErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradingUtilityError", objectId = TempGradingUtilityErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradingUtilityError
	#'
	#' This function deletes a TempGradingUtilityError
	#' @param TempGradingUtilityErrorID The ID of the TempGradingUtilityError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradingUtilityErrorID of the deleted TempGradingUtilityError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradingUtilityError <- function(TempGradingUtilityErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradingUtilityError", objectId = TempGradingUtilityErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradingUtilityError
	#'
	#' This function creates a TempGradingUtilityError
	#' @param fieldNames The field values to give the created TempGradingUtilityError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradingUtilityError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradingUtilityError <- function(HonorRollName = NULL, StudentName = NULL, Grade = NULL, Value = NULL, Error = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradingUtilityError", body = list(DataObject = body), searchFields = append("TempGradingUtilityErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradingUtilityError
	#'
	#' This function modifies a TempGradingUtilityError
	#' @param fieldNames The field values to give the modified TempGradingUtilityError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradingUtilityError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradingUtilityError <- function(TempGradingUtilityErrorID, HonorRollName = NULL, StudentName = NULL, Grade = NULL, Value = NULL, Error = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradingUtilityError", objectId = TempGradingUtilityErrorID, body = list(DataObject = body), searchFields = append("TempGradingUtilityErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempHonorRollMethodRanges
	#'
	#' This function returns a dataframe or json object of TempHonorRollMethodRanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollMethodRanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollMethodRanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollMethodRange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempHonorRollMethodRanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempHonorRollMethodRanges <- function(searchConditionsList = NULL, TempHonorRollMethodRangeID = F, HonorRollMethodRangeID = F, IsUsed = F, Name = F, GPALow = F, GPAHigh = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempHonorRollMethodRange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempHonorRollMethodRange
	#'
	#' This function returns a dataframe or json object of a TempHonorRollMethodRange
	#' @param TempHonorRollMethodRangeID The ID of the TempHonorRollMethodRange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempHonorRollMethodRange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempHonorRollMethodRange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempHonorRollMethodRange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempHonorRollMethodRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempHonorRollMethodRange <- function(TempHonorRollMethodRangeID, HonorRollMethodRangeID = F, IsUsed = F, Name = F, GPALow = F, GPAHigh = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempHonorRollMethodRangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempHonorRollMethodRange", objectId = TempHonorRollMethodRangeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempHonorRollMethodRange
	#'
	#' This function deletes a TempHonorRollMethodRange
	#' @param TempHonorRollMethodRangeID The ID of the TempHonorRollMethodRange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempHonorRollMethodRangeID of the deleted TempHonorRollMethodRange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempHonorRollMethodRange <- function(TempHonorRollMethodRangeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempHonorRollMethodRange", objectId = TempHonorRollMethodRangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempHonorRollMethodRange
	#'
	#' This function creates a TempHonorRollMethodRange
	#' @param fieldNames The field values to give the created TempHonorRollMethodRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempHonorRollMethodRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempHonorRollMethodRange <- function(HonorRollMethodRangeID = NULL, IsUsed = NULL, Name = NULL, GPALow = NULL, GPAHigh = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempHonorRollMethodRange", body = list(DataObject = body), searchFields = append("TempHonorRollMethodRangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempHonorRollMethodRange
	#'
	#' This function modifies a TempHonorRollMethodRange
	#' @param fieldNames The field values to give the modified TempHonorRollMethodRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempHonorRollMethodRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempHonorRollMethodRange <- function(TempHonorRollMethodRangeID, HonorRollMethodRangeID = NULL, IsUsed = NULL, Name = NULL, GPALow = NULL, GPAHigh = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempHonorRollMethodRange", objectId = TempHonorRollMethodRangeID, body = list(DataObject = body), searchFields = append("TempHonorRollMethodRangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentRanks
	#'
	#' This function returns a dataframe or json object of TempStudentRanks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentRanks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentRanks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentRank') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentRanks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentRanks <- function(searchConditionsList = NULL, TempStudentRankID = F, StudentRankID = F, StudentID = F, StudentName = F, GPA = F, Rank = F, NoRank = F, TotalStudents = F, RankMethodID = F, StudentGrade = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsProspectiveRank = F, UseOverride = F, IsManualRank = F, SchoolYearIDCohort = F, CohortNumericYear = F, StudentRankSort = F, GraduationRequirementYear = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentRank", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentRank
	#'
	#' This function returns a dataframe or json object of a TempStudentRank
	#' @param TempStudentRankID The ID of the TempStudentRank to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentRank. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentRank.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentRank') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentRank
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentRank <- function(TempStudentRankID, StudentRankID = F, StudentID = F, StudentName = F, GPA = F, Rank = F, NoRank = F, TotalStudents = F, RankMethodID = F, StudentGrade = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsProspectiveRank = F, UseOverride = F, IsManualRank = F, SchoolYearIDCohort = F, CohortNumericYear = F, StudentRankSort = F, GraduationRequirementYear = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentRankID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentRank", objectId = TempStudentRankID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentRank
	#'
	#' This function deletes a TempStudentRank
	#' @param TempStudentRankID The ID of the TempStudentRank to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentRankID of the deleted TempStudentRank.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentRank <- function(TempStudentRankID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentRank", objectId = TempStudentRankID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentRank
	#'
	#' This function creates a TempStudentRank
	#' @param fieldNames The field values to give the created TempStudentRank. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentRank
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentRank <- function(StudentRankID = NULL, StudentID = NULL, StudentName = NULL, GPA = NULL, Rank = NULL, NoRank = NULL, TotalStudents = NULL, RankMethodID = NULL, StudentGrade = NULL, IsProspectiveRank = NULL, UseOverride = NULL, IsManualRank = NULL, SchoolYearIDCohort = NULL, CohortNumericYear = NULL, StudentRankSort = NULL, GraduationRequirementYear = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentRank", body = list(DataObject = body), searchFields = append("TempStudentRankID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentRank
	#'
	#' This function modifies a TempStudentRank
	#' @param fieldNames The field values to give the modified TempStudentRank. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentRank
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentRank <- function(TempStudentRankID, StudentRankID = NULL, StudentID = NULL, StudentName = NULL, GPA = NULL, Rank = NULL, NoRank = NULL, TotalStudents = NULL, RankMethodID = NULL, StudentGrade = NULL, IsProspectiveRank = NULL, UseOverride = NULL, IsManualRank = NULL, SchoolYearIDCohort = NULL, CohortNumericYear = NULL, StudentRankSort = NULL, GraduationRequirementYear = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentRank", objectId = TempStudentRankID, body = list(DataObject = body), searchFields = append("TempStudentRankID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentRanks
	#'
	#' This function returns a dataframe or json object of StudentRanks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentRanks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentRanks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentRank') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentRanks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentRanks <- function(searchConditionsList = NULL, StudentRankID = F, StudentID = F, RankRunHistoryID = F, Value = F, NumberInRank = F, NumberOutOf = F, DisplayRank = F, UseOverride = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsProspectiveRank = F, IsManualRank = F, SchoolYearIDCohort = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentRank", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentRank
	#'
	#' This function returns a dataframe or json object of a StudentRank
	#' @param StudentRankID The ID of the StudentRank to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentRank. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentRank.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentRank') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentRank
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentRank <- function(StudentRankID, StudentID = F, RankRunHistoryID = F, Value = F, NumberInRank = F, NumberOutOf = F, DisplayRank = F, UseOverride = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsProspectiveRank = F, IsManualRank = F, SchoolYearIDCohort = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentRankID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentRank", objectId = StudentRankID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentRank
	#'
	#' This function deletes a StudentRank
	#' @param StudentRankID The ID of the StudentRank to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentRankID of the deleted StudentRank.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentRank <- function(StudentRankID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentRank", objectId = StudentRankID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentRank
	#'
	#' This function creates a StudentRank
	#' @param fieldNames The field values to give the created StudentRank. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentRank
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentRank <- function(StudentID = NULL, RankRunHistoryID = NULL, Value = NULL, NumberInRank = NULL, NumberOutOf = NULL, UseOverride = NULL, IsProspectiveRank = NULL, IsManualRank = NULL, SchoolYearIDCohort = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentRank", body = list(DataObject = body), searchFields = append("StudentRankID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentRank
	#'
	#' This function modifies a StudentRank
	#' @param fieldNames The field values to give the modified StudentRank. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentRank
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentRank <- function(StudentRankID, StudentID = NULL, RankRunHistoryID = NULL, Value = NULL, NumberInRank = NULL, NumberOutOf = NULL, UseOverride = NULL, IsProspectiveRank = NULL, IsManualRank = NULL, SchoolYearIDCohort = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentRank", objectId = StudentRankID, body = list(DataObject = body), searchFields = append("StudentRankID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List RankMethods
	#'
	#' This function returns a dataframe or json object of RankMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RankMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RankMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RankMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of RankMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listRankMethods <- function(searchConditionsList = NULL, RankMethodID = F, Name = F, GPABucketID = F, GPAMethodID = F, ValueRangeType = F, ValueRangeLow = F, ValueRangeHigh = F, IncludeNonRankedStudents = F, DistrictGroupKey = F, IsActive = F, GPAType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "RankMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a RankMethod
	#'
	#' This function returns a dataframe or json object of a RankMethod
	#' @param RankMethodID The ID of the RankMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RankMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RankMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RankMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of RankMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getRankMethod <- function(RankMethodID, Name = F, GPABucketID = F, GPAMethodID = F, ValueRangeType = F, ValueRangeLow = F, ValueRangeHigh = F, IncludeNonRankedStudents = F, DistrictGroupKey = F, IsActive = F, GPAType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "RankMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "RankMethod", objectId = RankMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a RankMethod
	#'
	#' This function deletes a RankMethod
	#' @param RankMethodID The ID of the RankMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The RankMethodID of the deleted RankMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteRankMethod <- function(RankMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "RankMethod", objectId = RankMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a RankMethod
	#'
	#' This function creates a RankMethod
	#' @param fieldNames The field values to give the created RankMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created RankMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createRankMethod <- function(Name = NULL, GPABucketID = NULL, GPAMethodID = NULL, ValueRangeType = NULL, ValueRangeLow = NULL, ValueRangeHigh = NULL, IncludeNonRankedStudents = NULL, DistrictGroupKey = NULL, IsActive = NULL, GPAType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "RankMethod", body = list(DataObject = body), searchFields = append("RankMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a RankMethod
	#'
	#' This function modifies a RankMethod
	#' @param fieldNames The field values to give the modified RankMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified RankMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyRankMethod <- function(RankMethodID, Name = NULL, GPABucketID = NULL, GPAMethodID = NULL, ValueRangeType = NULL, ValueRangeLow = NULL, ValueRangeHigh = NULL, IncludeNonRankedStudents = NULL, DistrictGroupKey = NULL, IsActive = NULL, GPAType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "RankMethod", objectId = RankMethodID, body = list(DataObject = body), searchFields = append("RankMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradingPeriodErrors
	#'
	#' This function returns a dataframe or json object of TempGradingPeriodErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriodErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriodErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriodError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradingPeriodErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradingPeriodErrors <- function(searchConditionsList = NULL, TempGradingPeriodErrorID = F, CourseDescription = F, SectionNumber = F, TeacherName = F, AssignmentName = F, Category = F, DueDate = F, Error = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Subject = F, AcademicStandard = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradingPeriodError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradingPeriodError
	#'
	#' This function returns a dataframe or json object of a TempGradingPeriodError
	#' @param TempGradingPeriodErrorID The ID of the TempGradingPeriodError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriodError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriodError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriodError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradingPeriodError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradingPeriodError <- function(TempGradingPeriodErrorID, CourseDescription = F, SectionNumber = F, TeacherName = F, AssignmentName = F, Category = F, DueDate = F, Error = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Subject = F, AcademicStandard = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradingPeriodErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradingPeriodError", objectId = TempGradingPeriodErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradingPeriodError
	#'
	#' This function deletes a TempGradingPeriodError
	#' @param TempGradingPeriodErrorID The ID of the TempGradingPeriodError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradingPeriodErrorID of the deleted TempGradingPeriodError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradingPeriodError <- function(TempGradingPeriodErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradingPeriodError", objectId = TempGradingPeriodErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradingPeriodError
	#'
	#' This function creates a TempGradingPeriodError
	#' @param fieldNames The field values to give the created TempGradingPeriodError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradingPeriodError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradingPeriodError <- function(CourseDescription = NULL, SectionNumber = NULL, TeacherName = NULL, AssignmentName = NULL, Category = NULL, DueDate = NULL, Error = NULL, Subject = NULL, AcademicStandard = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradingPeriodError", body = list(DataObject = body), searchFields = append("TempGradingPeriodErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradingPeriodError
	#'
	#' This function modifies a TempGradingPeriodError
	#' @param fieldNames The field values to give the modified TempGradingPeriodError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradingPeriodError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradingPeriodError <- function(TempGradingPeriodErrorID, CourseDescription = NULL, SectionNumber = NULL, TeacherName = NULL, AssignmentName = NULL, Category = NULL, DueDate = NULL, Error = NULL, Subject = NULL, AcademicStandard = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradingPeriodError", objectId = TempGradingPeriodErrorID, body = list(DataObject = body), searchFields = append("TempGradingPeriodErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentSectionFailedUpdates
	#'
	#' This function returns a dataframe or json object of TempStudentSectionFailedUpdates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSectionFailedUpdates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSectionFailedUpdates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSectionFailedUpdate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentSectionFailedUpdates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentSectionFailedUpdates <- function(searchConditionsList = NULL, TempStudentSectionFailedUpdateID = F, StudentNameLFM = F, Note = F, Course = F, Section = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentSectionFailedUpdate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentSectionFailedUpdate
	#'
	#' This function returns a dataframe or json object of a TempStudentSectionFailedUpdate
	#' @param TempStudentSectionFailedUpdateID The ID of the TempStudentSectionFailedUpdate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSectionFailedUpdate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSectionFailedUpdate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSectionFailedUpdate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentSectionFailedUpdate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentSectionFailedUpdate <- function(TempStudentSectionFailedUpdateID, StudentNameLFM = F, Note = F, Course = F, Section = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentSectionFailedUpdateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentSectionFailedUpdate", objectId = TempStudentSectionFailedUpdateID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentSectionFailedUpdate
	#'
	#' This function deletes a TempStudentSectionFailedUpdate
	#' @param TempStudentSectionFailedUpdateID The ID of the TempStudentSectionFailedUpdate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentSectionFailedUpdateID of the deleted TempStudentSectionFailedUpdate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentSectionFailedUpdate <- function(TempStudentSectionFailedUpdateID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentSectionFailedUpdate", objectId = TempStudentSectionFailedUpdateID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentSectionFailedUpdate
	#'
	#' This function creates a TempStudentSectionFailedUpdate
	#' @param fieldNames The field values to give the created TempStudentSectionFailedUpdate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentSectionFailedUpdate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentSectionFailedUpdate <- function(StudentNameLFM = NULL, Note = NULL, Course = NULL, Section = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentSectionFailedUpdate", body = list(DataObject = body), searchFields = append("TempStudentSectionFailedUpdateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentSectionFailedUpdate
	#'
	#' This function modifies a TempStudentSectionFailedUpdate
	#' @param fieldNames The field values to give the modified TempStudentSectionFailedUpdate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentSectionFailedUpdate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentSectionFailedUpdate <- function(TempStudentSectionFailedUpdateID, StudentNameLFM = NULL, Note = NULL, Course = NULL, Section = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentSectionFailedUpdate", objectId = TempStudentSectionFailedUpdateID, body = list(DataObject = body), searchFields = append("TempStudentSectionFailedUpdateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradingPeriods
	#'
	#' This function returns a dataframe or json object of TempGradingPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradingPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradingPeriods <- function(searchConditionsList = NULL, TempGradingPeriodID = F, GradingPeriodSetID = F, GradingPeriodID = F, GradingPeriodSetCode = F, SectionLengthCode = F, Number = F, Description = F, StartDate = F, EndDate = F, OriginalStartDate = F, OriginalEndDate = F, IsUpdated = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingPeriodSetCodeDescription = F, SectionLengthID = F, SectionLengthCodeDescription = F, ProcessAction = F, SectionLengthStartDate = F, SectionLengthEndDate = F, CourseLengthCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradingPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradingPeriod
	#'
	#' This function returns a dataframe or json object of a TempGradingPeriod
	#' @param TempGradingPeriodID The ID of the TempGradingPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradingPeriod <- function(TempGradingPeriodID, GradingPeriodSetID = F, GradingPeriodID = F, GradingPeriodSetCode = F, SectionLengthCode = F, Number = F, Description = F, StartDate = F, EndDate = F, OriginalStartDate = F, OriginalEndDate = F, IsUpdated = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingPeriodSetCodeDescription = F, SectionLengthID = F, SectionLengthCodeDescription = F, ProcessAction = F, SectionLengthStartDate = F, SectionLengthEndDate = F, CourseLengthCode = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradingPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradingPeriod", objectId = TempGradingPeriodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradingPeriod
	#'
	#' This function deletes a TempGradingPeriod
	#' @param TempGradingPeriodID The ID of the TempGradingPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradingPeriodID of the deleted TempGradingPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradingPeriod <- function(TempGradingPeriodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradingPeriod", objectId = TempGradingPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradingPeriod
	#'
	#' This function creates a TempGradingPeriod
	#' @param fieldNames The field values to give the created TempGradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradingPeriod <- function(GradingPeriodSetID = NULL, GradingPeriodID = NULL, GradingPeriodSetCode = NULL, SectionLengthCode = NULL, Number = NULL, Description = NULL, StartDate = NULL, EndDate = NULL, OriginalStartDate = NULL, OriginalEndDate = NULL, IsUpdated = NULL, GradingPeriodSetCodeDescription = NULL, SectionLengthID = NULL, SectionLengthCodeDescription = NULL, ProcessAction = NULL, SectionLengthStartDate = NULL, SectionLengthEndDate = NULL, CourseLengthCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradingPeriod", body = list(DataObject = body), searchFields = append("TempGradingPeriodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradingPeriod
	#'
	#' This function modifies a TempGradingPeriod
	#' @param fieldNames The field values to give the modified TempGradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradingPeriod <- function(TempGradingPeriodID, GradingPeriodSetID = NULL, GradingPeriodID = NULL, GradingPeriodSetCode = NULL, SectionLengthCode = NULL, Number = NULL, Description = NULL, StartDate = NULL, EndDate = NULL, OriginalStartDate = NULL, OriginalEndDate = NULL, IsUpdated = NULL, GradingPeriodSetCodeDescription = NULL, SectionLengthID = NULL, SectionLengthCodeDescription = NULL, ProcessAction = NULL, SectionLengthStartDate = NULL, SectionLengthEndDate = NULL, CourseLengthCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradingPeriod", objectId = TempGradingPeriodID, body = list(DataObject = body), searchFields = append("TempGradingPeriodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentSectionGPABucketGroups
	#'
	#' This function returns a dataframe or json object of TempStudentSectionGPABucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSectionGPABucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSectionGPABucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSectionGPABucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentSectionGPABucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentSectionGPABucketGroups <- function(searchConditionsList = NULL, TempStudentSectionGPABucketGroupID = F, StudentSectionID = F, StudentGPABucketGroupID = F, StudentGPABucketGroupIsFromTemp = F, StudentID = F, GPABucketID = F, GPAMethodID = F, EntityID = F, SchoolYearID = F, IsCumulative = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentSectionGPABucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentSectionGPABucketGroup
	#'
	#' This function returns a dataframe or json object of a TempStudentSectionGPABucketGroup
	#' @param TempStudentSectionGPABucketGroupID The ID of the TempStudentSectionGPABucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSectionGPABucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSectionGPABucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSectionGPABucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentSectionGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentSectionGPABucketGroup <- function(TempStudentSectionGPABucketGroupID, StudentSectionID = F, StudentGPABucketGroupID = F, StudentGPABucketGroupIsFromTemp = F, StudentID = F, GPABucketID = F, GPAMethodID = F, EntityID = F, SchoolYearID = F, IsCumulative = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentSectionGPABucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentSectionGPABucketGroup", objectId = TempStudentSectionGPABucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentSectionGPABucketGroup
	#'
	#' This function deletes a TempStudentSectionGPABucketGroup
	#' @param TempStudentSectionGPABucketGroupID The ID of the TempStudentSectionGPABucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentSectionGPABucketGroupID of the deleted TempStudentSectionGPABucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentSectionGPABucketGroup <- function(TempStudentSectionGPABucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentSectionGPABucketGroup", objectId = TempStudentSectionGPABucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentSectionGPABucketGroup
	#'
	#' This function creates a TempStudentSectionGPABucketGroup
	#' @param fieldNames The field values to give the created TempStudentSectionGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentSectionGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentSectionGPABucketGroup <- function(StudentSectionID = NULL, StudentGPABucketGroupID = NULL, StudentGPABucketGroupIsFromTemp = NULL, StudentID = NULL, GPABucketID = NULL, GPAMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, IsCumulative = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentSectionGPABucketGroup", body = list(DataObject = body), searchFields = append("TempStudentSectionGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentSectionGPABucketGroup
	#'
	#' This function modifies a TempStudentSectionGPABucketGroup
	#' @param fieldNames The field values to give the modified TempStudentSectionGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentSectionGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentSectionGPABucketGroup <- function(TempStudentSectionGPABucketGroupID, StudentSectionID = NULL, StudentGPABucketGroupID = NULL, StudentGPABucketGroupIsFromTemp = NULL, StudentID = NULL, GPABucketID = NULL, GPAMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, IsCumulative = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentSectionGPABucketGroup", objectId = TempStudentSectionGPABucketGroupID, body = list(DataObject = body), searchFields = append("TempStudentSectionGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionGPABucketGroups
	#'
	#' This function returns a dataframe or json object of StudentSectionGPABucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionGPABucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionGPABucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionGPABucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentSectionGPABucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionGPABucketGroups <- function(searchConditionsList = NULL, StudentSectionGPABucketGroupID = F, StudentSectionID = F, StudentGPABucketGroupID = F, UseGPATotalOverride = F, TotalGPACredits = F, TotalGPAPoints = F, TotalAddOnPoints = F, EntityID = F, SchoolYearID = F, GPACredits = F, GPAPoints = F, BonusGPA = F, FactorBasedGPACountTotal = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ElectiveGPACredits = F, ElectiveGPAPoints = F, ElectiveBonusGPA = F, RequiredGPACredits = F, RequiredGPAPoints = F, RequiredBonusGPA = F, IsGPAElective = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentSectionGPABucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionGPABucketGroup
	#'
	#' This function returns a dataframe or json object of a StudentSectionGPABucketGroup
	#' @param StudentSectionGPABucketGroupID The ID of the StudentSectionGPABucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionGPABucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionGPABucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionGPABucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentSectionGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionGPABucketGroup <- function(StudentSectionGPABucketGroupID, StudentSectionID = F, StudentGPABucketGroupID = F, UseGPATotalOverride = F, TotalGPACredits = F, TotalGPAPoints = F, TotalAddOnPoints = F, EntityID = F, SchoolYearID = F, GPACredits = F, GPAPoints = F, BonusGPA = F, FactorBasedGPACountTotal = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ElectiveGPACredits = F, ElectiveGPAPoints = F, ElectiveBonusGPA = F, RequiredGPACredits = F, RequiredGPAPoints = F, RequiredBonusGPA = F, IsGPAElective = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionGPABucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentSectionGPABucketGroup", objectId = StudentSectionGPABucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionGPABucketGroup
	#'
	#' This function deletes a StudentSectionGPABucketGroup
	#' @param StudentSectionGPABucketGroupID The ID of the StudentSectionGPABucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentSectionGPABucketGroupID of the deleted StudentSectionGPABucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionGPABucketGroup <- function(StudentSectionGPABucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentSectionGPABucketGroup", objectId = StudentSectionGPABucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionGPABucketGroup
	#'
	#' This function creates a StudentSectionGPABucketGroup
	#' @param fieldNames The field values to give the created StudentSectionGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentSectionGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionGPABucketGroup <- function(StudentSectionID = NULL, StudentGPABucketGroupID = NULL, UseGPATotalOverride = NULL, TotalGPACredits = NULL, TotalGPAPoints = NULL, TotalAddOnPoints = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentSectionGPABucketGroup", body = list(DataObject = body), searchFields = append("StudentSectionGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionGPABucketGroup
	#'
	#' This function modifies a StudentSectionGPABucketGroup
	#' @param fieldNames The field values to give the modified StudentSectionGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentSectionGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionGPABucketGroup <- function(StudentSectionGPABucketGroupID, StudentSectionID = NULL, StudentGPABucketGroupID = NULL, UseGPATotalOverride = NULL, TotalGPACredits = NULL, TotalGPAPoints = NULL, TotalAddOnPoints = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentSectionGPABucketGroup", objectId = StudentSectionGPABucketGroupID, body = list(DataObject = body), searchFields = append("StudentSectionGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentGPABuckets
	#'
	#' This function returns a dataframe or json object of TempStudentGPABuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGPABuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGPABuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGPABucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentGPABuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentGPABuckets <- function(searchConditionsList = NULL, TempStudentGPABucketID = F, StudentGradeBucketID = F, GPABucketID = F, GPAMethodID = F, StudentSectionGPABucketGroupID = F, StudentGPABucketGroupID = F, StudentSectionID = F, StudentSectionGPABucketGroupIsFromTemp = F, StudentGPABucketGroupIsFromTemp = F, StudentGradeBucketIsFromTemp = F, GradingPeriodGradeBucketID = F, StudentID = F, SchoolYearID = F, SchoolYearIDForPostSave = F, StudentGPABucketID = F, SchoolYearDescription = F, StudentNameLFM = F, StudentNumber = F, CourseName = F, GPABucketCodeDescription = F, GPAMethodCodeDescription = F, GradeBucketCodeDescription = F, CurrentGPACredits = F, CurrentGPAPoints = F, CurrentBonusGPAPoints = F, CorrectGPACredits = F, CorrectGPAPoints = F, CorrectBonusGPAPoints = F, IsDelete = F, EntityID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsGradReqRankGPA = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentGPABucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentGPABucket
	#'
	#' This function returns a dataframe or json object of a TempStudentGPABucket
	#' @param TempStudentGPABucketID The ID of the TempStudentGPABucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGPABucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGPABucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGPABucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentGPABucket <- function(TempStudentGPABucketID, StudentGradeBucketID = F, GPABucketID = F, GPAMethodID = F, StudentSectionGPABucketGroupID = F, StudentGPABucketGroupID = F, StudentSectionID = F, StudentSectionGPABucketGroupIsFromTemp = F, StudentGPABucketGroupIsFromTemp = F, StudentGradeBucketIsFromTemp = F, GradingPeriodGradeBucketID = F, StudentID = F, SchoolYearID = F, SchoolYearIDForPostSave = F, StudentGPABucketID = F, SchoolYearDescription = F, StudentNameLFM = F, StudentNumber = F, CourseName = F, GPABucketCodeDescription = F, GPAMethodCodeDescription = F, GradeBucketCodeDescription = F, CurrentGPACredits = F, CurrentGPAPoints = F, CurrentBonusGPAPoints = F, CorrectGPACredits = F, CorrectGPAPoints = F, CorrectBonusGPAPoints = F, IsDelete = F, EntityID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsGradReqRankGPA = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentGPABucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentGPABucket", objectId = TempStudentGPABucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentGPABucket
	#'
	#' This function deletes a TempStudentGPABucket
	#' @param TempStudentGPABucketID The ID of the TempStudentGPABucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentGPABucketID of the deleted TempStudentGPABucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentGPABucket <- function(TempStudentGPABucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentGPABucket", objectId = TempStudentGPABucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentGPABucket
	#'
	#' This function creates a TempStudentGPABucket
	#' @param fieldNames The field values to give the created TempStudentGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentGPABucket <- function(StudentGradeBucketID = NULL, GPABucketID = NULL, GPAMethodID = NULL, StudentSectionGPABucketGroupID = NULL, StudentGPABucketGroupID = NULL, StudentSectionID = NULL, StudentSectionGPABucketGroupIsFromTemp = NULL, StudentGPABucketGroupIsFromTemp = NULL, StudentGradeBucketIsFromTemp = NULL, GradingPeriodGradeBucketID = NULL, StudentID = NULL, SchoolYearID = NULL, SchoolYearIDForPostSave = NULL, StudentGPABucketID = NULL, SchoolYearDescription = NULL, StudentNameLFM = NULL, StudentNumber = NULL, CourseName = NULL, GPABucketCodeDescription = NULL, GPAMethodCodeDescription = NULL, GradeBucketCodeDescription = NULL, CurrentGPACredits = NULL, CurrentGPAPoints = NULL, CurrentBonusGPAPoints = NULL, CorrectGPACredits = NULL, CorrectGPAPoints = NULL, CorrectBonusGPAPoints = NULL, IsDelete = NULL, EntityID = NULL, IsGradReqRankGPA = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentGPABucket", body = list(DataObject = body), searchFields = append("TempStudentGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentGPABucket
	#'
	#' This function modifies a TempStudentGPABucket
	#' @param fieldNames The field values to give the modified TempStudentGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentGPABucket <- function(TempStudentGPABucketID, StudentGradeBucketID = NULL, GPABucketID = NULL, GPAMethodID = NULL, StudentSectionGPABucketGroupID = NULL, StudentGPABucketGroupID = NULL, StudentSectionID = NULL, StudentSectionGPABucketGroupIsFromTemp = NULL, StudentGPABucketGroupIsFromTemp = NULL, StudentGradeBucketIsFromTemp = NULL, GradingPeriodGradeBucketID = NULL, StudentID = NULL, SchoolYearID = NULL, SchoolYearIDForPostSave = NULL, StudentGPABucketID = NULL, SchoolYearDescription = NULL, StudentNameLFM = NULL, StudentNumber = NULL, CourseName = NULL, GPABucketCodeDescription = NULL, GPAMethodCodeDescription = NULL, GradeBucketCodeDescription = NULL, CurrentGPACredits = NULL, CurrentGPAPoints = NULL, CurrentBonusGPAPoints = NULL, CorrectGPACredits = NULL, CorrectGPAPoints = NULL, CorrectBonusGPAPoints = NULL, IsDelete = NULL, EntityID = NULL, IsGradReqRankGPA = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentGPABucket", objectId = TempStudentGPABucketID, body = list(DataObject = body), searchFields = append("TempStudentGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentGPABucketGroups
	#'
	#' This function returns a dataframe or json object of TempStudentGPABucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGPABucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGPABucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGPABucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentGPABucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentGPABucketGroups <- function(searchConditionsList = NULL, TempStudentGPABucketGroupID = F, StudentID = F, GPABucketID = F, GPAMethodID = F, SchoolYearID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentGPABucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentGPABucketGroup
	#'
	#' This function returns a dataframe or json object of a TempStudentGPABucketGroup
	#' @param TempStudentGPABucketGroupID The ID of the TempStudentGPABucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGPABucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGPABucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGPABucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentGPABucketGroup <- function(TempStudentGPABucketGroupID, StudentID = F, GPABucketID = F, GPAMethodID = F, SchoolYearID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentGPABucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentGPABucketGroup", objectId = TempStudentGPABucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentGPABucketGroup
	#'
	#' This function deletes a TempStudentGPABucketGroup
	#' @param TempStudentGPABucketGroupID The ID of the TempStudentGPABucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentGPABucketGroupID of the deleted TempStudentGPABucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentGPABucketGroup <- function(TempStudentGPABucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentGPABucketGroup", objectId = TempStudentGPABucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentGPABucketGroup
	#'
	#' This function creates a TempStudentGPABucketGroup
	#' @param fieldNames The field values to give the created TempStudentGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentGPABucketGroup <- function(StudentID = NULL, GPABucketID = NULL, GPAMethodID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentGPABucketGroup", body = list(DataObject = body), searchFields = append("TempStudentGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentGPABucketGroup
	#'
	#' This function modifies a TempStudentGPABucketGroup
	#' @param fieldNames The field values to give the modified TempStudentGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentGPABucketGroup <- function(TempStudentGPABucketGroupID, StudentID = NULL, GPABucketID = NULL, GPAMethodID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentGPABucketGroup", objectId = TempStudentGPABucketGroupID, body = list(DataObject = body), searchFields = append("TempStudentGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentGradeBuckets
	#'
	#' This function returns a dataframe or json object of TempStudentGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentGradeBuckets <- function(searchConditionsList = NULL, TempStudentGradeBucketID = F, StudentGradeBucketID = F, StudentSectionID = F, SectionID = F, GradingPeriodGradeBucketID = F, GradeMarkID = F, PercentWithAdjustment = F, GradeMarkIDOverride = F, GradeMarkIDReverse = F, OldCode = F, NewCode = F, StudentName = F, StudentSectionName = F, OverallStatus = F, GradeBucketCode = F, OverallPercent = F, IsTransferBucket = F, DoNotPost = F, EntityID = F, SchoolYearID = F, IsTransferCourse = F, CompleteByTeacherCode = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentGradeBucket
	#'
	#' This function returns a dataframe or json object of a TempStudentGradeBucket
	#' @param TempStudentGradeBucketID The ID of the TempStudentGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentGradeBucket <- function(TempStudentGradeBucketID, StudentGradeBucketID = F, StudentSectionID = F, SectionID = F, GradingPeriodGradeBucketID = F, GradeMarkID = F, PercentWithAdjustment = F, GradeMarkIDOverride = F, GradeMarkIDReverse = F, OldCode = F, NewCode = F, StudentName = F, StudentSectionName = F, OverallStatus = F, GradeBucketCode = F, OverallPercent = F, IsTransferBucket = F, DoNotPost = F, EntityID = F, SchoolYearID = F, IsTransferCourse = F, CompleteByTeacherCode = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentGradeBucket", objectId = TempStudentGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentGradeBucket
	#'
	#' This function deletes a TempStudentGradeBucket
	#' @param TempStudentGradeBucketID The ID of the TempStudentGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentGradeBucketID of the deleted TempStudentGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentGradeBucket <- function(TempStudentGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentGradeBucket", objectId = TempStudentGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentGradeBucket
	#'
	#' This function creates a TempStudentGradeBucket
	#' @param fieldNames The field values to give the created TempStudentGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentGradeBucket <- function(StudentGradeBucketID = NULL, StudentSectionID = NULL, SectionID = NULL, GradingPeriodGradeBucketID = NULL, GradeMarkID = NULL, PercentWithAdjustment = NULL, GradeMarkIDOverride = NULL, GradeMarkIDReverse = NULL, OldCode = NULL, NewCode = NULL, StudentName = NULL, StudentSectionName = NULL, OverallStatus = NULL, GradeBucketCode = NULL, OverallPercent = NULL, IsTransferBucket = NULL, DoNotPost = NULL, EntityID = NULL, SchoolYearID = NULL, IsTransferCourse = NULL, CompleteByTeacherCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentGradeBucket", body = list(DataObject = body), searchFields = append("TempStudentGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentGradeBucket
	#'
	#' This function modifies a TempStudentGradeBucket
	#' @param fieldNames The field values to give the modified TempStudentGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentGradeBucket <- function(TempStudentGradeBucketID, StudentGradeBucketID = NULL, StudentSectionID = NULL, SectionID = NULL, GradingPeriodGradeBucketID = NULL, GradeMarkID = NULL, PercentWithAdjustment = NULL, GradeMarkIDOverride = NULL, GradeMarkIDReverse = NULL, OldCode = NULL, NewCode = NULL, StudentName = NULL, StudentSectionName = NULL, OverallStatus = NULL, GradeBucketCode = NULL, OverallPercent = NULL, IsTransferBucket = NULL, DoNotPost = NULL, EntityID = NULL, SchoolYearID = NULL, IsTransferCourse = NULL, CompleteByTeacherCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentGradeBucket", objectId = TempStudentGradeBucketID, body = list(DataObject = body), searchFields = append("TempStudentGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingTempStudentSections
	#'
	#' This function returns a dataframe or json object of GradingTempStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingTempStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingTempStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingTempStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingTempStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingTempStudentSections <- function(searchConditionsList = NULL, TempStudentSectionID = F, StudentNameLFM = F, StudentNumber = F, Course = F, SectionCode = F, StudentSectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingTempStudentSection
	#'
	#' This function returns a dataframe or json object of a GradingTempStudentSection
	#' @param GradingTempStudentSectionID The ID of the GradingTempStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingTempStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingTempStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingTempStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingTempStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingTempStudentSection <- function(GradingTempStudentSectionID, TempStudentSectionID = F, StudentNameLFM = F, StudentNumber = F, Course = F, SectionCode = F, StudentSectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingTempStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentSection", objectId = GradingTempStudentSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingTempStudentSection
	#'
	#' This function deletes a GradingTempStudentSection
	#' @param GradingTempStudentSectionID The ID of the GradingTempStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingTempStudentSectionID of the deleted GradingTempStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingTempStudentSection <- function(GradingTempStudentSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentSection", objectId = GradingTempStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingTempStudentSection
	#'
	#' This function creates a GradingTempStudentSection
	#' @param fieldNames The field values to give the created GradingTempStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingTempStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingTempStudentSection <- function(StudentNameLFM = NULL, StudentNumber = NULL, Course = NULL, SectionCode = NULL, StudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentSection", body = list(DataObject = body), searchFields = append("TempStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingTempStudentSection
	#'
	#' This function modifies a GradingTempStudentSection
	#' @param fieldNames The field values to give the modified GradingTempStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingTempStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingTempStudentSection <- function(TempStudentSectionID, StudentNameLFM = NULL, StudentNumber = NULL, Course = NULL, SectionCode = NULL, StudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentSection", objectId = TempStudentSectionID, body = list(DataObject = body), searchFields = append("TempStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingConfigDistrictGroups
	#'
	#' This function returns a dataframe or json object of GradingConfigDistrictGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigDistrictGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigDistrictGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigDistrictGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingConfigDistrictGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingConfigDistrictGroups <- function(searchConditionsList = NULL, ConfigDistrictGroupID = F, DistrictID = F, DistrictGroupKey = F, GPACalculationRoundingDecimals = F, FinalGPARoundingDecimals = F, EarnedCreditsRoundingDecimals = F, GPACalculationDecimalSetting = F, FinalGPADecimalSetting = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowAlternativeCreditCourses = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ConfigDistrictGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingConfigDistrictGroup
	#'
	#' This function returns a dataframe or json object of a GradingConfigDistrictGroup
	#' @param GradingConfigDistrictGroupID The ID of the GradingConfigDistrictGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigDistrictGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigDistrictGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigDistrictGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingConfigDistrictGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingConfigDistrictGroup <- function(GradingConfigDistrictGroupID, ConfigDistrictGroupID = F, DistrictID = F, DistrictGroupKey = F, GPACalculationRoundingDecimals = F, FinalGPARoundingDecimals = F, EarnedCreditsRoundingDecimals = F, GPACalculationDecimalSetting = F, FinalGPADecimalSetting = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowAlternativeCreditCourses = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingConfigDistrictGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ConfigDistrictGroup", objectId = GradingConfigDistrictGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingConfigDistrictGroup
	#'
	#' This function deletes a GradingConfigDistrictGroup
	#' @param GradingConfigDistrictGroupID The ID of the GradingConfigDistrictGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingConfigDistrictGroupID of the deleted GradingConfigDistrictGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingConfigDistrictGroup <- function(GradingConfigDistrictGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ConfigDistrictGroup", objectId = GradingConfigDistrictGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingConfigDistrictGroup
	#'
	#' This function creates a GradingConfigDistrictGroup
	#' @param fieldNames The field values to give the created GradingConfigDistrictGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingConfigDistrictGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingConfigDistrictGroup <- function(DistrictID = NULL, DistrictGroupKey = NULL, GPACalculationRoundingDecimals = NULL, FinalGPARoundingDecimals = NULL, EarnedCreditsRoundingDecimals = NULL, GPACalculationDecimalSetting = NULL, FinalGPADecimalSetting = NULL, AllowAlternativeCreditCourses = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ConfigDistrictGroup", body = list(DataObject = body), searchFields = append("ConfigDistrictGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingConfigDistrictGroup
	#'
	#' This function modifies a GradingConfigDistrictGroup
	#' @param fieldNames The field values to give the modified GradingConfigDistrictGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingConfigDistrictGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingConfigDistrictGroup <- function(ConfigDistrictGroupID, DistrictID = NULL, DistrictGroupKey = NULL, GPACalculationRoundingDecimals = NULL, FinalGPARoundingDecimals = NULL, EarnedCreditsRoundingDecimals = NULL, GPACalculationDecimalSetting = NULL, FinalGPADecimalSetting = NULL, AllowAlternativeCreditCourses = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ConfigDistrictGroup", objectId = ConfigDistrictGroupID, body = list(DataObject = body), searchFields = append("ConfigDistrictGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FactorBasedAddOns
	#'
	#' This function returns a dataframe or json object of FactorBasedAddOns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FactorBasedAddOns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FactorBasedAddOns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FactorBasedAddOn') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of FactorBasedAddOns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFactorBasedAddOns <- function(searchConditionsList = NULL, FactorBasedAddOnID = F, GPABucketEntityID = F, GradeReferenceID = F, Factor = F, GradingEndDateCutoffForCumulative = F, FactorBasedAddOnIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "FactorBasedAddOn", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FactorBasedAddOn
	#'
	#' This function returns a dataframe or json object of a FactorBasedAddOn
	#' @param FactorBasedAddOnID The ID of the FactorBasedAddOn to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FactorBasedAddOn. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FactorBasedAddOn.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FactorBasedAddOn') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of FactorBasedAddOn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFactorBasedAddOn <- function(FactorBasedAddOnID, GPABucketEntityID = F, GradeReferenceID = F, Factor = F, GradingEndDateCutoffForCumulative = F, FactorBasedAddOnIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FactorBasedAddOnID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "FactorBasedAddOn", objectId = FactorBasedAddOnID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FactorBasedAddOn
	#'
	#' This function deletes a FactorBasedAddOn
	#' @param FactorBasedAddOnID The ID of the FactorBasedAddOn to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The FactorBasedAddOnID of the deleted FactorBasedAddOn.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFactorBasedAddOn <- function(FactorBasedAddOnID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "FactorBasedAddOn", objectId = FactorBasedAddOnID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FactorBasedAddOn
	#'
	#' This function creates a FactorBasedAddOn
	#' @param fieldNames The field values to give the created FactorBasedAddOn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created FactorBasedAddOn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFactorBasedAddOn <- function(GPABucketEntityID = NULL, GradeReferenceID = NULL, Factor = NULL, GradingEndDateCutoffForCumulative = NULL, FactorBasedAddOnIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "FactorBasedAddOn", body = list(DataObject = body), searchFields = append("FactorBasedAddOnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FactorBasedAddOn
	#'
	#' This function modifies a FactorBasedAddOn
	#' @param fieldNames The field values to give the modified FactorBasedAddOn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified FactorBasedAddOn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFactorBasedAddOn <- function(FactorBasedAddOnID, GPABucketEntityID = NULL, GradeReferenceID = NULL, Factor = NULL, GradingEndDateCutoffForCumulative = NULL, FactorBasedAddOnIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "FactorBasedAddOn", objectId = FactorBasedAddOnID, body = list(DataObject = body), searchFields = append("FactorBasedAddOnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionLengthGPABuckets
	#'
	#' This function returns a dataframe or json object of SectionLengthGPABuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionLengthGPABuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionLengthGPABuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionLengthGPABucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of SectionLengthGPABuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionLengthGPABuckets <- function(searchConditionsList = NULL, SectionLengthGPABucketID = F, SectionLengthID = F, GPABucketEntityID = F, EntityGroupKey = F, IsUpToDate = F, SectionLengthGPABucketIDClonedFrom = F, Status = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "SectionLengthGPABucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionLengthGPABucket
	#'
	#' This function returns a dataframe or json object of a SectionLengthGPABucket
	#' @param SectionLengthGPABucketID The ID of the SectionLengthGPABucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionLengthGPABucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionLengthGPABucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionLengthGPABucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of SectionLengthGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionLengthGPABucket <- function(SectionLengthGPABucketID, SectionLengthID = F, GPABucketEntityID = F, EntityGroupKey = F, IsUpToDate = F, SectionLengthGPABucketIDClonedFrom = F, Status = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionLengthGPABucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "SectionLengthGPABucket", objectId = SectionLengthGPABucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionLengthGPABucket
	#'
	#' This function deletes a SectionLengthGPABucket
	#' @param SectionLengthGPABucketID The ID of the SectionLengthGPABucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The SectionLengthGPABucketID of the deleted SectionLengthGPABucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionLengthGPABucket <- function(SectionLengthGPABucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "SectionLengthGPABucket", objectId = SectionLengthGPABucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionLengthGPABucket
	#'
	#' This function creates a SectionLengthGPABucket
	#' @param fieldNames The field values to give the created SectionLengthGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created SectionLengthGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionLengthGPABucket <- function(SectionLengthID = NULL, GPABucketEntityID = NULL, EntityGroupKey = NULL, IsUpToDate = NULL, SectionLengthGPABucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "SectionLengthGPABucket", body = list(DataObject = body), searchFields = append("SectionLengthGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionLengthGPABucket
	#'
	#' This function modifies a SectionLengthGPABucket
	#' @param fieldNames The field values to give the modified SectionLengthGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified SectionLengthGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionLengthGPABucket <- function(SectionLengthGPABucketID, SectionLengthID = NULL, GPABucketEntityID = NULL, EntityGroupKey = NULL, IsUpToDate = NULL, SectionLengthGPABucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "SectionLengthGPABucket", objectId = SectionLengthGPABucketID, body = list(DataObject = body), searchFields = append("SectionLengthGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGPABucketGroups
	#'
	#' This function returns a dataframe or json object of StudentGPABucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGPABucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGPABucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGPABucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentGPABucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGPABucketGroups <- function(searchConditionsList = NULL, StudentGPABucketGroupID = F, StudentID = F, SchoolYearID = F, GPABucketID = F, GPAMethodID = F, GPACredits = F, GPACreditsWithoutRounding = F, GPAPoints = F, GPAPointsWithoutRounding = F, GPA = F, BonusGPA = F, BonusGPAWithoutRounding = F, GPAWithBonus = F, FactorBonusGPA = F, FactorBonusGPAWithoutRounding = F, GPAWithFactorBonus = F, CurrentDefaultDistrictID = F, FinalGPARoundingDecimals = F, GPACalculationRoundingDecimals = F, FailedCredits = F, EarnedCredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ElectiveBonusGPA = F, RequiredBonusGPA = F, ElectiveFactor = F, RequiredGPACredits = F, RequiredGPAPoints = F, ElectiveGPACredits = F, ElectiveGPAPoints = F, GradReqRankGPABreakdown = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentGPABucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGPABucketGroup
	#'
	#' This function returns a dataframe or json object of a StudentGPABucketGroup
	#' @param StudentGPABucketGroupID The ID of the StudentGPABucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGPABucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGPABucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGPABucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGPABucketGroup <- function(StudentGPABucketGroupID, StudentID = F, SchoolYearID = F, GPABucketID = F, GPAMethodID = F, GPACredits = F, GPACreditsWithoutRounding = F, GPAPoints = F, GPAPointsWithoutRounding = F, GPA = F, BonusGPA = F, BonusGPAWithoutRounding = F, GPAWithBonus = F, FactorBonusGPA = F, FactorBonusGPAWithoutRounding = F, GPAWithFactorBonus = F, CurrentDefaultDistrictID = F, FinalGPARoundingDecimals = F, GPACalculationRoundingDecimals = F, FailedCredits = F, EarnedCredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ElectiveBonusGPA = F, RequiredBonusGPA = F, ElectiveFactor = F, RequiredGPACredits = F, RequiredGPAPoints = F, ElectiveGPACredits = F, ElectiveGPAPoints = F, GradReqRankGPABreakdown = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGPABucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentGPABucketGroup", objectId = StudentGPABucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGPABucketGroup
	#'
	#' This function deletes a StudentGPABucketGroup
	#' @param StudentGPABucketGroupID The ID of the StudentGPABucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentGPABucketGroupID of the deleted StudentGPABucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGPABucketGroup <- function(StudentGPABucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentGPABucketGroup", objectId = StudentGPABucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGPABucketGroup
	#'
	#' This function creates a StudentGPABucketGroup
	#' @param fieldNames The field values to give the created StudentGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGPABucketGroup <- function(StudentID = NULL, SchoolYearID = NULL, GPABucketID = NULL, GPAMethodID = NULL, GradReqRankGPABreakdown = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentGPABucketGroup", body = list(DataObject = body), searchFields = append("StudentGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGPABucketGroup
	#'
	#' This function modifies a StudentGPABucketGroup
	#' @param fieldNames The field values to give the modified StudentGPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentGPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGPABucketGroup <- function(StudentGPABucketGroupID, StudentID = NULL, SchoolYearID = NULL, GPABucketID = NULL, GPAMethodID = NULL, GradReqRankGPABreakdown = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentGPABucketGroup", objectId = StudentGPABucketGroupID, body = list(DataObject = body), searchFields = append("StudentGPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketTypes
	#'
	#' This function returns a dataframe or json object of GPABucketTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketTypes <- function(searchConditionsList = NULL, GPABucketTypeID = F, Code = F, Description = F, DistrictID = F, DistrictGroupKey = F, IsCumulative = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketType
	#'
	#' This function returns a dataframe or json object of a GPABucketType
	#' @param GPABucketTypeID The ID of the GPABucketType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketType <- function(GPABucketTypeID, Code = F, Description = F, DistrictID = F, DistrictGroupKey = F, IsCumulative = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketType", objectId = GPABucketTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketType
	#'
	#' This function deletes a GPABucketType
	#' @param GPABucketTypeID The ID of the GPABucketType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketTypeID of the deleted GPABucketType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketType <- function(GPABucketTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketType", objectId = GPABucketTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketType
	#'
	#' This function creates a GPABucketType
	#' @param fieldNames The field values to give the created GPABucketType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketType <- function(Code = NULL, Description = NULL, DistrictID = NULL, DistrictGroupKey = NULL, IsCumulative = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketType", body = list(DataObject = body), searchFields = append("GPABucketTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketType
	#'
	#' This function modifies a GPABucketType
	#' @param fieldNames The field values to give the modified GPABucketType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketType <- function(GPABucketTypeID, Code = NULL, Description = NULL, DistrictID = NULL, DistrictGroupKey = NULL, IsCumulative = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketType", objectId = GPABucketTypeID, body = list(DataObject = body), searchFields = append("GPABucketTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketGroupSummaries
	#'
	#' This function returns a dataframe or json object of GPABucketGroupSummaries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroupSummaries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroupSummaries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroupSummary') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketGroupSummaries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketGroupSummaries <- function(searchConditionsList = NULL, GPABucketGroupSummaryID = F, EntityGroupKey = F, GradingPeriodSetID = F, SectionLengthID = F, GPABucketSetID = F, GPABucketGroupSummaryIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketGroupSummary", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketGroupSummary
	#'
	#' This function returns a dataframe or json object of a GPABucketGroupSummary
	#' @param GPABucketGroupSummaryID The ID of the GPABucketGroupSummary to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroupSummary. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroupSummary.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroupSummary') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketGroupSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketGroupSummary <- function(GPABucketGroupSummaryID, EntityGroupKey = F, GradingPeriodSetID = F, SectionLengthID = F, GPABucketSetID = F, GPABucketGroupSummaryIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketGroupSummaryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketGroupSummary", objectId = GPABucketGroupSummaryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketGroupSummary
	#'
	#' This function deletes a GPABucketGroupSummary
	#' @param GPABucketGroupSummaryID The ID of the GPABucketGroupSummary to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketGroupSummaryID of the deleted GPABucketGroupSummary.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketGroupSummary <- function(GPABucketGroupSummaryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketGroupSummary", objectId = GPABucketGroupSummaryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketGroupSummary
	#'
	#' This function creates a GPABucketGroupSummary
	#' @param fieldNames The field values to give the created GPABucketGroupSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketGroupSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketGroupSummary <- function(EntityGroupKey = NULL, GradingPeriodSetID = NULL, SectionLengthID = NULL, GPABucketSetID = NULL, GPABucketGroupSummaryIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketGroupSummary", body = list(DataObject = body), searchFields = append("GPABucketGroupSummaryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketGroupSummary
	#'
	#' This function modifies a GPABucketGroupSummary
	#' @param fieldNames The field values to give the modified GPABucketGroupSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketGroupSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketGroupSummary <- function(GPABucketGroupSummaryID, EntityGroupKey = NULL, GradingPeriodSetID = NULL, SectionLengthID = NULL, GPABucketSetID = NULL, GPABucketGroupSummaryIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketGroupSummary", objectId = GPABucketGroupSummaryID, body = list(DataObject = body), searchFields = append("GPABucketGroupSummaryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionGPAMethods
	#'
	#' This function returns a dataframe or json object of StudentSectionGPAMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionGPAMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionGPAMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionGPAMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentSectionGPAMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionGPAMethods <- function(searchConditionsList = NULL, StudentSectionGPAMethodID = F, StudentSectionID = F, GPAMethodEntityID = F, PointSetEntityID = F, GPACredits = F, UseOverride = F, CumulativeGPACredits = F, CumulativeGPAPoints = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DefaultGPACredits = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentSectionGPAMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionGPAMethod
	#'
	#' This function returns a dataframe or json object of a StudentSectionGPAMethod
	#' @param StudentSectionGPAMethodID The ID of the StudentSectionGPAMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionGPAMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionGPAMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionGPAMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentSectionGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionGPAMethod <- function(StudentSectionGPAMethodID, StudentSectionID = F, GPAMethodEntityID = F, PointSetEntityID = F, GPACredits = F, UseOverride = F, CumulativeGPACredits = F, CumulativeGPAPoints = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DefaultGPACredits = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionGPAMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentSectionGPAMethod", objectId = StudentSectionGPAMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionGPAMethod
	#'
	#' This function deletes a StudentSectionGPAMethod
	#' @param StudentSectionGPAMethodID The ID of the StudentSectionGPAMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentSectionGPAMethodID of the deleted StudentSectionGPAMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionGPAMethod <- function(StudentSectionGPAMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentSectionGPAMethod", objectId = StudentSectionGPAMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionGPAMethod
	#'
	#' This function creates a StudentSectionGPAMethod
	#' @param fieldNames The field values to give the created StudentSectionGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentSectionGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionGPAMethod <- function(StudentSectionID = NULL, GPAMethodEntityID = NULL, PointSetEntityID = NULL, GPACredits = NULL, UseOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentSectionGPAMethod", body = list(DataObject = body), searchFields = append("StudentSectionGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionGPAMethod
	#'
	#' This function modifies a StudentSectionGPAMethod
	#' @param fieldNames The field values to give the modified StudentSectionGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentSectionGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionGPAMethod <- function(StudentSectionGPAMethodID, StudentSectionID = NULL, GPAMethodEntityID = NULL, PointSetEntityID = NULL, GPACredits = NULL, UseOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentSectionGPAMethod", objectId = StudentSectionGPAMethodID, body = list(DataObject = body), searchFields = append("StudentSectionGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EarnedCreditsBucketGroupGradeBuckets
	#'
	#' This function returns a dataframe or json object of EarnedCreditsBucketGroupGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsBucketGroupGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsBucketGroupGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsBucketGroupGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of EarnedCreditsBucketGroupGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEarnedCreditsBucketGroupGradeBuckets <- function(searchConditionsList = NULL, EarnedCreditsBucketGroupGradeBucketID = F, GradeBucketID = F, EarnedCreditsBucketGroupID = F, Percent = F, EntityGroupKey = F, EarnedCreditsBucketGroupGradeBucketIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EarnedCreditsBucketGroupGradeBucket
	#'
	#' This function returns a dataframe or json object of an EarnedCreditsBucketGroupGradeBucket
	#' @param EarnedCreditsBucketGroupGradeBucketID The ID of the EarnedCreditsBucketGroupGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsBucketGroupGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsBucketGroupGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsBucketGroupGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of EarnedCreditsBucketGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEarnedCreditsBucketGroupGradeBucket <- function(EarnedCreditsBucketGroupGradeBucketID, GradeBucketID = F, EarnedCreditsBucketGroupID = F, Percent = F, EntityGroupKey = F, EarnedCreditsBucketGroupGradeBucketIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EarnedCreditsBucketGroupGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucket", objectId = EarnedCreditsBucketGroupGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EarnedCreditsBucketGroupGradeBucket
	#'
	#' This function deletes an EarnedCreditsBucketGroupGradeBucket
	#' @param EarnedCreditsBucketGroupGradeBucketID The ID of the EarnedCreditsBucketGroupGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The EarnedCreditsBucketGroupGradeBucketID of the deleted EarnedCreditsBucketGroupGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEarnedCreditsBucketGroupGradeBucket <- function(EarnedCreditsBucketGroupGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucket", objectId = EarnedCreditsBucketGroupGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EarnedCreditsBucketGroupGradeBucket
	#'
	#' This function creates an EarnedCreditsBucketGroupGradeBucket
	#' @param fieldNames The field values to give the created EarnedCreditsBucketGroupGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created EarnedCreditsBucketGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEarnedCreditsBucketGroupGradeBucket <- function(GradeBucketID = NULL, EarnedCreditsBucketGroupID = NULL, Percent = NULL, EntityGroupKey = NULL, EarnedCreditsBucketGroupGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucket", body = list(DataObject = body), searchFields = append("EarnedCreditsBucketGroupGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EarnedCreditsBucketGroupGradeBucket
	#'
	#' This function modifies an EarnedCreditsBucketGroupGradeBucket
	#' @param fieldNames The field values to give the modified EarnedCreditsBucketGroupGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified EarnedCreditsBucketGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEarnedCreditsBucketGroupGradeBucket <- function(EarnedCreditsBucketGroupGradeBucketID, GradeBucketID = NULL, EarnedCreditsBucketGroupID = NULL, Percent = NULL, EntityGroupKey = NULL, EarnedCreditsBucketGroupGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucket", objectId = EarnedCreditsBucketGroupGradeBucketID, body = list(DataObject = body), searchFields = append("EarnedCreditsBucketGroupGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EarnedCreditsBucketGroupGradeBucketStudentOverrides
	#'
	#' This function returns a dataframe or json object of EarnedCreditsBucketGroupGradeBucketStudentOverrides
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsBucketGroupGradeBucketStudentOverrides. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsBucketGroupGradeBucketStudentOverrides.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsBucketGroupGradeBucketStudentOverride') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of EarnedCreditsBucketGroupGradeBucketStudentOverrides
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEarnedCreditsBucketGroupGradeBucketStudentOverrides <- function(searchConditionsList = NULL, EarnedCreditsBucketGroupGradeBucketStudentOverrideID = F, EarnedCreditsBucketGroupGradeBucketID = F, StudentSectionID = F, UseOverride = F, Percent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucketStudentOverride", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#'
	#' This function returns a dataframe or json object of an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' @param EarnedCreditsBucketGroupGradeBucketStudentOverrideID The ID of the EarnedCreditsBucketGroupGradeBucketStudentOverride to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsBucketGroupGradeBucketStudentOverride. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsBucketGroupGradeBucketStudentOverride.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsBucketGroupGradeBucketStudentOverride') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEarnedCreditsBucketGroupGradeBucketStudentOverride <- function(EarnedCreditsBucketGroupGradeBucketStudentOverrideID, EarnedCreditsBucketGroupGradeBucketID = F, StudentSectionID = F, UseOverride = F, Percent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EarnedCreditsBucketGroupGradeBucketStudentOverrideID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucketStudentOverride", objectId = EarnedCreditsBucketGroupGradeBucketStudentOverrideID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#'
	#' This function deletes an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' @param EarnedCreditsBucketGroupGradeBucketStudentOverrideID The ID of the EarnedCreditsBucketGroupGradeBucketStudentOverride to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The EarnedCreditsBucketGroupGradeBucketStudentOverrideID of the deleted EarnedCreditsBucketGroupGradeBucketStudentOverride.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEarnedCreditsBucketGroupGradeBucketStudentOverride <- function(EarnedCreditsBucketGroupGradeBucketStudentOverrideID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucketStudentOverride", objectId = EarnedCreditsBucketGroupGradeBucketStudentOverrideID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#'
	#' This function creates an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' @param fieldNames The field values to give the created EarnedCreditsBucketGroupGradeBucketStudentOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEarnedCreditsBucketGroupGradeBucketStudentOverride <- function(EarnedCreditsBucketGroupGradeBucketID = NULL, StudentSectionID = NULL, UseOverride = NULL, Percent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucketStudentOverride", body = list(DataObject = body), searchFields = append("EarnedCreditsBucketGroupGradeBucketStudentOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#'
	#' This function modifies an EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' @param fieldNames The field values to give the modified EarnedCreditsBucketGroupGradeBucketStudentOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified EarnedCreditsBucketGroupGradeBucketStudentOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEarnedCreditsBucketGroupGradeBucketStudentOverride <- function(EarnedCreditsBucketGroupGradeBucketStudentOverrideID, EarnedCreditsBucketGroupGradeBucketID = NULL, StudentSectionID = NULL, UseOverride = NULL, Percent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroupGradeBucketStudentOverride", objectId = EarnedCreditsBucketGroupGradeBucketStudentOverrideID, body = list(DataObject = body), searchFields = append("EarnedCreditsBucketGroupGradeBucketStudentOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketGroupGradeBucketStudentOverrides
	#'
	#' This function returns a dataframe or json object of GPABucketGroupGradeBucketStudentOverrides
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroupGradeBucketStudentOverrides. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroupGradeBucketStudentOverrides.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroupGradeBucketStudentOverride') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketGroupGradeBucketStudentOverrides
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketGroupGradeBucketStudentOverrides <- function(searchConditionsList = NULL, GPABucketGroupGradeBucketStudentOverrideID = F, GPABucketGroupGradeBucketID = F, UseOverride = F, Percent = F, StudentSectionID = F, GradeRequiredForGPABucket = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketGroupGradeBucketStudentOverride", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketGroupGradeBucketStudentOverride
	#'
	#' This function returns a dataframe or json object of a GPABucketGroupGradeBucketStudentOverride
	#' @param GPABucketGroupGradeBucketStudentOverrideID The ID of the GPABucketGroupGradeBucketStudentOverride to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroupGradeBucketStudentOverride. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroupGradeBucketStudentOverride.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroupGradeBucketStudentOverride') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketGroupGradeBucketStudentOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketGroupGradeBucketStudentOverride <- function(GPABucketGroupGradeBucketStudentOverrideID, GPABucketGroupGradeBucketID = F, UseOverride = F, Percent = F, StudentSectionID = F, GradeRequiredForGPABucket = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketGroupGradeBucketStudentOverrideID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucketStudentOverride", objectId = GPABucketGroupGradeBucketStudentOverrideID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketGroupGradeBucketStudentOverride
	#'
	#' This function deletes a GPABucketGroupGradeBucketStudentOverride
	#' @param GPABucketGroupGradeBucketStudentOverrideID The ID of the GPABucketGroupGradeBucketStudentOverride to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketGroupGradeBucketStudentOverrideID of the deleted GPABucketGroupGradeBucketStudentOverride.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketGroupGradeBucketStudentOverride <- function(GPABucketGroupGradeBucketStudentOverrideID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucketStudentOverride", objectId = GPABucketGroupGradeBucketStudentOverrideID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketGroupGradeBucketStudentOverride
	#'
	#' This function creates a GPABucketGroupGradeBucketStudentOverride
	#' @param fieldNames The field values to give the created GPABucketGroupGradeBucketStudentOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketGroupGradeBucketStudentOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketGroupGradeBucketStudentOverride <- function(GPABucketGroupGradeBucketID = NULL, UseOverride = NULL, Percent = NULL, StudentSectionID = NULL, GradeRequiredForGPABucket = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucketStudentOverride", body = list(DataObject = body), searchFields = append("GPABucketGroupGradeBucketStudentOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketGroupGradeBucketStudentOverride
	#'
	#' This function modifies a GPABucketGroupGradeBucketStudentOverride
	#' @param fieldNames The field values to give the modified GPABucketGroupGradeBucketStudentOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketGroupGradeBucketStudentOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketGroupGradeBucketStudentOverride <- function(GPABucketGroupGradeBucketStudentOverrideID, GPABucketGroupGradeBucketID = NULL, UseOverride = NULL, Percent = NULL, StudentSectionID = NULL, GradeRequiredForGPABucket = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucketStudentOverride", objectId = GPABucketGroupGradeBucketStudentOverrideID, body = list(DataObject = body), searchFields = append("GPABucketGroupGradeBucketStudentOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGPABuckets
	#'
	#' This function returns a dataframe or json object of StudentGPABuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGPABuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGPABuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGPABucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentGPABuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGPABuckets <- function(searchConditionsList = NULL, StudentGPABucketID = F, StudentGradeBucketID = F, StudentSectionGPABucketGroupID = F, EntityID = F, SchoolYearID = F, GPACredits = F, GPAPoints = F, UseOverride = F, BonusGPAPoints = F, GPAWithBonus = F, HasAllGradesRequiredForGPACalculation = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradReqRankGPAStatus = F, PointsAndCreditsMultiplier = F, DisplayGPACredits = F, DisplayGPAPoints = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentGPABucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGPABucket
	#'
	#' This function returns a dataframe or json object of a StudentGPABucket
	#' @param StudentGPABucketID The ID of the StudentGPABucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGPABucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGPABucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGPABucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGPABucket <- function(StudentGPABucketID, StudentGradeBucketID = F, StudentSectionGPABucketGroupID = F, EntityID = F, SchoolYearID = F, GPACredits = F, GPAPoints = F, UseOverride = F, BonusGPAPoints = F, GPAWithBonus = F, HasAllGradesRequiredForGPACalculation = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradReqRankGPAStatus = F, PointsAndCreditsMultiplier = F, DisplayGPACredits = F, DisplayGPAPoints = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGPABucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentGPABucket", objectId = StudentGPABucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGPABucket
	#'
	#' This function deletes a StudentGPABucket
	#' @param StudentGPABucketID The ID of the StudentGPABucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentGPABucketID of the deleted StudentGPABucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGPABucket <- function(StudentGPABucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentGPABucket", objectId = StudentGPABucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGPABucket
	#'
	#' This function creates a StudentGPABucket
	#' @param fieldNames The field values to give the created StudentGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGPABucket <- function(StudentGradeBucketID = NULL, StudentSectionGPABucketGroupID = NULL, EntityID = NULL, SchoolYearID = NULL, GPACredits = NULL, GPAPoints = NULL, UseOverride = NULL, BonusGPAPoints = NULL, GradReqRankGPAStatus = NULL, PointsAndCreditsMultiplier = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentGPABucket", body = list(DataObject = body), searchFields = append("StudentGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGPABucket
	#'
	#' This function modifies a StudentGPABucket
	#' @param fieldNames The field values to give the modified StudentGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGPABucket <- function(StudentGPABucketID, StudentGradeBucketID = NULL, StudentSectionGPABucketGroupID = NULL, EntityID = NULL, SchoolYearID = NULL, GPACredits = NULL, GPAPoints = NULL, UseOverride = NULL, BonusGPAPoints = NULL, GradReqRankGPAStatus = NULL, PointsAndCreditsMultiplier = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentGPABucket", objectId = StudentGPABucketID, body = list(DataObject = body), searchFields = append("StudentGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketGroups
	#'
	#' This function returns a dataframe or json object of GPABucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketGroups <- function(searchConditionsList = NULL, GPABucketGroupID = F, EntityGroupKey = F, GPABucketID = F, GPABucketGroupSummaryID = F, GPABucketGroupIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketGroup
	#'
	#' This function returns a dataframe or json object of a GPABucketGroup
	#' @param GPABucketGroupID The ID of the GPABucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketGroup <- function(GPABucketGroupID, EntityGroupKey = F, GPABucketID = F, GPABucketGroupSummaryID = F, GPABucketGroupIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketGroup", objectId = GPABucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketGroup
	#'
	#' This function deletes a GPABucketGroup
	#' @param GPABucketGroupID The ID of the GPABucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketGroupID of the deleted GPABucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketGroup <- function(GPABucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketGroup", objectId = GPABucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketGroup
	#'
	#' This function creates a GPABucketGroup
	#' @param fieldNames The field values to give the created GPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketGroup <- function(EntityGroupKey = NULL, GPABucketID = NULL, GPABucketGroupSummaryID = NULL, GPABucketGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketGroup", body = list(DataObject = body), searchFields = append("GPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketGroup
	#'
	#' This function modifies a GPABucketGroup
	#' @param fieldNames The field values to give the modified GPABucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketGroup <- function(GPABucketGroupID, EntityGroupKey = NULL, GPABucketID = NULL, GPABucketGroupSummaryID = NULL, GPABucketGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketGroup", objectId = GPABucketGroupID, body = list(DataObject = body), searchFields = append("GPABucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketGroupGradeBuckets
	#'
	#' This function returns a dataframe or json object of GPABucketGroupGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroupGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroupGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroupGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketGroupGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketGroupGradeBuckets <- function(searchConditionsList = NULL, GPABucketGroupGradeBucketID = F, EntityGroupKey = F, GPABucketGroupID = F, GradeBucketID = F, Percent = F, GradeRequiredForGPABucket = F, GPABucketGroupGradeBucketIDClonedFrom = F, IsUpToDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketGroupGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketGroupGradeBucket
	#'
	#' This function returns a dataframe or json object of a GPABucketGroupGradeBucket
	#' @param GPABucketGroupGradeBucketID The ID of the GPABucketGroupGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketGroupGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketGroupGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketGroupGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketGroupGradeBucket <- function(GPABucketGroupGradeBucketID, EntityGroupKey = F, GPABucketGroupID = F, GradeBucketID = F, Percent = F, GradeRequiredForGPABucket = F, GPABucketGroupGradeBucketIDClonedFrom = F, IsUpToDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketGroupGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucket", objectId = GPABucketGroupGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketGroupGradeBucket
	#'
	#' This function deletes a GPABucketGroupGradeBucket
	#' @param GPABucketGroupGradeBucketID The ID of the GPABucketGroupGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketGroupGradeBucketID of the deleted GPABucketGroupGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketGroupGradeBucket <- function(GPABucketGroupGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucket", objectId = GPABucketGroupGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketGroupGradeBucket
	#'
	#' This function creates a GPABucketGroupGradeBucket
	#' @param fieldNames The field values to give the created GPABucketGroupGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketGroupGradeBucket <- function(EntityGroupKey = NULL, GPABucketGroupID = NULL, GradeBucketID = NULL, Percent = NULL, GradeRequiredForGPABucket = NULL, GPABucketGroupGradeBucketIDClonedFrom = NULL, IsUpToDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucket", body = list(DataObject = body), searchFields = append("GPABucketGroupGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketGroupGradeBucket
	#'
	#' This function modifies a GPABucketGroupGradeBucket
	#' @param fieldNames The field values to give the modified GPABucketGroupGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketGroupGradeBucket <- function(GPABucketGroupGradeBucketID, EntityGroupKey = NULL, GPABucketGroupID = NULL, GradeBucketID = NULL, Percent = NULL, GradeRequiredForGPABucket = NULL, GPABucketGroupGradeBucketIDClonedFrom = NULL, IsUpToDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketGroupGradeBucket", objectId = GPABucketGroupGradeBucketID, body = list(DataObject = body), searchFields = append("GPABucketGroupGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EarnedCreditsBucketGroups
	#'
	#' This function returns a dataframe or json object of EarnedCreditsBucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsBucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsBucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsBucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of EarnedCreditsBucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEarnedCreditsBucketGroups <- function(searchConditionsList = NULL, EarnedCreditsBucketGroupID = F, EntityGroupKey = F, GradingPeriodSetID = F, SectionLengthID = F, EarnedCreditsBucketGroupIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "EarnedCreditsBucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EarnedCreditsBucketGroup
	#'
	#' This function returns a dataframe or json object of an EarnedCreditsBucketGroup
	#' @param EarnedCreditsBucketGroupID The ID of the EarnedCreditsBucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsBucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsBucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsBucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of EarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEarnedCreditsBucketGroup <- function(EarnedCreditsBucketGroupID, EntityGroupKey = F, GradingPeriodSetID = F, SectionLengthID = F, EarnedCreditsBucketGroupIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EarnedCreditsBucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroup", objectId = EarnedCreditsBucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EarnedCreditsBucketGroup
	#'
	#' This function deletes an EarnedCreditsBucketGroup
	#' @param EarnedCreditsBucketGroupID The ID of the EarnedCreditsBucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The EarnedCreditsBucketGroupID of the deleted EarnedCreditsBucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEarnedCreditsBucketGroup <- function(EarnedCreditsBucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroup", objectId = EarnedCreditsBucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EarnedCreditsBucketGroup
	#'
	#' This function creates an EarnedCreditsBucketGroup
	#' @param fieldNames The field values to give the created EarnedCreditsBucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created EarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEarnedCreditsBucketGroup <- function(EntityGroupKey = NULL, GradingPeriodSetID = NULL, SectionLengthID = NULL, EarnedCreditsBucketGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroup", body = list(DataObject = body), searchFields = append("EarnedCreditsBucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EarnedCreditsBucketGroup
	#'
	#' This function modifies an EarnedCreditsBucketGroup
	#' @param fieldNames The field values to give the modified EarnedCreditsBucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified EarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEarnedCreditsBucketGroup <- function(EarnedCreditsBucketGroupID, EntityGroupKey = NULL, GradingPeriodSetID = NULL, SectionLengthID = NULL, EarnedCreditsBucketGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "EarnedCreditsBucketGroup", objectId = EarnedCreditsBucketGroupID, body = list(DataObject = body), searchFields = append("EarnedCreditsBucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeMarkPointSets
	#'
	#' This function returns a dataframe or json object of GradeMarkPointSets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeMarkPointSets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeMarkPointSets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeMarkPointSet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeMarkPointSets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeMarkPointSets <- function(searchConditionsList = NULL, GradeMarkPointSetID = F, EntityGroupKey = F, GPAMethodEntityID = F, GradeMarkID = F, PointSetEntityID = F, RegularPoints = F, AddOnPoints = F, GradeMarkPointSetIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeMarkPointSet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeMarkPointSet
	#'
	#' This function returns a dataframe or json object of a GradeMarkPointSet
	#' @param GradeMarkPointSetID The ID of the GradeMarkPointSet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeMarkPointSet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeMarkPointSet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeMarkPointSet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeMarkPointSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeMarkPointSet <- function(GradeMarkPointSetID, EntityGroupKey = F, GPAMethodEntityID = F, GradeMarkID = F, PointSetEntityID = F, RegularPoints = F, AddOnPoints = F, GradeMarkPointSetIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeMarkPointSetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeMarkPointSet", objectId = GradeMarkPointSetID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeMarkPointSet
	#'
	#' This function deletes a GradeMarkPointSet
	#' @param GradeMarkPointSetID The ID of the GradeMarkPointSet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeMarkPointSetID of the deleted GradeMarkPointSet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeMarkPointSet <- function(GradeMarkPointSetID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeMarkPointSet", objectId = GradeMarkPointSetID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeMarkPointSet
	#'
	#' This function creates a GradeMarkPointSet
	#' @param fieldNames The field values to give the created GradeMarkPointSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeMarkPointSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeMarkPointSet <- function(EntityGroupKey = NULL, GPAMethodEntityID = NULL, GradeMarkID = NULL, PointSetEntityID = NULL, RegularPoints = NULL, AddOnPoints = NULL, GradeMarkPointSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeMarkPointSet", body = list(DataObject = body), searchFields = append("GradeMarkPointSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeMarkPointSet
	#'
	#' This function modifies a GradeMarkPointSet
	#' @param fieldNames The field values to give the modified GradeMarkPointSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeMarkPointSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeMarkPointSet <- function(GradeMarkPointSetID, EntityGroupKey = NULL, GPAMethodEntityID = NULL, GradeMarkID = NULL, PointSetEntityID = NULL, RegularPoints = NULL, AddOnPoints = NULL, GradeMarkPointSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeMarkPointSet", objectId = GradeMarkPointSetID, body = list(DataObject = body), searchFields = append("GradeMarkPointSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentFreeFormCommentBuckets
	#'
	#' This function returns a dataframe or json object of StudentFreeFormCommentBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentFreeFormCommentBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentFreeFormCommentBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentFreeFormCommentBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentFreeFormCommentBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentFreeFormCommentBuckets <- function(searchConditionsList = NULL, StudentFreeFormCommentBucketID = F, StudentSectionID = F, GradingPeriodID = F, Comment = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentFreeFormCommentBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentFreeFormCommentBucket
	#'
	#' This function returns a dataframe or json object of a StudentFreeFormCommentBucket
	#' @param StudentFreeFormCommentBucketID The ID of the StudentFreeFormCommentBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentFreeFormCommentBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentFreeFormCommentBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentFreeFormCommentBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentFreeFormCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentFreeFormCommentBucket <- function(StudentFreeFormCommentBucketID, StudentSectionID = F, GradingPeriodID = F, Comment = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentFreeFormCommentBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentFreeFormCommentBucket", objectId = StudentFreeFormCommentBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentFreeFormCommentBucket
	#'
	#' This function deletes a StudentFreeFormCommentBucket
	#' @param StudentFreeFormCommentBucketID The ID of the StudentFreeFormCommentBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentFreeFormCommentBucketID of the deleted StudentFreeFormCommentBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentFreeFormCommentBucket <- function(StudentFreeFormCommentBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentFreeFormCommentBucket", objectId = StudentFreeFormCommentBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentFreeFormCommentBucket
	#'
	#' This function creates a StudentFreeFormCommentBucket
	#' @param fieldNames The field values to give the created StudentFreeFormCommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentFreeFormCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentFreeFormCommentBucket <- function(StudentSectionID = NULL, GradingPeriodID = NULL, Comment = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentFreeFormCommentBucket", body = list(DataObject = body), searchFields = append("StudentFreeFormCommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentFreeFormCommentBucket
	#'
	#' This function modifies a StudentFreeFormCommentBucket
	#' @param fieldNames The field values to give the modified StudentFreeFormCommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentFreeFormCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentFreeFormCommentBucket <- function(StudentFreeFormCommentBucketID, StudentSectionID = NULL, GradingPeriodID = NULL, Comment = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentFreeFormCommentBucket", objectId = StudentFreeFormCommentBucketID, body = list(DataObject = body), searchFields = append("StudentFreeFormCommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPAMethods
	#'
	#' This function returns a dataframe or json object of GPAMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPAMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPAMethods <- function(searchConditionsList = NULL, GPAMethodID = F, DistrictGroupKey = F, DistrictID = F, GPABucketSetID = F, Name = F, Description = F, SortNumber = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, LockGradeMarkPoints = F, UseTotalElectiveCredits = F, TotalElectiveCreditsPossible = F, UseGradReqRankGPA = F, ExcludeTermTwoFinalYearGrade = F, GradReqRankGPARequiredCourseRule = F, CancelSubAreaCreditFromMiddleSchoolCredit = F, AllowFurtherAttemptsOfNonHighSchoolCourses = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPAMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPAMethod
	#'
	#' This function returns a dataframe or json object of a GPAMethod
	#' @param GPAMethodID The ID of the GPAMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPAMethod <- function(GPAMethodID, DistrictGroupKey = F, DistrictID = F, GPABucketSetID = F, Name = F, Description = F, SortNumber = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, LockGradeMarkPoints = F, UseTotalElectiveCredits = F, TotalElectiveCreditsPossible = F, UseGradReqRankGPA = F, ExcludeTermTwoFinalYearGrade = F, GradReqRankGPARequiredCourseRule = F, CancelSubAreaCreditFromMiddleSchoolCredit = F, AllowFurtherAttemptsOfNonHighSchoolCourses = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPAMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPAMethod", objectId = GPAMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPAMethod
	#'
	#' This function deletes a GPAMethod
	#' @param GPAMethodID The ID of the GPAMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPAMethodID of the deleted GPAMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPAMethod <- function(GPAMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPAMethod", objectId = GPAMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPAMethod
	#'
	#' This function creates a GPAMethod
	#' @param fieldNames The field values to give the created GPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPAMethod <- function(DistrictGroupKey = NULL, DistrictID = NULL, GPABucketSetID = NULL, Name = NULL, Description = NULL, SortNumber = NULL, LockGradeMarkPoints = NULL, UseTotalElectiveCredits = NULL, TotalElectiveCreditsPossible = NULL, UseGradReqRankGPA = NULL, ExcludeTermTwoFinalYearGrade = NULL, GradReqRankGPARequiredCourseRule = NULL, CancelSubAreaCreditFromMiddleSchoolCredit = NULL, AllowFurtherAttemptsOfNonHighSchoolCourses = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPAMethod", body = list(DataObject = body), searchFields = append("GPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPAMethod
	#'
	#' This function modifies a GPAMethod
	#' @param fieldNames The field values to give the modified GPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPAMethod <- function(GPAMethodID, DistrictGroupKey = NULL, DistrictID = NULL, GPABucketSetID = NULL, Name = NULL, Description = NULL, SortNumber = NULL, LockGradeMarkPoints = NULL, UseTotalElectiveCredits = NULL, TotalElectiveCreditsPossible = NULL, UseGradReqRankGPA = NULL, ExcludeTermTwoFinalYearGrade = NULL, GradReqRankGPARequiredCourseRule = NULL, CancelSubAreaCreditFromMiddleSchoolCredit = NULL, AllowFurtherAttemptsOfNonHighSchoolCourses = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPAMethod", objectId = GPAMethodID, body = list(DataObject = body), searchFields = append("GPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CommentBucketCourses
	#'
	#' This function returns a dataframe or json object of CommentBucketCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentBucketCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentBucketCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentBucketCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CommentBucketCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCommentBucketCourses <- function(searchConditionsList = NULL, CommentBucketCourseID = F, CommentBucketID = F, CourseID = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CommentBucketCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CommentBucketCourse
	#'
	#' This function returns a dataframe or json object of a CommentBucketCourse
	#' @param CommentBucketCourseID The ID of the CommentBucketCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentBucketCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentBucketCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentBucketCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CommentBucketCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCommentBucketCourse <- function(CommentBucketCourseID, CommentBucketID = F, CourseID = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CommentBucketCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CommentBucketCourse", objectId = CommentBucketCourseID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CommentBucketCourse
	#'
	#' This function deletes a CommentBucketCourse
	#' @param CommentBucketCourseID The ID of the CommentBucketCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CommentBucketCourseID of the deleted CommentBucketCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCommentBucketCourse <- function(CommentBucketCourseID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CommentBucketCourse", objectId = CommentBucketCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CommentBucketCourse
	#'
	#' This function creates a CommentBucketCourse
	#' @param fieldNames The field values to give the created CommentBucketCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CommentBucketCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCommentBucketCourse <- function(CommentBucketID = NULL, CourseID = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CommentBucketCourse", body = list(DataObject = body), searchFields = append("CommentBucketCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CommentBucketCourse
	#'
	#' This function modifies a CommentBucketCourse
	#' @param fieldNames The field values to give the modified CommentBucketCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CommentBucketCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCommentBucketCourse <- function(CommentBucketCourseID, CommentBucketID = NULL, CourseID = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CommentBucketCourse", objectId = CommentBucketCourseID, body = list(DataObject = body), searchFields = append("CommentBucketCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABuckets
	#'
	#' This function returns a dataframe or json object of GPABuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABuckets <- function(searchConditionsList = NULL, GPABucketID = F, DistrictGroupKey = F, Description = F, GPABucketTypeID = F, Name = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucket
	#'
	#' This function returns a dataframe or json object of a GPABucket
	#' @param GPABucketID The ID of the GPABucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucket <- function(GPABucketID, DistrictGroupKey = F, Description = F, GPABucketTypeID = F, Name = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucket", objectId = GPABucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucket
	#'
	#' This function deletes a GPABucket
	#' @param GPABucketID The ID of the GPABucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketID of the deleted GPABucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucket <- function(GPABucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucket", objectId = GPABucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucket
	#'
	#' This function creates a GPABucket
	#' @param fieldNames The field values to give the created GPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucket <- function(DistrictGroupKey = NULL, Description = NULL, GPABucketTypeID = NULL, Name = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucket", body = list(DataObject = body), searchFields = append("GPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucket
	#'
	#' This function modifies a GPABucket
	#' @param fieldNames The field values to give the modified GPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucket <- function(GPABucketID, DistrictGroupKey = NULL, Description = NULL, GPABucketTypeID = NULL, Name = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucket", objectId = GPABucketID, body = list(DataObject = body), searchFields = append("GPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketEntities
	#'
	#' This function returns a dataframe or json object of GPABucketEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketEntities <- function(searchConditionsList = NULL, GPABucketEntityID = F, EntityGroupKey = F, GPABucketID = F, EntityID = F, SchoolYearID = F, DisplayOrder = F, UseForFamilyAccess = F, FamilyAccessDisplayGradYearLow = F, FamilyAccessDisplayGradYearHigh = F, GPABucketEntityIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketEntity
	#'
	#' This function returns a dataframe or json object of a GPABucketEntity
	#' @param GPABucketEntityID The ID of the GPABucketEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketEntity <- function(GPABucketEntityID, EntityGroupKey = F, GPABucketID = F, EntityID = F, SchoolYearID = F, DisplayOrder = F, UseForFamilyAccess = F, FamilyAccessDisplayGradYearLow = F, FamilyAccessDisplayGradYearHigh = F, GPABucketEntityIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketEntity", objectId = GPABucketEntityID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketEntity
	#'
	#' This function deletes a GPABucketEntity
	#' @param GPABucketEntityID The ID of the GPABucketEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketEntityID of the deleted GPABucketEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketEntity <- function(GPABucketEntityID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketEntity", objectId = GPABucketEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketEntity
	#'
	#' This function creates a GPABucketEntity
	#' @param fieldNames The field values to give the created GPABucketEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketEntity <- function(EntityGroupKey = NULL, GPABucketID = NULL, EntityID = NULL, SchoolYearID = NULL, DisplayOrder = NULL, UseForFamilyAccess = NULL, FamilyAccessDisplayGradYearLow = NULL, FamilyAccessDisplayGradYearHigh = NULL, GPABucketEntityIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketEntity", body = list(DataObject = body), searchFields = append("GPABucketEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketEntity
	#'
	#' This function modifies a GPABucketEntity
	#' @param fieldNames The field values to give the modified GPABucketEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketEntity <- function(GPABucketEntityID, EntityGroupKey = NULL, GPABucketID = NULL, EntityID = NULL, SchoolYearID = NULL, DisplayOrder = NULL, UseForFamilyAccess = NULL, FamilyAccessDisplayGradYearLow = NULL, FamilyAccessDisplayGradYearHigh = NULL, GPABucketEntityIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketEntity", objectId = GPABucketEntityID, body = list(DataObject = body), searchFields = append("GPABucketEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPAMethodEntities
	#'
	#' This function returns a dataframe or json object of GPAMethodEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethodEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethodEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethodEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPAMethodEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPAMethodEntities <- function(searchConditionsList = NULL, GPAMethodEntityID = F, EntityGroupKey = F, GPAMethodID = F, EntityID = F, SchoolYearID = F, DisplayOrder = F, IsUpToDate = F, Status = F, GPAMethodEntityIDClonedFrom = F, UseForFamilyAccess = F, FamilyAccessDisplayGradYearLow = F, FamilyAccessDisplayGradYearHigh = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPAMethodEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPAMethodEntity
	#'
	#' This function returns a dataframe or json object of a GPAMethodEntity
	#' @param GPAMethodEntityID The ID of the GPAMethodEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethodEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethodEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethodEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPAMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPAMethodEntity <- function(GPAMethodEntityID, EntityGroupKey = F, GPAMethodID = F, EntityID = F, SchoolYearID = F, DisplayOrder = F, IsUpToDate = F, Status = F, GPAMethodEntityIDClonedFrom = F, UseForFamilyAccess = F, FamilyAccessDisplayGradYearLow = F, FamilyAccessDisplayGradYearHigh = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPAMethodEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPAMethodEntity", objectId = GPAMethodEntityID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPAMethodEntity
	#'
	#' This function deletes a GPAMethodEntity
	#' @param GPAMethodEntityID The ID of the GPAMethodEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPAMethodEntityID of the deleted GPAMethodEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPAMethodEntity <- function(GPAMethodEntityID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPAMethodEntity", objectId = GPAMethodEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPAMethodEntity
	#'
	#' This function creates a GPAMethodEntity
	#' @param fieldNames The field values to give the created GPAMethodEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPAMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPAMethodEntity <- function(EntityGroupKey = NULL, GPAMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, DisplayOrder = NULL, IsUpToDate = NULL, GPAMethodEntityIDClonedFrom = NULL, UseForFamilyAccess = NULL, FamilyAccessDisplayGradYearLow = NULL, FamilyAccessDisplayGradYearHigh = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPAMethodEntity", body = list(DataObject = body), searchFields = append("GPAMethodEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPAMethodEntity
	#'
	#' This function modifies a GPAMethodEntity
	#' @param fieldNames The field values to give the modified GPAMethodEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPAMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPAMethodEntity <- function(GPAMethodEntityID, EntityGroupKey = NULL, GPAMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, DisplayOrder = NULL, IsUpToDate = NULL, GPAMethodEntityIDClonedFrom = NULL, UseForFamilyAccess = NULL, FamilyAccessDisplayGradYearLow = NULL, FamilyAccessDisplayGradYearHigh = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPAMethodEntity", objectId = GPAMethodEntityID, body = list(DataObject = body), searchFields = append("GPAMethodEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List PointSetEntities
	#'
	#' This function returns a dataframe or json object of PointSetEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given PointSetEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the PointSetEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('PointSetEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of PointSetEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listPointSetEntities <- function(searchConditionsList = NULL, PointSetEntityID = F, EntityGroupKey = F, PointSetID = F, EntityID = F, SchoolYearID = F, DisplayOrder = F, IsDefault = F, ApplyFactorBasedAddOn = F, PointSetEntityIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsWeighted = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "PointSetEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a PointSetEntity
	#'
	#' This function returns a dataframe or json object of a PointSetEntity
	#' @param PointSetEntityID The ID of the PointSetEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given PointSetEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the PointSetEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('PointSetEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of PointSetEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getPointSetEntity <- function(PointSetEntityID, EntityGroupKey = F, PointSetID = F, EntityID = F, SchoolYearID = F, DisplayOrder = F, IsDefault = F, ApplyFactorBasedAddOn = F, PointSetEntityIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsWeighted = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "PointSetEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "PointSetEntity", objectId = PointSetEntityID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a PointSetEntity
	#'
	#' This function deletes a PointSetEntity
	#' @param PointSetEntityID The ID of the PointSetEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The PointSetEntityID of the deleted PointSetEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deletePointSetEntity <- function(PointSetEntityID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "PointSetEntity", objectId = PointSetEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a PointSetEntity
	#'
	#' This function creates a PointSetEntity
	#' @param fieldNames The field values to give the created PointSetEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created PointSetEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createPointSetEntity <- function(EntityGroupKey = NULL, PointSetID = NULL, EntityID = NULL, SchoolYearID = NULL, DisplayOrder = NULL, IsDefault = NULL, ApplyFactorBasedAddOn = NULL, PointSetEntityIDClonedFrom = NULL, IsWeighted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "PointSetEntity", body = list(DataObject = body), searchFields = append("PointSetEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a PointSetEntity
	#'
	#' This function modifies a PointSetEntity
	#' @param fieldNames The field values to give the modified PointSetEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified PointSetEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyPointSetEntity <- function(PointSetEntityID, EntityGroupKey = NULL, PointSetID = NULL, EntityID = NULL, SchoolYearID = NULL, DisplayOrder = NULL, IsDefault = NULL, ApplyFactorBasedAddOn = NULL, PointSetEntityIDClonedFrom = NULL, IsWeighted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "PointSetEntity", objectId = PointSetEntityID, body = list(DataObject = body), searchFields = append("PointSetEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPABucketSets
	#'
	#' This function returns a dataframe or json object of GPABucketSets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketSets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketSets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketSet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPABucketSets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPABucketSets <- function(searchConditionsList = NULL, GPABucketSetID = F, DistrictGroupKey = F, DistrictID = F, Name = F, Description = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPABucketSet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPABucketSet
	#'
	#' This function returns a dataframe or json object of a GPABucketSet
	#' @param GPABucketSetID The ID of the GPABucketSet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPABucketSet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPABucketSet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPABucketSet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPABucketSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPABucketSet <- function(GPABucketSetID, DistrictGroupKey = F, DistrictID = F, Name = F, Description = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPABucketSetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPABucketSet", objectId = GPABucketSetID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPABucketSet
	#'
	#' This function deletes a GPABucketSet
	#' @param GPABucketSetID The ID of the GPABucketSet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPABucketSetID of the deleted GPABucketSet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPABucketSet <- function(GPABucketSetID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPABucketSet", objectId = GPABucketSetID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPABucketSet
	#'
	#' This function creates a GPABucketSet
	#' @param fieldNames The field values to give the created GPABucketSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPABucketSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPABucketSet <- function(DistrictGroupKey = NULL, DistrictID = NULL, Name = NULL, Description = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPABucketSet", body = list(DataObject = body), searchFields = append("GPABucketSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPABucketSet
	#'
	#' This function modifies a GPABucketSet
	#' @param fieldNames The field values to give the modified GPABucketSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPABucketSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPABucketSet <- function(GPABucketSetID, DistrictGroupKey = NULL, DistrictID = NULL, Name = NULL, Description = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPABucketSet", objectId = GPABucketSetID, body = list(DataObject = body), searchFields = append("GPABucketSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List PointSets
	#'
	#' This function returns a dataframe or json object of PointSets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given PointSets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the PointSets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('PointSet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of PointSets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listPointSets <- function(searchConditionsList = NULL, PointSetID = F, DistrictGroupKey = F, DistrictID = F, Name = F, Description = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "PointSet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a PointSet
	#'
	#' This function returns a dataframe or json object of a PointSet
	#' @param PointSetID The ID of the PointSet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given PointSet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the PointSet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('PointSet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of PointSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getPointSet <- function(PointSetID, DistrictGroupKey = F, DistrictID = F, Name = F, Description = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "PointSetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "PointSet", objectId = PointSetID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a PointSet
	#'
	#' This function deletes a PointSet
	#' @param PointSetID The ID of the PointSet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The PointSetID of the deleted PointSet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deletePointSet <- function(PointSetID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "PointSet", objectId = PointSetID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a PointSet
	#'
	#' This function creates a PointSet
	#' @param fieldNames The field values to give the created PointSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created PointSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createPointSet <- function(DistrictGroupKey = NULL, DistrictID = NULL, Name = NULL, Description = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "PointSet", body = list(DataObject = body), searchFields = append("PointSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a PointSet
	#'
	#' This function modifies a PointSet
	#' @param fieldNames The field values to give the modified PointSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified PointSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyPointSet <- function(PointSetID, DistrictGroupKey = NULL, DistrictID = NULL, Name = NULL, Description = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "PointSet", objectId = PointSetID, body = list(DataObject = body), searchFields = append("PointSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CommentBuckets
	#'
	#' This function returns a dataframe or json object of CommentBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CommentBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCommentBuckets <- function(searchConditionsList = NULL, CommentBucketID = F, Name = F, DisplayOrder = F, IsLimitedByCourse = F, EntityGroupKey = F, GradingPeriodID = F, CommentSetID = F, CommentBucketIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CommentBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CommentBucket
	#'
	#' This function returns a dataframe or json object of a CommentBucket
	#' @param CommentBucketID The ID of the CommentBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCommentBucket <- function(CommentBucketID, Name = F, DisplayOrder = F, IsLimitedByCourse = F, EntityGroupKey = F, GradingPeriodID = F, CommentSetID = F, CommentBucketIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CommentBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CommentBucket", objectId = CommentBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CommentBucket
	#'
	#' This function deletes a CommentBucket
	#' @param CommentBucketID The ID of the CommentBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CommentBucketID of the deleted CommentBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCommentBucket <- function(CommentBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CommentBucket", objectId = CommentBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CommentBucket
	#'
	#' This function creates a CommentBucket
	#' @param fieldNames The field values to give the created CommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCommentBucket <- function(Name = NULL, DisplayOrder = NULL, IsLimitedByCourse = NULL, EntityGroupKey = NULL, GradingPeriodID = NULL, CommentSetID = NULL, CommentBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CommentBucket", body = list(DataObject = body), searchFields = append("CommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CommentBucket
	#'
	#' This function modifies a CommentBucket
	#' @param fieldNames The field values to give the modified CommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCommentBucket <- function(CommentBucketID, Name = NULL, DisplayOrder = NULL, IsLimitedByCourse = NULL, EntityGroupKey = NULL, GradingPeriodID = NULL, CommentSetID = NULL, CommentBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CommentBucket", objectId = CommentBucketID, body = list(DataObject = body), searchFields = append("CommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CommentCodes
	#'
	#' This function returns a dataframe or json object of CommentCodes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentCodes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentCodes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentCode') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CommentCodes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCommentCodes <- function(searchConditionsList = NULL, CommentCodeID = F, Code = F, Description = F, EntityGroupKey = F, CommentSetID = F, CodeDescription = F, CommentCodeIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CommentCode", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CommentCode
	#'
	#' This function returns a dataframe or json object of a CommentCode
	#' @param CommentCodeID The ID of the CommentCode to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentCode. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentCode.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentCode') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CommentCode
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCommentCode <- function(CommentCodeID, Code = F, Description = F, EntityGroupKey = F, CommentSetID = F, CodeDescription = F, CommentCodeIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CommentCodeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CommentCode", objectId = CommentCodeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CommentCode
	#'
	#' This function deletes a CommentCode
	#' @param CommentCodeID The ID of the CommentCode to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CommentCodeID of the deleted CommentCode.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCommentCode <- function(CommentCodeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CommentCode", objectId = CommentCodeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CommentCode
	#'
	#' This function creates a CommentCode
	#' @param fieldNames The field values to give the created CommentCode. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CommentCode
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCommentCode <- function(Code = NULL, Description = NULL, EntityGroupKey = NULL, CommentSetID = NULL, CommentCodeIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CommentCode", body = list(DataObject = body), searchFields = append("CommentCodeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CommentCode
	#'
	#' This function modifies a CommentCode
	#' @param fieldNames The field values to give the modified CommentCode. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CommentCode
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCommentCode <- function(CommentCodeID, Code = NULL, Description = NULL, EntityGroupKey = NULL, CommentSetID = NULL, CommentCodeIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CommentCode", objectId = CommentCodeID, body = list(DataObject = body), searchFields = append("CommentCodeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CommentSets
	#'
	#' This function returns a dataframe or json object of CommentSets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentSets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentSets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentSet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CommentSets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCommentSets <- function(searchConditionsList = NULL, CommentSetID = F, Code = F, EntityGroupKey = F, Description = F, EntityID = F, SchoolYearID = F, CommentSetIDClonedFrom = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CommentSet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CommentSet
	#'
	#' This function returns a dataframe or json object of a CommentSet
	#' @param CommentSetID The ID of the CommentSet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CommentSet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CommentSet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CommentSet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CommentSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCommentSet <- function(CommentSetID, Code = F, EntityGroupKey = F, Description = F, EntityID = F, SchoolYearID = F, CommentSetIDClonedFrom = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CommentSetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CommentSet", objectId = CommentSetID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CommentSet
	#'
	#' This function deletes a CommentSet
	#' @param CommentSetID The ID of the CommentSet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CommentSetID of the deleted CommentSet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCommentSet <- function(CommentSetID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CommentSet", objectId = CommentSetID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CommentSet
	#'
	#' This function creates a CommentSet
	#' @param fieldNames The field values to give the created CommentSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CommentSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCommentSet <- function(Code = NULL, EntityGroupKey = NULL, Description = NULL, EntityID = NULL, SchoolYearID = NULL, CommentSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CommentSet", body = list(DataObject = body), searchFields = append("CommentSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CommentSet
	#'
	#' This function modifies a CommentSet
	#' @param fieldNames The field values to give the modified CommentSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CommentSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCommentSet <- function(CommentSetID, Code = NULL, EntityGroupKey = NULL, Description = NULL, EntityID = NULL, SchoolYearID = NULL, CommentSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CommentSet", objectId = CommentSetID, body = list(DataObject = body), searchFields = append("CommentSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingPeriodSets
	#'
	#' This function returns a dataframe or json object of GradingPeriodSets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriodSets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriodSets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriodSet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingPeriodSets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingPeriodSets <- function(searchConditionsList = NULL, GradingPeriodSetID = F, EntityGroupKey = F, Code = F, Description = F, CourseLengthID = F, IsDefault = F, GradingPeriodSetIDClonedFrom = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingPeriodSetIDClonedTo = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradingPeriodSet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingPeriodSet
	#'
	#' This function returns a dataframe or json object of a GradingPeriodSet
	#' @param GradingPeriodSetID The ID of the GradingPeriodSet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriodSet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriodSet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriodSet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingPeriodSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingPeriodSet <- function(GradingPeriodSetID, EntityGroupKey = F, Code = F, Description = F, CourseLengthID = F, IsDefault = F, GradingPeriodSetIDClonedFrom = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingPeriodSetIDClonedTo = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingPeriodSetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradingPeriodSet", objectId = GradingPeriodSetID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingPeriodSet
	#'
	#' This function deletes a GradingPeriodSet
	#' @param GradingPeriodSetID The ID of the GradingPeriodSet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingPeriodSetID of the deleted GradingPeriodSet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingPeriodSet <- function(GradingPeriodSetID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradingPeriodSet", objectId = GradingPeriodSetID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingPeriodSet
	#'
	#' This function creates a GradingPeriodSet
	#' @param fieldNames The field values to give the created GradingPeriodSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingPeriodSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingPeriodSet <- function(EntityGroupKey = NULL, Code = NULL, Description = NULL, CourseLengthID = NULL, IsDefault = NULL, GradingPeriodSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradingPeriodSet", body = list(DataObject = body), searchFields = append("GradingPeriodSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingPeriodSet
	#'
	#' This function modifies a GradingPeriodSet
	#' @param fieldNames The field values to give the modified GradingPeriodSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingPeriodSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingPeriodSet <- function(GradingPeriodSetID, EntityGroupKey = NULL, Code = NULL, Description = NULL, CourseLengthID = NULL, IsDefault = NULL, GradingPeriodSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradingPeriodSet", objectId = GradingPeriodSetID, body = list(DataObject = body), searchFields = append("GradingPeriodSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingConfigEntityGroupYears
	#'
	#' This function returns a dataframe or json object of GradingConfigEntityGroupYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigEntityGroupYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigEntityGroupYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigEntityGroupYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingConfigEntityGroupYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingConfigEntityGroupYears <- function(searchConditionsList = NULL, ConfigEntityGroupYearID = F, EntityID = F, SchoolYearID = F, UseAddOnGPA = F, UseFactorBasedAddOn = F, FreeFormCommentMaxLength = F, EntityGroupKey = F, RetainGradesNumberOfDays = F, CurrentCalculation = F, ConfigEntityGroupYearIDClonedFrom = F, LockGradebookCalculation = F, LockGradebookAssignmentsAfterDate = F, LockGradebookStartTime = F, GradebookLockMessage = F, LockGradeBuckets = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeLevelIDCohort = F, EarnedCreditsMethodIDDefault = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ConfigEntityGroupYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingConfigEntityGroupYear
	#'
	#' This function returns a dataframe or json object of a GradingConfigEntityGroupYear
	#' @param GradingConfigEntityGroupYearID The ID of the GradingConfigEntityGroupYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigEntityGroupYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigEntityGroupYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigEntityGroupYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingConfigEntityGroupYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingConfigEntityGroupYear <- function(GradingConfigEntityGroupYearID, ConfigEntityGroupYearID = F, EntityID = F, SchoolYearID = F, UseAddOnGPA = F, UseFactorBasedAddOn = F, FreeFormCommentMaxLength = F, EntityGroupKey = F, RetainGradesNumberOfDays = F, CurrentCalculation = F, ConfigEntityGroupYearIDClonedFrom = F, LockGradebookCalculation = F, LockGradebookAssignmentsAfterDate = F, LockGradebookStartTime = F, GradebookLockMessage = F, LockGradeBuckets = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeLevelIDCohort = F, EarnedCreditsMethodIDDefault = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingConfigEntityGroupYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ConfigEntityGroupYear", objectId = GradingConfigEntityGroupYearID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingConfigEntityGroupYear
	#'
	#' This function deletes a GradingConfigEntityGroupYear
	#' @param GradingConfigEntityGroupYearID The ID of the GradingConfigEntityGroupYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingConfigEntityGroupYearID of the deleted GradingConfigEntityGroupYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingConfigEntityGroupYear <- function(GradingConfigEntityGroupYearID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ConfigEntityGroupYear", objectId = GradingConfigEntityGroupYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingConfigEntityGroupYear
	#'
	#' This function creates a GradingConfigEntityGroupYear
	#' @param fieldNames The field values to give the created GradingConfigEntityGroupYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingConfigEntityGroupYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingConfigEntityGroupYear <- function(EntityID = NULL, SchoolYearID = NULL, UseAddOnGPA = NULL, UseFactorBasedAddOn = NULL, FreeFormCommentMaxLength = NULL, EntityGroupKey = NULL, RetainGradesNumberOfDays = NULL, CurrentCalculation = NULL, ConfigEntityGroupYearIDClonedFrom = NULL, LockGradebookCalculation = NULL, LockGradebookAssignmentsAfterDate = NULL, LockGradebookStartTime = NULL, GradebookLockMessage = NULL, GradeLevelIDCohort = NULL, EarnedCreditsMethodIDDefault = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ConfigEntityGroupYear", body = list(DataObject = body), searchFields = append("ConfigEntityGroupYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingConfigEntityGroupYear
	#'
	#' This function modifies a GradingConfigEntityGroupYear
	#' @param fieldNames The field values to give the modified GradingConfigEntityGroupYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingConfigEntityGroupYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingConfigEntityGroupYear <- function(ConfigEntityGroupYearID, EntityID = NULL, SchoolYearID = NULL, UseAddOnGPA = NULL, UseFactorBasedAddOn = NULL, FreeFormCommentMaxLength = NULL, EntityGroupKey = NULL, RetainGradesNumberOfDays = NULL, CurrentCalculation = NULL, ConfigEntityGroupYearIDClonedFrom = NULL, LockGradebookCalculation = NULL, LockGradebookAssignmentsAfterDate = NULL, LockGradebookStartTime = NULL, GradebookLockMessage = NULL, GradeLevelIDCohort = NULL, EarnedCreditsMethodIDDefault = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ConfigEntityGroupYear", objectId = ConfigEntityGroupYearID, body = list(DataObject = body), searchFields = append("ConfigEntityGroupYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGPAMethods
	#'
	#' This function returns a dataframe or json object of CourseGPAMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGPAMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGPAMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGPAMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CourseGPAMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGPAMethods <- function(searchConditionsList = NULL, CourseGPAMethodID = F, EntityGroupKey = F, GPAMethodEntityID = F, CourseID = F, PointSetEntityID = F, UseOverride = F, GPACredit = F, GPACredits = F, CourseGPAMethodIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CourseGPAMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGPAMethod
	#'
	#' This function returns a dataframe or json object of a CourseGPAMethod
	#' @param CourseGPAMethodID The ID of the CourseGPAMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGPAMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGPAMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGPAMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CourseGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGPAMethod <- function(CourseGPAMethodID, EntityGroupKey = F, GPAMethodEntityID = F, CourseID = F, PointSetEntityID = F, UseOverride = F, GPACredit = F, GPACredits = F, CourseGPAMethodIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGPAMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CourseGPAMethod", objectId = CourseGPAMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGPAMethod
	#'
	#' This function deletes a CourseGPAMethod
	#' @param CourseGPAMethodID The ID of the CourseGPAMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CourseGPAMethodID of the deleted CourseGPAMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGPAMethod <- function(CourseGPAMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CourseGPAMethod", objectId = CourseGPAMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGPAMethod
	#'
	#' This function creates a CourseGPAMethod
	#' @param fieldNames The field values to give the created CourseGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CourseGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGPAMethod <- function(EntityGroupKey = NULL, GPAMethodEntityID = NULL, CourseID = NULL, PointSetEntityID = NULL, UseOverride = NULL, GPACredit = NULL, CourseGPAMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CourseGPAMethod", body = list(DataObject = body), searchFields = append("CourseGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGPAMethod
	#'
	#' This function modifies a CourseGPAMethod
	#' @param fieldNames The field values to give the modified CourseGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CourseGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGPAMethod <- function(CourseGPAMethodID, EntityGroupKey = NULL, GPAMethodEntityID = NULL, CourseID = NULL, PointSetEntityID = NULL, UseOverride = NULL, GPACredit = NULL, CourseGPAMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CourseGPAMethod", objectId = CourseGPAMethodID, body = list(DataObject = body), searchFields = append("CourseGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeBuckets
	#'
	#' This function returns a dataframe or json object of GradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeBuckets <- function(searchConditionsList = NULL, GradeBucketID = F, EntityGroupKey = F, Description = F, Label = F, LabelDescription = F, Number = F, GradeBucketTypeID = F, Order = F, GradeBucketIDClonedFrom = F, UseForFamilyAccess = F, FamilyAccessDisplayGradYearLow = F, FamilyAccessDisplayGradYearHigh = F, EdFiGradingPeriodID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EdFiGradingPeriodDescriptorID = F, DynamicRelationshipID = F, EdFiGradingPeriodDescriptorIDDashboard = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeBucket
	#'
	#' This function returns a dataframe or json object of a GradeBucket
	#' @param GradeBucketID The ID of the GradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeBucket <- function(GradeBucketID, EntityGroupKey = F, Description = F, Label = F, LabelDescription = F, Number = F, GradeBucketTypeID = F, Order = F, GradeBucketIDClonedFrom = F, UseForFamilyAccess = F, FamilyAccessDisplayGradYearLow = F, FamilyAccessDisplayGradYearHigh = F, EdFiGradingPeriodID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EdFiGradingPeriodDescriptorID = F, DynamicRelationshipID = F, EdFiGradingPeriodDescriptorIDDashboard = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeBucket", objectId = GradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeBucket
	#'
	#' This function deletes a GradeBucket
	#' @param GradeBucketID The ID of the GradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeBucketID of the deleted GradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeBucket <- function(GradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeBucket", objectId = GradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeBucket
	#'
	#' This function creates a GradeBucket
	#' @param fieldNames The field values to give the created GradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeBucket <- function(EntityGroupKey = NULL, Description = NULL, Label = NULL, Number = NULL, GradeBucketTypeID = NULL, Order = NULL, GradeBucketIDClonedFrom = NULL, UseForFamilyAccess = NULL, FamilyAccessDisplayGradYearLow = NULL, FamilyAccessDisplayGradYearHigh = NULL, EdFiGradingPeriodID = NULL, EdFiGradingPeriodDescriptorID = NULL, EdFiGradingPeriodDescriptorIDDashboard = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeBucket", body = list(DataObject = body), searchFields = append("GradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeBucket
	#'
	#' This function modifies a GradeBucket
	#' @param fieldNames The field values to give the modified GradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeBucket <- function(GradeBucketID, EntityGroupKey = NULL, Description = NULL, Label = NULL, Number = NULL, GradeBucketTypeID = NULL, Order = NULL, GradeBucketIDClonedFrom = NULL, UseForFamilyAccess = NULL, FamilyAccessDisplayGradYearLow = NULL, FamilyAccessDisplayGradYearHigh = NULL, EdFiGradingPeriodID = NULL, EdFiGradingPeriodDescriptorID = NULL, EdFiGradingPeriodDescriptorIDDashboard = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeBucket", objectId = GradeBucketID, body = list(DataObject = body), searchFields = append("GradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeBucketTypes
	#'
	#' This function returns a dataframe or json object of GradeBucketTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeBucketTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeBucketTypes <- function(searchConditionsList = NULL, GradeBucketTypeID = F, EntityGroupKey = F, Code = F, Description = F, DisplayOrder = F, EntityID = F, SchoolYearID = F, UseAllCategories = F, GradeBucketTypeIDClonedFrom = F, UseSnapshotGrade = F, SnapshotGradeExtensionDays = F, EdFiGradeTypeID = F, CodeDescription = F, SpecificCategoryGradeBucketTypeCount = F, HasSpecificCategories = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, MinimumPercent = F, EdFiGradeTypeDescriptorID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeBucketType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeBucketType
	#'
	#' This function returns a dataframe or json object of a GradeBucketType
	#' @param GradeBucketTypeID The ID of the GradeBucketType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeBucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeBucketType <- function(GradeBucketTypeID, EntityGroupKey = F, Code = F, Description = F, DisplayOrder = F, EntityID = F, SchoolYearID = F, UseAllCategories = F, GradeBucketTypeIDClonedFrom = F, UseSnapshotGrade = F, SnapshotGradeExtensionDays = F, EdFiGradeTypeID = F, CodeDescription = F, SpecificCategoryGradeBucketTypeCount = F, HasSpecificCategories = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, MinimumPercent = F, EdFiGradeTypeDescriptorID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeBucketTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeBucketType", objectId = GradeBucketTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeBucketType
	#'
	#' This function deletes a GradeBucketType
	#' @param GradeBucketTypeID The ID of the GradeBucketType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeBucketTypeID of the deleted GradeBucketType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeBucketType <- function(GradeBucketTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeBucketType", objectId = GradeBucketTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeBucketType
	#'
	#' This function creates a GradeBucketType
	#' @param fieldNames The field values to give the created GradeBucketType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeBucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeBucketType <- function(EntityGroupKey = NULL, Code = NULL, Description = NULL, DisplayOrder = NULL, EntityID = NULL, SchoolYearID = NULL, UseAllCategories = NULL, GradeBucketTypeIDClonedFrom = NULL, UseSnapshotGrade = NULL, SnapshotGradeExtensionDays = NULL, EdFiGradeTypeID = NULL, MinimumPercent = NULL, EdFiGradeTypeDescriptorID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeBucketType", body = list(DataObject = body), searchFields = append("GradeBucketTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeBucketType
	#'
	#' This function modifies a GradeBucketType
	#' @param fieldNames The field values to give the modified GradeBucketType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeBucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeBucketType <- function(GradeBucketTypeID, EntityGroupKey = NULL, Code = NULL, Description = NULL, DisplayOrder = NULL, EntityID = NULL, SchoolYearID = NULL, UseAllCategories = NULL, GradeBucketTypeIDClonedFrom = NULL, UseSnapshotGrade = NULL, SnapshotGradeExtensionDays = NULL, EdFiGradeTypeID = NULL, MinimumPercent = NULL, EdFiGradeTypeDescriptorID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeBucketType", objectId = GradeBucketTypeID, body = list(DataObject = body), searchFields = append("GradeBucketTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradeBuckets
	#'
	#' This function returns a dataframe or json object of StudentGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradeBuckets <- function(searchConditionsList = NULL, StudentGradeBucketID = F, GradingPeriodGradeBucketID = F, StudentSectionID = F, GradeMarkID = F, GradeMarkIDOverride = F, EntityID = F, SchoolYearID = F, GradeMarkOverrideComment = F, CompleteByTeacher = F, EarnedCredits = F, EarnedCreditsPossible = F, SectionID = F, ConfigEarnedCredits = F, FailedCredits = F, ConfigFailedCredits = F, EarnedCreditAttempted = F, Percent = F, EarnedPoints = F, PossiblePoints = F, NoGradebookOverride = F, NoGradebookOrAdminOverride = F, PercentAdjustment = F, PercentAdjustmentComment = F, StartingPercent = F, IsAdminOverride = F, IsTransferBucket = F, DoNotPost = F, IsComplete = F, HasUnscoredRequiredFeederBucket = F, OverrideComment = F, StudentCommentBucketCount = F, StudentFreeFormCommentBucketCount = F, ClosedGradingPeriodGradeChangeStatus = F, CalculatedClosedGradingPeriodGradeChangeStatus = F, CalculatedClosedGradingPeriodGradeChangeStatusCode = F, CalculatedCalculationTypeCode = F, GradingPeriodEndDateHasPassed = F, PercentWithAdjustment = F, PercentWithAdjustmentNoCap = F, GradeMarkToUse = F, GradeMarkIDToApply = F, GradeMarkToUseIgnoreDoNotPost = F, ReportCardGradeMarkToUse = F, PercentWithAdjustmentWithGradeMarkToUse = F, PercentWithAdjustmentWithGradeMarkToUseIgnoreDoNotPost = F, PercentWithGradeMarkIgnoreDoNotPost = F, PercentWithAdjustmentWithGradeMarkToUseNoCap = F, WeightPercentForGradeBucket = F, HasAssignments = F, AbsentCount = F, ExcusedCount = F, UnexcusedCount = F, TardyCount = F, OtherCount = F, HasStudentSectionGradingScaleGradeBucket = F, HasStudentSectionLinkConflict = F, StudentGradeBucketStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeMarkIDOutOfDistrictTransferWithdraw = F, HasSubjectGrades = F, HasAcademicStandardGrades = F, PercentWithAdjustmentIgnoreMinimum = F, GradeMarkIDToUseIgnoreDoNotPost = F, CalculatedPoints = F, IsUsingPointsBasedScale = F, StudentGradeBucketFlag = F, GradeMarkIDToUse = F, PercentHasChangedWithinSpecifiedAmountOfTime = F, UseCompletionDateOverride = F, CompletionDateOverride = F, CompletionDate = F, IsUnused = F, HasGrade = F, IsWeightedOnByEnrolledInBucket = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradeBucket
	#'
	#' This function returns a dataframe or json object of a StudentGradeBucket
	#' @param StudentGradeBucketID The ID of the StudentGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradeBucket <- function(StudentGradeBucketID, GradingPeriodGradeBucketID = F, StudentSectionID = F, GradeMarkID = F, GradeMarkIDOverride = F, EntityID = F, SchoolYearID = F, GradeMarkOverrideComment = F, CompleteByTeacher = F, EarnedCredits = F, EarnedCreditsPossible = F, SectionID = F, ConfigEarnedCredits = F, FailedCredits = F, ConfigFailedCredits = F, EarnedCreditAttempted = F, Percent = F, EarnedPoints = F, PossiblePoints = F, NoGradebookOverride = F, NoGradebookOrAdminOverride = F, PercentAdjustment = F, PercentAdjustmentComment = F, StartingPercent = F, IsAdminOverride = F, IsTransferBucket = F, DoNotPost = F, IsComplete = F, HasUnscoredRequiredFeederBucket = F, OverrideComment = F, StudentCommentBucketCount = F, StudentFreeFormCommentBucketCount = F, ClosedGradingPeriodGradeChangeStatus = F, CalculatedClosedGradingPeriodGradeChangeStatus = F, CalculatedClosedGradingPeriodGradeChangeStatusCode = F, CalculatedCalculationTypeCode = F, GradingPeriodEndDateHasPassed = F, PercentWithAdjustment = F, PercentWithAdjustmentNoCap = F, GradeMarkToUse = F, GradeMarkIDToApply = F, GradeMarkToUseIgnoreDoNotPost = F, ReportCardGradeMarkToUse = F, PercentWithAdjustmentWithGradeMarkToUse = F, PercentWithAdjustmentWithGradeMarkToUseIgnoreDoNotPost = F, PercentWithGradeMarkIgnoreDoNotPost = F, PercentWithAdjustmentWithGradeMarkToUseNoCap = F, WeightPercentForGradeBucket = F, HasAssignments = F, AbsentCount = F, ExcusedCount = F, UnexcusedCount = F, TardyCount = F, OtherCount = F, HasStudentSectionGradingScaleGradeBucket = F, HasStudentSectionLinkConflict = F, StudentGradeBucketStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeMarkIDOutOfDistrictTransferWithdraw = F, HasSubjectGrades = F, HasAcademicStandardGrades = F, PercentWithAdjustmentIgnoreMinimum = F, GradeMarkIDToUseIgnoreDoNotPost = F, CalculatedPoints = F, IsUsingPointsBasedScale = F, StudentGradeBucketFlag = F, GradeMarkIDToUse = F, PercentHasChangedWithinSpecifiedAmountOfTime = F, UseCompletionDateOverride = F, CompletionDateOverride = F, CompletionDate = F, IsUnused = F, HasGrade = F, IsWeightedOnByEnrolledInBucket = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentGradeBucket", objectId = StudentGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradeBucket
	#'
	#' This function deletes a StudentGradeBucket
	#' @param StudentGradeBucketID The ID of the StudentGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentGradeBucketID of the deleted StudentGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradeBucket <- function(StudentGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentGradeBucket", objectId = StudentGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradeBucket
	#'
	#' This function creates a StudentGradeBucket
	#' @param fieldNames The field values to give the created StudentGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradeBucket <- function(GradingPeriodGradeBucketID = NULL, StudentSectionID = NULL, GradeMarkID = NULL, GradeMarkIDOverride = NULL, EntityID = NULL, SchoolYearID = NULL, GradeMarkOverrideComment = NULL, CompleteByTeacher = NULL, SectionID = NULL, Percent = NULL, EarnedPoints = NULL, PossiblePoints = NULL, PercentAdjustment = NULL, PercentAdjustmentComment = NULL, StartingPercent = NULL, IsAdminOverride = NULL, IsTransferBucket = NULL, DoNotPost = NULL, IsComplete = NULL, HasUnscoredRequiredFeederBucket = NULL, OverrideComment = NULL, ClosedGradingPeriodGradeChangeStatus = NULL, GradeMarkIDToApply = NULL, GradeMarkIDOutOfDistrictTransferWithdraw = NULL, CalculatedPoints = NULL, StudentGradeBucketFlag = NULL, UseCompletionDateOverride = NULL, CompletionDateOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentGradeBucket", body = list(DataObject = body), searchFields = append("StudentGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradeBucket
	#'
	#' This function modifies a StudentGradeBucket
	#' @param fieldNames The field values to give the modified StudentGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradeBucket <- function(StudentGradeBucketID, GradingPeriodGradeBucketID = NULL, StudentSectionID = NULL, GradeMarkID = NULL, GradeMarkIDOverride = NULL, EntityID = NULL, SchoolYearID = NULL, GradeMarkOverrideComment = NULL, CompleteByTeacher = NULL, SectionID = NULL, Percent = NULL, EarnedPoints = NULL, PossiblePoints = NULL, PercentAdjustment = NULL, PercentAdjustmentComment = NULL, StartingPercent = NULL, IsAdminOverride = NULL, IsTransferBucket = NULL, DoNotPost = NULL, IsComplete = NULL, HasUnscoredRequiredFeederBucket = NULL, OverrideComment = NULL, ClosedGradingPeriodGradeChangeStatus = NULL, GradeMarkIDToApply = NULL, GradeMarkIDOutOfDistrictTransferWithdraw = NULL, CalculatedPoints = NULL, StudentGradeBucketFlag = NULL, UseCompletionDateOverride = NULL, CompletionDateOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentGradeBucket", objectId = StudentGradeBucketID, body = list(DataObject = body), searchFields = append("StudentGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentCommentBuckets
	#'
	#' This function returns a dataframe or json object of StudentCommentBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCommentBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCommentBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCommentBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentCommentBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentCommentBuckets <- function(searchConditionsList = NULL, StudentCommentBucketID = F, GradingPeriodID = F, StudentSectionID = F, CommentBucketID = F, CommentCodeID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentCommentBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentCommentBucket
	#'
	#' This function returns a dataframe or json object of a StudentCommentBucket
	#' @param StudentCommentBucketID The ID of the StudentCommentBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCommentBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCommentBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCommentBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentCommentBucket <- function(StudentCommentBucketID, GradingPeriodID = F, StudentSectionID = F, CommentBucketID = F, CommentCodeID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentCommentBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentCommentBucket", objectId = StudentCommentBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentCommentBucket
	#'
	#' This function deletes a StudentCommentBucket
	#' @param StudentCommentBucketID The ID of the StudentCommentBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentCommentBucketID of the deleted StudentCommentBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentCommentBucket <- function(StudentCommentBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentCommentBucket", objectId = StudentCommentBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentCommentBucket
	#'
	#' This function creates a StudentCommentBucket
	#' @param fieldNames The field values to give the created StudentCommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentCommentBucket <- function(GradingPeriodID = NULL, StudentSectionID = NULL, CommentBucketID = NULL, CommentCodeID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentCommentBucket", body = list(DataObject = body), searchFields = append("StudentCommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentCommentBucket
	#'
	#' This function modifies a StudentCommentBucket
	#' @param fieldNames The field values to give the modified StudentCommentBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentCommentBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentCommentBucket <- function(StudentCommentBucketID, GradingPeriodID = NULL, StudentSectionID = NULL, CommentBucketID = NULL, CommentCodeID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentCommentBucket", objectId = StudentCommentBucketID, body = list(DataObject = body), searchFields = append("StudentCommentBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingPeriodGradeBuckets
	#'
	#' This function returns a dataframe or json object of GradingPeriodGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriodGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriodGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriodGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingPeriodGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingPeriodGradeBuckets <- function(searchConditionsList = NULL, GradingPeriodGradeBucketID = F, GradingPeriodIDStart = F, GradingPeriodIDEnd = F, GradeBucketID = F, EntityGroupKey = F, FactorBasedGPACountAs = F, IsUpToDate = F, GradingPeriodGradeBucketIDClonedFrom = F, UseMaxExtraCredit = F, MaxExtraCredit = F, Status = F, NumberOfGradeBucketsToWeight = F, GradeBucketLabelWithDates = F, GradingPeriodEndDateAddSnapshotGraceDays = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasGradeBucketTypeCategories = F, GradingPeriodGradeBucketExistsInSpecifcEntity = F, StateETranscriptSessionTypeID = F, IsGradeBucketForETranscript = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradingPeriodGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingPeriodGradeBucket
	#'
	#' This function returns a dataframe or json object of a GradingPeriodGradeBucket
	#' @param GradingPeriodGradeBucketID The ID of the GradingPeriodGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriodGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriodGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriodGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingPeriodGradeBucket <- function(GradingPeriodGradeBucketID, GradingPeriodIDStart = F, GradingPeriodIDEnd = F, GradeBucketID = F, EntityGroupKey = F, FactorBasedGPACountAs = F, IsUpToDate = F, GradingPeriodGradeBucketIDClonedFrom = F, UseMaxExtraCredit = F, MaxExtraCredit = F, Status = F, NumberOfGradeBucketsToWeight = F, GradeBucketLabelWithDates = F, GradingPeriodEndDateAddSnapshotGraceDays = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasGradeBucketTypeCategories = F, GradingPeriodGradeBucketExistsInSpecifcEntity = F, StateETranscriptSessionTypeID = F, IsGradeBucketForETranscript = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingPeriodGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradingPeriodGradeBucket", objectId = GradingPeriodGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingPeriodGradeBucket
	#'
	#' This function deletes a GradingPeriodGradeBucket
	#' @param GradingPeriodGradeBucketID The ID of the GradingPeriodGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingPeriodGradeBucketID of the deleted GradingPeriodGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingPeriodGradeBucket <- function(GradingPeriodGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradingPeriodGradeBucket", objectId = GradingPeriodGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingPeriodGradeBucket
	#'
	#' This function creates a GradingPeriodGradeBucket
	#' @param fieldNames The field values to give the created GradingPeriodGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingPeriodGradeBucket <- function(GradingPeriodIDStart = NULL, GradingPeriodIDEnd = NULL, GradeBucketID = NULL, EntityGroupKey = NULL, FactorBasedGPACountAs = NULL, IsUpToDate = NULL, GradingPeriodGradeBucketIDClonedFrom = NULL, StateETranscriptSessionTypeID = NULL, IsGradeBucketForETranscript = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradingPeriodGradeBucket", body = list(DataObject = body), searchFields = append("GradingPeriodGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingPeriodGradeBucket
	#'
	#' This function modifies a GradingPeriodGradeBucket
	#' @param fieldNames The field values to give the modified GradingPeriodGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingPeriodGradeBucket <- function(GradingPeriodGradeBucketID, GradingPeriodIDStart = NULL, GradingPeriodIDEnd = NULL, GradeBucketID = NULL, EntityGroupKey = NULL, FactorBasedGPACountAs = NULL, IsUpToDate = NULL, GradingPeriodGradeBucketIDClonedFrom = NULL, StateETranscriptSessionTypeID = NULL, IsGradeBucketForETranscript = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradingPeriodGradeBucket", objectId = GradingPeriodGradeBucketID, body = list(DataObject = body), searchFields = append("GradingPeriodGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingPeriods
	#'
	#' This function returns a dataframe or json object of GradingPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingPeriods <- function(searchConditionsList = NULL, GradingPeriodID = F, EntityGroupKey = F, Description = F, SectionLengthID = F, GradingPeriodSetID = F, Number = F, StartDate = F, EndDate = F, DateOverrideTeacherGracePeriod = F, GradingPeriodIDClonedFrom = F, DateOverrideTeacherGracePeriodDisplay = F, ExtendedEndDateGreaterThanToday = F, WithinGradingPeriod = F, BeforeSectionLengthStartFirstGradingPeriod = F, AfterSectionLengthEndLastGradingPeriod = F, DisplayAssignments = F, IncludeMissingAssignments = F, DisplayGradeBuckets = F, ProgressReportGradingPeriodNumberDateDisplay = F, GradeBucketLabels = F, OptionsHeaderText = F, CompletedText = F, CompletedFieldText = F, StartDateCopy = F, EndDateCopy = F, NumberDescription = F, StatusDisplay = F, CalculatedEndDateWithExtension = F, StatusDisplayWithExtensionDate = F, ExtensionDays = F, IsCompleted = F, ClosedGradingPeriodGradeChangeID = F, ClosedGradingPeriodGradeChangeIDForNotReviewedRequests = F, ExtensionEndTime = F, CurrentActiveStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SectionIDForActiveStatus = F, IncludeMissingAssignmentsOrIsCurrentGradingPeriod = F, GradingPeriodIDClonedTo = F, IsActiveOrExtended = F, UniqueCode = F, EndDateWithRetainGradesNumberOfDays = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradingPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingPeriod
	#'
	#' This function returns a dataframe or json object of a GradingPeriod
	#' @param GradingPeriodID The ID of the GradingPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingPeriod <- function(GradingPeriodID, EntityGroupKey = F, Description = F, SectionLengthID = F, GradingPeriodSetID = F, Number = F, StartDate = F, EndDate = F, DateOverrideTeacherGracePeriod = F, GradingPeriodIDClonedFrom = F, DateOverrideTeacherGracePeriodDisplay = F, ExtendedEndDateGreaterThanToday = F, WithinGradingPeriod = F, BeforeSectionLengthStartFirstGradingPeriod = F, AfterSectionLengthEndLastGradingPeriod = F, DisplayAssignments = F, IncludeMissingAssignments = F, DisplayGradeBuckets = F, ProgressReportGradingPeriodNumberDateDisplay = F, GradeBucketLabels = F, OptionsHeaderText = F, CompletedText = F, CompletedFieldText = F, StartDateCopy = F, EndDateCopy = F, NumberDescription = F, StatusDisplay = F, CalculatedEndDateWithExtension = F, StatusDisplayWithExtensionDate = F, ExtensionDays = F, IsCompleted = F, ClosedGradingPeriodGradeChangeID = F, ClosedGradingPeriodGradeChangeIDForNotReviewedRequests = F, ExtensionEndTime = F, CurrentActiveStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SectionIDForActiveStatus = F, IncludeMissingAssignmentsOrIsCurrentGradingPeriod = F, GradingPeriodIDClonedTo = F, IsActiveOrExtended = F, UniqueCode = F, EndDateWithRetainGradesNumberOfDays = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradingPeriod", objectId = GradingPeriodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingPeriod
	#'
	#' This function deletes a GradingPeriod
	#' @param GradingPeriodID The ID of the GradingPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingPeriodID of the deleted GradingPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingPeriod <- function(GradingPeriodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradingPeriod", objectId = GradingPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingPeriod
	#'
	#' This function creates a GradingPeriod
	#' @param fieldNames The field values to give the created GradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingPeriod <- function(EntityGroupKey = NULL, Description = NULL, SectionLengthID = NULL, GradingPeriodSetID = NULL, Number = NULL, StartDate = NULL, EndDate = NULL, DateOverrideTeacherGracePeriod = NULL, GradingPeriodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradingPeriod", body = list(DataObject = body), searchFields = append("GradingPeriodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingPeriod
	#'
	#' This function modifies a GradingPeriod
	#' @param fieldNames The field values to give the modified GradingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingPeriod <- function(GradingPeriodID, EntityGroupKey = NULL, Description = NULL, SectionLengthID = NULL, GradingPeriodSetID = NULL, Number = NULL, StartDate = NULL, EndDate = NULL, DateOverrideTeacherGracePeriod = NULL, GradingPeriodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradingPeriod", objectId = GradingPeriodID, body = list(DataObject = body), searchFields = append("GradingPeriodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeMarks
	#'
	#' This function returns a dataframe or json object of GradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeMarks <- function(searchConditionsList = NULL, GradeMarkID = F, EntityGroupKey = F, EntityID = F, SchoolYearID = F, Code = F, Description = F, GradYearLow = F, GradYearHigh = F, DisplayOrder = F, ReportCardDisplayValue = F, TranscriptDisplayValue = F, UseInEarned = F, UseInFailed = F, UseInGPA = F, UseAsTeacherOverride = F, GradeMarkIDReverse = F, GradeMarkIDClonedFrom = F, CodeDescription = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeMarkMNID = F, StateGradeMarkMNID = F, GradeMarkExistsInSpecifcEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeMark
	#'
	#' This function returns a dataframe or json object of a GradeMark
	#' @param GradeMarkID The ID of the GradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeMark <- function(GradeMarkID, EntityGroupKey = F, EntityID = F, SchoolYearID = F, Code = F, Description = F, GradYearLow = F, GradYearHigh = F, DisplayOrder = F, ReportCardDisplayValue = F, TranscriptDisplayValue = F, UseInEarned = F, UseInFailed = F, UseInGPA = F, UseAsTeacherOverride = F, GradeMarkIDReverse = F, GradeMarkIDClonedFrom = F, CodeDescription = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeMarkMNID = F, StateGradeMarkMNID = F, GradeMarkExistsInSpecifcEntity = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeMark", objectId = GradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeMark
	#'
	#' This function deletes a GradeMark
	#' @param GradeMarkID The ID of the GradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeMarkID of the deleted GradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeMark <- function(GradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeMark", objectId = GradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeMark
	#'
	#' This function creates a GradeMark
	#' @param fieldNames The field values to give the created GradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeMark <- function(EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, Code = NULL, Description = NULL, GradYearLow = NULL, GradYearHigh = NULL, DisplayOrder = NULL, ReportCardDisplayValue = NULL, TranscriptDisplayValue = NULL, UseInEarned = NULL, UseInFailed = NULL, UseInGPA = NULL, UseAsTeacherOverride = NULL, GradeMarkIDReverse = NULL, GradeMarkIDClonedFrom = NULL, StateGradeMarkMNID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeMark", body = list(DataObject = body), searchFields = append("GradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeMark
	#'
	#' This function modifies a GradeMark
	#' @param fieldNames The field values to give the modified GradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeMark <- function(GradeMarkID, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, Code = NULL, Description = NULL, GradYearLow = NULL, GradYearHigh = NULL, DisplayOrder = NULL, ReportCardDisplayValue = NULL, TranscriptDisplayValue = NULL, UseInEarned = NULL, UseInFailed = NULL, UseInGPA = NULL, UseAsTeacherOverride = NULL, GradeMarkIDReverse = NULL, GradeMarkIDClonedFrom = NULL, StateGradeMarkMNID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeMark", objectId = GradeMarkID, body = list(DataObject = body), searchFields = append("GradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingConfigEntityYears
	#'
	#' This function returns a dataframe or json object of GradingConfigEntityYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigEntityYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigEntityYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigEntityYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradingConfigEntityYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingConfigEntityYears <- function(searchConditionsList = NULL, ConfigEntityYearID = F, EntityID = F, SchoolYearID = F, ElectronicSignatureIDTranscript = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ElectronicSignatureIDReportCard = F, ConfigEntityYearIDClonedFrom = F, TranscriptTitle = F, GPAMethodEntityIDETranscript = F, RankMethodIDETranscript = F, GPAMaximumRangeETranscript = F, AcademicSummaryTypeETranscript = F, RequiredCreditTypeETranscript = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ConfigEntityYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingConfigEntityYear
	#'
	#' This function returns a dataframe or json object of a GradingConfigEntityYear
	#' @param GradingConfigEntityYearID The ID of the GradingConfigEntityYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingConfigEntityYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingConfigEntityYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingConfigEntityYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradingConfigEntityYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingConfigEntityYear <- function(GradingConfigEntityYearID, ConfigEntityYearID = F, EntityID = F, SchoolYearID = F, ElectronicSignatureIDTranscript = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ElectronicSignatureIDReportCard = F, ConfigEntityYearIDClonedFrom = F, TranscriptTitle = F, GPAMethodEntityIDETranscript = F, RankMethodIDETranscript = F, GPAMaximumRangeETranscript = F, AcademicSummaryTypeETranscript = F, RequiredCreditTypeETranscript = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingConfigEntityYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ConfigEntityYear", objectId = GradingConfigEntityYearID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingConfigEntityYear
	#'
	#' This function deletes a GradingConfigEntityYear
	#' @param GradingConfigEntityYearID The ID of the GradingConfigEntityYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradingConfigEntityYearID of the deleted GradingConfigEntityYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingConfigEntityYear <- function(GradingConfigEntityYearID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ConfigEntityYear", objectId = GradingConfigEntityYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingConfigEntityYear
	#'
	#' This function creates a GradingConfigEntityYear
	#' @param fieldNames The field values to give the created GradingConfigEntityYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradingConfigEntityYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingConfigEntityYear <- function(EntityID = NULL, SchoolYearID = NULL, ElectronicSignatureIDTranscript = NULL, ElectronicSignatureIDReportCard = NULL, ConfigEntityYearIDClonedFrom = NULL, TranscriptTitle = NULL, GPAMethodEntityIDETranscript = NULL, RankMethodIDETranscript = NULL, GPAMaximumRangeETranscript = NULL, AcademicSummaryTypeETranscript = NULL, RequiredCreditTypeETranscript = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ConfigEntityYear", body = list(DataObject = body), searchFields = append("ConfigEntityYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingConfigEntityYear
	#'
	#' This function modifies a GradingConfigEntityYear
	#' @param fieldNames The field values to give the modified GradingConfigEntityYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradingConfigEntityYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingConfigEntityYear <- function(ConfigEntityYearID, EntityID = NULL, SchoolYearID = NULL, ElectronicSignatureIDTranscript = NULL, ElectronicSignatureIDReportCard = NULL, ConfigEntityYearIDClonedFrom = NULL, TranscriptTitle = NULL, GPAMethodEntityIDETranscript = NULL, RankMethodIDETranscript = NULL, GPAMaximumRangeETranscript = NULL, AcademicSummaryTypeETranscript = NULL, RequiredCreditTypeETranscript = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ConfigEntityYear", objectId = ConfigEntityYearID, body = list(DataObject = body), searchFields = append("ConfigEntityYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportRunHistories
	#'
	#' This function returns a dataframe or json object of GradeReportRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportRunHistories <- function(searchConditionsList = NULL, GradeReportRunHistoryID = F, ParameterDescription = F, FooterMessage = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateID = F, PrintCompletedGradingPeriodComments = F, EntityID = F, Code = F, Name = F, CEEBACT = F, AddressLine1 = F, AddressLine2 = F, City = F, StateProvince = F, PostalCode = F, PhoneNumber = F, FaxNumber = F, FormattedFullAddress = F, FamilyPrintType = F, StatusType = F, ReportCardFileName = F, PostReportCardToFASA = F, OverwriteExistingReportCard = F, RequireFamilyAccessElectronicSignature = F, IsTexasTranscript = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportRunHistory
	#'
	#' This function returns a dataframe or json object of a GradeReportRunHistory
	#' @param GradeReportRunHistoryID The ID of the GradeReportRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportRunHistory <- function(GradeReportRunHistoryID, ParameterDescription = F, FooterMessage = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateID = F, PrintCompletedGradingPeriodComments = F, EntityID = F, Code = F, Name = F, CEEBACT = F, AddressLine1 = F, AddressLine2 = F, City = F, StateProvince = F, PostalCode = F, PhoneNumber = F, FaxNumber = F, FormattedFullAddress = F, FamilyPrintType = F, StatusType = F, ReportCardFileName = F, PostReportCardToFASA = F, OverwriteExistingReportCard = F, RequireFamilyAccessElectronicSignature = F, IsTexasTranscript = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportRunHistory", objectId = GradeReportRunHistoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportRunHistory
	#'
	#' This function deletes a GradeReportRunHistory
	#' @param GradeReportRunHistoryID The ID of the GradeReportRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportRunHistoryID of the deleted GradeReportRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportRunHistory <- function(GradeReportRunHistoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportRunHistory", objectId = GradeReportRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportRunHistory
	#'
	#' This function creates a GradeReportRunHistory
	#' @param fieldNames The field values to give the created GradeReportRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportRunHistory <- function(ParameterDescription = NULL, FooterMessage = NULL, GradeReportTemplateID = NULL, PrintCompletedGradingPeriodComments = NULL, EntityID = NULL, Code = NULL, Name = NULL, CEEBACT = NULL, AddressLine1 = NULL, AddressLine2 = NULL, City = NULL, StateProvince = NULL, PostalCode = NULL, PhoneNumber = NULL, FaxNumber = NULL, FormattedFullAddress = NULL, FamilyPrintType = NULL, StatusType = NULL, ReportCardFileName = NULL, PostReportCardToFASA = NULL, OverwriteExistingReportCard = NULL, RequireFamilyAccessElectronicSignature = NULL, IsTexasTranscript = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportRunHistory", body = list(DataObject = body), searchFields = append("GradeReportRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportRunHistory
	#'
	#' This function modifies a GradeReportRunHistory
	#' @param fieldNames The field values to give the modified GradeReportRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportRunHistory <- function(GradeReportRunHistoryID, ParameterDescription = NULL, FooterMessage = NULL, GradeReportTemplateID = NULL, PrintCompletedGradingPeriodComments = NULL, EntityID = NULL, Code = NULL, Name = NULL, CEEBACT = NULL, AddressLine1 = NULL, AddressLine2 = NULL, City = NULL, StateProvince = NULL, PostalCode = NULL, PhoneNumber = NULL, FaxNumber = NULL, FormattedFullAddress = NULL, FamilyPrintType = NULL, StatusType = NULL, ReportCardFileName = NULL, PostReportCardToFASA = NULL, OverwriteExistingReportCard = NULL, RequireFamilyAccessElectronicSignature = NULL, IsTexasTranscript = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportRunHistory", objectId = GradeReportRunHistoryID, body = list(DataObject = body), searchFields = append("GradeReportRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentHonorRollRunLevels
	#'
	#' This function returns a dataframe or json object of TempStudentHonorRollRunLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentHonorRollRunLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentHonorRollRunLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentHonorRollRunLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempStudentHonorRollRunLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentHonorRollRunLevels <- function(searchConditionsList = NULL, TempStudentHonorRollRunLevelID = F, StudentID = F, HonorRollRunLevelID = F, HonorRollRunLevelName = F, HonorRollRunLevelOrder = F, StudentName = F, Grade = F, GPAValue = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempStudentHonorRollRunLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentHonorRollRunLevel
	#'
	#' This function returns a dataframe or json object of a TempStudentHonorRollRunLevel
	#' @param TempStudentHonorRollRunLevelID The ID of the TempStudentHonorRollRunLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentHonorRollRunLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentHonorRollRunLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentHonorRollRunLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempStudentHonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentHonorRollRunLevel <- function(TempStudentHonorRollRunLevelID, StudentID = F, HonorRollRunLevelID = F, HonorRollRunLevelName = F, HonorRollRunLevelOrder = F, StudentName = F, Grade = F, GPAValue = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentHonorRollRunLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempStudentHonorRollRunLevel", objectId = TempStudentHonorRollRunLevelID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentHonorRollRunLevel
	#'
	#' This function deletes a TempStudentHonorRollRunLevel
	#' @param TempStudentHonorRollRunLevelID The ID of the TempStudentHonorRollRunLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempStudentHonorRollRunLevelID of the deleted TempStudentHonorRollRunLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentHonorRollRunLevel <- function(TempStudentHonorRollRunLevelID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempStudentHonorRollRunLevel", objectId = TempStudentHonorRollRunLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentHonorRollRunLevel
	#'
	#' This function creates a TempStudentHonorRollRunLevel
	#' @param fieldNames The field values to give the created TempStudentHonorRollRunLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempStudentHonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentHonorRollRunLevel <- function(StudentID = NULL, HonorRollRunLevelID = NULL, HonorRollRunLevelName = NULL, HonorRollRunLevelOrder = NULL, StudentName = NULL, Grade = NULL, GPAValue = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempStudentHonorRollRunLevel", body = list(DataObject = body), searchFields = append("TempStudentHonorRollRunLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentHonorRollRunLevel
	#'
	#' This function modifies a TempStudentHonorRollRunLevel
	#' @param fieldNames The field values to give the modified TempStudentHonorRollRunLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempStudentHonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentHonorRollRunLevel <- function(TempStudentHonorRollRunLevelID, StudentID = NULL, HonorRollRunLevelID = NULL, HonorRollRunLevelName = NULL, HonorRollRunLevelOrder = NULL, StudentName = NULL, Grade = NULL, GPAValue = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempStudentHonorRollRunLevel", objectId = TempStudentHonorRollRunLevelID, body = list(DataObject = body), searchFields = append("TempStudentHonorRollRunLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRuleGPAS
	#'
	#' This function returns a dataframe or json object of HonorRollRuleGPAS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGPAS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGPAS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGPA') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRuleGPAS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRuleGPAS <- function(searchConditionsList = NULL, HonorRollRuleGPAID = F, HonorRollRunLevelRuleID = F, GPABucketID = F, GPAMethodID = F, MinimumGPA = F, MaximumGPA = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGPAIDClonedFrom = F, GPAType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRuleGPA", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRuleGPA
	#'
	#' This function returns a dataframe or json object of a HonorRollRuleGPA
	#' @param HonorRollRuleGPAID The ID of the HonorRollRuleGPA to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGPA. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGPA.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGPA') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRuleGPA
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRuleGPA <- function(HonorRollRuleGPAID, HonorRollRunLevelRuleID = F, GPABucketID = F, GPAMethodID = F, MinimumGPA = F, MaximumGPA = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGPAIDClonedFrom = F, GPAType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRuleGPAID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRuleGPA", objectId = HonorRollRuleGPAID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRuleGPA
	#'
	#' This function deletes a HonorRollRuleGPA
	#' @param HonorRollRuleGPAID The ID of the HonorRollRuleGPA to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRuleGPAID of the deleted HonorRollRuleGPA.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRuleGPA <- function(HonorRollRuleGPAID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRuleGPA", objectId = HonorRollRuleGPAID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRuleGPA
	#'
	#' This function creates a HonorRollRuleGPA
	#' @param fieldNames The field values to give the created HonorRollRuleGPA. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRuleGPA
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRuleGPA <- function(HonorRollRunLevelRuleID = NULL, GPABucketID = NULL, GPAMethodID = NULL, MinimumGPA = NULL, MaximumGPA = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGPAIDClonedFrom = NULL, GPAType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRuleGPA", body = list(DataObject = body), searchFields = append("HonorRollRuleGPAID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRuleGPA
	#'
	#' This function modifies a HonorRollRuleGPA
	#' @param fieldNames The field values to give the modified HonorRollRuleGPA. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRuleGPA
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRuleGPA <- function(HonorRollRuleGPAID, HonorRollRunLevelRuleID = NULL, GPABucketID = NULL, GPAMethodID = NULL, MinimumGPA = NULL, MaximumGPA = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGPAIDClonedFrom = NULL, GPAType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRuleGPA", objectId = HonorRollRuleGPAID, body = list(DataObject = body), searchFields = append("HonorRollRuleGPAID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRuleGradeMarks
	#'
	#' This function returns a dataframe or json object of HonorRollRuleGradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRuleGradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRuleGradeMarks <- function(searchConditionsList = NULL, HonorRollRuleGradeMarkID = F, HonorRollRunLevelRuleID = F, AllowException = F, TotalAllowableExceptions = F, CourseStandardFilterXML = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGradeMarkIDClonedFrom = F, InclusionType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ExclusionThreshold = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRuleGradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRuleGradeMark
	#'
	#' This function returns a dataframe or json object of a HonorRollRuleGradeMark
	#' @param HonorRollRuleGradeMarkID The ID of the HonorRollRuleGradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRuleGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRuleGradeMark <- function(HonorRollRuleGradeMarkID, HonorRollRunLevelRuleID = F, AllowException = F, TotalAllowableExceptions = F, CourseStandardFilterXML = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGradeMarkIDClonedFrom = F, InclusionType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, ExclusionThreshold = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRuleGradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMark", objectId = HonorRollRuleGradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRuleGradeMark
	#'
	#' This function deletes a HonorRollRuleGradeMark
	#' @param HonorRollRuleGradeMarkID The ID of the HonorRollRuleGradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRuleGradeMarkID of the deleted HonorRollRuleGradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRuleGradeMark <- function(HonorRollRuleGradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMark", objectId = HonorRollRuleGradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRuleGradeMark
	#'
	#' This function creates a HonorRollRuleGradeMark
	#' @param fieldNames The field values to give the created HonorRollRuleGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRuleGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRuleGradeMark <- function(HonorRollRunLevelRuleID = NULL, AllowException = NULL, TotalAllowableExceptions = NULL, CourseStandardFilterXML = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGradeMarkIDClonedFrom = NULL, InclusionType = NULL, ExclusionThreshold = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMark", body = list(DataObject = body), searchFields = append("HonorRollRuleGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRuleGradeMark
	#'
	#' This function modifies a HonorRollRuleGradeMark
	#' @param fieldNames The field values to give the modified HonorRollRuleGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRuleGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRuleGradeMark <- function(HonorRollRuleGradeMarkID, HonorRollRunLevelRuleID = NULL, AllowException = NULL, TotalAllowableExceptions = NULL, CourseStandardFilterXML = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGradeMarkIDClonedFrom = NULL, InclusionType = NULL, ExclusionThreshold = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRuleGradeMark", objectId = HonorRollRuleGradeMarkID, body = list(DataObject = body), searchFields = append("HonorRollRuleGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRuleGradeMarkGradeBuckets
	#'
	#' This function returns a dataframe or json object of HonorRollRuleGradeMarkGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGradeMarkGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGradeMarkGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGradeMarkGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRuleGradeMarkGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRuleGradeMarkGradeBuckets <- function(searchConditionsList = NULL, HonorRollRuleGradeMarkGradeBucketID = F, HonorRollRuleGradeMarkID = F, GradeBucketID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGradeMarkGradeBucketIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRuleGradeMarkGradeBucket
	#'
	#' This function returns a dataframe or json object of a HonorRollRuleGradeMarkGradeBucket
	#' @param HonorRollRuleGradeMarkGradeBucketID The ID of the HonorRollRuleGradeMarkGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGradeMarkGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGradeMarkGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGradeMarkGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRuleGradeMarkGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRuleGradeMarkGradeBucket <- function(HonorRollRuleGradeMarkGradeBucketID, HonorRollRuleGradeMarkID = F, GradeBucketID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGradeMarkGradeBucketIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRuleGradeMarkGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeBucket", objectId = HonorRollRuleGradeMarkGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRuleGradeMarkGradeBucket
	#'
	#' This function deletes a HonorRollRuleGradeMarkGradeBucket
	#' @param HonorRollRuleGradeMarkGradeBucketID The ID of the HonorRollRuleGradeMarkGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRuleGradeMarkGradeBucketID of the deleted HonorRollRuleGradeMarkGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRuleGradeMarkGradeBucket <- function(HonorRollRuleGradeMarkGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeBucket", objectId = HonorRollRuleGradeMarkGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRuleGradeMarkGradeBucket
	#'
	#' This function creates a HonorRollRuleGradeMarkGradeBucket
	#' @param fieldNames The field values to give the created HonorRollRuleGradeMarkGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRuleGradeMarkGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRuleGradeMarkGradeBucket <- function(HonorRollRuleGradeMarkID = NULL, GradeBucketID = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGradeMarkGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeBucket", body = list(DataObject = body), searchFields = append("HonorRollRuleGradeMarkGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRuleGradeMarkGradeBucket
	#'
	#' This function modifies a HonorRollRuleGradeMarkGradeBucket
	#' @param fieldNames The field values to give the modified HonorRollRuleGradeMarkGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRuleGradeMarkGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRuleGradeMarkGradeBucket <- function(HonorRollRuleGradeMarkGradeBucketID, HonorRollRuleGradeMarkID = NULL, GradeBucketID = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGradeMarkGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeBucket", objectId = HonorRollRuleGradeMarkGradeBucketID, body = list(DataObject = body), searchFields = append("HonorRollRuleGradeMarkGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRuleGradeMarkGradeMarks
	#'
	#' This function returns a dataframe or json object of HonorRollRuleGradeMarkGradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGradeMarkGradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGradeMarkGradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGradeMarkGradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRuleGradeMarkGradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRuleGradeMarkGradeMarks <- function(searchConditionsList = NULL, HonorRollRuleGradeMarkGradeMarkID = F, HonorRollRuleGradeMarkID = F, GradeMarkID = F, IsException = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGradeMarkGradeMarkIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRuleGradeMarkGradeMark
	#'
	#' This function returns a dataframe or json object of a HonorRollRuleGradeMarkGradeMark
	#' @param HonorRollRuleGradeMarkGradeMarkID The ID of the HonorRollRuleGradeMarkGradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuleGradeMarkGradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuleGradeMarkGradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRuleGradeMarkGradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRuleGradeMarkGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRuleGradeMarkGradeMark <- function(HonorRollRuleGradeMarkGradeMarkID, HonorRollRuleGradeMarkID = F, GradeMarkID = F, IsException = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRuleGradeMarkGradeMarkIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRuleGradeMarkGradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeMark", objectId = HonorRollRuleGradeMarkGradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRuleGradeMarkGradeMark
	#'
	#' This function deletes a HonorRollRuleGradeMarkGradeMark
	#' @param HonorRollRuleGradeMarkGradeMarkID The ID of the HonorRollRuleGradeMarkGradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRuleGradeMarkGradeMarkID of the deleted HonorRollRuleGradeMarkGradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRuleGradeMarkGradeMark <- function(HonorRollRuleGradeMarkGradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeMark", objectId = HonorRollRuleGradeMarkGradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRuleGradeMarkGradeMark
	#'
	#' This function creates a HonorRollRuleGradeMarkGradeMark
	#' @param fieldNames The field values to give the created HonorRollRuleGradeMarkGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRuleGradeMarkGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRuleGradeMarkGradeMark <- function(HonorRollRuleGradeMarkID = NULL, GradeMarkID = NULL, IsException = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGradeMarkGradeMarkIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeMark", body = list(DataObject = body), searchFields = append("HonorRollRuleGradeMarkGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRuleGradeMarkGradeMark
	#'
	#' This function modifies a HonorRollRuleGradeMarkGradeMark
	#' @param fieldNames The field values to give the modified HonorRollRuleGradeMarkGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRuleGradeMarkGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRuleGradeMarkGradeMark <- function(HonorRollRuleGradeMarkGradeMarkID, HonorRollRuleGradeMarkID = NULL, GradeMarkID = NULL, IsException = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRuleGradeMarkGradeMarkIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRuleGradeMarkGradeMark", objectId = HonorRollRuleGradeMarkGradeMarkID, body = list(DataObject = body), searchFields = append("HonorRollRuleGradeMarkGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRulePriorHonorRolls
	#'
	#' This function returns a dataframe or json object of HonorRollRulePriorHonorRolls
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRulePriorHonorRolls. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRulePriorHonorRolls.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRulePriorHonorRoll') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRulePriorHonorRolls
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRulePriorHonorRolls <- function(searchConditionsList = NULL, HonorRollRulePriorHonorRollID = F, HonorRollRunLevelRuleID = F, HonorRollLevelTotal = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRulePriorHonorRollIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRulePriorHonorRoll", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRulePriorHonorRoll
	#'
	#' This function returns a dataframe or json object of a HonorRollRulePriorHonorRoll
	#' @param HonorRollRulePriorHonorRollID The ID of the HonorRollRulePriorHonorRoll to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRulePriorHonorRoll. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRulePriorHonorRoll.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRulePriorHonorRoll') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRulePriorHonorRoll
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRulePriorHonorRoll <- function(HonorRollRulePriorHonorRollID, HonorRollRunLevelRuleID = F, HonorRollLevelTotal = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRulePriorHonorRollIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRulePriorHonorRollID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRoll", objectId = HonorRollRulePriorHonorRollID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRulePriorHonorRoll
	#'
	#' This function deletes a HonorRollRulePriorHonorRoll
	#' @param HonorRollRulePriorHonorRollID The ID of the HonorRollRulePriorHonorRoll to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRulePriorHonorRollID of the deleted HonorRollRulePriorHonorRoll.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRulePriorHonorRoll <- function(HonorRollRulePriorHonorRollID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRoll", objectId = HonorRollRulePriorHonorRollID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRulePriorHonorRoll
	#'
	#' This function creates a HonorRollRulePriorHonorRoll
	#' @param fieldNames The field values to give the created HonorRollRulePriorHonorRoll. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRulePriorHonorRoll
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRulePriorHonorRoll <- function(HonorRollRunLevelRuleID = NULL, HonorRollLevelTotal = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRulePriorHonorRollIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRoll", body = list(DataObject = body), searchFields = append("HonorRollRulePriorHonorRollID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRulePriorHonorRoll
	#'
	#' This function modifies a HonorRollRulePriorHonorRoll
	#' @param fieldNames The field values to give the modified HonorRollRulePriorHonorRoll. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRulePriorHonorRoll
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRulePriorHonorRoll <- function(HonorRollRulePriorHonorRollID, HonorRollRunLevelRuleID = NULL, HonorRollLevelTotal = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRulePriorHonorRollIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRoll", objectId = HonorRollRulePriorHonorRollID, body = list(DataObject = body), searchFields = append("HonorRollRulePriorHonorRollID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRulePriorHonorRollLevels
	#'
	#' This function returns a dataframe or json object of HonorRollRulePriorHonorRollLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRulePriorHonorRollLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRulePriorHonorRollLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRulePriorHonorRollLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRulePriorHonorRollLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRulePriorHonorRollLevels <- function(searchConditionsList = NULL, HonorRollRulePriorHonorRollLevelID = F, HonorRollRulePriorHonorRollID = F, HonorRollRunLevelHistoryID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRulePriorHonorRollLevelIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRulePriorHonorRollLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRulePriorHonorRollLevel
	#'
	#' This function returns a dataframe or json object of a HonorRollRulePriorHonorRollLevel
	#' @param HonorRollRulePriorHonorRollLevelID The ID of the HonorRollRulePriorHonorRollLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRulePriorHonorRollLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRulePriorHonorRollLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRulePriorHonorRollLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRulePriorHonorRollLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRulePriorHonorRollLevel <- function(HonorRollRulePriorHonorRollLevelID, HonorRollRulePriorHonorRollID = F, HonorRollRunLevelHistoryID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRulePriorHonorRollLevelIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRulePriorHonorRollLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRollLevel", objectId = HonorRollRulePriorHonorRollLevelID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRulePriorHonorRollLevel
	#'
	#' This function deletes a HonorRollRulePriorHonorRollLevel
	#' @param HonorRollRulePriorHonorRollLevelID The ID of the HonorRollRulePriorHonorRollLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRulePriorHonorRollLevelID of the deleted HonorRollRulePriorHonorRollLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRulePriorHonorRollLevel <- function(HonorRollRulePriorHonorRollLevelID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRollLevel", objectId = HonorRollRulePriorHonorRollLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRulePriorHonorRollLevel
	#'
	#' This function creates a HonorRollRulePriorHonorRollLevel
	#' @param fieldNames The field values to give the created HonorRollRulePriorHonorRollLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRulePriorHonorRollLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRulePriorHonorRollLevel <- function(HonorRollRulePriorHonorRollID = NULL, HonorRollRunLevelHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRulePriorHonorRollLevelIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRollLevel", body = list(DataObject = body), searchFields = append("HonorRollRulePriorHonorRollLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRulePriorHonorRollLevel
	#'
	#' This function modifies a HonorRollRulePriorHonorRollLevel
	#' @param fieldNames The field values to give the modified HonorRollRulePriorHonorRollLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRulePriorHonorRollLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRulePriorHonorRollLevel <- function(HonorRollRulePriorHonorRollLevelID, HonorRollRulePriorHonorRollID = NULL, HonorRollRunLevelHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRulePriorHonorRollLevelIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRulePriorHonorRollLevel", objectId = HonorRollRulePriorHonorRollLevelID, body = list(DataObject = body), searchFields = append("HonorRollRulePriorHonorRollLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRuns
	#'
	#' This function returns a dataframe or json object of HonorRollRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRuns <- function(searchConditionsList = NULL, HonorRollRunID = F, EntityID = F, SchoolYearID = F, Name = F, IsActive = F, AllowMultipleHonorRollLevels = F, EntityGroupKey = F, HonorRollRunIDClonedFrom = F, ContainsGPARule = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayGPAForHonorRoll = F, GPAMethodIDToDisplay = F, GPABucketIDToDisplay = F, GPATypeToDisplay = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRun
	#'
	#' This function returns a dataframe or json object of a HonorRollRun
	#' @param HonorRollRunID The ID of the HonorRollRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRun <- function(HonorRollRunID, EntityID = F, SchoolYearID = F, Name = F, IsActive = F, AllowMultipleHonorRollLevels = F, EntityGroupKey = F, HonorRollRunIDClonedFrom = F, ContainsGPARule = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayGPAForHonorRoll = F, GPAMethodIDToDisplay = F, GPABucketIDToDisplay = F, GPATypeToDisplay = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRun", objectId = HonorRollRunID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRun
	#'
	#' This function deletes a HonorRollRun
	#' @param HonorRollRunID The ID of the HonorRollRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRunID of the deleted HonorRollRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRun <- function(HonorRollRunID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRun", objectId = HonorRollRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRun
	#'
	#' This function creates a HonorRollRun
	#' @param fieldNames The field values to give the created HonorRollRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRun <- function(EntityID = NULL, SchoolYearID = NULL, Name = NULL, IsActive = NULL, AllowMultipleHonorRollLevels = NULL, EntityGroupKey = NULL, HonorRollRunIDClonedFrom = NULL, DisplayGPAForHonorRoll = NULL, GPAMethodIDToDisplay = NULL, GPABucketIDToDisplay = NULL, GPATypeToDisplay = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRun", body = list(DataObject = body), searchFields = append("HonorRollRunID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRun
	#'
	#' This function modifies a HonorRollRun
	#' @param fieldNames The field values to give the modified HonorRollRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRun <- function(HonorRollRunID, EntityID = NULL, SchoolYearID = NULL, Name = NULL, IsActive = NULL, AllowMultipleHonorRollLevels = NULL, EntityGroupKey = NULL, HonorRollRunIDClonedFrom = NULL, DisplayGPAForHonorRoll = NULL, GPAMethodIDToDisplay = NULL, GPABucketIDToDisplay = NULL, GPATypeToDisplay = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRun", objectId = HonorRollRunID, body = list(DataObject = body), searchFields = append("HonorRollRunID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRunHistories
	#'
	#' This function returns a dataframe or json object of HonorRollRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRunHistories <- function(searchConditionsList = NULL, HonorRollRunHistoryID = F, HonorRollRunID = F, StudentFilterParameter = F, Date = F, GPAAsOfDate = F, Description = F, Calculation = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRunHistory
	#'
	#' This function returns a dataframe or json object of a HonorRollRunHistory
	#' @param HonorRollRunHistoryID The ID of the HonorRollRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRunHistory <- function(HonorRollRunHistoryID, HonorRollRunID = F, StudentFilterParameter = F, Date = F, GPAAsOfDate = F, Description = F, Calculation = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRunHistory", objectId = HonorRollRunHistoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRunHistory
	#'
	#' This function deletes a HonorRollRunHistory
	#' @param HonorRollRunHistoryID The ID of the HonorRollRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRunHistoryID of the deleted HonorRollRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRunHistory <- function(HonorRollRunHistoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRunHistory", objectId = HonorRollRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRunHistory
	#'
	#' This function creates a HonorRollRunHistory
	#' @param fieldNames The field values to give the created HonorRollRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRunHistory <- function(HonorRollRunID = NULL, StudentFilterParameter = NULL, Date = NULL, GPAAsOfDate = NULL, Description = NULL, Calculation = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRunHistory", body = list(DataObject = body), searchFields = append("HonorRollRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRunHistory
	#'
	#' This function modifies a HonorRollRunHistory
	#' @param fieldNames The field values to give the modified HonorRollRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRunHistory <- function(HonorRollRunHistoryID, HonorRollRunID = NULL, StudentFilterParameter = NULL, Date = NULL, GPAAsOfDate = NULL, Description = NULL, Calculation = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRunHistory", objectId = HonorRollRunHistoryID, body = list(DataObject = body), searchFields = append("HonorRollRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRunLevels
	#'
	#' This function returns a dataframe or json object of HonorRollRunLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRunLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRunLevels <- function(searchConditionsList = NULL, HonorRollRunLevelID = F, HonorRollRunID = F, Name = F, Order = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRunLevelIDClonedFrom = F, HonorRollRunNameName = F, RulesParameterDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRunLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRunLevel
	#'
	#' This function returns a dataframe or json object of a HonorRollRunLevel
	#' @param HonorRollRunLevelID The ID of the HonorRollRunLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRunLevel <- function(HonorRollRunLevelID, HonorRollRunID = F, Name = F, Order = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRunLevelIDClonedFrom = F, HonorRollRunNameName = F, RulesParameterDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRunLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRunLevel", objectId = HonorRollRunLevelID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRunLevel
	#'
	#' This function deletes a HonorRollRunLevel
	#' @param HonorRollRunLevelID The ID of the HonorRollRunLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRunLevelID of the deleted HonorRollRunLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRunLevel <- function(HonorRollRunLevelID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRunLevel", objectId = HonorRollRunLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRunLevel
	#'
	#' This function creates a HonorRollRunLevel
	#' @param fieldNames The field values to give the created HonorRollRunLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRunLevel <- function(HonorRollRunID = NULL, Name = NULL, Order = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRunLevelIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRunLevel", body = list(DataObject = body), searchFields = append("HonorRollRunLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRunLevel
	#'
	#' This function modifies a HonorRollRunLevel
	#' @param fieldNames The field values to give the modified HonorRollRunLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRunLevel <- function(HonorRollRunLevelID, HonorRollRunID = NULL, Name = NULL, Order = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRunLevelIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRunLevel", objectId = HonorRollRunLevelID, body = list(DataObject = body), searchFields = append("HonorRollRunLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRunLevelHistories
	#'
	#' This function returns a dataframe or json object of HonorRollRunLevelHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunLevelHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunLevelHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunLevelHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRunLevelHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRunLevelHistories <- function(searchConditionsList = NULL, HonorRollRunLevelHistoryID = F, HonorRollRunHistoryID = F, HonorRollRunLevelID = F, ParameterDescription = F, EntitySchoolYearHonorRollRunLevelName = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRunLevelHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRunLevelHistory
	#'
	#' This function returns a dataframe or json object of a HonorRollRunLevelHistory
	#' @param HonorRollRunLevelHistoryID The ID of the HonorRollRunLevelHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunLevelHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunLevelHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunLevelHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRunLevelHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRunLevelHistory <- function(HonorRollRunLevelHistoryID, HonorRollRunHistoryID = F, HonorRollRunLevelID = F, ParameterDescription = F, EntitySchoolYearHonorRollRunLevelName = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRunLevelHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRunLevelHistory", objectId = HonorRollRunLevelHistoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRunLevelHistory
	#'
	#' This function deletes a HonorRollRunLevelHistory
	#' @param HonorRollRunLevelHistoryID The ID of the HonorRollRunLevelHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRunLevelHistoryID of the deleted HonorRollRunLevelHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRunLevelHistory <- function(HonorRollRunLevelHistoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRunLevelHistory", objectId = HonorRollRunLevelHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRunLevelHistory
	#'
	#' This function creates a HonorRollRunLevelHistory
	#' @param fieldNames The field values to give the created HonorRollRunLevelHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRunLevelHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRunLevelHistory <- function(HonorRollRunHistoryID = NULL, HonorRollRunLevelID = NULL, ParameterDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRunLevelHistory", body = list(DataObject = body), searchFields = append("HonorRollRunLevelHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRunLevelHistory
	#'
	#' This function modifies a HonorRollRunLevelHistory
	#' @param fieldNames The field values to give the modified HonorRollRunLevelHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRunLevelHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRunLevelHistory <- function(HonorRollRunLevelHistoryID, HonorRollRunHistoryID = NULL, HonorRollRunLevelID = NULL, ParameterDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRunLevelHistory", objectId = HonorRollRunLevelHistoryID, body = list(DataObject = body), searchFields = append("HonorRollRunLevelHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List HonorRollRunLevelRules
	#'
	#' This function returns a dataframe or json object of HonorRollRunLevelRules
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunLevelRules. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunLevelRules.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunLevelRule') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of HonorRollRunLevelRules
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listHonorRollRunLevelRules <- function(searchConditionsList = NULL, HonorRollRunLevelRuleID = F, HonorRollRunLevelID = F, ParameterDescription = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRunLevelRuleIDClonedFrom = F, RuleType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Order = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "HonorRollRunLevelRule", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a HonorRollRunLevelRule
	#'
	#' This function returns a dataframe or json object of a HonorRollRunLevelRule
	#' @param HonorRollRunLevelRuleID The ID of the HonorRollRunLevelRule to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given HonorRollRunLevelRule. Defaults to FALSE for all return fields which, for convenience, returns all fields for the HonorRollRunLevelRule.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('HonorRollRunLevelRule') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of HonorRollRunLevelRule
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getHonorRollRunLevelRule <- function(HonorRollRunLevelRuleID, HonorRollRunLevelID = F, ParameterDescription = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, HonorRollRunLevelRuleIDClonedFrom = F, RuleType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Order = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "HonorRollRunLevelRuleID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "HonorRollRunLevelRule", objectId = HonorRollRunLevelRuleID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a HonorRollRunLevelRule
	#'
	#' This function deletes a HonorRollRunLevelRule
	#' @param HonorRollRunLevelRuleID The ID of the HonorRollRunLevelRule to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The HonorRollRunLevelRuleID of the deleted HonorRollRunLevelRule.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteHonorRollRunLevelRule <- function(HonorRollRunLevelRuleID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "HonorRollRunLevelRule", objectId = HonorRollRunLevelRuleID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a HonorRollRunLevelRule
	#'
	#' This function creates a HonorRollRunLevelRule
	#' @param fieldNames The field values to give the created HonorRollRunLevelRule. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created HonorRollRunLevelRule
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createHonorRollRunLevelRule <- function(HonorRollRunLevelID = NULL, ParameterDescription = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRunLevelRuleIDClonedFrom = NULL, RuleType = NULL, Order = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "HonorRollRunLevelRule", body = list(DataObject = body), searchFields = append("HonorRollRunLevelRuleID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a HonorRollRunLevelRule
	#'
	#' This function modifies a HonorRollRunLevelRule
	#' @param fieldNames The field values to give the modified HonorRollRunLevelRule. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified HonorRollRunLevelRule
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyHonorRollRunLevelRule <- function(HonorRollRunLevelRuleID, HonorRollRunLevelID = NULL, ParameterDescription = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, HonorRollRunLevelRuleIDClonedFrom = NULL, RuleType = NULL, Order = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "HonorRollRunLevelRule", objectId = HonorRollRunLevelRuleID, body = list(DataObject = body), searchFields = append("HonorRollRunLevelRuleID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentHonorRollRunLevels
	#'
	#' This function returns a dataframe or json object of StudentHonorRollRunLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentHonorRollRunLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentHonorRollRunLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentHonorRollRunLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentHonorRollRunLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentHonorRollRunLevels <- function(searchConditionsList = NULL, StudentHonorRollRunLevelID = F, StudentID = F, HonorRollRunLevelHistoryID = F, GPAValue = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HonorRollNameHonoRollRunLevelName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentHonorRollRunLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentHonorRollRunLevel
	#'
	#' This function returns a dataframe or json object of a StudentHonorRollRunLevel
	#' @param StudentHonorRollRunLevelID The ID of the StudentHonorRollRunLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentHonorRollRunLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentHonorRollRunLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentHonorRollRunLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentHonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentHonorRollRunLevel <- function(StudentHonorRollRunLevelID, StudentID = F, HonorRollRunLevelHistoryID = F, GPAValue = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HonorRollNameHonoRollRunLevelName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentHonorRollRunLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentHonorRollRunLevel", objectId = StudentHonorRollRunLevelID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentHonorRollRunLevel
	#'
	#' This function deletes a StudentHonorRollRunLevel
	#' @param StudentHonorRollRunLevelID The ID of the StudentHonorRollRunLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentHonorRollRunLevelID of the deleted StudentHonorRollRunLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentHonorRollRunLevel <- function(StudentHonorRollRunLevelID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentHonorRollRunLevel", objectId = StudentHonorRollRunLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentHonorRollRunLevel
	#'
	#' This function creates a StudentHonorRollRunLevel
	#' @param fieldNames The field values to give the created StudentHonorRollRunLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentHonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentHonorRollRunLevel <- function(StudentID = NULL, HonorRollRunLevelHistoryID = NULL, GPAValue = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentHonorRollRunLevel", body = list(DataObject = body), searchFields = append("StudentHonorRollRunLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentHonorRollRunLevel
	#'
	#' This function modifies a StudentHonorRollRunLevel
	#' @param fieldNames The field values to give the modified StudentHonorRollRunLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentHonorRollRunLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentHonorRollRunLevel <- function(StudentHonorRollRunLevelID, StudentID = NULL, HonorRollRunLevelHistoryID = NULL, GPAValue = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentHonorRollRunLevel", objectId = StudentHonorRollRunLevelID, body = list(DataObject = body), searchFields = append("StudentHonorRollRunLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentAttendanceTerms
	#'
	#' This function returns a dataframe or json object of GradeReportStudentAttendanceTerms
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentAttendanceTerms. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentAttendanceTerms.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentAttendanceTerm') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentAttendanceTerms
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentAttendanceTerms <- function(searchConditionsList = NULL, GradeReportStudentAttendanceTermID = F, GradeReportAcademicSessionID = F, AttendanceTermCode = F, SortNumber = F, DaysAbsent = F, DaysExcused = F, DaysUnexcused = F, DaysTardy = F, DaysOther = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentAttendanceTerm", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentAttendanceTerm
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentAttendanceTerm
	#' @param GradeReportStudentAttendanceTermID The ID of the GradeReportStudentAttendanceTerm to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentAttendanceTerm. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentAttendanceTerm.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentAttendanceTerm') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentAttendanceTerm <- function(GradeReportStudentAttendanceTermID, GradeReportAcademicSessionID = F, AttendanceTermCode = F, SortNumber = F, DaysAbsent = F, DaysExcused = F, DaysUnexcused = F, DaysTardy = F, DaysOther = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentAttendanceTermID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentAttendanceTerm", objectId = GradeReportStudentAttendanceTermID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentAttendanceTerm
	#'
	#' This function deletes a GradeReportStudentAttendanceTerm
	#' @param GradeReportStudentAttendanceTermID The ID of the GradeReportStudentAttendanceTerm to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentAttendanceTermID of the deleted GradeReportStudentAttendanceTerm.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentAttendanceTerm <- function(GradeReportStudentAttendanceTermID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentAttendanceTerm", objectId = GradeReportStudentAttendanceTermID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentAttendanceTerm
	#'
	#' This function creates a GradeReportStudentAttendanceTerm
	#' @param fieldNames The field values to give the created GradeReportStudentAttendanceTerm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentAttendanceTerm <- function(GradeReportAcademicSessionID = NULL, AttendanceTermCode = NULL, SortNumber = NULL, DaysAbsent = NULL, DaysExcused = NULL, DaysUnexcused = NULL, DaysTardy = NULL, DaysOther = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentAttendanceTerm", body = list(DataObject = body), searchFields = append("GradeReportStudentAttendanceTermID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentAttendanceTerm
	#'
	#' This function modifies a GradeReportStudentAttendanceTerm
	#' @param fieldNames The field values to give the modified GradeReportStudentAttendanceTerm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentAttendanceTerm <- function(GradeReportStudentAttendanceTermID, GradeReportAcademicSessionID = NULL, AttendanceTermCode = NULL, SortNumber = NULL, DaysAbsent = NULL, DaysExcused = NULL, DaysUnexcused = NULL, DaysTardy = NULL, DaysOther = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentAttendanceTerm", objectId = GradeReportStudentAttendanceTermID, body = list(DataObject = body), searchFields = append("GradeReportStudentAttendanceTermID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateCommentSets
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateCommentSets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateCommentSets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateCommentSets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateCommentSet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateCommentSets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateCommentSets <- function(searchConditionsList = NULL, GradeReportTemplateCommentSetID = F, GradeReportTemplateID = F, CommentSetID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateCommentSetIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateCommentSet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateCommentSet
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateCommentSet
	#' @param GradeReportTemplateCommentSetID The ID of the GradeReportTemplateCommentSet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateCommentSet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateCommentSet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateCommentSet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateCommentSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateCommentSet <- function(GradeReportTemplateCommentSetID, GradeReportTemplateID = F, CommentSetID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateCommentSetIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateCommentSetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateCommentSet", objectId = GradeReportTemplateCommentSetID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateCommentSet
	#'
	#' This function deletes a GradeReportTemplateCommentSet
	#' @param GradeReportTemplateCommentSetID The ID of the GradeReportTemplateCommentSet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateCommentSetID of the deleted GradeReportTemplateCommentSet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateCommentSet <- function(GradeReportTemplateCommentSetID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateCommentSet", objectId = GradeReportTemplateCommentSetID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateCommentSet
	#'
	#' This function creates a GradeReportTemplateCommentSet
	#' @param fieldNames The field values to give the created GradeReportTemplateCommentSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateCommentSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateCommentSet <- function(GradeReportTemplateID = NULL, CommentSetID = NULL, GradeReportTemplateCommentSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateCommentSet", body = list(DataObject = body), searchFields = append("GradeReportTemplateCommentSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateCommentSet
	#'
	#' This function modifies a GradeReportTemplateCommentSet
	#' @param fieldNames The field values to give the modified GradeReportTemplateCommentSet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateCommentSet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateCommentSet <- function(GradeReportTemplateCommentSetID, GradeReportTemplateID = NULL, CommentSetID = NULL, GradeReportTemplateCommentSetIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateCommentSet", objectId = GradeReportTemplateCommentSetID, body = list(DataObject = body), searchFields = append("GradeReportTemplateCommentSetID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportAcademicSessions
	#'
	#' This function returns a dataframe or json object of GradeReportAcademicSessions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSession') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportAcademicSessions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportAcademicSessions <- function(searchConditionsList = NULL, GradeReportAcademicSessionID = F, GradeReportStudentID = F, SchoolYearID = F, SortNumber = F, GradeLevelCode = F, EarnedCredit = F, EarnedCreditAttempted = F, EntryDate = F, WithdrawalDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GPAValue = F, EarnedCreditsValue = F, HeaderDescription = F, SchoolYearDescription = F, GradeReportAcademicSessionTemplateGroupID = F, DaysEnrolledYTD = F, DaysExcusedYTD = F, DaysUnexcusedYTD = F, DaysAbsentYTD = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportAcademicSession", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportAcademicSession
	#'
	#' This function returns a dataframe or json object of a GradeReportAcademicSession
	#' @param GradeReportAcademicSessionID The ID of the GradeReportAcademicSession to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSession. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSession.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSession') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportAcademicSession
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportAcademicSession <- function(GradeReportAcademicSessionID, GradeReportStudentID = F, SchoolYearID = F, SortNumber = F, GradeLevelCode = F, EarnedCredit = F, EarnedCreditAttempted = F, EntryDate = F, WithdrawalDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GPAValue = F, EarnedCreditsValue = F, HeaderDescription = F, SchoolYearDescription = F, GradeReportAcademicSessionTemplateGroupID = F, DaysEnrolledYTD = F, DaysExcusedYTD = F, DaysUnexcusedYTD = F, DaysAbsentYTD = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportAcademicSessionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportAcademicSession", objectId = GradeReportAcademicSessionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportAcademicSession
	#'
	#' This function deletes a GradeReportAcademicSession
	#' @param GradeReportAcademicSessionID The ID of the GradeReportAcademicSession to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportAcademicSessionID of the deleted GradeReportAcademicSession.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportAcademicSession <- function(GradeReportAcademicSessionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportAcademicSession", objectId = GradeReportAcademicSessionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportAcademicSession
	#'
	#' This function creates a GradeReportAcademicSession
	#' @param fieldNames The field values to give the created GradeReportAcademicSession. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportAcademicSession
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportAcademicSession <- function(GradeReportStudentID = NULL, SchoolYearID = NULL, SortNumber = NULL, GradeLevelCode = NULL, EarnedCredit = NULL, EarnedCreditAttempted = NULL, EntryDate = NULL, WithdrawalDate = NULL, GPAValue = NULL, EarnedCreditsValue = NULL, HeaderDescription = NULL, SchoolYearDescription = NULL, GradeReportAcademicSessionTemplateGroupID = NULL, DaysEnrolledYTD = NULL, DaysExcusedYTD = NULL, DaysUnexcusedYTD = NULL, DaysAbsentYTD = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportAcademicSession", body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportAcademicSession
	#'
	#' This function modifies a GradeReportAcademicSession
	#' @param fieldNames The field values to give the modified GradeReportAcademicSession. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportAcademicSession
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportAcademicSession <- function(GradeReportAcademicSessionID, GradeReportStudentID = NULL, SchoolYearID = NULL, SortNumber = NULL, GradeLevelCode = NULL, EarnedCredit = NULL, EarnedCreditAttempted = NULL, EntryDate = NULL, WithdrawalDate = NULL, GPAValue = NULL, EarnedCreditsValue = NULL, HeaderDescription = NULL, SchoolYearDescription = NULL, GradeReportAcademicSessionTemplateGroupID = NULL, DaysEnrolledYTD = NULL, DaysExcusedYTD = NULL, DaysUnexcusedYTD = NULL, DaysAbsentYTD = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportAcademicSession", objectId = GradeReportAcademicSessionID, body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportColumnGroups
	#'
	#' This function returns a dataframe or json object of GradeReportColumnGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportColumnGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportColumnGroups <- function(searchConditionsList = NULL, GradeReportColumnGroupID = F, ConfigDistrictYearID = F, Description = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnGroupIDClonedFrom = F, AlwaysDisplayGradingColumns = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportColumnGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportColumnGroup
	#'
	#' This function returns a dataframe or json object of a GradeReportColumnGroup
	#' @param GradeReportColumnGroupID The ID of the GradeReportColumnGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportColumnGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportColumnGroup <- function(GradeReportColumnGroupID, ConfigDistrictYearID = F, Description = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnGroupIDClonedFrom = F, AlwaysDisplayGradingColumns = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportColumnGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportColumnGroup", objectId = GradeReportColumnGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportColumnGroup
	#'
	#' This function deletes a GradeReportColumnGroup
	#' @param GradeReportColumnGroupID The ID of the GradeReportColumnGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportColumnGroupID of the deleted GradeReportColumnGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportColumnGroup <- function(GradeReportColumnGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportColumnGroup", objectId = GradeReportColumnGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportColumnGroup
	#'
	#' This function creates a GradeReportColumnGroup
	#' @param fieldNames The field values to give the created GradeReportColumnGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportColumnGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportColumnGroup <- function(ConfigDistrictYearID = NULL, Description = NULL, GradeReportColumnGroupIDClonedFrom = NULL, AlwaysDisplayGradingColumns = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportColumnGroup", body = list(DataObject = body), searchFields = append("GradeReportColumnGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportColumnGroup
	#'
	#' This function modifies a GradeReportColumnGroup
	#' @param fieldNames The field values to give the modified GradeReportColumnGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportColumnGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportColumnGroup <- function(GradeReportColumnGroupID, ConfigDistrictYearID = NULL, Description = NULL, GradeReportColumnGroupIDClonedFrom = NULL, AlwaysDisplayGradingColumns = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportColumnGroup", objectId = GradeReportColumnGroupID, body = list(DataObject = body), searchFields = append("GradeReportColumnGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportColumnGroupColumns
	#'
	#' This function returns a dataframe or json object of GradeReportColumnGroupColumns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnGroupColumns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnGroupColumns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnGroupColumn') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportColumnGroupColumns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportColumnGroupColumns <- function(searchConditionsList = NULL, GradeReportColumnGroupColumnID = F, GradeReportColumnGroupID = F, SortNumber = F, ColumnType = F, ColumnHeader = F, ContinueIfBlank = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnGroupColumnIDClonedFrom = F, AttendanceOption = F, DynamicRelationshipID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportColumnGroupColumn", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportColumnGroupColumn
	#'
	#' This function returns a dataframe or json object of a GradeReportColumnGroupColumn
	#' @param GradeReportColumnGroupColumnID The ID of the GradeReportColumnGroupColumn to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnGroupColumn. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnGroupColumn.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnGroupColumn') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportColumnGroupColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportColumnGroupColumn <- function(GradeReportColumnGroupColumnID, GradeReportColumnGroupID = F, SortNumber = F, ColumnType = F, ColumnHeader = F, ContinueIfBlank = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnGroupColumnIDClonedFrom = F, AttendanceOption = F, DynamicRelationshipID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportColumnGroupColumnID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportColumnGroupColumn", objectId = GradeReportColumnGroupColumnID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportColumnGroupColumn
	#'
	#' This function deletes a GradeReportColumnGroupColumn
	#' @param GradeReportColumnGroupColumnID The ID of the GradeReportColumnGroupColumn to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportColumnGroupColumnID of the deleted GradeReportColumnGroupColumn.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportColumnGroupColumn <- function(GradeReportColumnGroupColumnID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportColumnGroupColumn", objectId = GradeReportColumnGroupColumnID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportColumnGroupColumn
	#'
	#' This function creates a GradeReportColumnGroupColumn
	#' @param fieldNames The field values to give the created GradeReportColumnGroupColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportColumnGroupColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportColumnGroupColumn <- function(GradeReportColumnGroupID = NULL, SortNumber = NULL, ColumnType = NULL, ColumnHeader = NULL, ContinueIfBlank = NULL, GradeReportColumnGroupColumnIDClonedFrom = NULL, AttendanceOption = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportColumnGroupColumn", body = list(DataObject = body), searchFields = append("GradeReportColumnGroupColumnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportColumnGroupColumn
	#'
	#' This function modifies a GradeReportColumnGroupColumn
	#' @param fieldNames The field values to give the modified GradeReportColumnGroupColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportColumnGroupColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportColumnGroupColumn <- function(GradeReportColumnGroupColumnID, GradeReportColumnGroupID = NULL, SortNumber = NULL, ColumnType = NULL, ColumnHeader = NULL, ContinueIfBlank = NULL, GradeReportColumnGroupColumnIDClonedFrom = NULL, AttendanceOption = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportColumnGroupColumn", objectId = GradeReportColumnGroupColumnID, body = list(DataObject = body), searchFields = append("GradeReportColumnGroupColumnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateGPABuckets
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateGPABuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateGPABuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateGPABuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateGPABucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateGPABuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateGPABuckets <- function(searchConditionsList = NULL, GradeReportTemplateGPABucketID = F, GradeReportTemplateID = F, GPABucketID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateGPABucketIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateGPABucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateGPABucket
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateGPABucket
	#' @param GradeReportTemplateGPABucketID The ID of the GradeReportTemplateGPABucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateGPABucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateGPABucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateGPABucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateGPABucket <- function(GradeReportTemplateGPABucketID, GradeReportTemplateID = F, GPABucketID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateGPABucketIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateGPABucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateGPABucket", objectId = GradeReportTemplateGPABucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateGPABucket
	#'
	#' This function deletes a GradeReportTemplateGPABucket
	#' @param GradeReportTemplateGPABucketID The ID of the GradeReportTemplateGPABucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateGPABucketID of the deleted GradeReportTemplateGPABucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateGPABucket <- function(GradeReportTemplateGPABucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateGPABucket", objectId = GradeReportTemplateGPABucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateGPABucket
	#'
	#' This function creates a GradeReportTemplateGPABucket
	#' @param fieldNames The field values to give the created GradeReportTemplateGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateGPABucket <- function(GradeReportTemplateID = NULL, GPABucketID = NULL, GradeReportTemplateGPABucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateGPABucket", body = list(DataObject = body), searchFields = append("GradeReportTemplateGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateGPABucket
	#'
	#' This function modifies a GradeReportTemplateGPABucket
	#' @param fieldNames The field values to give the modified GradeReportTemplateGPABucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateGPABucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateGPABucket <- function(GradeReportTemplateGPABucketID, GradeReportTemplateID = NULL, GPABucketID = NULL, GradeReportTemplateGPABucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateGPABucket", objectId = GradeReportTemplateGPABucketID, body = list(DataObject = body), searchFields = append("GradeReportTemplateGPABucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateHeaderColumns
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateHeaderColumns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateHeaderColumns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateHeaderColumns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateHeaderColumn') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateHeaderColumns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateHeaderColumns <- function(searchConditionsList = NULL, GradeReportTemplateHeaderColumnID = F, GradeReportTemplateHeaderRowID = F, SortNumber = F, FieldType = F, LabelOverride = F, FreeformText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, RankMethodID = F, GPAMethodID = F, GPABucketID = F, GradeReportTemplateHeaderColumnIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateHeaderColumn", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateHeaderColumn
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateHeaderColumn
	#' @param GradeReportTemplateHeaderColumnID The ID of the GradeReportTemplateHeaderColumn to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateHeaderColumn. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateHeaderColumn.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateHeaderColumn') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateHeaderColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateHeaderColumn <- function(GradeReportTemplateHeaderColumnID, GradeReportTemplateHeaderRowID = F, SortNumber = F, FieldType = F, LabelOverride = F, FreeformText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, RankMethodID = F, GPAMethodID = F, GPABucketID = F, GradeReportTemplateHeaderColumnIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateHeaderColumnID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderColumn", objectId = GradeReportTemplateHeaderColumnID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateHeaderColumn
	#'
	#' This function deletes a GradeReportTemplateHeaderColumn
	#' @param GradeReportTemplateHeaderColumnID The ID of the GradeReportTemplateHeaderColumn to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateHeaderColumnID of the deleted GradeReportTemplateHeaderColumn.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateHeaderColumn <- function(GradeReportTemplateHeaderColumnID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderColumn", objectId = GradeReportTemplateHeaderColumnID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateHeaderColumn
	#'
	#' This function creates a GradeReportTemplateHeaderColumn
	#' @param fieldNames The field values to give the created GradeReportTemplateHeaderColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateHeaderColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateHeaderColumn <- function(GradeReportTemplateHeaderRowID = NULL, SortNumber = NULL, FieldType = NULL, LabelOverride = NULL, FreeformText = NULL, RankMethodID = NULL, GPAMethodID = NULL, GPABucketID = NULL, GradeReportTemplateHeaderColumnIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderColumn", body = list(DataObject = body), searchFields = append("GradeReportTemplateHeaderColumnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateHeaderColumn
	#'
	#' This function modifies a GradeReportTemplateHeaderColumn
	#' @param fieldNames The field values to give the modified GradeReportTemplateHeaderColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateHeaderColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateHeaderColumn <- function(GradeReportTemplateHeaderColumnID, GradeReportTemplateHeaderRowID = NULL, SortNumber = NULL, FieldType = NULL, LabelOverride = NULL, FreeformText = NULL, RankMethodID = NULL, GPAMethodID = NULL, GPABucketID = NULL, GradeReportTemplateHeaderColumnIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderColumn", objectId = GradeReportTemplateHeaderColumnID, body = list(DataObject = body), searchFields = append("GradeReportTemplateHeaderColumnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateGPAMethods
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateGPAMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateGPAMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateGPAMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateGPAMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateGPAMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateGPAMethods <- function(searchConditionsList = NULL, GradeReportTemplateGPAMethodID = F, GradeReportTemplateID = F, GPAMethodID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateGPAMethodIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateGPAMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateGPAMethod
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateGPAMethod
	#' @param GradeReportTemplateGPAMethodID The ID of the GradeReportTemplateGPAMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateGPAMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateGPAMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateGPAMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateGPAMethod <- function(GradeReportTemplateGPAMethodID, GradeReportTemplateID = F, GPAMethodID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateGPAMethodIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateGPAMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateGPAMethod", objectId = GradeReportTemplateGPAMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateGPAMethod
	#'
	#' This function deletes a GradeReportTemplateGPAMethod
	#' @param GradeReportTemplateGPAMethodID The ID of the GradeReportTemplateGPAMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateGPAMethodID of the deleted GradeReportTemplateGPAMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateGPAMethod <- function(GradeReportTemplateGPAMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateGPAMethod", objectId = GradeReportTemplateGPAMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateGPAMethod
	#'
	#' This function creates a GradeReportTemplateGPAMethod
	#' @param fieldNames The field values to give the created GradeReportTemplateGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateGPAMethod <- function(GradeReportTemplateID = NULL, GPAMethodID = NULL, GradeReportTemplateGPAMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateGPAMethod", body = list(DataObject = body), searchFields = append("GradeReportTemplateGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateGPAMethod
	#'
	#' This function modifies a GradeReportTemplateGPAMethod
	#' @param fieldNames The field values to give the modified GradeReportTemplateGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateGPAMethod <- function(GradeReportTemplateGPAMethodID, GradeReportTemplateID = NULL, GPAMethodID = NULL, GradeReportTemplateGPAMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateGPAMethod", objectId = GradeReportTemplateGPAMethodID, body = list(DataObject = body), searchFields = append("GradeReportTemplateGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateHeaderRows
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateHeaderRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateHeaderRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateHeaderRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateHeaderRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateHeaderRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateHeaderRows <- function(searchConditionsList = NULL, GradeReportTemplateHeaderRowID = F, GradeReportTemplateID = F, SortNumber = F, ColumnCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateHeaderRowIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateHeaderRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateHeaderRow
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateHeaderRow
	#' @param GradeReportTemplateHeaderRowID The ID of the GradeReportTemplateHeaderRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateHeaderRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateHeaderRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateHeaderRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateHeaderRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateHeaderRow <- function(GradeReportTemplateHeaderRowID, GradeReportTemplateID = F, SortNumber = F, ColumnCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateHeaderRowIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateHeaderRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderRow", objectId = GradeReportTemplateHeaderRowID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateHeaderRow
	#'
	#' This function deletes a GradeReportTemplateHeaderRow
	#' @param GradeReportTemplateHeaderRowID The ID of the GradeReportTemplateHeaderRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateHeaderRowID of the deleted GradeReportTemplateHeaderRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateHeaderRow <- function(GradeReportTemplateHeaderRowID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderRow", objectId = GradeReportTemplateHeaderRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateHeaderRow
	#'
	#' This function creates a GradeReportTemplateHeaderRow
	#' @param fieldNames The field values to give the created GradeReportTemplateHeaderRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateHeaderRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateHeaderRow <- function(GradeReportTemplateID = NULL, SortNumber = NULL, ColumnCount = NULL, GradeReportTemplateHeaderRowIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderRow", body = list(DataObject = body), searchFields = append("GradeReportTemplateHeaderRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateHeaderRow
	#'
	#' This function modifies a GradeReportTemplateHeaderRow
	#' @param fieldNames The field values to give the modified GradeReportTemplateHeaderRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateHeaderRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateHeaderRow <- function(GradeReportTemplateHeaderRowID, GradeReportTemplateID = NULL, SortNumber = NULL, ColumnCount = NULL, GradeReportTemplateHeaderRowIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateHeaderRow", objectId = GradeReportTemplateHeaderRowID, body = list(DataObject = body), searchFields = append("GradeReportTemplateHeaderRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateGradeMarks
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateGradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateGradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateGradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateGradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateGradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateGradeMarks <- function(searchConditionsList = NULL, GradeReportTemplateGradeMarkID = F, GradeReportTemplateID = F, GradeMarkID = F, Type = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateGradeMarkIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateGradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateGradeMark
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateGradeMark
	#' @param GradeReportTemplateGradeMarkID The ID of the GradeReportTemplateGradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateGradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateGradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateGradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateGradeMark <- function(GradeReportTemplateGradeMarkID, GradeReportTemplateID = F, GradeMarkID = F, Type = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateGradeMarkIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateGradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateGradeMark", objectId = GradeReportTemplateGradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateGradeMark
	#'
	#' This function deletes a GradeReportTemplateGradeMark
	#' @param GradeReportTemplateGradeMarkID The ID of the GradeReportTemplateGradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateGradeMarkID of the deleted GradeReportTemplateGradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateGradeMark <- function(GradeReportTemplateGradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateGradeMark", objectId = GradeReportTemplateGradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateGradeMark
	#'
	#' This function creates a GradeReportTemplateGradeMark
	#' @param fieldNames The field values to give the created GradeReportTemplateGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateGradeMark <- function(GradeReportTemplateID = NULL, GradeMarkID = NULL, Type = NULL, GradeReportTemplateGradeMarkIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateGradeMark", body = list(DataObject = body), searchFields = append("GradeReportTemplateGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateGradeMark
	#'
	#' This function modifies a GradeReportTemplateGradeMark
	#' @param fieldNames The field values to give the modified GradeReportTemplateGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateGradeMark <- function(GradeReportTemplateGradeMarkID, GradeReportTemplateID = NULL, GradeMarkID = NULL, Type = NULL, GradeReportTemplateGradeMarkIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateGradeMark", objectId = GradeReportTemplateGradeMarkID, body = list(DataObject = body), searchFields = append("GradeReportTemplateGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplates
	#'
	#' This function returns a dataframe or json object of GradeReportTemplates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplates <- function(searchConditionsList = NULL, GradeReportTemplateID = F, ConfigEntityYearID = F, GradeReportColumnGroupIDSecondary = F, ReportRunInfoID = F, MediaIDLogo = F, HasLogo = F, Description = F, TemplateType = F, UseFreeFormGradingLegend = F, FreeFormGradingLegend = F, UseGradeMarkDescriptionGradingLegend = F, UseGradeMarkRangeGradingLegend = F, UseStudentSectionLinking = F, TeacherNameFormat = F, StudentNameFormat = F, AdvisorNameFormat = F, GuardianNameFormat = F, PrintGPA = F, PrintGradeScaleAtTop = F, PrintElectronicSignature = F, PrintBlankSignatureLine = F, OfficialSignatureLabel = F, BlankSignatureLabel = F, HideSignatureSection = F, PrintAttendanceTotals = F, PrintYearToDateTotals = F, PrintIndividualAttendanceTerms = F, PrintComments = F, PrintFreeFormComments = F, CommentPrintType = F, ReceivesForms = F, NoReceivingFamily = F, GradingSort = F, CourseDescriptionFormat = F, StudentSort1 = F, StudentSort2 = F, StudentSort3 = F, StudentSort4 = F, DisplayPeriodCodeSort1 = F, DisplayPeriodCodeSort2 = F, DisplayPeriodCodeSort3 = F, DisplayPeriodCodeSort4 = F, GPAField1 = F, GPALabel1 = F, GPAField2 = F, GPALabel2 = F, GPAField3 = F, GPALabel3 = F, GPAField4 = F, GPALabel4 = F, GPAField5 = F, GPALabel5 = F, GPAField6 = F, GPALabel6 = F, GPAField7 = F, GPALabel7 = F, UseFullGPASection = F, ColumnHeaderLabel1 = F, ColumnHeaderLabel2 = F, ColumnHeaderLabel3 = F, ColumnHeaderLabel4 = F, ColumnHeaderLabel5 = F, ColumnHeaderLabel6 = F, ColumnHeaderLabel7 = F, ColumnHeaderLabel8 = F, ColumnHeaderLabel9 = F, ColumnHeaderLabel10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CourseFilter = F, IncludeTranscriptNotes = F, GradeReportAcademicSessionTemplateGroupID = F, AcademicSessionType = F, IncludeInProgressGrades = F, IncludeCurrentYearClasses = F, UseFullGradesSection = F, FamilyPrintType = F, IncludeTransferCourses = F, PrintAllCourseRowHeaders = F, GradeReportTemplateIDClonedFrom = F, IncludePhoneInEntityAddress = F, IncludePhoneInGuardianAddress = F, PrintEndorsements = F, PrintHighFrequencyWords = F, PrintLettersAndSounds = F, EarnedCreditsMethodIDDefaultOverride = F, UsesGPAOrEarnedCredits = F, PrintYearAttendanceTotals = F, PrintStudentHighlights = F, PrintVaccinations = F, PrintCTEPrograms = F, HideReportSection = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplate
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplate
	#' @param GradeReportTemplateID The ID of the GradeReportTemplate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplate <- function(GradeReportTemplateID, ConfigEntityYearID = F, GradeReportColumnGroupIDSecondary = F, ReportRunInfoID = F, MediaIDLogo = F, HasLogo = F, Description = F, TemplateType = F, UseFreeFormGradingLegend = F, FreeFormGradingLegend = F, UseGradeMarkDescriptionGradingLegend = F, UseGradeMarkRangeGradingLegend = F, UseStudentSectionLinking = F, TeacherNameFormat = F, StudentNameFormat = F, AdvisorNameFormat = F, GuardianNameFormat = F, PrintGPA = F, PrintGradeScaleAtTop = F, PrintElectronicSignature = F, PrintBlankSignatureLine = F, OfficialSignatureLabel = F, BlankSignatureLabel = F, HideSignatureSection = F, PrintAttendanceTotals = F, PrintYearToDateTotals = F, PrintIndividualAttendanceTerms = F, PrintComments = F, PrintFreeFormComments = F, CommentPrintType = F, ReceivesForms = F, NoReceivingFamily = F, GradingSort = F, CourseDescriptionFormat = F, StudentSort1 = F, StudentSort2 = F, StudentSort3 = F, StudentSort4 = F, DisplayPeriodCodeSort1 = F, DisplayPeriodCodeSort2 = F, DisplayPeriodCodeSort3 = F, DisplayPeriodCodeSort4 = F, GPAField1 = F, GPALabel1 = F, GPAField2 = F, GPALabel2 = F, GPAField3 = F, GPALabel3 = F, GPAField4 = F, GPALabel4 = F, GPAField5 = F, GPALabel5 = F, GPAField6 = F, GPALabel6 = F, GPAField7 = F, GPALabel7 = F, UseFullGPASection = F, ColumnHeaderLabel1 = F, ColumnHeaderLabel2 = F, ColumnHeaderLabel3 = F, ColumnHeaderLabel4 = F, ColumnHeaderLabel5 = F, ColumnHeaderLabel6 = F, ColumnHeaderLabel7 = F, ColumnHeaderLabel8 = F, ColumnHeaderLabel9 = F, ColumnHeaderLabel10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CourseFilter = F, IncludeTranscriptNotes = F, GradeReportAcademicSessionTemplateGroupID = F, AcademicSessionType = F, IncludeInProgressGrades = F, IncludeCurrentYearClasses = F, UseFullGradesSection = F, FamilyPrintType = F, IncludeTransferCourses = F, PrintAllCourseRowHeaders = F, GradeReportTemplateIDClonedFrom = F, IncludePhoneInEntityAddress = F, IncludePhoneInGuardianAddress = F, PrintEndorsements = F, PrintHighFrequencyWords = F, PrintLettersAndSounds = F, EarnedCreditsMethodIDDefaultOverride = F, UsesGPAOrEarnedCredits = F, PrintYearAttendanceTotals = F, PrintStudentHighlights = F, PrintVaccinations = F, PrintCTEPrograms = F, HideReportSection = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplate", objectId = GradeReportTemplateID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplate
	#'
	#' This function deletes a GradeReportTemplate
	#' @param GradeReportTemplateID The ID of the GradeReportTemplate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateID of the deleted GradeReportTemplate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplate <- function(GradeReportTemplateID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplate", objectId = GradeReportTemplateID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplate
	#'
	#' This function creates a GradeReportTemplate
	#' @param fieldNames The field values to give the created GradeReportTemplate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplate <- function(ConfigEntityYearID = NULL, GradeReportColumnGroupIDSecondary = NULL, ReportRunInfoID = NULL, MediaIDLogo = NULL, Description = NULL, TemplateType = NULL, UseFreeFormGradingLegend = NULL, FreeFormGradingLegend = NULL, UseGradeMarkDescriptionGradingLegend = NULL, UseGradeMarkRangeGradingLegend = NULL, UseStudentSectionLinking = NULL, TeacherNameFormat = NULL, StudentNameFormat = NULL, AdvisorNameFormat = NULL, GuardianNameFormat = NULL, PrintGPA = NULL, PrintGradeScaleAtTop = NULL, PrintElectronicSignature = NULL, PrintBlankSignatureLine = NULL, OfficialSignatureLabel = NULL, BlankSignatureLabel = NULL, PrintAttendanceTotals = NULL, PrintYearToDateTotals = NULL, PrintIndividualAttendanceTerms = NULL, PrintComments = NULL, PrintFreeFormComments = NULL, CommentPrintType = NULL, ReceivesForms = NULL, NoReceivingFamily = NULL, GradingSort = NULL, CourseDescriptionFormat = NULL, StudentSort1 = NULL, StudentSort2 = NULL, StudentSort3 = NULL, StudentSort4 = NULL, DisplayPeriodCodeSort1 = NULL, DisplayPeriodCodeSort2 = NULL, DisplayPeriodCodeSort3 = NULL, DisplayPeriodCodeSort4 = NULL, GPAField1 = NULL, GPAField2 = NULL, GPAField3 = NULL, GPAField4 = NULL, GPAField5 = NULL, GPAField6 = NULL, GPAField7 = NULL, ColumnHeaderLabel1 = NULL, ColumnHeaderLabel2 = NULL, ColumnHeaderLabel3 = NULL, ColumnHeaderLabel4 = NULL, ColumnHeaderLabel5 = NULL, ColumnHeaderLabel6 = NULL, ColumnHeaderLabel7 = NULL, ColumnHeaderLabel8 = NULL, ColumnHeaderLabel9 = NULL, ColumnHeaderLabel10 = NULL, CourseFilter = NULL, IncludeTranscriptNotes = NULL, GradeReportAcademicSessionTemplateGroupID = NULL, AcademicSessionType = NULL, IncludeInProgressGrades = NULL, IncludeCurrentYearClasses = NULL, FamilyPrintType = NULL, IncludeTransferCourses = NULL, PrintAllCourseRowHeaders = NULL, GradeReportTemplateIDClonedFrom = NULL, IncludePhoneInEntityAddress = NULL, IncludePhoneInGuardianAddress = NULL, PrintEndorsements = NULL, PrintHighFrequencyWords = NULL, PrintLettersAndSounds = NULL, EarnedCreditsMethodIDDefaultOverride = NULL, PrintYearAttendanceTotals = NULL, PrintStudentHighlights = NULL, PrintVaccinations = NULL, PrintCTEPrograms = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplate", body = list(DataObject = body), searchFields = append("GradeReportTemplateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplate
	#'
	#' This function modifies a GradeReportTemplate
	#' @param fieldNames The field values to give the modified GradeReportTemplate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplate <- function(GradeReportTemplateID, ConfigEntityYearID = NULL, GradeReportColumnGroupIDSecondary = NULL, ReportRunInfoID = NULL, MediaIDLogo = NULL, Description = NULL, TemplateType = NULL, UseFreeFormGradingLegend = NULL, FreeFormGradingLegend = NULL, UseGradeMarkDescriptionGradingLegend = NULL, UseGradeMarkRangeGradingLegend = NULL, UseStudentSectionLinking = NULL, TeacherNameFormat = NULL, StudentNameFormat = NULL, AdvisorNameFormat = NULL, GuardianNameFormat = NULL, PrintGPA = NULL, PrintGradeScaleAtTop = NULL, PrintElectronicSignature = NULL, PrintBlankSignatureLine = NULL, OfficialSignatureLabel = NULL, BlankSignatureLabel = NULL, PrintAttendanceTotals = NULL, PrintYearToDateTotals = NULL, PrintIndividualAttendanceTerms = NULL, PrintComments = NULL, PrintFreeFormComments = NULL, CommentPrintType = NULL, ReceivesForms = NULL, NoReceivingFamily = NULL, GradingSort = NULL, CourseDescriptionFormat = NULL, StudentSort1 = NULL, StudentSort2 = NULL, StudentSort3 = NULL, StudentSort4 = NULL, DisplayPeriodCodeSort1 = NULL, DisplayPeriodCodeSort2 = NULL, DisplayPeriodCodeSort3 = NULL, DisplayPeriodCodeSort4 = NULL, GPAField1 = NULL, GPAField2 = NULL, GPAField3 = NULL, GPAField4 = NULL, GPAField5 = NULL, GPAField6 = NULL, GPAField7 = NULL, ColumnHeaderLabel1 = NULL, ColumnHeaderLabel2 = NULL, ColumnHeaderLabel3 = NULL, ColumnHeaderLabel4 = NULL, ColumnHeaderLabel5 = NULL, ColumnHeaderLabel6 = NULL, ColumnHeaderLabel7 = NULL, ColumnHeaderLabel8 = NULL, ColumnHeaderLabel9 = NULL, ColumnHeaderLabel10 = NULL, CourseFilter = NULL, IncludeTranscriptNotes = NULL, GradeReportAcademicSessionTemplateGroupID = NULL, AcademicSessionType = NULL, IncludeInProgressGrades = NULL, IncludeCurrentYearClasses = NULL, FamilyPrintType = NULL, IncludeTransferCourses = NULL, PrintAllCourseRowHeaders = NULL, GradeReportTemplateIDClonedFrom = NULL, IncludePhoneInEntityAddress = NULL, IncludePhoneInGuardianAddress = NULL, PrintEndorsements = NULL, PrintHighFrequencyWords = NULL, PrintLettersAndSounds = NULL, EarnedCreditsMethodIDDefaultOverride = NULL, PrintYearAttendanceTotals = NULL, PrintStudentHighlights = NULL, PrintVaccinations = NULL, PrintCTEPrograms = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplate", objectId = GradeReportTemplateID, body = list(DataObject = body), searchFields = append("GradeReportTemplateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeReportGradeBuckets
	#'
	#' This function returns a dataframe or json object of TempGradeReportGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeReportGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeReportGradeBuckets <- function(searchConditionsList = NULL, TempGradeReportGradeBucketID = F, GradeBucketLabel = F, GradingPeriodGradeBucketID = F, EntityName = F, SchoolYearDescription = F, SectionLengthDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeReportGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeReportGradeBucket
	#'
	#' This function returns a dataframe or json object of a TempGradeReportGradeBucket
	#' @param TempGradeReportGradeBucketID The ID of the TempGradeReportGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeReportGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeReportGradeBucket <- function(TempGradeReportGradeBucketID, GradeBucketLabel = F, GradingPeriodGradeBucketID = F, EntityName = F, SchoolYearDescription = F, SectionLengthDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeReportGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeReportGradeBucket", objectId = TempGradeReportGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeReportGradeBucket
	#'
	#' This function deletes a TempGradeReportGradeBucket
	#' @param TempGradeReportGradeBucketID The ID of the TempGradeReportGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeReportGradeBucketID of the deleted TempGradeReportGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeReportGradeBucket <- function(TempGradeReportGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeReportGradeBucket", objectId = TempGradeReportGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeReportGradeBucket
	#'
	#' This function creates a TempGradeReportGradeBucket
	#' @param fieldNames The field values to give the created TempGradeReportGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeReportGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeReportGradeBucket <- function(GradeBucketLabel = NULL, GradingPeriodGradeBucketID = NULL, EntityName = NULL, SchoolYearDescription = NULL, SectionLengthDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeReportGradeBucket", body = list(DataObject = body), searchFields = append("TempGradeReportGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeReportGradeBucket
	#'
	#' This function modifies a TempGradeReportGradeBucket
	#' @param fieldNames The field values to give the modified TempGradeReportGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeReportGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeReportGradeBucket <- function(TempGradeReportGradeBucketID, GradeBucketLabel = NULL, GradingPeriodGradeBucketID = NULL, EntityName = NULL, SchoolYearDescription = NULL, SectionLengthDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeReportGradeBucket", objectId = TempGradeReportGradeBucketID, body = list(DataObject = body), searchFields = append("TempGradeReportGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportColumnAttendanceCategories
	#'
	#' This function returns a dataframe or json object of GradeReportColumnAttendanceCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnAttendanceCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnAttendanceCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnAttendanceCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportColumnAttendanceCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportColumnAttendanceCategories <- function(searchConditionsList = NULL, GradeReportColumnAttendanceCategoryID = F, GradeReportColumnGroupColumnID = F, Category = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnAttendanceCategoryIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportColumnAttendanceCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportColumnAttendanceCategory
	#'
	#' This function returns a dataframe or json object of a GradeReportColumnAttendanceCategory
	#' @param GradeReportColumnAttendanceCategoryID The ID of the GradeReportColumnAttendanceCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnAttendanceCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnAttendanceCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnAttendanceCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportColumnAttendanceCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportColumnAttendanceCategory <- function(GradeReportColumnAttendanceCategoryID, GradeReportColumnGroupColumnID = F, Category = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnAttendanceCategoryIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportColumnAttendanceCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceCategory", objectId = GradeReportColumnAttendanceCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportColumnAttendanceCategory
	#'
	#' This function deletes a GradeReportColumnAttendanceCategory
	#' @param GradeReportColumnAttendanceCategoryID The ID of the GradeReportColumnAttendanceCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportColumnAttendanceCategoryID of the deleted GradeReportColumnAttendanceCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportColumnAttendanceCategory <- function(GradeReportColumnAttendanceCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceCategory", objectId = GradeReportColumnAttendanceCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportColumnAttendanceCategory
	#'
	#' This function creates a GradeReportColumnAttendanceCategory
	#' @param fieldNames The field values to give the created GradeReportColumnAttendanceCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportColumnAttendanceCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportColumnAttendanceCategory <- function(GradeReportColumnGroupColumnID = NULL, Category = NULL, GradeReportColumnAttendanceCategoryIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceCategory", body = list(DataObject = body), searchFields = append("GradeReportColumnAttendanceCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportColumnAttendanceCategory
	#'
	#' This function modifies a GradeReportColumnAttendanceCategory
	#' @param fieldNames The field values to give the modified GradeReportColumnAttendanceCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportColumnAttendanceCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportColumnAttendanceCategory <- function(GradeReportColumnAttendanceCategoryID, GradeReportColumnGroupColumnID = NULL, Category = NULL, GradeReportColumnAttendanceCategoryIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceCategory", objectId = GradeReportColumnAttendanceCategoryID, body = list(DataObject = body), searchFields = append("GradeReportColumnAttendanceCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportColumnGradeBuckets
	#'
	#' This function returns a dataframe or json object of GradeReportColumnGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportColumnGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportColumnGradeBuckets <- function(searchConditionsList = NULL, GradeReportColumnGradeBucketID = F, GradeReportColumnGroupColumnID = F, GradingPeriodGradeBucketID = F, SortNumber = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnGradeBucketIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportColumnGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportColumnGradeBucket
	#'
	#' This function returns a dataframe or json object of a GradeReportColumnGradeBucket
	#' @param GradeReportColumnGradeBucketID The ID of the GradeReportColumnGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportColumnGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportColumnGradeBucket <- function(GradeReportColumnGradeBucketID, GradeReportColumnGroupColumnID = F, GradingPeriodGradeBucketID = F, SortNumber = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportColumnGradeBucketIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportColumnGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportColumnGradeBucket", objectId = GradeReportColumnGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportColumnGradeBucket
	#'
	#' This function deletes a GradeReportColumnGradeBucket
	#' @param GradeReportColumnGradeBucketID The ID of the GradeReportColumnGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportColumnGradeBucketID of the deleted GradeReportColumnGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportColumnGradeBucket <- function(GradeReportColumnGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportColumnGradeBucket", objectId = GradeReportColumnGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportColumnGradeBucket
	#'
	#' This function creates a GradeReportColumnGradeBucket
	#' @param fieldNames The field values to give the created GradeReportColumnGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportColumnGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportColumnGradeBucket <- function(GradeReportColumnGroupColumnID = NULL, GradingPeriodGradeBucketID = NULL, SortNumber = NULL, GradeReportColumnGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportColumnGradeBucket", body = list(DataObject = body), searchFields = append("GradeReportColumnGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportColumnGradeBucket
	#'
	#' This function modifies a GradeReportColumnGradeBucket
	#' @param fieldNames The field values to give the modified GradeReportColumnGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportColumnGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportColumnGradeBucket <- function(GradeReportColumnGradeBucketID, GradeReportColumnGroupColumnID = NULL, GradingPeriodGradeBucketID = NULL, SortNumber = NULL, GradeReportColumnGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportColumnGradeBucket", objectId = GradeReportColumnGradeBucketID, body = list(DataObject = body), searchFields = append("GradeReportColumnGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportGPARows
	#'
	#' This function returns a dataframe or json object of GradeReportGPARows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportGPARows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportGPARows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportGPARow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportGPARows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportGPARows <- function(searchConditionsList = NULL, GradeReportGPARowID = F, GradeReportAcademicSessionID = F, GPABucketID = F, GPAMethodID = F, GPABucketDescription = F, GPAMethodDescription = F, SortNumber = F, DataColumn1 = F, DataColumn2 = F, DataColumn3 = F, DataColumn4 = F, DataColumn5 = F, DataColumn6 = F, DataColumn7 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportAcademicSessionSortNumber = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportGPARow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportGPARow
	#'
	#' This function returns a dataframe or json object of a GradeReportGPARow
	#' @param GradeReportGPARowID The ID of the GradeReportGPARow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportGPARow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportGPARow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportGPARow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportGPARow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportGPARow <- function(GradeReportGPARowID, GradeReportAcademicSessionID = F, GPABucketID = F, GPAMethodID = F, GPABucketDescription = F, GPAMethodDescription = F, SortNumber = F, DataColumn1 = F, DataColumn2 = F, DataColumn3 = F, DataColumn4 = F, DataColumn5 = F, DataColumn6 = F, DataColumn7 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportAcademicSessionSortNumber = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportGPARowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportGPARow", objectId = GradeReportGPARowID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportGPARow
	#'
	#' This function deletes a GradeReportGPARow
	#' @param GradeReportGPARowID The ID of the GradeReportGPARow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportGPARowID of the deleted GradeReportGPARow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportGPARow <- function(GradeReportGPARowID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportGPARow", objectId = GradeReportGPARowID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportGPARow
	#'
	#' This function creates a GradeReportGPARow
	#' @param fieldNames The field values to give the created GradeReportGPARow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportGPARow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportGPARow <- function(GradeReportAcademicSessionID = NULL, GPABucketID = NULL, GPAMethodID = NULL, GPABucketDescription = NULL, GPAMethodDescription = NULL, SortNumber = NULL, DataColumn1 = NULL, DataColumn2 = NULL, DataColumn3 = NULL, DataColumn4 = NULL, DataColumn5 = NULL, DataColumn6 = NULL, DataColumn7 = NULL, GradeReportAcademicSessionSortNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportGPARow", body = list(DataObject = body), searchFields = append("GradeReportGPARowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportGPARow
	#'
	#' This function modifies a GradeReportGPARow
	#' @param fieldNames The field values to give the modified GradeReportGPARow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportGPARow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportGPARow <- function(GradeReportGPARowID, GradeReportAcademicSessionID = NULL, GPABucketID = NULL, GPAMethodID = NULL, GPABucketDescription = NULL, GPAMethodDescription = NULL, SortNumber = NULL, DataColumn1 = NULL, DataColumn2 = NULL, DataColumn3 = NULL, DataColumn4 = NULL, DataColumn5 = NULL, DataColumn6 = NULL, DataColumn7 = NULL, GradeReportAcademicSessionSortNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportGPARow", objectId = GradeReportGPARowID, body = list(DataObject = body), searchFields = append("GradeReportGPARowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportGradingScales
	#'
	#' This function returns a dataframe or json object of GradeReportGradingScales
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportGradingScales. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportGradingScales.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportGradingScale') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportGradingScales
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportGradingScales <- function(searchConditionsList = NULL, GradeReportGradingScaleID = F, SortNumber = F, DisplayType = F, GradeMarkCode = F, RangeLow = F, RangeHigh = F, FreeformText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportStudentID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportGradingScale", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportGradingScale
	#'
	#' This function returns a dataframe or json object of a GradeReportGradingScale
	#' @param GradeReportGradingScaleID The ID of the GradeReportGradingScale to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportGradingScale. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportGradingScale.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportGradingScale') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportGradingScale
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportGradingScale <- function(GradeReportGradingScaleID, SortNumber = F, DisplayType = F, GradeMarkCode = F, RangeLow = F, RangeHigh = F, FreeformText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportStudentID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportGradingScaleID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportGradingScale", objectId = GradeReportGradingScaleID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportGradingScale
	#'
	#' This function deletes a GradeReportGradingScale
	#' @param GradeReportGradingScaleID The ID of the GradeReportGradingScale to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportGradingScaleID of the deleted GradeReportGradingScale.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportGradingScale <- function(GradeReportGradingScaleID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportGradingScale", objectId = GradeReportGradingScaleID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportGradingScale
	#'
	#' This function creates a GradeReportGradingScale
	#' @param fieldNames The field values to give the created GradeReportGradingScale. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportGradingScale
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportGradingScale <- function(SortNumber = NULL, DisplayType = NULL, GradeMarkCode = NULL, RangeLow = NULL, RangeHigh = NULL, FreeformText = NULL, GradeReportStudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportGradingScale", body = list(DataObject = body), searchFields = append("GradeReportGradingScaleID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportGradingScale
	#'
	#' This function modifies a GradeReportGradingScale
	#' @param fieldNames The field values to give the modified GradeReportGradingScale. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportGradingScale
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportGradingScale <- function(GradeReportGradingScaleID, SortNumber = NULL, DisplayType = NULL, GradeMarkCode = NULL, RangeLow = NULL, RangeHigh = NULL, FreeformText = NULL, GradeReportStudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportGradingScale", objectId = GradeReportGradingScaleID, body = list(DataObject = body), searchFields = append("GradeReportGradingScaleID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportRows
	#'
	#' This function returns a dataframe or json object of GradeReportRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportRows <- function(searchConditionsList = NULL, GradeReportRowID = F, GradeReportAcademicSessionID = F, StudentSectionID = F, Sort1 = F, Sort2 = F, Sort3 = F, Sort4 = F, RowType = F, Description = F, SortNumber = F, ClassPeriod = F, EarnedCredit = F, TotalPossibleCredit = F, CourseTypeCode = F, CourseTypeDescription = F, Department = F, CourseSubjectDescription = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, StaffName = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportRowIDParent = F, GradeReportAcademicSessionSortNumber = F, AttemptedCredit = F, BackgroundColor = F, TextColor = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportRow
	#'
	#' This function returns a dataframe or json object of a GradeReportRow
	#' @param GradeReportRowID The ID of the GradeReportRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportRow <- function(GradeReportRowID, GradeReportAcademicSessionID = F, StudentSectionID = F, Sort1 = F, Sort2 = F, Sort3 = F, Sort4 = F, RowType = F, Description = F, SortNumber = F, ClassPeriod = F, EarnedCredit = F, TotalPossibleCredit = F, CourseTypeCode = F, CourseTypeDescription = F, Department = F, CourseSubjectDescription = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, StaffName = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportRowIDParent = F, GradeReportAcademicSessionSortNumber = F, AttemptedCredit = F, BackgroundColor = F, TextColor = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportRow", objectId = GradeReportRowID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportRow
	#'
	#' This function deletes a GradeReportRow
	#' @param GradeReportRowID The ID of the GradeReportRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportRowID of the deleted GradeReportRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportRow <- function(GradeReportRowID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportRow", objectId = GradeReportRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportRow
	#'
	#' This function creates a GradeReportRow
	#' @param fieldNames The field values to give the created GradeReportRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportRow <- function(GradeReportAcademicSessionID = NULL, StudentSectionID = NULL, Sort1 = NULL, Sort2 = NULL, Sort3 = NULL, Sort4 = NULL, RowType = NULL, Description = NULL, SortNumber = NULL, ClassPeriod = NULL, EarnedCredit = NULL, TotalPossibleCredit = NULL, CourseTypeCode = NULL, CourseTypeDescription = NULL, Department = NULL, CourseSubjectDescription = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, StaffName = NULL, GradeReportRowIDParent = NULL, GradeReportAcademicSessionSortNumber = NULL, AttemptedCredit = NULL, BackgroundColor = NULL, TextColor = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportRow", body = list(DataObject = body), searchFields = append("GradeReportRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportRow
	#'
	#' This function modifies a GradeReportRow
	#' @param fieldNames The field values to give the modified GradeReportRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportRow <- function(GradeReportRowID, GradeReportAcademicSessionID = NULL, StudentSectionID = NULL, Sort1 = NULL, Sort2 = NULL, Sort3 = NULL, Sort4 = NULL, RowType = NULL, Description = NULL, SortNumber = NULL, ClassPeriod = NULL, EarnedCredit = NULL, TotalPossibleCredit = NULL, CourseTypeCode = NULL, CourseTypeDescription = NULL, Department = NULL, CourseSubjectDescription = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, StaffName = NULL, GradeReportRowIDParent = NULL, GradeReportAcademicSessionSortNumber = NULL, AttemptedCredit = NULL, BackgroundColor = NULL, TextColor = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportRow", objectId = GradeReportRowID, body = list(DataObject = body), searchFields = append("GradeReportRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportRowColumns
	#'
	#' This function returns a dataframe or json object of GradeReportRowColumns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRowColumns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRowColumns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRowColumn') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportRowColumns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportRowColumns <- function(searchConditionsList = NULL, GradeReportRowColumnID = F, GradeReportRowID = F, GradeReportColumnGroupColumnID = F, GradeMarkID = F, DisplayValue = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportRowColumn", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportRowColumn
	#'
	#' This function returns a dataframe or json object of a GradeReportRowColumn
	#' @param GradeReportRowColumnID The ID of the GradeReportRowColumn to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRowColumn. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRowColumn.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRowColumn') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportRowColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportRowColumn <- function(GradeReportRowColumnID, GradeReportRowID = F, GradeReportColumnGroupColumnID = F, GradeMarkID = F, DisplayValue = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportRowColumnID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportRowColumn", objectId = GradeReportRowColumnID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportRowColumn
	#'
	#' This function deletes a GradeReportRowColumn
	#' @param GradeReportRowColumnID The ID of the GradeReportRowColumn to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportRowColumnID of the deleted GradeReportRowColumn.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportRowColumn <- function(GradeReportRowColumnID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportRowColumn", objectId = GradeReportRowColumnID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportRowColumn
	#'
	#' This function creates a GradeReportRowColumn
	#' @param fieldNames The field values to give the created GradeReportRowColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportRowColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportRowColumn <- function(GradeReportRowID = NULL, GradeReportColumnGroupColumnID = NULL, GradeMarkID = NULL, DisplayValue = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportRowColumn", body = list(DataObject = body), searchFields = append("GradeReportRowColumnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportRowColumn
	#'
	#' This function modifies a GradeReportRowColumn
	#' @param fieldNames The field values to give the modified GradeReportRowColumn. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportRowColumn
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportRowColumn <- function(GradeReportRowColumnID, GradeReportRowID = NULL, GradeReportColumnGroupColumnID = NULL, GradeMarkID = NULL, DisplayValue = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportRowColumn", objectId = GradeReportRowColumnID, body = list(DataObject = body), searchFields = append("GradeReportRowColumnID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportRowDetails
	#'
	#' This function returns a dataframe or json object of GradeReportRowDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRowDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRowDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRowDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportRowDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportRowDetails <- function(searchConditionsList = NULL, GradeReportRowDetailID = F, GradeReportRowID = F, SortNumber = F, GradingPeriodSortNumber = F, Label = F, DetailData = F, GradingPeriodLabel = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportRowDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportRowDetail
	#'
	#' This function returns a dataframe or json object of a GradeReportRowDetail
	#' @param GradeReportRowDetailID The ID of the GradeReportRowDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRowDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRowDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRowDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportRowDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportRowDetail <- function(GradeReportRowDetailID, GradeReportRowID = F, SortNumber = F, GradingPeriodSortNumber = F, Label = F, DetailData = F, GradingPeriodLabel = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportRowDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportRowDetail", objectId = GradeReportRowDetailID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportRowDetail
	#'
	#' This function deletes a GradeReportRowDetail
	#' @param GradeReportRowDetailID The ID of the GradeReportRowDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportRowDetailID of the deleted GradeReportRowDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportRowDetail <- function(GradeReportRowDetailID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportRowDetail", objectId = GradeReportRowDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportRowDetail
	#'
	#' This function creates a GradeReportRowDetail
	#' @param fieldNames The field values to give the created GradeReportRowDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportRowDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportRowDetail <- function(GradeReportRowID = NULL, SortNumber = NULL, GradingPeriodSortNumber = NULL, Label = NULL, DetailData = NULL, GradingPeriodLabel = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportRowDetail", body = list(DataObject = body), searchFields = append("GradeReportRowDetailID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportRowDetail
	#'
	#' This function modifies a GradeReportRowDetail
	#' @param fieldNames The field values to give the modified GradeReportRowDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportRowDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportRowDetail <- function(GradeReportRowDetailID, GradeReportRowID = NULL, SortNumber = NULL, GradingPeriodSortNumber = NULL, Label = NULL, DetailData = NULL, GradingPeriodLabel = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportRowDetail", objectId = GradeReportRowDetailID, body = list(DataObject = body), searchFields = append("GradeReportRowDetailID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudents
	#'
	#' This function returns a dataframe or json object of GradeReportStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudents <- function(searchConditionsList = NULL, GradeReportStudentID = F, GradeReportRunHistoryID = F, Sort1 = F, Sort2 = F, Sort3 = F, Sort4 = F, StudentID = F, NameTitle = F, FirstName = F, MiddleName = F, LastName = F, NameSuffix = F, FormattedName = F, BirthDate = F, StudentNumber = F, Gender = F, EthnicityAndRace = F, EmailAddress = F, GraduationDate = F, Homeroom = F, AdvisorName = F, PromotionStatus = F, StudentFamilyID = F, StudentFamilyRank = F, AddressLine1 = F, AddressLine2 = F, City = F, StateProvince = F, PostalCode = F, NameIDPrimaryGuardian = F, PrimaryGuardianNameTitle = F, PrimaryGuardianFirstName = F, PrimaryGuardianMiddleName = F, PrimaryGuardianLastName = F, PrimaryGuardianNameSuffix = F, PrimaryGuardianFormattedName = F, PrimaryGuardianPhoneNumber = F, PrimaryGuardianEmailAddress = F, SingleColumnHeaderField1 = F, SingleColumnHeaderField2 = F, SingleColumnHeaderField3 = F, SingleColumnHeaderField4 = F, SingleColumnHeaderField5 = F, DoubleColumnHeaderField1 = F, DoubleColumnHeaderField2 = F, DoubleColumnHeaderField3 = F, DoubleColumnHeaderField4 = F, DoubleColumnHeaderField5 = F, DoubleColumnHeaderField6 = F, DoubleColumnHeaderField7 = F, DoubleColumnHeaderField8 = F, DoubleColumnHeaderField9 = F, DoubleColumnHeaderField10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, FormattedFullAddress = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudent
	#'
	#' This function returns a dataframe or json object of a GradeReportStudent
	#' @param GradeReportStudentID The ID of the GradeReportStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudent <- function(GradeReportStudentID, GradeReportRunHistoryID = F, Sort1 = F, Sort2 = F, Sort3 = F, Sort4 = F, StudentID = F, NameTitle = F, FirstName = F, MiddleName = F, LastName = F, NameSuffix = F, FormattedName = F, BirthDate = F, StudentNumber = F, Gender = F, EthnicityAndRace = F, EmailAddress = F, GraduationDate = F, Homeroom = F, AdvisorName = F, PromotionStatus = F, StudentFamilyID = F, StudentFamilyRank = F, AddressLine1 = F, AddressLine2 = F, City = F, StateProvince = F, PostalCode = F, NameIDPrimaryGuardian = F, PrimaryGuardianNameTitle = F, PrimaryGuardianFirstName = F, PrimaryGuardianMiddleName = F, PrimaryGuardianLastName = F, PrimaryGuardianNameSuffix = F, PrimaryGuardianFormattedName = F, PrimaryGuardianPhoneNumber = F, PrimaryGuardianEmailAddress = F, SingleColumnHeaderField1 = F, SingleColumnHeaderField2 = F, SingleColumnHeaderField3 = F, SingleColumnHeaderField4 = F, SingleColumnHeaderField5 = F, DoubleColumnHeaderField1 = F, DoubleColumnHeaderField2 = F, DoubleColumnHeaderField3 = F, DoubleColumnHeaderField4 = F, DoubleColumnHeaderField5 = F, DoubleColumnHeaderField6 = F, DoubleColumnHeaderField7 = F, DoubleColumnHeaderField8 = F, DoubleColumnHeaderField9 = F, DoubleColumnHeaderField10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, FormattedFullAddress = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudent", objectId = GradeReportStudentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudent
	#'
	#' This function deletes a GradeReportStudent
	#' @param GradeReportStudentID The ID of the GradeReportStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentID of the deleted GradeReportStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudent <- function(GradeReportStudentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudent", objectId = GradeReportStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudent
	#'
	#' This function creates a GradeReportStudent
	#' @param fieldNames The field values to give the created GradeReportStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudent <- function(GradeReportRunHistoryID = NULL, Sort1 = NULL, Sort2 = NULL, Sort3 = NULL, Sort4 = NULL, StudentID = NULL, NameTitle = NULL, FirstName = NULL, MiddleName = NULL, LastName = NULL, NameSuffix = NULL, FormattedName = NULL, BirthDate = NULL, StudentNumber = NULL, Gender = NULL, EthnicityAndRace = NULL, EmailAddress = NULL, GraduationDate = NULL, Homeroom = NULL, AdvisorName = NULL, PromotionStatus = NULL, StudentFamilyID = NULL, StudentFamilyRank = NULL, AddressLine1 = NULL, AddressLine2 = NULL, City = NULL, StateProvince = NULL, PostalCode = NULL, NameIDPrimaryGuardian = NULL, PrimaryGuardianNameTitle = NULL, PrimaryGuardianFirstName = NULL, PrimaryGuardianMiddleName = NULL, PrimaryGuardianLastName = NULL, PrimaryGuardianNameSuffix = NULL, PrimaryGuardianFormattedName = NULL, PrimaryGuardianPhoneNumber = NULL, PrimaryGuardianEmailAddress = NULL, SingleColumnHeaderField1 = NULL, SingleColumnHeaderField2 = NULL, SingleColumnHeaderField3 = NULL, SingleColumnHeaderField4 = NULL, SingleColumnHeaderField5 = NULL, DoubleColumnHeaderField1 = NULL, DoubleColumnHeaderField2 = NULL, DoubleColumnHeaderField3 = NULL, DoubleColumnHeaderField4 = NULL, DoubleColumnHeaderField5 = NULL, DoubleColumnHeaderField6 = NULL, DoubleColumnHeaderField7 = NULL, DoubleColumnHeaderField8 = NULL, DoubleColumnHeaderField9 = NULL, DoubleColumnHeaderField10 = NULL, FormattedFullAddress = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudent", body = list(DataObject = body), searchFields = append("GradeReportStudentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudent
	#'
	#' This function modifies a GradeReportStudent
	#' @param fieldNames The field values to give the modified GradeReportStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudent <- function(GradeReportStudentID, GradeReportRunHistoryID = NULL, Sort1 = NULL, Sort2 = NULL, Sort3 = NULL, Sort4 = NULL, StudentID = NULL, NameTitle = NULL, FirstName = NULL, MiddleName = NULL, LastName = NULL, NameSuffix = NULL, FormattedName = NULL, BirthDate = NULL, StudentNumber = NULL, Gender = NULL, EthnicityAndRace = NULL, EmailAddress = NULL, GraduationDate = NULL, Homeroom = NULL, AdvisorName = NULL, PromotionStatus = NULL, StudentFamilyID = NULL, StudentFamilyRank = NULL, AddressLine1 = NULL, AddressLine2 = NULL, City = NULL, StateProvince = NULL, PostalCode = NULL, NameIDPrimaryGuardian = NULL, PrimaryGuardianNameTitle = NULL, PrimaryGuardianFirstName = NULL, PrimaryGuardianMiddleName = NULL, PrimaryGuardianLastName = NULL, PrimaryGuardianNameSuffix = NULL, PrimaryGuardianFormattedName = NULL, PrimaryGuardianPhoneNumber = NULL, PrimaryGuardianEmailAddress = NULL, SingleColumnHeaderField1 = NULL, SingleColumnHeaderField2 = NULL, SingleColumnHeaderField3 = NULL, SingleColumnHeaderField4 = NULL, SingleColumnHeaderField5 = NULL, DoubleColumnHeaderField1 = NULL, DoubleColumnHeaderField2 = NULL, DoubleColumnHeaderField3 = NULL, DoubleColumnHeaderField4 = NULL, DoubleColumnHeaderField5 = NULL, DoubleColumnHeaderField6 = NULL, DoubleColumnHeaderField7 = NULL, DoubleColumnHeaderField8 = NULL, DoubleColumnHeaderField9 = NULL, DoubleColumnHeaderField10 = NULL, FormattedFullAddress = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudent", objectId = GradeReportStudentID, body = list(DataObject = body), searchFields = append("GradeReportStudentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TranscriptNotes
	#'
	#' This function returns a dataframe or json object of TranscriptNotes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TranscriptNotes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TranscriptNotes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TranscriptNote') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TranscriptNotes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTranscriptNotes <- function(searchConditionsList = NULL, TranscriptNoteID = F, Note = F, StudentID = F, DateAdded = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TranscriptNote", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TranscriptNote
	#'
	#' This function returns a dataframe or json object of a TranscriptNote
	#' @param TranscriptNoteID The ID of the TranscriptNote to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TranscriptNote. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TranscriptNote.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TranscriptNote') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TranscriptNote
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTranscriptNote <- function(TranscriptNoteID, Note = F, StudentID = F, DateAdded = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TranscriptNoteID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TranscriptNote", objectId = TranscriptNoteID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TranscriptNote
	#'
	#' This function deletes a TranscriptNote
	#' @param TranscriptNoteID The ID of the TranscriptNote to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TranscriptNoteID of the deleted TranscriptNote.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTranscriptNote <- function(TranscriptNoteID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TranscriptNote", objectId = TranscriptNoteID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TranscriptNote
	#'
	#' This function creates a TranscriptNote
	#' @param fieldNames The field values to give the created TranscriptNote. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TranscriptNote
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTranscriptNote <- function(Note = NULL, StudentID = NULL, DateAdded = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TranscriptNote", body = list(DataObject = body), searchFields = append("TranscriptNoteID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TranscriptNote
	#'
	#' This function modifies a TranscriptNote
	#' @param fieldNames The field values to give the modified TranscriptNote. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TranscriptNote
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTranscriptNote <- function(TranscriptNoteID, Note = NULL, StudentID = NULL, DateAdded = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TranscriptNote", objectId = TranscriptNoteID, body = list(DataObject = body), searchFields = append("TranscriptNoteID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentTestRows
	#'
	#' This function returns a dataframe or json object of GradeReportStudentTestRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentTestRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentTestRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentTestRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentTestRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentTestRows <- function(searchConditionsList = NULL, GradeReportStudentTestRowID = F, GradeReportStudentTestTypeID = F, DateTaken = F, SortNumber = F, TestColumn1 = F, TestColumn2 = F, TestColumn3 = F, TestColumn4 = F, TestColumn5 = F, TestColumn6 = F, TestColumn7 = F, TestColumn8 = F, TestColumn9 = F, TestColumn10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentTestRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentTestRow
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentTestRow
	#' @param GradeReportStudentTestRowID The ID of the GradeReportStudentTestRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentTestRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentTestRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentTestRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentTestRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentTestRow <- function(GradeReportStudentTestRowID, GradeReportStudentTestTypeID = F, DateTaken = F, SortNumber = F, TestColumn1 = F, TestColumn2 = F, TestColumn3 = F, TestColumn4 = F, TestColumn5 = F, TestColumn6 = F, TestColumn7 = F, TestColumn8 = F, TestColumn9 = F, TestColumn10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentTestRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentTestRow", objectId = GradeReportStudentTestRowID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentTestRow
	#'
	#' This function deletes a GradeReportStudentTestRow
	#' @param GradeReportStudentTestRowID The ID of the GradeReportStudentTestRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentTestRowID of the deleted GradeReportStudentTestRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentTestRow <- function(GradeReportStudentTestRowID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentTestRow", objectId = GradeReportStudentTestRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentTestRow
	#'
	#' This function creates a GradeReportStudentTestRow
	#' @param fieldNames The field values to give the created GradeReportStudentTestRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentTestRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentTestRow <- function(GradeReportStudentTestTypeID = NULL, DateTaken = NULL, SortNumber = NULL, TestColumn1 = NULL, TestColumn2 = NULL, TestColumn3 = NULL, TestColumn4 = NULL, TestColumn5 = NULL, TestColumn6 = NULL, TestColumn7 = NULL, TestColumn8 = NULL, TestColumn9 = NULL, TestColumn10 = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentTestRow", body = list(DataObject = body), searchFields = append("GradeReportStudentTestRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentTestRow
	#'
	#' This function modifies a GradeReportStudentTestRow
	#' @param fieldNames The field values to give the modified GradeReportStudentTestRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentTestRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentTestRow <- function(GradeReportStudentTestRowID, GradeReportStudentTestTypeID = NULL, DateTaken = NULL, SortNumber = NULL, TestColumn1 = NULL, TestColumn2 = NULL, TestColumn3 = NULL, TestColumn4 = NULL, TestColumn5 = NULL, TestColumn6 = NULL, TestColumn7 = NULL, TestColumn8 = NULL, TestColumn9 = NULL, TestColumn10 = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentTestRow", objectId = GradeReportStudentTestRowID, body = list(DataObject = body), searchFields = append("GradeReportStudentTestRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentTestTypes
	#'
	#' This function returns a dataframe or json object of GradeReportStudentTestTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentTestTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentTestTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentTestType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentTestTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentTestTypes <- function(searchConditionsList = NULL, GradeReportStudentTestTypeID = F, GradeReportStudentID = F, TestType = F, TestVersion = F, SortNumber = F, TestColumnHeader1 = F, TestColumnHeader2 = F, TestColumnHeader3 = F, TestColumnHeader4 = F, TestColumnHeader5 = F, TestColumnHeader6 = F, TestColumnHeader7 = F, TestColumnHeader8 = F, TestColumnHeader9 = F, TestColumnHeader10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, TestCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentTestType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentTestType
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentTestType
	#' @param GradeReportStudentTestTypeID The ID of the GradeReportStudentTestType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentTestType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentTestType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentTestType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentTestType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentTestType <- function(GradeReportStudentTestTypeID, GradeReportStudentID = F, TestType = F, TestVersion = F, SortNumber = F, TestColumnHeader1 = F, TestColumnHeader2 = F, TestColumnHeader3 = F, TestColumnHeader4 = F, TestColumnHeader5 = F, TestColumnHeader6 = F, TestColumnHeader7 = F, TestColumnHeader8 = F, TestColumnHeader9 = F, TestColumnHeader10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, TestCode = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentTestTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentTestType", objectId = GradeReportStudentTestTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentTestType
	#'
	#' This function deletes a GradeReportStudentTestType
	#' @param GradeReportStudentTestTypeID The ID of the GradeReportStudentTestType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentTestTypeID of the deleted GradeReportStudentTestType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentTestType <- function(GradeReportStudentTestTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentTestType", objectId = GradeReportStudentTestTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentTestType
	#'
	#' This function creates a GradeReportStudentTestType
	#' @param fieldNames The field values to give the created GradeReportStudentTestType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentTestType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentTestType <- function(GradeReportStudentID = NULL, TestType = NULL, TestVersion = NULL, SortNumber = NULL, TestColumnHeader1 = NULL, TestColumnHeader2 = NULL, TestColumnHeader3 = NULL, TestColumnHeader4 = NULL, TestColumnHeader5 = NULL, TestColumnHeader6 = NULL, TestColumnHeader7 = NULL, TestColumnHeader8 = NULL, TestColumnHeader9 = NULL, TestColumnHeader10 = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentTestType", body = list(DataObject = body), searchFields = append("GradeReportStudentTestTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentTestType
	#'
	#' This function modifies a GradeReportStudentTestType
	#' @param fieldNames The field values to give the modified GradeReportStudentTestType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentTestType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentTestType <- function(GradeReportStudentTestTypeID, GradeReportStudentID = NULL, TestType = NULL, TestVersion = NULL, SortNumber = NULL, TestColumnHeader1 = NULL, TestColumnHeader2 = NULL, TestColumnHeader3 = NULL, TestColumnHeader4 = NULL, TestColumnHeader5 = NULL, TestColumnHeader6 = NULL, TestColumnHeader7 = NULL, TestColumnHeader8 = NULL, TestColumnHeader9 = NULL, TestColumnHeader10 = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentTestType", objectId = GradeReportStudentTestTypeID, body = list(DataObject = body), searchFields = append("GradeReportStudentTestTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateTestTypes
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateTestTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateTestTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateTestTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateTestType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateTestTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateTestTypes <- function(searchConditionsList = NULL, GradeReportTemplateTestTypeID = F, GradeReportTemplateID = F, TestType = F, TestVersion = F, SortNumber = F, FieldGUIDTestColumn1 = F, TestColumnHeaderOverride1 = F, FieldGUIDTestColumn2 = F, TestColumnHeaderOverride2 = F, FieldGUIDTestColumn3 = F, TestColumnHeaderOverride3 = F, FieldGUIDTestColumn4 = F, TestColumnHeaderOverride4 = F, FieldGUIDTestColumn5 = F, TestColumnHeaderOverride5 = F, FieldGUIDTestColumn6 = F, TestColumnHeaderOverride6 = F, FieldGUIDTestColumn7 = F, TestColumnHeaderOverride7 = F, FieldGUIDTestColumn8 = F, TestColumnHeaderOverride8 = F, FieldGUIDTestColumn9 = F, TestColumnHeaderOverride9 = F, FieldGUIDTestColumn10 = F, TestColumnHeaderOverride10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateTestTypeIDClonedFrom = F, PrintHighestScoreOnly = F, TestCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateTestType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateTestType
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateTestType
	#' @param GradeReportTemplateTestTypeID The ID of the GradeReportTemplateTestType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateTestType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateTestType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateTestType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateTestType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateTestType <- function(GradeReportTemplateTestTypeID, GradeReportTemplateID = F, TestType = F, TestVersion = F, SortNumber = F, FieldGUIDTestColumn1 = F, TestColumnHeaderOverride1 = F, FieldGUIDTestColumn2 = F, TestColumnHeaderOverride2 = F, FieldGUIDTestColumn3 = F, TestColumnHeaderOverride3 = F, FieldGUIDTestColumn4 = F, TestColumnHeaderOverride4 = F, FieldGUIDTestColumn5 = F, TestColumnHeaderOverride5 = F, FieldGUIDTestColumn6 = F, TestColumnHeaderOverride6 = F, FieldGUIDTestColumn7 = F, TestColumnHeaderOverride7 = F, FieldGUIDTestColumn8 = F, TestColumnHeaderOverride8 = F, FieldGUIDTestColumn9 = F, TestColumnHeaderOverride9 = F, FieldGUIDTestColumn10 = F, TestColumnHeaderOverride10 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportTemplateTestTypeIDClonedFrom = F, PrintHighestScoreOnly = F, TestCode = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateTestTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateTestType", objectId = GradeReportTemplateTestTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateTestType
	#'
	#' This function deletes a GradeReportTemplateTestType
	#' @param GradeReportTemplateTestTypeID The ID of the GradeReportTemplateTestType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateTestTypeID of the deleted GradeReportTemplateTestType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateTestType <- function(GradeReportTemplateTestTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateTestType", objectId = GradeReportTemplateTestTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateTestType
	#'
	#' This function creates a GradeReportTemplateTestType
	#' @param fieldNames The field values to give the created GradeReportTemplateTestType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateTestType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateTestType <- function(GradeReportTemplateID = NULL, TestType = NULL, TestVersion = NULL, SortNumber = NULL, FieldGUIDTestColumn1 = NULL, TestColumnHeaderOverride1 = NULL, FieldGUIDTestColumn2 = NULL, TestColumnHeaderOverride2 = NULL, FieldGUIDTestColumn3 = NULL, TestColumnHeaderOverride3 = NULL, FieldGUIDTestColumn4 = NULL, TestColumnHeaderOverride4 = NULL, FieldGUIDTestColumn5 = NULL, TestColumnHeaderOverride5 = NULL, FieldGUIDTestColumn6 = NULL, TestColumnHeaderOverride6 = NULL, FieldGUIDTestColumn7 = NULL, TestColumnHeaderOverride7 = NULL, FieldGUIDTestColumn8 = NULL, TestColumnHeaderOverride8 = NULL, FieldGUIDTestColumn9 = NULL, TestColumnHeaderOverride9 = NULL, FieldGUIDTestColumn10 = NULL, TestColumnHeaderOverride10 = NULL, GradeReportTemplateTestTypeIDClonedFrom = NULL, PrintHighestScoreOnly = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateTestType", body = list(DataObject = body), searchFields = append("GradeReportTemplateTestTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateTestType
	#'
	#' This function modifies a GradeReportTemplateTestType
	#' @param fieldNames The field values to give the modified GradeReportTemplateTestType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateTestType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateTestType <- function(GradeReportTemplateTestTypeID, GradeReportTemplateID = NULL, TestType = NULL, TestVersion = NULL, SortNumber = NULL, FieldGUIDTestColumn1 = NULL, TestColumnHeaderOverride1 = NULL, FieldGUIDTestColumn2 = NULL, TestColumnHeaderOverride2 = NULL, FieldGUIDTestColumn3 = NULL, TestColumnHeaderOverride3 = NULL, FieldGUIDTestColumn4 = NULL, TestColumnHeaderOverride4 = NULL, FieldGUIDTestColumn5 = NULL, TestColumnHeaderOverride5 = NULL, FieldGUIDTestColumn6 = NULL, TestColumnHeaderOverride6 = NULL, FieldGUIDTestColumn7 = NULL, TestColumnHeaderOverride7 = NULL, FieldGUIDTestColumn8 = NULL, TestColumnHeaderOverride8 = NULL, FieldGUIDTestColumn9 = NULL, TestColumnHeaderOverride9 = NULL, FieldGUIDTestColumn10 = NULL, TestColumnHeaderOverride10 = NULL, GradeReportTemplateTestTypeIDClonedFrom = NULL, PrintHighestScoreOnly = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateTestType", objectId = GradeReportTemplateTestTypeID, body = list(DataObject = body), searchFields = append("GradeReportTemplateTestTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportAcademicSessionTemplates
	#'
	#' This function returns a dataframe or json object of GradeReportAcademicSessionTemplates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportAcademicSessionTemplates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportAcademicSessionTemplates <- function(searchConditionsList = NULL, GradeReportAcademicSessionTemplateID = F, GradeReportAcademicSessionTemplateGroupID = F, HeaderDescription = F, IncludeNonCreditEarningCourses = F, SortNumber = F, CourseFilter = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, BreakBySchoolYear = F, UseSchoolYearDescending = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportAcademicSessionTemplate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportAcademicSessionTemplate
	#'
	#' This function returns a dataframe or json object of a GradeReportAcademicSessionTemplate
	#' @param GradeReportAcademicSessionTemplateID The ID of the GradeReportAcademicSessionTemplate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportAcademicSessionTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportAcademicSessionTemplate <- function(GradeReportAcademicSessionTemplateID, GradeReportAcademicSessionTemplateGroupID = F, HeaderDescription = F, IncludeNonCreditEarningCourses = F, SortNumber = F, CourseFilter = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, BreakBySchoolYear = F, UseSchoolYearDescending = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportAcademicSessionTemplateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplate", objectId = GradeReportAcademicSessionTemplateID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportAcademicSessionTemplate
	#'
	#' This function deletes a GradeReportAcademicSessionTemplate
	#' @param GradeReportAcademicSessionTemplateID The ID of the GradeReportAcademicSessionTemplate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportAcademicSessionTemplateID of the deleted GradeReportAcademicSessionTemplate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportAcademicSessionTemplate <- function(GradeReportAcademicSessionTemplateID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplate", objectId = GradeReportAcademicSessionTemplateID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportAcademicSessionTemplate
	#'
	#' This function creates a GradeReportAcademicSessionTemplate
	#' @param fieldNames The field values to give the created GradeReportAcademicSessionTemplate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportAcademicSessionTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportAcademicSessionTemplate <- function(GradeReportAcademicSessionTemplateGroupID = NULL, HeaderDescription = NULL, IncludeNonCreditEarningCourses = NULL, SortNumber = NULL, CourseFilter = NULL, BreakBySchoolYear = NULL, UseSchoolYearDescending = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplate", body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportAcademicSessionTemplate
	#'
	#' This function modifies a GradeReportAcademicSessionTemplate
	#' @param fieldNames The field values to give the modified GradeReportAcademicSessionTemplate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportAcademicSessionTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportAcademicSessionTemplate <- function(GradeReportAcademicSessionTemplateID, GradeReportAcademicSessionTemplateGroupID = NULL, HeaderDescription = NULL, IncludeNonCreditEarningCourses = NULL, SortNumber = NULL, CourseFilter = NULL, BreakBySchoolYear = NULL, UseSchoolYearDescending = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplate", objectId = GradeReportAcademicSessionTemplateID, body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportAcademicSessionTemplateCourses
	#'
	#' This function returns a dataframe or json object of GradeReportAcademicSessionTemplateCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportAcademicSessionTemplateCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportAcademicSessionTemplateCourses <- function(searchConditionsList = NULL, GradeReportAcademicSessionTemplateCourseID = F, GradeReportAcademicSessionTemplateID = F, CourseID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportAcademicSessionTemplateCourseIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportAcademicSessionTemplateCourse
	#'
	#' This function returns a dataframe or json object of a GradeReportAcademicSessionTemplateCourse
	#' @param GradeReportAcademicSessionTemplateCourseID The ID of the GradeReportAcademicSessionTemplateCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportAcademicSessionTemplateCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportAcademicSessionTemplateCourse <- function(GradeReportAcademicSessionTemplateCourseID, GradeReportAcademicSessionTemplateID = F, CourseID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeReportAcademicSessionTemplateCourseIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportAcademicSessionTemplateCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourse", objectId = GradeReportAcademicSessionTemplateCourseID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportAcademicSessionTemplateCourse
	#'
	#' This function deletes a GradeReportAcademicSessionTemplateCourse
	#' @param GradeReportAcademicSessionTemplateCourseID The ID of the GradeReportAcademicSessionTemplateCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportAcademicSessionTemplateCourseID of the deleted GradeReportAcademicSessionTemplateCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportAcademicSessionTemplateCourse <- function(GradeReportAcademicSessionTemplateCourseID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourse", objectId = GradeReportAcademicSessionTemplateCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportAcademicSessionTemplateCourse
	#'
	#' This function creates a GradeReportAcademicSessionTemplateCourse
	#' @param fieldNames The field values to give the created GradeReportAcademicSessionTemplateCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportAcademicSessionTemplateCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportAcademicSessionTemplateCourse <- function(GradeReportAcademicSessionTemplateID = NULL, CourseID = NULL, GradeReportAcademicSessionTemplateCourseIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourse", body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportAcademicSessionTemplateCourse
	#'
	#' This function modifies a GradeReportAcademicSessionTemplateCourse
	#' @param fieldNames The field values to give the modified GradeReportAcademicSessionTemplateCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportAcademicSessionTemplateCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportAcademicSessionTemplateCourse <- function(GradeReportAcademicSessionTemplateCourseID, GradeReportAcademicSessionTemplateID = NULL, CourseID = NULL, GradeReportAcademicSessionTemplateCourseIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourse", objectId = GradeReportAcademicSessionTemplateCourseID, body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportAcademicSessionTemplateCourseGroups
	#'
	#' This function returns a dataframe or json object of GradeReportAcademicSessionTemplateCourseGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateCourseGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateCourseGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateCourseGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportAcademicSessionTemplateCourseGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportAcademicSessionTemplateCourseGroups <- function(searchConditionsList = NULL, GradeReportAcademicSessionTemplateCourseGroupID = F, GradeReportAcademicSessionTemplateID = F, CourseGroupID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourseGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportAcademicSessionTemplateCourseGroup
	#'
	#' This function returns a dataframe or json object of a GradeReportAcademicSessionTemplateCourseGroup
	#' @param GradeReportAcademicSessionTemplateCourseGroupID The ID of the GradeReportAcademicSessionTemplateCourseGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateCourseGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateCourseGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateCourseGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportAcademicSessionTemplateCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportAcademicSessionTemplateCourseGroup <- function(GradeReportAcademicSessionTemplateCourseGroupID, GradeReportAcademicSessionTemplateID = F, CourseGroupID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportAcademicSessionTemplateCourseGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourseGroup", objectId = GradeReportAcademicSessionTemplateCourseGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportAcademicSessionTemplateCourseGroup
	#'
	#' This function deletes a GradeReportAcademicSessionTemplateCourseGroup
	#' @param GradeReportAcademicSessionTemplateCourseGroupID The ID of the GradeReportAcademicSessionTemplateCourseGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportAcademicSessionTemplateCourseGroupID of the deleted GradeReportAcademicSessionTemplateCourseGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportAcademicSessionTemplateCourseGroup <- function(GradeReportAcademicSessionTemplateCourseGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourseGroup", objectId = GradeReportAcademicSessionTemplateCourseGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportAcademicSessionTemplateCourseGroup
	#'
	#' This function creates a GradeReportAcademicSessionTemplateCourseGroup
	#' @param fieldNames The field values to give the created GradeReportAcademicSessionTemplateCourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportAcademicSessionTemplateCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportAcademicSessionTemplateCourseGroup <- function(GradeReportAcademicSessionTemplateID = NULL, CourseGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourseGroup", body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateCourseGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportAcademicSessionTemplateCourseGroup
	#'
	#' This function modifies a GradeReportAcademicSessionTemplateCourseGroup
	#' @param fieldNames The field values to give the modified GradeReportAcademicSessionTemplateCourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportAcademicSessionTemplateCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportAcademicSessionTemplateCourseGroup <- function(GradeReportAcademicSessionTemplateCourseGroupID, GradeReportAcademicSessionTemplateID = NULL, CourseGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateCourseGroup", objectId = GradeReportAcademicSessionTemplateCourseGroupID, body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateCourseGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportAcademicSessionTemplateGradeLevels
	#'
	#' This function returns a dataframe or json object of GradeReportAcademicSessionTemplateGradeLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateGradeLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateGradeLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateGradeLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportAcademicSessionTemplateGradeLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportAcademicSessionTemplateGradeLevels <- function(searchConditionsList = NULL, GradeReportAcademicSessionTemplateGradeLevelID = F, GradeReportAcademicSessionTemplateID = F, GradeLevelID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGradeLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportAcademicSessionTemplateGradeLevel
	#'
	#' This function returns a dataframe or json object of a GradeReportAcademicSessionTemplateGradeLevel
	#' @param GradeReportAcademicSessionTemplateGradeLevelID The ID of the GradeReportAcademicSessionTemplateGradeLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateGradeLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateGradeLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateGradeLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportAcademicSessionTemplateGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportAcademicSessionTemplateGradeLevel <- function(GradeReportAcademicSessionTemplateGradeLevelID, GradeReportAcademicSessionTemplateID = F, GradeLevelID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportAcademicSessionTemplateGradeLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGradeLevel", objectId = GradeReportAcademicSessionTemplateGradeLevelID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportAcademicSessionTemplateGradeLevel
	#'
	#' This function deletes a GradeReportAcademicSessionTemplateGradeLevel
	#' @param GradeReportAcademicSessionTemplateGradeLevelID The ID of the GradeReportAcademicSessionTemplateGradeLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportAcademicSessionTemplateGradeLevelID of the deleted GradeReportAcademicSessionTemplateGradeLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportAcademicSessionTemplateGradeLevel <- function(GradeReportAcademicSessionTemplateGradeLevelID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGradeLevel", objectId = GradeReportAcademicSessionTemplateGradeLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportAcademicSessionTemplateGradeLevel
	#'
	#' This function creates a GradeReportAcademicSessionTemplateGradeLevel
	#' @param fieldNames The field values to give the created GradeReportAcademicSessionTemplateGradeLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportAcademicSessionTemplateGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportAcademicSessionTemplateGradeLevel <- function(GradeReportAcademicSessionTemplateID = NULL, GradeLevelID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGradeLevel", body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateGradeLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportAcademicSessionTemplateGradeLevel
	#'
	#' This function modifies a GradeReportAcademicSessionTemplateGradeLevel
	#' @param fieldNames The field values to give the modified GradeReportAcademicSessionTemplateGradeLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportAcademicSessionTemplateGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportAcademicSessionTemplateGradeLevel <- function(GradeReportAcademicSessionTemplateGradeLevelID, GradeReportAcademicSessionTemplateID = NULL, GradeLevelID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGradeLevel", objectId = GradeReportAcademicSessionTemplateGradeLevelID, body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateGradeLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportAcademicSessionTemplateGroups
	#'
	#' This function returns a dataframe or json object of GradeReportAcademicSessionTemplateGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportAcademicSessionTemplateGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportAcademicSessionTemplateGroups <- function(searchConditionsList = NULL, GradeReportAcademicSessionTemplateGroupID = F, DistrictID = F, Description = F, GPAMethodID = F, GPABucketID = F, GPAField = F, IncludeGPA = F, IncludeEarnedCredits = F, IncludeSchoolYearDescription = F, GPALabel1 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EarnedCreditsMethodIDDefaultOverride = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportAcademicSessionTemplateGroup
	#'
	#' This function returns a dataframe or json object of a GradeReportAcademicSessionTemplateGroup
	#' @param GradeReportAcademicSessionTemplateGroupID The ID of the GradeReportAcademicSessionTemplateGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportAcademicSessionTemplateGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportAcademicSessionTemplateGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportAcademicSessionTemplateGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportAcademicSessionTemplateGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportAcademicSessionTemplateGroup <- function(GradeReportAcademicSessionTemplateGroupID, DistrictID = F, Description = F, GPAMethodID = F, GPABucketID = F, GPAField = F, IncludeGPA = F, IncludeEarnedCredits = F, IncludeSchoolYearDescription = F, GPALabel1 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EarnedCreditsMethodIDDefaultOverride = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportAcademicSessionTemplateGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGroup", objectId = GradeReportAcademicSessionTemplateGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportAcademicSessionTemplateGroup
	#'
	#' This function deletes a GradeReportAcademicSessionTemplateGroup
	#' @param GradeReportAcademicSessionTemplateGroupID The ID of the GradeReportAcademicSessionTemplateGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportAcademicSessionTemplateGroupID of the deleted GradeReportAcademicSessionTemplateGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportAcademicSessionTemplateGroup <- function(GradeReportAcademicSessionTemplateGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGroup", objectId = GradeReportAcademicSessionTemplateGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportAcademicSessionTemplateGroup
	#'
	#' This function creates a GradeReportAcademicSessionTemplateGroup
	#' @param fieldNames The field values to give the created GradeReportAcademicSessionTemplateGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportAcademicSessionTemplateGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportAcademicSessionTemplateGroup <- function(DistrictID = NULL, Description = NULL, GPAMethodID = NULL, GPABucketID = NULL, GPAField = NULL, IncludeGPA = NULL, IncludeEarnedCredits = NULL, IncludeSchoolYearDescription = NULL, EarnedCreditsMethodIDDefaultOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGroup", body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportAcademicSessionTemplateGroup
	#'
	#' This function modifies a GradeReportAcademicSessionTemplateGroup
	#' @param fieldNames The field values to give the modified GradeReportAcademicSessionTemplateGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportAcademicSessionTemplateGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportAcademicSessionTemplateGroup <- function(GradeReportAcademicSessionTemplateGroupID, DistrictID = NULL, Description = NULL, GPAMethodID = NULL, GPABucketID = NULL, GPAField = NULL, IncludeGPA = NULL, IncludeEarnedCredits = NULL, IncludeSchoolYearDescription = NULL, EarnedCreditsMethodIDDefaultOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportAcademicSessionTemplateGroup", objectId = GradeReportAcademicSessionTemplateGroupID, body = list(DataObject = body), searchFields = append("GradeReportAcademicSessionTemplateGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeMarkPointSetErrors
	#'
	#' This function returns a dataframe or json object of TempGradeMarkPointSetErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeMarkPointSetErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeMarkPointSetErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeMarkPointSetError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeMarkPointSetErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeMarkPointSetErrors <- function(searchConditionsList = NULL, TempGradeMarkPointSetErrorID = F, GradeMarkCode = F, PointSetName = F, GPAMethodName = F, ErrorString = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeMarkPointSetError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeMarkPointSetError
	#'
	#' This function returns a dataframe or json object of a TempGradeMarkPointSetError
	#' @param TempGradeMarkPointSetErrorID The ID of the TempGradeMarkPointSetError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeMarkPointSetError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeMarkPointSetError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeMarkPointSetError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeMarkPointSetError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeMarkPointSetError <- function(TempGradeMarkPointSetErrorID, GradeMarkCode = F, PointSetName = F, GPAMethodName = F, ErrorString = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeMarkPointSetErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeMarkPointSetError", objectId = TempGradeMarkPointSetErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeMarkPointSetError
	#'
	#' This function deletes a TempGradeMarkPointSetError
	#' @param TempGradeMarkPointSetErrorID The ID of the TempGradeMarkPointSetError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeMarkPointSetErrorID of the deleted TempGradeMarkPointSetError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeMarkPointSetError <- function(TempGradeMarkPointSetErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeMarkPointSetError", objectId = TempGradeMarkPointSetErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeMarkPointSetError
	#'
	#' This function creates a TempGradeMarkPointSetError
	#' @param fieldNames The field values to give the created TempGradeMarkPointSetError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeMarkPointSetError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeMarkPointSetError <- function(GradeMarkCode = NULL, PointSetName = NULL, GPAMethodName = NULL, ErrorString = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeMarkPointSetError", body = list(DataObject = body), searchFields = append("TempGradeMarkPointSetErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeMarkPointSetError
	#'
	#' This function modifies a TempGradeMarkPointSetError
	#' @param fieldNames The field values to give the modified TempGradeMarkPointSetError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeMarkPointSetError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeMarkPointSetError <- function(TempGradeMarkPointSetErrorID, GradeMarkCode = NULL, PointSetName = NULL, GPAMethodName = NULL, ErrorString = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeMarkPointSetError", objectId = TempGradeMarkPointSetErrorID, body = list(DataObject = body), searchFields = append("TempGradeMarkPointSetErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeReportTemplates
	#'
	#' This function returns a dataframe or json object of TempGradeReportTemplates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportTemplates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportTemplates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportTemplate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeReportTemplates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeReportTemplates <- function(searchConditionsList = NULL, TempGradeReportTemplateID = F, EntityID = F, SchoolYearID = F, EntityCodeNameClonedFrom = F, EntityCodeNameClonedTo = F, OriginalGradeReportTemplateDescription = F, NewGradeReportTemplateDescription = F, HasErrors = F, ErrorCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeReportTemplate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeReportTemplate
	#'
	#' This function returns a dataframe or json object of a TempGradeReportTemplate
	#' @param TempGradeReportTemplateID The ID of the TempGradeReportTemplate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportTemplate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportTemplate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportTemplate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeReportTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeReportTemplate <- function(TempGradeReportTemplateID, EntityID = F, SchoolYearID = F, EntityCodeNameClonedFrom = F, EntityCodeNameClonedTo = F, OriginalGradeReportTemplateDescription = F, NewGradeReportTemplateDescription = F, HasErrors = F, ErrorCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeReportTemplateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeReportTemplate", objectId = TempGradeReportTemplateID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeReportTemplate
	#'
	#' This function deletes a TempGradeReportTemplate
	#' @param TempGradeReportTemplateID The ID of the TempGradeReportTemplate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeReportTemplateID of the deleted TempGradeReportTemplate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeReportTemplate <- function(TempGradeReportTemplateID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeReportTemplate", objectId = TempGradeReportTemplateID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeReportTemplate
	#'
	#' This function creates a TempGradeReportTemplate
	#' @param fieldNames The field values to give the created TempGradeReportTemplate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeReportTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeReportTemplate <- function(EntityID = NULL, SchoolYearID = NULL, EntityCodeNameClonedFrom = NULL, EntityCodeNameClonedTo = NULL, OriginalGradeReportTemplateDescription = NULL, NewGradeReportTemplateDescription = NULL, HasErrors = NULL, ErrorCount = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeReportTemplate", body = list(DataObject = body), searchFields = append("TempGradeReportTemplateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeReportTemplate
	#'
	#' This function modifies a TempGradeReportTemplate
	#' @param fieldNames The field values to give the modified TempGradeReportTemplate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeReportTemplate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeReportTemplate <- function(TempGradeReportTemplateID, EntityID = NULL, SchoolYearID = NULL, EntityCodeNameClonedFrom = NULL, EntityCodeNameClonedTo = NULL, OriginalGradeReportTemplateDescription = NULL, NewGradeReportTemplateDescription = NULL, HasErrors = NULL, ErrorCount = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeReportTemplate", objectId = TempGradeReportTemplateID, body = list(DataObject = body), searchFields = append("TempGradeReportTemplateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeReportTemplateErrors
	#'
	#' This function returns a dataframe or json object of TempGradeReportTemplateErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportTemplateErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportTemplateErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportTemplateError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeReportTemplateErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeReportTemplateErrors <- function(searchConditionsList = NULL, TempGradeReportTemplateErrorID = F, TempGradeReportTemplateID = F, Error = F, ErrorDetail = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeReportTemplateError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeReportTemplateError
	#'
	#' This function returns a dataframe or json object of a TempGradeReportTemplateError
	#' @param TempGradeReportTemplateErrorID The ID of the TempGradeReportTemplateError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportTemplateError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportTemplateError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportTemplateError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeReportTemplateError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeReportTemplateError <- function(TempGradeReportTemplateErrorID, TempGradeReportTemplateID = F, Error = F, ErrorDetail = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeReportTemplateErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeReportTemplateError", objectId = TempGradeReportTemplateErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeReportTemplateError
	#'
	#' This function deletes a TempGradeReportTemplateError
	#' @param TempGradeReportTemplateErrorID The ID of the TempGradeReportTemplateError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeReportTemplateErrorID of the deleted TempGradeReportTemplateError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeReportTemplateError <- function(TempGradeReportTemplateErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeReportTemplateError", objectId = TempGradeReportTemplateErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeReportTemplateError
	#'
	#' This function creates a TempGradeReportTemplateError
	#' @param fieldNames The field values to give the created TempGradeReportTemplateError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeReportTemplateError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeReportTemplateError <- function(TempGradeReportTemplateID = NULL, Error = NULL, ErrorDetail = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeReportTemplateError", body = list(DataObject = body), searchFields = append("TempGradeReportTemplateErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeReportTemplateError
	#'
	#' This function modifies a TempGradeReportTemplateError
	#' @param fieldNames The field values to give the modified TempGradeReportTemplateError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeReportTemplateError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeReportTemplateError <- function(TempGradeReportTemplateErrorID, TempGradeReportTemplateID = NULL, Error = NULL, ErrorDetail = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeReportTemplateError", objectId = TempGradeReportTemplateErrorID, body = list(DataObject = body), searchFields = append("TempGradeReportTemplateErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPAMethodCourseGroups
	#'
	#' This function returns a dataframe or json object of GPAMethodCourseGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethodCourseGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethodCourseGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethodCourseGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPAMethodCourseGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPAMethodCourseGroups <- function(searchConditionsList = NULL, GPAMethodCourseGroupID = F, GPAMethodID = F, CourseGroupID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPAMethodCourseGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPAMethodCourseGroup
	#'
	#' This function returns a dataframe or json object of a GPAMethodCourseGroup
	#' @param GPAMethodCourseGroupID The ID of the GPAMethodCourseGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethodCourseGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethodCourseGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethodCourseGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPAMethodCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPAMethodCourseGroup <- function(GPAMethodCourseGroupID, GPAMethodID = F, CourseGroupID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPAMethodCourseGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPAMethodCourseGroup", objectId = GPAMethodCourseGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPAMethodCourseGroup
	#'
	#' This function deletes a GPAMethodCourseGroup
	#' @param GPAMethodCourseGroupID The ID of the GPAMethodCourseGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPAMethodCourseGroupID of the deleted GPAMethodCourseGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPAMethodCourseGroup <- function(GPAMethodCourseGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPAMethodCourseGroup", objectId = GPAMethodCourseGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPAMethodCourseGroup
	#'
	#' This function creates a GPAMethodCourseGroup
	#' @param fieldNames The field values to give the created GPAMethodCourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPAMethodCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPAMethodCourseGroup <- function(GPAMethodID = NULL, CourseGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPAMethodCourseGroup", body = list(DataObject = body), searchFields = append("GPAMethodCourseGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPAMethodCourseGroup
	#'
	#' This function modifies a GPAMethodCourseGroup
	#' @param fieldNames The field values to give the modified GPAMethodCourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPAMethodCourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPAMethodCourseGroup <- function(GPAMethodCourseGroupID, GPAMethodID = NULL, CourseGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPAMethodCourseGroup", objectId = GPAMethodCourseGroupID, body = list(DataObject = body), searchFields = append("GPAMethodCourseGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradReqRankGPAMethodEntities
	#'
	#' This function returns a dataframe or json object of GradReqRankGPAMethodEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankGPAMethodEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankGPAMethodEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankGPAMethodEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradReqRankGPAMethodEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradReqRankGPAMethodEntities <- function(searchConditionsList = NULL, GradReqRankGPAMethodEntityID = F, GPAMethodEntityID = F, GradeBucketFlagIDLocalCredit = F, GradeBucketIDTermOne = F, GradeBucketIDTermTwo = F, PlanID = F, GradPlanSetting = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradReqRankGPAMethodEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradReqRankGPAMethodEntity
	#'
	#' This function returns a dataframe or json object of a GradReqRankGPAMethodEntity
	#' @param GradReqRankGPAMethodEntityID The ID of the GradReqRankGPAMethodEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankGPAMethodEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankGPAMethodEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankGPAMethodEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradReqRankGPAMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradReqRankGPAMethodEntity <- function(GradReqRankGPAMethodEntityID, GPAMethodEntityID = F, GradeBucketFlagIDLocalCredit = F, GradeBucketIDTermOne = F, GradeBucketIDTermTwo = F, PlanID = F, GradPlanSetting = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradReqRankGPAMethodEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradReqRankGPAMethodEntity", objectId = GradReqRankGPAMethodEntityID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradReqRankGPAMethodEntity
	#'
	#' This function deletes a GradReqRankGPAMethodEntity
	#' @param GradReqRankGPAMethodEntityID The ID of the GradReqRankGPAMethodEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradReqRankGPAMethodEntityID of the deleted GradReqRankGPAMethodEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradReqRankGPAMethodEntity <- function(GradReqRankGPAMethodEntityID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradReqRankGPAMethodEntity", objectId = GradReqRankGPAMethodEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradReqRankGPAMethodEntity
	#'
	#' This function creates a GradReqRankGPAMethodEntity
	#' @param fieldNames The field values to give the created GradReqRankGPAMethodEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradReqRankGPAMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradReqRankGPAMethodEntity <- function(GPAMethodEntityID = NULL, GradeBucketFlagIDLocalCredit = NULL, GradeBucketIDTermOne = NULL, GradeBucketIDTermTwo = NULL, PlanID = NULL, GradPlanSetting = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradReqRankGPAMethodEntity", body = list(DataObject = body), searchFields = append("GradReqRankGPAMethodEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradReqRankGPAMethodEntity
	#'
	#' This function modifies a GradReqRankGPAMethodEntity
	#' @param fieldNames The field values to give the modified GradReqRankGPAMethodEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradReqRankGPAMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradReqRankGPAMethodEntity <- function(GradReqRankGPAMethodEntityID, GPAMethodEntityID = NULL, GradeBucketFlagIDLocalCredit = NULL, GradeBucketIDTermOne = NULL, GradeBucketIDTermTwo = NULL, PlanID = NULL, GradPlanSetting = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradReqRankGPAMethodEntity", objectId = GradReqRankGPAMethodEntityID, body = list(DataObject = body), searchFields = append("GradReqRankGPAMethodEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradReqRankSchoolYearIncludeLocalCredits
	#'
	#' This function returns a dataframe or json object of GradReqRankSchoolYearIncludeLocalCredits
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankSchoolYearIncludeLocalCredits. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankSchoolYearIncludeLocalCredits.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankSchoolYearIncludeLocalCredit') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradReqRankSchoolYearIncludeLocalCredits
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradReqRankSchoolYearIncludeLocalCredits <- function(searchConditionsList = NULL, GradReqRankSchoolYearIncludeLocalCreditID = F, GPAMethodID = F, SchoolYearID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradReqRankSchoolYearIncludeLocalCredit", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradReqRankSchoolYearIncludeLocalCredit
	#'
	#' This function returns a dataframe or json object of a GradReqRankSchoolYearIncludeLocalCredit
	#' @param GradReqRankSchoolYearIncludeLocalCreditID The ID of the GradReqRankSchoolYearIncludeLocalCredit to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankSchoolYearIncludeLocalCredit. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankSchoolYearIncludeLocalCredit.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankSchoolYearIncludeLocalCredit') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradReqRankSchoolYearIncludeLocalCredit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradReqRankSchoolYearIncludeLocalCredit <- function(GradReqRankSchoolYearIncludeLocalCreditID, GPAMethodID = F, SchoolYearID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradReqRankSchoolYearIncludeLocalCreditID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradReqRankSchoolYearIncludeLocalCredit", objectId = GradReqRankSchoolYearIncludeLocalCreditID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradReqRankSchoolYearIncludeLocalCredit
	#'
	#' This function deletes a GradReqRankSchoolYearIncludeLocalCredit
	#' @param GradReqRankSchoolYearIncludeLocalCreditID The ID of the GradReqRankSchoolYearIncludeLocalCredit to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradReqRankSchoolYearIncludeLocalCreditID of the deleted GradReqRankSchoolYearIncludeLocalCredit.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradReqRankSchoolYearIncludeLocalCredit <- function(GradReqRankSchoolYearIncludeLocalCreditID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradReqRankSchoolYearIncludeLocalCredit", objectId = GradReqRankSchoolYearIncludeLocalCreditID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradReqRankSchoolYearIncludeLocalCredit
	#'
	#' This function creates a GradReqRankSchoolYearIncludeLocalCredit
	#' @param fieldNames The field values to give the created GradReqRankSchoolYearIncludeLocalCredit. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradReqRankSchoolYearIncludeLocalCredit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradReqRankSchoolYearIncludeLocalCredit <- function(GPAMethodID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradReqRankSchoolYearIncludeLocalCredit", body = list(DataObject = body), searchFields = append("GradReqRankSchoolYearIncludeLocalCreditID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradReqRankSchoolYearIncludeLocalCredit
	#'
	#' This function modifies a GradReqRankSchoolYearIncludeLocalCredit
	#' @param fieldNames The field values to give the modified GradReqRankSchoolYearIncludeLocalCredit. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradReqRankSchoolYearIncludeLocalCredit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradReqRankSchoolYearIncludeLocalCredit <- function(GradReqRankSchoolYearIncludeLocalCreditID, GPAMethodID = NULL, SchoolYearID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradReqRankSchoolYearIncludeLocalCredit", objectId = GradReqRankSchoolYearIncludeLocalCreditID, body = list(DataObject = body), searchFields = append("GradReqRankSchoolYearIncludeLocalCreditID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportEndorsementRows
	#'
	#' This function returns a dataframe or json object of GradeReportEndorsementRows
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportEndorsementRows. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportEndorsementRows.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportEndorsementRow') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportEndorsementRows
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportEndorsementRows <- function(searchConditionsList = NULL, GradeReportEndorsementRowID = F, GradeReportStudentID = F, Description = F, Status = F, IsDistrictDefined = F, Sort1 = F, Sort2 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportEndorsementRow", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportEndorsementRow
	#'
	#' This function returns a dataframe or json object of a GradeReportEndorsementRow
	#' @param GradeReportEndorsementRowID The ID of the GradeReportEndorsementRow to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportEndorsementRow. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportEndorsementRow.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportEndorsementRow') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportEndorsementRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportEndorsementRow <- function(GradeReportEndorsementRowID, GradeReportStudentID = F, Description = F, Status = F, IsDistrictDefined = F, Sort1 = F, Sort2 = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportEndorsementRowID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportEndorsementRow", objectId = GradeReportEndorsementRowID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportEndorsementRow
	#'
	#' This function deletes a GradeReportEndorsementRow
	#' @param GradeReportEndorsementRowID The ID of the GradeReportEndorsementRow to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportEndorsementRowID of the deleted GradeReportEndorsementRow.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportEndorsementRow <- function(GradeReportEndorsementRowID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportEndorsementRow", objectId = GradeReportEndorsementRowID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportEndorsementRow
	#'
	#' This function creates a GradeReportEndorsementRow
	#' @param fieldNames The field values to give the created GradeReportEndorsementRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportEndorsementRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportEndorsementRow <- function(GradeReportStudentID = NULL, Description = NULL, Status = NULL, IsDistrictDefined = NULL, Sort1 = NULL, Sort2 = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportEndorsementRow", body = list(DataObject = body), searchFields = append("GradeReportEndorsementRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportEndorsementRow
	#'
	#' This function modifies a GradeReportEndorsementRow
	#' @param fieldNames The field values to give the modified GradeReportEndorsementRow. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportEndorsementRow
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportEndorsementRow <- function(GradeReportEndorsementRowID, GradeReportStudentID = NULL, Description = NULL, Status = NULL, IsDistrictDefined = NULL, Sort1 = NULL, Sort2 = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportEndorsementRow", objectId = GradeReportEndorsementRowID, body = list(DataObject = body), searchFields = append("GradeReportEndorsementRowID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateEndorsements
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateEndorsements
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateEndorsements. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateEndorsements.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateEndorsement') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateEndorsements
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateEndorsements <- function(searchConditionsList = NULL, GradeReportTemplateEndorsementID = F, GradeReportTemplateID = F, EndorsementID = F, GradYearLow = F, GradYearHigh = F, OrderNumber = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateEndorsement", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateEndorsement
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateEndorsement
	#' @param GradeReportTemplateEndorsementID The ID of the GradeReportTemplateEndorsement to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateEndorsement. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateEndorsement.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateEndorsement') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateEndorsement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateEndorsement <- function(GradeReportTemplateEndorsementID, GradeReportTemplateID = F, EndorsementID = F, GradYearLow = F, GradYearHigh = F, OrderNumber = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateEndorsementID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateEndorsement", objectId = GradeReportTemplateEndorsementID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateEndorsement
	#'
	#' This function deletes a GradeReportTemplateEndorsement
	#' @param GradeReportTemplateEndorsementID The ID of the GradeReportTemplateEndorsement to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateEndorsementID of the deleted GradeReportTemplateEndorsement.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateEndorsement <- function(GradeReportTemplateEndorsementID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateEndorsement", objectId = GradeReportTemplateEndorsementID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateEndorsement
	#'
	#' This function creates a GradeReportTemplateEndorsement
	#' @param fieldNames The field values to give the created GradeReportTemplateEndorsement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateEndorsement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateEndorsement <- function(GradeReportTemplateID = NULL, EndorsementID = NULL, GradYearLow = NULL, GradYearHigh = NULL, OrderNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateEndorsement", body = list(DataObject = body), searchFields = append("GradeReportTemplateEndorsementID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateEndorsement
	#'
	#' This function modifies a GradeReportTemplateEndorsement
	#' @param fieldNames The field values to give the modified GradeReportTemplateEndorsement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateEndorsement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateEndorsement <- function(GradeReportTemplateEndorsementID, GradeReportTemplateID = NULL, EndorsementID = NULL, GradYearLow = NULL, GradYearHigh = NULL, OrderNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateEndorsement", objectId = GradeReportTemplateEndorsementID, body = list(DataObject = body), searchFields = append("GradeReportTemplateEndorsementID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradReqSubjectTypes
	#'
	#' This function returns a dataframe or json object of GradReqSubjectTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqSubjectTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqSubjectTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqSubjectType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradReqSubjectTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradReqSubjectTypes <- function(searchConditionsList = NULL, GradReqSubjectTypeID = F, Code = F, Description = F, DistrictID = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradReqSubjectType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradReqSubjectType
	#'
	#' This function returns a dataframe or json object of a GradReqSubjectType
	#' @param GradReqSubjectTypeID The ID of the GradReqSubjectType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqSubjectType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqSubjectType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqSubjectType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradReqSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradReqSubjectType <- function(GradReqSubjectTypeID, Code = F, Description = F, DistrictID = F, CodeDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradReqSubjectTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradReqSubjectType", objectId = GradReqSubjectTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradReqSubjectType
	#'
	#' This function deletes a GradReqSubjectType
	#' @param GradReqSubjectTypeID The ID of the GradReqSubjectType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradReqSubjectTypeID of the deleted GradReqSubjectType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradReqSubjectType <- function(GradReqSubjectTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradReqSubjectType", objectId = GradReqSubjectTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradReqSubjectType
	#'
	#' This function creates a GradReqSubjectType
	#' @param fieldNames The field values to give the created GradReqSubjectType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradReqSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradReqSubjectType <- function(Code = NULL, Description = NULL, DistrictID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradReqSubjectType", body = list(DataObject = body), searchFields = append("GradReqSubjectTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradReqSubjectType
	#'
	#' This function modifies a GradReqSubjectType
	#' @param fieldNames The field values to give the modified GradReqSubjectType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradReqSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradReqSubjectType <- function(GradReqSubjectTypeID, Code = NULL, Description = NULL, DistrictID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradReqSubjectType", objectId = GradReqSubjectTypeID, body = list(DataObject = body), searchFields = append("GradReqSubjectTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportRunHistoryAttachments
	#'
	#' This function returns a dataframe or json object of GradeReportRunHistoryAttachments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRunHistoryAttachments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRunHistoryAttachments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRunHistoryAttachment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportRunHistoryAttachments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportRunHistoryAttachments <- function(searchConditionsList = NULL, GradeReportRunHistoryAttachmentID = F, GradeReportRunHistoryID = F, AttachmentID = F, AttachmentCanBeViewedByStudentFamilyFamilyAccess = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SignedByGuardian = F, GuardianSignedTime = F, NameIDGuardianSignedBy = F, StudentID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportRunHistoryAttachment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportRunHistoryAttachment
	#'
	#' This function returns a dataframe or json object of a GradeReportRunHistoryAttachment
	#' @param GradeReportRunHistoryAttachmentID The ID of the GradeReportRunHistoryAttachment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportRunHistoryAttachment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportRunHistoryAttachment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportRunHistoryAttachment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportRunHistoryAttachment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportRunHistoryAttachment <- function(GradeReportRunHistoryAttachmentID, GradeReportRunHistoryID = F, AttachmentID = F, AttachmentCanBeViewedByStudentFamilyFamilyAccess = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SignedByGuardian = F, GuardianSignedTime = F, NameIDGuardianSignedBy = F, StudentID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportRunHistoryAttachmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportRunHistoryAttachment", objectId = GradeReportRunHistoryAttachmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportRunHistoryAttachment
	#'
	#' This function deletes a GradeReportRunHistoryAttachment
	#' @param GradeReportRunHistoryAttachmentID The ID of the GradeReportRunHistoryAttachment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportRunHistoryAttachmentID of the deleted GradeReportRunHistoryAttachment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportRunHistoryAttachment <- function(GradeReportRunHistoryAttachmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportRunHistoryAttachment", objectId = GradeReportRunHistoryAttachmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportRunHistoryAttachment
	#'
	#' This function creates a GradeReportRunHistoryAttachment
	#' @param fieldNames The field values to give the created GradeReportRunHistoryAttachment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportRunHistoryAttachment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportRunHistoryAttachment <- function(GradeReportRunHistoryID = NULL, AttachmentID = NULL, GuardianSignedTime = NULL, NameIDGuardianSignedBy = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportRunHistoryAttachment", body = list(DataObject = body), searchFields = append("GradeReportRunHistoryAttachmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportRunHistoryAttachment
	#'
	#' This function modifies a GradeReportRunHistoryAttachment
	#' @param fieldNames The field values to give the modified GradeReportRunHistoryAttachment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportRunHistoryAttachment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportRunHistoryAttachment <- function(GradeReportRunHistoryAttachmentID, GradeReportRunHistoryID = NULL, AttachmentID = NULL, GuardianSignedTime = NULL, NameIDGuardianSignedBy = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportRunHistoryAttachment", objectId = GradeReportRunHistoryAttachmentID, body = list(DataObject = body), searchFields = append("GradeReportRunHistoryAttachmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradReqRankHighSchoolGradeLevels
	#'
	#' This function returns a dataframe or json object of GradReqRankHighSchoolGradeLevels
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankHighSchoolGradeLevels. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankHighSchoolGradeLevels.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankHighSchoolGradeLevel') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradReqRankHighSchoolGradeLevels
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradReqRankHighSchoolGradeLevels <- function(searchConditionsList = NULL, GradReqRankHighSchoolGradeLevelID = F, GPAMethodID = F, GradeLevelID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradReqRankHighSchoolGradeLevel", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradReqRankHighSchoolGradeLevel
	#'
	#' This function returns a dataframe or json object of a GradReqRankHighSchoolGradeLevel
	#' @param GradReqRankHighSchoolGradeLevelID The ID of the GradReqRankHighSchoolGradeLevel to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankHighSchoolGradeLevel. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankHighSchoolGradeLevel.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankHighSchoolGradeLevel') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradReqRankHighSchoolGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradReqRankHighSchoolGradeLevel <- function(GradReqRankHighSchoolGradeLevelID, GPAMethodID = F, GradeLevelID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradReqRankHighSchoolGradeLevelID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradReqRankHighSchoolGradeLevel", objectId = GradReqRankHighSchoolGradeLevelID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradReqRankHighSchoolGradeLevel
	#'
	#' This function deletes a GradReqRankHighSchoolGradeLevel
	#' @param GradReqRankHighSchoolGradeLevelID The ID of the GradReqRankHighSchoolGradeLevel to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradReqRankHighSchoolGradeLevelID of the deleted GradReqRankHighSchoolGradeLevel.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradReqRankHighSchoolGradeLevel <- function(GradReqRankHighSchoolGradeLevelID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradReqRankHighSchoolGradeLevel", objectId = GradReqRankHighSchoolGradeLevelID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradReqRankHighSchoolGradeLevel
	#'
	#' This function creates a GradReqRankHighSchoolGradeLevel
	#' @param fieldNames The field values to give the created GradReqRankHighSchoolGradeLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradReqRankHighSchoolGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradReqRankHighSchoolGradeLevel <- function(GPAMethodID = NULL, GradeLevelID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradReqRankHighSchoolGradeLevel", body = list(DataObject = body), searchFields = append("GradReqRankHighSchoolGradeLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradReqRankHighSchoolGradeLevel
	#'
	#' This function modifies a GradReqRankHighSchoolGradeLevel
	#' @param fieldNames The field values to give the modified GradReqRankHighSchoolGradeLevel. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradReqRankHighSchoolGradeLevel
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradReqRankHighSchoolGradeLevel <- function(GradReqRankHighSchoolGradeLevelID, GPAMethodID = NULL, GradeLevelID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradReqRankHighSchoolGradeLevel", objectId = GradReqRankHighSchoolGradeLevelID, body = list(DataObject = body), searchFields = append("GradReqRankHighSchoolGradeLevelID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradReqRankGPACourseTypes
	#'
	#' This function returns a dataframe or json object of GradReqRankGPACourseTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankGPACourseTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankGPACourseTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankGPACourseType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradReqRankGPACourseTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradReqRankGPACourseTypes <- function(searchConditionsList = NULL, GradReqRankGPACourseTypeID = F, GradReqRankGPAMethodEntityID = F, CourseTypeID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradReqRankGPACourseType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradReqRankGPACourseType
	#'
	#' This function returns a dataframe or json object of a GradReqRankGPACourseType
	#' @param GradReqRankGPACourseTypeID The ID of the GradReqRankGPACourseType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradReqRankGPACourseType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradReqRankGPACourseType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradReqRankGPACourseType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradReqRankGPACourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradReqRankGPACourseType <- function(GradReqRankGPACourseTypeID, GradReqRankGPAMethodEntityID = F, CourseTypeID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradReqRankGPACourseTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradReqRankGPACourseType", objectId = GradReqRankGPACourseTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradReqRankGPACourseType
	#'
	#' This function deletes a GradReqRankGPACourseType
	#' @param GradReqRankGPACourseTypeID The ID of the GradReqRankGPACourseType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradReqRankGPACourseTypeID of the deleted GradReqRankGPACourseType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradReqRankGPACourseType <- function(GradReqRankGPACourseTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradReqRankGPACourseType", objectId = GradReqRankGPACourseTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradReqRankGPACourseType
	#'
	#' This function creates a GradReqRankGPACourseType
	#' @param fieldNames The field values to give the created GradReqRankGPACourseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradReqRankGPACourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradReqRankGPACourseType <- function(GradReqRankGPAMethodEntityID = NULL, CourseTypeID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradReqRankGPACourseType", body = list(DataObject = body), searchFields = append("GradReqRankGPACourseTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradReqRankGPACourseType
	#'
	#' This function modifies a GradReqRankGPACourseType
	#' @param fieldNames The field values to give the modified GradReqRankGPACourseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradReqRankGPACourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradReqRankGPACourseType <- function(GradReqRankGPACourseTypeID, GradReqRankGPAMethodEntityID = NULL, CourseTypeID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradReqRankGPACourseType", objectId = GradReqRankGPACourseTypeID, body = list(DataObject = body), searchFields = append("GradReqRankGPACourseTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GPAMethodGradeBucketFlagGPAPointsOverrides
	#'
	#' This function returns a dataframe or json object of GPAMethodGradeBucketFlagGPAPointsOverrides
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethodGradeBucketFlagGPAPointsOverrides. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethodGradeBucketFlagGPAPointsOverrides.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethodGradeBucketFlagGPAPointsOverride') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GPAMethodGradeBucketFlagGPAPointsOverrides
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGPAMethodGradeBucketFlagGPAPointsOverrides <- function(searchConditionsList = NULL, GPAMethodGradeBucketFlagGPAPointsOverrideID = F, GPAMethodID = F, GradeBucketFlagID = F, GPAPoints = F, Rank = F, GPAPointsOverrideOption = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GPAMethodGradeBucketFlagGPAPointsOverride", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GPAMethodGradeBucketFlagGPAPointsOverride
	#'
	#' This function returns a dataframe or json object of a GPAMethodGradeBucketFlagGPAPointsOverride
	#' @param GPAMethodGradeBucketFlagGPAPointsOverrideID The ID of the GPAMethodGradeBucketFlagGPAPointsOverride to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GPAMethodGradeBucketFlagGPAPointsOverride. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GPAMethodGradeBucketFlagGPAPointsOverride.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GPAMethodGradeBucketFlagGPAPointsOverride') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GPAMethodGradeBucketFlagGPAPointsOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGPAMethodGradeBucketFlagGPAPointsOverride <- function(GPAMethodGradeBucketFlagGPAPointsOverrideID, GPAMethodID = F, GradeBucketFlagID = F, GPAPoints = F, Rank = F, GPAPointsOverrideOption = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GPAMethodGradeBucketFlagGPAPointsOverrideID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GPAMethodGradeBucketFlagGPAPointsOverride", objectId = GPAMethodGradeBucketFlagGPAPointsOverrideID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GPAMethodGradeBucketFlagGPAPointsOverride
	#'
	#' This function deletes a GPAMethodGradeBucketFlagGPAPointsOverride
	#' @param GPAMethodGradeBucketFlagGPAPointsOverrideID The ID of the GPAMethodGradeBucketFlagGPAPointsOverride to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GPAMethodGradeBucketFlagGPAPointsOverrideID of the deleted GPAMethodGradeBucketFlagGPAPointsOverride.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGPAMethodGradeBucketFlagGPAPointsOverride <- function(GPAMethodGradeBucketFlagGPAPointsOverrideID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GPAMethodGradeBucketFlagGPAPointsOverride", objectId = GPAMethodGradeBucketFlagGPAPointsOverrideID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GPAMethodGradeBucketFlagGPAPointsOverride
	#'
	#' This function creates a GPAMethodGradeBucketFlagGPAPointsOverride
	#' @param fieldNames The field values to give the created GPAMethodGradeBucketFlagGPAPointsOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GPAMethodGradeBucketFlagGPAPointsOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGPAMethodGradeBucketFlagGPAPointsOverride <- function(GPAMethodID = NULL, GradeBucketFlagID = NULL, GPAPoints = NULL, Rank = NULL, GPAPointsOverrideOption = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GPAMethodGradeBucketFlagGPAPointsOverride", body = list(DataObject = body), searchFields = append("GPAMethodGradeBucketFlagGPAPointsOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GPAMethodGradeBucketFlagGPAPointsOverride
	#'
	#' This function modifies a GPAMethodGradeBucketFlagGPAPointsOverride
	#' @param fieldNames The field values to give the modified GPAMethodGradeBucketFlagGPAPointsOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GPAMethodGradeBucketFlagGPAPointsOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGPAMethodGradeBucketFlagGPAPointsOverride <- function(GPAMethodGradeBucketFlagGPAPointsOverrideID, GPAMethodID = NULL, GradeBucketFlagID = NULL, GPAPoints = NULL, Rank = NULL, GPAPointsOverrideOption = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GPAMethodGradeBucketFlagGPAPointsOverride", objectId = GPAMethodGradeBucketFlagGPAPointsOverrideID, body = list(DataObject = body), searchFields = append("GPAMethodGradeBucketFlagGPAPointsOverrideID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportColumnAttendanceTerms
	#'
	#' This function returns a dataframe or json object of GradeReportColumnAttendanceTerms
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnAttendanceTerms. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnAttendanceTerms.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnAttendanceTerm') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportColumnAttendanceTerms
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportColumnAttendanceTerms <- function(searchConditionsList = NULL, GradeReportColumnAttendanceTermID = F, AttendanceTermID = F, GradeReportColumnGroupColumnID = F, GradeReportColumnAttendanceTermIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportColumnAttendanceTerm", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportColumnAttendanceTerm
	#'
	#' This function returns a dataframe or json object of a GradeReportColumnAttendanceTerm
	#' @param GradeReportColumnAttendanceTermID The ID of the GradeReportColumnAttendanceTerm to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportColumnAttendanceTerm. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportColumnAttendanceTerm.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportColumnAttendanceTerm') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportColumnAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportColumnAttendanceTerm <- function(GradeReportColumnAttendanceTermID, AttendanceTermID = F, GradeReportColumnGroupColumnID = F, GradeReportColumnAttendanceTermIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportColumnAttendanceTermID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceTerm", objectId = GradeReportColumnAttendanceTermID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportColumnAttendanceTerm
	#'
	#' This function deletes a GradeReportColumnAttendanceTerm
	#' @param GradeReportColumnAttendanceTermID The ID of the GradeReportColumnAttendanceTerm to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportColumnAttendanceTermID of the deleted GradeReportColumnAttendanceTerm.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportColumnAttendanceTerm <- function(GradeReportColumnAttendanceTermID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceTerm", objectId = GradeReportColumnAttendanceTermID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportColumnAttendanceTerm
	#'
	#' This function creates a GradeReportColumnAttendanceTerm
	#' @param fieldNames The field values to give the created GradeReportColumnAttendanceTerm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportColumnAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportColumnAttendanceTerm <- function(AttendanceTermID = NULL, GradeReportColumnGroupColumnID = NULL, GradeReportColumnAttendanceTermIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceTerm", body = list(DataObject = body), searchFields = append("GradeReportColumnAttendanceTermID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportColumnAttendanceTerm
	#'
	#' This function modifies a GradeReportColumnAttendanceTerm
	#' @param fieldNames The field values to give the modified GradeReportColumnAttendanceTerm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportColumnAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportColumnAttendanceTerm <- function(GradeReportColumnAttendanceTermID, AttendanceTermID = NULL, GradeReportColumnGroupColumnID = NULL, GradeReportColumnAttendanceTermIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportColumnAttendanceTerm", objectId = GradeReportColumnAttendanceTermID, body = list(DataObject = body), searchFields = append("GradeReportColumnAttendanceTermID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeReportAttendanceTerms
	#'
	#' This function returns a dataframe or json object of TempGradeReportAttendanceTerms
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportAttendanceTerms. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportAttendanceTerms.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportAttendanceTerm') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeReportAttendanceTerms
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeReportAttendanceTerms <- function(searchConditionsList = NULL, TempGradeReportAttendanceTermID = F, AttendanceTermID = F, AttendanceTermCode = F, EntityName = F, SchoolYearDescription = F, StartDate = F, EndDate = F, CalendarDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeReportAttendanceTerm", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeReportAttendanceTerm
	#'
	#' This function returns a dataframe or json object of a TempGradeReportAttendanceTerm
	#' @param TempGradeReportAttendanceTermID The ID of the TempGradeReportAttendanceTerm to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeReportAttendanceTerm. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeReportAttendanceTerm.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeReportAttendanceTerm') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeReportAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeReportAttendanceTerm <- function(TempGradeReportAttendanceTermID, AttendanceTermID = F, AttendanceTermCode = F, EntityName = F, SchoolYearDescription = F, StartDate = F, EndDate = F, CalendarDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeReportAttendanceTermID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeReportAttendanceTerm", objectId = TempGradeReportAttendanceTermID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeReportAttendanceTerm
	#'
	#' This function deletes a TempGradeReportAttendanceTerm
	#' @param TempGradeReportAttendanceTermID The ID of the TempGradeReportAttendanceTerm to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeReportAttendanceTermID of the deleted TempGradeReportAttendanceTerm.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeReportAttendanceTerm <- function(TempGradeReportAttendanceTermID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeReportAttendanceTerm", objectId = TempGradeReportAttendanceTermID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeReportAttendanceTerm
	#'
	#' This function creates a TempGradeReportAttendanceTerm
	#' @param fieldNames The field values to give the created TempGradeReportAttendanceTerm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeReportAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeReportAttendanceTerm <- function(AttendanceTermID = NULL, AttendanceTermCode = NULL, EntityName = NULL, SchoolYearDescription = NULL, StartDate = NULL, EndDate = NULL, CalendarDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeReportAttendanceTerm", body = list(DataObject = body), searchFields = append("TempGradeReportAttendanceTermID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeReportAttendanceTerm
	#'
	#' This function modifies a TempGradeReportAttendanceTerm
	#' @param fieldNames The field values to give the modified TempGradeReportAttendanceTerm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeReportAttendanceTerm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeReportAttendanceTerm <- function(TempGradeReportAttendanceTermID, AttendanceTermID = NULL, AttendanceTermCode = NULL, EntityName = NULL, SchoolYearDescription = NULL, StartDate = NULL, EndDate = NULL, CalendarDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeReportAttendanceTerm", objectId = TempGradeReportAttendanceTermID, body = list(DataObject = body), searchFields = append("TempGradeReportAttendanceTermID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeBucketFlagDetailGPAMethods
	#'
	#' This function returns a dataframe or json object of GradeBucketFlagDetailGPAMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketFlagDetailGPAMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketFlagDetailGPAMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketFlagDetailGPAMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeBucketFlagDetailGPAMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeBucketFlagDetailGPAMethods <- function(searchConditionsList = NULL, GradeBucketFlagDetailGPAMethodID = F, GradeBucketFlagDetailID = F, GPAMethodEntityID = F, PointSetEntityID = F, UseOverride = F, GPAPoints = F, GPAPointsOverrideOption = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeBucketFlagDetailGPAMethodIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeBucketFlagDetailGPAMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeBucketFlagDetailGPAMethod
	#'
	#' This function returns a dataframe or json object of a GradeBucketFlagDetailGPAMethod
	#' @param GradeBucketFlagDetailGPAMethodID The ID of the GradeBucketFlagDetailGPAMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeBucketFlagDetailGPAMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeBucketFlagDetailGPAMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeBucketFlagDetailGPAMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeBucketFlagDetailGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeBucketFlagDetailGPAMethod <- function(GradeBucketFlagDetailGPAMethodID, GradeBucketFlagDetailID = F, GPAMethodEntityID = F, PointSetEntityID = F, UseOverride = F, GPAPoints = F, GPAPointsOverrideOption = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeBucketFlagDetailGPAMethodIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeBucketFlagDetailGPAMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeBucketFlagDetailGPAMethod", objectId = GradeBucketFlagDetailGPAMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeBucketFlagDetailGPAMethod
	#'
	#' This function deletes a GradeBucketFlagDetailGPAMethod
	#' @param GradeBucketFlagDetailGPAMethodID The ID of the GradeBucketFlagDetailGPAMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeBucketFlagDetailGPAMethodID of the deleted GradeBucketFlagDetailGPAMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeBucketFlagDetailGPAMethod <- function(GradeBucketFlagDetailGPAMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeBucketFlagDetailGPAMethod", objectId = GradeBucketFlagDetailGPAMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeBucketFlagDetailGPAMethod
	#'
	#' This function creates a GradeBucketFlagDetailGPAMethod
	#' @param fieldNames The field values to give the created GradeBucketFlagDetailGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeBucketFlagDetailGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeBucketFlagDetailGPAMethod <- function(GradeBucketFlagDetailID = NULL, GPAMethodEntityID = NULL, PointSetEntityID = NULL, UseOverride = NULL, GPAPoints = NULL, GPAPointsOverrideOption = NULL, EntityGroupKey = NULL, GradeBucketFlagDetailGPAMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeBucketFlagDetailGPAMethod", body = list(DataObject = body), searchFields = append("GradeBucketFlagDetailGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeBucketFlagDetailGPAMethod
	#'
	#' This function modifies a GradeBucketFlagDetailGPAMethod
	#' @param fieldNames The field values to give the modified GradeBucketFlagDetailGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeBucketFlagDetailGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeBucketFlagDetailGPAMethod <- function(GradeBucketFlagDetailGPAMethodID, GradeBucketFlagDetailID = NULL, GPAMethodEntityID = NULL, PointSetEntityID = NULL, UseOverride = NULL, GPAPoints = NULL, GPAPointsOverrideOption = NULL, EntityGroupKey = NULL, GradeBucketFlagDetailGPAMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeBucketFlagDetailGPAMethod", objectId = GradeBucketFlagDetailGPAMethodID, body = list(DataObject = body), searchFields = append("GradeBucketFlagDetailGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeBucketFlagDetailGPAMethods
	#'
	#' This function returns a dataframe or json object of TempGradeBucketFlagDetailGPAMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeBucketFlagDetailGPAMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeBucketFlagDetailGPAMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeBucketFlagDetailGPAMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeBucketFlagDetailGPAMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeBucketFlagDetailGPAMethods <- function(searchConditionsList = NULL, TempGradeBucketFlagDetailGPAMethodID = F, GPAMethodDescription = F, PointSetDescription = F, GradeBucketFlagCodeName = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeBucketFlagDetailGPAMethodID = F, GradeBucketFlagDetailID = F, GPAMethodEntityID = F, PointSetEntityID = F, EntityCode = F, NumericYear = F, UseOverride = F, GPAPoints = F, GPAPointsOverrideOption = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeBucketFlagDetailGPAMethod
	#'
	#' This function returns a dataframe or json object of a TempGradeBucketFlagDetailGPAMethod
	#' @param TempGradeBucketFlagDetailGPAMethodID The ID of the TempGradeBucketFlagDetailGPAMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeBucketFlagDetailGPAMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeBucketFlagDetailGPAMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeBucketFlagDetailGPAMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeBucketFlagDetailGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeBucketFlagDetailGPAMethod <- function(TempGradeBucketFlagDetailGPAMethodID, GPAMethodDescription = F, PointSetDescription = F, GradeBucketFlagCodeName = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeBucketFlagDetailGPAMethodID = F, GradeBucketFlagDetailID = F, GPAMethodEntityID = F, PointSetEntityID = F, EntityCode = F, NumericYear = F, UseOverride = F, GPAPoints = F, GPAPointsOverrideOption = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeBucketFlagDetailGPAMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethod", objectId = TempGradeBucketFlagDetailGPAMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeBucketFlagDetailGPAMethod
	#'
	#' This function deletes a TempGradeBucketFlagDetailGPAMethod
	#' @param TempGradeBucketFlagDetailGPAMethodID The ID of the TempGradeBucketFlagDetailGPAMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeBucketFlagDetailGPAMethodID of the deleted TempGradeBucketFlagDetailGPAMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeBucketFlagDetailGPAMethod <- function(TempGradeBucketFlagDetailGPAMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethod", objectId = TempGradeBucketFlagDetailGPAMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeBucketFlagDetailGPAMethod
	#'
	#' This function creates a TempGradeBucketFlagDetailGPAMethod
	#' @param fieldNames The field values to give the created TempGradeBucketFlagDetailGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeBucketFlagDetailGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeBucketFlagDetailGPAMethod <- function(GPAMethodDescription = NULL, PointSetDescription = NULL, GradeBucketFlagCodeName = NULL, GradeBucketFlagDetailGPAMethodID = NULL, GradeBucketFlagDetailID = NULL, GPAMethodEntityID = NULL, PointSetEntityID = NULL, EntityCode = NULL, NumericYear = NULL, UseOverride = NULL, GPAPoints = NULL, GPAPointsOverrideOption = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethod", body = list(DataObject = body), searchFields = append("TempGradeBucketFlagDetailGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeBucketFlagDetailGPAMethod
	#'
	#' This function modifies a TempGradeBucketFlagDetailGPAMethod
	#' @param fieldNames The field values to give the modified TempGradeBucketFlagDetailGPAMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeBucketFlagDetailGPAMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeBucketFlagDetailGPAMethod <- function(TempGradeBucketFlagDetailGPAMethodID, GPAMethodDescription = NULL, PointSetDescription = NULL, GradeBucketFlagCodeName = NULL, GradeBucketFlagDetailGPAMethodID = NULL, GradeBucketFlagDetailID = NULL, GPAMethodEntityID = NULL, PointSetEntityID = NULL, EntityCode = NULL, NumericYear = NULL, UseOverride = NULL, GPAPoints = NULL, GPAPointsOverrideOption = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethod", objectId = TempGradeBucketFlagDetailGPAMethodID, body = list(DataObject = body), searchFields = append("TempGradeBucketFlagDetailGPAMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseEarnedCreditsMethods
	#'
	#' This function returns a dataframe or json object of CourseEarnedCreditsMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEarnedCreditsMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEarnedCreditsMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEarnedCreditsMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of CourseEarnedCreditsMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseEarnedCreditsMethods <- function(searchConditionsList = NULL, CourseEarnedCreditsMethodID = F, EntityGroupKey = F, EarnedCreditsMethodEntityID = F, CourseID = F, UseOverride = F, EarnedCreditsOverride = F, CourseEarnedCreditsMethodIDClonedFrom = F, CourseOrOverrideEarnedCredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "CourseEarnedCreditsMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseEarnedCreditsMethod
	#'
	#' This function returns a dataframe or json object of a CourseEarnedCreditsMethod
	#' @param CourseEarnedCreditsMethodID The ID of the CourseEarnedCreditsMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEarnedCreditsMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEarnedCreditsMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEarnedCreditsMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of CourseEarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseEarnedCreditsMethod <- function(CourseEarnedCreditsMethodID, EntityGroupKey = F, EarnedCreditsMethodEntityID = F, CourseID = F, UseOverride = F, EarnedCreditsOverride = F, CourseEarnedCreditsMethodIDClonedFrom = F, CourseOrOverrideEarnedCredits = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseEarnedCreditsMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "CourseEarnedCreditsMethod", objectId = CourseEarnedCreditsMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseEarnedCreditsMethod
	#'
	#' This function deletes a CourseEarnedCreditsMethod
	#' @param CourseEarnedCreditsMethodID The ID of the CourseEarnedCreditsMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The CourseEarnedCreditsMethodID of the deleted CourseEarnedCreditsMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseEarnedCreditsMethod <- function(CourseEarnedCreditsMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "CourseEarnedCreditsMethod", objectId = CourseEarnedCreditsMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseEarnedCreditsMethod
	#'
	#' This function creates a CourseEarnedCreditsMethod
	#' @param fieldNames The field values to give the created CourseEarnedCreditsMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created CourseEarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseEarnedCreditsMethod <- function(EntityGroupKey = NULL, EarnedCreditsMethodEntityID = NULL, CourseID = NULL, UseOverride = NULL, EarnedCreditsOverride = NULL, CourseEarnedCreditsMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "CourseEarnedCreditsMethod", body = list(DataObject = body), searchFields = append("CourseEarnedCreditsMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseEarnedCreditsMethod
	#'
	#' This function modifies a CourseEarnedCreditsMethod
	#' @param fieldNames The field values to give the modified CourseEarnedCreditsMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified CourseEarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseEarnedCreditsMethod <- function(CourseEarnedCreditsMethodID, EntityGroupKey = NULL, EarnedCreditsMethodEntityID = NULL, CourseID = NULL, UseOverride = NULL, EarnedCreditsOverride = NULL, CourseEarnedCreditsMethodIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "CourseEarnedCreditsMethod", objectId = CourseEarnedCreditsMethodID, body = list(DataObject = body), searchFields = append("CourseEarnedCreditsMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EarnedCreditsMethods
	#'
	#' This function returns a dataframe or json object of EarnedCreditsMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of EarnedCreditsMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEarnedCreditsMethods <- function(searchConditionsList = NULL, EarnedCreditsMethodID = F, DistrictGroupKey = F, DistrictID = F, Name = F, Description = F, SortNumber = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "EarnedCreditsMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EarnedCreditsMethod
	#'
	#' This function returns a dataframe or json object of an EarnedCreditsMethod
	#' @param EarnedCreditsMethodID The ID of the EarnedCreditsMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of EarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEarnedCreditsMethod <- function(EarnedCreditsMethodID, DistrictGroupKey = F, DistrictID = F, Name = F, Description = F, SortNumber = F, NameDescription = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EarnedCreditsMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "EarnedCreditsMethod", objectId = EarnedCreditsMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EarnedCreditsMethod
	#'
	#' This function deletes an EarnedCreditsMethod
	#' @param EarnedCreditsMethodID The ID of the EarnedCreditsMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The EarnedCreditsMethodID of the deleted EarnedCreditsMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEarnedCreditsMethod <- function(EarnedCreditsMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "EarnedCreditsMethod", objectId = EarnedCreditsMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EarnedCreditsMethod
	#'
	#' This function creates an EarnedCreditsMethod
	#' @param fieldNames The field values to give the created EarnedCreditsMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created EarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEarnedCreditsMethod <- function(DistrictGroupKey = NULL, DistrictID = NULL, Name = NULL, Description = NULL, SortNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "EarnedCreditsMethod", body = list(DataObject = body), searchFields = append("EarnedCreditsMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EarnedCreditsMethod
	#'
	#' This function modifies an EarnedCreditsMethod
	#' @param fieldNames The field values to give the modified EarnedCreditsMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified EarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEarnedCreditsMethod <- function(EarnedCreditsMethodID, DistrictGroupKey = NULL, DistrictID = NULL, Name = NULL, Description = NULL, SortNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "EarnedCreditsMethod", objectId = EarnedCreditsMethodID, body = list(DataObject = body), searchFields = append("EarnedCreditsMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EarnedCreditsMethodEntities
	#'
	#' This function returns a dataframe or json object of EarnedCreditsMethodEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsMethodEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsMethodEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsMethodEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of EarnedCreditsMethodEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEarnedCreditsMethodEntities <- function(searchConditionsList = NULL, EarnedCreditsMethodEntityID = F, EntityGroupKey = F, EarnedCreditsMethodID = F, EntityID = F, SchoolYearID = F, EarnedCreditsMethodEntityIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "EarnedCreditsMethodEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EarnedCreditsMethodEntity
	#'
	#' This function returns a dataframe or json object of an EarnedCreditsMethodEntity
	#' @param EarnedCreditsMethodEntityID The ID of the EarnedCreditsMethodEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarnedCreditsMethodEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarnedCreditsMethodEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarnedCreditsMethodEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of EarnedCreditsMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEarnedCreditsMethodEntity <- function(EarnedCreditsMethodEntityID, EntityGroupKey = F, EarnedCreditsMethodID = F, EntityID = F, SchoolYearID = F, EarnedCreditsMethodEntityIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EarnedCreditsMethodEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "EarnedCreditsMethodEntity", objectId = EarnedCreditsMethodEntityID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EarnedCreditsMethodEntity
	#'
	#' This function deletes an EarnedCreditsMethodEntity
	#' @param EarnedCreditsMethodEntityID The ID of the EarnedCreditsMethodEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The EarnedCreditsMethodEntityID of the deleted EarnedCreditsMethodEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEarnedCreditsMethodEntity <- function(EarnedCreditsMethodEntityID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "EarnedCreditsMethodEntity", objectId = EarnedCreditsMethodEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EarnedCreditsMethodEntity
	#'
	#' This function creates an EarnedCreditsMethodEntity
	#' @param fieldNames The field values to give the created EarnedCreditsMethodEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created EarnedCreditsMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEarnedCreditsMethodEntity <- function(EntityGroupKey = NULL, EarnedCreditsMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, EarnedCreditsMethodEntityIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "EarnedCreditsMethodEntity", body = list(DataObject = body), searchFields = append("EarnedCreditsMethodEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EarnedCreditsMethodEntity
	#'
	#' This function modifies an EarnedCreditsMethodEntity
	#' @param fieldNames The field values to give the modified EarnedCreditsMethodEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified EarnedCreditsMethodEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEarnedCreditsMethodEntity <- function(EarnedCreditsMethodEntityID, EntityGroupKey = NULL, EarnedCreditsMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, EarnedCreditsMethodEntityIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "EarnedCreditsMethodEntity", objectId = EarnedCreditsMethodEntityID, body = list(DataObject = body), searchFields = append("EarnedCreditsMethodEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentEarnedCreditsBuckets
	#'
	#' This function returns a dataframe or json object of StudentEarnedCreditsBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentEarnedCreditsBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentEarnedCreditsBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentEarnedCreditsBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentEarnedCreditsBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentEarnedCreditsBuckets <- function(searchConditionsList = NULL, StudentEarnedCreditsBucketID = F, StudentGradeBucketID = F, StudentSectionEarnedCreditsBucketGroupID = F, StudentEarnedCreditsBucketGroupID = F, StudentSectionID = F, StudentID = F, EarnedCreditsMethodID = F, EntityID = F, SchoolYearID = F, EarnedCredits = F, FailedCredits = F, EarnedCreditsPossible = F, EarnedCreditsAttempted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EarnedCreditsDefault = F, EarnedCreditsCompleted = F, EarnedCreditsAllEntered = F, FailedCreditsDefault = F, FailedCreditsCompleted = F, FailedCreditsAllEntered = F, EarnedCreditsAttemptedDefault = F, EarnedCreditsAttemptedCompleted = F, EarnedCreditsAttemptedAllEntered = F, EarnedCreditsPossibleDefault = F, EarnedCreditsPossibleCompleted = F, EarnedCreditsPossibleAllEntered = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentEarnedCreditsBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentEarnedCreditsBucket
	#'
	#' This function returns a dataframe or json object of a StudentEarnedCreditsBucket
	#' @param StudentEarnedCreditsBucketID The ID of the StudentEarnedCreditsBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentEarnedCreditsBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentEarnedCreditsBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentEarnedCreditsBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentEarnedCreditsBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentEarnedCreditsBucket <- function(StudentEarnedCreditsBucketID, StudentGradeBucketID = F, StudentSectionEarnedCreditsBucketGroupID = F, StudentEarnedCreditsBucketGroupID = F, StudentSectionID = F, StudentID = F, EarnedCreditsMethodID = F, EntityID = F, SchoolYearID = F, EarnedCredits = F, FailedCredits = F, EarnedCreditsPossible = F, EarnedCreditsAttempted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EarnedCreditsDefault = F, EarnedCreditsCompleted = F, EarnedCreditsAllEntered = F, FailedCreditsDefault = F, FailedCreditsCompleted = F, FailedCreditsAllEntered = F, EarnedCreditsAttemptedDefault = F, EarnedCreditsAttemptedCompleted = F, EarnedCreditsAttemptedAllEntered = F, EarnedCreditsPossibleDefault = F, EarnedCreditsPossibleCompleted = F, EarnedCreditsPossibleAllEntered = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentEarnedCreditsBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucket", objectId = StudentEarnedCreditsBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentEarnedCreditsBucket
	#'
	#' This function deletes a StudentEarnedCreditsBucket
	#' @param StudentEarnedCreditsBucketID The ID of the StudentEarnedCreditsBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentEarnedCreditsBucketID of the deleted StudentEarnedCreditsBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentEarnedCreditsBucket <- function(StudentEarnedCreditsBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucket", objectId = StudentEarnedCreditsBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentEarnedCreditsBucket
	#'
	#' This function creates a StudentEarnedCreditsBucket
	#' @param fieldNames The field values to give the created StudentEarnedCreditsBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentEarnedCreditsBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentEarnedCreditsBucket <- function(StudentGradeBucketID = NULL, StudentSectionEarnedCreditsBucketGroupID = NULL, StudentEarnedCreditsBucketGroupID = NULL, StudentSectionID = NULL, StudentID = NULL, EarnedCreditsMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, EarnedCredits = NULL, FailedCredits = NULL, EarnedCreditsPossible = NULL, EarnedCreditsAttempted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucket", body = list(DataObject = body), searchFields = append("StudentEarnedCreditsBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentEarnedCreditsBucket
	#'
	#' This function modifies a StudentEarnedCreditsBucket
	#' @param fieldNames The field values to give the modified StudentEarnedCreditsBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentEarnedCreditsBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentEarnedCreditsBucket <- function(StudentEarnedCreditsBucketID, StudentGradeBucketID = NULL, StudentSectionEarnedCreditsBucketGroupID = NULL, StudentEarnedCreditsBucketGroupID = NULL, StudentSectionID = NULL, StudentID = NULL, EarnedCreditsMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, EarnedCredits = NULL, FailedCredits = NULL, EarnedCreditsPossible = NULL, EarnedCreditsAttempted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucket", objectId = StudentEarnedCreditsBucketID, body = list(DataObject = body), searchFields = append("StudentEarnedCreditsBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentEarnedCreditsBucketGroups
	#'
	#' This function returns a dataframe or json object of StudentEarnedCreditsBucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentEarnedCreditsBucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentEarnedCreditsBucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentEarnedCreditsBucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentEarnedCreditsBucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentEarnedCreditsBucketGroups <- function(searchConditionsList = NULL, StudentEarnedCreditsBucketGroupID = F, StudentID = F, EarnedCreditsMethodID = F, EarnedCreditsDefault = F, EarnedCreditsCompleted = F, EarnedCreditsAllEntered = F, FailedCreditsDefault = F, FailedCreditsCompleted = F, FailedCreditsAllEntered = F, EarnedCreditsAttemptedDefault = F, EarnedCreditsAttemptedCompleted = F, EarnedCreditsAttemptedAllEntered = F, EarnedCreditsPossibleDefault = F, EarnedCreditsPossibleCompleted = F, EarnedCreditsPossibleAllEntered = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentEarnedCreditsBucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentEarnedCreditsBucketGroup
	#'
	#' This function returns a dataframe or json object of a StudentEarnedCreditsBucketGroup
	#' @param StudentEarnedCreditsBucketGroupID The ID of the StudentEarnedCreditsBucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentEarnedCreditsBucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentEarnedCreditsBucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentEarnedCreditsBucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentEarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentEarnedCreditsBucketGroup <- function(StudentEarnedCreditsBucketGroupID, StudentID = F, EarnedCreditsMethodID = F, EarnedCreditsDefault = F, EarnedCreditsCompleted = F, EarnedCreditsAllEntered = F, FailedCreditsDefault = F, FailedCreditsCompleted = F, FailedCreditsAllEntered = F, EarnedCreditsAttemptedDefault = F, EarnedCreditsAttemptedCompleted = F, EarnedCreditsAttemptedAllEntered = F, EarnedCreditsPossibleDefault = F, EarnedCreditsPossibleCompleted = F, EarnedCreditsPossibleAllEntered = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentEarnedCreditsBucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucketGroup", objectId = StudentEarnedCreditsBucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentEarnedCreditsBucketGroup
	#'
	#' This function deletes a StudentEarnedCreditsBucketGroup
	#' @param StudentEarnedCreditsBucketGroupID The ID of the StudentEarnedCreditsBucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentEarnedCreditsBucketGroupID of the deleted StudentEarnedCreditsBucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentEarnedCreditsBucketGroup <- function(StudentEarnedCreditsBucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucketGroup", objectId = StudentEarnedCreditsBucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentEarnedCreditsBucketGroup
	#'
	#' This function creates a StudentEarnedCreditsBucketGroup
	#' @param fieldNames The field values to give the created StudentEarnedCreditsBucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentEarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentEarnedCreditsBucketGroup <- function(StudentID = NULL, EarnedCreditsMethodID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucketGroup", body = list(DataObject = body), searchFields = append("StudentEarnedCreditsBucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentEarnedCreditsBucketGroup
	#'
	#' This function modifies a StudentEarnedCreditsBucketGroup
	#' @param fieldNames The field values to give the modified StudentEarnedCreditsBucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentEarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentEarnedCreditsBucketGroup <- function(StudentEarnedCreditsBucketGroupID, StudentID = NULL, EarnedCreditsMethodID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentEarnedCreditsBucketGroup", objectId = StudentEarnedCreditsBucketGroupID, body = list(DataObject = body), searchFields = append("StudentEarnedCreditsBucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionEarnedCreditsBucketGroups
	#'
	#' This function returns a dataframe or json object of StudentSectionEarnedCreditsBucketGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionEarnedCreditsBucketGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionEarnedCreditsBucketGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionEarnedCreditsBucketGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentSectionEarnedCreditsBucketGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionEarnedCreditsBucketGroups <- function(searchConditionsList = NULL, StudentSectionEarnedCreditsBucketGroupID = F, StudentEarnedCreditsBucketGroupID = F, StudentSectionID = F, StudentID = F, EarnedCreditsMethodID = F, EntityID = F, SchoolYearID = F, UseEarnedCreditsTotalOverride = F, TotalEarnedCreditsOverride = F, TotalFailedCreditsOverride = F, EarnedCreditsDefault = F, EarnedCreditsCompleted = F, EarnedCreditsAllEntered = F, FailedCreditsDefault = F, FailedCreditsCompleted = F, FailedCreditsAllEntered = F, EarnedCreditsAttemptedDefault = F, EarnedCreditsAttemptedCompleted = F, EarnedCreditsAttemptedAllEntered = F, EarnedCreditsPossibleDefault = F, EarnedCreditsPossibleCompleted = F, EarnedCreditsPossibleAllEntered = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentSectionEarnedCreditsBucketGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionEarnedCreditsBucketGroup
	#'
	#' This function returns a dataframe or json object of a StudentSectionEarnedCreditsBucketGroup
	#' @param StudentSectionEarnedCreditsBucketGroupID The ID of the StudentSectionEarnedCreditsBucketGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionEarnedCreditsBucketGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionEarnedCreditsBucketGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionEarnedCreditsBucketGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentSectionEarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionEarnedCreditsBucketGroup <- function(StudentSectionEarnedCreditsBucketGroupID, StudentEarnedCreditsBucketGroupID = F, StudentSectionID = F, StudentID = F, EarnedCreditsMethodID = F, EntityID = F, SchoolYearID = F, UseEarnedCreditsTotalOverride = F, TotalEarnedCreditsOverride = F, TotalFailedCreditsOverride = F, EarnedCreditsDefault = F, EarnedCreditsCompleted = F, EarnedCreditsAllEntered = F, FailedCreditsDefault = F, FailedCreditsCompleted = F, FailedCreditsAllEntered = F, EarnedCreditsAttemptedDefault = F, EarnedCreditsAttemptedCompleted = F, EarnedCreditsAttemptedAllEntered = F, EarnedCreditsPossibleDefault = F, EarnedCreditsPossibleCompleted = F, EarnedCreditsPossibleAllEntered = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionEarnedCreditsBucketGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsBucketGroup", objectId = StudentSectionEarnedCreditsBucketGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionEarnedCreditsBucketGroup
	#'
	#' This function deletes a StudentSectionEarnedCreditsBucketGroup
	#' @param StudentSectionEarnedCreditsBucketGroupID The ID of the StudentSectionEarnedCreditsBucketGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentSectionEarnedCreditsBucketGroupID of the deleted StudentSectionEarnedCreditsBucketGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionEarnedCreditsBucketGroup <- function(StudentSectionEarnedCreditsBucketGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsBucketGroup", objectId = StudentSectionEarnedCreditsBucketGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionEarnedCreditsBucketGroup
	#'
	#' This function creates a StudentSectionEarnedCreditsBucketGroup
	#' @param fieldNames The field values to give the created StudentSectionEarnedCreditsBucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentSectionEarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionEarnedCreditsBucketGroup <- function(StudentEarnedCreditsBucketGroupID = NULL, StudentSectionID = NULL, StudentID = NULL, EarnedCreditsMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, UseEarnedCreditsTotalOverride = NULL, TotalEarnedCreditsOverride = NULL, TotalFailedCreditsOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsBucketGroup", body = list(DataObject = body), searchFields = append("StudentSectionEarnedCreditsBucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionEarnedCreditsBucketGroup
	#'
	#' This function modifies a StudentSectionEarnedCreditsBucketGroup
	#' @param fieldNames The field values to give the modified StudentSectionEarnedCreditsBucketGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentSectionEarnedCreditsBucketGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionEarnedCreditsBucketGroup <- function(StudentSectionEarnedCreditsBucketGroupID, StudentEarnedCreditsBucketGroupID = NULL, StudentSectionID = NULL, StudentID = NULL, EarnedCreditsMethodID = NULL, EntityID = NULL, SchoolYearID = NULL, UseEarnedCreditsTotalOverride = NULL, TotalEarnedCreditsOverride = NULL, TotalFailedCreditsOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsBucketGroup", objectId = StudentSectionEarnedCreditsBucketGroupID, body = list(DataObject = body), searchFields = append("StudentSectionEarnedCreditsBucketGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionEarnedCreditsMethods
	#'
	#' This function returns a dataframe or json object of StudentSectionEarnedCreditsMethods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionEarnedCreditsMethods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionEarnedCreditsMethods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionEarnedCreditsMethod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of StudentSectionEarnedCreditsMethods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionEarnedCreditsMethods <- function(searchConditionsList = NULL, StudentSectionEarnedCreditsMethodID = F, StudentSectionID = F, EarnedCreditsMethodEntityID = F, UseOverride = F, EarnedCreditsOverride = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DefaultEarnedCredits = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "StudentSectionEarnedCreditsMethod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionEarnedCreditsMethod
	#'
	#' This function returns a dataframe or json object of a StudentSectionEarnedCreditsMethod
	#' @param StudentSectionEarnedCreditsMethodID The ID of the StudentSectionEarnedCreditsMethod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionEarnedCreditsMethod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionEarnedCreditsMethod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionEarnedCreditsMethod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of StudentSectionEarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionEarnedCreditsMethod <- function(StudentSectionEarnedCreditsMethodID, StudentSectionID = F, EarnedCreditsMethodEntityID = F, UseOverride = F, EarnedCreditsOverride = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DefaultEarnedCredits = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionEarnedCreditsMethodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsMethod", objectId = StudentSectionEarnedCreditsMethodID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionEarnedCreditsMethod
	#'
	#' This function deletes a StudentSectionEarnedCreditsMethod
	#' @param StudentSectionEarnedCreditsMethodID The ID of the StudentSectionEarnedCreditsMethod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The StudentSectionEarnedCreditsMethodID of the deleted StudentSectionEarnedCreditsMethod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionEarnedCreditsMethod <- function(StudentSectionEarnedCreditsMethodID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsMethod", objectId = StudentSectionEarnedCreditsMethodID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionEarnedCreditsMethod
	#'
	#' This function creates a StudentSectionEarnedCreditsMethod
	#' @param fieldNames The field values to give the created StudentSectionEarnedCreditsMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created StudentSectionEarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionEarnedCreditsMethod <- function(StudentSectionID = NULL, EarnedCreditsMethodEntityID = NULL, UseOverride = NULL, EarnedCreditsOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsMethod", body = list(DataObject = body), searchFields = append("StudentSectionEarnedCreditsMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionEarnedCreditsMethod
	#'
	#' This function modifies a StudentSectionEarnedCreditsMethod
	#' @param fieldNames The field values to give the modified StudentSectionEarnedCreditsMethod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified StudentSectionEarnedCreditsMethod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionEarnedCreditsMethod <- function(StudentSectionEarnedCreditsMethodID, StudentSectionID = NULL, EarnedCreditsMethodEntityID = NULL, UseOverride = NULL, EarnedCreditsOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "StudentSectionEarnedCreditsMethod", objectId = StudentSectionEarnedCreditsMethodID, body = list(DataObject = body), searchFields = append("StudentSectionEarnedCreditsMethodID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateHierarchyDepthSettings
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateHierarchyDepthSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateHierarchyDepthSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateHierarchyDepthSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateHierarchyDepthSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateHierarchyDepthSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateHierarchyDepthSettings <- function(searchConditionsList = NULL, GradeReportTemplateHierarchyDepthSettingID = F, GradeReportTemplateID = F, DepthLevel = F, PrintBackgroundColor = F, BackgroundColor = F, TextColor = F, GradeReportTemplateHierarchyDepthSettingIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateHierarchyDepthSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateHierarchyDepthSetting
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateHierarchyDepthSetting
	#' @param GradeReportTemplateHierarchyDepthSettingID The ID of the GradeReportTemplateHierarchyDepthSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateHierarchyDepthSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateHierarchyDepthSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateHierarchyDepthSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateHierarchyDepthSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateHierarchyDepthSetting <- function(GradeReportTemplateHierarchyDepthSettingID, GradeReportTemplateID = F, DepthLevel = F, PrintBackgroundColor = F, BackgroundColor = F, TextColor = F, GradeReportTemplateHierarchyDepthSettingIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateHierarchyDepthSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateHierarchyDepthSetting", objectId = GradeReportTemplateHierarchyDepthSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateHierarchyDepthSetting
	#'
	#' This function deletes a GradeReportTemplateHierarchyDepthSetting
	#' @param GradeReportTemplateHierarchyDepthSettingID The ID of the GradeReportTemplateHierarchyDepthSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateHierarchyDepthSettingID of the deleted GradeReportTemplateHierarchyDepthSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateHierarchyDepthSetting <- function(GradeReportTemplateHierarchyDepthSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateHierarchyDepthSetting", objectId = GradeReportTemplateHierarchyDepthSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateHierarchyDepthSetting
	#'
	#' This function creates a GradeReportTemplateHierarchyDepthSetting
	#' @param fieldNames The field values to give the created GradeReportTemplateHierarchyDepthSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateHierarchyDepthSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateHierarchyDepthSetting <- function(GradeReportTemplateID = NULL, DepthLevel = NULL, PrintBackgroundColor = NULL, BackgroundColor = NULL, TextColor = NULL, GradeReportTemplateHierarchyDepthSettingIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateHierarchyDepthSetting", body = list(DataObject = body), searchFields = append("GradeReportTemplateHierarchyDepthSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateHierarchyDepthSetting
	#'
	#' This function modifies a GradeReportTemplateHierarchyDepthSetting
	#' @param fieldNames The field values to give the modified GradeReportTemplateHierarchyDepthSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateHierarchyDepthSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateHierarchyDepthSetting <- function(GradeReportTemplateHierarchyDepthSettingID, GradeReportTemplateID = NULL, DepthLevel = NULL, PrintBackgroundColor = NULL, BackgroundColor = NULL, TextColor = NULL, GradeReportTemplateHierarchyDepthSettingIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateHierarchyDepthSetting", objectId = GradeReportTemplateHierarchyDepthSettingID, body = list(DataObject = body), searchFields = append("GradeReportTemplateHierarchyDepthSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAPITransactions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAPITransactions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAPITransactions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAPITransactions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAPITransaction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAPITransactions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAPITransactions <- function(searchConditionsList = NULL, ElectronicTranscriptAPITransactionID = F, ElectronicTranscriptRunHistoryID = F, StudentID = F, Message = F, IsError = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAPITransaction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAPITransaction
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAPITransaction
	#' @param ElectronicTranscriptAPITransactionID The ID of the ElectronicTranscriptAPITransaction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAPITransaction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAPITransaction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAPITransaction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAPITransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAPITransaction <- function(ElectronicTranscriptAPITransactionID, ElectronicTranscriptRunHistoryID = F, StudentID = F, Message = F, IsError = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAPITransactionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAPITransaction", objectId = ElectronicTranscriptAPITransactionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAPITransaction
	#'
	#' This function deletes an ElectronicTranscriptAPITransaction
	#' @param ElectronicTranscriptAPITransactionID The ID of the ElectronicTranscriptAPITransaction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAPITransactionID of the deleted ElectronicTranscriptAPITransaction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAPITransaction <- function(ElectronicTranscriptAPITransactionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAPITransaction", objectId = ElectronicTranscriptAPITransactionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAPITransaction
	#'
	#' This function creates an ElectronicTranscriptAPITransaction
	#' @param fieldNames The field values to give the created ElectronicTranscriptAPITransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAPITransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAPITransaction <- function(ElectronicTranscriptRunHistoryID = NULL, StudentID = NULL, Message = NULL, IsError = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAPITransaction", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAPITransactionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAPITransaction
	#'
	#' This function modifies an ElectronicTranscriptAPITransaction
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAPITransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAPITransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAPITransaction <- function(ElectronicTranscriptAPITransactionID, ElectronicTranscriptRunHistoryID = NULL, StudentID = NULL, Message = NULL, IsError = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAPITransaction", objectId = ElectronicTranscriptAPITransactionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAPITransactionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicAwardExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicAwardExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicAwardExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicAwardExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicAwardException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicAwardExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicAwardExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicAwardExceptionID = F, ElectronicTranscriptAcademicAwardV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicAwardException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicAwardException
	#' @param ElectronicTranscriptAcademicAwardExceptionID The ID of the ElectronicTranscriptAcademicAwardException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicAwardException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicAwardException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicAwardException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicAwardException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicAwardException <- function(ElectronicTranscriptAcademicAwardExceptionID, ElectronicTranscriptAcademicAwardV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicAwardExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardException", objectId = ElectronicTranscriptAcademicAwardExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicAwardException
	#'
	#' This function deletes an ElectronicTranscriptAcademicAwardException
	#' @param ElectronicTranscriptAcademicAwardExceptionID The ID of the ElectronicTranscriptAcademicAwardException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicAwardExceptionID of the deleted ElectronicTranscriptAcademicAwardException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicAwardException <- function(ElectronicTranscriptAcademicAwardExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardException", objectId = ElectronicTranscriptAcademicAwardExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicAwardException
	#'
	#' This function creates an ElectronicTranscriptAcademicAwardException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicAwardException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicAwardException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicAwardException <- function(ElectronicTranscriptAcademicAwardV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicAwardExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicAwardException
	#'
	#' This function modifies an ElectronicTranscriptAcademicAwardException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicAwardException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicAwardException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicAwardException <- function(ElectronicTranscriptAcademicAwardExceptionID, ElectronicTranscriptAcademicAwardV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardException", objectId = ElectronicTranscriptAcademicAwardExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicAwardExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicHonorExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicHonorExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicHonorExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicHonorExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicHonorException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicHonorExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicHonorExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicHonorExceptionID = F, ElectronicTranscriptAcademicHonorV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicHonorException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicHonorException
	#' @param ElectronicTranscriptAcademicHonorExceptionID The ID of the ElectronicTranscriptAcademicHonorException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicHonorException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicHonorException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicHonorException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicHonorException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicHonorException <- function(ElectronicTranscriptAcademicHonorExceptionID, ElectronicTranscriptAcademicHonorV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicHonorExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorException", objectId = ElectronicTranscriptAcademicHonorExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicHonorException
	#'
	#' This function deletes an ElectronicTranscriptAcademicHonorException
	#' @param ElectronicTranscriptAcademicHonorExceptionID The ID of the ElectronicTranscriptAcademicHonorException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicHonorExceptionID of the deleted ElectronicTranscriptAcademicHonorException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicHonorException <- function(ElectronicTranscriptAcademicHonorExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorException", objectId = ElectronicTranscriptAcademicHonorExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicHonorException
	#'
	#' This function creates an ElectronicTranscriptAcademicHonorException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicHonorException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicHonorException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicHonorException <- function(ElectronicTranscriptAcademicHonorV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicHonorExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicHonorException
	#'
	#' This function modifies an ElectronicTranscriptAcademicHonorException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicHonorException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicHonorException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicHonorException <- function(ElectronicTranscriptAcademicHonorExceptionID, ElectronicTranscriptAcademicHonorV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorException", objectId = ElectronicTranscriptAcademicHonorExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicHonorExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicSessionExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicSessionExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSessionExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSessionExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSessionException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicSessionExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicSessionExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicSessionExceptionID = F, ElectronicTranscriptAcademicSessionV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicSessionException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicSessionException
	#' @param ElectronicTranscriptAcademicSessionExceptionID The ID of the ElectronicTranscriptAcademicSessionException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSessionException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSessionException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSessionException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicSessionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicSessionException <- function(ElectronicTranscriptAcademicSessionExceptionID, ElectronicTranscriptAcademicSessionV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicSessionExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionException", objectId = ElectronicTranscriptAcademicSessionExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicSessionException
	#'
	#' This function deletes an ElectronicTranscriptAcademicSessionException
	#' @param ElectronicTranscriptAcademicSessionExceptionID The ID of the ElectronicTranscriptAcademicSessionException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicSessionExceptionID of the deleted ElectronicTranscriptAcademicSessionException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicSessionException <- function(ElectronicTranscriptAcademicSessionExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionException", objectId = ElectronicTranscriptAcademicSessionExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicSessionException
	#'
	#' This function creates an ElectronicTranscriptAcademicSessionException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicSessionException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicSessionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicSessionException <- function(ElectronicTranscriptAcademicSessionV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSessionExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicSessionException
	#'
	#' This function modifies an ElectronicTranscriptAcademicSessionException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicSessionException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicSessionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicSessionException <- function(ElectronicTranscriptAcademicSessionExceptionID, ElectronicTranscriptAcademicSessionV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionException", objectId = ElectronicTranscriptAcademicSessionExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSessionExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicSummaryExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicSummaryExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSummaryExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSummaryExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSummaryException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicSummaryExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicSummaryExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicSummaryExceptionID = F, ElectronicTranscriptAcademicSummaryV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicSummaryException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicSummaryException
	#' @param ElectronicTranscriptAcademicSummaryExceptionID The ID of the ElectronicTranscriptAcademicSummaryException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSummaryException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSummaryException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSummaryException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicSummaryException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicSummaryException <- function(ElectronicTranscriptAcademicSummaryExceptionID, ElectronicTranscriptAcademicSummaryV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicSummaryExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryException", objectId = ElectronicTranscriptAcademicSummaryExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicSummaryException
	#'
	#' This function deletes an ElectronicTranscriptAcademicSummaryException
	#' @param ElectronicTranscriptAcademicSummaryExceptionID The ID of the ElectronicTranscriptAcademicSummaryException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicSummaryExceptionID of the deleted ElectronicTranscriptAcademicSummaryException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicSummaryException <- function(ElectronicTranscriptAcademicSummaryExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryException", objectId = ElectronicTranscriptAcademicSummaryExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicSummaryException
	#'
	#' This function creates an ElectronicTranscriptAcademicSummaryException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicSummaryException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicSummaryException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicSummaryException <- function(ElectronicTranscriptAcademicSummaryV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSummaryExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicSummaryException
	#'
	#' This function modifies an ElectronicTranscriptAcademicSummaryException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicSummaryException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicSummaryException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicSummaryException <- function(ElectronicTranscriptAcademicSummaryExceptionID, ElectronicTranscriptAcademicSummaryV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryException", objectId = ElectronicTranscriptAcademicSummaryExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSummaryExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicRecordExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicRecordExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicRecordExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicRecordExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicRecordException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicRecordExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicRecordExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicRecordExceptionID = F, ElectronicTranscriptAcademicRecordV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicRecordException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicRecordException
	#' @param ElectronicTranscriptAcademicRecordExceptionID The ID of the ElectronicTranscriptAcademicRecordException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicRecordException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicRecordException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicRecordException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicRecordException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicRecordException <- function(ElectronicTranscriptAcademicRecordExceptionID, ElectronicTranscriptAcademicRecordV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicRecordExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordException", objectId = ElectronicTranscriptAcademicRecordExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicRecordException
	#'
	#' This function deletes an ElectronicTranscriptAcademicRecordException
	#' @param ElectronicTranscriptAcademicRecordExceptionID The ID of the ElectronicTranscriptAcademicRecordException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicRecordExceptionID of the deleted ElectronicTranscriptAcademicRecordException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicRecordException <- function(ElectronicTranscriptAcademicRecordExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordException", objectId = ElectronicTranscriptAcademicRecordExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicRecordException
	#'
	#' This function creates an ElectronicTranscriptAcademicRecordException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicRecordException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicRecordException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicRecordException <- function(ElectronicTranscriptAcademicRecordV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicRecordExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicRecordException
	#'
	#' This function modifies an ElectronicTranscriptAcademicRecordException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicRecordException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicRecordException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicRecordException <- function(ElectronicTranscriptAcademicRecordExceptionID, ElectronicTranscriptAcademicRecordV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordException", objectId = ElectronicTranscriptAcademicRecordExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicRecordExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptTestScoreExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptTestScoreExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestScoreExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestScoreExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestScoreException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptTestScoreExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptTestScoreExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptTestScoreExceptionID = F, ElectronicTranscriptTestScoreV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentTestID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptTestScoreException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptTestScoreException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptTestScoreException
	#' @param ElectronicTranscriptTestScoreExceptionID The ID of the ElectronicTranscriptTestScoreException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestScoreException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestScoreException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestScoreException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptTestScoreException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptTestScoreException <- function(ElectronicTranscriptTestScoreExceptionID, ElectronicTranscriptTestScoreV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentTestID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptTestScoreExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreException", objectId = ElectronicTranscriptTestScoreExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptTestScoreException
	#'
	#' This function deletes an ElectronicTranscriptTestScoreException
	#' @param ElectronicTranscriptTestScoreExceptionID The ID of the ElectronicTranscriptTestScoreException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptTestScoreExceptionID of the deleted ElectronicTranscriptTestScoreException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptTestScoreException <- function(ElectronicTranscriptTestScoreExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreException", objectId = ElectronicTranscriptTestScoreExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptTestScoreException
	#'
	#' This function creates an ElectronicTranscriptTestScoreException
	#' @param fieldNames The field values to give the created ElectronicTranscriptTestScoreException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptTestScoreException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptTestScoreException <- function(ElectronicTranscriptTestScoreV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentTestID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestScoreExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptTestScoreException
	#'
	#' This function modifies an ElectronicTranscriptTestScoreException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptTestScoreException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptTestScoreException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptTestScoreException <- function(ElectronicTranscriptTestScoreExceptionID, ElectronicTranscriptTestScoreV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentTestID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreException", objectId = ElectronicTranscriptTestScoreExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestScoreExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptSubtestExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptSubtestExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptSubtestExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptSubtestExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptSubtestException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptSubtestExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptSubtestExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptSubtestExceptionID = F, ElectronicTranscriptSubtestV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentTestID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptSubtestException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptSubtestException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptSubtestException
	#' @param ElectronicTranscriptSubtestExceptionID The ID of the ElectronicTranscriptSubtestException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptSubtestException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptSubtestException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptSubtestException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptSubtestException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptSubtestException <- function(ElectronicTranscriptSubtestExceptionID, ElectronicTranscriptSubtestV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentTestID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptSubtestExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestException", objectId = ElectronicTranscriptSubtestExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptSubtestException
	#'
	#' This function deletes an ElectronicTranscriptSubtestException
	#' @param ElectronicTranscriptSubtestExceptionID The ID of the ElectronicTranscriptSubtestException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptSubtestExceptionID of the deleted ElectronicTranscriptSubtestException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptSubtestException <- function(ElectronicTranscriptSubtestExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestException", objectId = ElectronicTranscriptSubtestExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptSubtestException
	#'
	#' This function creates an ElectronicTranscriptSubtestException
	#' @param fieldNames The field values to give the created ElectronicTranscriptSubtestException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptSubtestException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptSubtestException <- function(ElectronicTranscriptSubtestV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentTestID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptSubtestExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptSubtestException
	#'
	#' This function modifies an ElectronicTranscriptSubtestException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptSubtestException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptSubtestException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptSubtestException <- function(ElectronicTranscriptSubtestExceptionID, ElectronicTranscriptSubtestV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentTestID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestException", objectId = ElectronicTranscriptSubtestExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptSubtestExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptTestExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptTestExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptTestExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptTestExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptTestExceptionID = F, ElectronicTranscriptTestV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentTestID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptTestException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptTestException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptTestException
	#' @param ElectronicTranscriptTestExceptionID The ID of the ElectronicTranscriptTestException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptTestException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptTestException <- function(ElectronicTranscriptTestExceptionID, ElectronicTranscriptTestV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentTestID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptTestExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestException", objectId = ElectronicTranscriptTestExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptTestException
	#'
	#' This function deletes an ElectronicTranscriptTestException
	#' @param ElectronicTranscriptTestExceptionID The ID of the ElectronicTranscriptTestException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptTestExceptionID of the deleted ElectronicTranscriptTestException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptTestException <- function(ElectronicTranscriptTestExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestException", objectId = ElectronicTranscriptTestExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptTestException
	#'
	#' This function creates an ElectronicTranscriptTestException
	#' @param fieldNames The field values to give the created ElectronicTranscriptTestException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptTestException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptTestException <- function(ElectronicTranscriptTestV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentTestID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptTestException
	#'
	#' This function modifies an ElectronicTranscriptTestException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptTestException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptTestException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptTestException <- function(ElectronicTranscriptTestExceptionID, ElectronicTranscriptTestV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentTestID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptTestException", objectId = ElectronicTranscriptTestExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptImmunizationExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptImmunizationExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptImmunizationExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptImmunizationExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptImmunizationException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptImmunizationExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptImmunizationExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptImmunizationExceptionID = F, ElectronicTranscriptImmunizationV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentVaccineID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptImmunizationException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptImmunizationException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptImmunizationException
	#' @param ElectronicTranscriptImmunizationExceptionID The ID of the ElectronicTranscriptImmunizationException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptImmunizationException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptImmunizationException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptImmunizationException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptImmunizationException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptImmunizationException <- function(ElectronicTranscriptImmunizationExceptionID, ElectronicTranscriptImmunizationV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentVaccineID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptImmunizationExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationException", objectId = ElectronicTranscriptImmunizationExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptImmunizationException
	#'
	#' This function deletes an ElectronicTranscriptImmunizationException
	#' @param ElectronicTranscriptImmunizationExceptionID The ID of the ElectronicTranscriptImmunizationException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptImmunizationExceptionID of the deleted ElectronicTranscriptImmunizationException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptImmunizationException <- function(ElectronicTranscriptImmunizationExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationException", objectId = ElectronicTranscriptImmunizationExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptImmunizationException
	#'
	#' This function creates an ElectronicTranscriptImmunizationException
	#' @param fieldNames The field values to give the created ElectronicTranscriptImmunizationException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptImmunizationException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptImmunizationException <- function(ElectronicTranscriptImmunizationV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentVaccineID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptImmunizationExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptImmunizationException
	#'
	#' This function modifies an ElectronicTranscriptImmunizationException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptImmunizationException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptImmunizationException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptImmunizationException <- function(ElectronicTranscriptImmunizationExceptionID, ElectronicTranscriptImmunizationV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentVaccineID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationException", objectId = ElectronicTranscriptImmunizationExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptImmunizationExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptLicensureExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptLicensureExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptLicensureExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptLicensureExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptLicensureExceptionID = F, ElectronicTranscriptLicensureV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptLicensureException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptLicensureException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptLicensureException
	#' @param ElectronicTranscriptLicensureExceptionID The ID of the ElectronicTranscriptLicensureException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptLicensureException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptLicensureException <- function(ElectronicTranscriptLicensureExceptionID, ElectronicTranscriptLicensureV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptLicensureExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureException", objectId = ElectronicTranscriptLicensureExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptLicensureException
	#'
	#' This function deletes an ElectronicTranscriptLicensureException
	#' @param ElectronicTranscriptLicensureExceptionID The ID of the ElectronicTranscriptLicensureException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptLicensureExceptionID of the deleted ElectronicTranscriptLicensureException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptLicensureException <- function(ElectronicTranscriptLicensureExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureException", objectId = ElectronicTranscriptLicensureExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptLicensureException
	#'
	#' This function creates an ElectronicTranscriptLicensureException
	#' @param fieldNames The field values to give the created ElectronicTranscriptLicensureException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptLicensureException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptLicensureException <- function(ElectronicTranscriptLicensureV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptLicensureException
	#'
	#' This function modifies an ElectronicTranscriptLicensureException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptLicensureException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptLicensureException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptLicensureException <- function(ElectronicTranscriptLicensureExceptionID, ElectronicTranscriptLicensureV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureException", objectId = ElectronicTranscriptLicensureExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptCourseExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptCourseExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptCourseExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptCourseExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptCourseException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptCourseExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptCourseExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptCourseExceptionID = F, ElectronicTranscriptCourseV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentSectionID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptCourseException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptCourseException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptCourseException
	#' @param ElectronicTranscriptCourseExceptionID The ID of the ElectronicTranscriptCourseException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptCourseException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptCourseException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptCourseException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptCourseException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptCourseException <- function(ElectronicTranscriptCourseExceptionID, ElectronicTranscriptCourseV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentSectionID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptCourseExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseException", objectId = ElectronicTranscriptCourseExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptCourseException
	#'
	#' This function deletes an ElectronicTranscriptCourseException
	#' @param ElectronicTranscriptCourseExceptionID The ID of the ElectronicTranscriptCourseException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptCourseExceptionID of the deleted ElectronicTranscriptCourseException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptCourseException <- function(ElectronicTranscriptCourseExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseException", objectId = ElectronicTranscriptCourseExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptCourseException
	#'
	#' This function creates an ElectronicTranscriptCourseException
	#' @param fieldNames The field values to give the created ElectronicTranscriptCourseException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptCourseException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptCourseException <- function(ElectronicTranscriptCourseV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptCourseExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptCourseException
	#'
	#' This function modifies an ElectronicTranscriptCourseException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptCourseException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptCourseException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptCourseException <- function(ElectronicTranscriptCourseExceptionID, ElectronicTranscriptCourseV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, StudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseException", objectId = ElectronicTranscriptCourseExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptCourseExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptPhoneExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptPhoneExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPhoneExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPhoneExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPhoneException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptPhoneExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptPhoneExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptPhoneExceptionID = F, ElectronicTranscriptPhoneV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptPhoneException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptPhoneException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptPhoneException
	#' @param ElectronicTranscriptPhoneExceptionID The ID of the ElectronicTranscriptPhoneException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPhoneException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPhoneException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPhoneException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptPhoneException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptPhoneException <- function(ElectronicTranscriptPhoneExceptionID, ElectronicTranscriptPhoneV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptPhoneExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneException", objectId = ElectronicTranscriptPhoneExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptPhoneException
	#'
	#' This function deletes an ElectronicTranscriptPhoneException
	#' @param ElectronicTranscriptPhoneExceptionID The ID of the ElectronicTranscriptPhoneException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptPhoneExceptionID of the deleted ElectronicTranscriptPhoneException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptPhoneException <- function(ElectronicTranscriptPhoneExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneException", objectId = ElectronicTranscriptPhoneExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptPhoneException
	#'
	#' This function creates an ElectronicTranscriptPhoneException
	#' @param fieldNames The field values to give the created ElectronicTranscriptPhoneException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptPhoneException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptPhoneException <- function(ElectronicTranscriptPhoneV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptPhoneExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptPhoneException
	#'
	#' This function modifies an ElectronicTranscriptPhoneException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptPhoneException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptPhoneException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptPhoneException <- function(ElectronicTranscriptPhoneExceptionID, ElectronicTranscriptPhoneV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneException", objectId = ElectronicTranscriptPhoneExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptPhoneExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAddressExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAddressExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAddressExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAddressExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAddressException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAddressExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAddressExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAddressExceptionID = F, ElectronicTranscriptAddressV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAddressException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAddressException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAddressException
	#' @param ElectronicTranscriptAddressExceptionID The ID of the ElectronicTranscriptAddressException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAddressException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAddressException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAddressException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAddressException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAddressException <- function(ElectronicTranscriptAddressExceptionID, ElectronicTranscriptAddressV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAddressExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressException", objectId = ElectronicTranscriptAddressExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAddressException
	#'
	#' This function deletes an ElectronicTranscriptAddressException
	#' @param ElectronicTranscriptAddressExceptionID The ID of the ElectronicTranscriptAddressException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAddressExceptionID of the deleted ElectronicTranscriptAddressException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAddressException <- function(ElectronicTranscriptAddressExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressException", objectId = ElectronicTranscriptAddressExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAddressException
	#'
	#' This function creates an ElectronicTranscriptAddressException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAddressException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAddressException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAddressException <- function(ElectronicTranscriptAddressV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAddressExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAddressException
	#'
	#' This function modifies an ElectronicTranscriptAddressException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAddressException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAddressException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAddressException <- function(ElectronicTranscriptAddressExceptionID, ElectronicTranscriptAddressV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressException", objectId = ElectronicTranscriptAddressExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAddressExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAgencyIdentifierExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAgencyIdentifierExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAgencyIdentifierExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAgencyIdentifierExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAgencyIdentifierException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAgencyIdentifierExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAgencyIdentifierExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptAgencyIdentifierExceptionID = F, ElectronicTranscriptAgencyIdentifierV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAgencyIdentifierException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAgencyIdentifierException
	#' @param ElectronicTranscriptAgencyIdentifierExceptionID The ID of the ElectronicTranscriptAgencyIdentifierException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAgencyIdentifierException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAgencyIdentifierException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAgencyIdentifierException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAgencyIdentifierException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAgencyIdentifierException <- function(ElectronicTranscriptAgencyIdentifierExceptionID, ElectronicTranscriptAgencyIdentifierV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAgencyIdentifierExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierException", objectId = ElectronicTranscriptAgencyIdentifierExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAgencyIdentifierException
	#'
	#' This function deletes an ElectronicTranscriptAgencyIdentifierException
	#' @param ElectronicTranscriptAgencyIdentifierExceptionID The ID of the ElectronicTranscriptAgencyIdentifierException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAgencyIdentifierExceptionID of the deleted ElectronicTranscriptAgencyIdentifierException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAgencyIdentifierException <- function(ElectronicTranscriptAgencyIdentifierExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierException", objectId = ElectronicTranscriptAgencyIdentifierExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAgencyIdentifierException
	#'
	#' This function creates an ElectronicTranscriptAgencyIdentifierException
	#' @param fieldNames The field values to give the created ElectronicTranscriptAgencyIdentifierException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAgencyIdentifierException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAgencyIdentifierException <- function(ElectronicTranscriptAgencyIdentifierV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAgencyIdentifierExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAgencyIdentifierException
	#'
	#' This function modifies an ElectronicTranscriptAgencyIdentifierException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAgencyIdentifierException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAgencyIdentifierException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAgencyIdentifierException <- function(ElectronicTranscriptAgencyIdentifierExceptionID, ElectronicTranscriptAgencyIdentifierV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierException", objectId = ElectronicTranscriptAgencyIdentifierExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAgencyIdentifierExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicAwardV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicAwardV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicAwardV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicAwardV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicAwardV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicAwardV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicAwardV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicAwardV1ID = F, AcademicAwardLevel = F, AcademicAwardDate = F, AcademicAwardTitle = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicAwardV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicAwardV1
	#' @param ElectronicTranscriptAcademicAwardV1ID The ID of the ElectronicTranscriptAcademicAwardV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicAwardV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicAwardV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicAwardV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicAwardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicAwardV1 <- function(ElectronicTranscriptAcademicAwardV1ID, AcademicAwardLevel = F, AcademicAwardDate = F, AcademicAwardTitle = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicAwardV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardV1", objectId = ElectronicTranscriptAcademicAwardV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicAwardV1
	#'
	#' This function deletes an ElectronicTranscriptAcademicAwardV1
	#' @param ElectronicTranscriptAcademicAwardV1ID The ID of the ElectronicTranscriptAcademicAwardV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicAwardV1ID of the deleted ElectronicTranscriptAcademicAwardV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicAwardV1 <- function(ElectronicTranscriptAcademicAwardV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardV1", objectId = ElectronicTranscriptAcademicAwardV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicAwardV1
	#'
	#' This function creates an ElectronicTranscriptAcademicAwardV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicAwardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicAwardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicAwardV1 <- function(AcademicAwardLevel = NULL, AcademicAwardDate = NULL, AcademicAwardTitle = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicAwardV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicAwardV1
	#'
	#' This function modifies an ElectronicTranscriptAcademicAwardV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicAwardV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicAwardV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicAwardV1 <- function(ElectronicTranscriptAcademicAwardV1ID, AcademicAwardLevel = NULL, AcademicAwardDate = NULL, AcademicAwardTitle = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicAwardV1", objectId = ElectronicTranscriptAcademicAwardV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicAwardV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicHonorV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicHonorV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicHonorV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicHonorV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicHonorV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicHonorV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicHonorV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicHonorV1ID = F, AcademicAwardLevel = F, HonorsTitle = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicHonorV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicHonorV1
	#' @param ElectronicTranscriptAcademicHonorV1ID The ID of the ElectronicTranscriptAcademicHonorV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicHonorV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicHonorV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicHonorV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicHonorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicHonorV1 <- function(ElectronicTranscriptAcademicHonorV1ID, AcademicAwardLevel = F, HonorsTitle = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicHonorV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorV1", objectId = ElectronicTranscriptAcademicHonorV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicHonorV1
	#'
	#' This function deletes an ElectronicTranscriptAcademicHonorV1
	#' @param ElectronicTranscriptAcademicHonorV1ID The ID of the ElectronicTranscriptAcademicHonorV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicHonorV1ID of the deleted ElectronicTranscriptAcademicHonorV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicHonorV1 <- function(ElectronicTranscriptAcademicHonorV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorV1", objectId = ElectronicTranscriptAcademicHonorV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicHonorV1
	#'
	#' This function creates an ElectronicTranscriptAcademicHonorV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicHonorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicHonorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicHonorV1 <- function(AcademicAwardLevel = NULL, HonorsTitle = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicHonorV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicHonorV1
	#'
	#' This function modifies an ElectronicTranscriptAcademicHonorV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicHonorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicHonorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicHonorV1 <- function(ElectronicTranscriptAcademicHonorV1ID, AcademicAwardLevel = NULL, HonorsTitle = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicHonorV1", objectId = ElectronicTranscriptAcademicHonorV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicHonorV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicSessionV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicSessionV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSessionV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSessionV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSessionV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicSessionV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicSessionV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicSessionV1ID = F, SessionDesignator = F, SectionLengthID = F, SessionName = F, SessionType = F, SessionSchoolYear = F, StudentLevel = F, DaysPresent = F, DaysAbsent = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntryWithdrawalID = F, OrganizationName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicSessionV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicSessionV1
	#' @param ElectronicTranscriptAcademicSessionV1ID The ID of the ElectronicTranscriptAcademicSessionV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSessionV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSessionV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSessionV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicSessionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicSessionV1 <- function(ElectronicTranscriptAcademicSessionV1ID, SessionDesignator = F, SectionLengthID = F, SessionName = F, SessionType = F, SessionSchoolYear = F, StudentLevel = F, DaysPresent = F, DaysAbsent = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntryWithdrawalID = F, OrganizationName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicSessionV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionV1", objectId = ElectronicTranscriptAcademicSessionV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicSessionV1
	#'
	#' This function deletes an ElectronicTranscriptAcademicSessionV1
	#' @param ElectronicTranscriptAcademicSessionV1ID The ID of the ElectronicTranscriptAcademicSessionV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicSessionV1ID of the deleted ElectronicTranscriptAcademicSessionV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicSessionV1 <- function(ElectronicTranscriptAcademicSessionV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionV1", objectId = ElectronicTranscriptAcademicSessionV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicSessionV1
	#'
	#' This function creates an ElectronicTranscriptAcademicSessionV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicSessionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicSessionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicSessionV1 <- function(SessionDesignator = NULL, SectionLengthID = NULL, SessionName = NULL, SessionType = NULL, SessionSchoolYear = NULL, StudentLevel = NULL, DaysPresent = NULL, DaysAbsent = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EntryWithdrawalID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSessionV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicSessionV1
	#'
	#' This function modifies an ElectronicTranscriptAcademicSessionV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicSessionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicSessionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicSessionV1 <- function(ElectronicTranscriptAcademicSessionV1ID, SessionDesignator = NULL, SectionLengthID = NULL, SessionName = NULL, SessionType = NULL, SessionSchoolYear = NULL, StudentLevel = NULL, DaysPresent = NULL, DaysAbsent = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EntryWithdrawalID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSessionV1", objectId = ElectronicTranscriptAcademicSessionV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSessionV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptTestScoreV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptTestScoreV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestScoreV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestScoreV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestScoreV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptTestScoreV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptTestScoreV1s <- function(searchConditionsList = NULL, ElectronicTranscriptTestScoreV1ID = F, StudentTestID = F, ScoreValue = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EducationTest = F, TestDate = F, NoteMessage = F, EducationSubtest = F, TestScoreMethod = F, SubtestName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptTestScoreV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptTestScoreV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptTestScoreV1
	#' @param ElectronicTranscriptTestScoreV1ID The ID of the ElectronicTranscriptTestScoreV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestScoreV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestScoreV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestScoreV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptTestScoreV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptTestScoreV1 <- function(ElectronicTranscriptTestScoreV1ID, StudentTestID = F, ScoreValue = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EducationTest = F, TestDate = F, NoteMessage = F, EducationSubtest = F, TestScoreMethod = F, SubtestName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptTestScoreV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreV1", objectId = ElectronicTranscriptTestScoreV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptTestScoreV1
	#'
	#' This function deletes an ElectronicTranscriptTestScoreV1
	#' @param ElectronicTranscriptTestScoreV1ID The ID of the ElectronicTranscriptTestScoreV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptTestScoreV1ID of the deleted ElectronicTranscriptTestScoreV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptTestScoreV1 <- function(ElectronicTranscriptTestScoreV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreV1", objectId = ElectronicTranscriptTestScoreV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptTestScoreV1
	#'
	#' This function creates an ElectronicTranscriptTestScoreV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptTestScoreV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptTestScoreV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptTestScoreV1 <- function(StudentTestID = NULL, ScoreValue = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EducationTest = NULL, TestDate = NULL, NoteMessage = NULL, EducationSubtest = NULL, TestScoreMethod = NULL, SubtestName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestScoreV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptTestScoreV1
	#'
	#' This function modifies an ElectronicTranscriptTestScoreV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptTestScoreV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptTestScoreV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptTestScoreV1 <- function(ElectronicTranscriptTestScoreV1ID, StudentTestID = NULL, ScoreValue = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EducationTest = NULL, TestDate = NULL, NoteMessage = NULL, EducationSubtest = NULL, TestScoreMethod = NULL, SubtestName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptTestScoreV1", objectId = ElectronicTranscriptTestScoreV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestScoreV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptSubtestV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptSubtestV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptSubtestV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptSubtestV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptSubtestV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptSubtestV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptSubtestV1s <- function(searchConditionsList = NULL, ElectronicTranscriptSubtestV1ID = F, StudentTestID = F, SubtestName = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EducationTest = F, TestDate = F, NoteMessage = F, EducationSubtest = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptSubtestV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptSubtestV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptSubtestV1
	#' @param ElectronicTranscriptSubtestV1ID The ID of the ElectronicTranscriptSubtestV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptSubtestV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptSubtestV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptSubtestV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptSubtestV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptSubtestV1 <- function(ElectronicTranscriptSubtestV1ID, StudentTestID = F, SubtestName = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EducationTest = F, TestDate = F, NoteMessage = F, EducationSubtest = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptSubtestV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestV1", objectId = ElectronicTranscriptSubtestV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptSubtestV1
	#'
	#' This function deletes an ElectronicTranscriptSubtestV1
	#' @param ElectronicTranscriptSubtestV1ID The ID of the ElectronicTranscriptSubtestV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptSubtestV1ID of the deleted ElectronicTranscriptSubtestV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptSubtestV1 <- function(ElectronicTranscriptSubtestV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestV1", objectId = ElectronicTranscriptSubtestV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptSubtestV1
	#'
	#' This function creates an ElectronicTranscriptSubtestV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptSubtestV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptSubtestV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptSubtestV1 <- function(StudentTestID = NULL, SubtestName = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EducationTest = NULL, TestDate = NULL, NoteMessage = NULL, EducationSubtest = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptSubtestV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptSubtestV1
	#'
	#' This function modifies an ElectronicTranscriptSubtestV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptSubtestV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptSubtestV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptSubtestV1 <- function(ElectronicTranscriptSubtestV1ID, StudentTestID = NULL, SubtestName = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EducationTest = NULL, TestDate = NULL, NoteMessage = NULL, EducationSubtest = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptSubtestV1", objectId = ElectronicTranscriptSubtestV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptSubtestV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptTestV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptTestV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptTestV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptTestV1s <- function(searchConditionsList = NULL, ElectronicTranscriptTestV1ID = F, StudentTestID = F, TestName = F, TestDate = F, NoteMessage = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EducationTest = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptTestV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptTestV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptTestV1
	#' @param ElectronicTranscriptTestV1ID The ID of the ElectronicTranscriptTestV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptTestV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptTestV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptTestV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptTestV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptTestV1 <- function(ElectronicTranscriptTestV1ID, StudentTestID = F, TestName = F, TestDate = F, NoteMessage = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EducationTest = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptTestV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestV1", objectId = ElectronicTranscriptTestV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptTestV1
	#'
	#' This function deletes an ElectronicTranscriptTestV1
	#' @param ElectronicTranscriptTestV1ID The ID of the ElectronicTranscriptTestV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptTestV1ID of the deleted ElectronicTranscriptTestV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptTestV1 <- function(ElectronicTranscriptTestV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestV1", objectId = ElectronicTranscriptTestV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptTestV1
	#'
	#' This function creates an ElectronicTranscriptTestV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptTestV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptTestV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptTestV1 <- function(StudentTestID = NULL, TestName = NULL, TestDate = NULL, NoteMessage = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EducationTest = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptTestV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptTestV1
	#'
	#' This function modifies an ElectronicTranscriptTestV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptTestV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptTestV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptTestV1 <- function(ElectronicTranscriptTestV1ID, StudentTestID = NULL, TestName = NULL, TestDate = NULL, NoteMessage = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EducationTest = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptTestV1", objectId = ElectronicTranscriptTestV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptTestV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptImmunizationV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptImmunizationV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptImmunizationV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptImmunizationV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptImmunizationV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptImmunizationV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptImmunizationV1s <- function(searchConditionsList = NULL, ElectronicTranscriptImmunizationV1ID = F, ImmunizationDate = F, ImmunizationCode = F, ImmunizationStatus = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentVaccineID = F, StudentVaccinationWaiverID = F, StudentChildhoodIllnessID = F, VaccineID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptImmunizationV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptImmunizationV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptImmunizationV1
	#' @param ElectronicTranscriptImmunizationV1ID The ID of the ElectronicTranscriptImmunizationV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptImmunizationV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptImmunizationV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptImmunizationV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptImmunizationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptImmunizationV1 <- function(ElectronicTranscriptImmunizationV1ID, ImmunizationDate = F, ImmunizationCode = F, ImmunizationStatus = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentVaccineID = F, StudentVaccinationWaiverID = F, StudentChildhoodIllnessID = F, VaccineID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptImmunizationV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationV1", objectId = ElectronicTranscriptImmunizationV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptImmunizationV1
	#'
	#' This function deletes an ElectronicTranscriptImmunizationV1
	#' @param ElectronicTranscriptImmunizationV1ID The ID of the ElectronicTranscriptImmunizationV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptImmunizationV1ID of the deleted ElectronicTranscriptImmunizationV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptImmunizationV1 <- function(ElectronicTranscriptImmunizationV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationV1", objectId = ElectronicTranscriptImmunizationV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptImmunizationV1
	#'
	#' This function creates an ElectronicTranscriptImmunizationV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptImmunizationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptImmunizationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptImmunizationV1 <- function(ImmunizationDate = NULL, ImmunizationCode = NULL, ImmunizationStatus = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, StudentVaccineID = NULL, StudentVaccinationWaiverID = NULL, StudentChildhoodIllnessID = NULL, VaccineID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptImmunizationV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptImmunizationV1
	#'
	#' This function modifies an ElectronicTranscriptImmunizationV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptImmunizationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptImmunizationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptImmunizationV1 <- function(ElectronicTranscriptImmunizationV1ID, ImmunizationDate = NULL, ImmunizationCode = NULL, ImmunizationStatus = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, StudentVaccineID = NULL, StudentVaccinationWaiverID = NULL, StudentChildhoodIllnessID = NULL, VaccineID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptImmunizationV1", objectId = ElectronicTranscriptImmunizationV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptImmunizationV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptLicensureV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptLicensureV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptLicensureV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptLicensureV1s <- function(searchConditionsList = NULL, ElectronicTranscriptLicensureV1ID = F, LicensureName = F, LicensurePassageDate = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptLicensureV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptLicensureV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptLicensureV1
	#' @param ElectronicTranscriptLicensureV1ID The ID of the ElectronicTranscriptLicensureV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptLicensureV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptLicensureV1 <- function(ElectronicTranscriptLicensureV1ID, LicensureName = F, LicensurePassageDate = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptLicensureV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureV1", objectId = ElectronicTranscriptLicensureV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptLicensureV1
	#'
	#' This function deletes an ElectronicTranscriptLicensureV1
	#' @param ElectronicTranscriptLicensureV1ID The ID of the ElectronicTranscriptLicensureV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptLicensureV1ID of the deleted ElectronicTranscriptLicensureV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptLicensureV1 <- function(ElectronicTranscriptLicensureV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureV1", objectId = ElectronicTranscriptLicensureV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptLicensureV1
	#'
	#' This function creates an ElectronicTranscriptLicensureV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptLicensureV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptLicensureV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptLicensureV1 <- function(LicensureName = NULL, LicensurePassageDate = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptLicensureV1
	#'
	#' This function modifies an ElectronicTranscriptLicensureV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptLicensureV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptLicensureV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptLicensureV1 <- function(ElectronicTranscriptLicensureV1ID, LicensureName = NULL, LicensurePassageDate = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureV1", objectId = ElectronicTranscriptLicensureV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptCourseV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptCourseV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptCourseV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptCourseV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptCourseV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptCourseV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptCourseV1s <- function(searchConditionsList = NULL, ElectronicTranscriptCourseV1ID = F, SessionDesignator = F, StudentSectionID = F, IsInProgress = F, CourseCreditBasis = F, CourseCreditEarned = F, CourseAcademicGrade = F, CourseQualityPointsEarned = F, CourseLevel = F, CourseGPAApplicability = F, CourseNumber = F, AgencyCourseID = F, CourseTitle = F, CourseBeginDate = F, CourseEndDate = F, NoteMessage = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentGradeBucketID = F, OrganizationName = F, SectionLengthID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptCourseV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptCourseV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptCourseV1
	#' @param ElectronicTranscriptCourseV1ID The ID of the ElectronicTranscriptCourseV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptCourseV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptCourseV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptCourseV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptCourseV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptCourseV1 <- function(ElectronicTranscriptCourseV1ID, SessionDesignator = F, StudentSectionID = F, IsInProgress = F, CourseCreditBasis = F, CourseCreditEarned = F, CourseAcademicGrade = F, CourseQualityPointsEarned = F, CourseLevel = F, CourseGPAApplicability = F, CourseNumber = F, AgencyCourseID = F, CourseTitle = F, CourseBeginDate = F, CourseEndDate = F, NoteMessage = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentGradeBucketID = F, OrganizationName = F, SectionLengthID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptCourseV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseV1", objectId = ElectronicTranscriptCourseV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptCourseV1
	#'
	#' This function deletes an ElectronicTranscriptCourseV1
	#' @param ElectronicTranscriptCourseV1ID The ID of the ElectronicTranscriptCourseV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptCourseV1ID of the deleted ElectronicTranscriptCourseV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptCourseV1 <- function(ElectronicTranscriptCourseV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseV1", objectId = ElectronicTranscriptCourseV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptCourseV1
	#'
	#' This function creates an ElectronicTranscriptCourseV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptCourseV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptCourseV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptCourseV1 <- function(SessionDesignator = NULL, StudentSectionID = NULL, IsInProgress = NULL, CourseCreditBasis = NULL, CourseCreditEarned = NULL, CourseAcademicGrade = NULL, CourseQualityPointsEarned = NULL, CourseLevel = NULL, CourseGPAApplicability = NULL, CourseNumber = NULL, AgencyCourseID = NULL, CourseTitle = NULL, CourseBeginDate = NULL, CourseEndDate = NULL, NoteMessage = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, StudentGradeBucketID = NULL, OrganizationName = NULL, SectionLengthID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptCourseV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptCourseV1
	#'
	#' This function modifies an ElectronicTranscriptCourseV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptCourseV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptCourseV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptCourseV1 <- function(ElectronicTranscriptCourseV1ID, SessionDesignator = NULL, StudentSectionID = NULL, IsInProgress = NULL, CourseCreditBasis = NULL, CourseCreditEarned = NULL, CourseAcademicGrade = NULL, CourseQualityPointsEarned = NULL, CourseLevel = NULL, CourseGPAApplicability = NULL, CourseNumber = NULL, AgencyCourseID = NULL, CourseTitle = NULL, CourseBeginDate = NULL, CourseEndDate = NULL, NoteMessage = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, StudentGradeBucketID = NULL, OrganizationName = NULL, SectionLengthID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptCourseV1", objectId = ElectronicTranscriptCourseV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptCourseV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicSummaryV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicSummaryV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSummaryV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSummaryV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSummaryV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicSummaryV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicSummaryV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicSummaryV1ID = F, AcademicSummaryType = F, CreditHoursEarned = F, CreditHoursRequired = F, GradePointAverage = F, TotalQualityPoints = F, CreditHoursForGPA = F, GPARangeMaximum = F, EntryDate = F, ExitDate = F, ClassRank = F, ClassSize = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntryWithdrawalID = F, StudentPlanID = F, StudentGPABucketGroupID = F, StudentRankID = F, OrganizationName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicSummaryV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicSummaryV1
	#' @param ElectronicTranscriptAcademicSummaryV1ID The ID of the ElectronicTranscriptAcademicSummaryV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicSummaryV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicSummaryV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicSummaryV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicSummaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicSummaryV1 <- function(ElectronicTranscriptAcademicSummaryV1ID, AcademicSummaryType = F, CreditHoursEarned = F, CreditHoursRequired = F, GradePointAverage = F, TotalQualityPoints = F, CreditHoursForGPA = F, GPARangeMaximum = F, EntryDate = F, ExitDate = F, ClassRank = F, ClassSize = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntryWithdrawalID = F, StudentPlanID = F, StudentGPABucketGroupID = F, StudentRankID = F, OrganizationName = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicSummaryV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryV1", objectId = ElectronicTranscriptAcademicSummaryV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicSummaryV1
	#'
	#' This function deletes an ElectronicTranscriptAcademicSummaryV1
	#' @param ElectronicTranscriptAcademicSummaryV1ID The ID of the ElectronicTranscriptAcademicSummaryV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicSummaryV1ID of the deleted ElectronicTranscriptAcademicSummaryV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicSummaryV1 <- function(ElectronicTranscriptAcademicSummaryV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryV1", objectId = ElectronicTranscriptAcademicSummaryV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicSummaryV1
	#'
	#' This function creates an ElectronicTranscriptAcademicSummaryV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicSummaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicSummaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicSummaryV1 <- function(AcademicSummaryType = NULL, CreditHoursEarned = NULL, CreditHoursRequired = NULL, GradePointAverage = NULL, TotalQualityPoints = NULL, CreditHoursForGPA = NULL, GPARangeMaximum = NULL, EntryDate = NULL, ExitDate = NULL, ClassRank = NULL, ClassSize = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EntryWithdrawalID = NULL, StudentPlanID = NULL, StudentGPABucketGroupID = NULL, StudentRankID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSummaryV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicSummaryV1
	#'
	#' This function modifies an ElectronicTranscriptAcademicSummaryV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicSummaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicSummaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicSummaryV1 <- function(ElectronicTranscriptAcademicSummaryV1ID, AcademicSummaryType = NULL, CreditHoursEarned = NULL, CreditHoursRequired = NULL, GradePointAverage = NULL, TotalQualityPoints = NULL, CreditHoursForGPA = NULL, GPARangeMaximum = NULL, EntryDate = NULL, ExitDate = NULL, ClassRank = NULL, ClassSize = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, EntryWithdrawalID = NULL, StudentPlanID = NULL, StudentGPABucketGroupID = NULL, StudentRankID = NULL, OrganizationName = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicSummaryV1", objectId = ElectronicTranscriptAcademicSummaryV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicSummaryV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAcademicRecordV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAcademicRecordV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicRecordV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicRecordV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicRecordV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAcademicRecordV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAcademicRecordV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAcademicRecordV1ID = F, StudentLevel = F, CohortGraduationYear = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, CEEBACT = F, HasSchoolOverride = F, SchoolOverride = F, SchoolLevel = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAcademicRecordV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAcademicRecordV1
	#' @param ElectronicTranscriptAcademicRecordV1ID The ID of the ElectronicTranscriptAcademicRecordV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAcademicRecordV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAcademicRecordV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAcademicRecordV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAcademicRecordV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAcademicRecordV1 <- function(ElectronicTranscriptAcademicRecordV1ID, StudentLevel = F, CohortGraduationYear = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OrganizationName = F, CEEBACT = F, HasSchoolOverride = F, SchoolOverride = F, SchoolLevel = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAcademicRecordV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordV1", objectId = ElectronicTranscriptAcademicRecordV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAcademicRecordV1
	#'
	#' This function deletes an ElectronicTranscriptAcademicRecordV1
	#' @param ElectronicTranscriptAcademicRecordV1ID The ID of the ElectronicTranscriptAcademicRecordV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAcademicRecordV1ID of the deleted ElectronicTranscriptAcademicRecordV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAcademicRecordV1 <- function(ElectronicTranscriptAcademicRecordV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordV1", objectId = ElectronicTranscriptAcademicRecordV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAcademicRecordV1
	#'
	#' This function creates an ElectronicTranscriptAcademicRecordV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAcademicRecordV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAcademicRecordV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAcademicRecordV1 <- function(StudentLevel = NULL, CohortGraduationYear = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, CEEBACT = NULL, HasSchoolOverride = NULL, SchoolOverride = NULL, SchoolLevel = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicRecordV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAcademicRecordV1
	#'
	#' This function modifies an ElectronicTranscriptAcademicRecordV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAcademicRecordV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAcademicRecordV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAcademicRecordV1 <- function(ElectronicTranscriptAcademicRecordV1ID, StudentLevel = NULL, CohortGraduationYear = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, OrganizationName = NULL, CEEBACT = NULL, HasSchoolOverride = NULL, SchoolOverride = NULL, SchoolLevel = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAcademicRecordV1", objectId = ElectronicTranscriptAcademicRecordV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAcademicRecordV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptPhoneV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptPhoneV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPhoneV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPhoneV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPhoneV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptPhoneV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptPhoneV1s <- function(searchConditionsList = NULL, ElectronicTranscriptPhoneV1ID = F, AreaCityCode = F, PhoneNumber = F, NamePhoneID = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptPhoneV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptPhoneV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptPhoneV1
	#' @param ElectronicTranscriptPhoneV1ID The ID of the ElectronicTranscriptPhoneV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPhoneV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPhoneV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPhoneV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptPhoneV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptPhoneV1 <- function(ElectronicTranscriptPhoneV1ID, AreaCityCode = F, PhoneNumber = F, NamePhoneID = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptPhoneV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneV1", objectId = ElectronicTranscriptPhoneV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptPhoneV1
	#'
	#' This function deletes an ElectronicTranscriptPhoneV1
	#' @param ElectronicTranscriptPhoneV1ID The ID of the ElectronicTranscriptPhoneV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptPhoneV1ID of the deleted ElectronicTranscriptPhoneV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptPhoneV1 <- function(ElectronicTranscriptPhoneV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneV1", objectId = ElectronicTranscriptPhoneV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptPhoneV1
	#'
	#' This function creates an ElectronicTranscriptPhoneV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptPhoneV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptPhoneV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptPhoneV1 <- function(AreaCityCode = NULL, PhoneNumber = NULL, NamePhoneID = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptPhoneV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptPhoneV1
	#'
	#' This function modifies an ElectronicTranscriptPhoneV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptPhoneV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptPhoneV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptPhoneV1 <- function(ElectronicTranscriptPhoneV1ID, AreaCityCode = NULL, PhoneNumber = NULL, NamePhoneID = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptPhoneV1", objectId = ElectronicTranscriptPhoneV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptPhoneV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAddressV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAddressV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAddressV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAddressV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAddressV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAddressV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAddressV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAddressV1ID = F, AddressLine1 = F, AddressLine2 = F, AddressLine3 = F, City = F, StateProvinceCode = F, PostalCode = F, AddressID = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAddressV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAddressV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAddressV1
	#' @param ElectronicTranscriptAddressV1ID The ID of the ElectronicTranscriptAddressV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAddressV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAddressV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAddressV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAddressV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAddressV1 <- function(ElectronicTranscriptAddressV1ID, AddressLine1 = F, AddressLine2 = F, AddressLine3 = F, City = F, StateProvinceCode = F, PostalCode = F, AddressID = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAddressV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressV1", objectId = ElectronicTranscriptAddressV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAddressV1
	#'
	#' This function deletes an ElectronicTranscriptAddressV1
	#' @param ElectronicTranscriptAddressV1ID The ID of the ElectronicTranscriptAddressV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAddressV1ID of the deleted ElectronicTranscriptAddressV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAddressV1 <- function(ElectronicTranscriptAddressV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressV1", objectId = ElectronicTranscriptAddressV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAddressV1
	#'
	#' This function creates an ElectronicTranscriptAddressV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAddressV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAddressV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAddressV1 <- function(AddressLine1 = NULL, AddressLine2 = NULL, AddressLine3 = NULL, City = NULL, StateProvinceCode = NULL, PostalCode = NULL, AddressID = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAddressV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAddressV1
	#'
	#' This function modifies an ElectronicTranscriptAddressV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAddressV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAddressV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAddressV1 <- function(ElectronicTranscriptAddressV1ID, AddressLine1 = NULL, AddressLine2 = NULL, AddressLine3 = NULL, City = NULL, StateProvinceCode = NULL, PostalCode = NULL, AddressID = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAddressV1", objectId = ElectronicTranscriptAddressV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAddressV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptAgencyIdentifierV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptAgencyIdentifierV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAgencyIdentifierV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAgencyIdentifierV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAgencyIdentifierV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptAgencyIdentifierV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptAgencyIdentifierV1s <- function(searchConditionsList = NULL, ElectronicTranscriptAgencyIdentifierV1ID = F, AgencyAssignedID = F, Agency = F, AgencyName = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptAgencyIdentifierV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptAgencyIdentifierV1
	#' @param ElectronicTranscriptAgencyIdentifierV1ID The ID of the ElectronicTranscriptAgencyIdentifierV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptAgencyIdentifierV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptAgencyIdentifierV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptAgencyIdentifierV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptAgencyIdentifierV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptAgencyIdentifierV1 <- function(ElectronicTranscriptAgencyIdentifierV1ID, AgencyAssignedID = F, Agency = F, AgencyName = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptAgencyIdentifierV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierV1", objectId = ElectronicTranscriptAgencyIdentifierV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptAgencyIdentifierV1
	#'
	#' This function deletes an ElectronicTranscriptAgencyIdentifierV1
	#' @param ElectronicTranscriptAgencyIdentifierV1ID The ID of the ElectronicTranscriptAgencyIdentifierV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptAgencyIdentifierV1ID of the deleted ElectronicTranscriptAgencyIdentifierV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptAgencyIdentifierV1 <- function(ElectronicTranscriptAgencyIdentifierV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierV1", objectId = ElectronicTranscriptAgencyIdentifierV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptAgencyIdentifierV1
	#'
	#' This function creates an ElectronicTranscriptAgencyIdentifierV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptAgencyIdentifierV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptAgencyIdentifierV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptAgencyIdentifierV1 <- function(AgencyAssignedID = NULL, Agency = NULL, AgencyName = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptAgencyIdentifierV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptAgencyIdentifierV1
	#'
	#' This function modifies an ElectronicTranscriptAgencyIdentifierV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptAgencyIdentifierV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptAgencyIdentifierV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptAgencyIdentifierV1 <- function(ElectronicTranscriptAgencyIdentifierV1ID, AgencyAssignedID = NULL, Agency = NULL, AgencyName = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptAgencyIdentifierV1", objectId = ElectronicTranscriptAgencyIdentifierV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptAgencyIdentifierV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptPersonExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptPersonExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPersonExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPersonExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPersonException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptPersonExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptPersonExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptPersonExceptionID = F, ElectronicTranscriptPersonV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptPersonException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptPersonException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptPersonException
	#' @param ElectronicTranscriptPersonExceptionID The ID of the ElectronicTranscriptPersonException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPersonException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPersonException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPersonException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptPersonException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptPersonException <- function(ElectronicTranscriptPersonExceptionID, ElectronicTranscriptPersonV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptPersonExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonException", objectId = ElectronicTranscriptPersonExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptPersonException
	#'
	#' This function deletes an ElectronicTranscriptPersonException
	#' @param ElectronicTranscriptPersonExceptionID The ID of the ElectronicTranscriptPersonException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptPersonExceptionID of the deleted ElectronicTranscriptPersonException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptPersonException <- function(ElectronicTranscriptPersonExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonException", objectId = ElectronicTranscriptPersonExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptPersonException
	#'
	#' This function creates an ElectronicTranscriptPersonException
	#' @param fieldNames The field values to give the created ElectronicTranscriptPersonException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptPersonException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptPersonException <- function(ElectronicTranscriptPersonV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptPersonExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptPersonException
	#'
	#' This function modifies an ElectronicTranscriptPersonException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptPersonException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptPersonException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptPersonException <- function(ElectronicTranscriptPersonExceptionID, ElectronicTranscriptPersonV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonException", objectId = ElectronicTranscriptPersonExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptPersonExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptRunHistories
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptRunHistories <- function(searchConditionsList = NULL, ElectronicTranscriptRunHistoryID = F, DistrictID = F, SchoolYearID = F, EntityIDExportedFor = F, HasAnEntityExport = F, StartDateTime = F, EndDateTime = F, Type = F, MediaID = F, ScopeAccessAllowed = F, HasValidMedia = F, CanAccessMedia = F, IsExport = F, IsLocked = F, Status = F, RunData = F, UserIDCanceledBy = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, RunParameters = F, StudentFilterDisplay = F, APISuccessCount = F, APIErrorCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptRunHistory
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptRunHistory
	#' @param ElectronicTranscriptRunHistoryID The ID of the ElectronicTranscriptRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptRunHistory <- function(ElectronicTranscriptRunHistoryID, DistrictID = F, SchoolYearID = F, EntityIDExportedFor = F, HasAnEntityExport = F, StartDateTime = F, EndDateTime = F, Type = F, MediaID = F, ScopeAccessAllowed = F, HasValidMedia = F, CanAccessMedia = F, IsExport = F, IsLocked = F, Status = F, RunData = F, UserIDCanceledBy = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, RunParameters = F, StudentFilterDisplay = F, APISuccessCount = F, APIErrorCount = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptRunHistory", objectId = ElectronicTranscriptRunHistoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptRunHistory
	#'
	#' This function deletes an ElectronicTranscriptRunHistory
	#' @param ElectronicTranscriptRunHistoryID The ID of the ElectronicTranscriptRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptRunHistoryID of the deleted ElectronicTranscriptRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptRunHistory <- function(ElectronicTranscriptRunHistoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptRunHistory", objectId = ElectronicTranscriptRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptRunHistory
	#'
	#' This function creates an ElectronicTranscriptRunHistory
	#' @param fieldNames The field values to give the created ElectronicTranscriptRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptRunHistory <- function(DistrictID = NULL, SchoolYearID = NULL, EntityIDExportedFor = NULL, StartDateTime = NULL, EndDateTime = NULL, Type = NULL, MediaID = NULL, Status = NULL, UserIDCanceledBy = NULL, StudentFilterDisplay = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptRunHistory", body = list(DataObject = body), searchFields = append("ElectronicTranscriptRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptRunHistory
	#'
	#' This function modifies an ElectronicTranscriptRunHistory
	#' @param fieldNames The field values to give the modified ElectronicTranscriptRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptRunHistory <- function(ElectronicTranscriptRunHistoryID, DistrictID = NULL, SchoolYearID = NULL, EntityIDExportedFor = NULL, StartDateTime = NULL, EndDateTime = NULL, Type = NULL, MediaID = NULL, Status = NULL, UserIDCanceledBy = NULL, StudentFilterDisplay = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptRunHistory", objectId = ElectronicTranscriptRunHistoryID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptPersonV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptPersonV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPersonV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPersonV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPersonV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptPersonV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptPersonV1s <- function(searchConditionsList = NULL, ElectronicTranscriptPersonV1ID = F, SchoolAssignedPersonID = F, BirthDate = F, FirstName = F, MiddleName = F, LastName = F, NameSuffix = F, GuardianFirstName = F, GuardianLastName = F, Gender = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, NameIDGuardian = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptPersonV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptPersonV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptPersonV1
	#' @param ElectronicTranscriptPersonV1ID The ID of the ElectronicTranscriptPersonV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptPersonV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptPersonV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptPersonV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptPersonV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptPersonV1 <- function(ElectronicTranscriptPersonV1ID, SchoolAssignedPersonID = F, BirthDate = F, FirstName = F, MiddleName = F, LastName = F, NameSuffix = F, GuardianFirstName = F, GuardianLastName = F, Gender = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, NameIDGuardian = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptPersonV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonV1", objectId = ElectronicTranscriptPersonV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptPersonV1
	#'
	#' This function deletes an ElectronicTranscriptPersonV1
	#' @param ElectronicTranscriptPersonV1ID The ID of the ElectronicTranscriptPersonV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptPersonV1ID of the deleted ElectronicTranscriptPersonV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptPersonV1 <- function(ElectronicTranscriptPersonV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonV1", objectId = ElectronicTranscriptPersonV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptPersonV1
	#'
	#' This function creates an ElectronicTranscriptPersonV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptPersonV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptPersonV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptPersonV1 <- function(SchoolAssignedPersonID = NULL, BirthDate = NULL, FirstName = NULL, MiddleName = NULL, LastName = NULL, NameSuffix = NULL, GuardianFirstName = NULL, GuardianLastName = NULL, Gender = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, NameIDGuardian = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptPersonV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptPersonV1
	#'
	#' This function modifies an ElectronicTranscriptPersonV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptPersonV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptPersonV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptPersonV1 <- function(ElectronicTranscriptPersonV1ID, SchoolAssignedPersonID = NULL, BirthDate = NULL, FirstName = NULL, MiddleName = NULL, LastName = NULL, NameSuffix = NULL, GuardianFirstName = NULL, GuardianLastName = NULL, Gender = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, NameIDGuardian = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptPersonV1", objectId = ElectronicTranscriptPersonV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptPersonV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptLicensureNoteExceptions
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptLicensureNoteExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureNoteExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureNoteExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureNoteException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptLicensureNoteExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptLicensureNoteExceptions <- function(searchConditionsList = NULL, ElectronicTranscriptLicensureNoteExceptionID = F, ElectronicTranscriptLicensureNoteV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptLicensureNoteException
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptLicensureNoteException
	#' @param ElectronicTranscriptLicensureNoteExceptionID The ID of the ElectronicTranscriptLicensureNoteException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureNoteException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureNoteException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureNoteException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptLicensureNoteException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptLicensureNoteException <- function(ElectronicTranscriptLicensureNoteExceptionID, ElectronicTranscriptLicensureNoteV1ID = F, ElectronicTranscriptRunHistoryID = F, MessageType = F, RuleNumber = F, Message = F, EntityID = F, SchoolYearID = F, StudentID = F, ProcessType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptLicensureNoteExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteException", objectId = ElectronicTranscriptLicensureNoteExceptionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptLicensureNoteException
	#'
	#' This function deletes an ElectronicTranscriptLicensureNoteException
	#' @param ElectronicTranscriptLicensureNoteExceptionID The ID of the ElectronicTranscriptLicensureNoteException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptLicensureNoteExceptionID of the deleted ElectronicTranscriptLicensureNoteException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptLicensureNoteException <- function(ElectronicTranscriptLicensureNoteExceptionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteException", objectId = ElectronicTranscriptLicensureNoteExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptLicensureNoteException
	#'
	#' This function creates an ElectronicTranscriptLicensureNoteException
	#' @param fieldNames The field values to give the created ElectronicTranscriptLicensureNoteException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptLicensureNoteException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptLicensureNoteException <- function(ElectronicTranscriptLicensureNoteV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteException", body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureNoteExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptLicensureNoteException
	#'
	#' This function modifies an ElectronicTranscriptLicensureNoteException
	#' @param fieldNames The field values to give the modified ElectronicTranscriptLicensureNoteException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptLicensureNoteException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptLicensureNoteException <- function(ElectronicTranscriptLicensureNoteExceptionID, ElectronicTranscriptLicensureNoteV1ID = NULL, ElectronicTranscriptRunHistoryID = NULL, MessageType = NULL, RuleNumber = NULL, Message = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteException", objectId = ElectronicTranscriptLicensureNoteExceptionID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureNoteExceptionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ElectronicTranscriptLicensureNoteV1s
	#'
	#' This function returns a dataframe or json object of ElectronicTranscriptLicensureNoteV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureNoteV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureNoteV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureNoteV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ElectronicTranscriptLicensureNoteV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listElectronicTranscriptLicensureNoteV1s <- function(searchConditionsList = NULL, ElectronicTranscriptLicensureNoteV1ID = F, LicensureName = F, LicensurePassageDate = F, NoteMessage = F, OrganizationName = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ElectronicTranscriptLicensureNoteV1
	#'
	#' This function returns a dataframe or json object of an ElectronicTranscriptLicensureNoteV1
	#' @param ElectronicTranscriptLicensureNoteV1ID The ID of the ElectronicTranscriptLicensureNoteV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ElectronicTranscriptLicensureNoteV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ElectronicTranscriptLicensureNoteV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ElectronicTranscriptLicensureNoteV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ElectronicTranscriptLicensureNoteV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getElectronicTranscriptLicensureNoteV1 <- function(ElectronicTranscriptLicensureNoteV1ID, LicensureName = F, LicensurePassageDate = F, NoteMessage = F, OrganizationName = F, ElectronicTranscriptRunHistoryID = F, EntityID = F, SchoolYearID = F, StudentID = F, HasErrors = F, KeyHash = F, UpdateHash = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ElectronicTranscriptLicensureNoteV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteV1", objectId = ElectronicTranscriptLicensureNoteV1ID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ElectronicTranscriptLicensureNoteV1
	#'
	#' This function deletes an ElectronicTranscriptLicensureNoteV1
	#' @param ElectronicTranscriptLicensureNoteV1ID The ID of the ElectronicTranscriptLicensureNoteV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ElectronicTranscriptLicensureNoteV1ID of the deleted ElectronicTranscriptLicensureNoteV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteElectronicTranscriptLicensureNoteV1 <- function(ElectronicTranscriptLicensureNoteV1ID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteV1", objectId = ElectronicTranscriptLicensureNoteV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ElectronicTranscriptLicensureNoteV1
	#'
	#' This function creates an ElectronicTranscriptLicensureNoteV1
	#' @param fieldNames The field values to give the created ElectronicTranscriptLicensureNoteV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ElectronicTranscriptLicensureNoteV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createElectronicTranscriptLicensureNoteV1 <- function(LicensureName = NULL, LicensurePassageDate = NULL, NoteMessage = NULL, OrganizationName = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteV1", body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureNoteV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ElectronicTranscriptLicensureNoteV1
	#'
	#' This function modifies an ElectronicTranscriptLicensureNoteV1
	#' @param fieldNames The field values to give the modified ElectronicTranscriptLicensureNoteV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ElectronicTranscriptLicensureNoteV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyElectronicTranscriptLicensureNoteV1 <- function(ElectronicTranscriptLicensureNoteV1ID, LicensureName = NULL, LicensurePassageDate = NULL, NoteMessage = NULL, OrganizationName = NULL, ElectronicTranscriptRunHistoryID = NULL, EntityID = NULL, SchoolYearID = NULL, StudentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ElectronicTranscriptLicensureNoteV1", objectId = ElectronicTranscriptLicensureNoteV1ID, body = list(DataObject = body), searchFields = append("ElectronicTranscriptLicensureNoteV1ID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DefaultGradingScaleGroupGradeMarks
	#'
	#' This function returns a dataframe or json object of DefaultGradingScaleGroupGradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DefaultGradingScaleGroupGradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DefaultGradingScaleGroupGradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DefaultGradingScaleGroupGradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of DefaultGradingScaleGroupGradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDefaultGradingScaleGroupGradeMarks <- function(searchConditionsList = NULL, DefaultGradingScaleGroupGradeMarkID = F, GradeMarkID = F, PercentLow = F, PercentHigh = F, DefaultCalculationPercent = F, ConfigEntityGroupYearID = F, DefaultGradingScaleGroupGradeMarkIDClonedFrom = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "DefaultGradingScaleGroupGradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DefaultGradingScaleGroupGradeMark
	#'
	#' This function returns a dataframe or json object of a DefaultGradingScaleGroupGradeMark
	#' @param DefaultGradingScaleGroupGradeMarkID The ID of the DefaultGradingScaleGroupGradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DefaultGradingScaleGroupGradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DefaultGradingScaleGroupGradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DefaultGradingScaleGroupGradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of DefaultGradingScaleGroupGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDefaultGradingScaleGroupGradeMark <- function(DefaultGradingScaleGroupGradeMarkID, GradeMarkID = F, PercentLow = F, PercentHigh = F, DefaultCalculationPercent = F, ConfigEntityGroupYearID = F, DefaultGradingScaleGroupGradeMarkIDClonedFrom = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DefaultGradingScaleGroupGradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "DefaultGradingScaleGroupGradeMark", objectId = DefaultGradingScaleGroupGradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DefaultGradingScaleGroupGradeMark
	#'
	#' This function deletes a DefaultGradingScaleGroupGradeMark
	#' @param DefaultGradingScaleGroupGradeMarkID The ID of the DefaultGradingScaleGroupGradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The DefaultGradingScaleGroupGradeMarkID of the deleted DefaultGradingScaleGroupGradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDefaultGradingScaleGroupGradeMark <- function(DefaultGradingScaleGroupGradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "DefaultGradingScaleGroupGradeMark", objectId = DefaultGradingScaleGroupGradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DefaultGradingScaleGroupGradeMark
	#'
	#' This function creates a DefaultGradingScaleGroupGradeMark
	#' @param fieldNames The field values to give the created DefaultGradingScaleGroupGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created DefaultGradingScaleGroupGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDefaultGradingScaleGroupGradeMark <- function(GradeMarkID = NULL, PercentLow = NULL, PercentHigh = NULL, DefaultCalculationPercent = NULL, ConfigEntityGroupYearID = NULL, DefaultGradingScaleGroupGradeMarkIDClonedFrom = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "DefaultGradingScaleGroupGradeMark", body = list(DataObject = body), searchFields = append("DefaultGradingScaleGroupGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DefaultGradingScaleGroupGradeMark
	#'
	#' This function modifies a DefaultGradingScaleGroupGradeMark
	#' @param fieldNames The field values to give the modified DefaultGradingScaleGroupGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified DefaultGradingScaleGroupGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDefaultGradingScaleGroupGradeMark <- function(DefaultGradingScaleGroupGradeMarkID, GradeMarkID = NULL, PercentLow = NULL, PercentHigh = NULL, DefaultCalculationPercent = NULL, ConfigEntityGroupYearID = NULL, DefaultGradingScaleGroupGradeMarkIDClonedFrom = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "DefaultGradingScaleGroupGradeMark", objectId = DefaultGradingScaleGroupGradeMarkID, body = list(DataObject = body), searchFields = append("DefaultGradingScaleGroupGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeBucketFlagDetailGPAMethodErrors
	#'
	#' This function returns a dataframe or json object of TempGradeBucketFlagDetailGPAMethodErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeBucketFlagDetailGPAMethodErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeBucketFlagDetailGPAMethodErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeBucketFlagDetailGPAMethodError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of TempGradeBucketFlagDetailGPAMethodErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeBucketFlagDetailGPAMethodErrors <- function(searchConditionsList = NULL, TempGradeBucketFlagDetailGPAMethodErrorID = F, GradeBucketFlagDetailGPAMethod = F, ErrorString = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethodError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeBucketFlagDetailGPAMethodError
	#'
	#' This function returns a dataframe or json object of a TempGradeBucketFlagDetailGPAMethodError
	#' @param TempGradeBucketFlagDetailGPAMethodErrorID The ID of the TempGradeBucketFlagDetailGPAMethodError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeBucketFlagDetailGPAMethodError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeBucketFlagDetailGPAMethodError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeBucketFlagDetailGPAMethodError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of TempGradeBucketFlagDetailGPAMethodError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeBucketFlagDetailGPAMethodError <- function(TempGradeBucketFlagDetailGPAMethodErrorID, GradeBucketFlagDetailGPAMethod = F, ErrorString = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeBucketFlagDetailGPAMethodErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethodError", objectId = TempGradeBucketFlagDetailGPAMethodErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeBucketFlagDetailGPAMethodError
	#'
	#' This function deletes a TempGradeBucketFlagDetailGPAMethodError
	#' @param TempGradeBucketFlagDetailGPAMethodErrorID The ID of the TempGradeBucketFlagDetailGPAMethodError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The TempGradeBucketFlagDetailGPAMethodErrorID of the deleted TempGradeBucketFlagDetailGPAMethodError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeBucketFlagDetailGPAMethodError <- function(TempGradeBucketFlagDetailGPAMethodErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethodError", objectId = TempGradeBucketFlagDetailGPAMethodErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeBucketFlagDetailGPAMethodError
	#'
	#' This function creates a TempGradeBucketFlagDetailGPAMethodError
	#' @param fieldNames The field values to give the created TempGradeBucketFlagDetailGPAMethodError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created TempGradeBucketFlagDetailGPAMethodError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeBucketFlagDetailGPAMethodError <- function(GradeBucketFlagDetailGPAMethod = NULL, ErrorString = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethodError", body = list(DataObject = body), searchFields = append("TempGradeBucketFlagDetailGPAMethodErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeBucketFlagDetailGPAMethodError
	#'
	#' This function modifies a TempGradeBucketFlagDetailGPAMethodError
	#' @param fieldNames The field values to give the modified TempGradeBucketFlagDetailGPAMethodError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified TempGradeBucketFlagDetailGPAMethodError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeBucketFlagDetailGPAMethodError <- function(TempGradeBucketFlagDetailGPAMethodErrorID, GradeBucketFlagDetailGPAMethod = NULL, ErrorString = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "TempGradeBucketFlagDetailGPAMethodError", objectId = TempGradeBucketFlagDetailGPAMethodErrorID, body = list(DataObject = body), searchFields = append("TempGradeBucketFlagDetailGPAMethodErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentHighlightINS
	#'
	#' This function returns a dataframe or json object of GradeReportStudentHighlightINS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentHighlightINS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentHighlightINS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentHighlightIN') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentHighlightINS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentHighlightINS <- function(searchConditionsList = NULL, GradeReportStudentHighlightINID = F, GradeReportStudentID = F, HighlightType = F, Title = F, SchoolYear = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentHighlightIN", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentHighlightIN
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentHighlightIN
	#' @param GradeReportStudentHighlightINID The ID of the GradeReportStudentHighlightIN to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentHighlightIN. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentHighlightIN.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentHighlightIN') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentHighlightIN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentHighlightIN <- function(GradeReportStudentHighlightINID, GradeReportStudentID = F, HighlightType = F, Title = F, SchoolYear = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentHighlightINID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentHighlightIN", objectId = GradeReportStudentHighlightINID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentHighlightIN
	#'
	#' This function deletes a GradeReportStudentHighlightIN
	#' @param GradeReportStudentHighlightINID The ID of the GradeReportStudentHighlightIN to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentHighlightINID of the deleted GradeReportStudentHighlightIN.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentHighlightIN <- function(GradeReportStudentHighlightINID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentHighlightIN", objectId = GradeReportStudentHighlightINID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentHighlightIN
	#'
	#' This function creates a GradeReportStudentHighlightIN
	#' @param fieldNames The field values to give the created GradeReportStudentHighlightIN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentHighlightIN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentHighlightIN <- function(GradeReportStudentID = NULL, HighlightType = NULL, Title = NULL, SchoolYear = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentHighlightIN", body = list(DataObject = body), searchFields = append("GradeReportStudentHighlightINID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentHighlightIN
	#'
	#' This function modifies a GradeReportStudentHighlightIN
	#' @param fieldNames The field values to give the modified GradeReportStudentHighlightIN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentHighlightIN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentHighlightIN <- function(GradeReportStudentHighlightINID, GradeReportStudentID = NULL, HighlightType = NULL, Title = NULL, SchoolYear = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentHighlightIN", objectId = GradeReportStudentHighlightINID, body = list(DataObject = body), searchFields = append("GradeReportStudentHighlightINID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ConfigEntityYearGraduationRequiredCredits
	#'
	#' This function returns a dataframe or json object of ConfigEntityYearGraduationRequiredCredits
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ConfigEntityYearGraduationRequiredCredits. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ConfigEntityYearGraduationRequiredCredits.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ConfigEntityYearGraduationRequiredCredit') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of ConfigEntityYearGraduationRequiredCredits
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listConfigEntityYearGraduationRequiredCredits <- function(searchConditionsList = NULL, ConfigEntityYearGraduationRequiredCreditID = F, ConfigEntityYearID = F, GradYearLow = F, GradYearHigh = F, CreditsRequired = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "ConfigEntityYearGraduationRequiredCredit", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ConfigEntityYearGraduationRequiredCredit
	#'
	#' This function returns a dataframe or json object of a ConfigEntityYearGraduationRequiredCredit
	#' @param ConfigEntityYearGraduationRequiredCreditID The ID of the ConfigEntityYearGraduationRequiredCredit to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ConfigEntityYearGraduationRequiredCredit. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ConfigEntityYearGraduationRequiredCredit.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ConfigEntityYearGraduationRequiredCredit') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of ConfigEntityYearGraduationRequiredCredit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getConfigEntityYearGraduationRequiredCredit <- function(ConfigEntityYearGraduationRequiredCreditID, ConfigEntityYearID = F, GradYearLow = F, GradYearHigh = F, CreditsRequired = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ConfigEntityYearGraduationRequiredCreditID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "ConfigEntityYearGraduationRequiredCredit", objectId = ConfigEntityYearGraduationRequiredCreditID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ConfigEntityYearGraduationRequiredCredit
	#'
	#' This function deletes a ConfigEntityYearGraduationRequiredCredit
	#' @param ConfigEntityYearGraduationRequiredCreditID The ID of the ConfigEntityYearGraduationRequiredCredit to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The ConfigEntityYearGraduationRequiredCreditID of the deleted ConfigEntityYearGraduationRequiredCredit.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteConfigEntityYearGraduationRequiredCredit <- function(ConfigEntityYearGraduationRequiredCreditID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "ConfigEntityYearGraduationRequiredCredit", objectId = ConfigEntityYearGraduationRequiredCreditID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ConfigEntityYearGraduationRequiredCredit
	#'
	#' This function creates a ConfigEntityYearGraduationRequiredCredit
	#' @param fieldNames The field values to give the created ConfigEntityYearGraduationRequiredCredit. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created ConfigEntityYearGraduationRequiredCredit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createConfigEntityYearGraduationRequiredCredit <- function(ConfigEntityYearID = NULL, GradYearLow = NULL, GradYearHigh = NULL, CreditsRequired = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "ConfigEntityYearGraduationRequiredCredit", body = list(DataObject = body), searchFields = append("ConfigEntityYearGraduationRequiredCreditID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ConfigEntityYearGraduationRequiredCredit
	#'
	#' This function modifies a ConfigEntityYearGraduationRequiredCredit
	#' @param fieldNames The field values to give the modified ConfigEntityYearGraduationRequiredCredit. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified ConfigEntityYearGraduationRequiredCredit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyConfigEntityYearGraduationRequiredCredit <- function(ConfigEntityYearGraduationRequiredCreditID, ConfigEntityYearID = NULL, GradYearLow = NULL, GradYearHigh = NULL, CreditsRequired = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "ConfigEntityYearGraduationRequiredCredit", objectId = ConfigEntityYearGraduationRequiredCreditID, body = list(DataObject = body), searchFields = append("ConfigEntityYearGraduationRequiredCreditID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentVaccinations
	#'
	#' This function returns a dataframe or json object of GradeReportStudentVaccinations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentVaccinations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentVaccinations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentVaccination') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentVaccinations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentVaccinations <- function(searchConditionsList = NULL, GradeReportStudentVaccinationID = F, GradeReportStudentID = F, VaccinationCodeDescription = F, WaiverCodeDescription = F, ComplianceScheduleCode = F, DoseDates = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentVaccination", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentVaccination
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentVaccination
	#' @param GradeReportStudentVaccinationID The ID of the GradeReportStudentVaccination to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentVaccination. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentVaccination.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentVaccination') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentVaccination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentVaccination <- function(GradeReportStudentVaccinationID, GradeReportStudentID = F, VaccinationCodeDescription = F, WaiverCodeDescription = F, ComplianceScheduleCode = F, DoseDates = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentVaccinationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentVaccination", objectId = GradeReportStudentVaccinationID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentVaccination
	#'
	#' This function deletes a GradeReportStudentVaccination
	#' @param GradeReportStudentVaccinationID The ID of the GradeReportStudentVaccination to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentVaccinationID of the deleted GradeReportStudentVaccination.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentVaccination <- function(GradeReportStudentVaccinationID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentVaccination", objectId = GradeReportStudentVaccinationID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentVaccination
	#'
	#' This function creates a GradeReportStudentVaccination
	#' @param fieldNames The field values to give the created GradeReportStudentVaccination. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentVaccination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentVaccination <- function(GradeReportStudentID = NULL, VaccinationCodeDescription = NULL, WaiverCodeDescription = NULL, ComplianceScheduleCode = NULL, DoseDates = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentVaccination", body = list(DataObject = body), searchFields = append("GradeReportStudentVaccinationID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentVaccination
	#'
	#' This function modifies a GradeReportStudentVaccination
	#' @param fieldNames The field values to give the modified GradeReportStudentVaccination. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentVaccination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentVaccination <- function(GradeReportStudentVaccinationID, GradeReportStudentID = NULL, VaccinationCodeDescription = NULL, WaiverCodeDescription = NULL, ComplianceScheduleCode = NULL, DoseDates = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentVaccination", objectId = GradeReportStudentVaccinationID, body = list(DataObject = body), searchFields = append("GradeReportStudentVaccinationID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportTemplateVaccinations
	#'
	#' This function returns a dataframe or json object of GradeReportTemplateVaccinations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateVaccinations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateVaccinations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateVaccination') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportTemplateVaccinations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportTemplateVaccinations <- function(searchConditionsList = NULL, GradeReportTemplateVaccinationID = F, GradeReportTemplateID = F, VaccinationID = F, GradeReportTemplateVaccinationIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportTemplateVaccination", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportTemplateVaccination
	#'
	#' This function returns a dataframe or json object of a GradeReportTemplateVaccination
	#' @param GradeReportTemplateVaccinationID The ID of the GradeReportTemplateVaccination to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportTemplateVaccination. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportTemplateVaccination.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportTemplateVaccination') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportTemplateVaccination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportTemplateVaccination <- function(GradeReportTemplateVaccinationID, GradeReportTemplateID = F, VaccinationID = F, GradeReportTemplateVaccinationIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportTemplateVaccinationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportTemplateVaccination", objectId = GradeReportTemplateVaccinationID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportTemplateVaccination
	#'
	#' This function deletes a GradeReportTemplateVaccination
	#' @param GradeReportTemplateVaccinationID The ID of the GradeReportTemplateVaccination to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportTemplateVaccinationID of the deleted GradeReportTemplateVaccination.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportTemplateVaccination <- function(GradeReportTemplateVaccinationID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportTemplateVaccination", objectId = GradeReportTemplateVaccinationID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportTemplateVaccination
	#'
	#' This function creates a GradeReportTemplateVaccination
	#' @param fieldNames The field values to give the created GradeReportTemplateVaccination. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportTemplateVaccination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportTemplateVaccination <- function(GradeReportTemplateID = NULL, VaccinationID = NULL, GradeReportTemplateVaccinationIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportTemplateVaccination", body = list(DataObject = body), searchFields = append("GradeReportTemplateVaccinationID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportTemplateVaccination
	#'
	#' This function modifies a GradeReportTemplateVaccination
	#' @param fieldNames The field values to give the modified GradeReportTemplateVaccination. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportTemplateVaccination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportTemplateVaccination <- function(GradeReportTemplateVaccinationID, GradeReportTemplateID = NULL, VaccinationID = NULL, GradeReportTemplateVaccinationIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportTemplateVaccination", objectId = GradeReportTemplateVaccinationID, body = list(DataObject = body), searchFields = append("GradeReportTemplateVaccinationID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentCTES
	#'
	#' This function returns a dataframe or json object of GradeReportStudentCTES
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentCTES. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentCTES.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentCTE') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentCTES
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentCTES <- function(searchConditionsList = NULL, GradeReportStudentCTEID = F, GradeReportStudentID = F, ProgramType = F, ProgramName = F, ProgramStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentCTE", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentCTE
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentCTE
	#' @param GradeReportStudentCTEID The ID of the GradeReportStudentCTE to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentCTE. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentCTE.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentCTE') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentCTE
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentCTE <- function(GradeReportStudentCTEID, GradeReportStudentID = F, ProgramType = F, ProgramName = F, ProgramStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentCTEID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentCTE", objectId = GradeReportStudentCTEID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentCTE
	#'
	#' This function deletes a GradeReportStudentCTE
	#' @param GradeReportStudentCTEID The ID of the GradeReportStudentCTE to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentCTEID of the deleted GradeReportStudentCTE.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentCTE <- function(GradeReportStudentCTEID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentCTE", objectId = GradeReportStudentCTEID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentCTE
	#'
	#' This function creates a GradeReportStudentCTE
	#' @param fieldNames The field values to give the created GradeReportStudentCTE. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentCTE
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentCTE <- function(GradeReportStudentID = NULL, ProgramType = NULL, ProgramName = NULL, ProgramStatus = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentCTE", body = list(DataObject = body), searchFields = append("GradeReportStudentCTEID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentCTE
	#'
	#' This function modifies a GradeReportStudentCTE
	#' @param fieldNames The field values to give the modified GradeReportStudentCTE. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentCTE
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentCTE <- function(GradeReportStudentCTEID, GradeReportStudentID = NULL, ProgramType = NULL, ProgramName = NULL, ProgramStatus = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentCTE", objectId = GradeReportStudentCTEID, body = list(DataObject = body), searchFields = append("GradeReportStudentCTEID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradeReportStudentTranscriptSealOfBiliteracyILS
	#'
	#' This function returns a dataframe or json object of GradeReportStudentTranscriptSealOfBiliteracyILS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentTranscriptSealOfBiliteracyILS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentTranscriptSealOfBiliteracyILS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentTranscriptSealOfBiliteracyIL') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of GradeReportStudentTranscriptSealOfBiliteracyILS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradeReportStudentTranscriptSealOfBiliteracyILS <- function(searchConditionsList = NULL, GradeReportStudentTranscriptSealOfBiliteracyILID = F, Description = F, GradeReportStudentID = F, SealOfBiliteracyILID = F, MediaIDTranscriptSealOfBiliteracyIL = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "GradeReportStudentTranscriptSealOfBiliteracyIL", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradeReportStudentTranscriptSealOfBiliteracyIL
	#'
	#' This function returns a dataframe or json object of a GradeReportStudentTranscriptSealOfBiliteracyIL
	#' @param GradeReportStudentTranscriptSealOfBiliteracyILID The ID of the GradeReportStudentTranscriptSealOfBiliteracyIL to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradeReportStudentTranscriptSealOfBiliteracyIL. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradeReportStudentTranscriptSealOfBiliteracyIL.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradeReportStudentTranscriptSealOfBiliteracyIL') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of GradeReportStudentTranscriptSealOfBiliteracyIL
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradeReportStudentTranscriptSealOfBiliteracyIL <- function(GradeReportStudentTranscriptSealOfBiliteracyILID, Description = F, GradeReportStudentID = F, SealOfBiliteracyILID = F, MediaIDTranscriptSealOfBiliteracyIL = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradeReportStudentTranscriptSealOfBiliteracyILID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "GradeReportStudentTranscriptSealOfBiliteracyIL", objectId = GradeReportStudentTranscriptSealOfBiliteracyILID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradeReportStudentTranscriptSealOfBiliteracyIL
	#'
	#' This function deletes a GradeReportStudentTranscriptSealOfBiliteracyIL
	#' @param GradeReportStudentTranscriptSealOfBiliteracyILID The ID of the GradeReportStudentTranscriptSealOfBiliteracyIL to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The GradeReportStudentTranscriptSealOfBiliteracyILID of the deleted GradeReportStudentTranscriptSealOfBiliteracyIL.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradeReportStudentTranscriptSealOfBiliteracyIL <- function(GradeReportStudentTranscriptSealOfBiliteracyILID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "GradeReportStudentTranscriptSealOfBiliteracyIL", objectId = GradeReportStudentTranscriptSealOfBiliteracyILID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradeReportStudentTranscriptSealOfBiliteracyIL
	#'
	#' This function creates a GradeReportStudentTranscriptSealOfBiliteracyIL
	#' @param fieldNames The field values to give the created GradeReportStudentTranscriptSealOfBiliteracyIL. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created GradeReportStudentTranscriptSealOfBiliteracyIL
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradeReportStudentTranscriptSealOfBiliteracyIL <- function(Description = NULL, GradeReportStudentID = NULL, SealOfBiliteracyILID = NULL, MediaIDTranscriptSealOfBiliteracyIL = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "GradeReportStudentTranscriptSealOfBiliteracyIL", body = list(DataObject = body), searchFields = append("GradeReportStudentTranscriptSealOfBiliteracyILID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradeReportStudentTranscriptSealOfBiliteracyIL
	#'
	#' This function modifies a GradeReportStudentTranscriptSealOfBiliteracyIL
	#' @param fieldNames The field values to give the modified GradeReportStudentTranscriptSealOfBiliteracyIL. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified GradeReportStudentTranscriptSealOfBiliteracyIL
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradeReportStudentTranscriptSealOfBiliteracyIL <- function(GradeReportStudentTranscriptSealOfBiliteracyILID, Description = NULL, GradeReportStudentID = NULL, SealOfBiliteracyILID = NULL, MediaIDTranscriptSealOfBiliteracyIL = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "GradeReportStudentTranscriptSealOfBiliteracyIL", objectId = GradeReportStudentTranscriptSealOfBiliteracyILID, body = list(DataObject = body), searchFields = append("GradeReportStudentTranscriptSealOfBiliteracyILID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List RepairGradingSetupRunHistories
	#'
	#' This function returns a dataframe or json object of RepairGradingSetupRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RepairGradingSetupRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RepairGradingSetupRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RepairGradingSetupRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of RepairGradingSetupRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listRepairGradingSetupRunHistories <- function(searchConditionsList = NULL, RepairGradingSetupRunHistoryID = F, Description = F, ErrorMessage = F, EntityID = F, SchoolYearID = F, StartTime = F, EndTime = F, Status = F, Duration = F, RecordsAdded = F, RecordsDeleted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "RepairGradingSetupRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a RepairGradingSetupRunHistory
	#'
	#' This function returns a dataframe or json object of a RepairGradingSetupRunHistory
	#' @param RepairGradingSetupRunHistoryID The ID of the RepairGradingSetupRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RepairGradingSetupRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RepairGradingSetupRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RepairGradingSetupRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of RepairGradingSetupRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getRepairGradingSetupRunHistory <- function(RepairGradingSetupRunHistoryID, Description = F, ErrorMessage = F, EntityID = F, SchoolYearID = F, StartTime = F, EndTime = F, Status = F, Duration = F, RecordsAdded = F, RecordsDeleted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "RepairGradingSetupRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistory", objectId = RepairGradingSetupRunHistoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a RepairGradingSetupRunHistory
	#'
	#' This function deletes a RepairGradingSetupRunHistory
	#' @param RepairGradingSetupRunHistoryID The ID of the RepairGradingSetupRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The RepairGradingSetupRunHistoryID of the deleted RepairGradingSetupRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteRepairGradingSetupRunHistory <- function(RepairGradingSetupRunHistoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistory", objectId = RepairGradingSetupRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a RepairGradingSetupRunHistory
	#'
	#' This function creates a RepairGradingSetupRunHistory
	#' @param fieldNames The field values to give the created RepairGradingSetupRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created RepairGradingSetupRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createRepairGradingSetupRunHistory <- function(Description = NULL, ErrorMessage = NULL, EntityID = NULL, SchoolYearID = NULL, StartTime = NULL, EndTime = NULL, Status = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistory", body = list(DataObject = body), searchFields = append("RepairGradingSetupRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a RepairGradingSetupRunHistory
	#'
	#' This function modifies a RepairGradingSetupRunHistory
	#' @param fieldNames The field values to give the modified RepairGradingSetupRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified RepairGradingSetupRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyRepairGradingSetupRunHistory <- function(RepairGradingSetupRunHistoryID, Description = NULL, ErrorMessage = NULL, EntityID = NULL, SchoolYearID = NULL, StartTime = NULL, EndTime = NULL, Status = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistory", objectId = RepairGradingSetupRunHistoryID, body = list(DataObject = body), searchFields = append("RepairGradingSetupRunHistoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List RepairGradingSetupRunHistoryProcesses
	#'
	#' This function returns a dataframe or json object of RepairGradingSetupRunHistoryProcesses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RepairGradingSetupRunHistoryProcesses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RepairGradingSetupRunHistoryProcesses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RepairGradingSetupRunHistoryProcess') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of RepairGradingSetupRunHistoryProcesses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listRepairGradingSetupRunHistoryProcesses <- function(searchConditionsList = NULL, RepairGradingSetupRunHistoryProcessID = F, RepairGradingSetupRunHistoryID = F, StartTime = F, EndTime = F, Status = F, ProcessType = F, Duration = F, RecordsAdded = F, RecordsDeleted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcess", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a RepairGradingSetupRunHistoryProcess
	#'
	#' This function returns a dataframe or json object of a RepairGradingSetupRunHistoryProcess
	#' @param RepairGradingSetupRunHistoryProcessID The ID of the RepairGradingSetupRunHistoryProcess to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RepairGradingSetupRunHistoryProcess. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RepairGradingSetupRunHistoryProcess.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RepairGradingSetupRunHistoryProcess') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of RepairGradingSetupRunHistoryProcess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getRepairGradingSetupRunHistoryProcess <- function(RepairGradingSetupRunHistoryProcessID, RepairGradingSetupRunHistoryID = F, StartTime = F, EndTime = F, Status = F, ProcessType = F, Duration = F, RecordsAdded = F, RecordsDeleted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "RepairGradingSetupRunHistoryProcessID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcess", objectId = RepairGradingSetupRunHistoryProcessID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a RepairGradingSetupRunHistoryProcess
	#'
	#' This function deletes a RepairGradingSetupRunHistoryProcess
	#' @param RepairGradingSetupRunHistoryProcessID The ID of the RepairGradingSetupRunHistoryProcess to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The RepairGradingSetupRunHistoryProcessID of the deleted RepairGradingSetupRunHistoryProcess.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteRepairGradingSetupRunHistoryProcess <- function(RepairGradingSetupRunHistoryProcessID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcess", objectId = RepairGradingSetupRunHistoryProcessID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a RepairGradingSetupRunHistoryProcess
	#'
	#' This function creates a RepairGradingSetupRunHistoryProcess
	#' @param fieldNames The field values to give the created RepairGradingSetupRunHistoryProcess. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created RepairGradingSetupRunHistoryProcess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createRepairGradingSetupRunHistoryProcess <- function(RepairGradingSetupRunHistoryID = NULL, StartTime = NULL, EndTime = NULL, Status = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcess", body = list(DataObject = body), searchFields = append("RepairGradingSetupRunHistoryProcessID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a RepairGradingSetupRunHistoryProcess
	#'
	#' This function modifies a RepairGradingSetupRunHistoryProcess
	#' @param fieldNames The field values to give the modified RepairGradingSetupRunHistoryProcess. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified RepairGradingSetupRunHistoryProcess
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyRepairGradingSetupRunHistoryProcess <- function(RepairGradingSetupRunHistoryProcessID, RepairGradingSetupRunHistoryID = NULL, StartTime = NULL, EndTime = NULL, Status = NULL, ProcessType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcess", objectId = RepairGradingSetupRunHistoryProcessID, body = list(DataObject = body), searchFields = append("RepairGradingSetupRunHistoryProcessID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List RepairGradingSetupRunHistoryProcessDetails
	#'
	#' This function returns a dataframe or json object of RepairGradingSetupRunHistoryProcessDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RepairGradingSetupRunHistoryProcessDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RepairGradingSetupRunHistoryProcessDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RepairGradingSetupRunHistoryProcessDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A list of RepairGradingSetupRunHistoryProcessDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listRepairGradingSetupRunHistoryProcessDetails <- function(searchConditionsList = NULL, RepairGradingSetupRunHistoryProcessDetailID = F, RepairGradingSetupRunHistoryProcessID = F, DataObjectName = F, RecordIdentifier = F, ActionOrder = F, ErrorMessage = F, Status = F, Action = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcessDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a RepairGradingSetupRunHistoryProcessDetail
	#'
	#' This function returns a dataframe or json object of a RepairGradingSetupRunHistoryProcessDetail
	#' @param RepairGradingSetupRunHistoryProcessDetailID The ID of the RepairGradingSetupRunHistoryProcessDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given RepairGradingSetupRunHistoryProcessDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the RepairGradingSetupRunHistoryProcessDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('RepairGradingSetupRunHistoryProcessDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A dataframe or of RepairGradingSetupRunHistoryProcessDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getRepairGradingSetupRunHistoryProcessDetail <- function(RepairGradingSetupRunHistoryProcessDetailID, RepairGradingSetupRunHistoryProcessID = F, DataObjectName = F, RecordIdentifier = F, ActionOrder = F, ErrorMessage = F, Status = F, Action = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "RepairGradingSetupRunHistoryProcessDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcessDetail", objectId = RepairGradingSetupRunHistoryProcessDetailID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a RepairGradingSetupRunHistoryProcessDetail
	#'
	#' This function deletes a RepairGradingSetupRunHistoryProcessDetail
	#' @param RepairGradingSetupRunHistoryProcessDetailID The ID of the RepairGradingSetupRunHistoryProcessDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The RepairGradingSetupRunHistoryProcessDetailID of the deleted RepairGradingSetupRunHistoryProcessDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteRepairGradingSetupRunHistoryProcessDetail <- function(RepairGradingSetupRunHistoryProcessDetailID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcessDetail", objectId = RepairGradingSetupRunHistoryProcessDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a RepairGradingSetupRunHistoryProcessDetail
	#'
	#' This function creates a RepairGradingSetupRunHistoryProcessDetail
	#' @param fieldNames The field values to give the created RepairGradingSetupRunHistoryProcessDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return A newly created RepairGradingSetupRunHistoryProcessDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createRepairGradingSetupRunHistoryProcessDetail <- function(RepairGradingSetupRunHistoryProcessID = NULL, DataObjectName = NULL, RecordIdentifier = NULL, ActionOrder = NULL, ErrorMessage = NULL, Status = NULL, Action = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcessDetail", body = list(DataObject = body), searchFields = append("RepairGradingSetupRunHistoryProcessDetailID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a RepairGradingSetupRunHistoryProcessDetail
	#'
	#' This function modifies a RepairGradingSetupRunHistoryProcessDetail
	#' @param fieldNames The field values to give the modified RepairGradingSetupRunHistoryProcessDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Grading
	#' @return The modified RepairGradingSetupRunHistoryProcessDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyRepairGradingSetupRunHistoryProcessDetail <- function(RepairGradingSetupRunHistoryProcessDetailID, RepairGradingSetupRunHistoryProcessID = NULL, DataObjectName = NULL, RecordIdentifier = NULL, ActionOrder = NULL, ErrorMessage = NULL, Status = NULL, Action = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Grading", objectName = "RepairGradingSetupRunHistoryProcessDetail", objectId = RepairGradingSetupRunHistoryProcessDetailID, body = list(DataObject = body), searchFields = append("RepairGradingSetupRunHistoryProcessDetailID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}
