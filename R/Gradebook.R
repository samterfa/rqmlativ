
	#' List QuestionAnswerMedias
	#'
	#' This function returns a dataframe or json object of QuestionAnswerMedias
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QuestionAnswerMedias. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QuestionAnswerMedias.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QuestionAnswerMedia') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of QuestionAnswerMedias
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listQuestionAnswerMedias <- function(searchConditionsList = NULL, QuestionAnswerMediaID = F, QuestionAnswerID = F, MediaID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayFor = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "QuestionAnswerMedia", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a QuestionAnswerMedia
	#'
	#' This function returns a dataframe or json object of a QuestionAnswerMedia
	#' @param QuestionAnswerMediaID The ID of the QuestionAnswerMedia to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QuestionAnswerMedia. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QuestionAnswerMedia.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QuestionAnswerMedia') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of QuestionAnswerMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getQuestionAnswerMedia <- function(QuestionAnswerMediaID, QuestionAnswerID = F, MediaID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayFor = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "QuestionAnswerMediaID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "QuestionAnswerMedia", objectId = QuestionAnswerMediaID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a QuestionAnswerMedia
	#'
	#' This function deletes a QuestionAnswerMedia
	#' @param QuestionAnswerMediaID The ID of the QuestionAnswerMedia to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The QuestionAnswerMediaID of the deleted QuestionAnswerMedia.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteQuestionAnswerMedia <- function(QuestionAnswerMediaID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "QuestionAnswerMedia", objectId = QuestionAnswerMediaID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a QuestionAnswerMedia
	#'
	#' This function creates a QuestionAnswerMedia
	#' @param fieldNames The field values to give the created QuestionAnswerMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created QuestionAnswerMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createQuestionAnswerMedia <- function(QuestionAnswerID = NULL, MediaID = NULL, DisplayFor = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "QuestionAnswerMedia", body = list(DataObject = body), searchFields = append("QuestionAnswerMediaID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a QuestionAnswerMedia
	#'
	#' This function modifies a QuestionAnswerMedia
	#' @param fieldNames The field values to give the modified QuestionAnswerMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified QuestionAnswerMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyQuestionAnswerMedia <- function(QuestionAnswerMediaID, QuestionAnswerID = NULL, MediaID = NULL, DisplayFor = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "QuestionAnswerMedia", objectId = QuestionAnswerMediaID, body = list(DataObject = body), searchFields = append("QuestionAnswerMediaID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List QuestionMedias
	#'
	#' This function returns a dataframe or json object of QuestionMedias
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QuestionMedias. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QuestionMedias.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QuestionMedia') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of QuestionMedias
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listQuestionMedias <- function(searchConditionsList = NULL, QuestionMediaID = F, QuestionID = F, MediaID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "QuestionMedia", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a QuestionMedia
	#'
	#' This function returns a dataframe or json object of a QuestionMedia
	#' @param QuestionMediaID The ID of the QuestionMedia to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QuestionMedia. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QuestionMedia.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QuestionMedia') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of QuestionMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getQuestionMedia <- function(QuestionMediaID, QuestionID = F, MediaID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "QuestionMediaID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "QuestionMedia", objectId = QuestionMediaID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a QuestionMedia
	#'
	#' This function deletes a QuestionMedia
	#' @param QuestionMediaID The ID of the QuestionMedia to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The QuestionMediaID of the deleted QuestionMedia.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteQuestionMedia <- function(QuestionMediaID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "QuestionMedia", objectId = QuestionMediaID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a QuestionMedia
	#'
	#' This function creates a QuestionMedia
	#' @param fieldNames The field values to give the created QuestionMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created QuestionMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createQuestionMedia <- function(QuestionID = NULL, MediaID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "QuestionMedia", body = list(DataObject = body), searchFields = append("QuestionMediaID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a QuestionMedia
	#'
	#' This function modifies a QuestionMedia
	#' @param fieldNames The field values to give the modified QuestionMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified QuestionMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyQuestionMedia <- function(QuestionMediaID, QuestionID = NULL, MediaID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "QuestionMedia", objectId = QuestionMediaID, body = list(DataObject = body), searchFields = append("QuestionMediaID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAnswers
	#'
	#' This function returns a dataframe or json object of StudentAnswers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAnswers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAnswers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAnswer') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentAnswers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAnswers <- function(searchConditionsList = NULL, StudentAnswerID = F, StudentQuestionID = F, QuestionAnswerID = F, Value = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentAnswer", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAnswer
	#'
	#' This function returns a dataframe or json object of a StudentAnswer
	#' @param StudentAnswerID The ID of the StudentAnswer to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAnswer. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAnswer.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAnswer') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentAnswer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAnswer <- function(StudentAnswerID, StudentQuestionID = F, QuestionAnswerID = F, Value = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAnswerID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentAnswer", objectId = StudentAnswerID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAnswer
	#'
	#' This function deletes a StudentAnswer
	#' @param StudentAnswerID The ID of the StudentAnswer to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentAnswerID of the deleted StudentAnswer.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAnswer <- function(StudentAnswerID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentAnswer", objectId = StudentAnswerID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAnswer
	#'
	#' This function creates a StudentAnswer
	#' @param fieldNames The field values to give the created StudentAnswer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentAnswer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAnswer <- function(StudentQuestionID = NULL, QuestionAnswerID = NULL, Value = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentAnswer", body = list(DataObject = body), searchFields = append("StudentAnswerID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAnswer
	#'
	#' This function modifies a StudentAnswer
	#' @param fieldNames The field values to give the modified StudentAnswer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentAnswer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAnswer <- function(StudentAnswerID, StudentQuestionID = NULL, QuestionAnswerID = NULL, Value = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentAnswer", objectId = StudentAnswerID, body = list(DataObject = body), searchFields = append("StudentAnswerID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentQuestions
	#'
	#' This function returns a dataframe or json object of StudentQuestions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentQuestions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentQuestions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentQuestion') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentQuestions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentQuestions <- function(searchConditionsList = NULL, StudentQuestionID = F, StudentAssignmentID = F, AssignmentQuestionID = F, Score = F, Order = F, Status = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AttachmentCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentQuestion", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentQuestion
	#'
	#' This function returns a dataframe or json object of a StudentQuestion
	#' @param StudentQuestionID The ID of the StudentQuestion to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentQuestion. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentQuestion.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentQuestion') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentQuestion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentQuestion <- function(StudentQuestionID, StudentAssignmentID = F, AssignmentQuestionID = F, Score = F, Order = F, Status = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AttachmentCount = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentQuestionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentQuestion", objectId = StudentQuestionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentQuestion
	#'
	#' This function deletes a StudentQuestion
	#' @param StudentQuestionID The ID of the StudentQuestion to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentQuestionID of the deleted StudentQuestion.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentQuestion <- function(StudentQuestionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentQuestion", objectId = StudentQuestionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentQuestion
	#'
	#' This function creates a StudentQuestion
	#' @param fieldNames The field values to give the created StudentQuestion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentQuestion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentQuestion <- function(StudentAssignmentID = NULL, AssignmentQuestionID = NULL, Score = NULL, Order = NULL, Status = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentQuestion", body = list(DataObject = body), searchFields = append("StudentQuestionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentQuestion
	#'
	#' This function modifies a StudentQuestion
	#' @param fieldNames The field values to give the modified StudentQuestion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentQuestion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentQuestion <- function(StudentQuestionID, StudentAssignmentID = NULL, AssignmentQuestionID = NULL, Score = NULL, Order = NULL, Status = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentQuestion", objectId = StudentQuestionID, body = list(DataObject = body), searchFields = append("StudentQuestionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AssignmentQuestions
	#'
	#' This function returns a dataframe or json object of AssignmentQuestions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AssignmentQuestions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AssignmentQuestions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AssignmentQuestion') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of AssignmentQuestions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAssignmentQuestions <- function(searchConditionsList = NULL, AssignmentQuestionID = F, AssignmentID = F, QuestionID = F, PointsAllowed = F, AllowPartialCredit = F, DisplayOrder = F, QuestionAnswerCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "AssignmentQuestion", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AssignmentQuestion
	#'
	#' This function returns a dataframe or json object of an AssignmentQuestion
	#' @param AssignmentQuestionID The ID of the AssignmentQuestion to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AssignmentQuestion. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AssignmentQuestion.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AssignmentQuestion') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of AssignmentQuestion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAssignmentQuestion <- function(AssignmentQuestionID, AssignmentID = F, QuestionID = F, PointsAllowed = F, AllowPartialCredit = F, DisplayOrder = F, QuestionAnswerCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AssignmentQuestionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "AssignmentQuestion", objectId = AssignmentQuestionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AssignmentQuestion
	#'
	#' This function deletes an AssignmentQuestion
	#' @param AssignmentQuestionID The ID of the AssignmentQuestion to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The AssignmentQuestionID of the deleted AssignmentQuestion.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAssignmentQuestion <- function(AssignmentQuestionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "AssignmentQuestion", objectId = AssignmentQuestionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AssignmentQuestion
	#'
	#' This function creates an AssignmentQuestion
	#' @param fieldNames The field values to give the created AssignmentQuestion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created AssignmentQuestion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAssignmentQuestion <- function(AssignmentID = NULL, QuestionID = NULL, PointsAllowed = NULL, AllowPartialCredit = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "AssignmentQuestion", body = list(DataObject = body), searchFields = append("AssignmentQuestionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AssignmentQuestion
	#'
	#' This function modifies an AssignmentQuestion
	#' @param fieldNames The field values to give the modified AssignmentQuestion. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified AssignmentQuestion
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAssignmentQuestion <- function(AssignmentQuestionID, AssignmentID = NULL, QuestionID = NULL, PointsAllowed = NULL, AllowPartialCredit = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "AssignmentQuestion", objectId = AssignmentQuestionID, body = list(DataObject = body), searchFields = append("AssignmentQuestionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ClassGroupSections
	#'
	#' This function returns a dataframe or json object of ClassGroupSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClassGroupSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClassGroupSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClassGroupSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of ClassGroupSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listClassGroupSections <- function(searchConditionsList = NULL, ClassGroupSectionID = F, ClassGroupID = F, SectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ClassGroupSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ClassGroupSection
	#'
	#' This function returns a dataframe or json object of a ClassGroupSection
	#' @param ClassGroupSectionID The ID of the ClassGroupSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClassGroupSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClassGroupSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClassGroupSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of ClassGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getClassGroupSection <- function(ClassGroupSectionID, ClassGroupID = F, SectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ClassGroupSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ClassGroupSection", objectId = ClassGroupSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ClassGroupSection
	#'
	#' This function deletes a ClassGroupSection
	#' @param ClassGroupSectionID The ID of the ClassGroupSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The ClassGroupSectionID of the deleted ClassGroupSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteClassGroupSection <- function(ClassGroupSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ClassGroupSection", objectId = ClassGroupSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ClassGroupSection
	#'
	#' This function creates a ClassGroupSection
	#' @param fieldNames The field values to give the created ClassGroupSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created ClassGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createClassGroupSection <- function(ClassGroupID = NULL, SectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ClassGroupSection", body = list(DataObject = body), searchFields = append("ClassGroupSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ClassGroupSection
	#'
	#' This function modifies a ClassGroupSection
	#' @param fieldNames The field values to give the modified ClassGroupSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified ClassGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyClassGroupSection <- function(ClassGroupSectionID, ClassGroupID = NULL, SectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ClassGroupSection", objectId = ClassGroupSectionID, body = list(DataObject = body), searchFields = append("ClassGroupSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Questions
	#'
	#' This function returns a dataframe or json object of Questions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Questions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Questions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Question') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of Questions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listQuestions <- function(searchConditionsList = NULL, QuestionID = F, StaffID = F, Description = F, Instructions = F, Type = F, AllowStudentAttachments = F, UseAnswers = F, IsShortAnswer = F, IsEssay = F, IsMatching = F, IsMultipleChoice = F, IsTrueFalse = F, HasAutoScoredAssignment = F, HasAssignmentPastEndTime = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AttachmentCount = F, AssignmentCount = F, HasMultipleAssignments = F, HasQuestionMedias = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "Question", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Question
	#'
	#' This function returns a dataframe or json object of a Question
	#' @param QuestionID The ID of the Question to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Question. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Question.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Question') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of Question
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getQuestion <- function(QuestionID, StaffID = F, Description = F, Instructions = F, Type = F, AllowStudentAttachments = F, UseAnswers = F, IsShortAnswer = F, IsEssay = F, IsMatching = F, IsMultipleChoice = F, IsTrueFalse = F, HasAutoScoredAssignment = F, HasAssignmentPastEndTime = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AttachmentCount = F, AssignmentCount = F, HasMultipleAssignments = F, HasQuestionMedias = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "QuestionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "Question", objectId = QuestionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Question
	#'
	#' This function deletes a Question
	#' @param QuestionID The ID of the Question to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The QuestionID of the deleted Question.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteQuestion <- function(QuestionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "Question", objectId = QuestionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Question
	#'
	#' This function creates a Question
	#' @param fieldNames The field values to give the created Question. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created Question
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createQuestion <- function(StaffID = NULL, Description = NULL, Instructions = NULL, Type = NULL, AllowStudentAttachments = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "Question", body = list(DataObject = body), searchFields = append("QuestionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Question
	#'
	#' This function modifies a Question
	#' @param fieldNames The field values to give the modified Question. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified Question
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyQuestion <- function(QuestionID, StaffID = NULL, Description = NULL, Instructions = NULL, Type = NULL, AllowStudentAttachments = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "Question", objectId = QuestionID, body = list(DataObject = body), searchFields = append("QuestionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List QuestionAnswers
	#'
	#' This function returns a dataframe or json object of QuestionAnswers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QuestionAnswers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QuestionAnswers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QuestionAnswer') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of QuestionAnswers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listQuestionAnswers <- function(searchConditionsList = NULL, QuestionAnswerID = F, QuestionID = F, IsCorrect = F, Value = F, Choice = F, ChoiceOrder = F, ValueOrder = F, IsShortAnswer = F, IsEssay = F, IsTrueFalse = F, IsMultipleChoice = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasAttachedMedia = F, HasAttachedChoiceMedia = F, HasAttachedMediaSafe = F, HasAttachedChoiceMediaSafe = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "QuestionAnswer", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a QuestionAnswer
	#'
	#' This function returns a dataframe or json object of a QuestionAnswer
	#' @param QuestionAnswerID The ID of the QuestionAnswer to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QuestionAnswer. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QuestionAnswer.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QuestionAnswer') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of QuestionAnswer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getQuestionAnswer <- function(QuestionAnswerID, QuestionID = F, IsCorrect = F, Value = F, Choice = F, ChoiceOrder = F, ValueOrder = F, IsShortAnswer = F, IsEssay = F, IsTrueFalse = F, IsMultipleChoice = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasAttachedMedia = F, HasAttachedChoiceMedia = F, HasAttachedMediaSafe = F, HasAttachedChoiceMediaSafe = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "QuestionAnswerID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "QuestionAnswer", objectId = QuestionAnswerID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a QuestionAnswer
	#'
	#' This function deletes a QuestionAnswer
	#' @param QuestionAnswerID The ID of the QuestionAnswer to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The QuestionAnswerID of the deleted QuestionAnswer.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteQuestionAnswer <- function(QuestionAnswerID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "QuestionAnswer", objectId = QuestionAnswerID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a QuestionAnswer
	#'
	#' This function creates a QuestionAnswer
	#' @param fieldNames The field values to give the created QuestionAnswer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created QuestionAnswer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createQuestionAnswer <- function(QuestionID = NULL, IsCorrect = NULL, Value = NULL, Choice = NULL, ChoiceOrder = NULL, ValueOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "QuestionAnswer", body = list(DataObject = body), searchFields = append("QuestionAnswerID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a QuestionAnswer
	#'
	#' This function modifies a QuestionAnswer
	#' @param fieldNames The field values to give the modified QuestionAnswer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified QuestionAnswer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyQuestionAnswer <- function(QuestionAnswerID, QuestionID = NULL, IsCorrect = NULL, Value = NULL, Choice = NULL, ChoiceOrder = NULL, ValueOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "QuestionAnswer", objectId = QuestionAnswerID, body = list(DataObject = body), searchFields = append("QuestionAnswerID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupSubjectAcademicStandards
	#'
	#' This function returns a dataframe or json object of CalculationGroupSubjectAcademicStandards
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjectAcademicStandards. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjectAcademicStandards.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubjectAcademicStandard') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupSubjectAcademicStandards
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupSubjectAcademicStandards <- function(searchConditionsList = NULL, CalculationGroupSubjectAcademicStandardID = F, EntityGroupKey = F, CalculationGroupID = F, SubjectID = F, AcademicStandardID = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupSubjectAcademicStandardIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupSubjectAcademicStandard", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupSubjectAcademicStandard
	#'
	#' This function returns a dataframe or json object of a CalculationGroupSubjectAcademicStandard
	#' @param CalculationGroupSubjectAcademicStandardID The ID of the CalculationGroupSubjectAcademicStandard to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjectAcademicStandard. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjectAcademicStandard.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubjectAcademicStandard') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupSubjectAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupSubjectAcademicStandard <- function(CalculationGroupSubjectAcademicStandardID, EntityGroupKey = F, CalculationGroupID = F, SubjectID = F, AcademicStandardID = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupSubjectAcademicStandardIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupSubjectAcademicStandardID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectAcademicStandard", objectId = CalculationGroupSubjectAcademicStandardID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupSubjectAcademicStandard
	#'
	#' This function deletes a CalculationGroupSubjectAcademicStandard
	#' @param CalculationGroupSubjectAcademicStandardID The ID of the CalculationGroupSubjectAcademicStandard to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupSubjectAcademicStandardID of the deleted CalculationGroupSubjectAcademicStandard.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupSubjectAcademicStandard <- function(CalculationGroupSubjectAcademicStandardID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectAcademicStandard", objectId = CalculationGroupSubjectAcademicStandardID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupSubjectAcademicStandard
	#'
	#' This function creates a CalculationGroupSubjectAcademicStandard
	#' @param fieldNames The field values to give the created CalculationGroupSubjectAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupSubjectAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupSubjectAcademicStandard <- function(EntityGroupKey = NULL, CalculationGroupID = NULL, SubjectID = NULL, AcademicStandardID = NULL, CalculationGroupSubjectAcademicStandardIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectAcademicStandard", body = list(DataObject = body), searchFields = append("CalculationGroupSubjectAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupSubjectAcademicStandard
	#'
	#' This function modifies a CalculationGroupSubjectAcademicStandard
	#' @param fieldNames The field values to give the modified CalculationGroupSubjectAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupSubjectAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupSubjectAcademicStandard <- function(CalculationGroupSubjectAcademicStandardID, EntityGroupKey = NULL, CalculationGroupID = NULL, SubjectID = NULL, AcademicStandardID = NULL, CalculationGroupSubjectAcademicStandardIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectAcademicStandard", objectId = CalculationGroupSubjectAcademicStandardID, body = list(DataObject = body), searchFields = append("CalculationGroupSubjectAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ClassGroups
	#'
	#' This function returns a dataframe or json object of ClassGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClassGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClassGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClassGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of ClassGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listClassGroups <- function(searchConditionsList = NULL, ClassGroupID = F, Name = F, EntityID = F, SchoolYearID = F, StaffID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsAHistoricRecord = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ClassGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ClassGroup
	#'
	#' This function returns a dataframe or json object of a ClassGroup
	#' @param ClassGroupID The ID of the ClassGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClassGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClassGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClassGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of ClassGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getClassGroup <- function(ClassGroupID, Name = F, EntityID = F, SchoolYearID = F, StaffID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsAHistoricRecord = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ClassGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ClassGroup", objectId = ClassGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ClassGroup
	#'
	#' This function deletes a ClassGroup
	#' @param ClassGroupID The ID of the ClassGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The ClassGroupID of the deleted ClassGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteClassGroup <- function(ClassGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ClassGroup", objectId = ClassGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ClassGroup
	#'
	#' This function creates a ClassGroup
	#' @param fieldNames The field values to give the created ClassGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created ClassGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createClassGroup <- function(Name = NULL, EntityID = NULL, SchoolYearID = NULL, StaffID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ClassGroup", body = list(DataObject = body), searchFields = append("ClassGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ClassGroup
	#'
	#' This function modifies a ClassGroup
	#' @param fieldNames The field values to give the modified ClassGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified ClassGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyClassGroup <- function(ClassGroupID, Name = NULL, EntityID = NULL, SchoolYearID = NULL, StaffID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ClassGroup", objectId = ClassGroupID, body = list(DataObject = body), searchFields = append("ClassGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionAcademicStandardWeights
	#'
	#' This function returns a dataframe or json object of SectionAcademicStandardWeights
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionAcademicStandardWeights. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionAcademicStandardWeights.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionAcademicStandardWeight') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionAcademicStandardWeights
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionAcademicStandardWeights <- function(searchConditionsList = NULL, SectionAcademicStandardWeightID = F, SectionGradeBucketID = F, AcademicStandardID = F, WeightPercent = F, HasAssignments = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionAcademicStandardWeight", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionAcademicStandardWeight
	#'
	#' This function returns a dataframe or json object of a SectionAcademicStandardWeight
	#' @param SectionAcademicStandardWeightID The ID of the SectionAcademicStandardWeight to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionAcademicStandardWeight. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionAcademicStandardWeight.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionAcademicStandardWeight') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionAcademicStandardWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionAcademicStandardWeight <- function(SectionAcademicStandardWeightID, SectionGradeBucketID = F, AcademicStandardID = F, WeightPercent = F, HasAssignments = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionAcademicStandardWeightID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionAcademicStandardWeight", objectId = SectionAcademicStandardWeightID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionAcademicStandardWeight
	#'
	#' This function deletes a SectionAcademicStandardWeight
	#' @param SectionAcademicStandardWeightID The ID of the SectionAcademicStandardWeight to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionAcademicStandardWeightID of the deleted SectionAcademicStandardWeight.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionAcademicStandardWeight <- function(SectionAcademicStandardWeightID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionAcademicStandardWeight", objectId = SectionAcademicStandardWeightID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionAcademicStandardWeight
	#'
	#' This function creates a SectionAcademicStandardWeight
	#' @param fieldNames The field values to give the created SectionAcademicStandardWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionAcademicStandardWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionAcademicStandardWeight <- function(SectionGradeBucketID = NULL, AcademicStandardID = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionAcademicStandardWeight", body = list(DataObject = body), searchFields = append("SectionAcademicStandardWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionAcademicStandardWeight
	#'
	#' This function modifies a SectionAcademicStandardWeight
	#' @param fieldNames The field values to give the modified SectionAcademicStandardWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionAcademicStandardWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionAcademicStandardWeight <- function(SectionAcademicStandardWeightID, SectionGradeBucketID = NULL, AcademicStandardID = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionAcademicStandardWeight", objectId = SectionAcademicStandardWeightID, body = list(DataObject = body), searchFields = append("SectionAcademicStandardWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AcademicStandardGradingScaleGroups
	#'
	#' This function returns a dataframe or json object of AcademicStandardGradingScaleGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AcademicStandardGradingScaleGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AcademicStandardGradingScaleGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AcademicStandardGradingScaleGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of AcademicStandardGradingScaleGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAcademicStandardGradingScaleGroups <- function(searchConditionsList = NULL, AcademicStandardGradingScaleGroupID = F, GradingScaleGroupID = F, CalculationGroupID = F, Description = F, EntityGroupKey = F, IsDefaultGroup = F, IsAHistoricRecord = F, StandardCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AcademicStandardGradingScaleGroupIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AcademicStandardGradingScaleGroup
	#'
	#' This function returns a dataframe or json object of an AcademicStandardGradingScaleGroup
	#' @param AcademicStandardGradingScaleGroupID The ID of the AcademicStandardGradingScaleGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AcademicStandardGradingScaleGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AcademicStandardGradingScaleGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AcademicStandardGradingScaleGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of AcademicStandardGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAcademicStandardGradingScaleGroup <- function(AcademicStandardGradingScaleGroupID, GradingScaleGroupID = F, CalculationGroupID = F, Description = F, EntityGroupKey = F, IsDefaultGroup = F, IsAHistoricRecord = F, StandardCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AcademicStandardGradingScaleGroupIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AcademicStandardGradingScaleGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroup", objectId = AcademicStandardGradingScaleGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AcademicStandardGradingScaleGroup
	#'
	#' This function deletes an AcademicStandardGradingScaleGroup
	#' @param AcademicStandardGradingScaleGroupID The ID of the AcademicStandardGradingScaleGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The AcademicStandardGradingScaleGroupID of the deleted AcademicStandardGradingScaleGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAcademicStandardGradingScaleGroup <- function(AcademicStandardGradingScaleGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroup", objectId = AcademicStandardGradingScaleGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AcademicStandardGradingScaleGroup
	#'
	#' This function creates an AcademicStandardGradingScaleGroup
	#' @param fieldNames The field values to give the created AcademicStandardGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created AcademicStandardGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAcademicStandardGradingScaleGroup <- function(GradingScaleGroupID = NULL, CalculationGroupID = NULL, Description = NULL, EntityGroupKey = NULL, IsDefaultGroup = NULL, AcademicStandardGradingScaleGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroup", body = list(DataObject = body), searchFields = append("AcademicStandardGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AcademicStandardGradingScaleGroup
	#'
	#' This function modifies an AcademicStandardGradingScaleGroup
	#' @param fieldNames The field values to give the modified AcademicStandardGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified AcademicStandardGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAcademicStandardGradingScaleGroup <- function(AcademicStandardGradingScaleGroupID, GradingScaleGroupID = NULL, CalculationGroupID = NULL, Description = NULL, EntityGroupKey = NULL, IsDefaultGroup = NULL, AcademicStandardGradingScaleGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroup", objectId = AcademicStandardGradingScaleGroupID, body = list(DataObject = body), searchFields = append("AcademicStandardGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AcademicStandardGradingScaleGroupAcademicStandards
	#'
	#' This function returns a dataframe or json object of AcademicStandardGradingScaleGroupAcademicStandards
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AcademicStandardGradingScaleGroupAcademicStandards. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AcademicStandardGradingScaleGroupAcademicStandards.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AcademicStandardGradingScaleGroupAcademicStandard') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of AcademicStandardGradingScaleGroupAcademicStandards
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAcademicStandardGradingScaleGroupAcademicStandards <- function(searchConditionsList = NULL, AcademicStandardGradingScaleGroupAcademicStandardID = F, AcademicStandardGradingScaleGroupID = F, CalculationGroupAcademicStandardID = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AcademicStandardGradingScaleGroupAcademicStandardIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroupAcademicStandard", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AcademicStandardGradingScaleGroupAcademicStandard
	#'
	#' This function returns a dataframe or json object of an AcademicStandardGradingScaleGroupAcademicStandard
	#' @param AcademicStandardGradingScaleGroupAcademicStandardID The ID of the AcademicStandardGradingScaleGroupAcademicStandard to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AcademicStandardGradingScaleGroupAcademicStandard. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AcademicStandardGradingScaleGroupAcademicStandard.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AcademicStandardGradingScaleGroupAcademicStandard') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of AcademicStandardGradingScaleGroupAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAcademicStandardGradingScaleGroupAcademicStandard <- function(AcademicStandardGradingScaleGroupAcademicStandardID, AcademicStandardGradingScaleGroupID = F, CalculationGroupAcademicStandardID = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AcademicStandardGradingScaleGroupAcademicStandardIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AcademicStandardGradingScaleGroupAcademicStandardID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroupAcademicStandard", objectId = AcademicStandardGradingScaleGroupAcademicStandardID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AcademicStandardGradingScaleGroupAcademicStandard
	#'
	#' This function deletes an AcademicStandardGradingScaleGroupAcademicStandard
	#' @param AcademicStandardGradingScaleGroupAcademicStandardID The ID of the AcademicStandardGradingScaleGroupAcademicStandard to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The AcademicStandardGradingScaleGroupAcademicStandardID of the deleted AcademicStandardGradingScaleGroupAcademicStandard.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAcademicStandardGradingScaleGroupAcademicStandard <- function(AcademicStandardGradingScaleGroupAcademicStandardID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroupAcademicStandard", objectId = AcademicStandardGradingScaleGroupAcademicStandardID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AcademicStandardGradingScaleGroupAcademicStandard
	#'
	#' This function creates an AcademicStandardGradingScaleGroupAcademicStandard
	#' @param fieldNames The field values to give the created AcademicStandardGradingScaleGroupAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created AcademicStandardGradingScaleGroupAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAcademicStandardGradingScaleGroupAcademicStandard <- function(AcademicStandardGradingScaleGroupID = NULL, CalculationGroupAcademicStandardID = NULL, EntityGroupKey = NULL, AcademicStandardGradingScaleGroupAcademicStandardIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroupAcademicStandard", body = list(DataObject = body), searchFields = append("AcademicStandardGradingScaleGroupAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AcademicStandardGradingScaleGroupAcademicStandard
	#'
	#' This function modifies an AcademicStandardGradingScaleGroupAcademicStandard
	#' @param fieldNames The field values to give the modified AcademicStandardGradingScaleGroupAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified AcademicStandardGradingScaleGroupAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAcademicStandardGradingScaleGroupAcademicStandard <- function(AcademicStandardGradingScaleGroupAcademicStandardID, AcademicStandardGradingScaleGroupID = NULL, CalculationGroupAcademicStandardID = NULL, EntityGroupKey = NULL, AcademicStandardGradingScaleGroupAcademicStandardIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "AcademicStandardGradingScaleGroupAcademicStandard", objectId = AcademicStandardGradingScaleGroupAcademicStandardID, body = list(DataObject = body), searchFields = append("AcademicStandardGradingScaleGroupAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSubjectWeights
	#'
	#' This function returns a dataframe or json object of SectionSubjectWeights
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSubjectWeights. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSubjectWeights.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSubjectWeight') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionSubjectWeights
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSubjectWeights <- function(searchConditionsList = NULL, SectionSubjectWeightID = F, SectionGradeBucketID = F, SubjectID = F, WeightPercent = F, HasAssignments = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionSubjectWeight", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSubjectWeight
	#'
	#' This function returns a dataframe or json object of a SectionSubjectWeight
	#' @param SectionSubjectWeightID The ID of the SectionSubjectWeight to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSubjectWeight. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSubjectWeight.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSubjectWeight') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionSubjectWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSubjectWeight <- function(SectionSubjectWeightID, SectionGradeBucketID = F, SubjectID = F, WeightPercent = F, HasAssignments = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSubjectWeightID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionSubjectWeight", objectId = SectionSubjectWeightID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSubjectWeight
	#'
	#' This function deletes a SectionSubjectWeight
	#' @param SectionSubjectWeightID The ID of the SectionSubjectWeight to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionSubjectWeightID of the deleted SectionSubjectWeight.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSubjectWeight <- function(SectionSubjectWeightID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionSubjectWeight", objectId = SectionSubjectWeightID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSubjectWeight
	#'
	#' This function creates a SectionSubjectWeight
	#' @param fieldNames The field values to give the created SectionSubjectWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionSubjectWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSubjectWeight <- function(SectionGradeBucketID = NULL, SubjectID = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionSubjectWeight", body = list(DataObject = body), searchFields = append("SectionSubjectWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSubjectWeight
	#'
	#' This function modifies a SectionSubjectWeight
	#' @param fieldNames The field values to give the modified SectionSubjectWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionSubjectWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSubjectWeight <- function(SectionSubjectWeightID, SectionGradeBucketID = NULL, SubjectID = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionSubjectWeight", objectId = SectionSubjectWeightID, body = list(DataObject = body), searchFields = append("SectionSubjectWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupAcademicStandardGradeBuckets
	#'
	#' This function returns a dataframe or json object of CalculationGroupAcademicStandardGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandardGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandardGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandardGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupAcademicStandardGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupAcademicStandardGradeBuckets <- function(searchConditionsList = NULL, CalculationGroupAcademicStandardGradeBucketID = F, CalculationGroupAcademicStandardID = F, CalculationGroupGradeBucketID = F, CalculationType = F, EntityGroupKey = F, IsAHistoricRecord = F, HasCategoryWeighting = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasAssignments = F, IsWeightedOnByACalculationGroupSubjectGradeBucket = F, IsWeightedOnInASectionOverride = F, CalculationGroupAcademicStandardGradeBucketIDClonedFrom = F, HasCalculationGroupWeight = F, HasCalculationGroupSubjectWeight = F, HasAcademicStandardWeighting = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupAcademicStandardGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupAcademicStandardGradeBucket
	#'
	#' This function returns a dataframe or json object of a CalculationGroupAcademicStandardGradeBucket
	#' @param CalculationGroupAcademicStandardGradeBucketID The ID of the CalculationGroupAcademicStandardGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandardGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandardGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandardGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupAcademicStandardGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupAcademicStandardGradeBucket <- function(CalculationGroupAcademicStandardGradeBucketID, CalculationGroupAcademicStandardID = F, CalculationGroupGradeBucketID = F, CalculationType = F, EntityGroupKey = F, IsAHistoricRecord = F, HasCategoryWeighting = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasAssignments = F, IsWeightedOnByACalculationGroupSubjectGradeBucket = F, IsWeightedOnInASectionOverride = F, CalculationGroupAcademicStandardGradeBucketIDClonedFrom = F, HasCalculationGroupWeight = F, HasCalculationGroupSubjectWeight = F, HasAcademicStandardWeighting = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupAcademicStandardGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardGradeBucket", objectId = CalculationGroupAcademicStandardGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupAcademicStandardGradeBucket
	#'
	#' This function deletes a CalculationGroupAcademicStandardGradeBucket
	#' @param CalculationGroupAcademicStandardGradeBucketID The ID of the CalculationGroupAcademicStandardGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupAcademicStandardGradeBucketID of the deleted CalculationGroupAcademicStandardGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupAcademicStandardGradeBucket <- function(CalculationGroupAcademicStandardGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardGradeBucket", objectId = CalculationGroupAcademicStandardGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupAcademicStandardGradeBucket
	#'
	#' This function creates a CalculationGroupAcademicStandardGradeBucket
	#' @param fieldNames The field values to give the created CalculationGroupAcademicStandardGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupAcademicStandardGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupAcademicStandardGradeBucket <- function(CalculationGroupAcademicStandardID = NULL, CalculationGroupGradeBucketID = NULL, CalculationType = NULL, EntityGroupKey = NULL, CalculationGroupAcademicStandardGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardGradeBucket", body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupAcademicStandardGradeBucket
	#'
	#' This function modifies a CalculationGroupAcademicStandardGradeBucket
	#' @param fieldNames The field values to give the modified CalculationGroupAcademicStandardGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupAcademicStandardGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupAcademicStandardGradeBucket <- function(CalculationGroupAcademicStandardGradeBucketID, CalculationGroupAcademicStandardID = NULL, CalculationGroupGradeBucketID = NULL, CalculationType = NULL, EntityGroupKey = NULL, CalculationGroupAcademicStandardGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardGradeBucket", objectId = CalculationGroupAcademicStandardGradeBucketID, body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupAcademicStandardWeightings
	#'
	#' This function returns a dataframe or json object of CalculationGroupAcademicStandardWeightings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandardWeightings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandardWeightings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandardWeighting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupAcademicStandardWeightings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupAcademicStandardWeightings <- function(searchConditionsList = NULL, CalculationGroupAcademicStandardWeightingID = F, CalculationGroupAcademicStandardGradeBucketID = F, CategoryIDToWeight = F, WeightPercent = F, IsAHistoricRecord = F, AdjustedWeightPercent = F, HasAssignments = F, PercentEarnedCategory = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, CalculationGroupAcademicStandardWeightingIDClonedFrom = F, AcademicStandardIDToWeight = F, UseForWeightSum = F, PointsEarnedCategory = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupAcademicStandardWeighting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupAcademicStandardWeighting
	#'
	#' This function returns a dataframe or json object of a CalculationGroupAcademicStandardWeighting
	#' @param CalculationGroupAcademicStandardWeightingID The ID of the CalculationGroupAcademicStandardWeighting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandardWeighting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandardWeighting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandardWeighting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupAcademicStandardWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupAcademicStandardWeighting <- function(CalculationGroupAcademicStandardWeightingID, CalculationGroupAcademicStandardGradeBucketID = F, CategoryIDToWeight = F, WeightPercent = F, IsAHistoricRecord = F, AdjustedWeightPercent = F, HasAssignments = F, PercentEarnedCategory = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, CalculationGroupAcademicStandardWeightingIDClonedFrom = F, AcademicStandardIDToWeight = F, UseForWeightSum = F, PointsEarnedCategory = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupAcademicStandardWeightingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardWeighting", objectId = CalculationGroupAcademicStandardWeightingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupAcademicStandardWeighting
	#'
	#' This function deletes a CalculationGroupAcademicStandardWeighting
	#' @param CalculationGroupAcademicStandardWeightingID The ID of the CalculationGroupAcademicStandardWeighting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupAcademicStandardWeightingID of the deleted CalculationGroupAcademicStandardWeighting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupAcademicStandardWeighting <- function(CalculationGroupAcademicStandardWeightingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardWeighting", objectId = CalculationGroupAcademicStandardWeightingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupAcademicStandardWeighting
	#'
	#' This function creates a CalculationGroupAcademicStandardWeighting
	#' @param fieldNames The field values to give the created CalculationGroupAcademicStandardWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupAcademicStandardWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupAcademicStandardWeighting <- function(CalculationGroupAcademicStandardGradeBucketID = NULL, CategoryIDToWeight = NULL, WeightPercent = NULL, EntityGroupKey = NULL, CalculationGroupAcademicStandardWeightingIDClonedFrom = NULL, AcademicStandardIDToWeight = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardWeighting", body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupAcademicStandardWeighting
	#'
	#' This function modifies a CalculationGroupAcademicStandardWeighting
	#' @param fieldNames The field values to give the modified CalculationGroupAcademicStandardWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupAcademicStandardWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupAcademicStandardWeighting <- function(CalculationGroupAcademicStandardWeightingID, CalculationGroupAcademicStandardGradeBucketID = NULL, CategoryIDToWeight = NULL, WeightPercent = NULL, EntityGroupKey = NULL, CalculationGroupAcademicStandardWeightingIDClonedFrom = NULL, AcademicStandardIDToWeight = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardWeighting", objectId = CalculationGroupAcademicStandardWeightingID, body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCalculationGroupAcademicStandardWeightings
	#'
	#' This function returns a dataframe or json object of TempCalculationGroupAcademicStandardWeightings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalculationGroupAcademicStandardWeightings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalculationGroupAcademicStandardWeightings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalculationGroupAcademicStandardWeighting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCalculationGroupAcademicStandardWeightings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCalculationGroupAcademicStandardWeightings <- function(searchConditionsList = NULL, TempCalculationGroupAcademicStandardWeightingID = F, CalculationGroupAcademicStandardGradeBucketID = F, CategoryID = F, CategoryDescription = F, WeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCalculationGroupAcademicStandardWeighting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCalculationGroupAcademicStandardWeighting
	#'
	#' This function returns a dataframe or json object of a TempCalculationGroupAcademicStandardWeighting
	#' @param TempCalculationGroupAcademicStandardWeightingID The ID of the TempCalculationGroupAcademicStandardWeighting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalculationGroupAcademicStandardWeighting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalculationGroupAcademicStandardWeighting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalculationGroupAcademicStandardWeighting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCalculationGroupAcademicStandardWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCalculationGroupAcademicStandardWeighting <- function(TempCalculationGroupAcademicStandardWeightingID, CalculationGroupAcademicStandardGradeBucketID = F, CategoryID = F, CategoryDescription = F, WeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCalculationGroupAcademicStandardWeightingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCalculationGroupAcademicStandardWeighting", objectId = TempCalculationGroupAcademicStandardWeightingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCalculationGroupAcademicStandardWeighting
	#'
	#' This function deletes a TempCalculationGroupAcademicStandardWeighting
	#' @param TempCalculationGroupAcademicStandardWeightingID The ID of the TempCalculationGroupAcademicStandardWeighting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCalculationGroupAcademicStandardWeightingID of the deleted TempCalculationGroupAcademicStandardWeighting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCalculationGroupAcademicStandardWeighting <- function(TempCalculationGroupAcademicStandardWeightingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCalculationGroupAcademicStandardWeighting", objectId = TempCalculationGroupAcademicStandardWeightingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCalculationGroupAcademicStandardWeighting
	#'
	#' This function creates a TempCalculationGroupAcademicStandardWeighting
	#' @param fieldNames The field values to give the created TempCalculationGroupAcademicStandardWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCalculationGroupAcademicStandardWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCalculationGroupAcademicStandardWeighting <- function(CalculationGroupAcademicStandardGradeBucketID = NULL, CategoryID = NULL, CategoryDescription = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCalculationGroupAcademicStandardWeighting", body = list(DataObject = body), searchFields = append("TempCalculationGroupAcademicStandardWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCalculationGroupAcademicStandardWeighting
	#'
	#' This function modifies a TempCalculationGroupAcademicStandardWeighting
	#' @param fieldNames The field values to give the modified TempCalculationGroupAcademicStandardWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCalculationGroupAcademicStandardWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCalculationGroupAcademicStandardWeighting <- function(TempCalculationGroupAcademicStandardWeightingID, CalculationGroupAcademicStandardGradeBucketID = NULL, CategoryID = NULL, CategoryDescription = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCalculationGroupAcademicStandardWeighting", objectId = TempCalculationGroupAcademicStandardWeightingID, body = list(DataObject = body), searchFields = append("TempCalculationGroupAcademicStandardWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupAcademicStandards
	#'
	#' This function returns a dataframe or json object of CalculationGroupAcademicStandards
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandards. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandards.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandard') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupAcademicStandards
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupAcademicStandards <- function(searchConditionsList = NULL, CalculationGroupAcademicStandardID = F, CalculationGroupID = F, AcademicStandardID = F, EntityGroupKey = F, HasParentSubject = F, HasGradingScale = F, HasAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupAcademicStandardIDClonedFrom = F, Order = F, Level = F, CalculationGroupAcademicStandardIDParent = F, DisplayOnReportCard = F, CalculateForSingleBucket = F, IsDescendantOfSpecifiedAcademicStandard = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupAcademicStandard", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupAcademicStandard
	#'
	#' This function returns a dataframe or json object of a CalculationGroupAcademicStandard
	#' @param CalculationGroupAcademicStandardID The ID of the CalculationGroupAcademicStandard to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandard. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandard.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandard') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupAcademicStandard <- function(CalculationGroupAcademicStandardID, CalculationGroupID = F, AcademicStandardID = F, EntityGroupKey = F, HasParentSubject = F, HasGradingScale = F, HasAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupAcademicStandardIDClonedFrom = F, Order = F, Level = F, CalculationGroupAcademicStandardIDParent = F, DisplayOnReportCard = F, CalculateForSingleBucket = F, IsDescendantOfSpecifiedAcademicStandard = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupAcademicStandardID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandard", objectId = CalculationGroupAcademicStandardID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupAcademicStandard
	#'
	#' This function deletes a CalculationGroupAcademicStandard
	#' @param CalculationGroupAcademicStandardID The ID of the CalculationGroupAcademicStandard to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupAcademicStandardID of the deleted CalculationGroupAcademicStandard.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupAcademicStandard <- function(CalculationGroupAcademicStandardID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandard", objectId = CalculationGroupAcademicStandardID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupAcademicStandard
	#'
	#' This function creates a CalculationGroupAcademicStandard
	#' @param fieldNames The field values to give the created CalculationGroupAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupAcademicStandard <- function(CalculationGroupID = NULL, AcademicStandardID = NULL, EntityGroupKey = NULL, CalculationGroupAcademicStandardIDClonedFrom = NULL, Order = NULL, Level = NULL, CalculationGroupAcademicStandardIDParent = NULL, DisplayOnReportCard = NULL, CalculateForSingleBucket = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandard", body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupAcademicStandard
	#'
	#' This function modifies a CalculationGroupAcademicStandard
	#' @param fieldNames The field values to give the modified CalculationGroupAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupAcademicStandard <- function(CalculationGroupAcademicStandardID, CalculationGroupID = NULL, AcademicStandardID = NULL, EntityGroupKey = NULL, CalculationGroupAcademicStandardIDClonedFrom = NULL, Order = NULL, Level = NULL, CalculationGroupAcademicStandardIDParent = NULL, DisplayOnReportCard = NULL, CalculateForSingleBucket = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandard", objectId = CalculationGroupAcademicStandardID, body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupSubjects
	#'
	#' This function returns a dataframe or json object of CalculationGroupSubjects
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjects. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjects.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubject') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupSubjects
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupSubjects <- function(searchConditionsList = NULL, CalculationGroupSubjectID = F, CalculationGroupID = F, SubjectID = F, EntityGroupKey = F, IsAHistoricRecord = F, HasGradingScale = F, HasAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupSubjectIDClonedFrom = F, Order = F, TopLevelAcademicStandardHierarchyDepthDescription = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupSubject", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupSubject
	#'
	#' This function returns a dataframe or json object of a CalculationGroupSubject
	#' @param CalculationGroupSubjectID The ID of the CalculationGroupSubject to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubject. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubject.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubject') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupSubject <- function(CalculationGroupSubjectID, CalculationGroupID = F, SubjectID = F, EntityGroupKey = F, IsAHistoricRecord = F, HasGradingScale = F, HasAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupSubjectIDClonedFrom = F, Order = F, TopLevelAcademicStandardHierarchyDepthDescription = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupSubjectID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupSubject", objectId = CalculationGroupSubjectID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupSubject
	#'
	#' This function deletes a CalculationGroupSubject
	#' @param CalculationGroupSubjectID The ID of the CalculationGroupSubject to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupSubjectID of the deleted CalculationGroupSubject.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupSubject <- function(CalculationGroupSubjectID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupSubject", objectId = CalculationGroupSubjectID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupSubject
	#'
	#' This function creates a CalculationGroupSubject
	#' @param fieldNames The field values to give the created CalculationGroupSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupSubject <- function(CalculationGroupID = NULL, SubjectID = NULL, EntityGroupKey = NULL, CalculationGroupSubjectIDClonedFrom = NULL, Order = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupSubject", body = list(DataObject = body), searchFields = append("CalculationGroupSubjectID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupSubject
	#'
	#' This function modifies a CalculationGroupSubject
	#' @param fieldNames The field values to give the modified CalculationGroupSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupSubject <- function(CalculationGroupSubjectID, CalculationGroupID = NULL, SubjectID = NULL, EntityGroupKey = NULL, CalculationGroupSubjectIDClonedFrom = NULL, Order = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupSubject", objectId = CalculationGroupSubjectID, body = list(DataObject = body), searchFields = append("CalculationGroupSubjectID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupSubjectWeightings
	#'
	#' This function returns a dataframe or json object of CalculationGroupSubjectWeightings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjectWeightings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjectWeightings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubjectWeighting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupSubjectWeightings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupSubjectWeightings <- function(searchConditionsList = NULL, CalculationGroupSubjectWeightingID = F, CalculationGroupSubjectGradeBucketID = F, CategoryIDToWeight = F, AcademicStandardIDToWeight = F, WeightPercent = F, IsAHistoricRecord = F, AdjustedWeightPercent = F, HasAssignments = F, UseForWeightSum = F, PercentEarnedCategory = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, CalculationGroupSubjectWeightingIDClonedFrom = F, PointsEarnedCategory = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupSubjectWeighting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupSubjectWeighting
	#'
	#' This function returns a dataframe or json object of a CalculationGroupSubjectWeighting
	#' @param CalculationGroupSubjectWeightingID The ID of the CalculationGroupSubjectWeighting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjectWeighting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjectWeighting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubjectWeighting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupSubjectWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupSubjectWeighting <- function(CalculationGroupSubjectWeightingID, CalculationGroupSubjectGradeBucketID = F, CategoryIDToWeight = F, AcademicStandardIDToWeight = F, WeightPercent = F, IsAHistoricRecord = F, AdjustedWeightPercent = F, HasAssignments = F, UseForWeightSum = F, PercentEarnedCategory = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, CalculationGroupSubjectWeightingIDClonedFrom = F, PointsEarnedCategory = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupSubjectWeightingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectWeighting", objectId = CalculationGroupSubjectWeightingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupSubjectWeighting
	#'
	#' This function deletes a CalculationGroupSubjectWeighting
	#' @param CalculationGroupSubjectWeightingID The ID of the CalculationGroupSubjectWeighting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupSubjectWeightingID of the deleted CalculationGroupSubjectWeighting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupSubjectWeighting <- function(CalculationGroupSubjectWeightingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectWeighting", objectId = CalculationGroupSubjectWeightingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupSubjectWeighting
	#'
	#' This function creates a CalculationGroupSubjectWeighting
	#' @param fieldNames The field values to give the created CalculationGroupSubjectWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupSubjectWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupSubjectWeighting <- function(CalculationGroupSubjectGradeBucketID = NULL, CategoryIDToWeight = NULL, AcademicStandardIDToWeight = NULL, WeightPercent = NULL, EntityGroupKey = NULL, CalculationGroupSubjectWeightingIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectWeighting", body = list(DataObject = body), searchFields = append("CalculationGroupSubjectWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupSubjectWeighting
	#'
	#' This function modifies a CalculationGroupSubjectWeighting
	#' @param fieldNames The field values to give the modified CalculationGroupSubjectWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupSubjectWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupSubjectWeighting <- function(CalculationGroupSubjectWeightingID, CalculationGroupSubjectGradeBucketID = NULL, CategoryIDToWeight = NULL, AcademicStandardIDToWeight = NULL, WeightPercent = NULL, EntityGroupKey = NULL, CalculationGroupSubjectWeightingIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectWeighting", objectId = CalculationGroupSubjectWeightingID, body = list(DataObject = body), searchFields = append("CalculationGroupSubjectWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCalculationGroupSubjectWeightings
	#'
	#' This function returns a dataframe or json object of TempCalculationGroupSubjectWeightings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalculationGroupSubjectWeightings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalculationGroupSubjectWeightings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalculationGroupSubjectWeighting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCalculationGroupSubjectWeightings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCalculationGroupSubjectWeightings <- function(searchConditionsList = NULL, TempCalculationGroupSubjectWeightingID = F, CalculationGroupSubjectGradeBucketID = F, CategoryID = F, CategoryDescription = F, WeightPercent = F, AcademicStandardID = F, AcademicStandardDescription = F, AcademicStandardFullKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCalculationGroupSubjectWeighting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCalculationGroupSubjectWeighting
	#'
	#' This function returns a dataframe or json object of a TempCalculationGroupSubjectWeighting
	#' @param TempCalculationGroupSubjectWeightingID The ID of the TempCalculationGroupSubjectWeighting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalculationGroupSubjectWeighting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalculationGroupSubjectWeighting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalculationGroupSubjectWeighting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCalculationGroupSubjectWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCalculationGroupSubjectWeighting <- function(TempCalculationGroupSubjectWeightingID, CalculationGroupSubjectGradeBucketID = F, CategoryID = F, CategoryDescription = F, WeightPercent = F, AcademicStandardID = F, AcademicStandardDescription = F, AcademicStandardFullKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCalculationGroupSubjectWeightingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCalculationGroupSubjectWeighting", objectId = TempCalculationGroupSubjectWeightingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCalculationGroupSubjectWeighting
	#'
	#' This function deletes a TempCalculationGroupSubjectWeighting
	#' @param TempCalculationGroupSubjectWeightingID The ID of the TempCalculationGroupSubjectWeighting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCalculationGroupSubjectWeightingID of the deleted TempCalculationGroupSubjectWeighting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCalculationGroupSubjectWeighting <- function(TempCalculationGroupSubjectWeightingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCalculationGroupSubjectWeighting", objectId = TempCalculationGroupSubjectWeightingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCalculationGroupSubjectWeighting
	#'
	#' This function creates a TempCalculationGroupSubjectWeighting
	#' @param fieldNames The field values to give the created TempCalculationGroupSubjectWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCalculationGroupSubjectWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCalculationGroupSubjectWeighting <- function(CalculationGroupSubjectGradeBucketID = NULL, CategoryID = NULL, CategoryDescription = NULL, WeightPercent = NULL, AcademicStandardID = NULL, AcademicStandardDescription = NULL, AcademicStandardFullKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCalculationGroupSubjectWeighting", body = list(DataObject = body), searchFields = append("TempCalculationGroupSubjectWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCalculationGroupSubjectWeighting
	#'
	#' This function modifies a TempCalculationGroupSubjectWeighting
	#' @param fieldNames The field values to give the modified TempCalculationGroupSubjectWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCalculationGroupSubjectWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCalculationGroupSubjectWeighting <- function(TempCalculationGroupSubjectWeightingID, CalculationGroupSubjectGradeBucketID = NULL, CategoryID = NULL, CategoryDescription = NULL, WeightPercent = NULL, AcademicStandardID = NULL, AcademicStandardDescription = NULL, AcademicStandardFullKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCalculationGroupSubjectWeighting", objectId = TempCalculationGroupSubjectWeightingID, body = list(DataObject = body), searchFields = append("TempCalculationGroupSubjectWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupSubjectGradeBuckets
	#'
	#' This function returns a dataframe or json object of CalculationGroupSubjectGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjectGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjectGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubjectGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupSubjectGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupSubjectGradeBuckets <- function(searchConditionsList = NULL, CalculationGroupSubjectGradeBucketID = F, CalculationGroupSubjectID = F, CalculationGroupGradeBucketID = F, CalculationType = F, EntityGroupKey = F, IsAHistoricRecord = F, HasAssignments = F, HasCategoryWeighting = F, HasSubjectAcademicStandards = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupSubjectGradeBucketIDClonedFrom = F, HasCalculationGroupWeight = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupSubjectGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupSubjectGradeBucket
	#'
	#' This function returns a dataframe or json object of a CalculationGroupSubjectGradeBucket
	#' @param CalculationGroupSubjectGradeBucketID The ID of the CalculationGroupSubjectGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupSubjectGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupSubjectGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupSubjectGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupSubjectGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupSubjectGradeBucket <- function(CalculationGroupSubjectGradeBucketID, CalculationGroupSubjectID = F, CalculationGroupGradeBucketID = F, CalculationType = F, EntityGroupKey = F, IsAHistoricRecord = F, HasAssignments = F, HasCategoryWeighting = F, HasSubjectAcademicStandards = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupSubjectGradeBucketIDClonedFrom = F, HasCalculationGroupWeight = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupSubjectGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectGradeBucket", objectId = CalculationGroupSubjectGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupSubjectGradeBucket
	#'
	#' This function deletes a CalculationGroupSubjectGradeBucket
	#' @param CalculationGroupSubjectGradeBucketID The ID of the CalculationGroupSubjectGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupSubjectGradeBucketID of the deleted CalculationGroupSubjectGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupSubjectGradeBucket <- function(CalculationGroupSubjectGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectGradeBucket", objectId = CalculationGroupSubjectGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupSubjectGradeBucket
	#'
	#' This function creates a CalculationGroupSubjectGradeBucket
	#' @param fieldNames The field values to give the created CalculationGroupSubjectGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupSubjectGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupSubjectGradeBucket <- function(CalculationGroupSubjectID = NULL, CalculationGroupGradeBucketID = NULL, CalculationType = NULL, EntityGroupKey = NULL, CalculationGroupSubjectGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectGradeBucket", body = list(DataObject = body), searchFields = append("CalculationGroupSubjectGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupSubjectGradeBucket
	#'
	#' This function modifies a CalculationGroupSubjectGradeBucket
	#' @param fieldNames The field values to give the modified CalculationGroupSubjectGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupSubjectGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupSubjectGradeBucket <- function(CalculationGroupSubjectGradeBucketID, CalculationGroupSubjectID = NULL, CalculationGroupGradeBucketID = NULL, CalculationType = NULL, EntityGroupKey = NULL, CalculationGroupSubjectGradeBucketIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupSubjectGradeBucket", objectId = CalculationGroupSubjectGradeBucketID, body = list(DataObject = body), searchFields = append("CalculationGroupSubjectGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SubjectGradingScaleGroupSubjects
	#'
	#' This function returns a dataframe or json object of SubjectGradingScaleGroupSubjects
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SubjectGradingScaleGroupSubjects. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SubjectGradingScaleGroupSubjects.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SubjectGradingScaleGroupSubject') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SubjectGradingScaleGroupSubjects
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSubjectGradingScaleGroupSubjects <- function(searchConditionsList = NULL, SubjectGradingScaleGroupSubjectID = F, SubjectGradingScaleGroupID = F, CalculationGroupSubjectID = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SubjectGradingScaleGroupSubjectIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SubjectGradingScaleGroupSubject", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SubjectGradingScaleGroupSubject
	#'
	#' This function returns a dataframe or json object of a SubjectGradingScaleGroupSubject
	#' @param SubjectGradingScaleGroupSubjectID The ID of the SubjectGradingScaleGroupSubject to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SubjectGradingScaleGroupSubject. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SubjectGradingScaleGroupSubject.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SubjectGradingScaleGroupSubject') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SubjectGradingScaleGroupSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSubjectGradingScaleGroupSubject <- function(SubjectGradingScaleGroupSubjectID, SubjectGradingScaleGroupID = F, CalculationGroupSubjectID = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SubjectGradingScaleGroupSubjectIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SubjectGradingScaleGroupSubjectID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroupSubject", objectId = SubjectGradingScaleGroupSubjectID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SubjectGradingScaleGroupSubject
	#'
	#' This function deletes a SubjectGradingScaleGroupSubject
	#' @param SubjectGradingScaleGroupSubjectID The ID of the SubjectGradingScaleGroupSubject to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SubjectGradingScaleGroupSubjectID of the deleted SubjectGradingScaleGroupSubject.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSubjectGradingScaleGroupSubject <- function(SubjectGradingScaleGroupSubjectID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroupSubject", objectId = SubjectGradingScaleGroupSubjectID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SubjectGradingScaleGroupSubject
	#'
	#' This function creates a SubjectGradingScaleGroupSubject
	#' @param fieldNames The field values to give the created SubjectGradingScaleGroupSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SubjectGradingScaleGroupSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSubjectGradingScaleGroupSubject <- function(SubjectGradingScaleGroupID = NULL, CalculationGroupSubjectID = NULL, EntityGroupKey = NULL, SubjectGradingScaleGroupSubjectIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroupSubject", body = list(DataObject = body), searchFields = append("SubjectGradingScaleGroupSubjectID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SubjectGradingScaleGroupSubject
	#'
	#' This function modifies a SubjectGradingScaleGroupSubject
	#' @param fieldNames The field values to give the modified SubjectGradingScaleGroupSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SubjectGradingScaleGroupSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySubjectGradingScaleGroupSubject <- function(SubjectGradingScaleGroupSubjectID, SubjectGradingScaleGroupID = NULL, CalculationGroupSubjectID = NULL, EntityGroupKey = NULL, SubjectGradingScaleGroupSubjectIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroupSubject", objectId = SubjectGradingScaleGroupSubjectID, body = list(DataObject = body), searchFields = append("SubjectGradingScaleGroupSubjectID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SubjectGradingScaleGroups
	#'
	#' This function returns a dataframe or json object of SubjectGradingScaleGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SubjectGradingScaleGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SubjectGradingScaleGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SubjectGradingScaleGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SubjectGradingScaleGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSubjectGradingScaleGroups <- function(searchConditionsList = NULL, SubjectGradingScaleGroupID = F, GradingScaleGroupID = F, CalculationGroupID = F, Description = F, EntityGroupKey = F, IsDefaultGroup = F, IsAHistoricRecord = F, SubjectCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SubjectGradingScaleGroupIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SubjectGradingScaleGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SubjectGradingScaleGroup
	#'
	#' This function returns a dataframe or json object of a SubjectGradingScaleGroup
	#' @param SubjectGradingScaleGroupID The ID of the SubjectGradingScaleGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SubjectGradingScaleGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SubjectGradingScaleGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SubjectGradingScaleGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SubjectGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSubjectGradingScaleGroup <- function(SubjectGradingScaleGroupID, GradingScaleGroupID = F, CalculationGroupID = F, Description = F, EntityGroupKey = F, IsDefaultGroup = F, IsAHistoricRecord = F, SubjectCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SubjectGradingScaleGroupIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SubjectGradingScaleGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroup", objectId = SubjectGradingScaleGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SubjectGradingScaleGroup
	#'
	#' This function deletes a SubjectGradingScaleGroup
	#' @param SubjectGradingScaleGroupID The ID of the SubjectGradingScaleGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SubjectGradingScaleGroupID of the deleted SubjectGradingScaleGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSubjectGradingScaleGroup <- function(SubjectGradingScaleGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroup", objectId = SubjectGradingScaleGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SubjectGradingScaleGroup
	#'
	#' This function creates a SubjectGradingScaleGroup
	#' @param fieldNames The field values to give the created SubjectGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SubjectGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSubjectGradingScaleGroup <- function(GradingScaleGroupID = NULL, CalculationGroupID = NULL, Description = NULL, EntityGroupKey = NULL, IsDefaultGroup = NULL, SubjectGradingScaleGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroup", body = list(DataObject = body), searchFields = append("SubjectGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SubjectGradingScaleGroup
	#'
	#' This function modifies a SubjectGradingScaleGroup
	#' @param fieldNames The field values to give the modified SubjectGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SubjectGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySubjectGradingScaleGroup <- function(SubjectGradingScaleGroupID, GradingScaleGroupID = NULL, CalculationGroupID = NULL, Description = NULL, EntityGroupKey = NULL, IsDefaultGroup = NULL, SubjectGradingScaleGroupIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SubjectGradingScaleGroup", objectId = SubjectGradingScaleGroupID, body = list(DataObject = body), searchFields = append("SubjectGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingScaleGroups
	#'
	#' This function returns a dataframe or json object of GradingScaleGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingScaleGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingScaleGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingScaleGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradingScaleGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingScaleGroups <- function(searchConditionsList = NULL, GradingScaleGroupID = F, EntityID = F, SchoolYearID = F, Description = F, EntityGroupKey = F, MaxScore = F, GradingScaleType = F, IsDefaultSubjectGradingScaleGroup = F, IsDefaultAcademicStandardGradingScaleGroup = F, IsAHistoricRecord = F, HasSubjectGradingScaleGroups = F, HasAcademicStandardGradingScaleGroups = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayGradeMarkPercentages = F, GradingScaleGroupIDClonedFrom = F, SectionIDTeacherOverride = F, HasCourseGradingScaleGroups = F, HasSectionGradingScaleGroups = F, IsDummySectionContainer = F, IsSectionRelatedGradingScaleGroup = F, HasStudentGradingScaleGroups = F, IsPointsBasedScale = F, UseAsMastery = F, GradeMarkIDMastered = F, GradingScaleGroupExistsInSpecifcEntity = F, HasGradeMarkScoring = F, HasGradeMarksThatHaveChangedPercent = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "GradingScaleGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingScaleGroup
	#'
	#' This function returns a dataframe or json object of a GradingScaleGroup
	#' @param GradingScaleGroupID The ID of the GradingScaleGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingScaleGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingScaleGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingScaleGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingScaleGroup <- function(GradingScaleGroupID, EntityID = F, SchoolYearID = F, Description = F, EntityGroupKey = F, MaxScore = F, GradingScaleType = F, IsDefaultSubjectGradingScaleGroup = F, IsDefaultAcademicStandardGradingScaleGroup = F, IsAHistoricRecord = F, HasSubjectGradingScaleGroups = F, HasAcademicStandardGradingScaleGroups = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayGradeMarkPercentages = F, GradingScaleGroupIDClonedFrom = F, SectionIDTeacherOverride = F, HasCourseGradingScaleGroups = F, HasSectionGradingScaleGroups = F, IsDummySectionContainer = F, IsSectionRelatedGradingScaleGroup = F, HasStudentGradingScaleGroups = F, IsPointsBasedScale = F, UseAsMastery = F, GradeMarkIDMastered = F, GradingScaleGroupExistsInSpecifcEntity = F, HasGradeMarkScoring = F, HasGradeMarksThatHaveChangedPercent = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingScaleGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "GradingScaleGroup", objectId = GradingScaleGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingScaleGroup
	#'
	#' This function deletes a GradingScaleGroup
	#' @param GradingScaleGroupID The ID of the GradingScaleGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradingScaleGroupID of the deleted GradingScaleGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingScaleGroup <- function(GradingScaleGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "GradingScaleGroup", objectId = GradingScaleGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingScaleGroup
	#'
	#' This function creates a GradingScaleGroup
	#' @param fieldNames The field values to give the created GradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingScaleGroup <- function(EntityID = NULL, SchoolYearID = NULL, Description = NULL, EntityGroupKey = NULL, MaxScore = NULL, GradingScaleType = NULL, IsDefaultSubjectGradingScaleGroup = NULL, IsDefaultAcademicStandardGradingScaleGroup = NULL, DisplayGradeMarkPercentages = NULL, GradingScaleGroupIDClonedFrom = NULL, SectionIDTeacherOverride = NULL, UseAsMastery = NULL, GradeMarkIDMastered = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "GradingScaleGroup", body = list(DataObject = body), searchFields = append("GradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingScaleGroup
	#'
	#' This function modifies a GradingScaleGroup
	#' @param fieldNames The field values to give the modified GradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingScaleGroup <- function(GradingScaleGroupID, EntityID = NULL, SchoolYearID = NULL, Description = NULL, EntityGroupKey = NULL, MaxScore = NULL, GradingScaleType = NULL, IsDefaultSubjectGradingScaleGroup = NULL, IsDefaultAcademicStandardGradingScaleGroup = NULL, DisplayGradeMarkPercentages = NULL, GradingScaleGroupIDClonedFrom = NULL, SectionIDTeacherOverride = NULL, UseAsMastery = NULL, GradeMarkIDMastered = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "GradingScaleGroup", objectId = GradingScaleGroupID, body = list(DataObject = body), searchFields = append("GradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradeBucketSubjects
	#'
	#' This function returns a dataframe or json object of StudentGradeBucketSubjects
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketSubjects. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketSubjects.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketSubject') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGradeBucketSubjects
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradeBucketSubjects <- function(searchConditionsList = NULL, StudentGradeBucketSubjectID = F, StudentGradeBucketID = F, SubjectID = F, SectionID = F, GradeMarkID = F, Percent = F, EarnedPoints = F, PossiblePoints = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsAdminOverride = F, GradeMarkIDOverride = F, GradeMarkOverrideComment = F, PercentAdjustment = F, PercentAdjustmentComment = F, PercentWithAdjustment = F, GradeMarkToUse = F, PercentWithAdjustmentWithGradeMarkToUse = F, PercentWithGradeMark = F, CalculatedPoints = F, IsUsingPointsBasedScale = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGradeBucketSubject", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradeBucketSubject
	#'
	#' This function returns a dataframe or json object of a StudentGradeBucketSubject
	#' @param StudentGradeBucketSubjectID The ID of the StudentGradeBucketSubject to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketSubject. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketSubject.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketSubject') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGradeBucketSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradeBucketSubject <- function(StudentGradeBucketSubjectID, StudentGradeBucketID = F, SubjectID = F, SectionID = F, GradeMarkID = F, Percent = F, EarnedPoints = F, PossiblePoints = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsAdminOverride = F, GradeMarkIDOverride = F, GradeMarkOverrideComment = F, PercentAdjustment = F, PercentAdjustmentComment = F, PercentWithAdjustment = F, GradeMarkToUse = F, PercentWithAdjustmentWithGradeMarkToUse = F, PercentWithGradeMark = F, CalculatedPoints = F, IsUsingPointsBasedScale = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradeBucketSubjectID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGradeBucketSubject", objectId = StudentGradeBucketSubjectID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradeBucketSubject
	#'
	#' This function deletes a StudentGradeBucketSubject
	#' @param StudentGradeBucketSubjectID The ID of the StudentGradeBucketSubject to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGradeBucketSubjectID of the deleted StudentGradeBucketSubject.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradeBucketSubject <- function(StudentGradeBucketSubjectID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGradeBucketSubject", objectId = StudentGradeBucketSubjectID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradeBucketSubject
	#'
	#' This function creates a StudentGradeBucketSubject
	#' @param fieldNames The field values to give the created StudentGradeBucketSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGradeBucketSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradeBucketSubject <- function(StudentGradeBucketID = NULL, SubjectID = NULL, SectionID = NULL, GradeMarkID = NULL, Percent = NULL, EarnedPoints = NULL, PossiblePoints = NULL, IsAdminOverride = NULL, GradeMarkIDOverride = NULL, GradeMarkOverrideComment = NULL, PercentAdjustment = NULL, PercentAdjustmentComment = NULL, CalculatedPoints = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGradeBucketSubject", body = list(DataObject = body), searchFields = append("StudentGradeBucketSubjectID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradeBucketSubject
	#'
	#' This function modifies a StudentGradeBucketSubject
	#' @param fieldNames The field values to give the modified StudentGradeBucketSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGradeBucketSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradeBucketSubject <- function(StudentGradeBucketSubjectID, StudentGradeBucketID = NULL, SubjectID = NULL, SectionID = NULL, GradeMarkID = NULL, Percent = NULL, EarnedPoints = NULL, PossiblePoints = NULL, IsAdminOverride = NULL, GradeMarkIDOverride = NULL, GradeMarkOverrideComment = NULL, PercentAdjustment = NULL, PercentAdjustmentComment = NULL, CalculatedPoints = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGradeBucketSubject", objectId = StudentGradeBucketSubjectID, body = list(DataObject = body), searchFields = append("StudentGradeBucketSubjectID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionStudentSectionSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionStudentSectionSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionStudentSectionSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionStudentSectionSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionStudentSectionSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionStudentSectionSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionStudentSectionSettings <- function(searchConditionsList = NULL, TeacherSectionStudentSectionSettingID = F, TeacherSectionSettingID = F, StudentSectionID = F, DisplayOrder = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionStudentSectionSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionStudentSectionSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionStudentSectionSetting
	#' @param TeacherSectionStudentSectionSettingID The ID of the TeacherSectionStudentSectionSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionStudentSectionSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionStudentSectionSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionStudentSectionSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionStudentSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionStudentSectionSetting <- function(TeacherSectionStudentSectionSettingID, TeacherSectionSettingID = F, StudentSectionID = F, DisplayOrder = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionStudentSectionSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionStudentSectionSetting", objectId = TeacherSectionStudentSectionSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionStudentSectionSetting
	#'
	#' This function deletes a TeacherSectionStudentSectionSetting
	#' @param TeacherSectionStudentSectionSettingID The ID of the TeacherSectionStudentSectionSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionStudentSectionSettingID of the deleted TeacherSectionStudentSectionSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionStudentSectionSetting <- function(TeacherSectionStudentSectionSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionStudentSectionSetting", objectId = TeacherSectionStudentSectionSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionStudentSectionSetting
	#'
	#' This function creates a TeacherSectionStudentSectionSetting
	#' @param fieldNames The field values to give the created TeacherSectionStudentSectionSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionStudentSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionStudentSectionSetting <- function(TeacherSectionSettingID = NULL, StudentSectionID = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionStudentSectionSetting", body = list(DataObject = body), searchFields = append("TeacherSectionStudentSectionSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionStudentSectionSetting
	#'
	#' This function modifies a TeacherSectionStudentSectionSetting
	#' @param fieldNames The field values to give the modified TeacherSectionStudentSectionSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionStudentSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionStudentSectionSetting <- function(TeacherSectionStudentSectionSettingID, TeacherSectionSettingID = NULL, StudentSectionID = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionStudentSectionSetting", objectId = TeacherSectionStudentSectionSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionStudentSectionSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookTempAssignmentErrors
	#'
	#' This function returns a dataframe or json object of GradebookTempAssignmentErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookTempAssignmentErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookTempAssignmentErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookTempAssignmentError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookTempAssignmentErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookTempAssignmentErrors <- function(searchConditionsList = NULL, TempAssignmentErrorID = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, TempSectionAssignmentID = F, ErrorNumber = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempAssignmentError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookTempAssignmentError
	#'
	#' This function returns a dataframe or json object of a GradebookTempAssignmentError
	#' @param GradebookTempAssignmentErrorID The ID of the GradebookTempAssignmentError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookTempAssignmentError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookTempAssignmentError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookTempAssignmentError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookTempAssignmentError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookTempAssignmentError <- function(GradebookTempAssignmentErrorID, TempAssignmentErrorID = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, TempSectionAssignmentID = F, ErrorNumber = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookTempAssignmentErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempAssignmentError", objectId = GradebookTempAssignmentErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookTempAssignmentError
	#'
	#' This function deletes a GradebookTempAssignmentError
	#' @param GradebookTempAssignmentErrorID The ID of the GradebookTempAssignmentError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookTempAssignmentErrorID of the deleted GradebookTempAssignmentError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookTempAssignmentError <- function(GradebookTempAssignmentErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempAssignmentError", objectId = GradebookTempAssignmentErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookTempAssignmentError
	#'
	#' This function creates a GradebookTempAssignmentError
	#' @param fieldNames The field values to give the created GradebookTempAssignmentError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookTempAssignmentError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookTempAssignmentError <- function(ErrorText = NULL, TempSectionAssignmentID = NULL, ErrorNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempAssignmentError", body = list(DataObject = body), searchFields = append("TempAssignmentErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookTempAssignmentError
	#'
	#' This function modifies a GradebookTempAssignmentError
	#' @param fieldNames The field values to give the modified GradebookTempAssignmentError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookTempAssignmentError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookTempAssignmentError <- function(TempAssignmentErrorID, ErrorText = NULL, TempSectionAssignmentID = NULL, ErrorNumber = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempAssignmentError", objectId = TempAssignmentErrorID, body = list(DataObject = body), searchFields = append("TempAssignmentErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionStandardsDisplayGradeBucketSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionStandardsDisplayGradeBucketSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionStandardsDisplayGradeBucketSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionStandardsDisplayGradeBucketSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionStandardsDisplayGradeBucketSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionStandardsDisplayGradeBucketSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionStandardsDisplayGradeBucketSettings <- function(searchConditionsList = NULL, TeacherSectionStandardsDisplayGradeBucketSettingID = F, TeacherSectionSettingID = F, CalculationGroupGradeBucketID = F, ShowBucket = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayGradeBucketSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionStandardsDisplayGradeBucketSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionStandardsDisplayGradeBucketSetting
	#' @param TeacherSectionStandardsDisplayGradeBucketSettingID The ID of the TeacherSectionStandardsDisplayGradeBucketSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionStandardsDisplayGradeBucketSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionStandardsDisplayGradeBucketSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionStandardsDisplayGradeBucketSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionStandardsDisplayGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionStandardsDisplayGradeBucketSetting <- function(TeacherSectionStandardsDisplayGradeBucketSettingID, TeacherSectionSettingID = F, CalculationGroupGradeBucketID = F, ShowBucket = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionStandardsDisplayGradeBucketSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayGradeBucketSetting", objectId = TeacherSectionStandardsDisplayGradeBucketSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionStandardsDisplayGradeBucketSetting
	#'
	#' This function deletes a TeacherSectionStandardsDisplayGradeBucketSetting
	#' @param TeacherSectionStandardsDisplayGradeBucketSettingID The ID of the TeacherSectionStandardsDisplayGradeBucketSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionStandardsDisplayGradeBucketSettingID of the deleted TeacherSectionStandardsDisplayGradeBucketSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionStandardsDisplayGradeBucketSetting <- function(TeacherSectionStandardsDisplayGradeBucketSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayGradeBucketSetting", objectId = TeacherSectionStandardsDisplayGradeBucketSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionStandardsDisplayGradeBucketSetting
	#'
	#' This function creates a TeacherSectionStandardsDisplayGradeBucketSetting
	#' @param fieldNames The field values to give the created TeacherSectionStandardsDisplayGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionStandardsDisplayGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionStandardsDisplayGradeBucketSetting <- function(TeacherSectionSettingID = NULL, CalculationGroupGradeBucketID = NULL, ShowBucket = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayGradeBucketSetting", body = list(DataObject = body), searchFields = append("TeacherSectionStandardsDisplayGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionStandardsDisplayGradeBucketSetting
	#'
	#' This function modifies a TeacherSectionStandardsDisplayGradeBucketSetting
	#' @param fieldNames The field values to give the modified TeacherSectionStandardsDisplayGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionStandardsDisplayGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionStandardsDisplayGradeBucketSetting <- function(TeacherSectionStandardsDisplayGradeBucketSettingID, TeacherSectionSettingID = NULL, CalculationGroupGradeBucketID = NULL, ShowBucket = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayGradeBucketSetting", objectId = TeacherSectionStandardsDisplayGradeBucketSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionStandardsDisplayGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempAdjustedCategories
	#'
	#' This function returns a dataframe or json object of TempAdjustedCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempAdjustedCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempAdjustedCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempAdjustedCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempAdjustedCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempAdjustedCategories <- function(searchConditionsList = NULL, TempAdjustedCategoryID = F, TempSectionCategoryID = F, CategoryID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempAdjustedCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempAdjustedCategory
	#'
	#' This function returns a dataframe or json object of a TempAdjustedCategory
	#' @param TempAdjustedCategoryID The ID of the TempAdjustedCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempAdjustedCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempAdjustedCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempAdjustedCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempAdjustedCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempAdjustedCategory <- function(TempAdjustedCategoryID, TempSectionCategoryID = F, CategoryID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempAdjustedCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempAdjustedCategory", objectId = TempAdjustedCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempAdjustedCategory
	#'
	#' This function deletes a TempAdjustedCategory
	#' @param TempAdjustedCategoryID The ID of the TempAdjustedCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempAdjustedCategoryID of the deleted TempAdjustedCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempAdjustedCategory <- function(TempAdjustedCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempAdjustedCategory", objectId = TempAdjustedCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempAdjustedCategory
	#'
	#' This function creates a TempAdjustedCategory
	#' @param fieldNames The field values to give the created TempAdjustedCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempAdjustedCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempAdjustedCategory <- function(TempSectionCategoryID = NULL, CategoryID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempAdjustedCategory", body = list(DataObject = body), searchFields = append("TempAdjustedCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempAdjustedCategory
	#'
	#' This function modifies a TempAdjustedCategory
	#' @param fieldNames The field values to give the modified TempAdjustedCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempAdjustedCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempAdjustedCategory <- function(TempAdjustedCategoryID, TempSectionCategoryID = NULL, CategoryID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempAdjustedCategory", objectId = TempAdjustedCategoryID, body = list(DataObject = body), searchFields = append("TempAdjustedCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentAssignments
	#'
	#' This function returns a dataframe or json object of TempStudentAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempStudentAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentAssignments <- function(searchConditionsList = NULL, TempStudentAssignmentID = F, StudentSectionID = F, AssignmentID = F, SectionID = F, Score = F, MaxScore = F, Weight = F, AssignmentName = F, AssignmentDescription = F, DueDate = F, Include = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsMissing = F, NoCount = F, ScoreClarifierID = F, ScoreClarifierCode = F, DueDateIsInGivenDateRange = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempStudentAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentAssignment
	#'
	#' This function returns a dataframe or json object of a TempStudentAssignment
	#' @param TempStudentAssignmentID The ID of the TempStudentAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentAssignment <- function(TempStudentAssignmentID, StudentSectionID = F, AssignmentID = F, SectionID = F, Score = F, MaxScore = F, Weight = F, AssignmentName = F, AssignmentDescription = F, DueDate = F, Include = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsMissing = F, NoCount = F, ScoreClarifierID = F, ScoreClarifierCode = F, DueDateIsInGivenDateRange = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempStudentAssignment", objectId = TempStudentAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentAssignment
	#'
	#' This function deletes a TempStudentAssignment
	#' @param TempStudentAssignmentID The ID of the TempStudentAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempStudentAssignmentID of the deleted TempStudentAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentAssignment <- function(TempStudentAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempStudentAssignment", objectId = TempStudentAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentAssignment
	#'
	#' This function creates a TempStudentAssignment
	#' @param fieldNames The field values to give the created TempStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentAssignment <- function(StudentSectionID = NULL, AssignmentID = NULL, SectionID = NULL, Score = NULL, MaxScore = NULL, Weight = NULL, AssignmentName = NULL, AssignmentDescription = NULL, DueDate = NULL, Include = NULL, IsMissing = NULL, NoCount = NULL, ScoreClarifierID = NULL, ScoreClarifierCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempStudentAssignment", body = list(DataObject = body), searchFields = append("TempStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentAssignment
	#'
	#' This function modifies a TempStudentAssignment
	#' @param fieldNames The field values to give the modified TempStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentAssignment <- function(TempStudentAssignmentID, StudentSectionID = NULL, AssignmentID = NULL, SectionID = NULL, Score = NULL, MaxScore = NULL, Weight = NULL, AssignmentName = NULL, AssignmentDescription = NULL, DueDate = NULL, Include = NULL, IsMissing = NULL, NoCount = NULL, ScoreClarifierID = NULL, ScoreClarifierCode = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempStudentAssignment", objectId = TempStudentAssignmentID, body = list(DataObject = body), searchFields = append("TempStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradingPeriodGradeBucketErrors
	#'
	#' This function returns a dataframe or json object of TempGradingPeriodGradeBucketErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriodGradeBucketErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriodGradeBucketErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriodGradeBucketError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempGradingPeriodGradeBucketErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradingPeriodGradeBucketErrors <- function(searchConditionsList = NULL, TempGradingPeriodGradeBucketErrorID = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SubjectOrStandardDescription = F, SubjectOrStandardKey = F, CalculationType = F, GradingPeriodGradeBucketID = F, ErrorNumber = F, GradeBucketLabelWithDates = F, StartDate = F, EndDate = F, StatusDisplayWithExtensionDate = F, SectionLengthCodeDescription = F, CalculationGroupID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempGradingPeriodGradeBucketError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradingPeriodGradeBucketError
	#'
	#' This function returns a dataframe or json object of a TempGradingPeriodGradeBucketError
	#' @param TempGradingPeriodGradeBucketErrorID The ID of the TempGradingPeriodGradeBucketError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriodGradeBucketError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriodGradeBucketError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriodGradeBucketError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempGradingPeriodGradeBucketError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradingPeriodGradeBucketError <- function(TempGradingPeriodGradeBucketErrorID, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SubjectOrStandardDescription = F, SubjectOrStandardKey = F, CalculationType = F, GradingPeriodGradeBucketID = F, ErrorNumber = F, GradeBucketLabelWithDates = F, StartDate = F, EndDate = F, StatusDisplayWithExtensionDate = F, SectionLengthCodeDescription = F, CalculationGroupID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradingPeriodGradeBucketErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucketError", objectId = TempGradingPeriodGradeBucketErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradingPeriodGradeBucketError
	#'
	#' This function deletes a TempGradingPeriodGradeBucketError
	#' @param TempGradingPeriodGradeBucketErrorID The ID of the TempGradingPeriodGradeBucketError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempGradingPeriodGradeBucketErrorID of the deleted TempGradingPeriodGradeBucketError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradingPeriodGradeBucketError <- function(TempGradingPeriodGradeBucketErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucketError", objectId = TempGradingPeriodGradeBucketErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradingPeriodGradeBucketError
	#'
	#' This function creates a TempGradingPeriodGradeBucketError
	#' @param fieldNames The field values to give the created TempGradingPeriodGradeBucketError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempGradingPeriodGradeBucketError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradingPeriodGradeBucketError <- function(ErrorText = NULL, SubjectOrStandardDescription = NULL, SubjectOrStandardKey = NULL, CalculationType = NULL, GradingPeriodGradeBucketID = NULL, ErrorNumber = NULL, GradeBucketLabelWithDates = NULL, StartDate = NULL, EndDate = NULL, StatusDisplayWithExtensionDate = NULL, SectionLengthCodeDescription = NULL, CalculationGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucketError", body = list(DataObject = body), searchFields = append("TempGradingPeriodGradeBucketErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradingPeriodGradeBucketError
	#'
	#' This function modifies a TempGradingPeriodGradeBucketError
	#' @param fieldNames The field values to give the modified TempGradingPeriodGradeBucketError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempGradingPeriodGradeBucketError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradingPeriodGradeBucketError <- function(TempGradingPeriodGradeBucketErrorID, ErrorText = NULL, SubjectOrStandardDescription = NULL, SubjectOrStandardKey = NULL, CalculationType = NULL, GradingPeriodGradeBucketID = NULL, ErrorNumber = NULL, GradeBucketLabelWithDates = NULL, StartDate = NULL, EndDate = NULL, StatusDisplayWithExtensionDate = NULL, SectionLengthCodeDescription = NULL, CalculationGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucketError", objectId = TempGradingPeriodGradeBucketErrorID, body = list(DataObject = body), searchFields = append("TempGradingPeriodGradeBucketErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionStandardsDisplayAcademicStandardSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionStandardsDisplayAcademicStandardSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionStandardsDisplayAcademicStandardSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionStandardsDisplayAcademicStandardSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionStandardsDisplayAcademicStandardSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionStandardsDisplayAcademicStandardSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionStandardsDisplayAcademicStandardSettings <- function(searchConditionsList = NULL, TeacherSectionStandardsDisplayAcademicStandardSettingID = F, AcademicStandardID = F, TeacherSectionStandardsDisplayGradeBucketSettingID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayAcademicStandardSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionStandardsDisplayAcademicStandardSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionStandardsDisplayAcademicStandardSetting
	#' @param TeacherSectionStandardsDisplayAcademicStandardSettingID The ID of the TeacherSectionStandardsDisplayAcademicStandardSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionStandardsDisplayAcademicStandardSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionStandardsDisplayAcademicStandardSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionStandardsDisplayAcademicStandardSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionStandardsDisplayAcademicStandardSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionStandardsDisplayAcademicStandardSetting <- function(TeacherSectionStandardsDisplayAcademicStandardSettingID, AcademicStandardID = F, TeacherSectionStandardsDisplayGradeBucketSettingID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionStandardsDisplayAcademicStandardSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayAcademicStandardSetting", objectId = TeacherSectionStandardsDisplayAcademicStandardSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionStandardsDisplayAcademicStandardSetting
	#'
	#' This function deletes a TeacherSectionStandardsDisplayAcademicStandardSetting
	#' @param TeacherSectionStandardsDisplayAcademicStandardSettingID The ID of the TeacherSectionStandardsDisplayAcademicStandardSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionStandardsDisplayAcademicStandardSettingID of the deleted TeacherSectionStandardsDisplayAcademicStandardSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionStandardsDisplayAcademicStandardSetting <- function(TeacherSectionStandardsDisplayAcademicStandardSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayAcademicStandardSetting", objectId = TeacherSectionStandardsDisplayAcademicStandardSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionStandardsDisplayAcademicStandardSetting
	#'
	#' This function creates a TeacherSectionStandardsDisplayAcademicStandardSetting
	#' @param fieldNames The field values to give the created TeacherSectionStandardsDisplayAcademicStandardSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionStandardsDisplayAcademicStandardSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionStandardsDisplayAcademicStandardSetting <- function(AcademicStandardID = NULL, TeacherSectionStandardsDisplayGradeBucketSettingID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayAcademicStandardSetting", body = list(DataObject = body), searchFields = append("TeacherSectionStandardsDisplayAcademicStandardSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionStandardsDisplayAcademicStandardSetting
	#'
	#' This function modifies a TeacherSectionStandardsDisplayAcademicStandardSetting
	#' @param fieldNames The field values to give the modified TeacherSectionStandardsDisplayAcademicStandardSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionStandardsDisplayAcademicStandardSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionStandardsDisplayAcademicStandardSetting <- function(TeacherSectionStandardsDisplayAcademicStandardSettingID, AcademicStandardID = NULL, TeacherSectionStandardsDisplayGradeBucketSettingID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionStandardsDisplayAcademicStandardSetting", objectId = TeacherSectionStandardsDisplayAcademicStandardSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionStandardsDisplayAcademicStandardSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradeBucketAcademicStandards
	#'
	#' This function returns a dataframe or json object of StudentGradeBucketAcademicStandards
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketAcademicStandards. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketAcademicStandards.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketAcademicStandard') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGradeBucketAcademicStandards
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradeBucketAcademicStandards <- function(searchConditionsList = NULL, StudentGradeBucketAcademicStandardID = F, StudentGradeBucketID = F, AcademicStandardID = F, GradeMarkID = F, Percent = F, EarnedPoints = F, PossiblePoints = F, SectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsAdminOverride = F, GradeMarkIDOverride = F, GradeMarkOverrideComment = F, PercentAdjustment = F, PercentAdjustmentComment = F, PercentWithAdjustment = F, PercentWithAdjustmentWithGradeMarkToUse = F, PercentWithGradeMark = F, GradeMarkToUse = F, CalculatedPoints = F, IsUsingPointsBasedScale = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGradeBucketAcademicStandard", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradeBucketAcademicStandard
	#'
	#' This function returns a dataframe or json object of a StudentGradeBucketAcademicStandard
	#' @param StudentGradeBucketAcademicStandardID The ID of the StudentGradeBucketAcademicStandard to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketAcademicStandard. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketAcademicStandard.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketAcademicStandard') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGradeBucketAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradeBucketAcademicStandard <- function(StudentGradeBucketAcademicStandardID, StudentGradeBucketID = F, AcademicStandardID = F, GradeMarkID = F, Percent = F, EarnedPoints = F, PossiblePoints = F, SectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsAdminOverride = F, GradeMarkIDOverride = F, GradeMarkOverrideComment = F, PercentAdjustment = F, PercentAdjustmentComment = F, PercentWithAdjustment = F, PercentWithAdjustmentWithGradeMarkToUse = F, PercentWithGradeMark = F, GradeMarkToUse = F, CalculatedPoints = F, IsUsingPointsBasedScale = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradeBucketAcademicStandardID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGradeBucketAcademicStandard", objectId = StudentGradeBucketAcademicStandardID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradeBucketAcademicStandard
	#'
	#' This function deletes a StudentGradeBucketAcademicStandard
	#' @param StudentGradeBucketAcademicStandardID The ID of the StudentGradeBucketAcademicStandard to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGradeBucketAcademicStandardID of the deleted StudentGradeBucketAcademicStandard.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradeBucketAcademicStandard <- function(StudentGradeBucketAcademicStandardID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGradeBucketAcademicStandard", objectId = StudentGradeBucketAcademicStandardID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradeBucketAcademicStandard
	#'
	#' This function creates a StudentGradeBucketAcademicStandard
	#' @param fieldNames The field values to give the created StudentGradeBucketAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGradeBucketAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradeBucketAcademicStandard <- function(StudentGradeBucketID = NULL, AcademicStandardID = NULL, GradeMarkID = NULL, Percent = NULL, EarnedPoints = NULL, PossiblePoints = NULL, SectionID = NULL, IsAdminOverride = NULL, GradeMarkIDOverride = NULL, GradeMarkOverrideComment = NULL, PercentAdjustment = NULL, PercentAdjustmentComment = NULL, CalculatedPoints = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGradeBucketAcademicStandard", body = list(DataObject = body), searchFields = append("StudentGradeBucketAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradeBucketAcademicStandard
	#'
	#' This function modifies a StudentGradeBucketAcademicStandard
	#' @param fieldNames The field values to give the modified StudentGradeBucketAcademicStandard. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGradeBucketAcademicStandard
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradeBucketAcademicStandard <- function(StudentGradeBucketAcademicStandardID, StudentGradeBucketID = NULL, AcademicStandardID = NULL, GradeMarkID = NULL, Percent = NULL, EarnedPoints = NULL, PossiblePoints = NULL, SectionID = NULL, IsAdminOverride = NULL, GradeMarkIDOverride = NULL, GradeMarkOverrideComment = NULL, PercentAdjustment = NULL, PercentAdjustmentComment = NULL, CalculatedPoints = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGradeBucketAcademicStandard", objectId = StudentGradeBucketAcademicStandardID, body = list(DataObject = body), searchFields = append("StudentGradeBucketAcademicStandardID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseMoveCourses
	#'
	#' This function returns a dataframe or json object of TempCourseMoveCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMoveCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMoveCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMoveCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCourseMoveCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseMoveCourses <- function(searchConditionsList = NULL, TempCourseMoveCourseID = F, CourseID = F, CourseCode = F, CourseDescription = F, OriginalCalculationGroupID = F, OriginalCalculationGroupDescription = F, NewCalculationGroupID = F, NewCalculationGroupDescription = F, IsValidUpdate = F, EntityID = F, SchoolYearID = F, GroupCourseID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CodeDescription = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCourseMoveCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseMoveCourse
	#'
	#' This function returns a dataframe or json object of a TempCourseMoveCourse
	#' @param TempCourseMoveCourseID The ID of the TempCourseMoveCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMoveCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMoveCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMoveCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCourseMoveCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseMoveCourse <- function(TempCourseMoveCourseID, CourseID = F, CourseCode = F, CourseDescription = F, OriginalCalculationGroupID = F, OriginalCalculationGroupDescription = F, NewCalculationGroupID = F, NewCalculationGroupDescription = F, IsValidUpdate = F, EntityID = F, SchoolYearID = F, GroupCourseID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CodeDescription = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseMoveCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCourseMoveCourse", objectId = TempCourseMoveCourseID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseMoveCourse
	#'
	#' This function deletes a TempCourseMoveCourse
	#' @param TempCourseMoveCourseID The ID of the TempCourseMoveCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCourseMoveCourseID of the deleted TempCourseMoveCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseMoveCourse <- function(TempCourseMoveCourseID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCourseMoveCourse", objectId = TempCourseMoveCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseMoveCourse
	#'
	#' This function creates a TempCourseMoveCourse
	#' @param fieldNames The field values to give the created TempCourseMoveCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCourseMoveCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseMoveCourse <- function(CourseID = NULL, CourseCode = NULL, CourseDescription = NULL, OriginalCalculationGroupID = NULL, OriginalCalculationGroupDescription = NULL, NewCalculationGroupID = NULL, NewCalculationGroupDescription = NULL, IsValidUpdate = NULL, EntityID = NULL, SchoolYearID = NULL, GroupCourseID = NULL, CodeDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCourseMoveCourse", body = list(DataObject = body), searchFields = append("TempCourseMoveCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseMoveCourse
	#'
	#' This function modifies a TempCourseMoveCourse
	#' @param fieldNames The field values to give the modified TempCourseMoveCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCourseMoveCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseMoveCourse <- function(TempCourseMoveCourseID, CourseID = NULL, CourseCode = NULL, CourseDescription = NULL, OriginalCalculationGroupID = NULL, OriginalCalculationGroupDescription = NULL, NewCalculationGroupID = NULL, NewCalculationGroupDescription = NULL, IsValidUpdate = NULL, EntityID = NULL, SchoolYearID = NULL, GroupCourseID = NULL, CodeDescription = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCourseMoveCourse", objectId = TempCourseMoveCourseID, body = list(DataObject = body), searchFields = append("TempCourseMoveCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseMoveErrors
	#'
	#' This function returns a dataframe or json object of TempCourseMoveErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMoveErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMoveErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMoveError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCourseMoveErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseMoveErrors <- function(searchConditionsList = NULL, TempCourseMoveErrorID = F, SectionID = F, BucketLabel = F, ErrorCode = F, ErrorMessage = F, ErrorType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCourseMoveError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseMoveError
	#'
	#' This function returns a dataframe or json object of a TempCourseMoveError
	#' @param TempCourseMoveErrorID The ID of the TempCourseMoveError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMoveError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMoveError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMoveError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCourseMoveError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseMoveError <- function(TempCourseMoveErrorID, SectionID = F, BucketLabel = F, ErrorCode = F, ErrorMessage = F, ErrorType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseMoveErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCourseMoveError", objectId = TempCourseMoveErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseMoveError
	#'
	#' This function deletes a TempCourseMoveError
	#' @param TempCourseMoveErrorID The ID of the TempCourseMoveError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCourseMoveErrorID of the deleted TempCourseMoveError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseMoveError <- function(TempCourseMoveErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCourseMoveError", objectId = TempCourseMoveErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseMoveError
	#'
	#' This function creates a TempCourseMoveError
	#' @param fieldNames The field values to give the created TempCourseMoveError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCourseMoveError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseMoveError <- function(SectionID = NULL, BucketLabel = NULL, ErrorCode = NULL, ErrorMessage = NULL, ErrorType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCourseMoveError", body = list(DataObject = body), searchFields = append("TempCourseMoveErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseMoveError
	#'
	#' This function modifies a TempCourseMoveError
	#' @param fieldNames The field values to give the modified TempCourseMoveError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCourseMoveError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseMoveError <- function(TempCourseMoveErrorID, SectionID = NULL, BucketLabel = NULL, ErrorCode = NULL, ErrorMessage = NULL, ErrorType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCourseMoveError", objectId = TempCourseMoveErrorID, body = list(DataObject = body), searchFields = append("TempCourseMoveErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseMoveSectionErrors
	#'
	#' This function returns a dataframe or json object of TempCourseMoveSectionErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMoveSectionErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMoveSectionErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMoveSectionError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCourseMoveSectionErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseMoveSectionErrors <- function(searchConditionsList = NULL, TempCourseMoveSectionErrorID = F, CourseID = F, SectionID = F, SectionCode = F, ErrorMessage = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCourseMoveSectionError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseMoveSectionError
	#'
	#' This function returns a dataframe or json object of a TempCourseMoveSectionError
	#' @param TempCourseMoveSectionErrorID The ID of the TempCourseMoveSectionError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMoveSectionError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMoveSectionError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMoveSectionError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCourseMoveSectionError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseMoveSectionError <- function(TempCourseMoveSectionErrorID, CourseID = F, SectionID = F, SectionCode = F, ErrorMessage = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseMoveSectionErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCourseMoveSectionError", objectId = TempCourseMoveSectionErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseMoveSectionError
	#'
	#' This function deletes a TempCourseMoveSectionError
	#' @param TempCourseMoveSectionErrorID The ID of the TempCourseMoveSectionError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCourseMoveSectionErrorID of the deleted TempCourseMoveSectionError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseMoveSectionError <- function(TempCourseMoveSectionErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCourseMoveSectionError", objectId = TempCourseMoveSectionErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseMoveSectionError
	#'
	#' This function creates a TempCourseMoveSectionError
	#' @param fieldNames The field values to give the created TempCourseMoveSectionError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCourseMoveSectionError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseMoveSectionError <- function(CourseID = NULL, SectionID = NULL, SectionCode = NULL, ErrorMessage = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCourseMoveSectionError", body = list(DataObject = body), searchFields = append("TempCourseMoveSectionErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseMoveSectionError
	#'
	#' This function modifies a TempCourseMoveSectionError
	#' @param fieldNames The field values to give the modified TempCourseMoveSectionError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCourseMoveSectionError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseMoveSectionError <- function(TempCourseMoveSectionErrorID, CourseID = NULL, SectionID = NULL, SectionCode = NULL, ErrorMessage = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCourseMoveSectionError", objectId = TempCourseMoveSectionErrorID, body = list(DataObject = body), searchFields = append("TempCourseMoveSectionErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradeMarkErrors
	#'
	#' This function returns a dataframe or json object of TempGradeMarkErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeMarkErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeMarkErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeMarkError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempGradeMarkErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradeMarkErrors <- function(searchConditionsList = NULL, TempGradeMarkErrorID = F, GradeMarkID = F, GradeMarkCode = F, SectionID = F, DisplayOrder = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempGradeMarkError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradeMarkError
	#'
	#' This function returns a dataframe or json object of a TempGradeMarkError
	#' @param TempGradeMarkErrorID The ID of the TempGradeMarkError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradeMarkError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradeMarkError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradeMarkError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempGradeMarkError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradeMarkError <- function(TempGradeMarkErrorID, GradeMarkID = F, GradeMarkCode = F, SectionID = F, DisplayOrder = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradeMarkErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempGradeMarkError", objectId = TempGradeMarkErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradeMarkError
	#'
	#' This function deletes a TempGradeMarkError
	#' @param TempGradeMarkErrorID The ID of the TempGradeMarkError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempGradeMarkErrorID of the deleted TempGradeMarkError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradeMarkError <- function(TempGradeMarkErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempGradeMarkError", objectId = TempGradeMarkErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradeMarkError
	#'
	#' This function creates a TempGradeMarkError
	#' @param fieldNames The field values to give the created TempGradeMarkError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempGradeMarkError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradeMarkError <- function(GradeMarkID = NULL, GradeMarkCode = NULL, SectionID = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempGradeMarkError", body = list(DataObject = body), searchFields = append("TempGradeMarkErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradeMarkError
	#'
	#' This function modifies a TempGradeMarkError
	#' @param fieldNames The field values to give the modified TempGradeMarkError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempGradeMarkError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradeMarkError <- function(TempGradeMarkErrorID, GradeMarkID = NULL, GradeMarkCode = NULL, SectionID = NULL, DisplayOrder = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempGradeMarkError", objectId = TempGradeMarkErrorID, body = list(DataObject = body), searchFields = append("TempGradeMarkErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionGradingScaleGroupToCreates
	#'
	#' This function returns a dataframe or json object of TempSectionGradingScaleGroupToCreates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradingScaleGroupToCreates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradingScaleGroupToCreates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradingScaleGroupToCreate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempSectionGradingScaleGroupToCreates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionGradingScaleGroupToCreates <- function(searchConditionsList = NULL, TempSectionGradingScaleGroupToCreateID = F, CourseGradingScaleGroupID = F, CourseGradingScaleGroupCourseID = F, NewCourseGradingScaleGroupID = F, NewCourseGradingScaleGroupDescription = F, SectionID = F, CourseID = F, SectionCode = F, Description = F, RequiresSectionGradingScaleGroups = F, Reason = F, GradingPeriodGradeBucketsToRetainJson = F, IsValidInNewGroup = F, SectionLengthCode = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CourseCode = F, InvalidGradeMarks = F, CourseDescriptionCodeSectionCode = F, StaffFullNameLFM = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempSectionGradingScaleGroupToCreate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionGradingScaleGroupToCreate
	#'
	#' This function returns a dataframe or json object of a TempSectionGradingScaleGroupToCreate
	#' @param TempSectionGradingScaleGroupToCreateID The ID of the TempSectionGradingScaleGroupToCreate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradingScaleGroupToCreate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradingScaleGroupToCreate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradingScaleGroupToCreate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempSectionGradingScaleGroupToCreate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionGradingScaleGroupToCreate <- function(TempSectionGradingScaleGroupToCreateID, CourseGradingScaleGroupID = F, CourseGradingScaleGroupCourseID = F, NewCourseGradingScaleGroupID = F, NewCourseGradingScaleGroupDescription = F, SectionID = F, CourseID = F, SectionCode = F, Description = F, RequiresSectionGradingScaleGroups = F, Reason = F, GradingPeriodGradeBucketsToRetainJson = F, IsValidInNewGroup = F, SectionLengthCode = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CourseCode = F, InvalidGradeMarks = F, CourseDescriptionCodeSectionCode = F, StaffFullNameLFM = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionGradingScaleGroupToCreateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempSectionGradingScaleGroupToCreate", objectId = TempSectionGradingScaleGroupToCreateID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionGradingScaleGroupToCreate
	#'
	#' This function deletes a TempSectionGradingScaleGroupToCreate
	#' @param TempSectionGradingScaleGroupToCreateID The ID of the TempSectionGradingScaleGroupToCreate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempSectionGradingScaleGroupToCreateID of the deleted TempSectionGradingScaleGroupToCreate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionGradingScaleGroupToCreate <- function(TempSectionGradingScaleGroupToCreateID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempSectionGradingScaleGroupToCreate", objectId = TempSectionGradingScaleGroupToCreateID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionGradingScaleGroupToCreate
	#'
	#' This function creates a TempSectionGradingScaleGroupToCreate
	#' @param fieldNames The field values to give the created TempSectionGradingScaleGroupToCreate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempSectionGradingScaleGroupToCreate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionGradingScaleGroupToCreate <- function(CourseGradingScaleGroupID = NULL, CourseGradingScaleGroupCourseID = NULL, NewCourseGradingScaleGroupID = NULL, NewCourseGradingScaleGroupDescription = NULL, SectionID = NULL, CourseID = NULL, SectionCode = NULL, Description = NULL, RequiresSectionGradingScaleGroups = NULL, Reason = NULL, GradingPeriodGradeBucketsToRetainJson = NULL, IsValidInNewGroup = NULL, SectionLengthCode = NULL, CourseCode = NULL, InvalidGradeMarks = NULL, CourseDescriptionCodeSectionCode = NULL, StaffFullNameLFM = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempSectionGradingScaleGroupToCreate", body = list(DataObject = body), searchFields = append("TempSectionGradingScaleGroupToCreateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionGradingScaleGroupToCreate
	#'
	#' This function modifies a TempSectionGradingScaleGroupToCreate
	#' @param fieldNames The field values to give the modified TempSectionGradingScaleGroupToCreate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempSectionGradingScaleGroupToCreate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionGradingScaleGroupToCreate <- function(TempSectionGradingScaleGroupToCreateID, CourseGradingScaleGroupID = NULL, CourseGradingScaleGroupCourseID = NULL, NewCourseGradingScaleGroupID = NULL, NewCourseGradingScaleGroupDescription = NULL, SectionID = NULL, CourseID = NULL, SectionCode = NULL, Description = NULL, RequiresSectionGradingScaleGroups = NULL, Reason = NULL, GradingPeriodGradeBucketsToRetainJson = NULL, IsValidInNewGroup = NULL, SectionLengthCode = NULL, CourseCode = NULL, InvalidGradeMarks = NULL, CourseDescriptionCodeSectionCode = NULL, StaffFullNameLFM = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempSectionGradingScaleGroupToCreate", objectId = TempSectionGradingScaleGroupToCreateID, body = list(DataObject = body), searchFields = append("TempSectionGradingScaleGroupToCreateID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionCategoryAnalyticsSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionCategoryAnalyticsSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionCategoryAnalyticsSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionCategoryAnalyticsSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionCategoryAnalyticsSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionCategoryAnalyticsSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionCategoryAnalyticsSettings <- function(searchConditionsList = NULL, TeacherSectionCategoryAnalyticsSettingID = F, CategoryID = F, TeacherSectionSettingID = F, AnalysisType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionCategoryAnalyticsSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionCategoryAnalyticsSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionCategoryAnalyticsSetting
	#' @param TeacherSectionCategoryAnalyticsSettingID The ID of the TeacherSectionCategoryAnalyticsSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionCategoryAnalyticsSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionCategoryAnalyticsSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionCategoryAnalyticsSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionCategoryAnalyticsSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionCategoryAnalyticsSetting <- function(TeacherSectionCategoryAnalyticsSettingID, CategoryID = F, TeacherSectionSettingID = F, AnalysisType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionCategoryAnalyticsSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionCategoryAnalyticsSetting", objectId = TeacherSectionCategoryAnalyticsSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionCategoryAnalyticsSetting
	#'
	#' This function deletes a TeacherSectionCategoryAnalyticsSetting
	#' @param TeacherSectionCategoryAnalyticsSettingID The ID of the TeacherSectionCategoryAnalyticsSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionCategoryAnalyticsSettingID of the deleted TeacherSectionCategoryAnalyticsSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionCategoryAnalyticsSetting <- function(TeacherSectionCategoryAnalyticsSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionCategoryAnalyticsSetting", objectId = TeacherSectionCategoryAnalyticsSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionCategoryAnalyticsSetting
	#'
	#' This function creates a TeacherSectionCategoryAnalyticsSetting
	#' @param fieldNames The field values to give the created TeacherSectionCategoryAnalyticsSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionCategoryAnalyticsSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionCategoryAnalyticsSetting <- function(CategoryID = NULL, TeacherSectionSettingID = NULL, AnalysisType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionCategoryAnalyticsSetting", body = list(DataObject = body), searchFields = append("TeacherSectionCategoryAnalyticsSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionCategoryAnalyticsSetting
	#'
	#' This function modifies a TeacherSectionCategoryAnalyticsSetting
	#' @param fieldNames The field values to give the modified TeacherSectionCategoryAnalyticsSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionCategoryAnalyticsSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionCategoryAnalyticsSetting <- function(TeacherSectionCategoryAnalyticsSettingID, CategoryID = NULL, TeacherSectionSettingID = NULL, AnalysisType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionCategoryAnalyticsSetting", objectId = TeacherSectionCategoryAnalyticsSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionCategoryAnalyticsSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Assessments
	#'
	#' This function returns a dataframe or json object of Assessments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Assessments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Assessments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Assessment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of Assessments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAssessments <- function(searchConditionsList = NULL, AssessmentID = F, SectionID = F, CategoryID = F, AssignedDate = F, DueDate = F, Name = F, Description = F, IsDeleted = F, CanDelete = F, DisplayRestoreButton = F, AssignmentCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "Assessment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an Assessment
	#'
	#' This function returns a dataframe or json object of an Assessment
	#' @param AssessmentID The ID of the Assessment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Assessment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Assessment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Assessment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of Assessment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAssessment <- function(AssessmentID, SectionID = F, CategoryID = F, AssignedDate = F, DueDate = F, Name = F, Description = F, IsDeleted = F, CanDelete = F, DisplayRestoreButton = F, AssignmentCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AssessmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "Assessment", objectId = AssessmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an Assessment
	#'
	#' This function deletes an Assessment
	#' @param AssessmentID The ID of the Assessment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The AssessmentID of the deleted Assessment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAssessment <- function(AssessmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "Assessment", objectId = AssessmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an Assessment
	#'
	#' This function creates an Assessment
	#' @param fieldNames The field values to give the created Assessment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created Assessment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAssessment <- function(SectionID = NULL, CategoryID = NULL, AssignedDate = NULL, DueDate = NULL, Name = NULL, Description = NULL, IsDeleted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "Assessment", body = list(DataObject = body), searchFields = append("AssessmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an Assessment
	#'
	#' This function modifies an Assessment
	#' @param fieldNames The field values to give the modified Assessment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified Assessment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAssessment <- function(AssessmentID, SectionID = NULL, CategoryID = NULL, AssignedDate = NULL, DueDate = NULL, Name = NULL, Description = NULL, IsDeleted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "Assessment", objectId = AssessmentID, body = list(DataObject = body), searchFields = append("AssessmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CategoryGradeBucketTypes
	#'
	#' This function returns a dataframe or json object of CategoryGradeBucketTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CategoryGradeBucketTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CategoryGradeBucketTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CategoryGradeBucketType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CategoryGradeBucketTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCategoryGradeBucketTypes <- function(searchConditionsList = NULL, CategoryGradeBucketTypeID = F, CategoryID = F, GradeBucketTypeID = F, EntityGroupKey = F, CategoryGradeBucketTypeIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CategoryGradeBucketType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CategoryGradeBucketType
	#'
	#' This function returns a dataframe or json object of a CategoryGradeBucketType
	#' @param CategoryGradeBucketTypeID The ID of the CategoryGradeBucketType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CategoryGradeBucketType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CategoryGradeBucketType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CategoryGradeBucketType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CategoryGradeBucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCategoryGradeBucketType <- function(CategoryGradeBucketTypeID, CategoryID = F, GradeBucketTypeID = F, EntityGroupKey = F, CategoryGradeBucketTypeIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CategoryGradeBucketTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CategoryGradeBucketType", objectId = CategoryGradeBucketTypeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CategoryGradeBucketType
	#'
	#' This function deletes a CategoryGradeBucketType
	#' @param CategoryGradeBucketTypeID The ID of the CategoryGradeBucketType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CategoryGradeBucketTypeID of the deleted CategoryGradeBucketType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCategoryGradeBucketType <- function(CategoryGradeBucketTypeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CategoryGradeBucketType", objectId = CategoryGradeBucketTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CategoryGradeBucketType
	#'
	#' This function creates a CategoryGradeBucketType
	#' @param fieldNames The field values to give the created CategoryGradeBucketType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CategoryGradeBucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCategoryGradeBucketType <- function(CategoryID = NULL, GradeBucketTypeID = NULL, EntityGroupKey = NULL, CategoryGradeBucketTypeIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CategoryGradeBucketType", body = list(DataObject = body), searchFields = append("CategoryGradeBucketTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CategoryGradeBucketType
	#'
	#' This function modifies a CategoryGradeBucketType
	#' @param fieldNames The field values to give the modified CategoryGradeBucketType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CategoryGradeBucketType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCategoryGradeBucketType <- function(CategoryGradeBucketTypeID, CategoryID = NULL, GradeBucketTypeID = NULL, EntityGroupKey = NULL, CategoryGradeBucketTypeIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CategoryGradeBucketType", objectId = CategoryGradeBucketTypeID, body = list(DataObject = body), searchFields = append("CategoryGradeBucketTypeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookConfigEntityGroupYears
	#'
	#' This function returns a dataframe or json object of GradebookConfigEntityGroupYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigEntityGroupYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigEntityGroupYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigEntityGroupYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookConfigEntityGroupYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookConfigEntityGroupYears <- function(searchConditionsList = NULL, ConfigEntityGroupYearID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, GradingPeriodEditExtensionDays = F, GradingPeriodEditExtensionEndTime = F, CapWeightedCategoryCalculationAt100Percent = F, AllowRetainedFutureScoring = F, OnlyShowGradebooksWithActiveStudentSectionTransactions = F, ClosedGradingPeriodGradeChangePermissionType = F, ClosedGradingPeriodGradeChangeCutOff = F, AllowPercentAdjustment = F, NumberOfDaysUntilUnscoredAssignmentsAreMissing = F, NumberOfDaysUntilNewStudentIconAppears = F, AssignmentMaxScore = F, AllowTeachersToOverrideDefaultMaxScore = F, AssignmentMaxWeight = F, AllowTeachersToOverrideDefaultMaxWeight = F, AllowTeacherComments = F, CapCalculationAt100Percent = F, ConfigEntityGroupYearIDClonedFrom = F, DisplayNonGradedClassesForTeachers = F, AllowOnlineAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AutoApproveGradeChangeRequest = F, AllowTeachersToTransferGrades = F, AllowTeachersToAddStudentNotes = F, UseFailingGradeScoreClarifier = F, FailingGradeThresholdPercent = F, ScoreClarifierIDFailing = F, AllowNegativePercentAdjustment = F, AllowGracePeriodAtEndOfGradingPeriod = F, AllowMultipleAssignmentAttempts = F, AllowStudentGroups = F, AllowClosedGradingPeriodGradeChangeRequestsToUpdateSnapshotGrades = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ConfigEntityGroupYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookConfigEntityGroupYear
	#'
	#' This function returns a dataframe or json object of a GradebookConfigEntityGroupYear
	#' @param GradebookConfigEntityGroupYearID The ID of the GradebookConfigEntityGroupYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigEntityGroupYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigEntityGroupYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigEntityGroupYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookConfigEntityGroupYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookConfigEntityGroupYear <- function(GradebookConfigEntityGroupYearID, ConfigEntityGroupYearID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, GradingPeriodEditExtensionDays = F, GradingPeriodEditExtensionEndTime = F, CapWeightedCategoryCalculationAt100Percent = F, AllowRetainedFutureScoring = F, OnlyShowGradebooksWithActiveStudentSectionTransactions = F, ClosedGradingPeriodGradeChangePermissionType = F, ClosedGradingPeriodGradeChangeCutOff = F, AllowPercentAdjustment = F, NumberOfDaysUntilUnscoredAssignmentsAreMissing = F, NumberOfDaysUntilNewStudentIconAppears = F, AssignmentMaxScore = F, AllowTeachersToOverrideDefaultMaxScore = F, AssignmentMaxWeight = F, AllowTeachersToOverrideDefaultMaxWeight = F, AllowTeacherComments = F, CapCalculationAt100Percent = F, ConfigEntityGroupYearIDClonedFrom = F, DisplayNonGradedClassesForTeachers = F, AllowOnlineAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AutoApproveGradeChangeRequest = F, AllowTeachersToTransferGrades = F, AllowTeachersToAddStudentNotes = F, UseFailingGradeScoreClarifier = F, FailingGradeThresholdPercent = F, ScoreClarifierIDFailing = F, AllowNegativePercentAdjustment = F, AllowGracePeriodAtEndOfGradingPeriod = F, AllowMultipleAssignmentAttempts = F, AllowStudentGroups = F, AllowClosedGradingPeriodGradeChangeRequestsToUpdateSnapshotGrades = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookConfigEntityGroupYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ConfigEntityGroupYear", objectId = GradebookConfigEntityGroupYearID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookConfigEntityGroupYear
	#'
	#' This function deletes a GradebookConfigEntityGroupYear
	#' @param GradebookConfigEntityGroupYearID The ID of the GradebookConfigEntityGroupYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookConfigEntityGroupYearID of the deleted GradebookConfigEntityGroupYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookConfigEntityGroupYear <- function(GradebookConfigEntityGroupYearID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ConfigEntityGroupYear", objectId = GradebookConfigEntityGroupYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookConfigEntityGroupYear
	#'
	#' This function creates a GradebookConfigEntityGroupYear
	#' @param fieldNames The field values to give the created GradebookConfigEntityGroupYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookConfigEntityGroupYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookConfigEntityGroupYear <- function(EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, GradingPeriodEditExtensionDays = NULL, GradingPeriodEditExtensionEndTime = NULL, CapWeightedCategoryCalculationAt100Percent = NULL, AllowRetainedFutureScoring = NULL, OnlyShowGradebooksWithActiveStudentSectionTransactions = NULL, ClosedGradingPeriodGradeChangePermissionType = NULL, ClosedGradingPeriodGradeChangeCutOff = NULL, AllowPercentAdjustment = NULL, NumberOfDaysUntilUnscoredAssignmentsAreMissing = NULL, NumberOfDaysUntilNewStudentIconAppears = NULL, AssignmentMaxScore = NULL, AllowTeachersToOverrideDefaultMaxScore = NULL, AssignmentMaxWeight = NULL, AllowTeachersToOverrideDefaultMaxWeight = NULL, AllowTeacherComments = NULL, CapCalculationAt100Percent = NULL, ConfigEntityGroupYearIDClonedFrom = NULL, DisplayNonGradedClassesForTeachers = NULL, AllowOnlineAssignments = NULL, AutoApproveGradeChangeRequest = NULL, AllowTeachersToTransferGrades = NULL, AllowTeachersToAddStudentNotes = NULL, UseFailingGradeScoreClarifier = NULL, FailingGradeThresholdPercent = NULL, ScoreClarifierIDFailing = NULL, AllowNegativePercentAdjustment = NULL, AllowGracePeriodAtEndOfGradingPeriod = NULL, AllowMultipleAssignmentAttempts = NULL, AllowStudentGroups = NULL, AllowClosedGradingPeriodGradeChangeRequestsToUpdateSnapshotGrades = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ConfigEntityGroupYear", body = list(DataObject = body), searchFields = append("ConfigEntityGroupYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookConfigEntityGroupYear
	#'
	#' This function modifies a GradebookConfigEntityGroupYear
	#' @param fieldNames The field values to give the modified GradebookConfigEntityGroupYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookConfigEntityGroupYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookConfigEntityGroupYear <- function(ConfigEntityGroupYearID, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, GradingPeriodEditExtensionDays = NULL, GradingPeriodEditExtensionEndTime = NULL, CapWeightedCategoryCalculationAt100Percent = NULL, AllowRetainedFutureScoring = NULL, OnlyShowGradebooksWithActiveStudentSectionTransactions = NULL, ClosedGradingPeriodGradeChangePermissionType = NULL, ClosedGradingPeriodGradeChangeCutOff = NULL, AllowPercentAdjustment = NULL, NumberOfDaysUntilUnscoredAssignmentsAreMissing = NULL, NumberOfDaysUntilNewStudentIconAppears = NULL, AssignmentMaxScore = NULL, AllowTeachersToOverrideDefaultMaxScore = NULL, AssignmentMaxWeight = NULL, AllowTeachersToOverrideDefaultMaxWeight = NULL, AllowTeacherComments = NULL, CapCalculationAt100Percent = NULL, ConfigEntityGroupYearIDClonedFrom = NULL, DisplayNonGradedClassesForTeachers = NULL, AllowOnlineAssignments = NULL, AutoApproveGradeChangeRequest = NULL, AllowTeachersToTransferGrades = NULL, AllowTeachersToAddStudentNotes = NULL, UseFailingGradeScoreClarifier = NULL, FailingGradeThresholdPercent = NULL, ScoreClarifierIDFailing = NULL, AllowNegativePercentAdjustment = NULL, AllowGracePeriodAtEndOfGradingPeriod = NULL, AllowMultipleAssignmentAttempts = NULL, AllowStudentGroups = NULL, AllowClosedGradingPeriodGradeChangeRequestsToUpdateSnapshotGrades = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ConfigEntityGroupYear", objectId = ConfigEntityGroupYearID, body = list(DataObject = body), searchFields = append("ConfigEntityGroupYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ClosedGradingPeriodStudentGradeBucketChanges
	#'
	#' This function returns a dataframe or json object of ClosedGradingPeriodStudentGradeBucketChanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClosedGradingPeriodStudentGradeBucketChanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClosedGradingPeriodStudentGradeBucketChanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClosedGradingPeriodStudentGradeBucketChange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of ClosedGradingPeriodStudentGradeBucketChanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listClosedGradingPeriodStudentGradeBucketChanges <- function(searchConditionsList = NULL, ClosedGradingPeriodStudentGradeBucketChangeID = F, ClosedGradingPeriodGradeChangeID = F, StaffMeetID = F, StudentGradeBucketID = F, GradeMarkIDOriginal = F, GradeMarkIDNew = F, ClosedGradingPeriodGradeChangeStatus = F, IsDisabled = F, IsGradeChangePastDueAndInProgress = F, CalculatedClosedGradingPeriodGradeChangeStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OriginalPercent = F, NewPercent = F, HasSnapshot = F, ChangeMadeByFullnameLFM = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ClosedGradingPeriodStudentGradeBucketChange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ClosedGradingPeriodStudentGradeBucketChange
	#'
	#' This function returns a dataframe or json object of a ClosedGradingPeriodStudentGradeBucketChange
	#' @param ClosedGradingPeriodStudentGradeBucketChangeID The ID of the ClosedGradingPeriodStudentGradeBucketChange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClosedGradingPeriodStudentGradeBucketChange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClosedGradingPeriodStudentGradeBucketChange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClosedGradingPeriodStudentGradeBucketChange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of ClosedGradingPeriodStudentGradeBucketChange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getClosedGradingPeriodStudentGradeBucketChange <- function(ClosedGradingPeriodStudentGradeBucketChangeID, ClosedGradingPeriodGradeChangeID = F, StaffMeetID = F, StudentGradeBucketID = F, GradeMarkIDOriginal = F, GradeMarkIDNew = F, ClosedGradingPeriodGradeChangeStatus = F, IsDisabled = F, IsGradeChangePastDueAndInProgress = F, CalculatedClosedGradingPeriodGradeChangeStatus = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, OriginalPercent = F, NewPercent = F, HasSnapshot = F, ChangeMadeByFullnameLFM = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ClosedGradingPeriodStudentGradeBucketChangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodStudentGradeBucketChange", objectId = ClosedGradingPeriodStudentGradeBucketChangeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ClosedGradingPeriodStudentGradeBucketChange
	#'
	#' This function deletes a ClosedGradingPeriodStudentGradeBucketChange
	#' @param ClosedGradingPeriodStudentGradeBucketChangeID The ID of the ClosedGradingPeriodStudentGradeBucketChange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The ClosedGradingPeriodStudentGradeBucketChangeID of the deleted ClosedGradingPeriodStudentGradeBucketChange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteClosedGradingPeriodStudentGradeBucketChange <- function(ClosedGradingPeriodStudentGradeBucketChangeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodStudentGradeBucketChange", objectId = ClosedGradingPeriodStudentGradeBucketChangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ClosedGradingPeriodStudentGradeBucketChange
	#'
	#' This function creates a ClosedGradingPeriodStudentGradeBucketChange
	#' @param fieldNames The field values to give the created ClosedGradingPeriodStudentGradeBucketChange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created ClosedGradingPeriodStudentGradeBucketChange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createClosedGradingPeriodStudentGradeBucketChange <- function(ClosedGradingPeriodGradeChangeID = NULL, StaffMeetID = NULL, StudentGradeBucketID = NULL, GradeMarkIDOriginal = NULL, GradeMarkIDNew = NULL, ClosedGradingPeriodGradeChangeStatus = NULL, OriginalPercent = NULL, NewPercent = NULL, HasSnapshot = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodStudentGradeBucketChange", body = list(DataObject = body), searchFields = append("ClosedGradingPeriodStudentGradeBucketChangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ClosedGradingPeriodStudentGradeBucketChange
	#'
	#' This function modifies a ClosedGradingPeriodStudentGradeBucketChange
	#' @param fieldNames The field values to give the modified ClosedGradingPeriodStudentGradeBucketChange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified ClosedGradingPeriodStudentGradeBucketChange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyClosedGradingPeriodStudentGradeBucketChange <- function(ClosedGradingPeriodStudentGradeBucketChangeID, ClosedGradingPeriodGradeChangeID = NULL, StaffMeetID = NULL, StudentGradeBucketID = NULL, GradeMarkIDOriginal = NULL, GradeMarkIDNew = NULL, ClosedGradingPeriodGradeChangeStatus = NULL, OriginalPercent = NULL, NewPercent = NULL, HasSnapshot = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodStudentGradeBucketChange", objectId = ClosedGradingPeriodStudentGradeBucketChangeID, body = list(DataObject = body), searchFields = append("ClosedGradingPeriodStudentGradeBucketChangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ClosedGradingPeriodGradeChanges
	#'
	#' This function returns a dataframe or json object of ClosedGradingPeriodGradeChanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClosedGradingPeriodGradeChanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClosedGradingPeriodGradeChanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClosedGradingPeriodGradeChange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of ClosedGradingPeriodGradeChanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listClosedGradingPeriodGradeChanges <- function(searchConditionsList = NULL, ClosedGradingPeriodGradeChangeID = F, SectionID = F, GradingPeriodID = F, CompletedTime = F, Reason = F, DisplayCompleteButton = F, CalculatedCompletedTime = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayMassReviewDenyButton = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ClosedGradingPeriodGradeChange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ClosedGradingPeriodGradeChange
	#'
	#' This function returns a dataframe or json object of a ClosedGradingPeriodGradeChange
	#' @param ClosedGradingPeriodGradeChangeID The ID of the ClosedGradingPeriodGradeChange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ClosedGradingPeriodGradeChange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ClosedGradingPeriodGradeChange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ClosedGradingPeriodGradeChange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of ClosedGradingPeriodGradeChange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getClosedGradingPeriodGradeChange <- function(ClosedGradingPeriodGradeChangeID, SectionID = F, GradingPeriodID = F, CompletedTime = F, Reason = F, DisplayCompleteButton = F, CalculatedCompletedTime = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DisplayMassReviewDenyButton = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ClosedGradingPeriodGradeChangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodGradeChange", objectId = ClosedGradingPeriodGradeChangeID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ClosedGradingPeriodGradeChange
	#'
	#' This function deletes a ClosedGradingPeriodGradeChange
	#' @param ClosedGradingPeriodGradeChangeID The ID of the ClosedGradingPeriodGradeChange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The ClosedGradingPeriodGradeChangeID of the deleted ClosedGradingPeriodGradeChange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteClosedGradingPeriodGradeChange <- function(ClosedGradingPeriodGradeChangeID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodGradeChange", objectId = ClosedGradingPeriodGradeChangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ClosedGradingPeriodGradeChange
	#'
	#' This function creates a ClosedGradingPeriodGradeChange
	#' @param fieldNames The field values to give the created ClosedGradingPeriodGradeChange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created ClosedGradingPeriodGradeChange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createClosedGradingPeriodGradeChange <- function(SectionID = NULL, GradingPeriodID = NULL, CompletedTime = NULL, Reason = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodGradeChange", body = list(DataObject = body), searchFields = append("ClosedGradingPeriodGradeChangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ClosedGradingPeriodGradeChange
	#'
	#' This function modifies a ClosedGradingPeriodGradeChange
	#' @param fieldNames The field values to give the modified ClosedGradingPeriodGradeChange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified ClosedGradingPeriodGradeChange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyClosedGradingPeriodGradeChange <- function(ClosedGradingPeriodGradeChangeID, SectionID = NULL, GradingPeriodID = NULL, CompletedTime = NULL, Reason = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ClosedGradingPeriodGradeChange", objectId = ClosedGradingPeriodGradeChangeID, body = list(DataObject = body), searchFields = append("ClosedGradingPeriodGradeChangeID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionGradeBucketAnalyticsSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionGradeBucketAnalyticsSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionGradeBucketAnalyticsSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionGradeBucketAnalyticsSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionGradeBucketAnalyticsSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionGradeBucketAnalyticsSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionGradeBucketAnalyticsSettings <- function(searchConditionsList = NULL, TeacherSectionGradeBucketAnalyticsSettingID = F, CalculationGroupGradeBucketID = F, TeacherSectionSettingID = F, AnalysisType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionGradeBucketAnalyticsSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionGradeBucketAnalyticsSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionGradeBucketAnalyticsSetting
	#' @param TeacherSectionGradeBucketAnalyticsSettingID The ID of the TeacherSectionGradeBucketAnalyticsSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionGradeBucketAnalyticsSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionGradeBucketAnalyticsSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionGradeBucketAnalyticsSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionGradeBucketAnalyticsSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionGradeBucketAnalyticsSetting <- function(TeacherSectionGradeBucketAnalyticsSettingID, CalculationGroupGradeBucketID = F, TeacherSectionSettingID = F, AnalysisType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionGradeBucketAnalyticsSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketAnalyticsSetting", objectId = TeacherSectionGradeBucketAnalyticsSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionGradeBucketAnalyticsSetting
	#'
	#' This function deletes a TeacherSectionGradeBucketAnalyticsSetting
	#' @param TeacherSectionGradeBucketAnalyticsSettingID The ID of the TeacherSectionGradeBucketAnalyticsSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionGradeBucketAnalyticsSettingID of the deleted TeacherSectionGradeBucketAnalyticsSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionGradeBucketAnalyticsSetting <- function(TeacherSectionGradeBucketAnalyticsSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketAnalyticsSetting", objectId = TeacherSectionGradeBucketAnalyticsSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionGradeBucketAnalyticsSetting
	#'
	#' This function creates a TeacherSectionGradeBucketAnalyticsSetting
	#' @param fieldNames The field values to give the created TeacherSectionGradeBucketAnalyticsSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionGradeBucketAnalyticsSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionGradeBucketAnalyticsSetting <- function(CalculationGroupGradeBucketID = NULL, TeacherSectionSettingID = NULL, AnalysisType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketAnalyticsSetting", body = list(DataObject = body), searchFields = append("TeacherSectionGradeBucketAnalyticsSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionGradeBucketAnalyticsSetting
	#'
	#' This function modifies a TeacherSectionGradeBucketAnalyticsSetting
	#' @param fieldNames The field values to give the modified TeacherSectionGradeBucketAnalyticsSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionGradeBucketAnalyticsSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionGradeBucketAnalyticsSetting <- function(TeacherSectionGradeBucketAnalyticsSettingID, CalculationGroupGradeBucketID = NULL, TeacherSectionSettingID = NULL, AnalysisType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketAnalyticsSetting", objectId = TeacherSectionGradeBucketAnalyticsSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionGradeBucketAnalyticsSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionNotes
	#'
	#' This function returns a dataframe or json object of StudentSectionNotes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionNotes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionNotes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionNote') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentSectionNotes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionNotes <- function(searchConditionsList = NULL, StudentSectionNoteID = F, StudentSectionID = F, Note = F, OtherTeachersHaveAccess = F, LimitToThisSection = F, CurrentUserCanModify = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentSectionNote", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionNote
	#'
	#' This function returns a dataframe or json object of a StudentSectionNote
	#' @param StudentSectionNoteID The ID of the StudentSectionNote to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionNote. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionNote.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionNote') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentSectionNote
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionNote <- function(StudentSectionNoteID, StudentSectionID = F, Note = F, OtherTeachersHaveAccess = F, LimitToThisSection = F, CurrentUserCanModify = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionNoteID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentSectionNote", objectId = StudentSectionNoteID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionNote
	#'
	#' This function deletes a StudentSectionNote
	#' @param StudentSectionNoteID The ID of the StudentSectionNote to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentSectionNoteID of the deleted StudentSectionNote.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionNote <- function(StudentSectionNoteID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentSectionNote", objectId = StudentSectionNoteID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionNote
	#'
	#' This function creates a StudentSectionNote
	#' @param fieldNames The field values to give the created StudentSectionNote. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentSectionNote
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionNote <- function(StudentSectionID = NULL, Note = NULL, OtherTeachersHaveAccess = NULL, LimitToThisSection = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentSectionNote", body = list(DataObject = body), searchFields = append("StudentSectionNoteID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionNote
	#'
	#' This function modifies a StudentSectionNote
	#' @param fieldNames The field values to give the modified StudentSectionNote. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentSectionNote
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionNote <- function(StudentSectionNoteID, StudentSectionID = NULL, Note = NULL, OtherTeachersHaveAccess = NULL, LimitToThisSection = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentSectionNote", objectId = StudentSectionNoteID, body = list(DataObject = body), searchFields = append("StudentSectionNoteID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionGradeBucketSettings
	#'
	#' This function returns a dataframe or json object of SectionGradeBucketSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradeBucketSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradeBucketSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradeBucketSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionGradeBucketSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionGradeBucketSettings <- function(searchConditionsList = NULL, SectionGradeBucketSettingID = F, SectionID = F, GradingPeriodGradeBucketID = F, UseMaxExtraCredit = F, MaxExtraCredit = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionGradeBucketSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionGradeBucketSetting
	#'
	#' This function returns a dataframe or json object of a SectionGradeBucketSetting
	#' @param SectionGradeBucketSettingID The ID of the SectionGradeBucketSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradeBucketSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradeBucketSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradeBucketSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionGradeBucketSetting <- function(SectionGradeBucketSettingID, SectionID = F, GradingPeriodGradeBucketID = F, UseMaxExtraCredit = F, MaxExtraCredit = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionGradeBucketSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionGradeBucketSetting", objectId = SectionGradeBucketSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionGradeBucketSetting
	#'
	#' This function deletes a SectionGradeBucketSetting
	#' @param SectionGradeBucketSettingID The ID of the SectionGradeBucketSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionGradeBucketSettingID of the deleted SectionGradeBucketSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionGradeBucketSetting <- function(SectionGradeBucketSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionGradeBucketSetting", objectId = SectionGradeBucketSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionGradeBucketSetting
	#'
	#' This function creates a SectionGradeBucketSetting
	#' @param fieldNames The field values to give the created SectionGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionGradeBucketSetting <- function(SectionID = NULL, GradingPeriodGradeBucketID = NULL, UseMaxExtraCredit = NULL, MaxExtraCredit = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionGradeBucketSetting", body = list(DataObject = body), searchFields = append("SectionGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionGradeBucketSetting
	#'
	#' This function modifies a SectionGradeBucketSetting
	#' @param fieldNames The field values to give the modified SectionGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionGradeBucketSetting <- function(SectionGradeBucketSettingID, SectionID = NULL, GradingPeriodGradeBucketID = NULL, UseMaxExtraCredit = NULL, MaxExtraCredit = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionGradeBucketSetting", objectId = SectionGradeBucketSettingID, body = list(DataObject = body), searchFields = append("SectionGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionGradingPeriodData
	#'
	#' This function returns a dataframe or json object of SectionGradingPeriodData
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradingPeriodData. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradingPeriodData.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradingPeriodData') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionGradingPeriodData
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionGradingPeriodData <- function(searchConditionsList = NULL, SectionGradingPeriodDataID = F, SectionID = F, GradingPeriodID = F, IsCompleted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionGradingPeriodData", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionGradingPeriodData
	#'
	#' This function returns a dataframe or json object of a SectionGradingPeriodData
	#' @param SectionGradingPeriodDataID The ID of the SectionGradingPeriodData to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradingPeriodData. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradingPeriodData.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradingPeriodData') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionGradingPeriodData
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionGradingPeriodData <- function(SectionGradingPeriodDataID, SectionID = F, GradingPeriodID = F, IsCompleted = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionGradingPeriodDataID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionGradingPeriodData", objectId = SectionGradingPeriodDataID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionGradingPeriodData
	#'
	#' This function deletes a SectionGradingPeriodData
	#' @param SectionGradingPeriodDataID The ID of the SectionGradingPeriodData to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionGradingPeriodDataID of the deleted SectionGradingPeriodData.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionGradingPeriodData <- function(SectionGradingPeriodDataID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionGradingPeriodData", objectId = SectionGradingPeriodDataID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionGradingPeriodData
	#'
	#' This function creates a SectionGradingPeriodData
	#' @param fieldNames The field values to give the created SectionGradingPeriodData. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionGradingPeriodData
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionGradingPeriodData <- function(SectionID = NULL, GradingPeriodID = NULL, IsCompleted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionGradingPeriodData", body = list(DataObject = body), searchFields = append("SectionGradingPeriodDataID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionGradingPeriodData
	#'
	#' This function modifies a SectionGradingPeriodData
	#' @param fieldNames The field values to give the modified SectionGradingPeriodData. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionGradingPeriodData
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionGradingPeriodData <- function(SectionGradingPeriodDataID, SectionID = NULL, GradingPeriodID = NULL, IsCompleted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionGradingPeriodData", objectId = SectionGradingPeriodDataID, body = list(DataObject = body), searchFields = append("SectionGradingPeriodDataID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionGradingPeriodSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionGradingPeriodSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionGradingPeriodSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionGradingPeriodSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionGradingPeriodSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionGradingPeriodSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionGradingPeriodSettings <- function(searchConditionsList = NULL, TeacherSectionGradingPeriodSettingID = F, TeacherSectionSettingID = F, GradingPeriodID = F, ShowGradeBuckets = F, ShowAssignments = F, ShowAssessments = F, IncludeMissingAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionGradingPeriodSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionGradingPeriodSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionGradingPeriodSetting
	#' @param TeacherSectionGradingPeriodSettingID The ID of the TeacherSectionGradingPeriodSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionGradingPeriodSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionGradingPeriodSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionGradingPeriodSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionGradingPeriodSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionGradingPeriodSetting <- function(TeacherSectionGradingPeriodSettingID, TeacherSectionSettingID = F, GradingPeriodID = F, ShowGradeBuckets = F, ShowAssignments = F, ShowAssessments = F, IncludeMissingAssignments = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionGradingPeriodSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionGradingPeriodSetting", objectId = TeacherSectionGradingPeriodSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionGradingPeriodSetting
	#'
	#' This function deletes a TeacherSectionGradingPeriodSetting
	#' @param TeacherSectionGradingPeriodSettingID The ID of the TeacherSectionGradingPeriodSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionGradingPeriodSettingID of the deleted TeacherSectionGradingPeriodSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionGradingPeriodSetting <- function(TeacherSectionGradingPeriodSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionGradingPeriodSetting", objectId = TeacherSectionGradingPeriodSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionGradingPeriodSetting
	#'
	#' This function creates a TeacherSectionGradingPeriodSetting
	#' @param fieldNames The field values to give the created TeacherSectionGradingPeriodSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionGradingPeriodSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionGradingPeriodSetting <- function(TeacherSectionSettingID = NULL, GradingPeriodID = NULL, ShowGradeBuckets = NULL, ShowAssignments = NULL, ShowAssessments = NULL, IncludeMissingAssignments = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionGradingPeriodSetting", body = list(DataObject = body), searchFields = append("TeacherSectionGradingPeriodSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionGradingPeriodSetting
	#'
	#' This function modifies a TeacherSectionGradingPeriodSetting
	#' @param fieldNames The field values to give the modified TeacherSectionGradingPeriodSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionGradingPeriodSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionGradingPeriodSetting <- function(TeacherSectionGradingPeriodSettingID, TeacherSectionSettingID = NULL, GradingPeriodID = NULL, ShowGradeBuckets = NULL, ShowAssignments = NULL, ShowAssessments = NULL, IncludeMissingAssignments = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionGradingPeriodSetting", objectId = TeacherSectionGradingPeriodSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionGradingPeriodSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionGradeBucketSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionGradeBucketSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionGradeBucketSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionGradeBucketSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionGradeBucketSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionGradeBucketSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionGradeBucketSettings <- function(searchConditionsList = NULL, TeacherSectionGradeBucketSettingID = F, TeacherSectionSettingID = F, GradingPeriodGradeBucketID = F, GradeBucketDisplayType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsExpanded = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionGradeBucketSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionGradeBucketSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionGradeBucketSetting
	#' @param TeacherSectionGradeBucketSettingID The ID of the TeacherSectionGradeBucketSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionGradeBucketSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionGradeBucketSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionGradeBucketSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionGradeBucketSetting <- function(TeacherSectionGradeBucketSettingID, TeacherSectionSettingID = F, GradingPeriodGradeBucketID = F, GradeBucketDisplayType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsExpanded = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionGradeBucketSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketSetting", objectId = TeacherSectionGradeBucketSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionGradeBucketSetting
	#'
	#' This function deletes a TeacherSectionGradeBucketSetting
	#' @param TeacherSectionGradeBucketSettingID The ID of the TeacherSectionGradeBucketSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionGradeBucketSettingID of the deleted TeacherSectionGradeBucketSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionGradeBucketSetting <- function(TeacherSectionGradeBucketSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketSetting", objectId = TeacherSectionGradeBucketSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionGradeBucketSetting
	#'
	#' This function creates a TeacherSectionGradeBucketSetting
	#' @param fieldNames The field values to give the created TeacherSectionGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionGradeBucketSetting <- function(TeacherSectionSettingID = NULL, GradingPeriodGradeBucketID = NULL, GradeBucketDisplayType = NULL, IsExpanded = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketSetting", body = list(DataObject = body), searchFields = append("TeacherSectionGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionGradeBucketSetting
	#'
	#' This function modifies a TeacherSectionGradeBucketSetting
	#' @param fieldNames The field values to give the modified TeacherSectionGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionGradeBucketSetting <- function(TeacherSectionGradeBucketSettingID, TeacherSectionSettingID = NULL, GradingPeriodGradeBucketID = NULL, GradeBucketDisplayType = NULL, IsExpanded = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionGradeBucketSetting", objectId = TeacherSectionGradeBucketSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionSettings <- function(searchConditionsList = NULL, TeacherSectionSettingID = F, SectionID = F, ClassGroupID = F, UserIDOwner = F, ManualDateEntryStartDate = F, ManualDateEntryEndDate = F, StudentsToDisplay = F, StudentNameDisplayType = F, DisplayStudentGradeLevel = F, DisplayStudentNumber = F, DisplayAttendance = F, DisplayMissingAssignment = F, DisplayUnscoredPastDueAssignments = F, DisplayGradebookAverages = F, DisplayAssignmentAttendanceIndicator = F, DisplayStudentGradingPeriodCommentIndicator = F, CalculationGroupGradeBucketIDLocked = F, BrowseViewID = F, HideLockedColumns = F, ShowGradebookLockedColumnButton = F, UseCustomClassRosterSort = F, HasCustomClassRosterSort = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HideAssignmentCategoryColors = F, DisplayStudentGroups = F, DisplayStudentGroupColors = F, DisplayAssignmentStudentGroupColors = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionSetting
	#' @param TeacherSectionSettingID The ID of the TeacherSectionSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionSetting <- function(TeacherSectionSettingID, SectionID = F, ClassGroupID = F, UserIDOwner = F, ManualDateEntryStartDate = F, ManualDateEntryEndDate = F, StudentsToDisplay = F, StudentNameDisplayType = F, DisplayStudentGradeLevel = F, DisplayStudentNumber = F, DisplayAttendance = F, DisplayMissingAssignment = F, DisplayUnscoredPastDueAssignments = F, DisplayGradebookAverages = F, DisplayAssignmentAttendanceIndicator = F, DisplayStudentGradingPeriodCommentIndicator = F, CalculationGroupGradeBucketIDLocked = F, BrowseViewID = F, HideLockedColumns = F, ShowGradebookLockedColumnButton = F, UseCustomClassRosterSort = F, HasCustomClassRosterSort = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HideAssignmentCategoryColors = F, DisplayStudentGroups = F, DisplayStudentGroupColors = F, DisplayAssignmentStudentGroupColors = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionSetting", objectId = TeacherSectionSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionSetting
	#'
	#' This function deletes a TeacherSectionSetting
	#' @param TeacherSectionSettingID The ID of the TeacherSectionSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionSettingID of the deleted TeacherSectionSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionSetting <- function(TeacherSectionSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionSetting", objectId = TeacherSectionSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionSetting
	#'
	#' This function creates a TeacherSectionSetting
	#' @param fieldNames The field values to give the created TeacherSectionSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionSetting <- function(SectionID = NULL, ClassGroupID = NULL, UserIDOwner = NULL, ManualDateEntryStartDate = NULL, ManualDateEntryEndDate = NULL, StudentsToDisplay = NULL, StudentNameDisplayType = NULL, DisplayStudentGradeLevel = NULL, DisplayStudentNumber = NULL, DisplayAttendance = NULL, DisplayMissingAssignment = NULL, DisplayUnscoredPastDueAssignments = NULL, DisplayGradebookAverages = NULL, DisplayAssignmentAttendanceIndicator = NULL, DisplayStudentGradingPeriodCommentIndicator = NULL, CalculationGroupGradeBucketIDLocked = NULL, BrowseViewID = NULL, HideLockedColumns = NULL, UseCustomClassRosterSort = NULL, HideAssignmentCategoryColors = NULL, DisplayStudentGroups = NULL, DisplayStudentGroupColors = NULL, DisplayAssignmentStudentGroupColors = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionSetting", body = list(DataObject = body), searchFields = append("TeacherSectionSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionSetting
	#'
	#' This function modifies a TeacherSectionSetting
	#' @param fieldNames The field values to give the modified TeacherSectionSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionSetting <- function(TeacherSectionSettingID, SectionID = NULL, ClassGroupID = NULL, UserIDOwner = NULL, ManualDateEntryStartDate = NULL, ManualDateEntryEndDate = NULL, StudentsToDisplay = NULL, StudentNameDisplayType = NULL, DisplayStudentGradeLevel = NULL, DisplayStudentNumber = NULL, DisplayAttendance = NULL, DisplayMissingAssignment = NULL, DisplayUnscoredPastDueAssignments = NULL, DisplayGradebookAverages = NULL, DisplayAssignmentAttendanceIndicator = NULL, DisplayStudentGradingPeriodCommentIndicator = NULL, CalculationGroupGradeBucketIDLocked = NULL, BrowseViewID = NULL, HideLockedColumns = NULL, UseCustomClassRosterSort = NULL, HideAssignmentCategoryColors = NULL, DisplayStudentGroups = NULL, DisplayStudentGroupColors = NULL, DisplayAssignmentStudentGroupColors = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionSetting", objectId = TeacherSectionSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradesheetAssignmentSettings
	#'
	#' This function returns a dataframe or json object of GradesheetAssignmentSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradesheetAssignmentSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradesheetAssignmentSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradesheetAssignmentSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradesheetAssignmentSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradesheetAssignmentSettings <- function(searchConditionsList = NULL, GradesheetAssignmentSettingID = F, TeacherSectionSettingID = F, MaxScoreDefault = F, IsDateAscending = F, SortBy = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DefaultAttemptType = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "GradesheetAssignmentSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradesheetAssignmentSetting
	#'
	#' This function returns a dataframe or json object of a GradesheetAssignmentSetting
	#' @param GradesheetAssignmentSettingID The ID of the GradesheetAssignmentSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradesheetAssignmentSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradesheetAssignmentSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradesheetAssignmentSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradesheetAssignmentSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradesheetAssignmentSetting <- function(GradesheetAssignmentSettingID, TeacherSectionSettingID = F, MaxScoreDefault = F, IsDateAscending = F, SortBy = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DefaultAttemptType = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradesheetAssignmentSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "GradesheetAssignmentSetting", objectId = GradesheetAssignmentSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradesheetAssignmentSetting
	#'
	#' This function deletes a GradesheetAssignmentSetting
	#' @param GradesheetAssignmentSettingID The ID of the GradesheetAssignmentSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradesheetAssignmentSettingID of the deleted GradesheetAssignmentSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradesheetAssignmentSetting <- function(GradesheetAssignmentSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "GradesheetAssignmentSetting", objectId = GradesheetAssignmentSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradesheetAssignmentSetting
	#'
	#' This function creates a GradesheetAssignmentSetting
	#' @param fieldNames The field values to give the created GradesheetAssignmentSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradesheetAssignmentSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradesheetAssignmentSetting <- function(TeacherSectionSettingID = NULL, MaxScoreDefault = NULL, IsDateAscending = NULL, SortBy = NULL, DefaultAttemptType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "GradesheetAssignmentSetting", body = list(DataObject = body), searchFields = append("GradesheetAssignmentSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradesheetAssignmentSetting
	#'
	#' This function modifies a GradesheetAssignmentSetting
	#' @param fieldNames The field values to give the modified GradesheetAssignmentSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradesheetAssignmentSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradesheetAssignmentSetting <- function(GradesheetAssignmentSettingID, TeacherSectionSettingID = NULL, MaxScoreDefault = NULL, IsDateAscending = NULL, SortBy = NULL, DefaultAttemptType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "GradesheetAssignmentSetting", objectId = GradesheetAssignmentSettingID, body = list(DataObject = body), searchFields = append("GradesheetAssignmentSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionGradingScales
	#'
	#' This function returns a dataframe or json object of TempSectionGradingScales
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradingScales. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradingScales.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradingScale') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempSectionGradingScales
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionGradingScales <- function(searchConditionsList = NULL, TempSectionGradingScaleID = F, SectionID = F, CanClone = F, ErrorMessage = F, ChildGradeBuckets = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempSectionGradingScale", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionGradingScale
	#'
	#' This function returns a dataframe or json object of a TempSectionGradingScale
	#' @param TempSectionGradingScaleID The ID of the TempSectionGradingScale to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradingScale. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradingScale.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradingScale') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempSectionGradingScale
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionGradingScale <- function(TempSectionGradingScaleID, SectionID = F, CanClone = F, ErrorMessage = F, ChildGradeBuckets = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionGradingScaleID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempSectionGradingScale", objectId = TempSectionGradingScaleID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionGradingScale
	#'
	#' This function deletes a TempSectionGradingScale
	#' @param TempSectionGradingScaleID The ID of the TempSectionGradingScale to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempSectionGradingScaleID of the deleted TempSectionGradingScale.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionGradingScale <- function(TempSectionGradingScaleID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempSectionGradingScale", objectId = TempSectionGradingScaleID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionGradingScale
	#'
	#' This function creates a TempSectionGradingScale
	#' @param fieldNames The field values to give the created TempSectionGradingScale. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempSectionGradingScale
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionGradingScale <- function(SectionID = NULL, CanClone = NULL, ErrorMessage = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempSectionGradingScale", body = list(DataObject = body), searchFields = append("TempSectionGradingScaleID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionGradingScale
	#'
	#' This function modifies a TempSectionGradingScale
	#' @param fieldNames The field values to give the modified TempSectionGradingScale. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempSectionGradingScale
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionGradingScale <- function(TempSectionGradingScaleID, SectionID = NULL, CanClone = NULL, ErrorMessage = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempSectionGradingScale", objectId = TempSectionGradingScaleID, body = list(DataObject = body), searchFields = append("TempSectionGradingScaleID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCloneGradebookSections
	#'
	#' This function returns a dataframe or json object of TempCloneGradebookSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCloneGradebookSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCloneGradebookSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCloneGradebookSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCloneGradebookSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCloneGradebookSections <- function(searchConditionsList = NULL, TempCloneGradebookSectionID = F, SectionID = F, CanClone = F, ErrorMessage = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCloneGradebookSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCloneGradebookSection
	#'
	#' This function returns a dataframe or json object of a TempCloneGradebookSection
	#' @param TempCloneGradebookSectionID The ID of the TempCloneGradebookSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCloneGradebookSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCloneGradebookSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCloneGradebookSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCloneGradebookSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCloneGradebookSection <- function(TempCloneGradebookSectionID, SectionID = F, CanClone = F, ErrorMessage = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCloneGradebookSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCloneGradebookSection", objectId = TempCloneGradebookSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCloneGradebookSection
	#'
	#' This function deletes a TempCloneGradebookSection
	#' @param TempCloneGradebookSectionID The ID of the TempCloneGradebookSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCloneGradebookSectionID of the deleted TempCloneGradebookSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCloneGradebookSection <- function(TempCloneGradebookSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCloneGradebookSection", objectId = TempCloneGradebookSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCloneGradebookSection
	#'
	#' This function creates a TempCloneGradebookSection
	#' @param fieldNames The field values to give the created TempCloneGradebookSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCloneGradebookSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCloneGradebookSection <- function(SectionID = NULL, CanClone = NULL, ErrorMessage = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCloneGradebookSection", body = list(DataObject = body), searchFields = append("TempCloneGradebookSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCloneGradebookSection
	#'
	#' This function modifies a TempCloneGradebookSection
	#' @param fieldNames The field values to give the modified TempCloneGradebookSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCloneGradebookSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCloneGradebookSection <- function(TempCloneGradebookSectionID, SectionID = NULL, CanClone = NULL, ErrorMessage = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCloneGradebookSection", objectId = TempCloneGradebookSectionID, body = list(DataObject = body), searchFields = append("TempCloneGradebookSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCalculationGroupWeightings
	#'
	#' This function returns a dataframe or json object of TempCalculationGroupWeightings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalculationGroupWeightings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalculationGroupWeightings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalculationGroupWeighting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCalculationGroupWeightings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCalculationGroupWeightings <- function(searchConditionsList = NULL, TempCalculationGroupWeightingID = F, CalculationGroupGradeBucketID = F, GradingPeriodGradeBucketID = F, CategoryID = F, CategoryDescription = F, GradeBucketLabel = F, WeightPercent = F, GradeBucketOrder = F, SubjectID = F, SubjectDescription = F, AcademicStandardID = F, AcademicStandardDescription = F, AcademicStandardFullKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCalculationGroupWeighting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCalculationGroupWeighting
	#'
	#' This function returns a dataframe or json object of a TempCalculationGroupWeighting
	#' @param TempCalculationGroupWeightingID The ID of the TempCalculationGroupWeighting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalculationGroupWeighting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalculationGroupWeighting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalculationGroupWeighting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCalculationGroupWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCalculationGroupWeighting <- function(TempCalculationGroupWeightingID, CalculationGroupGradeBucketID = F, GradingPeriodGradeBucketID = F, CategoryID = F, CategoryDescription = F, GradeBucketLabel = F, WeightPercent = F, GradeBucketOrder = F, SubjectID = F, SubjectDescription = F, AcademicStandardID = F, AcademicStandardDescription = F, AcademicStandardFullKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCalculationGroupWeightingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCalculationGroupWeighting", objectId = TempCalculationGroupWeightingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCalculationGroupWeighting
	#'
	#' This function deletes a TempCalculationGroupWeighting
	#' @param TempCalculationGroupWeightingID The ID of the TempCalculationGroupWeighting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCalculationGroupWeightingID of the deleted TempCalculationGroupWeighting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCalculationGroupWeighting <- function(TempCalculationGroupWeightingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCalculationGroupWeighting", objectId = TempCalculationGroupWeightingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCalculationGroupWeighting
	#'
	#' This function creates a TempCalculationGroupWeighting
	#' @param fieldNames The field values to give the created TempCalculationGroupWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCalculationGroupWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCalculationGroupWeighting <- function(CalculationGroupGradeBucketID = NULL, GradingPeriodGradeBucketID = NULL, CategoryID = NULL, CategoryDescription = NULL, GradeBucketLabel = NULL, WeightPercent = NULL, GradeBucketOrder = NULL, SubjectID = NULL, SubjectDescription = NULL, AcademicStandardID = NULL, AcademicStandardDescription = NULL, AcademicStandardFullKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCalculationGroupWeighting", body = list(DataObject = body), searchFields = append("TempCalculationGroupWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCalculationGroupWeighting
	#'
	#' This function modifies a TempCalculationGroupWeighting
	#' @param fieldNames The field values to give the modified TempCalculationGroupWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCalculationGroupWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCalculationGroupWeighting <- function(TempCalculationGroupWeightingID, CalculationGroupGradeBucketID = NULL, GradingPeriodGradeBucketID = NULL, CategoryID = NULL, CategoryDescription = NULL, GradeBucketLabel = NULL, WeightPercent = NULL, GradeBucketOrder = NULL, SubjectID = NULL, SubjectDescription = NULL, AcademicStandardID = NULL, AcademicStandardDescription = NULL, AcademicStandardFullKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCalculationGroupWeighting", objectId = TempCalculationGroupWeightingID, body = list(DataObject = body), searchFields = append("TempCalculationGroupWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradebookCloneErrors
	#'
	#' This function returns a dataframe or json object of TempGradebookCloneErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradebookCloneErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradebookCloneErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradebookCloneError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempGradebookCloneErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradebookCloneErrors <- function(searchConditionsList = NULL, TempGradebookCloneErrorID = F, CalculationGroupGradeBucketID = F, SectionID = F, Field = F, Message = F, GradeBucketLabel = F, RecordType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempGradebookCloneError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradebookCloneError
	#'
	#' This function returns a dataframe or json object of a TempGradebookCloneError
	#' @param TempGradebookCloneErrorID The ID of the TempGradebookCloneError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradebookCloneError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradebookCloneError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradebookCloneError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempGradebookCloneError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradebookCloneError <- function(TempGradebookCloneErrorID, CalculationGroupGradeBucketID = F, SectionID = F, Field = F, Message = F, GradeBucketLabel = F, RecordType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradebookCloneErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempGradebookCloneError", objectId = TempGradebookCloneErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradebookCloneError
	#'
	#' This function deletes a TempGradebookCloneError
	#' @param TempGradebookCloneErrorID The ID of the TempGradebookCloneError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempGradebookCloneErrorID of the deleted TempGradebookCloneError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradebookCloneError <- function(TempGradebookCloneErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempGradebookCloneError", objectId = TempGradebookCloneErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradebookCloneError
	#'
	#' This function creates a TempGradebookCloneError
	#' @param fieldNames The field values to give the created TempGradebookCloneError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempGradebookCloneError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradebookCloneError <- function(CalculationGroupGradeBucketID = NULL, SectionID = NULL, Field = NULL, Message = NULL, GradeBucketLabel = NULL, RecordType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempGradebookCloneError", body = list(DataObject = body), searchFields = append("TempGradebookCloneErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradebookCloneError
	#'
	#' This function modifies a TempGradebookCloneError
	#' @param fieldNames The field values to give the modified TempGradebookCloneError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempGradebookCloneError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradebookCloneError <- function(TempGradebookCloneErrorID, CalculationGroupGradeBucketID = NULL, SectionID = NULL, Field = NULL, Message = NULL, GradeBucketLabel = NULL, RecordType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempGradebookCloneError", objectId = TempGradebookCloneErrorID, body = list(DataObject = body), searchFields = append("TempGradebookCloneErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionGradeBucketWeights
	#'
	#' This function returns a dataframe or json object of TempSectionGradeBucketWeights
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradeBucketWeights. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradeBucketWeights.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradeBucketWeight') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempSectionGradeBucketWeights
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionGradeBucketWeights <- function(searchConditionsList = NULL, TempSectionGradeBucketWeightID = F, CalculationGroupGradeBucketIDFeeder = F, CalculationGroupGradeBucketIDComposite = F, SectionID = F, WeightPercent = F, CanClone = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AcademicStandardIDToWeight = F, SubjectIDToWeight = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempSectionGradeBucketWeight", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionGradeBucketWeight
	#'
	#' This function returns a dataframe or json object of a TempSectionGradeBucketWeight
	#' @param TempSectionGradeBucketWeightID The ID of the TempSectionGradeBucketWeight to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradeBucketWeight. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradeBucketWeight.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradeBucketWeight') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempSectionGradeBucketWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionGradeBucketWeight <- function(TempSectionGradeBucketWeightID, CalculationGroupGradeBucketIDFeeder = F, CalculationGroupGradeBucketIDComposite = F, SectionID = F, WeightPercent = F, CanClone = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AcademicStandardIDToWeight = F, SubjectIDToWeight = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionGradeBucketWeightID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempSectionGradeBucketWeight", objectId = TempSectionGradeBucketWeightID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionGradeBucketWeight
	#'
	#' This function deletes a TempSectionGradeBucketWeight
	#' @param TempSectionGradeBucketWeightID The ID of the TempSectionGradeBucketWeight to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempSectionGradeBucketWeightID of the deleted TempSectionGradeBucketWeight.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionGradeBucketWeight <- function(TempSectionGradeBucketWeightID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempSectionGradeBucketWeight", objectId = TempSectionGradeBucketWeightID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionGradeBucketWeight
	#'
	#' This function creates a TempSectionGradeBucketWeight
	#' @param fieldNames The field values to give the created TempSectionGradeBucketWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempSectionGradeBucketWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionGradeBucketWeight <- function(CalculationGroupGradeBucketIDFeeder = NULL, CalculationGroupGradeBucketIDComposite = NULL, SectionID = NULL, WeightPercent = NULL, CanClone = NULL, AcademicStandardIDToWeight = NULL, SubjectIDToWeight = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempSectionGradeBucketWeight", body = list(DataObject = body), searchFields = append("TempSectionGradeBucketWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionGradeBucketWeight
	#'
	#' This function modifies a TempSectionGradeBucketWeight
	#' @param fieldNames The field values to give the modified TempSectionGradeBucketWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempSectionGradeBucketWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionGradeBucketWeight <- function(TempSectionGradeBucketWeightID, CalculationGroupGradeBucketIDFeeder = NULL, CalculationGroupGradeBucketIDComposite = NULL, SectionID = NULL, WeightPercent = NULL, CanClone = NULL, AcademicStandardIDToWeight = NULL, SubjectIDToWeight = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempSectionGradeBucketWeight", objectId = TempSectionGradeBucketWeightID, body = list(DataObject = body), searchFields = append("TempSectionGradeBucketWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionCategories
	#'
	#' This function returns a dataframe or json object of TempSectionCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempSectionCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionCategories <- function(searchConditionsList = NULL, TempSectionCategoryID = F, CalculationGroupGradeBucketID = F, CalculationGroupID = F, SectionID = F, CategoryID = F, CategoryCode = F, WeightPercent = F, CanClone = F, ErrorMessages = F, CategoryIDIsInvalid = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempSectionCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionCategory
	#'
	#' This function returns a dataframe or json object of a TempSectionCategory
	#' @param TempSectionCategoryID The ID of the TempSectionCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempSectionCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionCategory <- function(TempSectionCategoryID, CalculationGroupGradeBucketID = F, CalculationGroupID = F, SectionID = F, CategoryID = F, CategoryCode = F, WeightPercent = F, CanClone = F, ErrorMessages = F, CategoryIDIsInvalid = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempSectionCategory", objectId = TempSectionCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionCategory
	#'
	#' This function deletes a TempSectionCategory
	#' @param TempSectionCategoryID The ID of the TempSectionCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempSectionCategoryID of the deleted TempSectionCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionCategory <- function(TempSectionCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempSectionCategory", objectId = TempSectionCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionCategory
	#'
	#' This function creates a TempSectionCategory
	#' @param fieldNames The field values to give the created TempSectionCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempSectionCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionCategory <- function(CalculationGroupGradeBucketID = NULL, CalculationGroupID = NULL, SectionID = NULL, CategoryID = NULL, CategoryCode = NULL, WeightPercent = NULL, CanClone = NULL, ErrorMessages = NULL, CategoryIDIsInvalid = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempSectionCategory", body = list(DataObject = body), searchFields = append("TempSectionCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionCategory
	#'
	#' This function modifies a TempSectionCategory
	#' @param fieldNames The field values to give the modified TempSectionCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempSectionCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionCategory <- function(TempSectionCategoryID, CalculationGroupGradeBucketID = NULL, CalculationGroupID = NULL, SectionID = NULL, CategoryID = NULL, CategoryCode = NULL, WeightPercent = NULL, CanClone = NULL, ErrorMessages = NULL, CategoryIDIsInvalid = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempSectionCategory", objectId = TempSectionCategoryID, body = list(DataObject = body), searchFields = append("TempSectionCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionGradeBucketWeights
	#'
	#' This function returns a dataframe or json object of SectionGradeBucketWeights
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradeBucketWeights. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradeBucketWeights.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradeBucketWeight') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionGradeBucketWeights
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionGradeBucketWeights <- function(searchConditionsList = NULL, SectionGradeBucketWeightID = F, SectionGradeBucketIDFeeder = F, SectionGradeBucketIDComposite = F, WeightPercent = F, RoundBucketPercent = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionGradeBucketWeight", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionGradeBucketWeight
	#'
	#' This function returns a dataframe or json object of a SectionGradeBucketWeight
	#' @param SectionGradeBucketWeightID The ID of the SectionGradeBucketWeight to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradeBucketWeight. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradeBucketWeight.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradeBucketWeight') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionGradeBucketWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionGradeBucketWeight <- function(SectionGradeBucketWeightID, SectionGradeBucketIDFeeder = F, SectionGradeBucketIDComposite = F, WeightPercent = F, RoundBucketPercent = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionGradeBucketWeightID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionGradeBucketWeight", objectId = SectionGradeBucketWeightID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionGradeBucketWeight
	#'
	#' This function deletes a SectionGradeBucketWeight
	#' @param SectionGradeBucketWeightID The ID of the SectionGradeBucketWeight to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionGradeBucketWeightID of the deleted SectionGradeBucketWeight.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionGradeBucketWeight <- function(SectionGradeBucketWeightID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionGradeBucketWeight", objectId = SectionGradeBucketWeightID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionGradeBucketWeight
	#'
	#' This function creates a SectionGradeBucketWeight
	#' @param fieldNames The field values to give the created SectionGradeBucketWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionGradeBucketWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionGradeBucketWeight <- function(SectionGradeBucketIDFeeder = NULL, SectionGradeBucketIDComposite = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionGradeBucketWeight", body = list(DataObject = body), searchFields = append("SectionGradeBucketWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionGradeBucketWeight
	#'
	#' This function modifies a SectionGradeBucketWeight
	#' @param fieldNames The field values to give the modified SectionGradeBucketWeight. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionGradeBucketWeight
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionGradeBucketWeight <- function(SectionGradeBucketWeightID, SectionGradeBucketIDFeeder = NULL, SectionGradeBucketIDComposite = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionGradeBucketWeight", objectId = SectionGradeBucketWeightID, body = list(DataObject = body), searchFields = append("SectionGradeBucketWeightID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionCategories
	#'
	#' This function returns a dataframe or json object of SectionCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionCategories <- function(searchConditionsList = NULL, SectionCategoryID = F, SectionGradeBucketID = F, CategoryID = F, WeightPercent = F, PercentEarnedForCategory = F, HasAssignments = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, PointsEarnedForCategory = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionCategory
	#'
	#' This function returns a dataframe or json object of a SectionCategory
	#' @param SectionCategoryID The ID of the SectionCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionCategory <- function(SectionCategoryID, SectionGradeBucketID = F, CategoryID = F, WeightPercent = F, PercentEarnedForCategory = F, HasAssignments = F, AdjustedWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, PointsEarnedForCategory = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionCategory", objectId = SectionCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionCategory
	#'
	#' This function deletes a SectionCategory
	#' @param SectionCategoryID The ID of the SectionCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionCategoryID of the deleted SectionCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionCategory <- function(SectionCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionCategory", objectId = SectionCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionCategory
	#'
	#' This function creates a SectionCategory
	#' @param fieldNames The field values to give the created SectionCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionCategory <- function(SectionGradeBucketID = NULL, CategoryID = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionCategory", body = list(DataObject = body), searchFields = append("SectionCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionCategory
	#'
	#' This function modifies a SectionCategory
	#' @param fieldNames The field values to give the modified SectionCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionCategory <- function(SectionCategoryID, SectionGradeBucketID = NULL, CategoryID = NULL, WeightPercent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionCategory", objectId = SectionCategoryID, body = list(DataObject = body), searchFields = append("SectionCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionGradingScaleGradeBuckets
	#'
	#' This function returns a dataframe or json object of StudentSectionGradingScaleGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionGradingScaleGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionGradingScaleGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionGradingScaleGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentSectionGradingScaleGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionGradingScaleGradeBuckets <- function(searchConditionsList = NULL, StudentSectionGradingScaleGradeBucketID = F, GradingPeriodGradeBucketID = F, StudentGradingScaleGroupStudentSectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentSectionGradingScaleGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionGradingScaleGradeBucket
	#'
	#' This function returns a dataframe or json object of a StudentSectionGradingScaleGradeBucket
	#' @param StudentSectionGradingScaleGradeBucketID The ID of the StudentSectionGradingScaleGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionGradingScaleGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionGradingScaleGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionGradingScaleGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentSectionGradingScaleGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionGradingScaleGradeBucket <- function(StudentSectionGradingScaleGradeBucketID, GradingPeriodGradeBucketID = F, StudentGradingScaleGroupStudentSectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionGradingScaleGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentSectionGradingScaleGradeBucket", objectId = StudentSectionGradingScaleGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionGradingScaleGradeBucket
	#'
	#' This function deletes a StudentSectionGradingScaleGradeBucket
	#' @param StudentSectionGradingScaleGradeBucketID The ID of the StudentSectionGradingScaleGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentSectionGradingScaleGradeBucketID of the deleted StudentSectionGradingScaleGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionGradingScaleGradeBucket <- function(StudentSectionGradingScaleGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentSectionGradingScaleGradeBucket", objectId = StudentSectionGradingScaleGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionGradingScaleGradeBucket
	#'
	#' This function creates a StudentSectionGradingScaleGradeBucket
	#' @param fieldNames The field values to give the created StudentSectionGradingScaleGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentSectionGradingScaleGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionGradingScaleGradeBucket <- function(GradingPeriodGradeBucketID = NULL, StudentGradingScaleGroupStudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentSectionGradingScaleGradeBucket", body = list(DataObject = body), searchFields = append("StudentSectionGradingScaleGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionGradingScaleGradeBucket
	#'
	#' This function modifies a StudentSectionGradingScaleGradeBucket
	#' @param fieldNames The field values to give the modified StudentSectionGradingScaleGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentSectionGradingScaleGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionGradingScaleGradeBucket <- function(StudentSectionGradingScaleGradeBucketID, GradingPeriodGradeBucketID = NULL, StudentGradingScaleGroupStudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentSectionGradingScaleGradeBucket", objectId = StudentSectionGradingScaleGradeBucketID, body = list(DataObject = body), searchFields = append("StudentSectionGradingScaleGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionGradeBuckets
	#'
	#' This function returns a dataframe or json object of TempSectionGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempSectionGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionGradeBuckets <- function(searchConditionsList = NULL, TempSectionGradeBucketID = F, SectionGradeBucketID = F, CalculationGroupGradeBucketID = F, GradeBucketTypeID = F, IsSpecificGradeBucket = F, SectionID = F, GradeBucketLabel = F, CanClone = F, HasLeftOverPercentage = F, Exclude = F, ErrorMessages = F, StartDate = F, EndDate = F, ChildRecords = F, IsCalculationTypeOverridden = F, CurrentAllowCategoryOverride = F, CurrentCalculationType = F, NewCalculationType = F, Order = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempSectionGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionGradeBucket
	#'
	#' This function returns a dataframe or json object of a TempSectionGradeBucket
	#' @param TempSectionGradeBucketID The ID of the TempSectionGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempSectionGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionGradeBucket <- function(TempSectionGradeBucketID, SectionGradeBucketID = F, CalculationGroupGradeBucketID = F, GradeBucketTypeID = F, IsSpecificGradeBucket = F, SectionID = F, GradeBucketLabel = F, CanClone = F, HasLeftOverPercentage = F, Exclude = F, ErrorMessages = F, StartDate = F, EndDate = F, ChildRecords = F, IsCalculationTypeOverridden = F, CurrentAllowCategoryOverride = F, CurrentCalculationType = F, NewCalculationType = F, Order = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempSectionGradeBucket", objectId = TempSectionGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionGradeBucket
	#'
	#' This function deletes a TempSectionGradeBucket
	#' @param TempSectionGradeBucketID The ID of the TempSectionGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempSectionGradeBucketID of the deleted TempSectionGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionGradeBucket <- function(TempSectionGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempSectionGradeBucket", objectId = TempSectionGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionGradeBucket
	#'
	#' This function creates a TempSectionGradeBucket
	#' @param fieldNames The field values to give the created TempSectionGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempSectionGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionGradeBucket <- function(SectionGradeBucketID = NULL, CalculationGroupGradeBucketID = NULL, GradeBucketTypeID = NULL, IsSpecificGradeBucket = NULL, SectionID = NULL, GradeBucketLabel = NULL, CanClone = NULL, HasLeftOverPercentage = NULL, Exclude = NULL, ErrorMessages = NULL, StartDate = NULL, EndDate = NULL, ChildRecords = NULL, IsCalculationTypeOverridden = NULL, CurrentAllowCategoryOverride = NULL, CurrentCalculationType = NULL, NewCalculationType = NULL, Order = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempSectionGradeBucket", body = list(DataObject = body), searchFields = append("TempSectionGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionGradeBucket
	#'
	#' This function modifies a TempSectionGradeBucket
	#' @param fieldNames The field values to give the modified TempSectionGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempSectionGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionGradeBucket <- function(TempSectionGradeBucketID, SectionGradeBucketID = NULL, CalculationGroupGradeBucketID = NULL, GradeBucketTypeID = NULL, IsSpecificGradeBucket = NULL, SectionID = NULL, GradeBucketLabel = NULL, CanClone = NULL, HasLeftOverPercentage = NULL, Exclude = NULL, ErrorMessages = NULL, StartDate = NULL, EndDate = NULL, ChildRecords = NULL, IsCalculationTypeOverridden = NULL, CurrentAllowCategoryOverride = NULL, CurrentCalculationType = NULL, NewCalculationType = NULL, Order = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempSectionGradeBucket", objectId = TempSectionGradeBucketID, body = list(DataObject = body), searchFields = append("TempSectionGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempDropLowScoreStudentAssignments
	#'
	#' This function returns a dataframe or json object of TempDropLowScoreStudentAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempDropLowScoreStudentAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempDropLowScoreStudentAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempDropLowScoreStudentAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempDropLowScoreStudentAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempDropLowScoreStudentAssignments <- function(searchConditionsList = NULL, TempDropLowScoreStudentAssignmentID = F, StudentAssignmentID = F, StudentName = F, StudentSectionID = F, AssignmentName = F, DropCode = F, DueDate = F, MaxScore = F, Weight = F, DropLowScore = F, NoDropReason = F, Comments = F, Score = F, OriginalGrade = F, NewGrade = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentDisplaySortCode = F, DropActionType = F, UndoDropActionType = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempDropLowScoreStudentAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempDropLowScoreStudentAssignment
	#'
	#' This function returns a dataframe or json object of a TempDropLowScoreStudentAssignment
	#' @param TempDropLowScoreStudentAssignmentID The ID of the TempDropLowScoreStudentAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempDropLowScoreStudentAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempDropLowScoreStudentAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempDropLowScoreStudentAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempDropLowScoreStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempDropLowScoreStudentAssignment <- function(TempDropLowScoreStudentAssignmentID, StudentAssignmentID = F, StudentName = F, StudentSectionID = F, AssignmentName = F, DropCode = F, DueDate = F, MaxScore = F, Weight = F, DropLowScore = F, NoDropReason = F, Comments = F, Score = F, OriginalGrade = F, NewGrade = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentDisplaySortCode = F, DropActionType = F, UndoDropActionType = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempDropLowScoreStudentAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempDropLowScoreStudentAssignment", objectId = TempDropLowScoreStudentAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempDropLowScoreStudentAssignment
	#'
	#' This function deletes a TempDropLowScoreStudentAssignment
	#' @param TempDropLowScoreStudentAssignmentID The ID of the TempDropLowScoreStudentAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempDropLowScoreStudentAssignmentID of the deleted TempDropLowScoreStudentAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempDropLowScoreStudentAssignment <- function(TempDropLowScoreStudentAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempDropLowScoreStudentAssignment", objectId = TempDropLowScoreStudentAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempDropLowScoreStudentAssignment
	#'
	#' This function creates a TempDropLowScoreStudentAssignment
	#' @param fieldNames The field values to give the created TempDropLowScoreStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempDropLowScoreStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempDropLowScoreStudentAssignment <- function(StudentAssignmentID = NULL, StudentName = NULL, StudentSectionID = NULL, AssignmentName = NULL, DropCode = NULL, DueDate = NULL, MaxScore = NULL, Weight = NULL, DropLowScore = NULL, NoDropReason = NULL, Comments = NULL, Score = NULL, OriginalGrade = NULL, NewGrade = NULL, StudentDisplaySortCode = NULL, DropActionType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempDropLowScoreStudentAssignment", body = list(DataObject = body), searchFields = append("TempDropLowScoreStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempDropLowScoreStudentAssignment
	#'
	#' This function modifies a TempDropLowScoreStudentAssignment
	#' @param fieldNames The field values to give the modified TempDropLowScoreStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempDropLowScoreStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempDropLowScoreStudentAssignment <- function(TempDropLowScoreStudentAssignmentID, StudentAssignmentID = NULL, StudentName = NULL, StudentSectionID = NULL, AssignmentName = NULL, DropCode = NULL, DueDate = NULL, MaxScore = NULL, Weight = NULL, DropLowScore = NULL, NoDropReason = NULL, Comments = NULL, Score = NULL, OriginalGrade = NULL, NewGrade = NULL, StudentDisplaySortCode = NULL, DropActionType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempDropLowScoreStudentAssignment", objectId = TempDropLowScoreStudentAssignmentID, body = list(DataObject = body), searchFields = append("TempDropLowScoreStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookAssignments
	#'
	#' This function returns a dataframe or json object of GradebookAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookAssignments <- function(searchConditionsList = NULL, AssignmentID = F, SectionID = F, CategoryID = F, AssignedDate = F, DueDate = F, Name = F, Description = F, MaxScore = F, Weight = F, ScoreDisplayType = F, AssignmentSyncKey = F, IsDeleted = F, IsOnlineAssignment = F, Instructions = F, StartTime = F, EndTime = F, DefaultPointsPerQuestion = F, RandomizeQuestions = F, AutoScore = F, ShowCorrectAnswers = F, AnswerRevealTime = F, AssessmentID = F, AcademicStandardID = F, SubjectID = F, CanDelete = F, DisplayRestoreButton = F, UseGradeMarkScoring = F, ScoredStudentAssignmentCount = F, ActiveStudentCount = F, HasUngradedStudentAssignments = F, HasQuestions = F, QuestionCount = F, AveragePercent = F, DueDateInCurrentOpenExtendedGradingPeriod = F, IsAHistoricRecord = F, DueDateIsInGivenDateRange = F, IsPastEndTime = F, AssignmentQuestionCount = F, AttachmentCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowAutoScore = F, CalculatedPointsAllowedFromQuestions = F, SendNotificationWhenAssignmentReadyToTake = F, SendNotificationWhenAnswersRevealed = F, HideScoreUntilTime = F, HasStudentAssignments = F, HasStudentAssignmentsWithScoreOrGradeMark = F, AllStudentAssignmentsAreNotStarted = F, HasStudentFamilyAttachmentsToDisplay = F, StudentFamilyAccessAttachmentCount = F, ShowScore = F, HasWholeNumberWeight = F, AssignmentIDParent = F, IsParent = F, HasParent = F, DueDateInTransactionForGivenStudentSection = F, RelatedAssignmentsHaveAcademicStandards = F, RelatedAssignmentsHaveSubjects = F, ChildAssignmentCount = F, Subjects = F, AcademicStandards = F, IsActiveOrUnlocked = F, HasStudentGroupAssignments = F, ActiveStudentGroups = F, ClassesSyncedTo = F, IsAvailableForStudentSection = F, IsAvailableForStudentGroup = F, CreateStudentGroupAssignmentErrorMessage = F, IsSynced = F, EffectiveAcademicStandardID = F, AssignmentIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "Assignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookAssignment
	#'
	#' This function returns a dataframe or json object of a GradebookAssignment
	#' @param GradebookAssignmentID The ID of the GradebookAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookAssignment <- function(GradebookAssignmentID, AssignmentID = F, SectionID = F, CategoryID = F, AssignedDate = F, DueDate = F, Name = F, Description = F, MaxScore = F, Weight = F, ScoreDisplayType = F, AssignmentSyncKey = F, IsDeleted = F, IsOnlineAssignment = F, Instructions = F, StartTime = F, EndTime = F, DefaultPointsPerQuestion = F, RandomizeQuestions = F, AutoScore = F, ShowCorrectAnswers = F, AnswerRevealTime = F, AssessmentID = F, AcademicStandardID = F, SubjectID = F, CanDelete = F, DisplayRestoreButton = F, UseGradeMarkScoring = F, ScoredStudentAssignmentCount = F, ActiveStudentCount = F, HasUngradedStudentAssignments = F, HasQuestions = F, QuestionCount = F, AveragePercent = F, DueDateInCurrentOpenExtendedGradingPeriod = F, IsAHistoricRecord = F, DueDateIsInGivenDateRange = F, IsPastEndTime = F, AssignmentQuestionCount = F, AttachmentCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowAutoScore = F, CalculatedPointsAllowedFromQuestions = F, SendNotificationWhenAssignmentReadyToTake = F, SendNotificationWhenAnswersRevealed = F, HideScoreUntilTime = F, HasStudentAssignments = F, HasStudentAssignmentsWithScoreOrGradeMark = F, AllStudentAssignmentsAreNotStarted = F, HasStudentFamilyAttachmentsToDisplay = F, StudentFamilyAccessAttachmentCount = F, ShowScore = F, HasWholeNumberWeight = F, AssignmentIDParent = F, IsParent = F, HasParent = F, DueDateInTransactionForGivenStudentSection = F, RelatedAssignmentsHaveAcademicStandards = F, RelatedAssignmentsHaveSubjects = F, ChildAssignmentCount = F, Subjects = F, AcademicStandards = F, IsActiveOrUnlocked = F, HasStudentGroupAssignments = F, ActiveStudentGroups = F, ClassesSyncedTo = F, IsAvailableForStudentSection = F, IsAvailableForStudentGroup = F, CreateStudentGroupAssignmentErrorMessage = F, IsSynced = F, EffectiveAcademicStandardID = F, AssignmentIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "Assignment", objectId = GradebookAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookAssignment
	#'
	#' This function deletes a GradebookAssignment
	#' @param GradebookAssignmentID The ID of the GradebookAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookAssignmentID of the deleted GradebookAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookAssignment <- function(GradebookAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "Assignment", objectId = GradebookAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookAssignment
	#'
	#' This function creates a GradebookAssignment
	#' @param fieldNames The field values to give the created GradebookAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookAssignment <- function(SectionID = NULL, CategoryID = NULL, AssignedDate = NULL, DueDate = NULL, Name = NULL, Description = NULL, MaxScore = NULL, Weight = NULL, ScoreDisplayType = NULL, AssignmentSyncKey = NULL, IsDeleted = NULL, IsOnlineAssignment = NULL, Instructions = NULL, StartTime = NULL, EndTime = NULL, DefaultPointsPerQuestion = NULL, RandomizeQuestions = NULL, AutoScore = NULL, ShowCorrectAnswers = NULL, AnswerRevealTime = NULL, AssessmentID = NULL, AcademicStandardID = NULL, SubjectID = NULL, UseGradeMarkScoring = NULL, SendNotificationWhenAssignmentReadyToTake = NULL, SendNotificationWhenAnswersRevealed = NULL, HideScoreUntilTime = NULL, AssignmentIDParent = NULL, IsParent = NULL, AssignmentIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "Assignment", body = list(DataObject = body), searchFields = append("AssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookAssignment
	#'
	#' This function modifies a GradebookAssignment
	#' @param fieldNames The field values to give the modified GradebookAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookAssignment <- function(AssignmentID, SectionID = NULL, CategoryID = NULL, AssignedDate = NULL, DueDate = NULL, Name = NULL, Description = NULL, MaxScore = NULL, Weight = NULL, ScoreDisplayType = NULL, AssignmentSyncKey = NULL, IsDeleted = NULL, IsOnlineAssignment = NULL, Instructions = NULL, StartTime = NULL, EndTime = NULL, DefaultPointsPerQuestion = NULL, RandomizeQuestions = NULL, AutoScore = NULL, ShowCorrectAnswers = NULL, AnswerRevealTime = NULL, AssessmentID = NULL, AcademicStandardID = NULL, SubjectID = NULL, UseGradeMarkScoring = NULL, SendNotificationWhenAssignmentReadyToTake = NULL, SendNotificationWhenAnswersRevealed = NULL, HideScoreUntilTime = NULL, AssignmentIDParent = NULL, IsParent = NULL, AssignmentIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "Assignment", objectId = AssignmentID, body = list(DataObject = body), searchFields = append("AssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookCategories
	#'
	#' This function returns a dataframe or json object of GradebookCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookCategories <- function(searchConditionsList = NULL, CategoryID = F, Code = F, Description = F, DistrictID = F, SchoolYearID = F, DistrictGroupKey = F, CodeDescription = F, BackgroundColor = F, TextColor = F, UseForSpecificGradeBucketType = F, CategoryIDClonedFrom = F, AssignmentCount = F, GradeBucketTypeCategory = F, DescriptionWithCategoryWeightPercent = F, CategoryUsedInTotalPointsDefaultCalculationSetup = F, CategoryUsedInCategoryWeightingDefaultSetup = F, CalculationGroupAllowCategoryOverride = F, HasAssignments = F, AverageStudentAssignmentScoreForCategoryAndStudentSection = F, IsValidInCalculationGroup = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasSpecificCalculationGroupWeight = F, HasSpecificCalculationGroupSubjectWeight = F, HasSpecificCalculationGroupAcademicStandardWeight = F, WeightingAllowedForGradeBucketType = F, IsAHistoricRecord = F, EdFiGradebookEntryTypeDescriptorID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "Category", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookCategory
	#'
	#' This function returns a dataframe or json object of a GradebookCategory
	#' @param GradebookCategoryID The ID of the GradebookCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookCategory <- function(GradebookCategoryID, CategoryID = F, Code = F, Description = F, DistrictID = F, SchoolYearID = F, DistrictGroupKey = F, CodeDescription = F, BackgroundColor = F, TextColor = F, UseForSpecificGradeBucketType = F, CategoryIDClonedFrom = F, AssignmentCount = F, GradeBucketTypeCategory = F, DescriptionWithCategoryWeightPercent = F, CategoryUsedInTotalPointsDefaultCalculationSetup = F, CategoryUsedInCategoryWeightingDefaultSetup = F, CalculationGroupAllowCategoryOverride = F, HasAssignments = F, AverageStudentAssignmentScoreForCategoryAndStudentSection = F, IsValidInCalculationGroup = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasSpecificCalculationGroupWeight = F, HasSpecificCalculationGroupSubjectWeight = F, HasSpecificCalculationGroupAcademicStandardWeight = F, WeightingAllowedForGradeBucketType = F, IsAHistoricRecord = F, EdFiGradebookEntryTypeDescriptorID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "Category", objectId = GradebookCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookCategory
	#'
	#' This function deletes a GradebookCategory
	#' @param GradebookCategoryID The ID of the GradebookCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookCategoryID of the deleted GradebookCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookCategory <- function(GradebookCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "Category", objectId = GradebookCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookCategory
	#'
	#' This function creates a GradebookCategory
	#' @param fieldNames The field values to give the created GradebookCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookCategory <- function(Code = NULL, Description = NULL, DistrictID = NULL, SchoolYearID = NULL, DistrictGroupKey = NULL, BackgroundColor = NULL, TextColor = NULL, UseForSpecificGradeBucketType = NULL, CategoryIDClonedFrom = NULL, EdFiGradebookEntryTypeDescriptorID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "Category", body = list(DataObject = body), searchFields = append("CategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookCategory
	#'
	#' This function modifies a GradebookCategory
	#' @param fieldNames The field values to give the modified GradebookCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookCategory <- function(CategoryID, Code = NULL, Description = NULL, DistrictID = NULL, SchoolYearID = NULL, DistrictGroupKey = NULL, BackgroundColor = NULL, TextColor = NULL, UseForSpecificGradeBucketType = NULL, CategoryIDClonedFrom = NULL, EdFiGradebookEntryTypeDescriptorID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "Category", objectId = CategoryID, body = list(DataObject = body), searchFields = append("CategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupCategories
	#'
	#' This function returns a dataframe or json object of CalculationGroupCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupCategories <- function(searchConditionsList = NULL, CalculationGroupCategoryID = F, CalculationGroupID = F, CategoryID = F, EntityGroupKey = F, HasAssignments = F, CalculationGroupCategoryIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupCategory
	#'
	#' This function returns a dataframe or json object of a CalculationGroupCategory
	#' @param CalculationGroupCategoryID The ID of the CalculationGroupCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupCategory <- function(CalculationGroupCategoryID, CalculationGroupID = F, CategoryID = F, EntityGroupKey = F, HasAssignments = F, CalculationGroupCategoryIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupCategory", objectId = CalculationGroupCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupCategory
	#'
	#' This function deletes a CalculationGroupCategory
	#' @param CalculationGroupCategoryID The ID of the CalculationGroupCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupCategoryID of the deleted CalculationGroupCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupCategory <- function(CalculationGroupCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupCategory", objectId = CalculationGroupCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupCategory
	#'
	#' This function creates a CalculationGroupCategory
	#' @param fieldNames The field values to give the created CalculationGroupCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupCategory <- function(CalculationGroupID = NULL, CategoryID = NULL, EntityGroupKey = NULL, CalculationGroupCategoryIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupCategory", body = list(DataObject = body), searchFields = append("CalculationGroupCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupCategory
	#'
	#' This function modifies a CalculationGroupCategory
	#' @param fieldNames The field values to give the modified CalculationGroupCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupCategory <- function(CalculationGroupCategoryID, CalculationGroupID = NULL, CategoryID = NULL, EntityGroupKey = NULL, CalculationGroupCategoryIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupCategory", objectId = CalculationGroupCategoryID, body = list(DataObject = body), searchFields = append("CalculationGroupCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupCourses
	#'
	#' This function returns a dataframe or json object of CalculationGroupCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupCourses <- function(searchConditionsList = NULL, CalculationGroupCourseID = F, CalculationGroupID = F, CourseID = F, CalculationGroupCourseIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupCourse
	#'
	#' This function returns a dataframe or json object of a CalculationGroupCourse
	#' @param CalculationGroupCourseID The ID of the CalculationGroupCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupCourse <- function(CalculationGroupCourseID, CalculationGroupID = F, CourseID = F, CalculationGroupCourseIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupCourse", objectId = CalculationGroupCourseID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupCourse
	#'
	#' This function deletes a CalculationGroupCourse
	#' @param CalculationGroupCourseID The ID of the CalculationGroupCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupCourseID of the deleted CalculationGroupCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupCourse <- function(CalculationGroupCourseID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupCourse", objectId = CalculationGroupCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupCourse
	#'
	#' This function creates a CalculationGroupCourse
	#' @param fieldNames The field values to give the created CalculationGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupCourse <- function(CalculationGroupID = NULL, CourseID = NULL, CalculationGroupCourseIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupCourse", body = list(DataObject = body), searchFields = append("CalculationGroupCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupCourse
	#'
	#' This function modifies a CalculationGroupCourse
	#' @param fieldNames The field values to give the modified CalculationGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupCourse <- function(CalculationGroupCourseID, CalculationGroupID = NULL, CourseID = NULL, CalculationGroupCourseIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupCourse", objectId = CalculationGroupCourseID, body = list(DataObject = body), searchFields = append("CalculationGroupCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupGradeBuckets
	#'
	#' This function returns a dataframe or json object of CalculationGroupGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupGradeBuckets <- function(searchConditionsList = NULL, CalculationGroupGradeBucketID = F, CalculationGroupID = F, GradingPeriodGradeBucketID = F, AllowCalculationTypeOverride = F, AllowWeightOverride = F, CalculationType = F, CalculationGroupGradeBucketIDClonedFrom = F, RoundBucketPercent = F, GradeMarkScoringType = F, IsAHistoricRecord = F, HasCategoryWeighting = F, HasGradeBucketWeighting = F, HasSubjectWeighting = F, HasAcademicStandardWeighting = F, WeightPercentTotal = F, InnerCalculationGroupGradeBucketsCount = F, EffectiveCalculationTypeCode = F, NumberOfCategories = F, HasSubjects = F, HasAcademicStandards = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, CourseCount = F, InUseBySections = F, IsMissingStudentGradeBucketAcademicStandards = F, IsMissingStudentGradeBucketSubjects = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupGradeBucket
	#'
	#' This function returns a dataframe or json object of a CalculationGroupGradeBucket
	#' @param CalculationGroupGradeBucketID The ID of the CalculationGroupGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupGradeBucket <- function(CalculationGroupGradeBucketID, CalculationGroupID = F, GradingPeriodGradeBucketID = F, AllowCalculationTypeOverride = F, AllowWeightOverride = F, CalculationType = F, CalculationGroupGradeBucketIDClonedFrom = F, RoundBucketPercent = F, GradeMarkScoringType = F, IsAHistoricRecord = F, HasCategoryWeighting = F, HasGradeBucketWeighting = F, HasSubjectWeighting = F, HasAcademicStandardWeighting = F, WeightPercentTotal = F, InnerCalculationGroupGradeBucketsCount = F, EffectiveCalculationTypeCode = F, NumberOfCategories = F, HasSubjects = F, HasAcademicStandards = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, CourseCount = F, InUseBySections = F, IsMissingStudentGradeBucketAcademicStandards = F, IsMissingStudentGradeBucketSubjects = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupGradeBucket", objectId = CalculationGroupGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupGradeBucket
	#'
	#' This function deletes a CalculationGroupGradeBucket
	#' @param CalculationGroupGradeBucketID The ID of the CalculationGroupGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupGradeBucketID of the deleted CalculationGroupGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupGradeBucket <- function(CalculationGroupGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupGradeBucket", objectId = CalculationGroupGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupGradeBucket
	#'
	#' This function creates a CalculationGroupGradeBucket
	#' @param fieldNames The field values to give the created CalculationGroupGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupGradeBucket <- function(CalculationGroupID = NULL, GradingPeriodGradeBucketID = NULL, AllowCalculationTypeOverride = NULL, AllowWeightOverride = NULL, CalculationType = NULL, CalculationGroupGradeBucketIDClonedFrom = NULL, RoundBucketPercent = NULL, GradeMarkScoringType = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupGradeBucket", body = list(DataObject = body), searchFields = append("CalculationGroupGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupGradeBucket
	#'
	#' This function modifies a CalculationGroupGradeBucket
	#' @param fieldNames The field values to give the modified CalculationGroupGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupGradeBucket <- function(CalculationGroupGradeBucketID, CalculationGroupID = NULL, GradingPeriodGradeBucketID = NULL, AllowCalculationTypeOverride = NULL, AllowWeightOverride = NULL, CalculationType = NULL, CalculationGroupGradeBucketIDClonedFrom = NULL, RoundBucketPercent = NULL, GradeMarkScoringType = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupGradeBucket", objectId = CalculationGroupGradeBucketID, body = list(DataObject = body), searchFields = append("CalculationGroupGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupWeightings
	#'
	#' This function returns a dataframe or json object of CalculationGroupWeightings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupWeightings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupWeightings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupWeighting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupWeightings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupWeightings <- function(searchConditionsList = NULL, CalculationGroupWeightingID = F, CalculationGroupGradeBucketID = F, CategoryIDToWeight = F, GradingPeriodGradeBucketIDToWeight = F, SubjectIDToWeight = F, AcademicStandardIDToWeight = F, WeightPercent = F, RoundBucketPercent = F, RequiredGrade = F, CalculationGroupWeightingIDClonedFrom = F, PercentEarnedForCategory = F, IsAHistoricRecord = F, AdjustedGradeBucketWeightPercent = F, AdjustedCategoryWeightPercent = F, AdjustedSubjectWeightPercent = F, AdjustedAcademicStandardWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, PointsEarnedForCategory = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupWeighting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupWeighting
	#'
	#' This function returns a dataframe or json object of a CalculationGroupWeighting
	#' @param CalculationGroupWeightingID The ID of the CalculationGroupWeighting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupWeighting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupWeighting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupWeighting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupWeighting <- function(CalculationGroupWeightingID, CalculationGroupGradeBucketID = F, CategoryIDToWeight = F, GradingPeriodGradeBucketIDToWeight = F, SubjectIDToWeight = F, AcademicStandardIDToWeight = F, WeightPercent = F, RoundBucketPercent = F, RequiredGrade = F, CalculationGroupWeightingIDClonedFrom = F, PercentEarnedForCategory = F, IsAHistoricRecord = F, AdjustedGradeBucketWeightPercent = F, AdjustedCategoryWeightPercent = F, AdjustedSubjectWeightPercent = F, AdjustedAcademicStandardWeightPercent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, EntityGroupKey = F, PointsEarnedForCategory = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupWeightingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupWeighting", objectId = CalculationGroupWeightingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupWeighting
	#'
	#' This function deletes a CalculationGroupWeighting
	#' @param CalculationGroupWeightingID The ID of the CalculationGroupWeighting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupWeightingID of the deleted CalculationGroupWeighting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupWeighting <- function(CalculationGroupWeightingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupWeighting", objectId = CalculationGroupWeightingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupWeighting
	#'
	#' This function creates a CalculationGroupWeighting
	#' @param fieldNames The field values to give the created CalculationGroupWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupWeighting <- function(CalculationGroupGradeBucketID = NULL, CategoryIDToWeight = NULL, GradingPeriodGradeBucketIDToWeight = NULL, SubjectIDToWeight = NULL, AcademicStandardIDToWeight = NULL, WeightPercent = NULL, RoundBucketPercent = NULL, RequiredGrade = NULL, CalculationGroupWeightingIDClonedFrom = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupWeighting", body = list(DataObject = body), searchFields = append("CalculationGroupWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupWeighting
	#'
	#' This function modifies a CalculationGroupWeighting
	#' @param fieldNames The field values to give the modified CalculationGroupWeighting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupWeighting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupWeighting <- function(CalculationGroupWeightingID, CalculationGroupGradeBucketID = NULL, CategoryIDToWeight = NULL, GradingPeriodGradeBucketIDToWeight = NULL, SubjectIDToWeight = NULL, AcademicStandardIDToWeight = NULL, WeightPercent = NULL, RoundBucketPercent = NULL, RequiredGrade = NULL, CalculationGroupWeightingIDClonedFrom = NULL, EntityGroupKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupWeighting", objectId = CalculationGroupWeightingID, body = list(DataObject = body), searchFields = append("CalculationGroupWeightingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGradingScaleGroupCourses
	#'
	#' This function returns a dataframe or json object of CourseGradingScaleGroupCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradingScaleGroupCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradingScaleGroupCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradingScaleGroupCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CourseGradingScaleGroupCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGradingScaleGroupCourses <- function(searchConditionsList = NULL, CourseGradingScaleGroupCourseID = F, CourseGradingScaleGroupID = F, CourseID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CourseGradingScaleGroupCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGradingScaleGroupCourse
	#'
	#' This function returns a dataframe or json object of a CourseGradingScaleGroupCourse
	#' @param CourseGradingScaleGroupCourseID The ID of the CourseGradingScaleGroupCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradingScaleGroupCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradingScaleGroupCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradingScaleGroupCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CourseGradingScaleGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGradingScaleGroupCourse <- function(CourseGradingScaleGroupCourseID, CourseGradingScaleGroupID = F, CourseID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGradingScaleGroupCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroupCourse", objectId = CourseGradingScaleGroupCourseID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGradingScaleGroupCourse
	#'
	#' This function deletes a CourseGradingScaleGroupCourse
	#' @param CourseGradingScaleGroupCourseID The ID of the CourseGradingScaleGroupCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CourseGradingScaleGroupCourseID of the deleted CourseGradingScaleGroupCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGradingScaleGroupCourse <- function(CourseGradingScaleGroupCourseID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroupCourse", objectId = CourseGradingScaleGroupCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGradingScaleGroupCourse
	#'
	#' This function creates a CourseGradingScaleGroupCourse
	#' @param fieldNames The field values to give the created CourseGradingScaleGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CourseGradingScaleGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGradingScaleGroupCourse <- function(CourseGradingScaleGroupID = NULL, CourseID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroupCourse", body = list(DataObject = body), searchFields = append("CourseGradingScaleGroupCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGradingScaleGroupCourse
	#'
	#' This function modifies a CourseGradingScaleGroupCourse
	#' @param fieldNames The field values to give the modified CourseGradingScaleGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CourseGradingScaleGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGradingScaleGroupCourse <- function(CourseGradingScaleGroupCourseID, CourseGradingScaleGroupID = NULL, CourseID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroupCourse", objectId = CourseGradingScaleGroupCourseID, body = list(DataObject = body), searchFields = append("CourseGradingScaleGroupCourseID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ScoreClarifiers
	#'
	#' This function returns a dataframe or json object of ScoreClarifiers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ScoreClarifiers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ScoreClarifiers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ScoreClarifier') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of ScoreClarifiers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listScoreClarifiers <- function(searchConditionsList = NULL, ScoreClarifierID = F, Code = F, Description = F, IsMissing = F, NoCount = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, ScoreClarifierIDClonedFrom = F, CodeDescription = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ScoreClarifier", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ScoreClarifier
	#'
	#' This function returns a dataframe or json object of a ScoreClarifier
	#' @param ScoreClarifierID The ID of the ScoreClarifier to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ScoreClarifier. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ScoreClarifier.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ScoreClarifier') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of ScoreClarifier
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getScoreClarifier <- function(ScoreClarifierID, Code = F, Description = F, IsMissing = F, NoCount = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, ScoreClarifierIDClonedFrom = F, CodeDescription = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ScoreClarifierID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ScoreClarifier", objectId = ScoreClarifierID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ScoreClarifier
	#'
	#' This function deletes a ScoreClarifier
	#' @param ScoreClarifierID The ID of the ScoreClarifier to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The ScoreClarifierID of the deleted ScoreClarifier.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteScoreClarifier <- function(ScoreClarifierID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ScoreClarifier", objectId = ScoreClarifierID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ScoreClarifier
	#'
	#' This function creates a ScoreClarifier
	#' @param fieldNames The field values to give the created ScoreClarifier. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created ScoreClarifier
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createScoreClarifier <- function(Code = NULL, Description = NULL, IsMissing = NULL, NoCount = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, ScoreClarifierIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ScoreClarifier", body = list(DataObject = body), searchFields = append("ScoreClarifierID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ScoreClarifier
	#'
	#' This function modifies a ScoreClarifier
	#' @param fieldNames The field values to give the modified ScoreClarifier. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified ScoreClarifier
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyScoreClarifier <- function(ScoreClarifierID, Code = NULL, Description = NULL, IsMissing = NULL, NoCount = NULL, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, ScoreClarifierIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ScoreClarifier", objectId = ScoreClarifierID, body = list(DataObject = body), searchFields = append("ScoreClarifierID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionGradeBuckets
	#'
	#' This function returns a dataframe or json object of SectionGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionGradeBuckets <- function(searchConditionsList = NULL, SectionGradeBucketID = F, SectionID = F, CalculationGroupGradeBucketID = F, IsCalculationTypeOverridden = F, IsOverridden = F, EffectiveCalculationTypeCode = F, HasSectionCategories = F, HasSectionGradeBucketWeights = F, IsAHistoricRecord = F, HasSectionSubjectWeights = F, HasSectionAcademicStandardWeights = F, CalculationModifiedTime = F, CalculationType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasWeightOverride = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionGradeBucket
	#'
	#' This function returns a dataframe or json object of a SectionGradeBucket
	#' @param SectionGradeBucketID The ID of the SectionGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionGradeBucket <- function(SectionGradeBucketID, SectionID = F, CalculationGroupGradeBucketID = F, IsCalculationTypeOverridden = F, IsOverridden = F, EffectiveCalculationTypeCode = F, HasSectionCategories = F, HasSectionGradeBucketWeights = F, IsAHistoricRecord = F, HasSectionSubjectWeights = F, HasSectionAcademicStandardWeights = F, CalculationModifiedTime = F, CalculationType = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, HasWeightOverride = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionGradeBucket", objectId = SectionGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionGradeBucket
	#'
	#' This function deletes a SectionGradeBucket
	#' @param SectionGradeBucketID The ID of the SectionGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionGradeBucketID of the deleted SectionGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionGradeBucket <- function(SectionGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionGradeBucket", objectId = SectionGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionGradeBucket
	#'
	#' This function creates a SectionGradeBucket
	#' @param fieldNames The field values to give the created SectionGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionGradeBucket <- function(SectionID = NULL, CalculationGroupGradeBucketID = NULL, IsCalculationTypeOverridden = NULL, CalculationType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionGradeBucket", body = list(DataObject = body), searchFields = append("SectionGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionGradeBucket
	#'
	#' This function modifies a SectionGradeBucket
	#' @param fieldNames The field values to give the modified SectionGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionGradeBucket <- function(SectionGradeBucketID, SectionID = NULL, CalculationGroupGradeBucketID = NULL, IsCalculationTypeOverridden = NULL, CalculationType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionGradeBucket", objectId = SectionGradeBucketID, body = list(DataObject = body), searchFields = append("SectionGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionGradingScaleGroups
	#'
	#' This function returns a dataframe or json object of SectionGradingScaleGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradingScaleGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradingScaleGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradingScaleGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of SectionGradingScaleGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionGradingScaleGroups <- function(searchConditionsList = NULL, SectionGradingScaleGroupID = F, SectionID = F, GradingPeriodGradeBucketID = F, SectionGradingScaleGroupIDClonedFrom = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingScaleGroupID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "SectionGradingScaleGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionGradingScaleGroup
	#'
	#' This function returns a dataframe or json object of a SectionGradingScaleGroup
	#' @param SectionGradingScaleGroupID The ID of the SectionGradingScaleGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionGradingScaleGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionGradingScaleGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionGradingScaleGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of SectionGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionGradingScaleGroup <- function(SectionGradingScaleGroupID, SectionID = F, GradingPeriodGradeBucketID = F, SectionGradingScaleGroupIDClonedFrom = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingScaleGroupID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionGradingScaleGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "SectionGradingScaleGroup", objectId = SectionGradingScaleGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionGradingScaleGroup
	#'
	#' This function deletes a SectionGradingScaleGroup
	#' @param SectionGradingScaleGroupID The ID of the SectionGradingScaleGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The SectionGradingScaleGroupID of the deleted SectionGradingScaleGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionGradingScaleGroup <- function(SectionGradingScaleGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "SectionGradingScaleGroup", objectId = SectionGradingScaleGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionGradingScaleGroup
	#'
	#' This function creates a SectionGradingScaleGroup
	#' @param fieldNames The field values to give the created SectionGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created SectionGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionGradingScaleGroup <- function(SectionID = NULL, GradingPeriodGradeBucketID = NULL, SectionGradingScaleGroupIDClonedFrom = NULL, GradingScaleGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "SectionGradingScaleGroup", body = list(DataObject = body), searchFields = append("SectionGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionGradingScaleGroup
	#'
	#' This function modifies a SectionGradingScaleGroup
	#' @param fieldNames The field values to give the modified SectionGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified SectionGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionGradingScaleGroup <- function(SectionGradingScaleGroupID, SectionID = NULL, GradingPeriodGradeBucketID = NULL, SectionGradingScaleGroupIDClonedFrom = NULL, GradingScaleGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "SectionGradingScaleGroup", objectId = SectionGradingScaleGroupID, body = list(DataObject = body), searchFields = append("SectionGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroups
	#'
	#' This function returns a dataframe or json object of CalculationGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroups <- function(searchConditionsList = NULL, CalculationGroupID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, Description = F, Filter = F, AllowCategoryOverride = F, Rank = F, IsDefaultGroup = F, CalculationGroupIDClonedFrom = F, LimitTeacherOverridesToCategoriesInGroup = F, IsAHistoricRecord = F, HasSubjects = F, HasAcademicStandards = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowAssignmentPointsTeacherOverride = F, AllowCategoryWeightingTeacherOverride = F, AllowGradeBucketWeightingTeacherOverride = F, AllowTotalPointsAndGradeBucketWeightingTeacherOverride = F, AllowNotGradedTeacherOverride = F, AllowSubjectiveTeacherOverride = F, AllowSubjectWeightingTeacherOverride = F, AllowAcademicStandardWeightingTeacherOverride = F, HasAssignmentPointsOverrides = F, HasCategoryWeightingOverrides = F, HasGradeBucketWeightingOverrides = F, HasTotalPointsAndGradeBucketWeightingOverrides = F, HasNotGradedOverrides = F, HasSubjectiveOverrides = F, HasSubjectWeightingOverrides = F, HasAcademicStandardWeightingOverrides = F, AllowDecayingAverageTeacherOverride = F, DecayingAveragePercent = F, HasDecayingAverageOverrides = F, HasDecayingAverage = F, AllowAssignmentAveragePercentTeacherOverride = F, HasAssignmentAveragePercentOverrides = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroup
	#'
	#' This function returns a dataframe or json object of a CalculationGroup
	#' @param CalculationGroupID The ID of the CalculationGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroup <- function(CalculationGroupID, EntityID = F, SchoolYearID = F, EntityGroupKey = F, Description = F, Filter = F, AllowCategoryOverride = F, Rank = F, IsDefaultGroup = F, CalculationGroupIDClonedFrom = F, LimitTeacherOverridesToCategoriesInGroup = F, IsAHistoricRecord = F, HasSubjects = F, HasAcademicStandards = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowAssignmentPointsTeacherOverride = F, AllowCategoryWeightingTeacherOverride = F, AllowGradeBucketWeightingTeacherOverride = F, AllowTotalPointsAndGradeBucketWeightingTeacherOverride = F, AllowNotGradedTeacherOverride = F, AllowSubjectiveTeacherOverride = F, AllowSubjectWeightingTeacherOverride = F, AllowAcademicStandardWeightingTeacherOverride = F, HasAssignmentPointsOverrides = F, HasCategoryWeightingOverrides = F, HasGradeBucketWeightingOverrides = F, HasTotalPointsAndGradeBucketWeightingOverrides = F, HasNotGradedOverrides = F, HasSubjectiveOverrides = F, HasSubjectWeightingOverrides = F, HasAcademicStandardWeightingOverrides = F, AllowDecayingAverageTeacherOverride = F, DecayingAveragePercent = F, HasDecayingAverageOverrides = F, HasDecayingAverage = F, AllowAssignmentAveragePercentTeacherOverride = F, HasAssignmentAveragePercentOverrides = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroup", objectId = CalculationGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroup
	#'
	#' This function deletes a CalculationGroup
	#' @param CalculationGroupID The ID of the CalculationGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupID of the deleted CalculationGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroup <- function(CalculationGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroup", objectId = CalculationGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroup
	#'
	#' This function creates a CalculationGroup
	#' @param fieldNames The field values to give the created CalculationGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroup <- function(EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, Description = NULL, Filter = NULL, AllowCategoryOverride = NULL, Rank = NULL, IsDefaultGroup = NULL, CalculationGroupIDClonedFrom = NULL, LimitTeacherOverridesToCategoriesInGroup = NULL, AllowAssignmentPointsTeacherOverride = NULL, AllowCategoryWeightingTeacherOverride = NULL, AllowGradeBucketWeightingTeacherOverride = NULL, AllowTotalPointsAndGradeBucketWeightingTeacherOverride = NULL, AllowNotGradedTeacherOverride = NULL, AllowSubjectiveTeacherOverride = NULL, AllowSubjectWeightingTeacherOverride = NULL, AllowAcademicStandardWeightingTeacherOverride = NULL, AllowDecayingAverageTeacherOverride = NULL, DecayingAveragePercent = NULL, AllowAssignmentAveragePercentTeacherOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroup", body = list(DataObject = body), searchFields = append("CalculationGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroup
	#'
	#' This function modifies a CalculationGroup
	#' @param fieldNames The field values to give the modified CalculationGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroup <- function(CalculationGroupID, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, Description = NULL, Filter = NULL, AllowCategoryOverride = NULL, Rank = NULL, IsDefaultGroup = NULL, CalculationGroupIDClonedFrom = NULL, LimitTeacherOverridesToCategoriesInGroup = NULL, AllowAssignmentPointsTeacherOverride = NULL, AllowCategoryWeightingTeacherOverride = NULL, AllowGradeBucketWeightingTeacherOverride = NULL, AllowTotalPointsAndGradeBucketWeightingTeacherOverride = NULL, AllowNotGradedTeacherOverride = NULL, AllowSubjectiveTeacherOverride = NULL, AllowSubjectWeightingTeacherOverride = NULL, AllowAcademicStandardWeightingTeacherOverride = NULL, AllowDecayingAverageTeacherOverride = NULL, DecayingAveragePercent = NULL, AllowAssignmentAveragePercentTeacherOverride = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroup", objectId = CalculationGroupID, body = list(DataObject = body), searchFields = append("CalculationGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGradingScaleGroups
	#'
	#' This function returns a dataframe or json object of CourseGradingScaleGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradingScaleGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradingScaleGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradingScaleGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CourseGradingScaleGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGradingScaleGroups <- function(searchConditionsList = NULL, CourseGradingScaleGroupID = F, EntityID = F, SchoolYearID = F, Filter = F, Description = F, EntityGroupKey = F, Rank = F, AllowSectionOverride = F, IsDefaultGroup = F, CourseGradingScaleGroupIDClonedFrom = F, LimitTeacherOverridesToGradeMarksInGroup = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingScaleGroupID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CourseGradingScaleGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGradingScaleGroup
	#'
	#' This function returns a dataframe or json object of a CourseGradingScaleGroup
	#' @param CourseGradingScaleGroupID The ID of the CourseGradingScaleGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradingScaleGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradingScaleGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradingScaleGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CourseGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGradingScaleGroup <- function(CourseGradingScaleGroupID, EntityID = F, SchoolYearID = F, Filter = F, Description = F, EntityGroupKey = F, Rank = F, AllowSectionOverride = F, IsDefaultGroup = F, CourseGradingScaleGroupIDClonedFrom = F, LimitTeacherOverridesToGradeMarksInGroup = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingScaleGroupID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGradingScaleGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroup", objectId = CourseGradingScaleGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGradingScaleGroup
	#'
	#' This function deletes a CourseGradingScaleGroup
	#' @param CourseGradingScaleGroupID The ID of the CourseGradingScaleGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CourseGradingScaleGroupID of the deleted CourseGradingScaleGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGradingScaleGroup <- function(CourseGradingScaleGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroup", objectId = CourseGradingScaleGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGradingScaleGroup
	#'
	#' This function creates a CourseGradingScaleGroup
	#' @param fieldNames The field values to give the created CourseGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CourseGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGradingScaleGroup <- function(EntityID = NULL, SchoolYearID = NULL, Filter = NULL, Description = NULL, EntityGroupKey = NULL, Rank = NULL, AllowSectionOverride = NULL, IsDefaultGroup = NULL, CourseGradingScaleGroupIDClonedFrom = NULL, LimitTeacherOverridesToGradeMarksInGroup = NULL, GradingScaleGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroup", body = list(DataObject = body), searchFields = append("CourseGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGradingScaleGroup
	#'
	#' This function modifies a CourseGradingScaleGroup
	#' @param fieldNames The field values to give the modified CourseGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CourseGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGradingScaleGroup <- function(CourseGradingScaleGroupID, EntityID = NULL, SchoolYearID = NULL, Filter = NULL, Description = NULL, EntityGroupKey = NULL, Rank = NULL, AllowSectionOverride = NULL, IsDefaultGroup = NULL, CourseGradingScaleGroupIDClonedFrom = NULL, LimitTeacherOverridesToGradeMarksInGroup = NULL, GradingScaleGroupID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CourseGradingScaleGroup", objectId = CourseGradingScaleGroupID, body = list(DataObject = body), searchFields = append("CourseGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradingScaleGroupGradeMarks
	#'
	#' This function returns a dataframe or json object of GradingScaleGroupGradeMarks
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingScaleGroupGradeMarks. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingScaleGroupGradeMarks.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingScaleGroupGradeMark') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradingScaleGroupGradeMarks
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradingScaleGroupGradeMarks <- function(searchConditionsList = NULL, GradingScaleGroupGradeMarkID = F, GradeMarkID = F, PercentLow = F, PercentHigh = F, DefaultCalculationPercent = F, Color = F, GradingScaleGroupID = F, GradingScaleGroupGradeMarkIDClonedFrom = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowSubjective = F, PointsLow = F, PointsHigh = F, DefaultCalculationPoints = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "GradingScaleGroupGradeMark", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradingScaleGroupGradeMark
	#'
	#' This function returns a dataframe or json object of a GradingScaleGroupGradeMark
	#' @param GradingScaleGroupGradeMarkID The ID of the GradingScaleGroupGradeMark to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradingScaleGroupGradeMark. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradingScaleGroupGradeMark.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradingScaleGroupGradeMark') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradingScaleGroupGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradingScaleGroupGradeMark <- function(GradingScaleGroupGradeMarkID, GradeMarkID = F, PercentLow = F, PercentHigh = F, DefaultCalculationPercent = F, Color = F, GradingScaleGroupID = F, GradingScaleGroupGradeMarkIDClonedFrom = F, EntityGroupKey = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AllowSubjective = F, PointsLow = F, PointsHigh = F, DefaultCalculationPoints = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradingScaleGroupGradeMarkID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "GradingScaleGroupGradeMark", objectId = GradingScaleGroupGradeMarkID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradingScaleGroupGradeMark
	#'
	#' This function deletes a GradingScaleGroupGradeMark
	#' @param GradingScaleGroupGradeMarkID The ID of the GradingScaleGroupGradeMark to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradingScaleGroupGradeMarkID of the deleted GradingScaleGroupGradeMark.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradingScaleGroupGradeMark <- function(GradingScaleGroupGradeMarkID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "GradingScaleGroupGradeMark", objectId = GradingScaleGroupGradeMarkID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradingScaleGroupGradeMark
	#'
	#' This function creates a GradingScaleGroupGradeMark
	#' @param fieldNames The field values to give the created GradingScaleGroupGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradingScaleGroupGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradingScaleGroupGradeMark <- function(GradeMarkID = NULL, PercentLow = NULL, PercentHigh = NULL, DefaultCalculationPercent = NULL, Color = NULL, GradingScaleGroupID = NULL, GradingScaleGroupGradeMarkIDClonedFrom = NULL, EntityGroupKey = NULL, AllowSubjective = NULL, PointsLow = NULL, PointsHigh = NULL, DefaultCalculationPoints = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "GradingScaleGroupGradeMark", body = list(DataObject = body), searchFields = append("GradingScaleGroupGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradingScaleGroupGradeMark
	#'
	#' This function modifies a GradingScaleGroupGradeMark
	#' @param fieldNames The field values to give the modified GradingScaleGroupGradeMark. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradingScaleGroupGradeMark
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradingScaleGroupGradeMark <- function(GradingScaleGroupGradeMarkID, GradeMarkID = NULL, PercentLow = NULL, PercentHigh = NULL, DefaultCalculationPercent = NULL, Color = NULL, GradingScaleGroupID = NULL, GradingScaleGroupGradeMarkIDClonedFrom = NULL, EntityGroupKey = NULL, AllowSubjective = NULL, PointsLow = NULL, PointsHigh = NULL, DefaultCalculationPoints = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "GradingScaleGroupGradeMark", objectId = GradingScaleGroupGradeMarkID, body = list(DataObject = body), searchFields = append("GradingScaleGroupGradeMarkID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAssignments
	#'
	#' This function returns a dataframe or json object of StudentAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAssignments <- function(searchConditionsList = NULL, StudentAssignmentID = F, StudentSectionID = F, AssignmentID = F, ScoreClarifierID = F, Score = F, Comments = F, IsMissing = F, IsGraded = F, NoCount = F, IsDropped = F, SubmissionStatus = F, SubmissionTime = F, SectionID = F, GradeMarkID = F, PointsEarned = F, PointsEarnedWithSlash = F, PointsEarnedPercent = F, ScoreDisplayValue = F, AllQuestionsScored = F, IsExpired = F, ScoreDisplayValueNoGradeMark = F, IsTransferredScore = F, IsFutureRetainedScore = F, IsScored = F, HasStudentSectionTransaction = F, AssignmentDueDateAttendance = F, StudentQuestionCount = F, HasAnswers = F, AnsweredQuestionCount = F, CurrentQuestionNumber = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentOnlineAssignmentDisplayPointsEarnedWithSlash = F, GradeMarkCode = F, OnlineAssignmentNotificationSent = F, OnlineAssignmentReviewNotificationSent = F, AnswerKeyIsAvailableToView = F, PointsEarnedWithSlashCheckDisplay = F, SubmissionStatusToUse = F, CannotBeTakenByStudent = F, CalculatedPoints = F, ScoreHasChangedWithinSpecifiedAmountOfTime = F, StudentOnlineAssignmentDisplayPointsEarned = F, AttemptCount = F, TimeLastScored = F, IsMissingHasChangedWithinSpecifiedAmountOfTime = F, GradeMarkCodeToUse = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAssignment
	#'
	#' This function returns a dataframe or json object of a StudentAssignment
	#' @param StudentAssignmentID The ID of the StudentAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAssignment <- function(StudentAssignmentID, StudentSectionID = F, AssignmentID = F, ScoreClarifierID = F, Score = F, Comments = F, IsMissing = F, IsGraded = F, NoCount = F, IsDropped = F, SubmissionStatus = F, SubmissionTime = F, SectionID = F, GradeMarkID = F, PointsEarned = F, PointsEarnedWithSlash = F, PointsEarnedPercent = F, ScoreDisplayValue = F, AllQuestionsScored = F, IsExpired = F, ScoreDisplayValueNoGradeMark = F, IsTransferredScore = F, IsFutureRetainedScore = F, IsScored = F, HasStudentSectionTransaction = F, AssignmentDueDateAttendance = F, StudentQuestionCount = F, HasAnswers = F, AnsweredQuestionCount = F, CurrentQuestionNumber = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, StudentOnlineAssignmentDisplayPointsEarnedWithSlash = F, GradeMarkCode = F, OnlineAssignmentNotificationSent = F, OnlineAssignmentReviewNotificationSent = F, AnswerKeyIsAvailableToView = F, PointsEarnedWithSlashCheckDisplay = F, SubmissionStatusToUse = F, CannotBeTakenByStudent = F, CalculatedPoints = F, ScoreHasChangedWithinSpecifiedAmountOfTime = F, StudentOnlineAssignmentDisplayPointsEarned = F, AttemptCount = F, TimeLastScored = F, IsMissingHasChangedWithinSpecifiedAmountOfTime = F, GradeMarkCodeToUse = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentAssignment", objectId = StudentAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAssignment
	#'
	#' This function deletes a StudentAssignment
	#' @param StudentAssignmentID The ID of the StudentAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentAssignmentID of the deleted StudentAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAssignment <- function(StudentAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentAssignment", objectId = StudentAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAssignment
	#'
	#' This function creates a StudentAssignment
	#' @param fieldNames The field values to give the created StudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAssignment <- function(StudentSectionID = NULL, AssignmentID = NULL, ScoreClarifierID = NULL, Score = NULL, Comments = NULL, IsMissing = NULL, IsGraded = NULL, NoCount = NULL, IsDropped = NULL, SubmissionStatus = NULL, SubmissionTime = NULL, SectionID = NULL, GradeMarkID = NULL, OnlineAssignmentNotificationSent = NULL, OnlineAssignmentReviewNotificationSent = NULL, CalculatedPoints = NULL, TimeLastScored = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentAssignment", body = list(DataObject = body), searchFields = append("StudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAssignment
	#'
	#' This function modifies a StudentAssignment
	#' @param fieldNames The field values to give the modified StudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAssignment <- function(StudentAssignmentID, StudentSectionID = NULL, AssignmentID = NULL, ScoreClarifierID = NULL, Score = NULL, Comments = NULL, IsMissing = NULL, IsGraded = NULL, NoCount = NULL, IsDropped = NULL, SubmissionStatus = NULL, SubmissionTime = NULL, SectionID = NULL, GradeMarkID = NULL, OnlineAssignmentNotificationSent = NULL, OnlineAssignmentReviewNotificationSent = NULL, CalculatedPoints = NULL, TimeLastScored = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentAssignment", objectId = StudentAssignmentID, body = list(DataObject = body), searchFields = append("StudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradingScaleGroups
	#'
	#' This function returns a dataframe or json object of StudentGradingScaleGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradingScaleGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradingScaleGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradingScaleGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGradingScaleGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradingScaleGroups <- function(searchConditionsList = NULL, StudentGradingScaleGroupID = F, EntityID = F, SchoolYearID = F, EntityGroupKey = F, Description = F, StudentGradingScaleGroupIDClonedFrom = F, StudentGradingScaleGroupStudentSectionCount = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingScaleGroupID = F, HasAttachedStudentSections = F, AllowTeachersToModify = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGradingScaleGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradingScaleGroup
	#'
	#' This function returns a dataframe or json object of a StudentGradingScaleGroup
	#' @param StudentGradingScaleGroupID The ID of the StudentGradingScaleGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradingScaleGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradingScaleGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradingScaleGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradingScaleGroup <- function(StudentGradingScaleGroupID, EntityID = F, SchoolYearID = F, EntityGroupKey = F, Description = F, StudentGradingScaleGroupIDClonedFrom = F, StudentGradingScaleGroupStudentSectionCount = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradingScaleGroupID = F, HasAttachedStudentSections = F, AllowTeachersToModify = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradingScaleGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroup", objectId = StudentGradingScaleGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradingScaleGroup
	#'
	#' This function deletes a StudentGradingScaleGroup
	#' @param StudentGradingScaleGroupID The ID of the StudentGradingScaleGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGradingScaleGroupID of the deleted StudentGradingScaleGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradingScaleGroup <- function(StudentGradingScaleGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroup", objectId = StudentGradingScaleGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradingScaleGroup
	#'
	#' This function creates a StudentGradingScaleGroup
	#' @param fieldNames The field values to give the created StudentGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradingScaleGroup <- function(EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, Description = NULL, StudentGradingScaleGroupIDClonedFrom = NULL, GradingScaleGroupID = NULL, AllowTeachersToModify = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroup", body = list(DataObject = body), searchFields = append("StudentGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradingScaleGroup
	#'
	#' This function modifies a StudentGradingScaleGroup
	#' @param fieldNames The field values to give the modified StudentGradingScaleGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGradingScaleGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradingScaleGroup <- function(StudentGradingScaleGroupID, EntityID = NULL, SchoolYearID = NULL, EntityGroupKey = NULL, Description = NULL, StudentGradingScaleGroupIDClonedFrom = NULL, GradingScaleGroupID = NULL, AllowTeachersToModify = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroup", objectId = StudentGradingScaleGroupID, body = list(DataObject = body), searchFields = append("StudentGradingScaleGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradingScaleGroupStudentSections
	#'
	#' This function returns a dataframe or json object of StudentGradingScaleGroupStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradingScaleGroupStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradingScaleGroupStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradingScaleGroupStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGradingScaleGroupStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradingScaleGroupStudentSections <- function(searchConditionsList = NULL, StudentGradingScaleGroupStudentSectionID = F, StudentGradingScaleGroupID = F, StudentSectionID = F, GradeBuckets = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGradingScaleGroupStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradingScaleGroupStudentSection
	#'
	#' This function returns a dataframe or json object of a StudentGradingScaleGroupStudentSection
	#' @param StudentGradingScaleGroupStudentSectionID The ID of the StudentGradingScaleGroupStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradingScaleGroupStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradingScaleGroupStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradingScaleGroupStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGradingScaleGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradingScaleGroupStudentSection <- function(StudentGradingScaleGroupStudentSectionID, StudentGradingScaleGroupID = F, StudentSectionID = F, GradeBuckets = F, IsAHistoricRecord = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradingScaleGroupStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroupStudentSection", objectId = StudentGradingScaleGroupStudentSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradingScaleGroupStudentSection
	#'
	#' This function deletes a StudentGradingScaleGroupStudentSection
	#' @param StudentGradingScaleGroupStudentSectionID The ID of the StudentGradingScaleGroupStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGradingScaleGroupStudentSectionID of the deleted StudentGradingScaleGroupStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradingScaleGroupStudentSection <- function(StudentGradingScaleGroupStudentSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroupStudentSection", objectId = StudentGradingScaleGroupStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradingScaleGroupStudentSection
	#'
	#' This function creates a StudentGradingScaleGroupStudentSection
	#' @param fieldNames The field values to give the created StudentGradingScaleGroupStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGradingScaleGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradingScaleGroupStudentSection <- function(StudentGradingScaleGroupID = NULL, StudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroupStudentSection", body = list(DataObject = body), searchFields = append("StudentGradingScaleGroupStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradingScaleGroupStudentSection
	#'
	#' This function modifies a StudentGradingScaleGroupStudentSection
	#' @param fieldNames The field values to give the modified StudentGradingScaleGroupStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGradingScaleGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradingScaleGroupStudentSection <- function(StudentGradingScaleGroupStudentSectionID, StudentGradingScaleGroupID = NULL, StudentSectionID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGradingScaleGroupStudentSection", objectId = StudentGradingScaleGroupStudentSectionID, body = list(DataObject = body), searchFields = append("StudentGradingScaleGroupStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCalcGroupBucketRemoveStandardOrSubjectResults
	#'
	#' This function returns a dataframe or json object of TempCalcGroupBucketRemoveStandardOrSubjectResults
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalcGroupBucketRemoveStandardOrSubjectResults. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalcGroupBucketRemoveStandardOrSubjectResults.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalcGroupBucketRemoveStandardOrSubjectResult') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempCalcGroupBucketRemoveStandardOrSubjectResults
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCalcGroupBucketRemoveStandardOrSubjectResults <- function(searchConditionsList = NULL, TempCalcGroupBucketRemoveStandardOrSubjectResultID = F, IsError = F, StandardOrSubjectDescription = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempCalcGroupBucketRemoveStandardOrSubjectResult", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#'
	#' This function returns a dataframe or json object of a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' @param TempCalcGroupBucketRemoveStandardOrSubjectResultID The ID of the TempCalcGroupBucketRemoveStandardOrSubjectResult to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCalcGroupBucketRemoveStandardOrSubjectResult. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCalcGroupBucketRemoveStandardOrSubjectResult.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCalcGroupBucketRemoveStandardOrSubjectResult') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCalcGroupBucketRemoveStandardOrSubjectResult <- function(TempCalcGroupBucketRemoveStandardOrSubjectResultID, IsError = F, StandardOrSubjectDescription = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCalcGroupBucketRemoveStandardOrSubjectResultID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempCalcGroupBucketRemoveStandardOrSubjectResult", objectId = TempCalcGroupBucketRemoveStandardOrSubjectResultID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#'
	#' This function deletes a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' @param TempCalcGroupBucketRemoveStandardOrSubjectResultID The ID of the TempCalcGroupBucketRemoveStandardOrSubjectResult to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempCalcGroupBucketRemoveStandardOrSubjectResultID of the deleted TempCalcGroupBucketRemoveStandardOrSubjectResult.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCalcGroupBucketRemoveStandardOrSubjectResult <- function(TempCalcGroupBucketRemoveStandardOrSubjectResultID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempCalcGroupBucketRemoveStandardOrSubjectResult", objectId = TempCalcGroupBucketRemoveStandardOrSubjectResultID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#'
	#' This function creates a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' @param fieldNames The field values to give the created TempCalcGroupBucketRemoveStandardOrSubjectResult. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCalcGroupBucketRemoveStandardOrSubjectResult <- function(IsError = NULL, StandardOrSubjectDescription = NULL, ErrorText = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempCalcGroupBucketRemoveStandardOrSubjectResult", body = list(DataObject = body), searchFields = append("TempCalcGroupBucketRemoveStandardOrSubjectResultID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#'
	#' This function modifies a TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' @param fieldNames The field values to give the modified TempCalcGroupBucketRemoveStandardOrSubjectResult. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempCalcGroupBucketRemoveStandardOrSubjectResult
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCalcGroupBucketRemoveStandardOrSubjectResult <- function(TempCalcGroupBucketRemoveStandardOrSubjectResultID, IsError = NULL, StandardOrSubjectDescription = NULL, ErrorText = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempCalcGroupBucketRemoveStandardOrSubjectResult", objectId = TempCalcGroupBucketRemoveStandardOrSubjectResultID, body = list(DataObject = body), searchFields = append("TempCalcGroupBucketRemoveStandardOrSubjectResultID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionAcademicStandardGradeBucketSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionAcademicStandardGradeBucketSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionAcademicStandardGradeBucketSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionAcademicStandardGradeBucketSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionAcademicStandardGradeBucketSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionAcademicStandardGradeBucketSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionAcademicStandardGradeBucketSettings <- function(searchConditionsList = NULL, TeacherSectionAcademicStandardGradeBucketSettingID = F, TeacherSectionSettingID = F, GradingPeriodGradeBucketID = F, AcademicStandardID = F, IsExpanded = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Display = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionAcademicStandardGradeBucketSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionAcademicStandardGradeBucketSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionAcademicStandardGradeBucketSetting
	#' @param TeacherSectionAcademicStandardGradeBucketSettingID The ID of the TeacherSectionAcademicStandardGradeBucketSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionAcademicStandardGradeBucketSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionAcademicStandardGradeBucketSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionAcademicStandardGradeBucketSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionAcademicStandardGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionAcademicStandardGradeBucketSetting <- function(TeacherSectionAcademicStandardGradeBucketSettingID, TeacherSectionSettingID = F, GradingPeriodGradeBucketID = F, AcademicStandardID = F, IsExpanded = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Display = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionAcademicStandardGradeBucketSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionAcademicStandardGradeBucketSetting", objectId = TeacherSectionAcademicStandardGradeBucketSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionAcademicStandardGradeBucketSetting
	#'
	#' This function deletes a TeacherSectionAcademicStandardGradeBucketSetting
	#' @param TeacherSectionAcademicStandardGradeBucketSettingID The ID of the TeacherSectionAcademicStandardGradeBucketSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionAcademicStandardGradeBucketSettingID of the deleted TeacherSectionAcademicStandardGradeBucketSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionAcademicStandardGradeBucketSetting <- function(TeacherSectionAcademicStandardGradeBucketSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionAcademicStandardGradeBucketSetting", objectId = TeacherSectionAcademicStandardGradeBucketSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionAcademicStandardGradeBucketSetting
	#'
	#' This function creates a TeacherSectionAcademicStandardGradeBucketSetting
	#' @param fieldNames The field values to give the created TeacherSectionAcademicStandardGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionAcademicStandardGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionAcademicStandardGradeBucketSetting <- function(TeacherSectionSettingID = NULL, GradingPeriodGradeBucketID = NULL, AcademicStandardID = NULL, IsExpanded = NULL, Display = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionAcademicStandardGradeBucketSetting", body = list(DataObject = body), searchFields = append("TeacherSectionAcademicStandardGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionAcademicStandardGradeBucketSetting
	#'
	#' This function modifies a TeacherSectionAcademicStandardGradeBucketSetting
	#' @param fieldNames The field values to give the modified TeacherSectionAcademicStandardGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionAcademicStandardGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionAcademicStandardGradeBucketSetting <- function(TeacherSectionAcademicStandardGradeBucketSettingID, TeacherSectionSettingID = NULL, GradingPeriodGradeBucketID = NULL, AcademicStandardID = NULL, IsExpanded = NULL, Display = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionAcademicStandardGradeBucketSetting", objectId = TeacherSectionAcademicStandardGradeBucketSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionAcademicStandardGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TeacherSectionSubjectGradeBucketSettings
	#'
	#' This function returns a dataframe or json object of TeacherSectionSubjectGradeBucketSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionSubjectGradeBucketSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionSubjectGradeBucketSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionSubjectGradeBucketSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TeacherSectionSubjectGradeBucketSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTeacherSectionSubjectGradeBucketSettings <- function(searchConditionsList = NULL, TeacherSectionSubjectGradeBucketSettingID = F, TeacherSectionSettingID = F, GradingPeriodGradeBucketID = F, SubjectID = F, IsExpanded = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Display = F, AllLinkedAcademicStandardsAreDisplayed = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TeacherSectionSubjectGradeBucketSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TeacherSectionSubjectGradeBucketSetting
	#'
	#' This function returns a dataframe or json object of a TeacherSectionSubjectGradeBucketSetting
	#' @param TeacherSectionSubjectGradeBucketSettingID The ID of the TeacherSectionSubjectGradeBucketSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TeacherSectionSubjectGradeBucketSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TeacherSectionSubjectGradeBucketSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TeacherSectionSubjectGradeBucketSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TeacherSectionSubjectGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTeacherSectionSubjectGradeBucketSetting <- function(TeacherSectionSubjectGradeBucketSettingID, TeacherSectionSettingID = F, GradingPeriodGradeBucketID = F, SubjectID = F, IsExpanded = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, Display = F, AllLinkedAcademicStandardsAreDisplayed = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TeacherSectionSubjectGradeBucketSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TeacherSectionSubjectGradeBucketSetting", objectId = TeacherSectionSubjectGradeBucketSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TeacherSectionSubjectGradeBucketSetting
	#'
	#' This function deletes a TeacherSectionSubjectGradeBucketSetting
	#' @param TeacherSectionSubjectGradeBucketSettingID The ID of the TeacherSectionSubjectGradeBucketSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TeacherSectionSubjectGradeBucketSettingID of the deleted TeacherSectionSubjectGradeBucketSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTeacherSectionSubjectGradeBucketSetting <- function(TeacherSectionSubjectGradeBucketSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TeacherSectionSubjectGradeBucketSetting", objectId = TeacherSectionSubjectGradeBucketSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TeacherSectionSubjectGradeBucketSetting
	#'
	#' This function creates a TeacherSectionSubjectGradeBucketSetting
	#' @param fieldNames The field values to give the created TeacherSectionSubjectGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TeacherSectionSubjectGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTeacherSectionSubjectGradeBucketSetting <- function(TeacherSectionSettingID = NULL, GradingPeriodGradeBucketID = NULL, SubjectID = NULL, IsExpanded = NULL, Display = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TeacherSectionSubjectGradeBucketSetting", body = list(DataObject = body), searchFields = append("TeacherSectionSubjectGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TeacherSectionSubjectGradeBucketSetting
	#'
	#' This function modifies a TeacherSectionSubjectGradeBucketSetting
	#' @param fieldNames The field values to give the modified TeacherSectionSubjectGradeBucketSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TeacherSectionSubjectGradeBucketSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTeacherSectionSubjectGradeBucketSetting <- function(TeacherSectionSubjectGradeBucketSettingID, TeacherSectionSettingID = NULL, GradingPeriodGradeBucketID = NULL, SubjectID = NULL, IsExpanded = NULL, Display = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TeacherSectionSubjectGradeBucketSetting", objectId = TeacherSectionSubjectGradeBucketSettingID, body = list(DataObject = body), searchFields = append("TeacherSectionSubjectGradeBucketSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MonitorSummaryByClasses
	#'
	#' This function returns a dataframe or json object of MonitorSummaryByClasses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MonitorSummaryByClasses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MonitorSummaryByClasses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MonitorSummaryByClass') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of MonitorSummaryByClasses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMonitorSummaryByClasses <- function(searchConditionsList = NULL, MonitorSummaryByClassID = F, LastUpdatedTime = F, StaffMeetID = F, ClosedGradingPeriodGradeChangeCount = F, AssignmentCountYTD = F, StudentCountForTerm = F, AssignmentCountForTerm = F, FutureAssignmentCountForTerm = F, GradedAssignmentCountForTerm = F, NonGradedAssignmentCountForTerm = F, MissingAssignmentCountForTerm = F, NoCountAssignmentCountForTerm = F, ScoreClarifierAssignmentCountForTerm = F, ScoredAssignmentRange100to90CurrentTerm = F, ScoredAssignmentRange89to80CurrentTerm = F, ScoredAssignmentRange79to70CurrentTerm = F, ScoredAssignmentRange69to60CurrentTerm = F, ScoredAssignmentRange59to50CurrentTerm = F, ScoredAssignmentRange49to1CurrentTerm = F, ScoredAssignmentRange0CurrentTerm = F, ExcusedAbsencesYTD = F, UnexcusedAbsencesYTD = F, TardiesYTD = F, OtherAbsencesYTD = F, OffenseCountYTD = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CurrentGradingPeriod = F, LastAccessedGradebookTime = F, LastScoredGradebookTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "MonitorSummaryByClass", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MonitorSummaryByClass
	#'
	#' This function returns a dataframe or json object of a MonitorSummaryByClass
	#' @param MonitorSummaryByClassID The ID of the MonitorSummaryByClass to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MonitorSummaryByClass. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MonitorSummaryByClass.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MonitorSummaryByClass') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of MonitorSummaryByClass
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMonitorSummaryByClass <- function(MonitorSummaryByClassID, LastUpdatedTime = F, StaffMeetID = F, ClosedGradingPeriodGradeChangeCount = F, AssignmentCountYTD = F, StudentCountForTerm = F, AssignmentCountForTerm = F, FutureAssignmentCountForTerm = F, GradedAssignmentCountForTerm = F, NonGradedAssignmentCountForTerm = F, MissingAssignmentCountForTerm = F, NoCountAssignmentCountForTerm = F, ScoreClarifierAssignmentCountForTerm = F, ScoredAssignmentRange100to90CurrentTerm = F, ScoredAssignmentRange89to80CurrentTerm = F, ScoredAssignmentRange79to70CurrentTerm = F, ScoredAssignmentRange69to60CurrentTerm = F, ScoredAssignmentRange59to50CurrentTerm = F, ScoredAssignmentRange49to1CurrentTerm = F, ScoredAssignmentRange0CurrentTerm = F, ExcusedAbsencesYTD = F, UnexcusedAbsencesYTD = F, TardiesYTD = F, OtherAbsencesYTD = F, OffenseCountYTD = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CurrentGradingPeriod = F, LastAccessedGradebookTime = F, LastScoredGradebookTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MonitorSummaryByClassID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "MonitorSummaryByClass", objectId = MonitorSummaryByClassID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MonitorSummaryByClass
	#'
	#' This function deletes a MonitorSummaryByClass
	#' @param MonitorSummaryByClassID The ID of the MonitorSummaryByClass to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The MonitorSummaryByClassID of the deleted MonitorSummaryByClass.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMonitorSummaryByClass <- function(MonitorSummaryByClassID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "MonitorSummaryByClass", objectId = MonitorSummaryByClassID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MonitorSummaryByClass
	#'
	#' This function creates a MonitorSummaryByClass
	#' @param fieldNames The field values to give the created MonitorSummaryByClass. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created MonitorSummaryByClass
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMonitorSummaryByClass <- function(ClosedGradingPeriodGradeChangeCount = NULL, AssignmentCountYTD = NULL, StudentCountForTerm = NULL, AssignmentCountForTerm = NULL, FutureAssignmentCountForTerm = NULL, GradedAssignmentCountForTerm = NULL, NonGradedAssignmentCountForTerm = NULL, MissingAssignmentCountForTerm = NULL, NoCountAssignmentCountForTerm = NULL, ScoreClarifierAssignmentCountForTerm = NULL, ScoredAssignmentRange100to90CurrentTerm = NULL, ScoredAssignmentRange89to80CurrentTerm = NULL, ScoredAssignmentRange79to70CurrentTerm = NULL, ScoredAssignmentRange69to60CurrentTerm = NULL, ScoredAssignmentRange59to50CurrentTerm = NULL, ScoredAssignmentRange49to1CurrentTerm = NULL, ScoredAssignmentRange0CurrentTerm = NULL, ExcusedAbsencesYTD = NULL, UnexcusedAbsencesYTD = NULL, TardiesYTD = NULL, OtherAbsencesYTD = NULL, OffenseCountYTD = NULL, CurrentGradingPeriod = NULL, LastAccessedGradebookTime = NULL, LastScoredGradebookTime = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "MonitorSummaryByClass", body = list(DataObject = body), searchFields = append("MonitorSummaryByClassID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MonitorSummaryByClass
	#'
	#' This function modifies a MonitorSummaryByClass
	#' @param fieldNames The field values to give the modified MonitorSummaryByClass. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified MonitorSummaryByClass
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMonitorSummaryByClass <- function(MonitorSummaryByClassID, ClosedGradingPeriodGradeChangeCount = NULL, AssignmentCountYTD = NULL, StudentCountForTerm = NULL, AssignmentCountForTerm = NULL, FutureAssignmentCountForTerm = NULL, GradedAssignmentCountForTerm = NULL, NonGradedAssignmentCountForTerm = NULL, MissingAssignmentCountForTerm = NULL, NoCountAssignmentCountForTerm = NULL, ScoreClarifierAssignmentCountForTerm = NULL, ScoredAssignmentRange100to90CurrentTerm = NULL, ScoredAssignmentRange89to80CurrentTerm = NULL, ScoredAssignmentRange79to70CurrentTerm = NULL, ScoredAssignmentRange69to60CurrentTerm = NULL, ScoredAssignmentRange59to50CurrentTerm = NULL, ScoredAssignmentRange49to1CurrentTerm = NULL, ScoredAssignmentRange0CurrentTerm = NULL, ExcusedAbsencesYTD = NULL, UnexcusedAbsencesYTD = NULL, TardiesYTD = NULL, OtherAbsencesYTD = NULL, OffenseCountYTD = NULL, CurrentGradingPeriod = NULL, LastAccessedGradebookTime = NULL, LastScoredGradebookTime = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "MonitorSummaryByClass", objectId = MonitorSummaryByClassID, body = list(DataObject = body), searchFields = append("MonitorSummaryByClassID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MonitorSummaryByTeachers
	#'
	#' This function returns a dataframe or json object of MonitorSummaryByTeachers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MonitorSummaryByTeachers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MonitorSummaryByTeachers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MonitorSummaryByTeacher') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of MonitorSummaryByTeachers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMonitorSummaryByTeachers <- function(searchConditionsList = NULL, MonitorSummaryByTeacherID = F, LastUpdatedTime = F, StaffID = F, StaffMeetCount = F, ActiveStudentCount = F, AssignmentCountForTerm = F, NonGradedAssignmentCountNoStudentAssignmentsForTerm = F, NonGradedAssignmentCountForTerm = F, GradedAssignmentCountForTerm = F, NoCountAssignmentCountForTerm = F, MissingAssignmentCountForTerm = F, ScoreClarifierAssignmentCountForTerm = F, FutureAssignmentCountForTerm = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SchoolYearID = F, LastAccessedGradebookTime = F, LastAssignmentScoredDueDate = F, LastScoredAssignmentTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "MonitorSummaryByTeacher", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MonitorSummaryByTeacher
	#'
	#' This function returns a dataframe or json object of a MonitorSummaryByTeacher
	#' @param MonitorSummaryByTeacherID The ID of the MonitorSummaryByTeacher to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MonitorSummaryByTeacher. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MonitorSummaryByTeacher.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MonitorSummaryByTeacher') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of MonitorSummaryByTeacher
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMonitorSummaryByTeacher <- function(MonitorSummaryByTeacherID, LastUpdatedTime = F, StaffID = F, StaffMeetCount = F, ActiveStudentCount = F, AssignmentCountForTerm = F, NonGradedAssignmentCountNoStudentAssignmentsForTerm = F, NonGradedAssignmentCountForTerm = F, GradedAssignmentCountForTerm = F, NoCountAssignmentCountForTerm = F, MissingAssignmentCountForTerm = F, ScoreClarifierAssignmentCountForTerm = F, FutureAssignmentCountForTerm = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, SchoolYearID = F, LastAccessedGradebookTime = F, LastAssignmentScoredDueDate = F, LastScoredAssignmentTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MonitorSummaryByTeacherID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "MonitorSummaryByTeacher", objectId = MonitorSummaryByTeacherID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MonitorSummaryByTeacher
	#'
	#' This function deletes a MonitorSummaryByTeacher
	#' @param MonitorSummaryByTeacherID The ID of the MonitorSummaryByTeacher to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The MonitorSummaryByTeacherID of the deleted MonitorSummaryByTeacher.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMonitorSummaryByTeacher <- function(MonitorSummaryByTeacherID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "MonitorSummaryByTeacher", objectId = MonitorSummaryByTeacherID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MonitorSummaryByTeacher
	#'
	#' This function creates a MonitorSummaryByTeacher
	#' @param fieldNames The field values to give the created MonitorSummaryByTeacher. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created MonitorSummaryByTeacher
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMonitorSummaryByTeacher <- function(StaffID = NULL, StaffMeetCount = NULL, ActiveStudentCount = NULL, AssignmentCountForTerm = NULL, NonGradedAssignmentCountNoStudentAssignmentsForTerm = NULL, NonGradedAssignmentCountForTerm = NULL, GradedAssignmentCountForTerm = NULL, NoCountAssignmentCountForTerm = NULL, MissingAssignmentCountForTerm = NULL, ScoreClarifierAssignmentCountForTerm = NULL, FutureAssignmentCountForTerm = NULL, LastAccessedGradebookTime = NULL, LastAssignmentScoredDueDate = NULL, LastScoredAssignmentTime = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "MonitorSummaryByTeacher", body = list(DataObject = body), searchFields = append("MonitorSummaryByTeacherID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MonitorSummaryByTeacher
	#'
	#' This function modifies a MonitorSummaryByTeacher
	#' @param fieldNames The field values to give the modified MonitorSummaryByTeacher. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified MonitorSummaryByTeacher
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMonitorSummaryByTeacher <- function(MonitorSummaryByTeacherID, StaffID = NULL, StaffMeetCount = NULL, ActiveStudentCount = NULL, AssignmentCountForTerm = NULL, NonGradedAssignmentCountNoStudentAssignmentsForTerm = NULL, NonGradedAssignmentCountForTerm = NULL, GradedAssignmentCountForTerm = NULL, NoCountAssignmentCountForTerm = NULL, MissingAssignmentCountForTerm = NULL, ScoreClarifierAssignmentCountForTerm = NULL, FutureAssignmentCountForTerm = NULL, LastAccessedGradebookTime = NULL, LastAssignmentScoredDueDate = NULL, LastScoredAssignmentTime = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "MonitorSummaryByTeacher", objectId = MonitorSummaryByTeacherID, body = list(DataObject = body), searchFields = append("MonitorSummaryByTeacherID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AssignmentAttachments
	#'
	#' This function returns a dataframe or json object of AssignmentAttachments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AssignmentAttachments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AssignmentAttachments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AssignmentAttachment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of AssignmentAttachments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAssignmentAttachments <- function(searchConditionsList = NULL, AssignmentAttachmentID = F, AssignmentID = F, AttachmentID = F, DisplayInStudentFamilyAccess = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "AssignmentAttachment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AssignmentAttachment
	#'
	#' This function returns a dataframe or json object of an AssignmentAttachment
	#' @param AssignmentAttachmentID The ID of the AssignmentAttachment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AssignmentAttachment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AssignmentAttachment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AssignmentAttachment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of AssignmentAttachment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAssignmentAttachment <- function(AssignmentAttachmentID, AssignmentID = F, AttachmentID = F, DisplayInStudentFamilyAccess = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AssignmentAttachmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "AssignmentAttachment", objectId = AssignmentAttachmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AssignmentAttachment
	#'
	#' This function deletes an AssignmentAttachment
	#' @param AssignmentAttachmentID The ID of the AssignmentAttachment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The AssignmentAttachmentID of the deleted AssignmentAttachment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAssignmentAttachment <- function(AssignmentAttachmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "AssignmentAttachment", objectId = AssignmentAttachmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AssignmentAttachment
	#'
	#' This function creates an AssignmentAttachment
	#' @param fieldNames The field values to give the created AssignmentAttachment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created AssignmentAttachment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAssignmentAttachment <- function(AssignmentID = NULL, AttachmentID = NULL, DisplayInStudentFamilyAccess = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "AssignmentAttachment", body = list(DataObject = body), searchFields = append("AssignmentAttachmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AssignmentAttachment
	#'
	#' This function modifies an AssignmentAttachment
	#' @param fieldNames The field values to give the modified AssignmentAttachment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified AssignmentAttachment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAssignmentAttachment <- function(AssignmentAttachmentID, AssignmentID = NULL, AttachmentID = NULL, DisplayInStudentFamilyAccess = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "AssignmentAttachment", objectId = AssignmentAttachmentID, body = list(DataObject = body), searchFields = append("AssignmentAttachmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookConfigEntities
	#'
	#' This function returns a dataframe or json object of GradebookConfigEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookConfigEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookConfigEntities <- function(searchConditionsList = NULL, ConfigEntityID = F, EntityID = F, RunInMonitorByScheduledTask = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ConfigEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookConfigEntity
	#'
	#' This function returns a dataframe or json object of a GradebookConfigEntity
	#' @param GradebookConfigEntityID The ID of the GradebookConfigEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookConfigEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookConfigEntity <- function(GradebookConfigEntityID, ConfigEntityID = F, EntityID = F, RunInMonitorByScheduledTask = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookConfigEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ConfigEntity", objectId = GradebookConfigEntityID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookConfigEntity
	#'
	#' This function deletes a GradebookConfigEntity
	#' @param GradebookConfigEntityID The ID of the GradebookConfigEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookConfigEntityID of the deleted GradebookConfigEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookConfigEntity <- function(GradebookConfigEntityID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ConfigEntity", objectId = GradebookConfigEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookConfigEntity
	#'
	#' This function creates a GradebookConfigEntity
	#' @param fieldNames The field values to give the created GradebookConfigEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookConfigEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookConfigEntity <- function(EntityID = NULL, RunInMonitorByScheduledTask = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ConfigEntity", body = list(DataObject = body), searchFields = append("ConfigEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookConfigEntity
	#'
	#' This function modifies a GradebookConfigEntity
	#' @param fieldNames The field values to give the modified GradebookConfigEntity. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookConfigEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookConfigEntity <- function(ConfigEntityID, EntityID = NULL, RunInMonitorByScheduledTask = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ConfigEntity", objectId = ConfigEntityID, body = list(DataObject = body), searchFields = append("ConfigEntityID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookConfigDistricts
	#'
	#' This function returns a dataframe or json object of GradebookConfigDistricts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigDistricts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigDistricts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigDistrict') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookConfigDistricts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookConfigDistricts <- function(searchConditionsList = NULL, ConfigDistrictID = F, DistrictID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ConfigDistrict", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookConfigDistrict
	#'
	#' This function returns a dataframe or json object of a GradebookConfigDistrict
	#' @param GradebookConfigDistrictID The ID of the GradebookConfigDistrict to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigDistrict. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigDistrict.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigDistrict') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookConfigDistrict <- function(GradebookConfigDistrictID, ConfigDistrictID = F, DistrictID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookConfigDistrictID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ConfigDistrict", objectId = GradebookConfigDistrictID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookConfigDistrict
	#'
	#' This function deletes a GradebookConfigDistrict
	#' @param GradebookConfigDistrictID The ID of the GradebookConfigDistrict to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookConfigDistrictID of the deleted GradebookConfigDistrict.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookConfigDistrict <- function(GradebookConfigDistrictID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ConfigDistrict", objectId = GradebookConfigDistrictID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookConfigDistrict
	#'
	#' This function creates a GradebookConfigDistrict
	#' @param fieldNames The field values to give the created GradebookConfigDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookConfigDistrict <- function(DistrictID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ConfigDistrict", body = list(DataObject = body), searchFields = append("ConfigDistrictID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookConfigDistrict
	#'
	#' This function modifies a GradebookConfigDistrict
	#' @param fieldNames The field values to give the modified GradebookConfigDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookConfigDistrict <- function(ConfigDistrictID, DistrictID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ConfigDistrict", objectId = ConfigDistrictID, body = list(DataObject = body), searchFields = append("ConfigDistrictID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionAssignments
	#'
	#' This function returns a dataframe or json object of TempSectionAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempSectionAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionAssignments <- function(searchConditionsList = NULL, TempSectionAssignmentID = F, AssignmentID = F, AssignmentName = F, SectionID = F, SectionDetails = F, CurrentPeriod = F, Sync = F, ErrorCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CategoryDescription = F, EntityID = F, SchoolYearID = F, AssignedDate = F, DueDate = F, SectionLengthStartDate = F, SectionLengthEndDate = F, MaxScore = F, Weight = F, IsOnlineAssignment = F, StartDate = F, StartTime = F, EndDate = F, EndTime = F, HideScoreUntilDate = F, HideScoreUntilTime = F, ShowCorrectAnswers = F, AnswerRevealDate = F, AnswerRevealTime = F, AttachmentCount = F, CategoryID = F, UseForSpecificGradeBucketType = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempSectionAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionAssignment
	#'
	#' This function returns a dataframe or json object of a TempSectionAssignment
	#' @param TempSectionAssignmentID The ID of the TempSectionAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempSectionAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionAssignment <- function(TempSectionAssignmentID, AssignmentID = F, AssignmentName = F, SectionID = F, SectionDetails = F, CurrentPeriod = F, Sync = F, ErrorCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CategoryDescription = F, EntityID = F, SchoolYearID = F, AssignedDate = F, DueDate = F, SectionLengthStartDate = F, SectionLengthEndDate = F, MaxScore = F, Weight = F, IsOnlineAssignment = F, StartDate = F, StartTime = F, EndDate = F, EndTime = F, HideScoreUntilDate = F, HideScoreUntilTime = F, ShowCorrectAnswers = F, AnswerRevealDate = F, AnswerRevealTime = F, AttachmentCount = F, CategoryID = F, UseForSpecificGradeBucketType = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempSectionAssignment", objectId = TempSectionAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionAssignment
	#'
	#' This function deletes a TempSectionAssignment
	#' @param TempSectionAssignmentID The ID of the TempSectionAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempSectionAssignmentID of the deleted TempSectionAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionAssignment <- function(TempSectionAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempSectionAssignment", objectId = TempSectionAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionAssignment
	#'
	#' This function creates a TempSectionAssignment
	#' @param fieldNames The field values to give the created TempSectionAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempSectionAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionAssignment <- function(AssignmentID = NULL, AssignmentName = NULL, SectionID = NULL, SectionDetails = NULL, CurrentPeriod = NULL, Sync = NULL, ErrorCount = NULL, CategoryDescription = NULL, EntityID = NULL, SchoolYearID = NULL, AssignedDate = NULL, DueDate = NULL, SectionLengthStartDate = NULL, SectionLengthEndDate = NULL, MaxScore = NULL, Weight = NULL, IsOnlineAssignment = NULL, StartDate = NULL, StartTime = NULL, EndDate = NULL, EndTime = NULL, HideScoreUntilDate = NULL, HideScoreUntilTime = NULL, ShowCorrectAnswers = NULL, AnswerRevealDate = NULL, AnswerRevealTime = NULL, AttachmentCount = NULL, CategoryID = NULL, UseForSpecificGradeBucketType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempSectionAssignment", body = list(DataObject = body), searchFields = append("TempSectionAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionAssignment
	#'
	#' This function modifies a TempSectionAssignment
	#' @param fieldNames The field values to give the modified TempSectionAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempSectionAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionAssignment <- function(TempSectionAssignmentID, AssignmentID = NULL, AssignmentName = NULL, SectionID = NULL, SectionDetails = NULL, CurrentPeriod = NULL, Sync = NULL, ErrorCount = NULL, CategoryDescription = NULL, EntityID = NULL, SchoolYearID = NULL, AssignedDate = NULL, DueDate = NULL, SectionLengthStartDate = NULL, SectionLengthEndDate = NULL, MaxScore = NULL, Weight = NULL, IsOnlineAssignment = NULL, StartDate = NULL, StartTime = NULL, EndDate = NULL, EndTime = NULL, HideScoreUntilDate = NULL, HideScoreUntilTime = NULL, ShowCorrectAnswers = NULL, AnswerRevealDate = NULL, AnswerRevealTime = NULL, AttachmentCount = NULL, CategoryID = NULL, UseForSpecificGradeBucketType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempSectionAssignment", objectId = TempSectionAssignmentID, body = list(DataObject = body), searchFields = append("TempSectionAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DropLowScoreRunStudentAssignments
	#'
	#' This function returns a dataframe or json object of DropLowScoreRunStudentAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DropLowScoreRunStudentAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DropLowScoreRunStudentAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DropLowScoreRunStudentAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of DropLowScoreRunStudentAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDropLowScoreRunStudentAssignments <- function(searchConditionsList = NULL, DropLowScoreRunStudentAssignmentID = F, DropLowScoreRunID = F, StudentAssignmentID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DropActionType = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "DropLowScoreRunStudentAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DropLowScoreRunStudentAssignment
	#'
	#' This function returns a dataframe or json object of a DropLowScoreRunStudentAssignment
	#' @param DropLowScoreRunStudentAssignmentID The ID of the DropLowScoreRunStudentAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DropLowScoreRunStudentAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DropLowScoreRunStudentAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DropLowScoreRunStudentAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of DropLowScoreRunStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDropLowScoreRunStudentAssignment <- function(DropLowScoreRunStudentAssignmentID, DropLowScoreRunID = F, StudentAssignmentID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DropActionType = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DropLowScoreRunStudentAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "DropLowScoreRunStudentAssignment", objectId = DropLowScoreRunStudentAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DropLowScoreRunStudentAssignment
	#'
	#' This function deletes a DropLowScoreRunStudentAssignment
	#' @param DropLowScoreRunStudentAssignmentID The ID of the DropLowScoreRunStudentAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The DropLowScoreRunStudentAssignmentID of the deleted DropLowScoreRunStudentAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDropLowScoreRunStudentAssignment <- function(DropLowScoreRunStudentAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "DropLowScoreRunStudentAssignment", objectId = DropLowScoreRunStudentAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DropLowScoreRunStudentAssignment
	#'
	#' This function creates a DropLowScoreRunStudentAssignment
	#' @param fieldNames The field values to give the created DropLowScoreRunStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created DropLowScoreRunStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDropLowScoreRunStudentAssignment <- function(DropLowScoreRunID = NULL, StudentAssignmentID = NULL, DropActionType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "DropLowScoreRunStudentAssignment", body = list(DataObject = body), searchFields = append("DropLowScoreRunStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DropLowScoreRunStudentAssignment
	#'
	#' This function modifies a DropLowScoreRunStudentAssignment
	#' @param fieldNames The field values to give the modified DropLowScoreRunStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified DropLowScoreRunStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDropLowScoreRunStudentAssignment <- function(DropLowScoreRunStudentAssignmentID, DropLowScoreRunID = NULL, StudentAssignmentID = NULL, DropActionType = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "DropLowScoreRunStudentAssignment", objectId = DropLowScoreRunStudentAssignmentID, body = list(DataObject = body), searchFields = append("DropLowScoreRunStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DropLowScoreRuns
	#'
	#' This function returns a dataframe or json object of DropLowScoreRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DropLowScoreRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DropLowScoreRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DropLowScoreRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of DropLowScoreRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDropLowScoreRuns <- function(searchConditionsList = NULL, DropLowScoreRunID = F, RunTime = F, IsEffective = F, SectionID = F, CalculationGroupGradeBucketID = F, AffectedStudentAssignmentCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "DropLowScoreRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DropLowScoreRun
	#'
	#' This function returns a dataframe or json object of a DropLowScoreRun
	#' @param DropLowScoreRunID The ID of the DropLowScoreRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DropLowScoreRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DropLowScoreRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DropLowScoreRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of DropLowScoreRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDropLowScoreRun <- function(DropLowScoreRunID, RunTime = F, IsEffective = F, SectionID = F, CalculationGroupGradeBucketID = F, AffectedStudentAssignmentCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DropLowScoreRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "DropLowScoreRun", objectId = DropLowScoreRunID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DropLowScoreRun
	#'
	#' This function deletes a DropLowScoreRun
	#' @param DropLowScoreRunID The ID of the DropLowScoreRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The DropLowScoreRunID of the deleted DropLowScoreRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDropLowScoreRun <- function(DropLowScoreRunID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "DropLowScoreRun", objectId = DropLowScoreRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DropLowScoreRun
	#'
	#' This function creates a DropLowScoreRun
	#' @param fieldNames The field values to give the created DropLowScoreRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created DropLowScoreRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDropLowScoreRun <- function(RunTime = NULL, IsEffective = NULL, SectionID = NULL, CalculationGroupGradeBucketID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "DropLowScoreRun", body = list(DataObject = body), searchFields = append("DropLowScoreRunID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DropLowScoreRun
	#'
	#' This function modifies a DropLowScoreRun
	#' @param fieldNames The field values to give the modified DropLowScoreRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified DropLowScoreRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDropLowScoreRun <- function(DropLowScoreRunID, RunTime = NULL, IsEffective = NULL, SectionID = NULL, CalculationGroupGradeBucketID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "DropLowScoreRun", objectId = DropLowScoreRunID, body = list(DataObject = body), searchFields = append("DropLowScoreRunID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradingPeriodGradeBuckets
	#'
	#' This function returns a dataframe or json object of TempGradingPeriodGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriodGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriodGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriodGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempGradingPeriodGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradingPeriodGradeBuckets <- function(searchConditionsList = NULL, TempGradingPeriodGradeBucketID = F, GradingPeriodGradeBucketID = F, GradeBucketLabelWithDates = F, StartDate = F, EndDate = F, StatusDisplayWithExtensionDate = F, SectionLengthCodeDescription = F, ErrorCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempGradingPeriodGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradingPeriodGradeBucket
	#'
	#' This function returns a dataframe or json object of a TempGradingPeriodGradeBucket
	#' @param TempGradingPeriodGradeBucketID The ID of the TempGradingPeriodGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradingPeriodGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradingPeriodGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradingPeriodGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempGradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradingPeriodGradeBucket <- function(TempGradingPeriodGradeBucketID, GradingPeriodGradeBucketID = F, GradeBucketLabelWithDates = F, StartDate = F, EndDate = F, StatusDisplayWithExtensionDate = F, SectionLengthCodeDescription = F, ErrorCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradingPeriodGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucket", objectId = TempGradingPeriodGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradingPeriodGradeBucket
	#'
	#' This function deletes a TempGradingPeriodGradeBucket
	#' @param TempGradingPeriodGradeBucketID The ID of the TempGradingPeriodGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempGradingPeriodGradeBucketID of the deleted TempGradingPeriodGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradingPeriodGradeBucket <- function(TempGradingPeriodGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucket", objectId = TempGradingPeriodGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradingPeriodGradeBucket
	#'
	#' This function creates a TempGradingPeriodGradeBucket
	#' @param fieldNames The field values to give the created TempGradingPeriodGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempGradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradingPeriodGradeBucket <- function(GradingPeriodGradeBucketID = NULL, GradeBucketLabelWithDates = NULL, StartDate = NULL, EndDate = NULL, StatusDisplayWithExtensionDate = NULL, SectionLengthCodeDescription = NULL, ErrorCount = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucket", body = list(DataObject = body), searchFields = append("TempGradingPeriodGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradingPeriodGradeBucket
	#'
	#' This function modifies a TempGradingPeriodGradeBucket
	#' @param fieldNames The field values to give the modified TempGradingPeriodGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempGradingPeriodGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradingPeriodGradeBucket <- function(TempGradingPeriodGradeBucketID, GradingPeriodGradeBucketID = NULL, GradeBucketLabelWithDates = NULL, StartDate = NULL, EndDate = NULL, StatusDisplayWithExtensionDate = NULL, SectionLengthCodeDescription = NULL, ErrorCount = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempGradingPeriodGradeBucket", objectId = TempGradingPeriodGradeBucketID, body = list(DataObject = body), searchFields = append("TempGradingPeriodGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempUnDropLowScoreStudentSections
	#'
	#' This function returns a dataframe or json object of TempUnDropLowScoreStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempUnDropLowScoreStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempUnDropLowScoreStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempUnDropLowScoreStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempUnDropLowScoreStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempUnDropLowScoreStudentSections <- function(searchConditionsList = NULL, TempUnDropLowScoreStudentSectionID = F, StudentName = F, StudentDisplaySortCode = F, StudentSectionID = F, OriginalGrade = F, NewGrade = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempUnDropLowScoreStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempUnDropLowScoreStudentSection
	#'
	#' This function returns a dataframe or json object of a TempUnDropLowScoreStudentSection
	#' @param TempUnDropLowScoreStudentSectionID The ID of the TempUnDropLowScoreStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempUnDropLowScoreStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempUnDropLowScoreStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempUnDropLowScoreStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempUnDropLowScoreStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempUnDropLowScoreStudentSection <- function(TempUnDropLowScoreStudentSectionID, StudentName = F, StudentDisplaySortCode = F, StudentSectionID = F, OriginalGrade = F, NewGrade = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempUnDropLowScoreStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempUnDropLowScoreStudentSection", objectId = TempUnDropLowScoreStudentSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempUnDropLowScoreStudentSection
	#'
	#' This function deletes a TempUnDropLowScoreStudentSection
	#' @param TempUnDropLowScoreStudentSectionID The ID of the TempUnDropLowScoreStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempUnDropLowScoreStudentSectionID of the deleted TempUnDropLowScoreStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempUnDropLowScoreStudentSection <- function(TempUnDropLowScoreStudentSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempUnDropLowScoreStudentSection", objectId = TempUnDropLowScoreStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempUnDropLowScoreStudentSection
	#'
	#' This function creates a TempUnDropLowScoreStudentSection
	#' @param fieldNames The field values to give the created TempUnDropLowScoreStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempUnDropLowScoreStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempUnDropLowScoreStudentSection <- function(StudentName = NULL, StudentDisplaySortCode = NULL, StudentSectionID = NULL, OriginalGrade = NULL, NewGrade = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempUnDropLowScoreStudentSection", body = list(DataObject = body), searchFields = append("TempUnDropLowScoreStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempUnDropLowScoreStudentSection
	#'
	#' This function modifies a TempUnDropLowScoreStudentSection
	#' @param fieldNames The field values to give the modified TempUnDropLowScoreStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempUnDropLowScoreStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempUnDropLowScoreStudentSection <- function(TempUnDropLowScoreStudentSectionID, StudentName = NULL, StudentDisplaySortCode = NULL, StudentSectionID = NULL, OriginalGrade = NULL, NewGrade = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempUnDropLowScoreStudentSection", objectId = TempUnDropLowScoreStudentSectionID, body = list(DataObject = body), searchFields = append("TempUnDropLowScoreStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupAcademicStandardCalculationGroupHierarchyDepths
	#'
	#' This function returns a dataframe or json object of CalculationGroupAcademicStandardCalculationGroupHierarchyDepths
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandardCalculationGroupHierarchyDepths. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandardCalculationGroupHierarchyDepths.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandardCalculationGroupHierarchyDepth') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupAcademicStandardCalculationGroupHierarchyDepths
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupAcademicStandardCalculationGroupHierarchyDepths <- function(searchConditionsList = NULL, CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID = F, EntityGroupKey = F, CalculationGroupAcademicStandardID = F, CalculationGroupHierarchyDepthID = F, CalculationGroupAcademicStandardIDAtLevel = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupAcademicStandardCalculationGroupHierarchyDepthIDClonedFrom = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupAcademicStandardCalculationGroupHierarchyDepth", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#'
	#' This function returns a dataframe or json object of a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' @param CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID The ID of the CalculationGroupAcademicStandardCalculationGroupHierarchyDepth to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupAcademicStandardCalculationGroupHierarchyDepth. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupAcademicStandardCalculationGroupHierarchyDepth.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupAcademicStandardCalculationGroupHierarchyDepth') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupAcademicStandardCalculationGroupHierarchyDepth <- function(CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID, EntityGroupKey = F, CalculationGroupAcademicStandardID = F, CalculationGroupHierarchyDepthID = F, CalculationGroupAcademicStandardIDAtLevel = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupAcademicStandardCalculationGroupHierarchyDepthIDClonedFrom = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardCalculationGroupHierarchyDepth", objectId = CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#'
	#' This function deletes a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' @param CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID The ID of the CalculationGroupAcademicStandardCalculationGroupHierarchyDepth to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID of the deleted CalculationGroupAcademicStandardCalculationGroupHierarchyDepth.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupAcademicStandardCalculationGroupHierarchyDepth <- function(CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardCalculationGroupHierarchyDepth", objectId = CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#'
	#' This function creates a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' @param fieldNames The field values to give the created CalculationGroupAcademicStandardCalculationGroupHierarchyDepth. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupAcademicStandardCalculationGroupHierarchyDepth <- function(EntityGroupKey = NULL, CalculationGroupAcademicStandardID = NULL, CalculationGroupHierarchyDepthID = NULL, CalculationGroupAcademicStandardIDAtLevel = NULL, CalculationGroupAcademicStandardCalculationGroupHierarchyDepthIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardCalculationGroupHierarchyDepth", body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#'
	#' This function modifies a CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' @param fieldNames The field values to give the modified CalculationGroupAcademicStandardCalculationGroupHierarchyDepth. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupAcademicStandardCalculationGroupHierarchyDepth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupAcademicStandardCalculationGroupHierarchyDepth <- function(CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID, EntityGroupKey = NULL, CalculationGroupAcademicStandardID = NULL, CalculationGroupHierarchyDepthID = NULL, CalculationGroupAcademicStandardIDAtLevel = NULL, CalculationGroupAcademicStandardCalculationGroupHierarchyDepthIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupAcademicStandardCalculationGroupHierarchyDepth", objectId = CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID, body = list(DataObject = body), searchFields = append("CalculationGroupAcademicStandardCalculationGroupHierarchyDepthID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CalculationGroupHierarchyDepths
	#'
	#' This function returns a dataframe or json object of CalculationGroupHierarchyDepths
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupHierarchyDepths. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupHierarchyDepths.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupHierarchyDepth') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of CalculationGroupHierarchyDepths
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCalculationGroupHierarchyDepths <- function(searchConditionsList = NULL, CalculationGroupHierarchyDepthID = F, EntityGroupKey = F, CalculationGroupID = F, Code = F, Description = F, DepthLevel = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupHierarchyDepthIDClonedFrom = F, DynamicRelationshipID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "CalculationGroupHierarchyDepth", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CalculationGroupHierarchyDepth
	#'
	#' This function returns a dataframe or json object of a CalculationGroupHierarchyDepth
	#' @param CalculationGroupHierarchyDepthID The ID of the CalculationGroupHierarchyDepth to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CalculationGroupHierarchyDepth. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CalculationGroupHierarchyDepth.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CalculationGroupHierarchyDepth') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of CalculationGroupHierarchyDepth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCalculationGroupHierarchyDepth <- function(CalculationGroupHierarchyDepthID, EntityGroupKey = F, CalculationGroupID = F, Code = F, Description = F, DepthLevel = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, CalculationGroupHierarchyDepthIDClonedFrom = F, DynamicRelationshipID = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CalculationGroupHierarchyDepthID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "CalculationGroupHierarchyDepth", objectId = CalculationGroupHierarchyDepthID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CalculationGroupHierarchyDepth
	#'
	#' This function deletes a CalculationGroupHierarchyDepth
	#' @param CalculationGroupHierarchyDepthID The ID of the CalculationGroupHierarchyDepth to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The CalculationGroupHierarchyDepthID of the deleted CalculationGroupHierarchyDepth.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCalculationGroupHierarchyDepth <- function(CalculationGroupHierarchyDepthID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "CalculationGroupHierarchyDepth", objectId = CalculationGroupHierarchyDepthID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CalculationGroupHierarchyDepth
	#'
	#' This function creates a CalculationGroupHierarchyDepth
	#' @param fieldNames The field values to give the created CalculationGroupHierarchyDepth. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created CalculationGroupHierarchyDepth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCalculationGroupHierarchyDepth <- function(EntityGroupKey = NULL, CalculationGroupID = NULL, Code = NULL, Description = NULL, DepthLevel = NULL, CalculationGroupHierarchyDepthIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "CalculationGroupHierarchyDepth", body = list(DataObject = body), searchFields = append("CalculationGroupHierarchyDepthID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CalculationGroupHierarchyDepth
	#'
	#' This function modifies a CalculationGroupHierarchyDepth
	#' @param fieldNames The field values to give the modified CalculationGroupHierarchyDepth. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified CalculationGroupHierarchyDepth
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCalculationGroupHierarchyDepth <- function(CalculationGroupHierarchyDepthID, EntityGroupKey = NULL, CalculationGroupID = NULL, Code = NULL, Description = NULL, DepthLevel = NULL, CalculationGroupHierarchyDepthIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "CalculationGroupHierarchyDepth", objectId = CalculationGroupHierarchyDepthID, body = list(DataObject = body), searchFields = append("CalculationGroupHierarchyDepthID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempGradebookGroupErrors
	#'
	#' This function returns a dataframe or json object of TempGradebookGroupErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradebookGroupErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradebookGroupErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradebookGroupError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempGradebookGroupErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempGradebookGroupErrors <- function(searchConditionsList = NULL, TempGradebookGroupErrorID = F, EntityID = F, CalculationGroupID = F, GradingScaleGroupID = F, Description = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempGradebookGroupError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempGradebookGroupError
	#'
	#' This function returns a dataframe or json object of a TempGradebookGroupError
	#' @param TempGradebookGroupErrorID The ID of the TempGradebookGroupError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempGradebookGroupError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempGradebookGroupError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempGradebookGroupError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempGradebookGroupError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempGradebookGroupError <- function(TempGradebookGroupErrorID, EntityID = F, CalculationGroupID = F, GradingScaleGroupID = F, Description = F, ErrorText = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempGradebookGroupErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempGradebookGroupError", objectId = TempGradebookGroupErrorID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempGradebookGroupError
	#'
	#' This function deletes a TempGradebookGroupError
	#' @param TempGradebookGroupErrorID The ID of the TempGradebookGroupError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempGradebookGroupErrorID of the deleted TempGradebookGroupError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempGradebookGroupError <- function(TempGradebookGroupErrorID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempGradebookGroupError", objectId = TempGradebookGroupErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempGradebookGroupError
	#'
	#' This function creates a TempGradebookGroupError
	#' @param fieldNames The field values to give the created TempGradebookGroupError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempGradebookGroupError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempGradebookGroupError <- function(EntityID = NULL, CalculationGroupID = NULL, GradingScaleGroupID = NULL, Description = NULL, ErrorText = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempGradebookGroupError", body = list(DataObject = body), searchFields = append("TempGradebookGroupErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempGradebookGroupError
	#'
	#' This function modifies a TempGradebookGroupError
	#' @param fieldNames The field values to give the modified TempGradebookGroupError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempGradebookGroupError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempGradebookGroupError <- function(TempGradebookGroupErrorID, EntityID = NULL, CalculationGroupID = NULL, GradingScaleGroupID = NULL, Description = NULL, ErrorText = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempGradebookGroupError", objectId = TempGradebookGroupErrorID, body = list(DataObject = body), searchFields = append("TempGradebookGroupErrorID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAssignmentAttempts
	#'
	#' This function returns a dataframe or json object of StudentAssignmentAttempts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAssignmentAttempts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAssignmentAttempts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAssignmentAttempt') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentAssignmentAttempts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAssignmentAttempts <- function(searchConditionsList = NULL, StudentAssignmentAttemptID = F, StudentAssignmentID = F, Score = F, Comments = F, IsUsed = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeMarkID = F, DateAttempted = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentAssignmentAttempt", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAssignmentAttempt
	#'
	#' This function returns a dataframe or json object of a StudentAssignmentAttempt
	#' @param StudentAssignmentAttemptID The ID of the StudentAssignmentAttempt to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAssignmentAttempt. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAssignmentAttempt.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAssignmentAttempt') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentAssignmentAttempt
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAssignmentAttempt <- function(StudentAssignmentAttemptID, StudentAssignmentID = F, Score = F, Comments = F, IsUsed = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, GradeMarkID = F, DateAttempted = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAssignmentAttemptID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentAssignmentAttempt", objectId = StudentAssignmentAttemptID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAssignmentAttempt
	#'
	#' This function deletes a StudentAssignmentAttempt
	#' @param StudentAssignmentAttemptID The ID of the StudentAssignmentAttempt to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentAssignmentAttemptID of the deleted StudentAssignmentAttempt.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAssignmentAttempt <- function(StudentAssignmentAttemptID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentAssignmentAttempt", objectId = StudentAssignmentAttemptID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAssignmentAttempt
	#'
	#' This function creates a StudentAssignmentAttempt
	#' @param fieldNames The field values to give the created StudentAssignmentAttempt. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentAssignmentAttempt
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAssignmentAttempt <- function(StudentAssignmentID = NULL, Score = NULL, Comments = NULL, IsUsed = NULL, GradeMarkID = NULL, DateAttempted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentAssignmentAttempt", body = list(DataObject = body), searchFields = append("StudentAssignmentAttemptID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAssignmentAttempt
	#'
	#' This function modifies a StudentAssignmentAttempt
	#' @param fieldNames The field values to give the modified StudentAssignmentAttempt. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentAssignmentAttempt
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAssignmentAttempt <- function(StudentAssignmentAttemptID, StudentAssignmentID = NULL, Score = NULL, Comments = NULL, IsUsed = NULL, GradeMarkID = NULL, DateAttempted = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentAssignmentAttempt", objectId = StudentAssignmentAttemptID, body = list(DataObject = body), searchFields = append("StudentAssignmentAttemptID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGroupTeacherSectionSettings
	#'
	#' This function returns a dataframe or json object of StudentGroupTeacherSectionSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroupTeacherSectionSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroupTeacherSectionSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroupTeacherSectionSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGroupTeacherSectionSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGroupTeacherSectionSettings <- function(searchConditionsList = NULL, StudentGroupTeacherSectionSettingID = F, StudentGroupID = F, TeacherSectionSettingID = F, Display = F, Color = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGroupTeacherSectionSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGroupTeacherSectionSetting
	#'
	#' This function returns a dataframe or json object of a StudentGroupTeacherSectionSetting
	#' @param StudentGroupTeacherSectionSettingID The ID of the StudentGroupTeacherSectionSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroupTeacherSectionSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroupTeacherSectionSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroupTeacherSectionSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGroupTeacherSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGroupTeacherSectionSetting <- function(StudentGroupTeacherSectionSettingID, StudentGroupID = F, TeacherSectionSettingID = F, Display = F, Color = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGroupTeacherSectionSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGroupTeacherSectionSetting", objectId = StudentGroupTeacherSectionSettingID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGroupTeacherSectionSetting
	#'
	#' This function deletes a StudentGroupTeacherSectionSetting
	#' @param StudentGroupTeacherSectionSettingID The ID of the StudentGroupTeacherSectionSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGroupTeacherSectionSettingID of the deleted StudentGroupTeacherSectionSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGroupTeacherSectionSetting <- function(StudentGroupTeacherSectionSettingID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGroupTeacherSectionSetting", objectId = StudentGroupTeacherSectionSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGroupTeacherSectionSetting
	#'
	#' This function creates a StudentGroupTeacherSectionSetting
	#' @param fieldNames The field values to give the created StudentGroupTeacherSectionSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGroupTeacherSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGroupTeacherSectionSetting <- function(StudentGroupID = NULL, TeacherSectionSettingID = NULL, Display = NULL, Color = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGroupTeacherSectionSetting", body = list(DataObject = body), searchFields = append("StudentGroupTeacherSectionSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGroupTeacherSectionSetting
	#'
	#' This function modifies a StudentGroupTeacherSectionSetting
	#' @param fieldNames The field values to give the modified StudentGroupTeacherSectionSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGroupTeacherSectionSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGroupTeacherSectionSetting <- function(StudentGroupTeacherSectionSettingID, StudentGroupID = NULL, TeacherSectionSettingID = NULL, Display = NULL, Color = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGroupTeacherSectionSetting", objectId = StudentGroupTeacherSectionSettingID, body = list(DataObject = body), searchFields = append("StudentGroupTeacherSectionSettingID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGroupAssignments
	#'
	#' This function returns a dataframe or json object of StudentGroupAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroupAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroupAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroupAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGroupAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGroupAssignments <- function(searchConditionsList = NULL, StudentGroupAssignmentID = F, StudentGroupID = F, AssignmentID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DeleteErrorMessage = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGroupAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGroupAssignment
	#'
	#' This function returns a dataframe or json object of a StudentGroupAssignment
	#' @param StudentGroupAssignmentID The ID of the StudentGroupAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroupAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroupAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroupAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGroupAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGroupAssignment <- function(StudentGroupAssignmentID, StudentGroupID = F, AssignmentID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, DeleteErrorMessage = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGroupAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGroupAssignment", objectId = StudentGroupAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGroupAssignment
	#'
	#' This function deletes a StudentGroupAssignment
	#' @param StudentGroupAssignmentID The ID of the StudentGroupAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGroupAssignmentID of the deleted StudentGroupAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGroupAssignment <- function(StudentGroupAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGroupAssignment", objectId = StudentGroupAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGroupAssignment
	#'
	#' This function creates a StudentGroupAssignment
	#' @param fieldNames The field values to give the created StudentGroupAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGroupAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGroupAssignment <- function(StudentGroupID = NULL, AssignmentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGroupAssignment", body = list(DataObject = body), searchFields = append("StudentGroupAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGroupAssignment
	#'
	#' This function modifies a StudentGroupAssignment
	#' @param fieldNames The field values to give the modified StudentGroupAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGroupAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGroupAssignment <- function(StudentGroupAssignmentID, StudentGroupID = NULL, AssignmentID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGroupAssignment", objectId = StudentGroupAssignmentID, body = list(DataObject = body), searchFields = append("StudentGroupAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGroupStudentSections
	#'
	#' This function returns a dataframe or json object of StudentGroupStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroupStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroupStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroupStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGroupStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGroupStudentSections <- function(searchConditionsList = NULL, StudentGroupStudentSectionID = F, StudentGroupID = F, StudentSectionID = F, StartDate = F, EndDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AssignmentCount = F, ScoredAssignmentCount = F, IsActive = F, CanBeDeleted = F, UnableToDeleteMessage = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGroupStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGroupStudentSection
	#'
	#' This function returns a dataframe or json object of a StudentGroupStudentSection
	#' @param StudentGroupStudentSectionID The ID of the StudentGroupStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroupStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroupStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroupStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGroupStudentSection <- function(StudentGroupStudentSectionID, StudentGroupID = F, StudentSectionID = F, StartDate = F, EndDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, AssignmentCount = F, ScoredAssignmentCount = F, IsActive = F, CanBeDeleted = F, UnableToDeleteMessage = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGroupStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGroupStudentSection", objectId = StudentGroupStudentSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGroupStudentSection
	#'
	#' This function deletes a StudentGroupStudentSection
	#' @param StudentGroupStudentSectionID The ID of the StudentGroupStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGroupStudentSectionID of the deleted StudentGroupStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGroupStudentSection <- function(StudentGroupStudentSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGroupStudentSection", objectId = StudentGroupStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGroupStudentSection
	#'
	#' This function creates a StudentGroupStudentSection
	#' @param fieldNames The field values to give the created StudentGroupStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGroupStudentSection <- function(StudentGroupID = NULL, StudentSectionID = NULL, StartDate = NULL, EndDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGroupStudentSection", body = list(DataObject = body), searchFields = append("StudentGroupStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGroupStudentSection
	#'
	#' This function modifies a StudentGroupStudentSection
	#' @param fieldNames The field values to give the modified StudentGroupStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGroupStudentSection <- function(StudentGroupStudentSectionID, StudentGroupID = NULL, StudentSectionID = NULL, StartDate = NULL, EndDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGroupStudentSection", objectId = StudentGroupStudentSectionID, body = list(DataObject = body), searchFields = append("StudentGroupStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGroups
	#'
	#' This function returns a dataframe or json object of StudentGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGroups <- function(searchConditionsList = NULL, StudentGroupID = F, SectionID = F, Name = F, StartDate = F, EndDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsActive = F, AssignmentCount = F, StudentCount = F, CanBeDeleted = F, StudentGroupSyncKey = F, IsAHistoricRecord = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGroup
	#'
	#' This function returns a dataframe or json object of a StudentGroup
	#' @param StudentGroupID The ID of the StudentGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGroup <- function(StudentGroupID, SectionID = F, Name = F, StartDate = F, EndDate = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsActive = F, AssignmentCount = F, StudentCount = F, CanBeDeleted = F, StudentGroupSyncKey = F, IsAHistoricRecord = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGroup", objectId = StudentGroupID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGroup
	#'
	#' This function deletes a StudentGroup
	#' @param StudentGroupID The ID of the StudentGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGroupID of the deleted StudentGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGroup <- function(StudentGroupID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGroup", objectId = StudentGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGroup
	#'
	#' This function creates a StudentGroup
	#' @param fieldNames The field values to give the created StudentGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGroup <- function(SectionID = NULL, Name = NULL, StartDate = NULL, EndDate = NULL, StudentGroupSyncKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGroup", body = list(DataObject = body), searchFields = append("StudentGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGroup
	#'
	#' This function modifies a StudentGroup
	#' @param fieldNames The field values to give the modified StudentGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGroup <- function(StudentGroupID, SectionID = NULL, Name = NULL, StartDate = NULL, EndDate = NULL, StudentGroupSyncKey = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGroup", objectId = StudentGroupID, body = list(DataObject = body), searchFields = append("StudentGroupID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookVendorCategories
	#'
	#' This function returns a dataframe or json object of GradebookVendorCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookVendorCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookVendorCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookVendorCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookVendorCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookVendorCategories <- function(searchConditionsList = NULL, VendorCategoryID = F, CategoryID = F, OneRosterVendorID = F, VendorSourceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "VendorCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookVendorCategory
	#'
	#' This function returns a dataframe or json object of a GradebookVendorCategory
	#' @param GradebookVendorCategoryID The ID of the GradebookVendorCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookVendorCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookVendorCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookVendorCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookVendorCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookVendorCategory <- function(GradebookVendorCategoryID, VendorCategoryID = F, CategoryID = F, OneRosterVendorID = F, VendorSourceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookVendorCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "VendorCategory", objectId = GradebookVendorCategoryID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookVendorCategory
	#'
	#' This function deletes a GradebookVendorCategory
	#' @param GradebookVendorCategoryID The ID of the GradebookVendorCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookVendorCategoryID of the deleted GradebookVendorCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookVendorCategory <- function(GradebookVendorCategoryID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "VendorCategory", objectId = GradebookVendorCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookVendorCategory
	#'
	#' This function creates a GradebookVendorCategory
	#' @param fieldNames The field values to give the created GradebookVendorCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookVendorCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookVendorCategory <- function(CategoryID = NULL, OneRosterVendorID = NULL, VendorSourceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "VendorCategory", body = list(DataObject = body), searchFields = append("VendorCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookVendorCategory
	#'
	#' This function modifies a GradebookVendorCategory
	#' @param fieldNames The field values to give the modified GradebookVendorCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookVendorCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookVendorCategory <- function(VendorCategoryID, CategoryID = NULL, OneRosterVendorID = NULL, VendorSourceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "VendorCategory", objectId = VendorCategoryID, body = list(DataObject = body), searchFields = append("VendorCategoryID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VendorStudentAssignments
	#'
	#' This function returns a dataframe or json object of VendorStudentAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VendorStudentAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VendorStudentAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VendorStudentAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of VendorStudentAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVendorStudentAssignments <- function(searchConditionsList = NULL, VendorStudentAssignmentID = F, StudentAssignmentID = F, OneRosterVendorID = F, VendorSourceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "VendorStudentAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VendorStudentAssignment
	#'
	#' This function returns a dataframe or json object of a VendorStudentAssignment
	#' @param VendorStudentAssignmentID The ID of the VendorStudentAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VendorStudentAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VendorStudentAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VendorStudentAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of VendorStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVendorStudentAssignment <- function(VendorStudentAssignmentID, StudentAssignmentID = F, OneRosterVendorID = F, VendorSourceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VendorStudentAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "VendorStudentAssignment", objectId = VendorStudentAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VendorStudentAssignment
	#'
	#' This function deletes a VendorStudentAssignment
	#' @param VendorStudentAssignmentID The ID of the VendorStudentAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The VendorStudentAssignmentID of the deleted VendorStudentAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVendorStudentAssignment <- function(VendorStudentAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "VendorStudentAssignment", objectId = VendorStudentAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VendorStudentAssignment
	#'
	#' This function creates a VendorStudentAssignment
	#' @param fieldNames The field values to give the created VendorStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created VendorStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVendorStudentAssignment <- function(StudentAssignmentID = NULL, OneRosterVendorID = NULL, VendorSourceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "VendorStudentAssignment", body = list(DataObject = body), searchFields = append("VendorStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VendorStudentAssignment
	#'
	#' This function modifies a VendorStudentAssignment
	#' @param fieldNames The field values to give the modified VendorStudentAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified VendorStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVendorStudentAssignment <- function(VendorStudentAssignmentID, StudentAssignmentID = NULL, OneRosterVendorID = NULL, VendorSourceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "VendorStudentAssignment", objectId = VendorStudentAssignmentID, body = list(DataObject = body), searchFields = append("VendorStudentAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VendorAssignments
	#'
	#' This function returns a dataframe or json object of VendorAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VendorAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VendorAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VendorAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of VendorAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVendorAssignments <- function(searchConditionsList = NULL, VendorAssignmentID = F, AssignmentID = F, OneRosterVendorID = F, VendorSourceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "VendorAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VendorAssignment
	#'
	#' This function returns a dataframe or json object of a VendorAssignment
	#' @param VendorAssignmentID The ID of the VendorAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VendorAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VendorAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VendorAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of VendorAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVendorAssignment <- function(VendorAssignmentID, AssignmentID = F, OneRosterVendorID = F, VendorSourceID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VendorAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "VendorAssignment", objectId = VendorAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VendorAssignment
	#'
	#' This function deletes a VendorAssignment
	#' @param VendorAssignmentID The ID of the VendorAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The VendorAssignmentID of the deleted VendorAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVendorAssignment <- function(VendorAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "VendorAssignment", objectId = VendorAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VendorAssignment
	#'
	#' This function creates a VendorAssignment
	#' @param fieldNames The field values to give the created VendorAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created VendorAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVendorAssignment <- function(AssignmentID = NULL, OneRosterVendorID = NULL, VendorSourceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "VendorAssignment", body = list(DataObject = body), searchFields = append("VendorAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VendorAssignment
	#'
	#' This function modifies a VendorAssignment
	#' @param fieldNames The field values to give the modified VendorAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified VendorAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVendorAssignment <- function(VendorAssignmentID, AssignmentID = NULL, OneRosterVendorID = NULL, VendorSourceID = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "VendorAssignment", objectId = VendorAssignmentID, body = list(DataObject = body), searchFields = append("VendorAssignmentID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentGradingScaleGroupStudentSections
	#'
	#' This function returns a dataframe or json object of TempStudentGradingScaleGroupStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGradingScaleGroupStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGradingScaleGroupStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGradingScaleGroupStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempStudentGradingScaleGroupStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentGradingScaleGroupStudentSections <- function(searchConditionsList = NULL, TempStudentGradingScaleGroupStudentSectionID = F, StudentID = F, StudentSectionID = F, SectionLengthID = F, SectionID = F, StudentFullNameLFM = F, CourseDescriptionCodeSectionCode = F, SectionLengthCode = F, HasError = F, ErrorText = F, GradingPeriodGradeBuckets = F, StudentGradingScaleGroupStudentSectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsSelfPacedSection = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempStudentGradingScaleGroupStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentGradingScaleGroupStudentSection
	#'
	#' This function returns a dataframe or json object of a TempStudentGradingScaleGroupStudentSection
	#' @param TempStudentGradingScaleGroupStudentSectionID The ID of the TempStudentGradingScaleGroupStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentGradingScaleGroupStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentGradingScaleGroupStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentGradingScaleGroupStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempStudentGradingScaleGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentGradingScaleGroupStudentSection <- function(TempStudentGradingScaleGroupStudentSectionID, StudentID = F, StudentSectionID = F, SectionLengthID = F, SectionID = F, StudentFullNameLFM = F, CourseDescriptionCodeSectionCode = F, SectionLengthCode = F, HasError = F, ErrorText = F, GradingPeriodGradeBuckets = F, StudentGradingScaleGroupStudentSectionID = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, IsSelfPacedSection = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentGradingScaleGroupStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempStudentGradingScaleGroupStudentSection", objectId = TempStudentGradingScaleGroupStudentSectionID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentGradingScaleGroupStudentSection
	#'
	#' This function deletes a TempStudentGradingScaleGroupStudentSection
	#' @param TempStudentGradingScaleGroupStudentSectionID The ID of the TempStudentGradingScaleGroupStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempStudentGradingScaleGroupStudentSectionID of the deleted TempStudentGradingScaleGroupStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentGradingScaleGroupStudentSection <- function(TempStudentGradingScaleGroupStudentSectionID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempStudentGradingScaleGroupStudentSection", objectId = TempStudentGradingScaleGroupStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentGradingScaleGroupStudentSection
	#'
	#' This function creates a TempStudentGradingScaleGroupStudentSection
	#' @param fieldNames The field values to give the created TempStudentGradingScaleGroupStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempStudentGradingScaleGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentGradingScaleGroupStudentSection <- function(StudentID = NULL, StudentSectionID = NULL, SectionLengthID = NULL, SectionID = NULL, StudentFullNameLFM = NULL, CourseDescriptionCodeSectionCode = NULL, SectionLengthCode = NULL, HasError = NULL, ErrorText = NULL, GradingPeriodGradeBuckets = NULL, StudentGradingScaleGroupStudentSectionID = NULL, IsSelfPacedSection = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempStudentGradingScaleGroupStudentSection", body = list(DataObject = body), searchFields = append("TempStudentGradingScaleGroupStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentGradingScaleGroupStudentSection
	#'
	#' This function modifies a TempStudentGradingScaleGroupStudentSection
	#' @param fieldNames The field values to give the modified TempStudentGradingScaleGroupStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempStudentGradingScaleGroupStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentGradingScaleGroupStudentSection <- function(TempStudentGradingScaleGroupStudentSectionID, StudentID = NULL, StudentSectionID = NULL, SectionLengthID = NULL, SectionID = NULL, StudentFullNameLFM = NULL, CourseDescriptionCodeSectionCode = NULL, SectionLengthCode = NULL, HasError = NULL, ErrorText = NULL, GradingPeriodGradeBuckets = NULL, StudentGradingScaleGroupStudentSectionID = NULL, IsSelfPacedSection = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempStudentGradingScaleGroupStudentSection", objectId = TempStudentGradingScaleGroupStudentSectionID, body = list(DataObject = body), searchFields = append("TempStudentGradingScaleGroupStudentSectionID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GradebookConfigDistrictYears
	#'
	#' This function returns a dataframe or json object of GradebookConfigDistrictYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigDistrictYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigDistrictYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigDistrictYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of GradebookConfigDistrictYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGradebookConfigDistrictYears <- function(searchConditionsList = NULL, ConfigDistrictYearID = F, DistrictID = F, SchoolYearID = F, UseCurriculumSubjectsInGradebook = F, ConfigDistrictYearIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ConfigDistrictYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GradebookConfigDistrictYear
	#'
	#' This function returns a dataframe or json object of a GradebookConfigDistrictYear
	#' @param GradebookConfigDistrictYearID The ID of the GradebookConfigDistrictYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GradebookConfigDistrictYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GradebookConfigDistrictYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GradebookConfigDistrictYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of GradebookConfigDistrictYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGradebookConfigDistrictYear <- function(GradebookConfigDistrictYearID, ConfigDistrictYearID = F, DistrictID = F, SchoolYearID = F, UseCurriculumSubjectsInGradebook = F, ConfigDistrictYearIDClonedFrom = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GradebookConfigDistrictYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ConfigDistrictYear", objectId = GradebookConfigDistrictYearID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GradebookConfigDistrictYear
	#'
	#' This function deletes a GradebookConfigDistrictYear
	#' @param GradebookConfigDistrictYearID The ID of the GradebookConfigDistrictYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The GradebookConfigDistrictYearID of the deleted GradebookConfigDistrictYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGradebookConfigDistrictYear <- function(GradebookConfigDistrictYearID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ConfigDistrictYear", objectId = GradebookConfigDistrictYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GradebookConfigDistrictYear
	#'
	#' This function creates a GradebookConfigDistrictYear
	#' @param fieldNames The field values to give the created GradebookConfigDistrictYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created GradebookConfigDistrictYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGradebookConfigDistrictYear <- function(DistrictID = NULL, SchoolYearID = NULL, UseCurriculumSubjectsInGradebook = NULL, ConfigDistrictYearIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "ConfigDistrictYear", body = list(DataObject = body), searchFields = append("ConfigDistrictYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GradebookConfigDistrictYear
	#'
	#' This function modifies a GradebookConfigDistrictYear
	#' @param fieldNames The field values to give the modified GradebookConfigDistrictYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified GradebookConfigDistrictYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGradebookConfigDistrictYear <- function(ConfigDistrictYearID, DistrictID = NULL, SchoolYearID = NULL, UseCurriculumSubjectsInGradebook = NULL, ConfigDistrictYearIDClonedFrom = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "ConfigDistrictYear", objectId = ConfigDistrictYearID, body = list(DataObject = body), searchFields = append("ConfigDistrictYearID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List UnenteredStudentGradeBuckets
	#'
	#' This function returns a dataframe or json object of UnenteredStudentGradeBuckets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnenteredStudentGradeBuckets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnenteredStudentGradeBuckets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnenteredStudentGradeBucket') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of UnenteredStudentGradeBuckets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listUnenteredStudentGradeBuckets <- function(searchConditionsList = NULL, StudentGradeBucketID = F, StudentSectionID = F, GradeMarkIDToApply = F, StudentID = F, StaffID = F, EntityID = F, SchoolYearID = F, SectionID = F, GradeBucketID = F, DisplayPeriodID = F, UseServiceIDOverride = F, GradeBucketLabel = F, GradeMarkCode = F, EntityCode = F, CourseDescription = F, Percent = F, StudentGradeBucketFlag = F, IsAdminOverride = F, StudentSectionTransactionStartDate = F, StudentSectionTransactionEndDate = F, GradingPeriodStartDate = F, GradingPeriodEndDate = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "UnenteredStudentGradeBucket", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an UnenteredStudentGradeBucket
	#'
	#' This function returns a dataframe or json object of an UnenteredStudentGradeBucket
	#' @param UnenteredStudentGradeBucketID The ID of the UnenteredStudentGradeBucket to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given UnenteredStudentGradeBucket. Defaults to FALSE for all return fields which, for convenience, returns all fields for the UnenteredStudentGradeBucket.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('UnenteredStudentGradeBucket') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of UnenteredStudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getUnenteredStudentGradeBucket <- function(UnenteredStudentGradeBucketID, StudentGradeBucketID = F, StudentSectionID = F, GradeMarkIDToApply = F, StudentID = F, StaffID = F, EntityID = F, SchoolYearID = F, SectionID = F, GradeBucketID = F, DisplayPeriodID = F, UseServiceIDOverride = F, GradeBucketLabel = F, GradeMarkCode = F, EntityCode = F, CourseDescription = F, Percent = F, StudentGradeBucketFlag = F, IsAdminOverride = F, StudentSectionTransactionStartDate = F, StudentSectionTransactionEndDate = F, GradingPeriodStartDate = F, GradingPeriodEndDate = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "UnenteredStudentGradeBucketID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "UnenteredStudentGradeBucket", objectId = UnenteredStudentGradeBucketID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an UnenteredStudentGradeBucket
	#'
	#' This function deletes an UnenteredStudentGradeBucket
	#' @param UnenteredStudentGradeBucketID The ID of the UnenteredStudentGradeBucket to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The UnenteredStudentGradeBucketID of the deleted UnenteredStudentGradeBucket.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteUnenteredStudentGradeBucket <- function(UnenteredStudentGradeBucketID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "UnenteredStudentGradeBucket", objectId = UnenteredStudentGradeBucketID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an UnenteredStudentGradeBucket
	#'
	#' This function creates an UnenteredStudentGradeBucket
	#' @param fieldNames The field values to give the created UnenteredStudentGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created UnenteredStudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createUnenteredStudentGradeBucket <- function(Percent = NULL, IsAdminOverride = NULL, StudentSectionTransactionStartDate = NULL, StudentSectionTransactionEndDate = NULL, GradingPeriodStartDate = NULL, GradingPeriodEndDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "UnenteredStudentGradeBucket", body = list(DataObject = body), searchFields = append("UnenteredStudentGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an UnenteredStudentGradeBucket
	#'
	#' This function modifies an UnenteredStudentGradeBucket
	#' @param fieldNames The field values to give the modified UnenteredStudentGradeBucket. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified UnenteredStudentGradeBucket
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyUnenteredStudentGradeBucket <- function(UnenteredStudentGradeBucketID, Percent = NULL, IsAdminOverride = NULL, StudentSectionTransactionStartDate = NULL, StudentSectionTransactionEndDate = NULL, GradingPeriodStartDate = NULL, GradingPeriodEndDate = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "UnenteredStudentGradeBucket", objectId = UnenteredStudentGradeBucketID, body = list(DataObject = body), searchFields = append("UnenteredStudentGradeBucketID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StaffMeetLastScoreds
	#'
	#' This function returns a dataframe or json object of StaffMeetLastScoreds
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffMeetLastScoreds. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffMeetLastScoreds.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffMeetLastScored') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StaffMeetLastScoreds
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStaffMeetLastScoreds <- function(searchConditionsList = NULL, StaffMeetLastScoredID = F, StaffMeetID = F, StudentAssignmentIDLastScored = F, TimeLastScored = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StaffMeetLastScored", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StaffMeetLastScored
	#'
	#' This function returns a dataframe or json object of a StaffMeetLastScored
	#' @param StaffMeetLastScoredID The ID of the StaffMeetLastScored to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffMeetLastScored. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffMeetLastScored.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffMeetLastScored') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StaffMeetLastScored
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStaffMeetLastScored <- function(StaffMeetLastScoredID, StaffMeetID = F, StudentAssignmentIDLastScored = F, TimeLastScored = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StaffMeetLastScoredID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StaffMeetLastScored", objectId = StaffMeetLastScoredID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StaffMeetLastScored
	#'
	#' This function deletes a StaffMeetLastScored
	#' @param StaffMeetLastScoredID The ID of the StaffMeetLastScored to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StaffMeetLastScoredID of the deleted StaffMeetLastScored.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStaffMeetLastScored <- function(StaffMeetLastScoredID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StaffMeetLastScored", objectId = StaffMeetLastScoredID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StaffMeetLastScored
	#'
	#' This function creates a StaffMeetLastScored
	#' @param fieldNames The field values to give the created StaffMeetLastScored. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StaffMeetLastScored
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStaffMeetLastScored <- function(StaffMeetID = NULL, StudentAssignmentIDLastScored = NULL, TimeLastScored = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StaffMeetLastScored", body = list(DataObject = body), searchFields = append("StaffMeetLastScoredID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StaffMeetLastScored
	#'
	#' This function modifies a StaffMeetLastScored
	#' @param fieldNames The field values to give the modified StaffMeetLastScored. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StaffMeetLastScored
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStaffMeetLastScored <- function(StaffMeetLastScoredID, StaffMeetID = NULL, StudentAssignmentIDLastScored = NULL, TimeLastScored = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StaffMeetLastScored", objectId = StaffMeetLastScoredID, body = list(DataObject = body), searchFields = append("StaffMeetLastScoredID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ContingentStudentAssignments
	#'
	#' This function returns a dataframe or json object of ContingentStudentAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ContingentStudentAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ContingentStudentAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ContingentStudentAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of ContingentStudentAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listContingentStudentAssignments <- function(searchConditionsList = NULL, StudentSectionID = F, AssignmentID = F, StudentAssignmentID = F, SectionID = F, EntityID = F, SchoolYearID = F, DueDate = F, IsEffectivelyMissing = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "ContingentStudentAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ContingentStudentAssignment
	#'
	#' This function returns a dataframe or json object of a ContingentStudentAssignment
	#' @param ContingentStudentAssignmentID The ID of the ContingentStudentAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ContingentStudentAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ContingentStudentAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ContingentStudentAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of ContingentStudentAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getContingentStudentAssignment <- function(ContingentStudentAssignmentID, StudentSectionID = F, AssignmentID = F, StudentAssignmentID = F, SectionID = F, EntityID = F, SchoolYearID = F, DueDate = F, IsEffectivelyMissing = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ContingentStudentAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "ContingentStudentAssignment", objectId = ContingentStudentAssignmentID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ContingentStudentAssignment
	#'
	#' This function deletes a ContingentStudentAssignment
	#' @param ContingentStudentAssignmentID The ID of the ContingentStudentAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The ContingentStudentAssignmentID of the deleted ContingentStudentAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteContingentStudentAssignment <- function(ContingentStudentAssignmentID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "ContingentStudentAssignment", objectId = ContingentStudentAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentAssignmentScores
	#'
	#' This function returns a dataframe or json object of TempStudentAssignmentScores
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentAssignmentScores. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentAssignmentScores.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentAssignmentScore') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of TempStudentAssignmentScores
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentAssignmentScores <- function(searchConditionsList = NULL, TempStudentAssignmentScoreID = F, StudentAssignmentID = F, SectionID = F, OriginalScore = F, NewScore = F, OriginalGradeMarkID = F, NewGradeMarkID = F, OriginalGradeMarkCode = F, NewGradeMarkCode = F, MaxScore = F, Weight = F, AssignmentName = F, DueDate = F, StudentName = F, ClassName = F, ErrorMessage = F, AttemptCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "TempStudentAssignmentScore", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentAssignmentScore
	#'
	#' This function returns a dataframe or json object of a TempStudentAssignmentScore
	#' @param TempStudentAssignmentScoreID The ID of the TempStudentAssignmentScore to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentAssignmentScore. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentAssignmentScore.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentAssignmentScore') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of TempStudentAssignmentScore
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentAssignmentScore <- function(TempStudentAssignmentScoreID, StudentAssignmentID = F, SectionID = F, OriginalScore = F, NewScore = F, OriginalGradeMarkID = F, NewGradeMarkID = F, OriginalGradeMarkCode = F, NewGradeMarkCode = F, MaxScore = F, Weight = F, AssignmentName = F, DueDate = F, StudentName = F, ClassName = F, ErrorMessage = F, AttemptCount = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentAssignmentScoreID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "TempStudentAssignmentScore", objectId = TempStudentAssignmentScoreID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentAssignmentScore
	#'
	#' This function deletes a TempStudentAssignmentScore
	#' @param TempStudentAssignmentScoreID The ID of the TempStudentAssignmentScore to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The TempStudentAssignmentScoreID of the deleted TempStudentAssignmentScore.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentAssignmentScore <- function(TempStudentAssignmentScoreID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "TempStudentAssignmentScore", objectId = TempStudentAssignmentScoreID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentAssignmentScore
	#'
	#' This function creates a TempStudentAssignmentScore
	#' @param fieldNames The field values to give the created TempStudentAssignmentScore. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created TempStudentAssignmentScore
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentAssignmentScore <- function(StudentAssignmentID = NULL, SectionID = NULL, OriginalScore = NULL, NewScore = NULL, OriginalGradeMarkID = NULL, NewGradeMarkID = NULL, OriginalGradeMarkCode = NULL, NewGradeMarkCode = NULL, MaxScore = NULL, Weight = NULL, AssignmentName = NULL, DueDate = NULL, StudentName = NULL, ClassName = NULL, ErrorMessage = NULL, AttemptCount = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "TempStudentAssignmentScore", body = list(DataObject = body), searchFields = append("TempStudentAssignmentScoreID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentAssignmentScore
	#'
	#' This function modifies a TempStudentAssignmentScore
	#' @param fieldNames The field values to give the modified TempStudentAssignmentScore. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified TempStudentAssignmentScore
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentAssignmentScore <- function(TempStudentAssignmentScoreID, StudentAssignmentID = NULL, SectionID = NULL, OriginalScore = NULL, NewScore = NULL, OriginalGradeMarkID = NULL, NewGradeMarkID = NULL, OriginalGradeMarkCode = NULL, NewGradeMarkCode = NULL, MaxScore = NULL, Weight = NULL, AssignmentName = NULL, DueDate = NULL, StudentName = NULL, ClassName = NULL, ErrorMessage = NULL, AttemptCount = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "TempStudentAssignmentScore", objectId = TempStudentAssignmentScoreID, body = list(DataObject = body), searchFields = append("TempStudentAssignmentScoreID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentGradeBucketTransferRuns
	#'
	#' This function returns a dataframe or json object of StudentGradeBucketTransferRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketTransferRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketTransferRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketTransferRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A list of StudentGradeBucketTransferRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentGradeBucketTransferRuns <- function(searchConditionsList = NULL, StudentGradeBucketTransferRunID = F, StudentGradeBucketID = F, RunTime = F, Percent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, schoolYearId = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Gradebook", objectName = "StudentGradeBucketTransferRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentGradeBucketTransferRun
	#'
	#' This function returns a dataframe or json object of a StudentGradeBucketTransferRun
	#' @param StudentGradeBucketTransferRunID The ID of the StudentGradeBucketTransferRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentGradeBucketTransferRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentGradeBucketTransferRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentGradeBucketTransferRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A dataframe or of StudentGradeBucketTransferRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentGradeBucketTransferRun <- function(StudentGradeBucketTransferRunID, StudentGradeBucketID = F, RunTime = F, Percent = F, UserIDCreatedBy = F, CreatedTime = F, UserIDModifiedBy = F, ModifiedTime = F, fieldPaths = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentGradeBucketTransferRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Gradebook", objectName = "StudentGradeBucketTransferRun", objectId = StudentGradeBucketTransferRunID, searchFields = searchFields, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentGradeBucketTransferRun
	#'
	#' This function deletes a StudentGradeBucketTransferRun
	#' @param StudentGradeBucketTransferRunID The ID of the StudentGradeBucketTransferRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The StudentGradeBucketTransferRunID of the deleted StudentGradeBucketTransferRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentGradeBucketTransferRun <- function(StudentGradeBucketTransferRunID, ignoreWarnings = F, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Gradebook", objectName = "StudentGradeBucketTransferRun", objectId = StudentGradeBucketTransferRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentGradeBucketTransferRun
	#'
	#' This function creates a StudentGradeBucketTransferRun
	#' @param fieldNames The field values to give the created StudentGradeBucketTransferRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return A newly created StudentGradeBucketTransferRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentGradeBucketTransferRun <- function(StudentGradeBucketID = NULL, RunTime = NULL, Percent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		createSkyObject(module = "Gradebook", objectName = "StudentGradeBucketTransferRun", body = list(DataObject = body), searchFields = append("StudentGradeBucketTransferRunID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentGradeBucketTransferRun
	#'
	#' This function modifies a StudentGradeBucketTransferRun
	#' @param fieldNames The field values to give the modified StudentGradeBucketTransferRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param schoolYearId The id of the schoolYear. Run \code{\link{listSchoolYears}} for a list of school years. Defaults to NULL (all school years).
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Gradebook
	#' @return The modified StudentGradeBucketTransferRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentGradeBucketTransferRun <- function(StudentGradeBucketTransferRunID, StudentGradeBucketID = NULL, RunTime = NULL, Percent = NULL, entityId = 1, schoolYearId = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% compact()

		modifySkyObject(module = "Gradebook", objectName = "StudentGradeBucketTransferRun", objectId = StudentGradeBucketTransferRunID, body = list(DataObject = body), searchFields = append("StudentGradeBucketTransferRunID", body %>% names()), entityId = entityId, schoolYearId = schoolYearId, flatten = flatten, returnResponse = returnResponse)
	}
