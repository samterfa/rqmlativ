
	#' List BlockPeriods
	#'
	#' This function returns a dataframe or json object of BlockPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BlockPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BlockPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BlockPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of BlockPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listBlockPeriods <- function(searchConditionsList = NULL, BlockPeriodID = F, BlockPeriodIDClonedFrom = F, BlockPeriodIDClonedTo = F, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityID = F, HasDisplayPeriods = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "BlockPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a BlockPeriod
	#'
	#' This function returns a dataframe or json object of a BlockPeriod
	#' @param BlockPeriodID The ID of the BlockPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BlockPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BlockPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BlockPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of BlockPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getBlockPeriod <- function(BlockPeriodID, BlockPeriodIDClonedFrom = F, BlockPeriodIDClonedTo = F, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityID = F, HasDisplayPeriods = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "BlockPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "BlockPeriod", objectId = BlockPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a BlockPeriod
	#'
	#' This function deletes a BlockPeriod
	#' @param BlockPeriodID The ID of the BlockPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The BlockPeriodID of the deleted BlockPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteBlockPeriod <- function(BlockPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "BlockPeriod", objectId = BlockPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a BlockPeriod
	#'
	#' This function creates a BlockPeriod
	#' @param fieldNames The field values to give the created BlockPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created BlockPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createBlockPeriod <- function(BlockPeriodIDClonedFrom = NULL, Code = NULL, Description = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "BlockPeriod", body = list(DataObject = body), searchFields = append("BlockPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a BlockPeriod
	#'
	#' This function modifies a BlockPeriod
	#' @param fieldNames The field values to give the modified BlockPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified BlockPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyBlockPeriod <- function(BlockPeriodID, BlockPeriodIDClonedFrom = NULL, Code = NULL, Description = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "BlockPeriod", objectId = BlockPeriodID, body = list(DataObject = body), searchFields = append("BlockPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List BlockPeriodDisplayPeriods
	#'
	#' This function returns a dataframe or json object of BlockPeriodDisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BlockPeriodDisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BlockPeriodDisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BlockPeriodDisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of BlockPeriodDisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listBlockPeriodDisplayPeriods <- function(searchConditionsList = NULL, BlockPeriodDisplayPeriodID = F, BlockPeriodDisplayPeriodIDClonedFrom = F, BlockPeriodID = F, CreatedTime = F, DisplayPeriodID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "BlockPeriodDisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a BlockPeriodDisplayPeriod
	#'
	#' This function returns a dataframe or json object of a BlockPeriodDisplayPeriod
	#' @param BlockPeriodDisplayPeriodID The ID of the BlockPeriodDisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BlockPeriodDisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BlockPeriodDisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BlockPeriodDisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of BlockPeriodDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getBlockPeriodDisplayPeriod <- function(BlockPeriodDisplayPeriodID, BlockPeriodDisplayPeriodIDClonedFrom = F, BlockPeriodID = F, CreatedTime = F, DisplayPeriodID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "BlockPeriodDisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "BlockPeriodDisplayPeriod", objectId = BlockPeriodDisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a BlockPeriodDisplayPeriod
	#'
	#' This function deletes a BlockPeriodDisplayPeriod
	#' @param BlockPeriodDisplayPeriodID The ID of the BlockPeriodDisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The BlockPeriodDisplayPeriodID of the deleted BlockPeriodDisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteBlockPeriodDisplayPeriod <- function(BlockPeriodDisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "BlockPeriodDisplayPeriod", objectId = BlockPeriodDisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a BlockPeriodDisplayPeriod
	#'
	#' This function creates a BlockPeriodDisplayPeriod
	#' @param fieldNames The field values to give the created BlockPeriodDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created BlockPeriodDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createBlockPeriodDisplayPeriod <- function(BlockPeriodDisplayPeriodIDClonedFrom = NULL, BlockPeriodID = NULL, DisplayPeriodID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "BlockPeriodDisplayPeriod", body = list(DataObject = body), searchFields = append("BlockPeriodDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a BlockPeriodDisplayPeriod
	#'
	#' This function modifies a BlockPeriodDisplayPeriod
	#' @param fieldNames The field values to give the modified BlockPeriodDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified BlockPeriodDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyBlockPeriodDisplayPeriod <- function(BlockPeriodDisplayPeriodID, BlockPeriodDisplayPeriodIDClonedFrom = NULL, BlockPeriodID = NULL, DisplayPeriodID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "BlockPeriodDisplayPeriod", objectId = BlockPeriodDisplayPeriodID, body = list(DataObject = body), searchFields = append("BlockPeriodDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MaximumTeachingHourOverrides
	#'
	#' This function returns a dataframe or json object of MaximumTeachingHourOverrides
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MaximumTeachingHourOverrides. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MaximumTeachingHourOverrides.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MaximumTeachingHourOverride') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MaximumTeachingHourOverrides
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMaximumTeachingHourOverrides <- function(searchConditionsList = NULL, CreatedTime = F, DayRotationID = F, MaximumConsecutiveTeachingHours = F, MaximumTeachingHourOverrideID = F, MaximumTeachingHourOverrideIDClonedFrom = F, MaximumTotalTeachingHours = F, ModifiedTime = F, StaffEntityYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MaximumTeachingHourOverride", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MaximumTeachingHourOverride
	#'
	#' This function returns a dataframe or json object of a MaximumTeachingHourOverride
	#' @param MaximumTeachingHourOverrideID The ID of the MaximumTeachingHourOverride to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MaximumTeachingHourOverride. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MaximumTeachingHourOverride.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MaximumTeachingHourOverride') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MaximumTeachingHourOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMaximumTeachingHourOverride <- function(MaximumTeachingHourOverrideID, CreatedTime = F, DayRotationID = F, MaximumConsecutiveTeachingHours = F, MaximumTeachingHourOverrideIDClonedFrom = F, MaximumTotalTeachingHours = F, ModifiedTime = F, StaffEntityYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MaximumTeachingHourOverrideID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MaximumTeachingHourOverride", objectId = MaximumTeachingHourOverrideID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MaximumTeachingHourOverride
	#'
	#' This function deletes a MaximumTeachingHourOverride
	#' @param MaximumTeachingHourOverrideID The ID of the MaximumTeachingHourOverride to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MaximumTeachingHourOverrideID of the deleted MaximumTeachingHourOverride.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMaximumTeachingHourOverride <- function(MaximumTeachingHourOverrideID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MaximumTeachingHourOverride", objectId = MaximumTeachingHourOverrideID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MaximumTeachingHourOverride
	#'
	#' This function creates a MaximumTeachingHourOverride
	#' @param fieldNames The field values to give the created MaximumTeachingHourOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MaximumTeachingHourOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMaximumTeachingHourOverride <- function(DayRotationID = NULL, MaximumConsecutiveTeachingHours = NULL, MaximumTeachingHourOverrideIDClonedFrom = NULL, MaximumTotalTeachingHours = NULL, StaffEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MaximumTeachingHourOverride", body = list(DataObject = body), searchFields = append("MaximumTeachingHourOverrideID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MaximumTeachingHourOverride
	#'
	#' This function modifies a MaximumTeachingHourOverride
	#' @param fieldNames The field values to give the modified MaximumTeachingHourOverride. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MaximumTeachingHourOverride
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMaximumTeachingHourOverride <- function(MaximumTeachingHourOverrideID, DayRotationID = NULL, MaximumConsecutiveTeachingHours = NULL, MaximumTeachingHourOverrideIDClonedFrom = NULL, MaximumTotalTeachingHours = NULL, StaffEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MaximumTeachingHourOverride", objectId = MaximumTeachingHourOverrideID, body = list(DataObject = body), searchFields = append("MaximumTeachingHourOverrideID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerDisplayPeriodExcludedForCourses
	#'
	#' This function returns a dataframe or json object of SectionSchedulerDisplayPeriodExcludedForCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerDisplayPeriodExcludedForCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerDisplayPeriodExcludedForCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerDisplayPeriodExcludedForCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerDisplayPeriodExcludedForCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerDisplayPeriodExcludedForCourses <- function(searchConditionsList = NULL, CourseID = F, CreatedTime = F, DisplayPeriodID = F, ModifiedTime = F, SectionSchedulerDisplayPeriodExcludedForCourseID = F, SectionSchedulerDisplayPeriodExcludedForCourseIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerDisplayPeriodExcludedForCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerDisplayPeriodExcludedForCourse
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerDisplayPeriodExcludedForCourse
	#' @param SectionSchedulerDisplayPeriodExcludedForCourseID The ID of the SectionSchedulerDisplayPeriodExcludedForCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerDisplayPeriodExcludedForCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerDisplayPeriodExcludedForCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerDisplayPeriodExcludedForCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerDisplayPeriodExcludedForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerDisplayPeriodExcludedForCourse <- function(SectionSchedulerDisplayPeriodExcludedForCourseID, CourseID = F, CreatedTime = F, DisplayPeriodID = F, ModifiedTime = F, SectionSchedulerDisplayPeriodExcludedForCourseIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerDisplayPeriodExcludedForCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerDisplayPeriodExcludedForCourse", objectId = SectionSchedulerDisplayPeriodExcludedForCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerDisplayPeriodExcludedForCourse
	#'
	#' This function deletes a SectionSchedulerDisplayPeriodExcludedForCourse
	#' @param SectionSchedulerDisplayPeriodExcludedForCourseID The ID of the SectionSchedulerDisplayPeriodExcludedForCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerDisplayPeriodExcludedForCourseID of the deleted SectionSchedulerDisplayPeriodExcludedForCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerDisplayPeriodExcludedForCourse <- function(SectionSchedulerDisplayPeriodExcludedForCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerDisplayPeriodExcludedForCourse", objectId = SectionSchedulerDisplayPeriodExcludedForCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerDisplayPeriodExcludedForCourse
	#'
	#' This function creates a SectionSchedulerDisplayPeriodExcludedForCourse
	#' @param fieldNames The field values to give the created SectionSchedulerDisplayPeriodExcludedForCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerDisplayPeriodExcludedForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerDisplayPeriodExcludedForCourse <- function(CourseID = NULL, DisplayPeriodID = NULL, SectionSchedulerDisplayPeriodExcludedForCourseIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerDisplayPeriodExcludedForCourse", body = list(DataObject = body), searchFields = append("SectionSchedulerDisplayPeriodExcludedForCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerDisplayPeriodExcludedForCourse
	#'
	#' This function modifies a SectionSchedulerDisplayPeriodExcludedForCourse
	#' @param fieldNames The field values to give the modified SectionSchedulerDisplayPeriodExcludedForCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerDisplayPeriodExcludedForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerDisplayPeriodExcludedForCourse <- function(SectionSchedulerDisplayPeriodExcludedForCourseID, CourseID = NULL, DisplayPeriodID = NULL, SectionSchedulerDisplayPeriodExcludedForCourseIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerDisplayPeriodExcludedForCourse", objectId = SectionSchedulerDisplayPeriodExcludedForCourseID, body = list(DataObject = body), searchFields = append("SectionSchedulerDisplayPeriodExcludedForCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseEntityOfferedToSectionMeetDisplayPeriods
	#'
	#' This function returns a dataframe or json object of CourseEntityOfferedToSectionMeetDisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSectionMeetDisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSectionMeetDisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSectionMeetDisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseEntityOfferedToSectionMeetDisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseEntityOfferedToSectionMeetDisplayPeriods <- function(searchConditionsList = NULL, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionMeetDisplayPeriodID = F, CourseEntityOfferedToSectionMeetDisplayPeriodIDClonedFrom = F, CreatedTime = F, DisplayPeriodID = F, HideMeetDisplayPeriod = F, IsPrimary = F, MeetID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeetDisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseEntityOfferedToSectionMeetDisplayPeriod
	#'
	#' This function returns a dataframe or json object of a CourseEntityOfferedToSectionMeetDisplayPeriod
	#' @param CourseEntityOfferedToSectionMeetDisplayPeriodID The ID of the CourseEntityOfferedToSectionMeetDisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSectionMeetDisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSectionMeetDisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSectionMeetDisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseEntityOfferedToSectionMeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseEntityOfferedToSectionMeetDisplayPeriod <- function(CourseEntityOfferedToSectionMeetDisplayPeriodID, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionMeetDisplayPeriodIDClonedFrom = F, CreatedTime = F, DisplayPeriodID = F, HideMeetDisplayPeriod = F, IsPrimary = F, MeetID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseEntityOfferedToSectionMeetDisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeetDisplayPeriod", objectId = CourseEntityOfferedToSectionMeetDisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseEntityOfferedToSectionMeetDisplayPeriod
	#'
	#' This function deletes a CourseEntityOfferedToSectionMeetDisplayPeriod
	#' @param CourseEntityOfferedToSectionMeetDisplayPeriodID The ID of the CourseEntityOfferedToSectionMeetDisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseEntityOfferedToSectionMeetDisplayPeriodID of the deleted CourseEntityOfferedToSectionMeetDisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseEntityOfferedToSectionMeetDisplayPeriod <- function(CourseEntityOfferedToSectionMeetDisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeetDisplayPeriod", objectId = CourseEntityOfferedToSectionMeetDisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseEntityOfferedToSectionMeetDisplayPeriod
	#'
	#' This function creates a CourseEntityOfferedToSectionMeetDisplayPeriod
	#' @param fieldNames The field values to give the created CourseEntityOfferedToSectionMeetDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseEntityOfferedToSectionMeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseEntityOfferedToSectionMeetDisplayPeriod <- function(CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionMeetDisplayPeriodIDClonedFrom = NULL, DisplayPeriodID = NULL, HideMeetDisplayPeriod = NULL, IsPrimary = NULL, MeetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeetDisplayPeriod", body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionMeetDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseEntityOfferedToSectionMeetDisplayPeriod
	#'
	#' This function modifies a CourseEntityOfferedToSectionMeetDisplayPeriod
	#' @param fieldNames The field values to give the modified CourseEntityOfferedToSectionMeetDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseEntityOfferedToSectionMeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseEntityOfferedToSectionMeetDisplayPeriod <- function(CourseEntityOfferedToSectionMeetDisplayPeriodID, CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionMeetDisplayPeriodIDClonedFrom = NULL, DisplayPeriodID = NULL, HideMeetDisplayPeriod = NULL, IsPrimary = NULL, MeetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeetDisplayPeriod", objectId = CourseEntityOfferedToSectionMeetDisplayPeriodID, body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionMeetDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseEntityOfferedTos
	#'
	#' This function returns a dataframe or json object of CourseEntityOfferedTos
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedTos. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedTos.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedTo') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseEntityOfferedTos
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseEntityOfferedTos <- function(searchConditionsList = NULL, ActiveSections = F, ActiveSectionsOpen = F, CourseCode = F, CourseEntityOfferedToID = F, CourseEntityOfferedToIDClonedFrom = F, CourseEntityOfferedToIDClonedTo = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, EntityIDOfferedTo = F, HasStudentCourseRequestsEntity = F, IsActive = F, IsAutoOffering = F, IsCurrentSchoolYearEntity = F, IsHomeCourse = F, IsOfferedCourse = F, IsRequired = F, IsRequiredOverride = F, ModifiedTime = F, NumberOfAlternateCourseRequestsEntity = F, NumberOfCourseRequestsEntity = F, NumberOfCourseRequestsFemalesEntity = F, NumberOfCourseRequestsMalesEntity = F, NumberOfSeatsRemainingEntity = F, NumberTransferStudentSectionsEntity = F, Required = F, SchedulingPriorityCode = F, SchedulingPriorityOverride = F, SchedulingTypeCode = F, SchedulingTypeOverride = F, SchoolYearID = F, TotalSeatsAvailable = F, TotalSectionCountEntity = F, TotalStudentCourseRequestSectionLengthSubsetCountEntity = F, TotalStudentSectionCountEntity = F, UseIsRequiredOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UseSchedulingPriorityOverride = F, UseSchedulingTypeOverride = F, ViewingFromOfferedEntity = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseEntityOfferedTo", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseEntityOfferedTo
	#'
	#' This function returns a dataframe or json object of a CourseEntityOfferedTo
	#' @param CourseEntityOfferedToID The ID of the CourseEntityOfferedTo to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedTo. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedTo.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedTo') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseEntityOfferedTo
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseEntityOfferedTo <- function(CourseEntityOfferedToID, ActiveSections = F, ActiveSectionsOpen = F, CourseCode = F, CourseEntityOfferedToIDClonedFrom = F, CourseEntityOfferedToIDClonedTo = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, EntityIDOfferedTo = F, HasStudentCourseRequestsEntity = F, IsActive = F, IsAutoOffering = F, IsCurrentSchoolYearEntity = F, IsHomeCourse = F, IsOfferedCourse = F, IsRequired = F, IsRequiredOverride = F, ModifiedTime = F, NumberOfAlternateCourseRequestsEntity = F, NumberOfCourseRequestsEntity = F, NumberOfCourseRequestsFemalesEntity = F, NumberOfCourseRequestsMalesEntity = F, NumberOfSeatsRemainingEntity = F, NumberTransferStudentSectionsEntity = F, Required = F, SchedulingPriorityCode = F, SchedulingPriorityOverride = F, SchedulingTypeCode = F, SchedulingTypeOverride = F, SchoolYearID = F, TotalSeatsAvailable = F, TotalSectionCountEntity = F, TotalStudentCourseRequestSectionLengthSubsetCountEntity = F, TotalStudentSectionCountEntity = F, UseIsRequiredOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UseSchedulingPriorityOverride = F, UseSchedulingTypeOverride = F, ViewingFromOfferedEntity = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseEntityOfferedToID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedTo", objectId = CourseEntityOfferedToID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseEntityOfferedTo
	#'
	#' This function deletes a CourseEntityOfferedTo
	#' @param CourseEntityOfferedToID The ID of the CourseEntityOfferedTo to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseEntityOfferedToID of the deleted CourseEntityOfferedTo.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseEntityOfferedTo <- function(CourseEntityOfferedToID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedTo", objectId = CourseEntityOfferedToID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseEntityOfferedTo
	#'
	#' This function creates a CourseEntityOfferedTo
	#' @param fieldNames The field values to give the created CourseEntityOfferedTo. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseEntityOfferedTo
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseEntityOfferedTo <- function(CourseCode = NULL, CourseEntityOfferedToIDClonedFrom = NULL, CourseID = NULL, EntityGroupKey = NULL, EntityIDOfferedTo = NULL, IsActive = NULL, IsRequiredOverride = NULL, SchedulingPriorityOverride = NULL, SchedulingTypeOverride = NULL, SchoolYearID = NULL, UseIsRequiredOverride = NULL, UseSchedulingPriorityOverride = NULL, UseSchedulingTypeOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedTo", body = list(DataObject = body), searchFields = append("CourseEntityOfferedToID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseEntityOfferedTo
	#'
	#' This function modifies a CourseEntityOfferedTo
	#' @param fieldNames The field values to give the modified CourseEntityOfferedTo. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseEntityOfferedTo
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseEntityOfferedTo <- function(CourseEntityOfferedToID, CourseCode = NULL, CourseEntityOfferedToIDClonedFrom = NULL, CourseID = NULL, EntityGroupKey = NULL, EntityIDOfferedTo = NULL, IsActive = NULL, IsRequiredOverride = NULL, SchedulingPriorityOverride = NULL, SchedulingTypeOverride = NULL, SchoolYearID = NULL, UseIsRequiredOverride = NULL, UseSchedulingPriorityOverride = NULL, UseSchedulingTypeOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseEntityOfferedTo", objectId = CourseEntityOfferedToID, body = list(DataObject = body), searchFields = append("CourseEntityOfferedToID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseEntityOfferedToSectionStaffMeets
	#'
	#' This function returns a dataframe or json object of CourseEntityOfferedToSectionStaffMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSectionStaffMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSectionStaffMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSectionStaffMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseEntityOfferedToSectionStaffMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseEntityOfferedToSectionStaffMeets <- function(searchConditionsList = NULL, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionStaffMeetID = F, CourseEntityOfferedToSectionStaffMeetIDClonedFrom = F, CreatedTime = F, MeetID = F, ModifiedTime = F, StaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseEntityOfferedToSectionStaffMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseEntityOfferedToSectionStaffMeet
	#'
	#' This function returns a dataframe or json object of a CourseEntityOfferedToSectionStaffMeet
	#' @param CourseEntityOfferedToSectionStaffMeetID The ID of the CourseEntityOfferedToSectionStaffMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSectionStaffMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSectionStaffMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSectionStaffMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseEntityOfferedToSectionStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseEntityOfferedToSectionStaffMeet <- function(CourseEntityOfferedToSectionStaffMeetID, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionStaffMeetIDClonedFrom = F, CreatedTime = F, MeetID = F, ModifiedTime = F, StaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseEntityOfferedToSectionStaffMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionStaffMeet", objectId = CourseEntityOfferedToSectionStaffMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseEntityOfferedToSectionStaffMeet
	#'
	#' This function deletes a CourseEntityOfferedToSectionStaffMeet
	#' @param CourseEntityOfferedToSectionStaffMeetID The ID of the CourseEntityOfferedToSectionStaffMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseEntityOfferedToSectionStaffMeetID of the deleted CourseEntityOfferedToSectionStaffMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseEntityOfferedToSectionStaffMeet <- function(CourseEntityOfferedToSectionStaffMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionStaffMeet", objectId = CourseEntityOfferedToSectionStaffMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseEntityOfferedToSectionStaffMeet
	#'
	#' This function creates a CourseEntityOfferedToSectionStaffMeet
	#' @param fieldNames The field values to give the created CourseEntityOfferedToSectionStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseEntityOfferedToSectionStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseEntityOfferedToSectionStaffMeet <- function(CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionStaffMeetIDClonedFrom = NULL, MeetID = NULL, StaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionStaffMeet", body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseEntityOfferedToSectionStaffMeet
	#'
	#' This function modifies a CourseEntityOfferedToSectionStaffMeet
	#' @param fieldNames The field values to give the modified CourseEntityOfferedToSectionStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseEntityOfferedToSectionStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseEntityOfferedToSectionStaffMeet <- function(CourseEntityOfferedToSectionStaffMeetID, CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionStaffMeetIDClonedFrom = NULL, MeetID = NULL, StaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionStaffMeet", objectId = CourseEntityOfferedToSectionStaffMeetID, body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseEntityOfferedToSections
	#'
	#' This function returns a dataframe or json object of CourseEntityOfferedToSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseEntityOfferedToSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseEntityOfferedToSections <- function(searchConditionsList = NULL, CourseEntityOfferedToID = F, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionIDClonedFrom = F, CourseEntityOfferedToSectionIDClonedTo = F, CourseID = F, CreatedTime = F, EntityIDOfferedTo = F, HasMeetDisplayPeriodOverrides = F, IsActive = F, MaximumStudentCount = F, ModifiedTime = F, RoomSeatsAvailable = F, SchoolYearID = F, SeatsAvailableEntity = F, SectionID = F, StudentEnrollmentEntity = F, TotalEnrollmentCountEntity = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferedEntity = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseEntityOfferedToSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseEntityOfferedToSection
	#'
	#' This function returns a dataframe or json object of a CourseEntityOfferedToSection
	#' @param CourseEntityOfferedToSectionID The ID of the CourseEntityOfferedToSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseEntityOfferedToSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseEntityOfferedToSection <- function(CourseEntityOfferedToSectionID, CourseEntityOfferedToID = F, CourseEntityOfferedToSectionIDClonedFrom = F, CourseEntityOfferedToSectionIDClonedTo = F, CourseID = F, CreatedTime = F, EntityIDOfferedTo = F, HasMeetDisplayPeriodOverrides = F, IsActive = F, MaximumStudentCount = F, ModifiedTime = F, RoomSeatsAvailable = F, SchoolYearID = F, SeatsAvailableEntity = F, SectionID = F, StudentEnrollmentEntity = F, TotalEnrollmentCountEntity = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferedEntity = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseEntityOfferedToSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSection", objectId = CourseEntityOfferedToSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseEntityOfferedToSection
	#'
	#' This function deletes a CourseEntityOfferedToSection
	#' @param CourseEntityOfferedToSectionID The ID of the CourseEntityOfferedToSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseEntityOfferedToSectionID of the deleted CourseEntityOfferedToSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseEntityOfferedToSection <- function(CourseEntityOfferedToSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSection", objectId = CourseEntityOfferedToSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseEntityOfferedToSection
	#'
	#' This function creates a CourseEntityOfferedToSection
	#' @param fieldNames The field values to give the created CourseEntityOfferedToSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseEntityOfferedToSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseEntityOfferedToSection <- function(CourseEntityOfferedToID = NULL, CourseEntityOfferedToSectionIDClonedFrom = NULL, CourseID = NULL, EntityIDOfferedTo = NULL, IsActive = NULL, MaximumStudentCount = NULL, SchoolYearID = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSection", body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseEntityOfferedToSection
	#'
	#' This function modifies a CourseEntityOfferedToSection
	#' @param fieldNames The field values to give the modified CourseEntityOfferedToSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseEntityOfferedToSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseEntityOfferedToSection <- function(CourseEntityOfferedToSectionID, CourseEntityOfferedToID = NULL, CourseEntityOfferedToSectionIDClonedFrom = NULL, CourseID = NULL, EntityIDOfferedTo = NULL, IsActive = NULL, MaximumStudentCount = NULL, SchoolYearID = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSection", objectId = CourseEntityOfferedToSectionID, body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseEntityOfferedToSectionMeets
	#'
	#' This function returns a dataframe or json object of CourseEntityOfferedToSectionMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSectionMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSectionMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSectionMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseEntityOfferedToSectionMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseEntityOfferedToSectionMeets <- function(searchConditionsList = NULL, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionMeetID = F, CourseEntityOfferedToSectionMeetIDClonedFrom = F, CreatedTime = F, EntityIDOfferedTo = F, MeetID = F, ModifiedTime = F, RoomID = F, SchoolYearID = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseEntityOfferedToSectionMeet
	#'
	#' This function returns a dataframe or json object of a CourseEntityOfferedToSectionMeet
	#' @param CourseEntityOfferedToSectionMeetID The ID of the CourseEntityOfferedToSectionMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseEntityOfferedToSectionMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseEntityOfferedToSectionMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseEntityOfferedToSectionMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseEntityOfferedToSectionMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseEntityOfferedToSectionMeet <- function(CourseEntityOfferedToSectionMeetID, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionMeetIDClonedFrom = F, CreatedTime = F, EntityIDOfferedTo = F, MeetID = F, ModifiedTime = F, RoomID = F, SchoolYearID = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseEntityOfferedToSectionMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeet", objectId = CourseEntityOfferedToSectionMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseEntityOfferedToSectionMeet
	#'
	#' This function deletes a CourseEntityOfferedToSectionMeet
	#' @param CourseEntityOfferedToSectionMeetID The ID of the CourseEntityOfferedToSectionMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseEntityOfferedToSectionMeetID of the deleted CourseEntityOfferedToSectionMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseEntityOfferedToSectionMeet <- function(CourseEntityOfferedToSectionMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeet", objectId = CourseEntityOfferedToSectionMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseEntityOfferedToSectionMeet
	#'
	#' This function creates a CourseEntityOfferedToSectionMeet
	#' @param fieldNames The field values to give the created CourseEntityOfferedToSectionMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseEntityOfferedToSectionMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseEntityOfferedToSectionMeet <- function(CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionMeetIDClonedFrom = NULL, EntityIDOfferedTo = NULL, MeetID = NULL, RoomID = NULL, SchoolYearID = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeet", body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseEntityOfferedToSectionMeet
	#'
	#' This function modifies a CourseEntityOfferedToSectionMeet
	#' @param fieldNames The field values to give the modified CourseEntityOfferedToSectionMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseEntityOfferedToSectionMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseEntityOfferedToSectionMeet <- function(CourseEntityOfferedToSectionMeetID, CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionMeetIDClonedFrom = NULL, EntityIDOfferedTo = NULL, MeetID = NULL, RoomID = NULL, SchoolYearID = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseEntityOfferedToSectionMeet", objectId = CourseEntityOfferedToSectionMeetID, body = list(DataObject = body), searchFields = append("CourseEntityOfferedToSectionMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseLevelMNS
	#'
	#' This function returns a dataframe or json object of CourseLevelMNS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseLevelMNS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseLevelMNS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseLevelMN') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseLevelMNS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseLevelMNS <- function(searchConditionsList = NULL, Code = F, CourseLevelMNID = F, CreatedTime = F, Credits = F, CurriculumYearID = F, InstitutionID = F, ModifiedTime = F, StateCollegeCourseLevelMNID = F, Title = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseLevelMN", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseLevelMN
	#'
	#' This function returns a dataframe or json object of a CourseLevelMN
	#' @param CourseLevelMNID The ID of the CourseLevelMN to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseLevelMN. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseLevelMN.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseLevelMN') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseLevelMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseLevelMN <- function(CourseLevelMNID, Code = F, CreatedTime = F, Credits = F, CurriculumYearID = F, InstitutionID = F, ModifiedTime = F, StateCollegeCourseLevelMNID = F, Title = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseLevelMNID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseLevelMN", objectId = CourseLevelMNID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseLevelMN
	#'
	#' This function deletes a CourseLevelMN
	#' @param CourseLevelMNID The ID of the CourseLevelMN to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseLevelMNID of the deleted CourseLevelMN.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseLevelMN <- function(CourseLevelMNID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseLevelMN", objectId = CourseLevelMNID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseLevelMN
	#'
	#' This function creates a CourseLevelMN
	#' @param fieldNames The field values to give the created CourseLevelMN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseLevelMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseLevelMN <- function(Code = NULL, Credits = NULL, CurriculumYearID = NULL, InstitutionID = NULL, StateCollegeCourseLevelMNID = NULL, Title = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseLevelMN", body = list(DataObject = body), searchFields = append("CourseLevelMNID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseLevelMN
	#'
	#' This function modifies a CourseLevelMN
	#' @param fieldNames The field values to give the modified CourseLevelMN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseLevelMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseLevelMN <- function(CourseLevelMNID, Code = NULL, Credits = NULL, CurriculumYearID = NULL, InstitutionID = NULL, StateCollegeCourseLevelMNID = NULL, Title = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseLevelMN", objectId = CourseLevelMNID, body = list(DataObject = body), searchFields = append("CourseLevelMNID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MCCCTermGradeBucketMNS
	#'
	#' This function returns a dataframe or json object of MCCCTermGradeBucketMNS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MCCCTermGradeBucketMNS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MCCCTermGradeBucketMNS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MCCCTermGradeBucketMN') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MCCCTermGradeBucketMNS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMCCCTermGradeBucketMNS <- function(searchConditionsList = NULL, CreatedTime = F, GradeBucketID = F, MCCCTermGradeBucketMNID = F, MCCCTermImportID = F, ModifiedTime = F, SectionLengthID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MCCCTermGradeBucketMN", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MCCCTermGradeBucketMN
	#'
	#' This function returns a dataframe or json object of a MCCCTermGradeBucketMN
	#' @param MCCCTermGradeBucketMNID The ID of the MCCCTermGradeBucketMN to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MCCCTermGradeBucketMN. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MCCCTermGradeBucketMN.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MCCCTermGradeBucketMN') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MCCCTermGradeBucketMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMCCCTermGradeBucketMN <- function(MCCCTermGradeBucketMNID, CreatedTime = F, GradeBucketID = F, MCCCTermImportID = F, ModifiedTime = F, SectionLengthID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MCCCTermGradeBucketMNID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MCCCTermGradeBucketMN", objectId = MCCCTermGradeBucketMNID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MCCCTermGradeBucketMN
	#'
	#' This function deletes a MCCCTermGradeBucketMN
	#' @param MCCCTermGradeBucketMNID The ID of the MCCCTermGradeBucketMN to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MCCCTermGradeBucketMNID of the deleted MCCCTermGradeBucketMN.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMCCCTermGradeBucketMN <- function(MCCCTermGradeBucketMNID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MCCCTermGradeBucketMN", objectId = MCCCTermGradeBucketMNID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MCCCTermGradeBucketMN
	#'
	#' This function creates a MCCCTermGradeBucketMN
	#' @param fieldNames The field values to give the created MCCCTermGradeBucketMN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MCCCTermGradeBucketMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMCCCTermGradeBucketMN <- function(GradeBucketID = NULL, MCCCTermImportID = NULL, SectionLengthID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MCCCTermGradeBucketMN", body = list(DataObject = body), searchFields = append("MCCCTermGradeBucketMNID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MCCCTermGradeBucketMN
	#'
	#' This function modifies a MCCCTermGradeBucketMN
	#' @param fieldNames The field values to give the modified MCCCTermGradeBucketMN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MCCCTermGradeBucketMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMCCCTermGradeBucketMN <- function(MCCCTermGradeBucketMNID, GradeBucketID = NULL, MCCCTermImportID = NULL, SectionLengthID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MCCCTermGradeBucketMN", objectId = MCCCTermGradeBucketMNID, body = list(DataObject = body), searchFields = append("MCCCTermGradeBucketMNID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingCourses
	#'
	#' This function returns a dataframe or json object of SchedulingCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingCourses <- function(searchConditionsList = NULL, AcademicMinutes = F, ActiveSections = F, ActiveSectionsOpen = F, ActivityID = F, AllowTeachersToAddAssignments = F, CanBeOfferedToAnotherEntity = F, Category = F, CodeDescription = F, CourseCloned = F, CourseCode = F, CourseFeesList = F, CourseGradeLevelCodes = F, CourseGroupDescriptions = F, CourseID = F, CourseIDClonedFrom = F, CourseIDClonedTo = F, CourseLengthID = F, CourseMNID = F, CourseSubjectID = F, CourseTypeID = F, CreatedTime = F, CurriculumID = F, DepartmentID = F, Description = F, EarnedCredits = F, EdFiCourseLevelCharacteristicID = F, EdFiSubjectTypeID = F, EntityGroupKey = F, EntityID = F, EstimatedNumberOfSections = F, ExcludeFromSTAR = F, ExcludeFromStudentSectionLink = F, GradeCourse = F, GradeLevelIDSummary = F, GradeLevelSummary = F, GradingPeriodSetID = F, HasAttachedStandards = F, HasCourseRequestsInCommonWithCourse = F, HasNonAlternateStudentCourseRequests = F, HasOfferedCourseEntity = F, HasStudentCourseRequests = F, HasSubjects = F, HideFromArenaScheduling = F, HideFromRequestEntry = F, IsActive = F, IsActiveForEntity = F, IsAHistoricRecord = F, IsCoreAcademic = F, IsCurrentSchoolYear = F, IsDirectPay = F, IsHonors = F, IsOffered = F, IsRepeatable = F, IsRequired = F, IsRequiredOverride = F, IsWritingEmphasis = F, KeepAttendance = F, LengthOfPeriod = F, LockCourseFromSectionScheduler = F, MaximumPercentageOfSectionsInSingleDisplayPeriod = F, MaxRequestedCount = F, ModifiedTime = F, NumberOfAlternateCourseRequests = F, NumberOfCourseRequests = F, NumberOfCourseRequestsFemales = F, NumberOfCourseRequestsInCommonWithCourse = F, NumberOfCourseRequestsMales = F, NumberOfSeatsAvailable = F, NumberOfSeatsRemaining = F, NumberOfTransferStudentSections = F, OfferingEntity = F, OverrideStudentSectionLinkByCourse = F, PeriodsPerWeek = F, PreventDrop = F, PreventMultipleSectionsUsingSingleDisplayPeriod = F, RequestLimitPerStudent = F, Required = F, SchedulingPriority = F, SchedulingPriorityCodeOverride = F, SchedulingPriorityOverrideCode = F, SchedulingTeamMode = F, SchedulingType = F, SchedulingTypeCodeOverride = F, SchedulingTypeOverrideCode = F, SchoolYearID = F, SectionSchedulerManualProcessingOrder = F, SequenceLimit = F, SequenceNumber = F, SpecificStudentRequests = F, StateCarlPerkinsProgramCodeMNID = F, StateSTARAssignmentCodeMNID = F, StateSTARGradeLevelMNID = F, SumTotalActiveSectionsOptimalStudentCount = F, TotalEntitiesOfferedTo = F, TotalSectionCount = F, TotalStudentCourseRequestSectionLengthSubsetCount = F, TotalStudentSectionCount = F, UseRequiredOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UseSchedulingPriorityOverride = F, UseSchedulingTypeOverride = F, ViewingFromOfferingEntity = F, Website = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "Course", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingCourse
	#'
	#' This function returns a dataframe or json object of a SchedulingCourse
	#' @param SchedulingCourseID The ID of the SchedulingCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingCourse <- function(SchedulingCourseID, AcademicMinutes = F, ActiveSections = F, ActiveSectionsOpen = F, ActivityID = F, AllowTeachersToAddAssignments = F, CanBeOfferedToAnotherEntity = F, Category = F, CodeDescription = F, CourseCloned = F, CourseCode = F, CourseFeesList = F, CourseGradeLevelCodes = F, CourseGroupDescriptions = F, CourseID = F, CourseIDClonedFrom = F, CourseIDClonedTo = F, CourseLengthID = F, CourseMNID = F, CourseSubjectID = F, CourseTypeID = F, CreatedTime = F, CurriculumID = F, DepartmentID = F, Description = F, EarnedCredits = F, EdFiCourseLevelCharacteristicID = F, EdFiSubjectTypeID = F, EntityGroupKey = F, EntityID = F, EstimatedNumberOfSections = F, ExcludeFromSTAR = F, ExcludeFromStudentSectionLink = F, GradeCourse = F, GradeLevelIDSummary = F, GradeLevelSummary = F, GradingPeriodSetID = F, HasAttachedStandards = F, HasCourseRequestsInCommonWithCourse = F, HasNonAlternateStudentCourseRequests = F, HasOfferedCourseEntity = F, HasStudentCourseRequests = F, HasSubjects = F, HideFromArenaScheduling = F, HideFromRequestEntry = F, IsActive = F, IsActiveForEntity = F, IsAHistoricRecord = F, IsCoreAcademic = F, IsCurrentSchoolYear = F, IsDirectPay = F, IsHonors = F, IsOffered = F, IsRepeatable = F, IsRequired = F, IsRequiredOverride = F, IsWritingEmphasis = F, KeepAttendance = F, LengthOfPeriod = F, LockCourseFromSectionScheduler = F, MaximumPercentageOfSectionsInSingleDisplayPeriod = F, MaxRequestedCount = F, ModifiedTime = F, NumberOfAlternateCourseRequests = F, NumberOfCourseRequests = F, NumberOfCourseRequestsFemales = F, NumberOfCourseRequestsInCommonWithCourse = F, NumberOfCourseRequestsMales = F, NumberOfSeatsAvailable = F, NumberOfSeatsRemaining = F, NumberOfTransferStudentSections = F, OfferingEntity = F, OverrideStudentSectionLinkByCourse = F, PeriodsPerWeek = F, PreventDrop = F, PreventMultipleSectionsUsingSingleDisplayPeriod = F, RequestLimitPerStudent = F, Required = F, SchedulingPriority = F, SchedulingPriorityCodeOverride = F, SchedulingPriorityOverrideCode = F, SchedulingTeamMode = F, SchedulingType = F, SchedulingTypeCodeOverride = F, SchedulingTypeOverrideCode = F, SchoolYearID = F, SectionSchedulerManualProcessingOrder = F, SequenceLimit = F, SequenceNumber = F, SpecificStudentRequests = F, StateCarlPerkinsProgramCodeMNID = F, StateSTARAssignmentCodeMNID = F, StateSTARGradeLevelMNID = F, SumTotalActiveSectionsOptimalStudentCount = F, TotalEntitiesOfferedTo = F, TotalSectionCount = F, TotalStudentCourseRequestSectionLengthSubsetCount = F, TotalStudentSectionCount = F, UseRequiredOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UseSchedulingPriorityOverride = F, UseSchedulingTypeOverride = F, ViewingFromOfferingEntity = F, Website = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "Course", objectId = SchedulingCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingCourse
	#'
	#' This function deletes a SchedulingCourse
	#' @param SchedulingCourseID The ID of the SchedulingCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingCourseID of the deleted SchedulingCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingCourse <- function(SchedulingCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "Course", objectId = SchedulingCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingCourse
	#'
	#' This function creates a SchedulingCourse
	#' @param fieldNames The field values to give the created SchedulingCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingCourse <- function(AcademicMinutes = NULL, ActivityID = NULL, AllowTeachersToAddAssignments = NULL, Category = NULL, CourseCode = NULL, CourseIDClonedFrom = NULL, CourseLengthID = NULL, CourseSubjectID = NULL, CourseTypeID = NULL, CurriculumID = NULL, DepartmentID = NULL, Description = NULL, EarnedCredits = NULL, EdFiCourseLevelCharacteristicID = NULL, EdFiSubjectTypeID = NULL, EntityGroupKey = NULL, EntityID = NULL, EstimatedNumberOfSections = NULL, ExcludeFromSTAR = NULL, ExcludeFromStudentSectionLink = NULL, GradeCourse = NULL, GradeLevelSummary = NULL, GradingPeriodSetID = NULL, HideFromArenaScheduling = NULL, HideFromRequestEntry = NULL, IsDirectPay = NULL, IsHonors = NULL, IsRepeatable = NULL, IsRequired = NULL, KeepAttendance = NULL, LengthOfPeriod = NULL, LockCourseFromSectionScheduler = NULL, OverrideStudentSectionLinkByCourse = NULL, PeriodsPerWeek = NULL, RequestLimitPerStudent = NULL, SchedulingPriority = NULL, SchedulingTeamMode = NULL, SchedulingType = NULL, SchoolYearID = NULL, SectionSchedulerManualProcessingOrder = NULL, SequenceLimit = NULL, SequenceNumber = NULL, StateCarlPerkinsProgramCodeMNID = NULL, StateSTARAssignmentCodeMNID = NULL, StateSTARGradeLevelMNID = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "Course", body = list(DataObject = body), searchFields = append("CourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingCourse
	#'
	#' This function modifies a SchedulingCourse
	#' @param fieldNames The field values to give the modified SchedulingCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingCourse <- function(CourseID, AcademicMinutes = NULL, ActivityID = NULL, AllowTeachersToAddAssignments = NULL, Category = NULL, CourseCode = NULL, CourseIDClonedFrom = NULL, CourseLengthID = NULL, CourseSubjectID = NULL, CourseTypeID = NULL, CurriculumID = NULL, DepartmentID = NULL, Description = NULL, EarnedCredits = NULL, EdFiCourseLevelCharacteristicID = NULL, EdFiSubjectTypeID = NULL, EntityGroupKey = NULL, EntityID = NULL, EstimatedNumberOfSections = NULL, ExcludeFromSTAR = NULL, ExcludeFromStudentSectionLink = NULL, GradeCourse = NULL, GradeLevelSummary = NULL, GradingPeriodSetID = NULL, HideFromArenaScheduling = NULL, HideFromRequestEntry = NULL, IsDirectPay = NULL, IsHonors = NULL, IsRepeatable = NULL, IsRequired = NULL, KeepAttendance = NULL, LengthOfPeriod = NULL, LockCourseFromSectionScheduler = NULL, OverrideStudentSectionLinkByCourse = NULL, PeriodsPerWeek = NULL, RequestLimitPerStudent = NULL, SchedulingPriority = NULL, SchedulingTeamMode = NULL, SchedulingType = NULL, SchoolYearID = NULL, SectionSchedulerManualProcessingOrder = NULL, SequenceLimit = NULL, SequenceNumber = NULL, StateCarlPerkinsProgramCodeMNID = NULL, StateSTARAssignmentCodeMNID = NULL, StateSTARGradeLevelMNID = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "Course", objectId = CourseID, body = list(DataObject = body), searchFields = append("CourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionLengthSubsets
	#'
	#' This function returns a dataframe or json object of SectionLengthSubsets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionLengthSubsets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionLengthSubsets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionLengthSubset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionLengthSubsets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionLengthSubsets <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EdFiTermDescriptorID = F, EdFiTermTypeID = F, EndDate = F, EntityGroupKey = F, GradeBucketIDCarlPerkins = F, IsFullSectionLength = F, MCCCDropDate = F, MCCCTermImportID = F, ModifiedTime = F, SectionLengthID = F, SectionLengthSubsetID = F, SectionLengthSubsetIDClonedFrom = F, SectionLengthSubsetIDClonedTo = F, SectionLengthSubsetMNID = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionLengthSubset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionLengthSubset
	#'
	#' This function returns a dataframe or json object of a SectionLengthSubset
	#' @param SectionLengthSubsetID The ID of the SectionLengthSubset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionLengthSubset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionLengthSubset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionLengthSubset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionLengthSubset <- function(SectionLengthSubsetID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EdFiTermDescriptorID = F, EdFiTermTypeID = F, EndDate = F, EntityGroupKey = F, GradeBucketIDCarlPerkins = F, IsFullSectionLength = F, MCCCDropDate = F, MCCCTermImportID = F, ModifiedTime = F, SectionLengthID = F, SectionLengthSubsetIDClonedFrom = F, SectionLengthSubsetIDClonedTo = F, SectionLengthSubsetMNID = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionLengthSubsetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionLengthSubset", objectId = SectionLengthSubsetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionLengthSubset
	#'
	#' This function deletes a SectionLengthSubset
	#' @param SectionLengthSubsetID The ID of the SectionLengthSubset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionLengthSubsetID of the deleted SectionLengthSubset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionLengthSubset <- function(SectionLengthSubsetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionLengthSubset", objectId = SectionLengthSubsetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionLengthSubset
	#'
	#' This function creates a SectionLengthSubset
	#' @param fieldNames The field values to give the created SectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionLengthSubset <- function(Code = NULL, Description = NULL, EdFiTermDescriptorID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityGroupKey = NULL, GradeBucketIDCarlPerkins = NULL, IsFullSectionLength = NULL, MCCCDropDate = NULL, MCCCTermImportID = NULL, SectionLengthID = NULL, SectionLengthSubsetIDClonedFrom = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionLengthSubset", body = list(DataObject = body), searchFields = append("SectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionLengthSubset
	#'
	#' This function modifies a SectionLengthSubset
	#' @param fieldNames The field values to give the modified SectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionLengthSubset <- function(SectionLengthSubsetID, Code = NULL, Description = NULL, EdFiTermDescriptorID = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityGroupKey = NULL, GradeBucketIDCarlPerkins = NULL, IsFullSectionLength = NULL, MCCCDropDate = NULL, MCCCTermImportID = NULL, SectionLengthID = NULL, SectionLengthSubsetIDClonedFrom = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionLengthSubset", objectId = SectionLengthSubsetID, body = list(DataObject = body), searchFields = append("SectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionLengths
	#'
	#' This function returns a dataframe or json object of SectionLengths
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionLengths. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionLengths.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionLength') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionLengths
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionLengths <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseLengthID = F, CreatedTime = F, Description = F, EdFiTermTypeID = F, EndDate = F, EntityGroupKey = F, IsSectionCurrent = F, MCCCDropDate = F, ModifiedTime = F, SectionLengthID = F, SectionLengthIDClonedFrom = F, SectionLengthIDClonedTo = F, SectionLengthMNID = F, SectionRange = F, StartDate = F, ThirdPartyTermNumber = F, ThirdPartyTermTypeOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionLength", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionLength
	#'
	#' This function returns a dataframe or json object of a SectionLength
	#' @param SectionLengthID The ID of the SectionLength to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionLength. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionLength.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionLength') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionLength <- function(SectionLengthID, Code = F, CodeDescription = F, CourseLengthID = F, CreatedTime = F, Description = F, EdFiTermTypeID = F, EndDate = F, EntityGroupKey = F, IsSectionCurrent = F, MCCCDropDate = F, ModifiedTime = F, SectionLengthIDClonedFrom = F, SectionLengthIDClonedTo = F, SectionLengthMNID = F, SectionRange = F, StartDate = F, ThirdPartyTermNumber = F, ThirdPartyTermTypeOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionLengthID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionLength", objectId = SectionLengthID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionLength
	#'
	#' This function deletes a SectionLength
	#' @param SectionLengthID The ID of the SectionLength to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionLengthID of the deleted SectionLength.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionLength <- function(SectionLengthID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionLength", objectId = SectionLengthID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionLength
	#'
	#' This function creates a SectionLength
	#' @param fieldNames The field values to give the created SectionLength. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionLength <- function(Code = NULL, CourseLengthID = NULL, Description = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityGroupKey = NULL, MCCCDropDate = NULL, SectionLengthIDClonedFrom = NULL, StartDate = NULL, ThirdPartyTermNumber = NULL, ThirdPartyTermTypeOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionLength", body = list(DataObject = body), searchFields = append("SectionLengthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionLength
	#'
	#' This function modifies a SectionLength
	#' @param fieldNames The field values to give the modified SectionLength. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionLength <- function(SectionLengthID, Code = NULL, CourseLengthID = NULL, Description = NULL, EdFiTermTypeID = NULL, EndDate = NULL, EntityGroupKey = NULL, MCCCDropDate = NULL, SectionLengthIDClonedFrom = NULL, StartDate = NULL, ThirdPartyTermNumber = NULL, ThirdPartyTermTypeOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionLength", objectId = SectionLengthID, body = list(DataObject = body), searchFields = append("SectionLengthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Sections
	#'
	#' This function returns a dataframe or json object of Sections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Sections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Sections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Section') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of Sections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSections <- function(searchConditionsList = NULL, AllowCECE = F, AllowStudentsWithoutCategoryToBeAssigned = F, AssignmentCount = F, AssignmentDataString = F, AssignmentsHaveBeenCreated = F, BellScheduleGroupID = F, CalculatedBellScheduleGroupID = F, CalendarIDMCCCOverride = F, CanAddStudentSection = F, CanBeOffered = F, Code = F, CourseCodeSectionCode = F, CourseCodeSectionCodeCourseDescription = F, CourseDescriptionCodeSectionCode = F, CourseID = F, CreatedTime = F, CurrentEnrollmentEntityForFilter = F, CurrentEnrollmentForFilter = F, CurrentGradingPeriod = F, CurrentlyRecalculating = F, DisplayForTeachers = F, DisplayPeriodIDCurrentStoredPrimary = F, DueDateOfLastAssignmentScored = F, EdFiEducationalEnvironmentID = F, EffectiveTeacherFirstLastName = F, EffectiveTeacherLastFirstName = F, EntityCodeTeacherNumber = F, EntityID = F, ExcludeFromStudentSectionLink = F, ExcusedAbsencesForTerm = F, ExcusedAbsencesYTD = F, FitCode = F, GradebookCanHaveSettingsCopiedFromPreviousYear = F, HasAssignments = F, HasAssignmentsWithAcademicStandards = F, HasAssignmentsWithStudentGroups = F, HasAssignmentsWithSubjects = F, HasCalculationGroupCourse = F, HasCategoriesInDistrict = F, HasCompletedForFilter = F, HasDeletedStudentAssignments = F, HasGradeMarksInEntity = F, HasGradingPeriodGradeBuckets = F, HasGradingScales = F, HasIncompleteClosedGradeChangeRequests = F, HasNonDeletedAssignments = F, HasNotStartedForFilter = F, HasPreviousYearSettings = F, HasStandardsCheckSectionLengthGradingPeriodSet = F, HasStudentSections = F, HasSubjectsCheckSectionLengthGradingPeriodSet = F, HasValidGradebookSetup = F, HasValidStandardsSetup = F, HasValidSubjectsSetup = F, HomeroomID = F, InPastYear = F, IsActive = F, IsActiveOverride = F, IsAdministeredForTSA = F, IsAHistoricRecord = F, IsBilingual = F, IsCreditRecovery = F, IsInProgressForFilter = F, IsOffered = F, IsOfferedToAnotherEntity = F, IsSelfPaced = F, IsSelfPacedAndActive = F, IsTSAProficient = F, LanguageID = F, LockSectionFromSectionScheduler = F, LockSectionLengthFromSectionScheduler = F, MaximumStudentCount = F, MaximumStudentCountEntityForFilter = F, MaximumStudentCountOfferedForFilter = F, MaximumStudentCountOfferedToSpecifiedEntity = F, MeetIDCurrentStoredPrimary = F, MeetSummaryIDCurrentStoredPrimary = F, MinimumStudentCount = F, MissingAssignmentCount = F, ModifiedTime = F, NonGradedAssignmentCountForTerm = F, NonGradedAssignmentCountNoStudentAssignmentsForTerm = F, NonTransferCourseEnrollmentCountFemales = F, NonTransferCourseEnrollmentCountFemalesEntity = F, NonTransferCourseEnrollmentCountFemalesEntityFirstDay = F, NonTransferCourseEnrollmentCountFemalesEntitySpecifiedDate = F, NonTransferCourseEnrollmentCountFemalesEntityToday = F, NonTransferCourseEnrollmentCountFemalesFirstDay = F, NonTransferCourseEnrollmentCountFemalesSpecifiedDate = F, NonTransferCourseEnrollmentCountFemalesToday = F, NonTransferCourseEnrollmentCountMales = F, NonTransferCourseEnrollmentCountMalesEntity = F, NonTransferCourseEnrollmentCountMalesEntityFirstDay = F, NonTransferCourseEnrollmentCountMalesEntitySpecifiedDate = F, NonTransferCourseEnrollmentCountMalesEntityToday = F, NonTransferCourseEnrollmentCountMalesFirstDay = F, NonTransferCourseEnrollmentCountMalesSpecifiedDate = F, NonTransferCourseEnrollmentCountMalesToday = F, NonTransferCourseStudentEnrollmentCount = F, NonTransferCourseStudentEnrollmentCountEntity = F, NumberOfTransferStudentSectionsForFilter = F, OffenseCount = F, OffenseCountYTD = F, OptimalStudentCount = F, OtherAbsencesForTerm = F, OtherAbsencesYTD = F, PreviousVersionOfFitCode = F, ProgressStatusSpecifiedDateCode = F, ProgressStatusTodayCode = F, RecalculateGradebook = F, RecalculateGradebookAdmin = F, ReservedSeatCount = F, RoomSeatsAvailable = F, SchedulingCategories = F, SchedulingTeams = F, SchoolYearID = F, ScoredAssignmentCount = F, ScoredAssignmentRange0CurrentTerm = F, ScoredAssignmentRange100to90CurrentTerm = F, ScoredAssignmentRange49to1CurrentTerm = F, ScoredAssignmentRange59to50CurrentTerm = F, ScoredAssignmentRange69to60CurrentTerm = F, ScoredAssignmentRange79to70CurrentTerm = F, ScoredAssignmentRange89to80CurrentTerm = F, SeatsAvailableEntityForFilter = F, SeatsAvailableForFilter = F, SectionEnrollmentTotalsForSectionLengthSubsetSummary = F, SectionEnrollmentTotalsForSectionLengthSubsetSummaryForEntity = F, SectionID = F, SectionIDClonedFrom = F, SectionIDClonedTo = F, SectionLengthID = F, SectionLengthScheduledBySectionScheduler = F, SectionMNID = F, SelfPacedEndTime = F, SingleSex = F, SpecialEdPercentageLimit = F, SpecifiedPeriodDaySummary = F, StaffIDCurrentStoredPrimary = F, StaffMeetIDCurrentStoredPrimary = F, StateInstructionalMethodCodeMNID = F, StateSTARModeOfTeachingMNID = F, StudentAssignmentDataString = F, StudentCountForTerm = F, StudentEnrollmentAsOfSpecifiedDateForFilter = F, StudentEnrollmentEntityForFilter = F, StudentEnrollmentFemalesAsOfSpecifiedDateForFilter = F, StudentEnrollmentFemalesEntityForFilter = F, StudentEnrollmentFemalesForFilter = F, StudentEnrollmentForFilter = F, StudentEnrollmentMalesAsOfSpecifiedDateForFilter = F, StudentEnrollmentMalesEntityForFilter = F, StudentEnrollmentMalesForFilter = F, TardiesForTerm = F, TardiesYTD = F, ThisPeriodDaySummary = F, TotalEnrollmentCountEntityForFilter = F, TotalEnrollmentCountForFilter = F, TotalMeetCount = F, TotalSeatsOfferedToOtherEntities = F, TransferCourseEnrollmentCountFemales = F, TransferCourseEnrollmentCountFemalesEntity = F, TransferCourseEnrollmentCountFemalesEntityFirstDay = F, TransferCourseEnrollmentCountFemalesEntitySpecifiedDate = F, TransferCourseEnrollmentCountFemalesEntityToday = F, TransferCourseEnrollmentCountFemalesFirstDay = F, TransferCourseEnrollmentCountFemalesSpecifiedDate = F, TransferCourseEnrollmentCountFemalesToday = F, TransferCourseEnrollmentCountMales = F, TransferCourseEnrollmentCountMalesEntity = F, TransferCourseEnrollmentCountMalesEntityFirstDay = F, TransferCourseEnrollmentCountMalesEntitySpecifiedDate = F, TransferCourseEnrollmentCountMalesEntityToday = F, TransferCourseEnrollmentCountMalesFirstDay = F, TransferCourseEnrollmentCountMalesSpecifiedDate = F, TransferCourseEnrollmentCountMalesToday = F, UnexcusedAbsencesForTerm = F, UnexcusedAbsencesYTD = F, UnscoredAssignmentCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UsingCurriculumSubjectsInGradebook = F, ViewingFromOfferingEntity = F, Website = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "Section", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Section
	#'
	#' This function returns a dataframe or json object of a Section
	#' @param SectionID The ID of the Section to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Section. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Section.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Section') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of Section
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSection <- function(SectionID, AllowCECE = F, AllowStudentsWithoutCategoryToBeAssigned = F, AssignmentCount = F, AssignmentDataString = F, AssignmentsHaveBeenCreated = F, BellScheduleGroupID = F, CalculatedBellScheduleGroupID = F, CalendarIDMCCCOverride = F, CanAddStudentSection = F, CanBeOffered = F, Code = F, CourseCodeSectionCode = F, CourseCodeSectionCodeCourseDescription = F, CourseDescriptionCodeSectionCode = F, CourseID = F, CreatedTime = F, CurrentEnrollmentEntityForFilter = F, CurrentEnrollmentForFilter = F, CurrentGradingPeriod = F, CurrentlyRecalculating = F, DisplayForTeachers = F, DisplayPeriodIDCurrentStoredPrimary = F, DueDateOfLastAssignmentScored = F, EdFiEducationalEnvironmentID = F, EffectiveTeacherFirstLastName = F, EffectiveTeacherLastFirstName = F, EntityCodeTeacherNumber = F, EntityID = F, ExcludeFromStudentSectionLink = F, ExcusedAbsencesForTerm = F, ExcusedAbsencesYTD = F, FitCode = F, GradebookCanHaveSettingsCopiedFromPreviousYear = F, HasAssignments = F, HasAssignmentsWithAcademicStandards = F, HasAssignmentsWithStudentGroups = F, HasAssignmentsWithSubjects = F, HasCalculationGroupCourse = F, HasCategoriesInDistrict = F, HasCompletedForFilter = F, HasDeletedStudentAssignments = F, HasGradeMarksInEntity = F, HasGradingPeriodGradeBuckets = F, HasGradingScales = F, HasIncompleteClosedGradeChangeRequests = F, HasNonDeletedAssignments = F, HasNotStartedForFilter = F, HasPreviousYearSettings = F, HasStandardsCheckSectionLengthGradingPeriodSet = F, HasStudentSections = F, HasSubjectsCheckSectionLengthGradingPeriodSet = F, HasValidGradebookSetup = F, HasValidStandardsSetup = F, HasValidSubjectsSetup = F, HomeroomID = F, InPastYear = F, IsActive = F, IsActiveOverride = F, IsAdministeredForTSA = F, IsAHistoricRecord = F, IsBilingual = F, IsCreditRecovery = F, IsInProgressForFilter = F, IsOffered = F, IsOfferedToAnotherEntity = F, IsSelfPaced = F, IsSelfPacedAndActive = F, IsTSAProficient = F, LanguageID = F, LockSectionFromSectionScheduler = F, LockSectionLengthFromSectionScheduler = F, MaximumStudentCount = F, MaximumStudentCountEntityForFilter = F, MaximumStudentCountOfferedForFilter = F, MaximumStudentCountOfferedToSpecifiedEntity = F, MeetIDCurrentStoredPrimary = F, MeetSummaryIDCurrentStoredPrimary = F, MinimumStudentCount = F, MissingAssignmentCount = F, ModifiedTime = F, NonGradedAssignmentCountForTerm = F, NonGradedAssignmentCountNoStudentAssignmentsForTerm = F, NonTransferCourseEnrollmentCountFemales = F, NonTransferCourseEnrollmentCountFemalesEntity = F, NonTransferCourseEnrollmentCountFemalesEntityFirstDay = F, NonTransferCourseEnrollmentCountFemalesEntitySpecifiedDate = F, NonTransferCourseEnrollmentCountFemalesEntityToday = F, NonTransferCourseEnrollmentCountFemalesFirstDay = F, NonTransferCourseEnrollmentCountFemalesSpecifiedDate = F, NonTransferCourseEnrollmentCountFemalesToday = F, NonTransferCourseEnrollmentCountMales = F, NonTransferCourseEnrollmentCountMalesEntity = F, NonTransferCourseEnrollmentCountMalesEntityFirstDay = F, NonTransferCourseEnrollmentCountMalesEntitySpecifiedDate = F, NonTransferCourseEnrollmentCountMalesEntityToday = F, NonTransferCourseEnrollmentCountMalesFirstDay = F, NonTransferCourseEnrollmentCountMalesSpecifiedDate = F, NonTransferCourseEnrollmentCountMalesToday = F, NonTransferCourseStudentEnrollmentCount = F, NonTransferCourseStudentEnrollmentCountEntity = F, NumberOfTransferStudentSectionsForFilter = F, OffenseCount = F, OffenseCountYTD = F, OptimalStudentCount = F, OtherAbsencesForTerm = F, OtherAbsencesYTD = F, PreviousVersionOfFitCode = F, ProgressStatusSpecifiedDateCode = F, ProgressStatusTodayCode = F, RecalculateGradebook = F, RecalculateGradebookAdmin = F, ReservedSeatCount = F, RoomSeatsAvailable = F, SchedulingCategories = F, SchedulingTeams = F, SchoolYearID = F, ScoredAssignmentCount = F, ScoredAssignmentRange0CurrentTerm = F, ScoredAssignmentRange100to90CurrentTerm = F, ScoredAssignmentRange49to1CurrentTerm = F, ScoredAssignmentRange59to50CurrentTerm = F, ScoredAssignmentRange69to60CurrentTerm = F, ScoredAssignmentRange79to70CurrentTerm = F, ScoredAssignmentRange89to80CurrentTerm = F, SeatsAvailableEntityForFilter = F, SeatsAvailableForFilter = F, SectionEnrollmentTotalsForSectionLengthSubsetSummary = F, SectionEnrollmentTotalsForSectionLengthSubsetSummaryForEntity = F, SectionIDClonedFrom = F, SectionIDClonedTo = F, SectionLengthID = F, SectionLengthScheduledBySectionScheduler = F, SectionMNID = F, SelfPacedEndTime = F, SingleSex = F, SpecialEdPercentageLimit = F, SpecifiedPeriodDaySummary = F, StaffIDCurrentStoredPrimary = F, StaffMeetIDCurrentStoredPrimary = F, StateInstructionalMethodCodeMNID = F, StateSTARModeOfTeachingMNID = F, StudentAssignmentDataString = F, StudentCountForTerm = F, StudentEnrollmentAsOfSpecifiedDateForFilter = F, StudentEnrollmentEntityForFilter = F, StudentEnrollmentFemalesAsOfSpecifiedDateForFilter = F, StudentEnrollmentFemalesEntityForFilter = F, StudentEnrollmentFemalesForFilter = F, StudentEnrollmentForFilter = F, StudentEnrollmentMalesAsOfSpecifiedDateForFilter = F, StudentEnrollmentMalesEntityForFilter = F, StudentEnrollmentMalesForFilter = F, TardiesForTerm = F, TardiesYTD = F, ThisPeriodDaySummary = F, TotalEnrollmentCountEntityForFilter = F, TotalEnrollmentCountForFilter = F, TotalMeetCount = F, TotalSeatsOfferedToOtherEntities = F, TransferCourseEnrollmentCountFemales = F, TransferCourseEnrollmentCountFemalesEntity = F, TransferCourseEnrollmentCountFemalesEntityFirstDay = F, TransferCourseEnrollmentCountFemalesEntitySpecifiedDate = F, TransferCourseEnrollmentCountFemalesEntityToday = F, TransferCourseEnrollmentCountFemalesFirstDay = F, TransferCourseEnrollmentCountFemalesSpecifiedDate = F, TransferCourseEnrollmentCountFemalesToday = F, TransferCourseEnrollmentCountMales = F, TransferCourseEnrollmentCountMalesEntity = F, TransferCourseEnrollmentCountMalesEntityFirstDay = F, TransferCourseEnrollmentCountMalesEntitySpecifiedDate = F, TransferCourseEnrollmentCountMalesEntityToday = F, TransferCourseEnrollmentCountMalesFirstDay = F, TransferCourseEnrollmentCountMalesSpecifiedDate = F, TransferCourseEnrollmentCountMalesToday = F, UnexcusedAbsencesForTerm = F, UnexcusedAbsencesYTD = F, UnscoredAssignmentCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UsingCurriculumSubjectsInGradebook = F, ViewingFromOfferingEntity = F, Website = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "Section", objectId = SectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Section
	#'
	#' This function deletes a Section
	#' @param SectionID The ID of the Section to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionID of the deleted Section.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSection <- function(SectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "Section", objectId = SectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Section
	#'
	#' This function creates a Section
	#' @param fieldNames The field values to give the created Section. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created Section
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSection <- function(AllowCECE = NULL, AllowStudentsWithoutCategoryToBeAssigned = NULL, BellScheduleGroupID = NULL, CalendarIDMCCCOverride = NULL, Code = NULL, CourseID = NULL, CurrentlyRecalculating = NULL, DisplayPeriodIDCurrentStoredPrimary = NULL, EdFiEducationalEnvironmentID = NULL, EntityID = NULL, ExcludeFromStudentSectionLink = NULL, HomeroomID = NULL, IsActive = NULL, IsAdministeredForTSA = NULL, IsBilingual = NULL, IsCreditRecovery = NULL, IsSelfPaced = NULL, IsTSAProficient = NULL, LanguageID = NULL, LockSectionFromSectionScheduler = NULL, LockSectionLengthFromSectionScheduler = NULL, MaximumStudentCount = NULL, MeetIDCurrentStoredPrimary = NULL, MeetSummaryIDCurrentStoredPrimary = NULL, MinimumStudentCount = NULL, OptimalStudentCount = NULL, RecalculateGradebook = NULL, RecalculateGradebookAdmin = NULL, ReservedSeatCount = NULL, SchedulingCategories = NULL, SchedulingTeams = NULL, SchoolYearID = NULL, SectionIDClonedFrom = NULL, SectionLengthID = NULL, SectionLengthScheduledBySectionScheduler = NULL, SelfPacedEndTime = NULL, SingleSex = NULL, StaffIDCurrentStoredPrimary = NULL, StaffMeetIDCurrentStoredPrimary = NULL, StateInstructionalMethodCodeMNID = NULL, StateSTARModeOfTeachingMNID = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "Section", body = list(DataObject = body), searchFields = append("SectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Section
	#'
	#' This function modifies a Section
	#' @param fieldNames The field values to give the modified Section. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified Section
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySection <- function(SectionID, AllowCECE = NULL, AllowStudentsWithoutCategoryToBeAssigned = NULL, BellScheduleGroupID = NULL, CalendarIDMCCCOverride = NULL, Code = NULL, CourseID = NULL, CurrentlyRecalculating = NULL, DisplayPeriodIDCurrentStoredPrimary = NULL, EdFiEducationalEnvironmentID = NULL, EntityID = NULL, ExcludeFromStudentSectionLink = NULL, HomeroomID = NULL, IsActive = NULL, IsAdministeredForTSA = NULL, IsBilingual = NULL, IsCreditRecovery = NULL, IsSelfPaced = NULL, IsTSAProficient = NULL, LanguageID = NULL, LockSectionFromSectionScheduler = NULL, LockSectionLengthFromSectionScheduler = NULL, MaximumStudentCount = NULL, MeetIDCurrentStoredPrimary = NULL, MeetSummaryIDCurrentStoredPrimary = NULL, MinimumStudentCount = NULL, OptimalStudentCount = NULL, RecalculateGradebook = NULL, RecalculateGradebookAdmin = NULL, ReservedSeatCount = NULL, SchedulingCategories = NULL, SchedulingTeams = NULL, SchoolYearID = NULL, SectionIDClonedFrom = NULL, SectionLengthID = NULL, SectionLengthScheduledBySectionScheduler = NULL, SelfPacedEndTime = NULL, SingleSex = NULL, StaffIDCurrentStoredPrimary = NULL, StaffMeetIDCurrentStoredPrimary = NULL, StateInstructionalMethodCodeMNID = NULL, StateSTARModeOfTeachingMNID = NULL, Website = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "Section", objectId = SectionID, body = list(DataObject = body), searchFields = append("SectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedSectionLengthSubsets
	#'
	#' This function returns a dataframe or json object of TempFailedSectionLengthSubsets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedSectionLengthSubsets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedSectionLengthSubsets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedSectionLengthSubset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedSectionLengthSubsets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedSectionLengthSubsets <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseLengthCode = F, CourseLengthCodeDescription = F, CourseLengthID = F, CreatedTime = F, Description = F, EndDate = F, IsFullSectionLength = F, IsUpdated = F, ModifiedTime = F, Note = F, ObjectIsDirty = F, OriginalEndDate = F, OriginalStartDate = F, ProcessAction = F, SectionLengthCode = F, SectionLengthCodeDescription = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, TempFailedSectionLengthSubsetID = F, TempSectionLengthSubsetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedSectionLengthSubset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedSectionLengthSubset
	#'
	#' This function returns a dataframe or json object of a TempFailedSectionLengthSubset
	#' @param TempFailedSectionLengthSubsetID The ID of the TempFailedSectionLengthSubset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedSectionLengthSubset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedSectionLengthSubset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedSectionLengthSubset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedSectionLengthSubset <- function(TempFailedSectionLengthSubsetID, Code = F, CodeDescription = F, CourseLengthCode = F, CourseLengthCodeDescription = F, CourseLengthID = F, CreatedTime = F, Description = F, EndDate = F, IsFullSectionLength = F, IsUpdated = F, ModifiedTime = F, Note = F, ObjectIsDirty = F, OriginalEndDate = F, OriginalStartDate = F, ProcessAction = F, SectionLengthCode = F, SectionLengthCodeDescription = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, TempSectionLengthSubsetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedSectionLengthSubsetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedSectionLengthSubset", objectId = TempFailedSectionLengthSubsetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedSectionLengthSubset
	#'
	#' This function deletes a TempFailedSectionLengthSubset
	#' @param TempFailedSectionLengthSubsetID The ID of the TempFailedSectionLengthSubset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedSectionLengthSubsetID of the deleted TempFailedSectionLengthSubset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedSectionLengthSubset <- function(TempFailedSectionLengthSubsetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedSectionLengthSubset", objectId = TempFailedSectionLengthSubsetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedSectionLengthSubset
	#'
	#' This function creates a TempFailedSectionLengthSubset
	#' @param fieldNames The field values to give the created TempFailedSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedSectionLengthSubset <- function(Code = NULL, CodeDescription = NULL, CourseLengthCode = NULL, CourseLengthCodeDescription = NULL, CourseLengthID = NULL, Description = NULL, EndDate = NULL, IsFullSectionLength = NULL, Note = NULL, ObjectIsDirty = NULL, OriginalEndDate = NULL, OriginalStartDate = NULL, ProcessAction = NULL, SectionLengthCode = NULL, SectionLengthCodeDescription = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, TempSectionLengthSubsetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedSectionLengthSubset", body = list(DataObject = body), searchFields = append("TempFailedSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedSectionLengthSubset
	#'
	#' This function modifies a TempFailedSectionLengthSubset
	#' @param fieldNames The field values to give the modified TempFailedSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedSectionLengthSubset <- function(TempFailedSectionLengthSubsetID, Code = NULL, CodeDescription = NULL, CourseLengthCode = NULL, CourseLengthCodeDescription = NULL, CourseLengthID = NULL, Description = NULL, EndDate = NULL, IsFullSectionLength = NULL, Note = NULL, ObjectIsDirty = NULL, OriginalEndDate = NULL, OriginalStartDate = NULL, ProcessAction = NULL, SectionLengthCode = NULL, SectionLengthCodeDescription = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, TempSectionLengthSubsetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedSectionLengthSubset", objectId = TempFailedSectionLengthSubsetID, body = list(DataObject = body), searchFields = append("TempFailedSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingGroupCourses
	#'
	#' This function returns a dataframe or json object of SchedulingGroupCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingGroupCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingGroupCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingGroupCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingGroupCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingGroupCourses <- function(searchConditionsList = NULL, CourseID = F, CreatedTime = F, ModifiedTime = F, SchedulingGroupCourseID = F, SchedulingGroupCourseIDClonedFrom = F, SchedulingGroupCourseIDClonedTo = F, SchedulingGroupID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingGroupCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingGroupCourse
	#'
	#' This function returns a dataframe or json object of a SchedulingGroupCourse
	#' @param SchedulingGroupCourseID The ID of the SchedulingGroupCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingGroupCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingGroupCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingGroupCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingGroupCourse <- function(SchedulingGroupCourseID, CourseID = F, CreatedTime = F, ModifiedTime = F, SchedulingGroupCourseIDClonedFrom = F, SchedulingGroupCourseIDClonedTo = F, SchedulingGroupID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingGroupCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingGroupCourse", objectId = SchedulingGroupCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingGroupCourse
	#'
	#' This function deletes a SchedulingGroupCourse
	#' @param SchedulingGroupCourseID The ID of the SchedulingGroupCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingGroupCourseID of the deleted SchedulingGroupCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingGroupCourse <- function(SchedulingGroupCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingGroupCourse", objectId = SchedulingGroupCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingGroupCourse
	#'
	#' This function creates a SchedulingGroupCourse
	#' @param fieldNames The field values to give the created SchedulingGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingGroupCourse <- function(CourseID = NULL, SchedulingGroupCourseIDClonedFrom = NULL, SchedulingGroupID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingGroupCourse", body = list(DataObject = body), searchFields = append("SchedulingGroupCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingGroupCourse
	#'
	#' This function modifies a SchedulingGroupCourse
	#' @param fieldNames The field values to give the modified SchedulingGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingGroupCourse <- function(SchedulingGroupCourseID, CourseID = NULL, SchedulingGroupCourseIDClonedFrom = NULL, SchedulingGroupID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingGroupCourse", objectId = SchedulingGroupCourseID, body = list(DataObject = body), searchFields = append("SchedulingGroupCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ProcessRestrictions
	#'
	#' This function returns a dataframe or json object of ProcessRestrictions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ProcessRestrictions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ProcessRestrictions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ProcessRestriction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of ProcessRestrictions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listProcessRestrictions <- function(searchConditionsList = NULL, CreatedTime = F, EntityID = F, LockCourseMaster = F, LockCourseMasterSetByMassStudentSchedulerUtility = F, LockMassStudentSchedulerUtility = F, LockMassStudentSchedulerUtilitySetByMassStudentSchedulerUtility = F, LockMassUnscheduleStudentSectionsUtility = F, LockMassUnscheduleStudentSectionsUtilitySetByMassStudentSchedulerUtility = F, LockSchedulingBoard = F, LockSchedulingBoardSetByMassStudentSchedulerUtility = F, ModifiedTime = F, ProcessRestrictionID = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDLockCourseMasterPerformer = F, UserIDLockMassStudentSchedulerUtilityPerformer = F, UserIDLockMassUnscheduleStudentSectionsUtilityPerformer = F, UserIDLockSchedulingBoardPerformer = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "ProcessRestriction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ProcessRestriction
	#'
	#' This function returns a dataframe or json object of a ProcessRestriction
	#' @param ProcessRestrictionID The ID of the ProcessRestriction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ProcessRestriction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ProcessRestriction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ProcessRestriction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of ProcessRestriction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getProcessRestriction <- function(ProcessRestrictionID, CreatedTime = F, EntityID = F, LockCourseMaster = F, LockCourseMasterSetByMassStudentSchedulerUtility = F, LockMassStudentSchedulerUtility = F, LockMassStudentSchedulerUtilitySetByMassStudentSchedulerUtility = F, LockMassUnscheduleStudentSectionsUtility = F, LockMassUnscheduleStudentSectionsUtilitySetByMassStudentSchedulerUtility = F, LockSchedulingBoard = F, LockSchedulingBoardSetByMassStudentSchedulerUtility = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDLockCourseMasterPerformer = F, UserIDLockMassStudentSchedulerUtilityPerformer = F, UserIDLockMassUnscheduleStudentSectionsUtilityPerformer = F, UserIDLockSchedulingBoardPerformer = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ProcessRestrictionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "ProcessRestriction", objectId = ProcessRestrictionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ProcessRestriction
	#'
	#' This function deletes a ProcessRestriction
	#' @param ProcessRestrictionID The ID of the ProcessRestriction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The ProcessRestrictionID of the deleted ProcessRestriction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteProcessRestriction <- function(ProcessRestrictionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "ProcessRestriction", objectId = ProcessRestrictionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ProcessRestriction
	#'
	#' This function creates a ProcessRestriction
	#' @param fieldNames The field values to give the created ProcessRestriction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created ProcessRestriction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createProcessRestriction <- function(EntityID = NULL, LockCourseMaster = NULL, LockCourseMasterSetByMassStudentSchedulerUtility = NULL, LockMassStudentSchedulerUtility = NULL, LockMassStudentSchedulerUtilitySetByMassStudentSchedulerUtility = NULL, LockMassUnscheduleStudentSectionsUtility = NULL, LockMassUnscheduleStudentSectionsUtilitySetByMassStudentSchedulerUtility = NULL, LockSchedulingBoard = NULL, LockSchedulingBoardSetByMassStudentSchedulerUtility = NULL, SchoolYearID = NULL, UserIDLockCourseMasterPerformer = NULL, UserIDLockMassStudentSchedulerUtilityPerformer = NULL, UserIDLockMassUnscheduleStudentSectionsUtilityPerformer = NULL, UserIDLockSchedulingBoardPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "ProcessRestriction", body = list(DataObject = body), searchFields = append("ProcessRestrictionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ProcessRestriction
	#'
	#' This function modifies a ProcessRestriction
	#' @param fieldNames The field values to give the modified ProcessRestriction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified ProcessRestriction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyProcessRestriction <- function(ProcessRestrictionID, EntityID = NULL, LockCourseMaster = NULL, LockCourseMasterSetByMassStudentSchedulerUtility = NULL, LockMassStudentSchedulerUtility = NULL, LockMassStudentSchedulerUtilitySetByMassStudentSchedulerUtility = NULL, LockMassUnscheduleStudentSectionsUtility = NULL, LockMassUnscheduleStudentSectionsUtilitySetByMassStudentSchedulerUtility = NULL, LockSchedulingBoard = NULL, LockSchedulingBoardSetByMassStudentSchedulerUtility = NULL, SchoolYearID = NULL, UserIDLockCourseMasterPerformer = NULL, UserIDLockMassStudentSchedulerUtilityPerformer = NULL, UserIDLockMassUnscheduleStudentSectionsUtilityPerformer = NULL, UserIDLockSchedulingBoardPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "ProcessRestriction", objectId = ProcessRestrictionID, body = list(DataObject = body), searchFields = append("ProcessRestrictionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingTeamGradeReferences
	#'
	#' This function returns a dataframe or json object of SchedulingTeamGradeReferences
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTeamGradeReferences. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTeamGradeReferences.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTeamGradeReference') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingTeamGradeReferences
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingTeamGradeReferences <- function(searchConditionsList = NULL, CreatedTime = F, GradeReferenceID = F, MaximumStudentCount = F, ModifiedTime = F, SchedulingTeamGradeReferenceID = F, SchedulingTeamGradeReferenceIDClonedFrom = F, SchedulingTeamID = F, StudentEntityYearsCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingTeamGradeReference", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingTeamGradeReference
	#'
	#' This function returns a dataframe or json object of a SchedulingTeamGradeReference
	#' @param SchedulingTeamGradeReferenceID The ID of the SchedulingTeamGradeReference to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTeamGradeReference. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTeamGradeReference.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTeamGradeReference') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingTeamGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingTeamGradeReference <- function(SchedulingTeamGradeReferenceID, CreatedTime = F, GradeReferenceID = F, MaximumStudentCount = F, ModifiedTime = F, SchedulingTeamGradeReferenceIDClonedFrom = F, SchedulingTeamID = F, StudentEntityYearsCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingTeamGradeReferenceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingTeamGradeReference", objectId = SchedulingTeamGradeReferenceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingTeamGradeReference
	#'
	#' This function deletes a SchedulingTeamGradeReference
	#' @param SchedulingTeamGradeReferenceID The ID of the SchedulingTeamGradeReference to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingTeamGradeReferenceID of the deleted SchedulingTeamGradeReference.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingTeamGradeReference <- function(SchedulingTeamGradeReferenceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingTeamGradeReference", objectId = SchedulingTeamGradeReferenceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingTeamGradeReference
	#'
	#' This function creates a SchedulingTeamGradeReference
	#' @param fieldNames The field values to give the created SchedulingTeamGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingTeamGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingTeamGradeReference <- function(GradeReferenceID = NULL, MaximumStudentCount = NULL, SchedulingTeamGradeReferenceIDClonedFrom = NULL, SchedulingTeamID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingTeamGradeReference", body = list(DataObject = body), searchFields = append("SchedulingTeamGradeReferenceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingTeamGradeReference
	#'
	#' This function modifies a SchedulingTeamGradeReference
	#' @param fieldNames The field values to give the modified SchedulingTeamGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingTeamGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingTeamGradeReference <- function(SchedulingTeamGradeReferenceID, GradeReferenceID = NULL, MaximumStudentCount = NULL, SchedulingTeamGradeReferenceIDClonedFrom = NULL, SchedulingTeamID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingTeamGradeReference", objectId = SchedulingTeamGradeReferenceID, body = list(DataObject = body), searchFields = append("SchedulingTeamGradeReferenceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedCourses
	#'
	#' This function returns a dataframe or json object of TempFailedCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedCourses <- function(searchConditionsList = NULL, ActiveSections = F, AveragePerSectionMinimumSectionsRequired = F, CodeDescription = F, CourseCode = F, CourseID = F, CourseLengthCode = F, CourseLengthID = F, CourseSubjectCode = F, CourseTypeCode = F, CreatedTime = F, CurriculumCode = F, DefaultSectionLengthID = F, Description = F, EarnedCredits = F, EntityCode = F, EstimatedNumberOfSections = F, EstimatedStudentsPerSection = F, GradeLevelSummary = F, GradingPeriodSetCode = F, GradingPeriodSetID = F, IsActive = F, MinimumSectionsRequired = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewGradingPeriodSetCode = F, NewGradingPeriodSetID = F, Note = F, NumberOfAlternateCourseRequests = F, NumberOfCourseRequests = F, NumberOfSeatsAvailable = F, NumberOfTransferStudentSections = F, ObjectIsDirty = F, ObjectName = F, OriginalEstimatedNumberOfSections = F, RecordsUpdated = F, RowIsReadOnly = F, RowIsSelected = F, TempCourseID = F, TempFailedCourseID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedCourse
	#'
	#' This function returns a dataframe or json object of a TempFailedCourse
	#' @param TempFailedCourseID The ID of the TempFailedCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedCourse <- function(TempFailedCourseID, ActiveSections = F, AveragePerSectionMinimumSectionsRequired = F, CodeDescription = F, CourseCode = F, CourseID = F, CourseLengthCode = F, CourseLengthID = F, CourseSubjectCode = F, CourseTypeCode = F, CreatedTime = F, CurriculumCode = F, DefaultSectionLengthID = F, Description = F, EarnedCredits = F, EntityCode = F, EstimatedNumberOfSections = F, EstimatedStudentsPerSection = F, GradeLevelSummary = F, GradingPeriodSetCode = F, GradingPeriodSetID = F, IsActive = F, MinimumSectionsRequired = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewGradingPeriodSetCode = F, NewGradingPeriodSetID = F, Note = F, NumberOfAlternateCourseRequests = F, NumberOfCourseRequests = F, NumberOfSeatsAvailable = F, NumberOfTransferStudentSections = F, ObjectIsDirty = F, ObjectName = F, OriginalEstimatedNumberOfSections = F, RecordsUpdated = F, RowIsReadOnly = F, RowIsSelected = F, TempCourseID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedCourse", objectId = TempFailedCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedCourse
	#'
	#' This function deletes a TempFailedCourse
	#' @param TempFailedCourseID The ID of the TempFailedCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedCourseID of the deleted TempFailedCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedCourse <- function(TempFailedCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedCourse", objectId = TempFailedCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedCourse
	#'
	#' This function creates a TempFailedCourse
	#' @param fieldNames The field values to give the created TempFailedCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedCourse <- function(ActiveSections = NULL, AveragePerSectionMinimumSectionsRequired = NULL, CodeDescription = NULL, CourseCode = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseLengthID = NULL, CourseSubjectCode = NULL, CourseTypeCode = NULL, CurriculumCode = NULL, DefaultSectionLengthID = NULL, Description = NULL, EarnedCredits = NULL, EntityCode = NULL, EstimatedNumberOfSections = NULL, EstimatedStudentsPerSection = NULL, GradeLevelSummary = NULL, GradingPeriodSetCode = NULL, GradingPeriodSetID = NULL, IsActive = NULL, MinimumSectionsRequired = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewGradingPeriodSetCode = NULL, NewGradingPeriodSetID = NULL, Note = NULL, NumberOfAlternateCourseRequests = NULL, NumberOfCourseRequests = NULL, NumberOfSeatsAvailable = NULL, NumberOfTransferStudentSections = NULL, ObjectIsDirty = NULL, ObjectName = NULL, OriginalEstimatedNumberOfSections = NULL, RecordsUpdated = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, TempCourseID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedCourse", body = list(DataObject = body), searchFields = append("TempFailedCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedCourse
	#'
	#' This function modifies a TempFailedCourse
	#' @param fieldNames The field values to give the modified TempFailedCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedCourse <- function(TempFailedCourseID, ActiveSections = NULL, AveragePerSectionMinimumSectionsRequired = NULL, CodeDescription = NULL, CourseCode = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseLengthID = NULL, CourseSubjectCode = NULL, CourseTypeCode = NULL, CurriculumCode = NULL, DefaultSectionLengthID = NULL, Description = NULL, EarnedCredits = NULL, EntityCode = NULL, EstimatedNumberOfSections = NULL, EstimatedStudentsPerSection = NULL, GradeLevelSummary = NULL, GradingPeriodSetCode = NULL, GradingPeriodSetID = NULL, IsActive = NULL, MinimumSectionsRequired = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewGradingPeriodSetCode = NULL, NewGradingPeriodSetID = NULL, Note = NULL, NumberOfAlternateCourseRequests = NULL, NumberOfCourseRequests = NULL, NumberOfSeatsAvailable = NULL, NumberOfTransferStudentSections = NULL, ObjectIsDirty = NULL, ObjectName = NULL, OriginalEstimatedNumberOfSections = NULL, RecordsUpdated = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, TempCourseID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedCourse", objectId = TempFailedCourseID, body = list(DataObject = body), searchFields = append("TempFailedCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingGroups
	#'
	#' This function returns a dataframe or json object of SchedulingGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingGroups <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityID = F, GradeReferenceID = F, HomeroomID = F, ModifiedTime = F, SchedulingGroupID = F, SchedulingGroupIDClonedFrom = F, SchedulingGroupIDClonedTo = F, SchoolYearID = F, TotalSchedulingGroupCoursesCount = F, TotalSchedulingGroupSectionsCount = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingGroup
	#'
	#' This function returns a dataframe or json object of a SchedulingGroup
	#' @param SchedulingGroupID The ID of the SchedulingGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingGroup <- function(SchedulingGroupID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityID = F, GradeReferenceID = F, HomeroomID = F, ModifiedTime = F, SchedulingGroupIDClonedFrom = F, SchedulingGroupIDClonedTo = F, SchoolYearID = F, TotalSchedulingGroupCoursesCount = F, TotalSchedulingGroupSectionsCount = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingGroup", objectId = SchedulingGroupID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingGroup
	#'
	#' This function deletes a SchedulingGroup
	#' @param SchedulingGroupID The ID of the SchedulingGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingGroupID of the deleted SchedulingGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingGroup <- function(SchedulingGroupID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingGroup", objectId = SchedulingGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingGroup
	#'
	#' This function creates a SchedulingGroup
	#' @param fieldNames The field values to give the created SchedulingGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingGroup <- function(Code = NULL, Description = NULL, EntityID = NULL, GradeReferenceID = NULL, HomeroomID = NULL, SchedulingGroupIDClonedFrom = NULL, SchoolYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingGroup", body = list(DataObject = body), searchFields = append("SchedulingGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingGroup
	#'
	#' This function modifies a SchedulingGroup
	#' @param fieldNames The field values to give the modified SchedulingGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingGroup <- function(SchedulingGroupID, Code = NULL, Description = NULL, EntityID = NULL, GradeReferenceID = NULL, HomeroomID = NULL, SchedulingGroupIDClonedFrom = NULL, SchoolYearID = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingGroup", objectId = SchedulingGroupID, body = list(DataObject = body), searchFields = append("SchedulingGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingGroupSections
	#'
	#' This function returns a dataframe or json object of SchedulingGroupSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingGroupSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingGroupSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingGroupSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingGroupSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingGroupSections <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SchedulingGroupID = F, SchedulingGroupSectionID = F, SchedulingGroupSectionIDClonedFrom = F, SchedulingGroupSectionIDClonedTo = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingGroupSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingGroupSection
	#'
	#' This function returns a dataframe or json object of a SchedulingGroupSection
	#' @param SchedulingGroupSectionID The ID of the SchedulingGroupSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingGroupSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingGroupSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingGroupSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingGroupSection <- function(SchedulingGroupSectionID, CreatedTime = F, ModifiedTime = F, SchedulingGroupID = F, SchedulingGroupSectionIDClonedFrom = F, SchedulingGroupSectionIDClonedTo = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingGroupSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingGroupSection", objectId = SchedulingGroupSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingGroupSection
	#'
	#' This function deletes a SchedulingGroupSection
	#' @param SchedulingGroupSectionID The ID of the SchedulingGroupSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingGroupSectionID of the deleted SchedulingGroupSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingGroupSection <- function(SchedulingGroupSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingGroupSection", objectId = SchedulingGroupSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingGroupSection
	#'
	#' This function creates a SchedulingGroupSection
	#' @param fieldNames The field values to give the created SchedulingGroupSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingGroupSection <- function(SchedulingGroupID = NULL, SchedulingGroupSectionIDClonedFrom = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingGroupSection", body = list(DataObject = body), searchFields = append("SchedulingGroupSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingGroupSection
	#'
	#' This function modifies a SchedulingGroupSection
	#' @param fieldNames The field values to give the modified SchedulingGroupSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingGroupSection <- function(SchedulingGroupSectionID, SchedulingGroupID = NULL, SchedulingGroupSectionIDClonedFrom = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingGroupSection", objectId = SchedulingGroupSectionID, body = list(DataObject = body), searchFields = append("SchedulingGroupSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourses
	#'
	#' This function returns a dataframe or json object of TempCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourses <- function(searchConditionsList = NULL, ActiveSections = F, AveragePerSectionMinimumSectionsRequired = F, CodeDescription = F, CourseCode = F, CourseID = F, CourseLengthCode = F, CourseLengthID = F, CourseSubjectCode = F, CourseTypeCode = F, CreatedTime = F, CurriculumCode = F, DefaultSectionLengthID = F, Description = F, EarnedCredits = F, EntityCode = F, EstimatedNumberOfSections = F, EstimatedStudentsPerSection = F, GradeLevelSummary = F, GradingPeriodSetCode = F, GradingPeriodSetID = F, IsActive = F, MinimumSectionsRequired = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewGradingPeriodSetCode = F, NewGradingPeriodSetID = F, Note = F, NumberOfAlternateCourseRequests = F, NumberOfCourseRequests = F, NumberOfSeatsAvailable = F, NumberOfTransferStudentSections = F, ObjectIsDirty = F, ObjectName = F, OriginalEstimatedNumberOfSections = F, RecordsUpdated = F, RowIsReadOnly = F, RowIsSelected = F, TempCourseID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourse
	#'
	#' This function returns a dataframe or json object of a TempCourse
	#' @param TempCourseID The ID of the TempCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourse <- function(TempCourseID, ActiveSections = F, AveragePerSectionMinimumSectionsRequired = F, CodeDescription = F, CourseCode = F, CourseID = F, CourseLengthCode = F, CourseLengthID = F, CourseSubjectCode = F, CourseTypeCode = F, CreatedTime = F, CurriculumCode = F, DefaultSectionLengthID = F, Description = F, EarnedCredits = F, EntityCode = F, EstimatedNumberOfSections = F, EstimatedStudentsPerSection = F, GradeLevelSummary = F, GradingPeriodSetCode = F, GradingPeriodSetID = F, IsActive = F, MinimumSectionsRequired = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewGradingPeriodSetCode = F, NewGradingPeriodSetID = F, Note = F, NumberOfAlternateCourseRequests = F, NumberOfCourseRequests = F, NumberOfSeatsAvailable = F, NumberOfTransferStudentSections = F, ObjectIsDirty = F, ObjectName = F, OriginalEstimatedNumberOfSections = F, RecordsUpdated = F, RowIsReadOnly = F, RowIsSelected = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempCourse", objectId = TempCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourse
	#'
	#' This function deletes a TempCourse
	#' @param TempCourseID The ID of the TempCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempCourseID of the deleted TempCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourse <- function(TempCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempCourse", objectId = TempCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourse
	#'
	#' This function creates a TempCourse
	#' @param fieldNames The field values to give the created TempCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourse <- function(ActiveSections = NULL, AveragePerSectionMinimumSectionsRequired = NULL, CodeDescription = NULL, CourseCode = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseLengthID = NULL, CourseSubjectCode = NULL, CourseTypeCode = NULL, CurriculumCode = NULL, DefaultSectionLengthID = NULL, Description = NULL, EarnedCredits = NULL, EntityCode = NULL, EstimatedNumberOfSections = NULL, EstimatedStudentsPerSection = NULL, GradeLevelSummary = NULL, GradingPeriodSetCode = NULL, GradingPeriodSetID = NULL, IsActive = NULL, MinimumSectionsRequired = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewGradingPeriodSetCode = NULL, NewGradingPeriodSetID = NULL, Note = NULL, NumberOfAlternateCourseRequests = NULL, NumberOfCourseRequests = NULL, NumberOfSeatsAvailable = NULL, NumberOfTransferStudentSections = NULL, ObjectIsDirty = NULL, ObjectName = NULL, OriginalEstimatedNumberOfSections = NULL, RecordsUpdated = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempCourse", body = list(DataObject = body), searchFields = append("TempCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourse
	#'
	#' This function modifies a TempCourse
	#' @param fieldNames The field values to give the modified TempCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourse <- function(TempCourseID, ActiveSections = NULL, AveragePerSectionMinimumSectionsRequired = NULL, CodeDescription = NULL, CourseCode = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseLengthID = NULL, CourseSubjectCode = NULL, CourseTypeCode = NULL, CurriculumCode = NULL, DefaultSectionLengthID = NULL, Description = NULL, EarnedCredits = NULL, EntityCode = NULL, EstimatedNumberOfSections = NULL, EstimatedStudentsPerSection = NULL, GradeLevelSummary = NULL, GradingPeriodSetCode = NULL, GradingPeriodSetID = NULL, IsActive = NULL, MinimumSectionsRequired = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewGradingPeriodSetCode = NULL, NewGradingPeriodSetID = NULL, Note = NULL, NumberOfAlternateCourseRequests = NULL, NumberOfCourseRequests = NULL, NumberOfSeatsAvailable = NULL, NumberOfTransferStudentSections = NULL, ObjectIsDirty = NULL, ObjectName = NULL, OriginalEstimatedNumberOfSections = NULL, RecordsUpdated = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempCourse", objectId = TempCourseID, body = list(DataObject = body), searchFields = append("TempCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedSections
	#'
	#' This function returns a dataframe or json object of TempFailedSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedSections <- function(searchConditionsList = NULL, Course = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseID = F, CourseIsActive = F, CourseTypeCode = F, CreatedTime = F, CurrentEnrollment = F, EntityIDCourse = F, EntityIDOfferedTo = F, ErrorCount = F, GradeLevelSummary = F, HasErrors = F, IsActive = F, IsSourceSection = F, MaximumStudentCount = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewSectionLengthCode = F, NewSectionLengthID = F, Note = F, NumberOfTransferStudentSections = F, PeriodDaySummary = F, PrimaryDays = F, PrimaryDisplayPeriod = F, RowIsReadOnly = F, RowIsSelected = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, StaffFullNameFML = F, TargetCourse = F, TempFailedSectionID = F, TempSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedSection
	#'
	#' This function returns a dataframe or json object of a TempFailedSection
	#' @param TempFailedSectionID The ID of the TempFailedSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedSection <- function(TempFailedSectionID, Course = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseID = F, CourseIsActive = F, CourseTypeCode = F, CreatedTime = F, CurrentEnrollment = F, EntityIDCourse = F, EntityIDOfferedTo = F, ErrorCount = F, GradeLevelSummary = F, HasErrors = F, IsActive = F, IsSourceSection = F, MaximumStudentCount = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewSectionLengthCode = F, NewSectionLengthID = F, Note = F, NumberOfTransferStudentSections = F, PeriodDaySummary = F, PrimaryDays = F, PrimaryDisplayPeriod = F, RowIsReadOnly = F, RowIsSelected = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, StaffFullNameFML = F, TargetCourse = F, TempSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedSection", objectId = TempFailedSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedSection
	#'
	#' This function deletes a TempFailedSection
	#' @param TempFailedSectionID The ID of the TempFailedSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedSectionID of the deleted TempFailedSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedSection <- function(TempFailedSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedSection", objectId = TempFailedSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedSection
	#'
	#' This function creates a TempFailedSection
	#' @param fieldNames The field values to give the created TempFailedSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedSection <- function(Course = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseID = NULL, CourseIsActive = NULL, CourseTypeCode = NULL, CurrentEnrollment = NULL, EntityIDCourse = NULL, EntityIDOfferedTo = NULL, ErrorCount = NULL, GradeLevelSummary = NULL, HasErrors = NULL, IsActive = NULL, MaximumStudentCount = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewSectionLengthCode = NULL, NewSectionLengthID = NULL, Note = NULL, NumberOfTransferStudentSections = NULL, PeriodDaySummary = NULL, PrimaryDays = NULL, PrimaryDisplayPeriod = NULL, RowIsReadOnly = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, StaffFullNameFML = NULL, TargetCourse = NULL, TempSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedSection", body = list(DataObject = body), searchFields = append("TempFailedSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedSection
	#'
	#' This function modifies a TempFailedSection
	#' @param fieldNames The field values to give the modified TempFailedSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedSection <- function(TempFailedSectionID, Course = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseID = NULL, CourseIsActive = NULL, CourseTypeCode = NULL, CurrentEnrollment = NULL, EntityIDCourse = NULL, EntityIDOfferedTo = NULL, ErrorCount = NULL, GradeLevelSummary = NULL, HasErrors = NULL, IsActive = NULL, MaximumStudentCount = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewSectionLengthCode = NULL, NewSectionLengthID = NULL, Note = NULL, NumberOfTransferStudentSections = NULL, PeriodDaySummary = NULL, PrimaryDays = NULL, PrimaryDisplayPeriod = NULL, RowIsReadOnly = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, StaffFullNameFML = NULL, TargetCourse = NULL, TempSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedSection", objectId = TempFailedSectionID, body = list(DataObject = body), searchFields = append("TempFailedSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSchedulingTeamGradeReferences
	#'
	#' This function returns a dataframe or json object of TempSchedulingTeamGradeReferences
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSchedulingTeamGradeReferences. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSchedulingTeamGradeReferences.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSchedulingTeamGradeReference') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempSchedulingTeamGradeReferences
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSchedulingTeamGradeReferences <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, CurrentStudentCount = F, Description = F, MaximumStudentCount = F, ModifiedTime = F, OverrideTotalToBeAssignedCount = F, OverrideTotalToBeAssignedPercent = F, SchedulingTeamGradeReferenceID = F, SchedulingTeamID = F, SortOrder = F, TempSchedulingTeamGradeReferenceID = F, TotalStudents = F, TotalToBeAssignedCount = F, TotalToBeAssignedPercent = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempSchedulingTeamGradeReference", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSchedulingTeamGradeReference
	#'
	#' This function returns a dataframe or json object of a TempSchedulingTeamGradeReference
	#' @param TempSchedulingTeamGradeReferenceID The ID of the TempSchedulingTeamGradeReference to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSchedulingTeamGradeReference. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSchedulingTeamGradeReference.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSchedulingTeamGradeReference') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempSchedulingTeamGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSchedulingTeamGradeReference <- function(TempSchedulingTeamGradeReferenceID, Code = F, CreatedTime = F, CurrentStudentCount = F, Description = F, MaximumStudentCount = F, ModifiedTime = F, OverrideTotalToBeAssignedCount = F, OverrideTotalToBeAssignedPercent = F, SchedulingTeamGradeReferenceID = F, SchedulingTeamID = F, SortOrder = F, TotalStudents = F, TotalToBeAssignedCount = F, TotalToBeAssignedPercent = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSchedulingTeamGradeReferenceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempSchedulingTeamGradeReference", objectId = TempSchedulingTeamGradeReferenceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSchedulingTeamGradeReference
	#'
	#' This function deletes a TempSchedulingTeamGradeReference
	#' @param TempSchedulingTeamGradeReferenceID The ID of the TempSchedulingTeamGradeReference to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempSchedulingTeamGradeReferenceID of the deleted TempSchedulingTeamGradeReference.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSchedulingTeamGradeReference <- function(TempSchedulingTeamGradeReferenceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempSchedulingTeamGradeReference", objectId = TempSchedulingTeamGradeReferenceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSchedulingTeamGradeReference
	#'
	#' This function creates a TempSchedulingTeamGradeReference
	#' @param fieldNames The field values to give the created TempSchedulingTeamGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempSchedulingTeamGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSchedulingTeamGradeReference <- function(Code = NULL, CurrentStudentCount = NULL, Description = NULL, MaximumStudentCount = NULL, OverrideTotalToBeAssignedCount = NULL, OverrideTotalToBeAssignedPercent = NULL, SchedulingTeamGradeReferenceID = NULL, SchedulingTeamID = NULL, SortOrder = NULL, TotalStudents = NULL, TotalToBeAssignedCount = NULL, TotalToBeAssignedPercent = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempSchedulingTeamGradeReference", body = list(DataObject = body), searchFields = append("TempSchedulingTeamGradeReferenceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSchedulingTeamGradeReference
	#'
	#' This function modifies a TempSchedulingTeamGradeReference
	#' @param fieldNames The field values to give the modified TempSchedulingTeamGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempSchedulingTeamGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSchedulingTeamGradeReference <- function(TempSchedulingTeamGradeReferenceID, Code = NULL, CurrentStudentCount = NULL, Description = NULL, MaximumStudentCount = NULL, OverrideTotalToBeAssignedCount = NULL, OverrideTotalToBeAssignedPercent = NULL, SchedulingTeamGradeReferenceID = NULL, SchedulingTeamID = NULL, SortOrder = NULL, TotalStudents = NULL, TotalToBeAssignedCount = NULL, TotalToBeAssignedPercent = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempSchedulingTeamGradeReference", objectId = TempSchedulingTeamGradeReferenceID, body = list(DataObject = body), searchFields = append("TempSchedulingTeamGradeReferenceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentSchedulingCategories
	#'
	#' This function returns a dataframe or json object of TempStudentSchedulingCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSchedulingCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSchedulingCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSchedulingCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentSchedulingCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentSchedulingCategories <- function(searchConditionsList = NULL, CreatedTime = F, Message = F, ModifiedTime = F, ProposedCategoriesDisplayValue = F, ProposedTargetCategoryIDsToDeleteCSV = F, ProposedTargetCategoryIDsToInsertCSV = F, SourceCategoriesDisplayValue = F, SourceCategoryIDsCSV = F, SourceStudentEntityYearID = F, StudentNameLFM = F, TargetCategoriesDisplayValue = F, TargetCategoryIDsCSV = F, TargetStudentEntityYearID = F, TempStudentSchedulingCategoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentSchedulingCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentSchedulingCategory
	#'
	#' This function returns a dataframe or json object of a TempStudentSchedulingCategory
	#' @param TempStudentSchedulingCategoryID The ID of the TempStudentSchedulingCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSchedulingCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSchedulingCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSchedulingCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentSchedulingCategory <- function(TempStudentSchedulingCategoryID, CreatedTime = F, Message = F, ModifiedTime = F, ProposedCategoriesDisplayValue = F, ProposedTargetCategoryIDsToDeleteCSV = F, ProposedTargetCategoryIDsToInsertCSV = F, SourceCategoriesDisplayValue = F, SourceCategoryIDsCSV = F, SourceStudentEntityYearID = F, StudentNameLFM = F, TargetCategoriesDisplayValue = F, TargetCategoryIDsCSV = F, TargetStudentEntityYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentSchedulingCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentSchedulingCategory", objectId = TempStudentSchedulingCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentSchedulingCategory
	#'
	#' This function deletes a TempStudentSchedulingCategory
	#' @param TempStudentSchedulingCategoryID The ID of the TempStudentSchedulingCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentSchedulingCategoryID of the deleted TempStudentSchedulingCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentSchedulingCategory <- function(TempStudentSchedulingCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentSchedulingCategory", objectId = TempStudentSchedulingCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentSchedulingCategory
	#'
	#' This function creates a TempStudentSchedulingCategory
	#' @param fieldNames The field values to give the created TempStudentSchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentSchedulingCategory <- function(Message = NULL, ProposedCategoriesDisplayValue = NULL, SourceCategoriesDisplayValue = NULL, SourceStudentEntityYearID = NULL, StudentNameLFM = NULL, TargetCategoriesDisplayValue = NULL, TargetStudentEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentSchedulingCategory", body = list(DataObject = body), searchFields = append("TempStudentSchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentSchedulingCategory
	#'
	#' This function modifies a TempStudentSchedulingCategory
	#' @param fieldNames The field values to give the modified TempStudentSchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentSchedulingCategory <- function(TempStudentSchedulingCategoryID, Message = NULL, ProposedCategoriesDisplayValue = NULL, SourceCategoriesDisplayValue = NULL, SourceStudentEntityYearID = NULL, StudentNameLFM = NULL, TargetCategoriesDisplayValue = NULL, TargetStudentEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentSchedulingCategory", objectId = TempStudentSchedulingCategoryID, body = list(DataObject = body), searchFields = append("TempStudentSchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentSchedulingTeams
	#'
	#' This function returns a dataframe or json object of TempStudentSchedulingTeams
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSchedulingTeams. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSchedulingTeams.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSchedulingTeam') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentSchedulingTeams
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentSchedulingTeams <- function(searchConditionsList = NULL, CreatedTime = F, Message = F, ModifiedTime = F, SourceSchedulingTeamCode = F, SourceSchedulingTeamDescription = F, SourceSchedulingTeamID = F, SourceSchoolYearDescription = F, SourceStudentEntityYearID = F, StudentNameLFM = F, TargetSchedulingTeamCode = F, TargetSchedulingTeamDescription = F, TargetSchedulingTeamID = F, TargetSchoolYearDescription = F, TargetStudentEntityYearID = F, TempStudentSchedulingTeamID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentSchedulingTeam", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentSchedulingTeam
	#'
	#' This function returns a dataframe or json object of a TempStudentSchedulingTeam
	#' @param TempStudentSchedulingTeamID The ID of the TempStudentSchedulingTeam to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSchedulingTeam. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSchedulingTeam.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSchedulingTeam') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentSchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentSchedulingTeam <- function(TempStudentSchedulingTeamID, CreatedTime = F, Message = F, ModifiedTime = F, SourceSchedulingTeamCode = F, SourceSchedulingTeamDescription = F, SourceSchedulingTeamID = F, SourceSchoolYearDescription = F, SourceStudentEntityYearID = F, StudentNameLFM = F, TargetSchedulingTeamCode = F, TargetSchedulingTeamDescription = F, TargetSchedulingTeamID = F, TargetSchoolYearDescription = F, TargetStudentEntityYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentSchedulingTeamID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentSchedulingTeam", objectId = TempStudentSchedulingTeamID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentSchedulingTeam
	#'
	#' This function deletes a TempStudentSchedulingTeam
	#' @param TempStudentSchedulingTeamID The ID of the TempStudentSchedulingTeam to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentSchedulingTeamID of the deleted TempStudentSchedulingTeam.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentSchedulingTeam <- function(TempStudentSchedulingTeamID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentSchedulingTeam", objectId = TempStudentSchedulingTeamID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentSchedulingTeam
	#'
	#' This function creates a TempStudentSchedulingTeam
	#' @param fieldNames The field values to give the created TempStudentSchedulingTeam. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentSchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentSchedulingTeam <- function(Message = NULL, SourceSchedulingTeamCode = NULL, SourceSchedulingTeamDescription = NULL, SourceSchedulingTeamID = NULL, SourceSchoolYearDescription = NULL, SourceStudentEntityYearID = NULL, StudentNameLFM = NULL, TargetSchedulingTeamCode = NULL, TargetSchedulingTeamDescription = NULL, TargetSchedulingTeamID = NULL, TargetSchoolYearDescription = NULL, TargetStudentEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentSchedulingTeam", body = list(DataObject = body), searchFields = append("TempStudentSchedulingTeamID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentSchedulingTeam
	#'
	#' This function modifies a TempStudentSchedulingTeam
	#' @param fieldNames The field values to give the modified TempStudentSchedulingTeam. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentSchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentSchedulingTeam <- function(TempStudentSchedulingTeamID, Message = NULL, SourceSchedulingTeamCode = NULL, SourceSchedulingTeamDescription = NULL, SourceSchedulingTeamID = NULL, SourceSchoolYearDescription = NULL, SourceStudentEntityYearID = NULL, StudentNameLFM = NULL, TargetSchedulingTeamCode = NULL, TargetSchedulingTeamDescription = NULL, TargetSchedulingTeamID = NULL, TargetSchoolYearDescription = NULL, TargetStudentEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentSchedulingTeam", objectId = TempStudentSchedulingTeamID, body = list(DataObject = body), searchFields = append("TempStudentSchedulingTeamID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerStudentCourseRequestSectionDetails
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerStudentCourseRequestSectionDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudentCourseRequestSectionDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudentCourseRequestSectionDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudentCourseRequestSectionDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerStudentCourseRequestSectionDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerStudentCourseRequestSectionDetails <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SectionConflictReason = F, SectionConflictReasonText = F, StudentAutoSchedulerStudentCourseRequestSectionDetailID = F, StudentAutoSchedulerStudentCourseRequestSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSectionDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' @param StudentAutoSchedulerStudentCourseRequestSectionDetailID The ID of the StudentAutoSchedulerStudentCourseRequestSectionDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudentCourseRequestSectionDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudentCourseRequestSectionDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudentCourseRequestSectionDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerStudentCourseRequestSectionDetail <- function(StudentAutoSchedulerStudentCourseRequestSectionDetailID, CreatedTime = F, ModifiedTime = F, SectionConflictReason = F, SectionConflictReasonText = F, StudentAutoSchedulerStudentCourseRequestSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerStudentCourseRequestSectionDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSectionDetail", objectId = StudentAutoSchedulerStudentCourseRequestSectionDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#'
	#' This function deletes a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' @param StudentAutoSchedulerStudentCourseRequestSectionDetailID The ID of the StudentAutoSchedulerStudentCourseRequestSectionDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerStudentCourseRequestSectionDetailID of the deleted StudentAutoSchedulerStudentCourseRequestSectionDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerStudentCourseRequestSectionDetail <- function(StudentAutoSchedulerStudentCourseRequestSectionDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSectionDetail", objectId = StudentAutoSchedulerStudentCourseRequestSectionDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#'
	#' This function creates a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' @param fieldNames The field values to give the created StudentAutoSchedulerStudentCourseRequestSectionDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerStudentCourseRequestSectionDetail <- function(SectionConflictReason = NULL, SectionConflictReasonText = NULL, StudentAutoSchedulerStudentCourseRequestSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSectionDetail", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentCourseRequestSectionDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#'
	#' This function modifies a StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerStudentCourseRequestSectionDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerStudentCourseRequestSectionDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerStudentCourseRequestSectionDetail <- function(StudentAutoSchedulerStudentCourseRequestSectionDetailID, SectionConflictReason = NULL, SectionConflictReasonText = NULL, StudentAutoSchedulerStudentCourseRequestSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSectionDetail", objectId = StudentAutoSchedulerStudentCourseRequestSectionDetailID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentCourseRequestSectionDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerStudentCourseRequestSections
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerStudentCourseRequestSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudentCourseRequestSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudentCourseRequestSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudentCourseRequestSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerStudentCourseRequestSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerStudentCourseRequestSections <- function(searchConditionsList = NULL, AssignedToThisSection = F, CreatedTime = F, EligibleToEnrollInSection = F, ModifiedTime = F, PeriodDaySummary = F, SeatsRemaining = F, SequenceNumber = F, StudentAutoSchedulerSectionID = F, StudentAutoSchedulerStudentCourseRequestID = F, StudentAutoSchedulerStudentCourseRequestSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerStudentCourseRequestSection
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerStudentCourseRequestSection
	#' @param StudentAutoSchedulerStudentCourseRequestSectionID The ID of the StudentAutoSchedulerStudentCourseRequestSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudentCourseRequestSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudentCourseRequestSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudentCourseRequestSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerStudentCourseRequestSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerStudentCourseRequestSection <- function(StudentAutoSchedulerStudentCourseRequestSectionID, AssignedToThisSection = F, CreatedTime = F, EligibleToEnrollInSection = F, ModifiedTime = F, PeriodDaySummary = F, SeatsRemaining = F, SequenceNumber = F, StudentAutoSchedulerSectionID = F, StudentAutoSchedulerStudentCourseRequestID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerStudentCourseRequestSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSection", objectId = StudentAutoSchedulerStudentCourseRequestSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerStudentCourseRequestSection
	#'
	#' This function deletes a StudentAutoSchedulerStudentCourseRequestSection
	#' @param StudentAutoSchedulerStudentCourseRequestSectionID The ID of the StudentAutoSchedulerStudentCourseRequestSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerStudentCourseRequestSectionID of the deleted StudentAutoSchedulerStudentCourseRequestSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerStudentCourseRequestSection <- function(StudentAutoSchedulerStudentCourseRequestSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSection", objectId = StudentAutoSchedulerStudentCourseRequestSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerStudentCourseRequestSection
	#'
	#' This function creates a StudentAutoSchedulerStudentCourseRequestSection
	#' @param fieldNames The field values to give the created StudentAutoSchedulerStudentCourseRequestSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerStudentCourseRequestSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerStudentCourseRequestSection <- function(PeriodDaySummary = NULL, SeatsRemaining = NULL, SequenceNumber = NULL, StudentAutoSchedulerSectionID = NULL, StudentAutoSchedulerStudentCourseRequestID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSection", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentCourseRequestSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerStudentCourseRequestSection
	#'
	#' This function modifies a StudentAutoSchedulerStudentCourseRequestSection
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerStudentCourseRequestSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerStudentCourseRequestSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerStudentCourseRequestSection <- function(StudentAutoSchedulerStudentCourseRequestSectionID, PeriodDaySummary = NULL, SeatsRemaining = NULL, SequenceNumber = NULL, StudentAutoSchedulerSectionID = NULL, StudentAutoSchedulerStudentCourseRequestID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequestSection", objectId = StudentAutoSchedulerStudentCourseRequestSectionID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentCourseRequestSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedStudentSectionTransactions
	#'
	#' This function returns a dataframe or json object of TempFailedStudentSectionTransactions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentSectionTransactions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentSectionTransactions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentSectionTransaction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedStudentSectionTransactions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedStudentSectionTransactions <- function(searchConditionsList = NULL, Course = F, CreatedTime = F, EarlyExitReasonID = F, EndDate = F, EntityIDCountsAs = F, HideNewStudentButton = F, ModifiedTime = F, NameIDRequestedBy = F, Note = F, SectionCode = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentNameLFM = F, StudentNumber = F, StudentSectionTransactionID = F, TempFailedStudentSectionTransactionID = F, TempStudentCourseRequestID = F, TempStudentCourseRequestToReactivateID = F, TempStudentSectionTransactionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedStudentSectionTransaction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedStudentSectionTransaction
	#'
	#' This function returns a dataframe or json object of a TempFailedStudentSectionTransaction
	#' @param TempFailedStudentSectionTransactionID The ID of the TempFailedStudentSectionTransaction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentSectionTransaction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentSectionTransaction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentSectionTransaction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedStudentSectionTransaction <- function(TempFailedStudentSectionTransactionID, Course = F, CreatedTime = F, EarlyExitReasonID = F, EndDate = F, EntityIDCountsAs = F, HideNewStudentButton = F, ModifiedTime = F, NameIDRequestedBy = F, Note = F, SectionCode = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentNameLFM = F, StudentNumber = F, StudentSectionTransactionID = F, TempStudentCourseRequestID = F, TempStudentCourseRequestToReactivateID = F, TempStudentSectionTransactionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedStudentSectionTransactionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedStudentSectionTransaction", objectId = TempFailedStudentSectionTransactionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedStudentSectionTransaction
	#'
	#' This function deletes a TempFailedStudentSectionTransaction
	#' @param TempFailedStudentSectionTransactionID The ID of the TempFailedStudentSectionTransaction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedStudentSectionTransactionID of the deleted TempFailedStudentSectionTransaction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedStudentSectionTransaction <- function(TempFailedStudentSectionTransactionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedStudentSectionTransaction", objectId = TempFailedStudentSectionTransactionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedStudentSectionTransaction
	#'
	#' This function creates a TempFailedStudentSectionTransaction
	#' @param fieldNames The field values to give the created TempFailedStudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedStudentSectionTransaction <- function(Course = NULL, EarlyExitReasonID = NULL, EndDate = NULL, EntityIDCountsAs = NULL, HideNewStudentButton = NULL, NameIDRequestedBy = NULL, Note = NULL, SectionCode = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentNameLFM = NULL, StudentNumber = NULL, StudentSectionTransactionID = NULL, TempStudentCourseRequestID = NULL, TempStudentCourseRequestToReactivateID = NULL, TempStudentSectionTransactionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedStudentSectionTransaction", body = list(DataObject = body), searchFields = append("TempFailedStudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedStudentSectionTransaction
	#'
	#' This function modifies a TempFailedStudentSectionTransaction
	#' @param fieldNames The field values to give the modified TempFailedStudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedStudentSectionTransaction <- function(TempFailedStudentSectionTransactionID, Course = NULL, EarlyExitReasonID = NULL, EndDate = NULL, EntityIDCountsAs = NULL, HideNewStudentButton = NULL, NameIDRequestedBy = NULL, Note = NULL, SectionCode = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentNameLFM = NULL, StudentNumber = NULL, StudentSectionTransactionID = NULL, TempStudentCourseRequestID = NULL, TempStudentCourseRequestToReactivateID = NULL, TempStudentSectionTransactionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedStudentSectionTransaction", objectId = TempFailedStudentSectionTransactionID, body = list(DataObject = body), searchFields = append("TempFailedStudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSections
	#'
	#' This function returns a dataframe or json object of TempSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSections <- function(searchConditionsList = NULL, Course = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseID = F, CourseIsActive = F, CourseTypeCode = F, CreatedTime = F, CurrentEnrollment = F, EntityIDCourse = F, EntityIDOfferedTo = F, GradeLevelSummary = F, IsActive = F, IsSourceSection = F, MaximumStudentCount = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewSectionLengthCode = F, NewSectionLengthID = F, Note = F, NumberOfTransferStudentSections = F, PeriodDaySummary = F, PrimaryDays = F, PrimaryDisplayPeriod = F, RowIsReadOnly = F, RowIsSelected = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, StaffFullNameFML = F, TargetCourse = F, TempSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSection
	#'
	#' This function returns a dataframe or json object of a TempSection
	#' @param TempSectionID The ID of the TempSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSection <- function(TempSectionID, Course = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseID = F, CourseIsActive = F, CourseTypeCode = F, CreatedTime = F, CurrentEnrollment = F, EntityIDCourse = F, EntityIDOfferedTo = F, GradeLevelSummary = F, IsActive = F, IsSourceSection = F, MaximumStudentCount = F, ModifiedTime = F, NewCourseLengthCode = F, NewCourseLengthID = F, NewSectionLengthCode = F, NewSectionLengthID = F, Note = F, NumberOfTransferStudentSections = F, PeriodDaySummary = F, PrimaryDays = F, PrimaryDisplayPeriod = F, RowIsReadOnly = F, RowIsSelected = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, StaffFullNameFML = F, TargetCourse = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempSection", objectId = TempSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSection
	#'
	#' This function deletes a TempSection
	#' @param TempSectionID The ID of the TempSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempSectionID of the deleted TempSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSection <- function(TempSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempSection", objectId = TempSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSection
	#'
	#' This function creates a TempSection
	#' @param fieldNames The field values to give the created TempSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSection <- function(Course = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseID = NULL, CourseIsActive = NULL, CourseTypeCode = NULL, CurrentEnrollment = NULL, EntityIDCourse = NULL, EntityIDOfferedTo = NULL, GradeLevelSummary = NULL, IsActive = NULL, MaximumStudentCount = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewSectionLengthCode = NULL, NewSectionLengthID = NULL, Note = NULL, NumberOfTransferStudentSections = NULL, PeriodDaySummary = NULL, PrimaryDays = NULL, PrimaryDisplayPeriod = NULL, RowIsReadOnly = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, StaffFullNameFML = NULL, TargetCourse = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempSection", body = list(DataObject = body), searchFields = append("TempSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSection
	#'
	#' This function modifies a TempSection
	#' @param fieldNames The field values to give the modified TempSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSection <- function(TempSectionID, Course = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseID = NULL, CourseIsActive = NULL, CourseTypeCode = NULL, CurrentEnrollment = NULL, EntityIDCourse = NULL, EntityIDOfferedTo = NULL, GradeLevelSummary = NULL, IsActive = NULL, MaximumStudentCount = NULL, NewCourseLengthCode = NULL, NewCourseLengthID = NULL, NewSectionLengthCode = NULL, NewSectionLengthID = NULL, Note = NULL, NumberOfTransferStudentSections = NULL, PeriodDaySummary = NULL, PrimaryDays = NULL, PrimaryDisplayPeriod = NULL, RowIsReadOnly = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, StaffFullNameFML = NULL, TargetCourse = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempSection", objectId = TempSectionID, body = list(DataObject = body), searchFields = append("TempSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentCourseRequestToReactivateNonStates
	#'
	#' This function returns a dataframe or json object of TempStudentCourseRequestToReactivateNonStates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequestToReactivateNonStates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequestToReactivateNonStates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequestToReactivateNonState') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentCourseRequestToReactivateNonStates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentCourseRequestToReactivateNonStates <- function(searchConditionsList = NULL, AlternateRank = F, AuditRecordIsRequestable = F, AuditRecordIsSchedulable = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, CurrentEnrollment = F, DateFrom = F, DateTo = F, Days = F, EarlyExitReasonCodeDescription = F, EarlyExitReasonID = F, EarnedCreditOverride = F, EarnedCredits = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDRequestedFrom = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, GradeReferenceID = F, IsAlternate = F, IsTransferCourse = F, MaximumStudentCount = F, ModifiedTime = F, NameIDRequestedBy = F, NameRequestedByLFM = F, Period = F, PreventReactivateCheckboxFromBeingRendered = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthID = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetID = F, StaffFullNameFML = F, StartDate = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, TempStudentCourseRequestToReactivateNonStateID = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransferCourseName = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateNonState", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentCourseRequestToReactivateNonState
	#'
	#' This function returns a dataframe or json object of a TempStudentCourseRequestToReactivateNonState
	#' @param TempStudentCourseRequestToReactivateNonStateID The ID of the TempStudentCourseRequestToReactivateNonState to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequestToReactivateNonState. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequestToReactivateNonState.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequestToReactivateNonState') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentCourseRequestToReactivateNonState
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentCourseRequestToReactivateNonState <- function(TempStudentCourseRequestToReactivateNonStateID, AlternateRank = F, AuditRecordIsRequestable = F, AuditRecordIsSchedulable = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, CurrentEnrollment = F, DateFrom = F, DateTo = F, Days = F, EarlyExitReasonCodeDescription = F, EarlyExitReasonID = F, EarnedCreditOverride = F, EarnedCredits = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDRequestedFrom = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, GradeReferenceID = F, IsAlternate = F, IsTransferCourse = F, MaximumStudentCount = F, ModifiedTime = F, NameIDRequestedBy = F, NameRequestedByLFM = F, Period = F, PreventReactivateCheckboxFromBeingRendered = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthID = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetID = F, StaffFullNameFML = F, StartDate = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransferCourseName = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentCourseRequestToReactivateNonStateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateNonState", objectId = TempStudentCourseRequestToReactivateNonStateID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentCourseRequestToReactivateNonState
	#'
	#' This function deletes a TempStudentCourseRequestToReactivateNonState
	#' @param TempStudentCourseRequestToReactivateNonStateID The ID of the TempStudentCourseRequestToReactivateNonState to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentCourseRequestToReactivateNonStateID of the deleted TempStudentCourseRequestToReactivateNonState.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentCourseRequestToReactivateNonState <- function(TempStudentCourseRequestToReactivateNonStateID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateNonState", objectId = TempStudentCourseRequestToReactivateNonStateID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentCourseRequestToReactivateNonState
	#'
	#' This function creates a TempStudentCourseRequestToReactivateNonState
	#' @param fieldNames The field values to give the created TempStudentCourseRequestToReactivateNonState. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentCourseRequestToReactivateNonState
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentCourseRequestToReactivateNonState <- function(AlternateRank = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CurrentEnrollment = NULL, DateFrom = NULL, DateTo = NULL, Days = NULL, EarlyExitReasonCodeDescription = NULL, EarlyExitReasonID = NULL, EarnedCreditOverride = NULL, EarnedCredits = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, EntityIDRequestedFrom = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsAlternate = NULL, IsTransferCourse = NULL, MaximumStudentCount = NULL, NameIDRequestedBy = NULL, NameRequestedByLFM = NULL, Period = NULL, PreventReactivateCheckboxFromBeingRendered = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthID = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, SectionLengthSubsetID = NULL, StaffFullNameFML = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, StudentSectionTransactionID = NULL, TotalEarnedCreditOverride = NULL, TotalFailedCreditOverride = NULL, TransferCourseName = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditTotalOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateNonState", body = list(DataObject = body), searchFields = append("TempStudentCourseRequestToReactivateNonStateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentCourseRequestToReactivateNonState
	#'
	#' This function modifies a TempStudentCourseRequestToReactivateNonState
	#' @param fieldNames The field values to give the modified TempStudentCourseRequestToReactivateNonState. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentCourseRequestToReactivateNonState
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentCourseRequestToReactivateNonState <- function(TempStudentCourseRequestToReactivateNonStateID, AlternateRank = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CurrentEnrollment = NULL, DateFrom = NULL, DateTo = NULL, Days = NULL, EarlyExitReasonCodeDescription = NULL, EarlyExitReasonID = NULL, EarnedCreditOverride = NULL, EarnedCredits = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, EntityIDRequestedFrom = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsAlternate = NULL, IsTransferCourse = NULL, MaximumStudentCount = NULL, NameIDRequestedBy = NULL, NameRequestedByLFM = NULL, Period = NULL, PreventReactivateCheckboxFromBeingRendered = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthID = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, SectionLengthSubsetID = NULL, StaffFullNameFML = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, StudentSectionTransactionID = NULL, TotalEarnedCreditOverride = NULL, TotalFailedCreditOverride = NULL, TransferCourseName = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditTotalOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateNonState", objectId = TempStudentCourseRequestToReactivateNonStateID, body = list(DataObject = body), searchFields = append("TempStudentCourseRequestToReactivateNonStateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentCourseRequestSectionLengthSubsets
	#'
	#' This function returns a dataframe or json object of TempStudentCourseRequestSectionLengthSubsets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequestSectionLengthSubsets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequestSectionLengthSubsets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequestSectionLengthSubset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentCourseRequestSectionLengthSubsets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentCourseRequestSectionLengthSubsets <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SectionLengthSubsetID = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, TempStudentCourseRequestSectionLengthSubsetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentCourseRequestSectionLengthSubset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentCourseRequestSectionLengthSubset
	#'
	#' This function returns a dataframe or json object of a TempStudentCourseRequestSectionLengthSubset
	#' @param TempStudentCourseRequestSectionLengthSubsetID The ID of the TempStudentCourseRequestSectionLengthSubset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequestSectionLengthSubset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequestSectionLengthSubset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequestSectionLengthSubset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentCourseRequestSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentCourseRequestSectionLengthSubset <- function(TempStudentCourseRequestSectionLengthSubsetID, CreatedTime = F, ModifiedTime = F, SectionLengthSubsetID = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentCourseRequestSectionLengthSubsetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestSectionLengthSubset", objectId = TempStudentCourseRequestSectionLengthSubsetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentCourseRequestSectionLengthSubset
	#'
	#' This function deletes a TempStudentCourseRequestSectionLengthSubset
	#' @param TempStudentCourseRequestSectionLengthSubsetID The ID of the TempStudentCourseRequestSectionLengthSubset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentCourseRequestSectionLengthSubsetID of the deleted TempStudentCourseRequestSectionLengthSubset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentCourseRequestSectionLengthSubset <- function(TempStudentCourseRequestSectionLengthSubsetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestSectionLengthSubset", objectId = TempStudentCourseRequestSectionLengthSubsetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentCourseRequestSectionLengthSubset
	#'
	#' This function creates a TempStudentCourseRequestSectionLengthSubset
	#' @param fieldNames The field values to give the created TempStudentCourseRequestSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentCourseRequestSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentCourseRequestSectionLengthSubset <- function(SectionLengthSubsetID = NULL, StudentCourseRequestID = NULL, StudentCourseRequestSectionLengthSubsetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestSectionLengthSubset", body = list(DataObject = body), searchFields = append("TempStudentCourseRequestSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentCourseRequestSectionLengthSubset
	#'
	#' This function modifies a TempStudentCourseRequestSectionLengthSubset
	#' @param fieldNames The field values to give the modified TempStudentCourseRequestSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentCourseRequestSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentCourseRequestSectionLengthSubset <- function(TempStudentCourseRequestSectionLengthSubsetID, SectionLengthSubsetID = NULL, StudentCourseRequestID = NULL, StudentCourseRequestSectionLengthSubsetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestSectionLengthSubset", objectId = TempStudentCourseRequestSectionLengthSubsetID, body = list(DataObject = body), searchFields = append("TempStudentCourseRequestSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentSectionTransactions
	#'
	#' This function returns a dataframe or json object of TempStudentSectionTransactions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSectionTransactions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSectionTransactions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSectionTransaction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentSectionTransactions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentSectionTransactions <- function(searchConditionsList = NULL, CreatedTime = F, EarlyExitReasonID = F, EndDate = F, EntityIDCountsAs = F, HideNewStudentButton = F, ModifiedTime = F, NameIDRequestedBy = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentSectionTransactionID = F, TempStudentCourseRequestID = F, TempStudentCourseRequestToReactivateID = F, TempStudentSectionTransactionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentSectionTransaction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentSectionTransaction
	#'
	#' This function returns a dataframe or json object of a TempStudentSectionTransaction
	#' @param TempStudentSectionTransactionID The ID of the TempStudentSectionTransaction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentSectionTransaction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentSectionTransaction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentSectionTransaction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentSectionTransaction <- function(TempStudentSectionTransactionID, CreatedTime = F, EarlyExitReasonID = F, EndDate = F, EntityIDCountsAs = F, HideNewStudentButton = F, ModifiedTime = F, NameIDRequestedBy = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentSectionTransactionID = F, TempStudentCourseRequestID = F, TempStudentCourseRequestToReactivateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentSectionTransactionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentSectionTransaction", objectId = TempStudentSectionTransactionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentSectionTransaction
	#'
	#' This function deletes a TempStudentSectionTransaction
	#' @param TempStudentSectionTransactionID The ID of the TempStudentSectionTransaction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentSectionTransactionID of the deleted TempStudentSectionTransaction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentSectionTransaction <- function(TempStudentSectionTransactionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentSectionTransaction", objectId = TempStudentSectionTransactionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentSectionTransaction
	#'
	#' This function creates a TempStudentSectionTransaction
	#' @param fieldNames The field values to give the created TempStudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentSectionTransaction <- function(EarlyExitReasonID = NULL, EndDate = NULL, EntityIDCountsAs = NULL, HideNewStudentButton = NULL, NameIDRequestedBy = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentSectionTransactionID = NULL, TempStudentCourseRequestID = NULL, TempStudentCourseRequestToReactivateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentSectionTransaction", body = list(DataObject = body), searchFields = append("TempStudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentSectionTransaction
	#'
	#' This function modifies a TempStudentSectionTransaction
	#' @param fieldNames The field values to give the modified TempStudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentSectionTransaction <- function(TempStudentSectionTransactionID, EarlyExitReasonID = NULL, EndDate = NULL, EntityIDCountsAs = NULL, HideNewStudentButton = NULL, NameIDRequestedBy = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentSectionTransactionID = NULL, TempStudentCourseRequestID = NULL, TempStudentCourseRequestToReactivateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentSectionTransaction", objectId = TempStudentSectionTransactionID, body = list(DataObject = body), searchFields = append("TempStudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List OpenPeriodAnalysisStudents
	#'
	#' This function returns a dataframe or json object of OpenPeriodAnalysisStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given OpenPeriodAnalysisStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the OpenPeriodAnalysisStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('OpenPeriodAnalysisStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of OpenPeriodAnalysisStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listOpenPeriodAnalysisStudents <- function(searchConditionsList = NULL, CreatedTime = F, FirstName = F, GradeLevelCode = F, IsActive = F, LastName = F, MiddleName = F, ModifiedTime = F, OpenPeriodAnalysisID = F, OpenPeriodAnalysisStudentID = F, StudentID = F, StudentNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "OpenPeriodAnalysisStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an OpenPeriodAnalysisStudent
	#'
	#' This function returns a dataframe or json object of an OpenPeriodAnalysisStudent
	#' @param OpenPeriodAnalysisStudentID The ID of the OpenPeriodAnalysisStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given OpenPeriodAnalysisStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the OpenPeriodAnalysisStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('OpenPeriodAnalysisStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of OpenPeriodAnalysisStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getOpenPeriodAnalysisStudent <- function(OpenPeriodAnalysisStudentID, CreatedTime = F, FirstName = F, GradeLevelCode = F, IsActive = F, LastName = F, MiddleName = F, ModifiedTime = F, OpenPeriodAnalysisID = F, StudentID = F, StudentNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "OpenPeriodAnalysisStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysisStudent", objectId = OpenPeriodAnalysisStudentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an OpenPeriodAnalysisStudent
	#'
	#' This function deletes an OpenPeriodAnalysisStudent
	#' @param OpenPeriodAnalysisStudentID The ID of the OpenPeriodAnalysisStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The OpenPeriodAnalysisStudentID of the deleted OpenPeriodAnalysisStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteOpenPeriodAnalysisStudent <- function(OpenPeriodAnalysisStudentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysisStudent", objectId = OpenPeriodAnalysisStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an OpenPeriodAnalysisStudent
	#'
	#' This function creates an OpenPeriodAnalysisStudent
	#' @param fieldNames The field values to give the created OpenPeriodAnalysisStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created OpenPeriodAnalysisStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createOpenPeriodAnalysisStudent <- function(FirstName = NULL, GradeLevelCode = NULL, IsActive = NULL, LastName = NULL, MiddleName = NULL, OpenPeriodAnalysisID = NULL, StudentID = NULL, StudentNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysisStudent", body = list(DataObject = body), searchFields = append("OpenPeriodAnalysisStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an OpenPeriodAnalysisStudent
	#'
	#' This function modifies an OpenPeriodAnalysisStudent
	#' @param fieldNames The field values to give the modified OpenPeriodAnalysisStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified OpenPeriodAnalysisStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyOpenPeriodAnalysisStudent <- function(OpenPeriodAnalysisStudentID, FirstName = NULL, GradeLevelCode = NULL, IsActive = NULL, LastName = NULL, MiddleName = NULL, OpenPeriodAnalysisID = NULL, StudentID = NULL, StudentNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysisStudent", objectId = OpenPeriodAnalysisStudentID, body = list(DataObject = body), searchFields = append("OpenPeriodAnalysisStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List OpenPeriodAnalyses
	#'
	#' This function returns a dataframe or json object of OpenPeriodAnalyses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given OpenPeriodAnalyses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the OpenPeriodAnalyses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('OpenPeriodAnalysis') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of OpenPeriodAnalyses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listOpenPeriodAnalyses <- function(searchConditionsList = NULL, CreatedTime = F, EndTime = F, EntityID = F, ModifiedTime = F, OpenPeriodAnalysisID = F, SchoolYearID = F, StartTime = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDPerformer = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "OpenPeriodAnalysis", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an OpenPeriodAnalysis
	#'
	#' This function returns a dataframe or json object of an OpenPeriodAnalysis
	#' @param OpenPeriodAnalysisID The ID of the OpenPeriodAnalysis to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given OpenPeriodAnalysis. Defaults to FALSE for all return fields which, for convenience, returns all fields for the OpenPeriodAnalysis.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('OpenPeriodAnalysis') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of OpenPeriodAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getOpenPeriodAnalysis <- function(OpenPeriodAnalysisID, CreatedTime = F, EndTime = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, StartTime = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDPerformer = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "OpenPeriodAnalysisID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysis", objectId = OpenPeriodAnalysisID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an OpenPeriodAnalysis
	#'
	#' This function deletes an OpenPeriodAnalysis
	#' @param OpenPeriodAnalysisID The ID of the OpenPeriodAnalysis to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The OpenPeriodAnalysisID of the deleted OpenPeriodAnalysis.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteOpenPeriodAnalysis <- function(OpenPeriodAnalysisID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysis", objectId = OpenPeriodAnalysisID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an OpenPeriodAnalysis
	#'
	#' This function creates an OpenPeriodAnalysis
	#' @param fieldNames The field values to give the created OpenPeriodAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created OpenPeriodAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createOpenPeriodAnalysis <- function(EndTime = NULL, EntityID = NULL, SchoolYearID = NULL, StartTime = NULL, Status = NULL, UserIDPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysis", body = list(DataObject = body), searchFields = append("OpenPeriodAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an OpenPeriodAnalysis
	#'
	#' This function modifies an OpenPeriodAnalysis
	#' @param fieldNames The field values to give the modified OpenPeriodAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified OpenPeriodAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyOpenPeriodAnalysis <- function(OpenPeriodAnalysisID, EndTime = NULL, EntityID = NULL, SchoolYearID = NULL, StartTime = NULL, Status = NULL, UserIDPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "OpenPeriodAnalysis", objectId = OpenPeriodAnalysisID, body = list(DataObject = body), searchFields = append("OpenPeriodAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List BaseRunAnalyses
	#'
	#' This function returns a dataframe or json object of BaseRunAnalyses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BaseRunAnalyses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BaseRunAnalyses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BaseRunAnalysis') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of BaseRunAnalyses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listBaseRunAnalyses <- function(searchConditionsList = NULL, BaseRunAnalysisID = F, CreatedTime = F, EntityID = F, ModifiedTime = F, RunType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDPerformer = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "BaseRunAnalysis", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a BaseRunAnalysis
	#'
	#' This function returns a dataframe or json object of a BaseRunAnalysis
	#' @param BaseRunAnalysisID The ID of the BaseRunAnalysis to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given BaseRunAnalysis. Defaults to FALSE for all return fields which, for convenience, returns all fields for the BaseRunAnalysis.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('BaseRunAnalysis') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of BaseRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getBaseRunAnalysis <- function(BaseRunAnalysisID, CreatedTime = F, EntityID = F, ModifiedTime = F, RunType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDPerformer = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "BaseRunAnalysisID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "BaseRunAnalysis", objectId = BaseRunAnalysisID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a BaseRunAnalysis
	#'
	#' This function deletes a BaseRunAnalysis
	#' @param BaseRunAnalysisID The ID of the BaseRunAnalysis to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The BaseRunAnalysisID of the deleted BaseRunAnalysis.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteBaseRunAnalysis <- function(BaseRunAnalysisID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "BaseRunAnalysis", objectId = BaseRunAnalysisID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a BaseRunAnalysis
	#'
	#' This function creates a BaseRunAnalysis
	#' @param fieldNames The field values to give the created BaseRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created BaseRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createBaseRunAnalysis <- function(EntityID = NULL, RunType = NULL, SchoolYearID = NULL, UserIDPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "BaseRunAnalysis", body = list(DataObject = body), searchFields = append("BaseRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a BaseRunAnalysis
	#'
	#' This function modifies a BaseRunAnalysis
	#' @param fieldNames The field values to give the modified BaseRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified BaseRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyBaseRunAnalysis <- function(BaseRunAnalysisID, EntityID = NULL, RunType = NULL, SchoolYearID = NULL, UserIDPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "BaseRunAnalysis", objectId = BaseRunAnalysisID, body = list(DataObject = body), searchFields = append("BaseRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudyHallSchedulerRunAnalyses
	#'
	#' This function returns a dataframe or json object of StudyHallSchedulerRunAnalyses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudyHallSchedulerRunAnalyses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudyHallSchedulerRunAnalyses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudyHallSchedulerRunAnalysis') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudyHallSchedulerRunAnalyses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudyHallSchedulerRunAnalyses <- function(searchConditionsList = NULL, BaseRunAnalysisID = F, CreatedTime = F, EndTimeAnalysis = F, EndTimeFinalize = F, ModifiedTime = F, RunInformation = F, StartTimeAnalysis = F, StartTimeFinalize = F, StudyHallSchedulerRunAnalysisID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudyHallSchedulerRunAnalysis", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudyHallSchedulerRunAnalysis
	#'
	#' This function returns a dataframe or json object of a StudyHallSchedulerRunAnalysis
	#' @param StudyHallSchedulerRunAnalysisID The ID of the StudyHallSchedulerRunAnalysis to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudyHallSchedulerRunAnalysis. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudyHallSchedulerRunAnalysis.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudyHallSchedulerRunAnalysis') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudyHallSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudyHallSchedulerRunAnalysis <- function(StudyHallSchedulerRunAnalysisID, BaseRunAnalysisID = F, CreatedTime = F, EndTimeAnalysis = F, EndTimeFinalize = F, ModifiedTime = F, RunInformation = F, StartTimeAnalysis = F, StartTimeFinalize = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudyHallSchedulerRunAnalysisID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudyHallSchedulerRunAnalysis", objectId = StudyHallSchedulerRunAnalysisID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudyHallSchedulerRunAnalysis
	#'
	#' This function deletes a StudyHallSchedulerRunAnalysis
	#' @param StudyHallSchedulerRunAnalysisID The ID of the StudyHallSchedulerRunAnalysis to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudyHallSchedulerRunAnalysisID of the deleted StudyHallSchedulerRunAnalysis.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudyHallSchedulerRunAnalysis <- function(StudyHallSchedulerRunAnalysisID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudyHallSchedulerRunAnalysis", objectId = StudyHallSchedulerRunAnalysisID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudyHallSchedulerRunAnalysis
	#'
	#' This function creates a StudyHallSchedulerRunAnalysis
	#' @param fieldNames The field values to give the created StudyHallSchedulerRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudyHallSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudyHallSchedulerRunAnalysis <- function(BaseRunAnalysisID = NULL, EndTimeAnalysis = NULL, EndTimeFinalize = NULL, RunInformation = NULL, StartTimeAnalysis = NULL, StartTimeFinalize = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudyHallSchedulerRunAnalysis", body = list(DataObject = body), searchFields = append("StudyHallSchedulerRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudyHallSchedulerRunAnalysis
	#'
	#' This function modifies a StudyHallSchedulerRunAnalysis
	#' @param fieldNames The field values to give the modified StudyHallSchedulerRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudyHallSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudyHallSchedulerRunAnalysis <- function(StudyHallSchedulerRunAnalysisID, BaseRunAnalysisID = NULL, EndTimeAnalysis = NULL, EndTimeFinalize = NULL, RunInformation = NULL, StartTimeAnalysis = NULL, StartTimeFinalize = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudyHallSchedulerRunAnalysis", objectId = StudyHallSchedulerRunAnalysisID, body = list(DataObject = body), searchFields = append("StudyHallSchedulerRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerRunAnalysisExceptions
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerRunAnalysisExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerRunAnalysisExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerRunAnalysisExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerRunAnalysisException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerRunAnalysisExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerRunAnalysisExceptions <- function(searchConditionsList = NULL, CourseCode = F, CourseDescription = F, CreatedTime = F, Message = F, ModifiedTime = F, SectionCode = F, SeverityType = F, StudentAutoSchedulerRunAnalysisExceptionID = F, StudentAutoSchedulerRunAnalysisID = F, StudentAutoSchedulerStudentCourseRequestID = F, StudentFullName = F, StudentNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerRunAnalysisException
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerRunAnalysisException
	#' @param StudentAutoSchedulerRunAnalysisExceptionID The ID of the StudentAutoSchedulerRunAnalysisException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerRunAnalysisException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerRunAnalysisException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerRunAnalysisException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerRunAnalysisException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerRunAnalysisException <- function(StudentAutoSchedulerRunAnalysisExceptionID, CourseCode = F, CourseDescription = F, CreatedTime = F, Message = F, ModifiedTime = F, SectionCode = F, SeverityType = F, StudentAutoSchedulerRunAnalysisID = F, StudentAutoSchedulerStudentCourseRequestID = F, StudentFullName = F, StudentNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerRunAnalysisExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisException", objectId = StudentAutoSchedulerRunAnalysisExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerRunAnalysisException
	#'
	#' This function deletes a StudentAutoSchedulerRunAnalysisException
	#' @param StudentAutoSchedulerRunAnalysisExceptionID The ID of the StudentAutoSchedulerRunAnalysisException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerRunAnalysisExceptionID of the deleted StudentAutoSchedulerRunAnalysisException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerRunAnalysisException <- function(StudentAutoSchedulerRunAnalysisExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisException", objectId = StudentAutoSchedulerRunAnalysisExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerRunAnalysisException
	#'
	#' This function creates a StudentAutoSchedulerRunAnalysisException
	#' @param fieldNames The field values to give the created StudentAutoSchedulerRunAnalysisException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerRunAnalysisException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerRunAnalysisException <- function(CourseCode = NULL, CourseDescription = NULL, Message = NULL, SectionCode = NULL, SeverityType = NULL, StudentAutoSchedulerRunAnalysisID = NULL, StudentAutoSchedulerStudentCourseRequestID = NULL, StudentFullName = NULL, StudentNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisException", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerRunAnalysisExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerRunAnalysisException
	#'
	#' This function modifies a StudentAutoSchedulerRunAnalysisException
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerRunAnalysisException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerRunAnalysisException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerRunAnalysisException <- function(StudentAutoSchedulerRunAnalysisExceptionID, CourseCode = NULL, CourseDescription = NULL, Message = NULL, SectionCode = NULL, SeverityType = NULL, StudentAutoSchedulerRunAnalysisID = NULL, StudentAutoSchedulerStudentCourseRequestID = NULL, StudentFullName = NULL, StudentNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisException", objectId = StudentAutoSchedulerRunAnalysisExceptionID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerRunAnalysisExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerProposedStudentSectionTransactions
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerProposedStudentSectionTransactions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerProposedStudentSectionTransactions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerProposedStudentSectionTransactions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerProposedStudentSectionTransaction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerProposedStudentSectionTransactions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerProposedStudentSectionTransactions <- function(searchConditionsList = NULL, BaseRunAnalysisID = F, CachedStudentSectionID = F, CreatedTime = F, DataCommittedToRealObjects = F, EndDate = F, EndDateOriginal = F, EntityIDCountsAs = F, ModifiedTime = F, SectionID = F, SectionIDOriginal = F, SectionLengthSubsetID = F, SectionLengthSubsetIDOriginal = F, StartDate = F, StartDateOriginal = F, StudentAutoSchedulerProposedStudentSectionTransactionID = F, StudentID = F, StudentIDOriginal = F, StudentSectionID = F, StudentSectionIDOriginal = F, StudentSectionTransactionID = F, StudentSectionTransactionIDOriginal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSectionTransaction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerProposedStudentSectionTransaction
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerProposedStudentSectionTransaction
	#' @param StudentAutoSchedulerProposedStudentSectionTransactionID The ID of the StudentAutoSchedulerProposedStudentSectionTransaction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerProposedStudentSectionTransaction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerProposedStudentSectionTransaction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerProposedStudentSectionTransaction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerProposedStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerProposedStudentSectionTransaction <- function(StudentAutoSchedulerProposedStudentSectionTransactionID, BaseRunAnalysisID = F, CachedStudentSectionID = F, CreatedTime = F, DataCommittedToRealObjects = F, EndDate = F, EndDateOriginal = F, EntityIDCountsAs = F, ModifiedTime = F, SectionID = F, SectionIDOriginal = F, SectionLengthSubsetID = F, SectionLengthSubsetIDOriginal = F, StartDate = F, StartDateOriginal = F, StudentID = F, StudentIDOriginal = F, StudentSectionID = F, StudentSectionIDOriginal = F, StudentSectionTransactionID = F, StudentSectionTransactionIDOriginal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerProposedStudentSectionTransactionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSectionTransaction", objectId = StudentAutoSchedulerProposedStudentSectionTransactionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerProposedStudentSectionTransaction
	#'
	#' This function deletes a StudentAutoSchedulerProposedStudentSectionTransaction
	#' @param StudentAutoSchedulerProposedStudentSectionTransactionID The ID of the StudentAutoSchedulerProposedStudentSectionTransaction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerProposedStudentSectionTransactionID of the deleted StudentAutoSchedulerProposedStudentSectionTransaction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerProposedStudentSectionTransaction <- function(StudentAutoSchedulerProposedStudentSectionTransactionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSectionTransaction", objectId = StudentAutoSchedulerProposedStudentSectionTransactionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerProposedStudentSectionTransaction
	#'
	#' This function creates a StudentAutoSchedulerProposedStudentSectionTransaction
	#' @param fieldNames The field values to give the created StudentAutoSchedulerProposedStudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerProposedStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerProposedStudentSectionTransaction <- function(BaseRunAnalysisID = NULL, CachedStudentSectionID = NULL, DataCommittedToRealObjects = NULL, EndDate = NULL, EndDateOriginal = NULL, EntityIDCountsAs = NULL, SectionID = NULL, SectionIDOriginal = NULL, SectionLengthSubsetID = NULL, SectionLengthSubsetIDOriginal = NULL, StartDate = NULL, StartDateOriginal = NULL, StudentID = NULL, StudentIDOriginal = NULL, StudentSectionID = NULL, StudentSectionIDOriginal = NULL, StudentSectionTransactionID = NULL, StudentSectionTransactionIDOriginal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSectionTransaction", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerProposedStudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerProposedStudentSectionTransaction
	#'
	#' This function modifies a StudentAutoSchedulerProposedStudentSectionTransaction
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerProposedStudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerProposedStudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerProposedStudentSectionTransaction <- function(StudentAutoSchedulerProposedStudentSectionTransactionID, BaseRunAnalysisID = NULL, CachedStudentSectionID = NULL, DataCommittedToRealObjects = NULL, EndDate = NULL, EndDateOriginal = NULL, EntityIDCountsAs = NULL, SectionID = NULL, SectionIDOriginal = NULL, SectionLengthSubsetID = NULL, SectionLengthSubsetIDOriginal = NULL, StartDate = NULL, StartDateOriginal = NULL, StudentID = NULL, StudentIDOriginal = NULL, StudentSectionID = NULL, StudentSectionIDOriginal = NULL, StudentSectionTransactionID = NULL, StudentSectionTransactionIDOriginal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSectionTransaction", objectId = StudentAutoSchedulerProposedStudentSectionTransactionID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerProposedStudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerProposedStudentSections
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerProposedStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerProposedStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerProposedStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerProposedStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerProposedStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerProposedStudentSections <- function(searchConditionsList = NULL, BaseRunAnalysisID = F, CachedStudentSectionID = F, CreatedTime = F, DataCommittedToRealObjects = F, GradeReferenceID = F, GradeReferenceIDOriginal = F, ModifiedTime = F, SectionID = F, SectionIDOriginal = F, SequenceWithinEntireProcess = F, SequenceWithinGrade = F, SequenceWithinStudent = F, StudentAutoSchedulerProposedStudentSectionID = F, StudentID = F, StudentIDOriginal = F, StudentSectionID = F, StudentSectionIDOriginal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerProposedStudentSection
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerProposedStudentSection
	#' @param StudentAutoSchedulerProposedStudentSectionID The ID of the StudentAutoSchedulerProposedStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerProposedStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerProposedStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerProposedStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerProposedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerProposedStudentSection <- function(StudentAutoSchedulerProposedStudentSectionID, BaseRunAnalysisID = F, CachedStudentSectionID = F, CreatedTime = F, DataCommittedToRealObjects = F, GradeReferenceID = F, GradeReferenceIDOriginal = F, ModifiedTime = F, SectionID = F, SectionIDOriginal = F, SequenceWithinEntireProcess = F, SequenceWithinGrade = F, SequenceWithinStudent = F, StudentID = F, StudentIDOriginal = F, StudentSectionID = F, StudentSectionIDOriginal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerProposedStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSection", objectId = StudentAutoSchedulerProposedStudentSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerProposedStudentSection
	#'
	#' This function deletes a StudentAutoSchedulerProposedStudentSection
	#' @param StudentAutoSchedulerProposedStudentSectionID The ID of the StudentAutoSchedulerProposedStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerProposedStudentSectionID of the deleted StudentAutoSchedulerProposedStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerProposedStudentSection <- function(StudentAutoSchedulerProposedStudentSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSection", objectId = StudentAutoSchedulerProposedStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerProposedStudentSection
	#'
	#' This function creates a StudentAutoSchedulerProposedStudentSection
	#' @param fieldNames The field values to give the created StudentAutoSchedulerProposedStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerProposedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerProposedStudentSection <- function(BaseRunAnalysisID = NULL, CachedStudentSectionID = NULL, DataCommittedToRealObjects = NULL, GradeReferenceID = NULL, GradeReferenceIDOriginal = NULL, SectionID = NULL, SectionIDOriginal = NULL, SequenceWithinEntireProcess = NULL, SequenceWithinGrade = NULL, SequenceWithinStudent = NULL, StudentID = NULL, StudentIDOriginal = NULL, StudentSectionID = NULL, StudentSectionIDOriginal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSection", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerProposedStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerProposedStudentSection
	#'
	#' This function modifies a StudentAutoSchedulerProposedStudentSection
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerProposedStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerProposedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerProposedStudentSection <- function(StudentAutoSchedulerProposedStudentSectionID, BaseRunAnalysisID = NULL, CachedStudentSectionID = NULL, DataCommittedToRealObjects = NULL, GradeReferenceID = NULL, GradeReferenceIDOriginal = NULL, SectionID = NULL, SectionIDOriginal = NULL, SequenceWithinEntireProcess = NULL, SequenceWithinGrade = NULL, SequenceWithinStudent = NULL, StudentID = NULL, StudentIDOriginal = NULL, StudentSectionID = NULL, StudentSectionIDOriginal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentSection", objectId = StudentAutoSchedulerProposedStudentSectionID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerProposedStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerProposedStudentCourseRequests
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerProposedStudentCourseRequests
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerProposedStudentCourseRequests. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerProposedStudentCourseRequests.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerProposedStudentCourseRequest') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerProposedStudentCourseRequests
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerProposedStudentCourseRequests <- function(searchConditionsList = NULL, BaseRunAnalysisID = F, CachedStudentSectionID = F, CourseConflictReason = F, CourseConflictReasonText = F, CourseID = F, CourseIDOriginal = F, CreatedTime = F, DataCommittedToRealObjects = F, EntityIDRequestedFrom = F, Ignore = F, IsAlternate = F, IsNewlyScheduled = F, ModifiedTime = F, RequestStatus = F, RequestStatusOriginal = F, SchedulingMethod = F, SchedulingMethodOriginal = F, SectionID = F, SectionIDOriginal = F, SectionLengthSubsetsRequested = F, SectionLengthSubsetSummary = F, SequenceWithinEntireProcess = F, SequenceWithinGrade = F, SequenceWithinStudent = F, StudentAutoSchedulerProposedStudentCourseRequestID = F, StudentCourseRequestID = F, StudentCourseRequestIDOriginal = F, StudentID = F, StudentIDOriginal = F, StudentSectionID = F, StudentSectionIDOriginal = F, UnscheduleStatus = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentCourseRequest", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerProposedStudentCourseRequest
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerProposedStudentCourseRequest
	#' @param StudentAutoSchedulerProposedStudentCourseRequestID The ID of the StudentAutoSchedulerProposedStudentCourseRequest to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerProposedStudentCourseRequest. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerProposedStudentCourseRequest.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerProposedStudentCourseRequest') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerProposedStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerProposedStudentCourseRequest <- function(StudentAutoSchedulerProposedStudentCourseRequestID, BaseRunAnalysisID = F, CachedStudentSectionID = F, CourseConflictReason = F, CourseConflictReasonText = F, CourseID = F, CourseIDOriginal = F, CreatedTime = F, DataCommittedToRealObjects = F, EntityIDRequestedFrom = F, Ignore = F, IsAlternate = F, IsNewlyScheduled = F, ModifiedTime = F, RequestStatus = F, RequestStatusOriginal = F, SchedulingMethod = F, SchedulingMethodOriginal = F, SectionID = F, SectionIDOriginal = F, SectionLengthSubsetsRequested = F, SectionLengthSubsetSummary = F, SequenceWithinEntireProcess = F, SequenceWithinGrade = F, SequenceWithinStudent = F, StudentCourseRequestID = F, StudentCourseRequestIDOriginal = F, StudentID = F, StudentIDOriginal = F, StudentSectionID = F, StudentSectionIDOriginal = F, UnscheduleStatus = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerProposedStudentCourseRequestID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentCourseRequest", objectId = StudentAutoSchedulerProposedStudentCourseRequestID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerProposedStudentCourseRequest
	#'
	#' This function deletes a StudentAutoSchedulerProposedStudentCourseRequest
	#' @param StudentAutoSchedulerProposedStudentCourseRequestID The ID of the StudentAutoSchedulerProposedStudentCourseRequest to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerProposedStudentCourseRequestID of the deleted StudentAutoSchedulerProposedStudentCourseRequest.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerProposedStudentCourseRequest <- function(StudentAutoSchedulerProposedStudentCourseRequestID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentCourseRequest", objectId = StudentAutoSchedulerProposedStudentCourseRequestID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerProposedStudentCourseRequest
	#'
	#' This function creates a StudentAutoSchedulerProposedStudentCourseRequest
	#' @param fieldNames The field values to give the created StudentAutoSchedulerProposedStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerProposedStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerProposedStudentCourseRequest <- function(BaseRunAnalysisID = NULL, CachedStudentSectionID = NULL, CourseConflictReason = NULL, CourseConflictReasonText = NULL, CourseID = NULL, CourseIDOriginal = NULL, DataCommittedToRealObjects = NULL, EntityIDRequestedFrom = NULL, Ignore = NULL, IsAlternate = NULL, RequestStatus = NULL, RequestStatusOriginal = NULL, SchedulingMethod = NULL, SchedulingMethodOriginal = NULL, SectionID = NULL, SectionIDOriginal = NULL, SectionLengthSubsetsRequested = NULL, SectionLengthSubsetSummary = NULL, SequenceWithinEntireProcess = NULL, SequenceWithinGrade = NULL, SequenceWithinStudent = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDOriginal = NULL, StudentID = NULL, StudentIDOriginal = NULL, StudentSectionID = NULL, StudentSectionIDOriginal = NULL, UnscheduleStatus = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentCourseRequest", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerProposedStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerProposedStudentCourseRequest
	#'
	#' This function modifies a StudentAutoSchedulerProposedStudentCourseRequest
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerProposedStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerProposedStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerProposedStudentCourseRequest <- function(StudentAutoSchedulerProposedStudentCourseRequestID, BaseRunAnalysisID = NULL, CachedStudentSectionID = NULL, CourseConflictReason = NULL, CourseConflictReasonText = NULL, CourseID = NULL, CourseIDOriginal = NULL, DataCommittedToRealObjects = NULL, EntityIDRequestedFrom = NULL, Ignore = NULL, IsAlternate = NULL, RequestStatus = NULL, RequestStatusOriginal = NULL, SchedulingMethod = NULL, SchedulingMethodOriginal = NULL, SectionID = NULL, SectionIDOriginal = NULL, SectionLengthSubsetsRequested = NULL, SectionLengthSubsetSummary = NULL, SequenceWithinEntireProcess = NULL, SequenceWithinGrade = NULL, SequenceWithinStudent = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDOriginal = NULL, StudentID = NULL, StudentIDOriginal = NULL, StudentSectionID = NULL, StudentSectionIDOriginal = NULL, UnscheduleStatus = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerProposedStudentCourseRequest", objectId = StudentAutoSchedulerProposedStudentCourseRequestID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerProposedStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerSections
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerSections <- function(searchConditionsList = NULL, AllowStudentsWithoutCategoryToBeAssigned = F, CreatedTime = F, DayRotationCode = F, DisplayPeriodCode = F, IsActive = F, MaximumStudentCount = F, MinimumStudentCount = F, ModifiedTime = F, OptimalStudentCount = F, PeriodDaySummary = F, PrimaryMeetBuildingCode = F, PrimaryMeetRoomNumber = F, PrimaryMeetStaffNameLFM = F, SchedulingCategories = F, SchedulingTeams = F, SectionCode = F, SectionID = F, SectionLengthCode = F, StudentAutoSchedulerCourseID = F, StudentAutoSchedulerSectionID = F, TotalNumberScheduledThisRun = F, TotalScheduled = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerSection
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerSection
	#' @param StudentAutoSchedulerSectionID The ID of the StudentAutoSchedulerSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerSection <- function(StudentAutoSchedulerSectionID, AllowStudentsWithoutCategoryToBeAssigned = F, CreatedTime = F, DayRotationCode = F, DisplayPeriodCode = F, IsActive = F, MaximumStudentCount = F, MinimumStudentCount = F, ModifiedTime = F, OptimalStudentCount = F, PeriodDaySummary = F, PrimaryMeetBuildingCode = F, PrimaryMeetRoomNumber = F, PrimaryMeetStaffNameLFM = F, SchedulingCategories = F, SchedulingTeams = F, SectionCode = F, SectionID = F, SectionLengthCode = F, StudentAutoSchedulerCourseID = F, TotalNumberScheduledThisRun = F, TotalScheduled = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerSection", objectId = StudentAutoSchedulerSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerSection
	#'
	#' This function deletes a StudentAutoSchedulerSection
	#' @param StudentAutoSchedulerSectionID The ID of the StudentAutoSchedulerSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerSectionID of the deleted StudentAutoSchedulerSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerSection <- function(StudentAutoSchedulerSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerSection", objectId = StudentAutoSchedulerSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerSection
	#'
	#' This function creates a StudentAutoSchedulerSection
	#' @param fieldNames The field values to give the created StudentAutoSchedulerSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerSection <- function(AllowStudentsWithoutCategoryToBeAssigned = NULL, DayRotationCode = NULL, DisplayPeriodCode = NULL, IsActive = NULL, MaximumStudentCount = NULL, MinimumStudentCount = NULL, OptimalStudentCount = NULL, PeriodDaySummary = NULL, PrimaryMeetBuildingCode = NULL, PrimaryMeetRoomNumber = NULL, PrimaryMeetStaffNameLFM = NULL, SchedulingCategories = NULL, SchedulingTeams = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, StudentAutoSchedulerCourseID = NULL, TotalNumberScheduledThisRun = NULL, TotalScheduled = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerSection", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerSection
	#'
	#' This function modifies a StudentAutoSchedulerSection
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerSection <- function(StudentAutoSchedulerSectionID, AllowStudentsWithoutCategoryToBeAssigned = NULL, DayRotationCode = NULL, DisplayPeriodCode = NULL, IsActive = NULL, MaximumStudentCount = NULL, MinimumStudentCount = NULL, OptimalStudentCount = NULL, PeriodDaySummary = NULL, PrimaryMeetBuildingCode = NULL, PrimaryMeetRoomNumber = NULL, PrimaryMeetStaffNameLFM = NULL, SchedulingCategories = NULL, SchedulingTeams = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, StudentAutoSchedulerCourseID = NULL, TotalNumberScheduledThisRun = NULL, TotalScheduled = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerSection", objectId = StudentAutoSchedulerSectionID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerStudentCourseRequests
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerStudentCourseRequests
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudentCourseRequests. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudentCourseRequests.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudentCourseRequest') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerStudentCourseRequests
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerStudentCourseRequests <- function(searchConditionsList = NULL, CourseConflictReason = F, CourseConflictReasonText = F, CreatedTime = F, EntityIDRequestedFrom = F, EntityRequestedFromEntityCode = F, HasRelatedStudentAutoSchedulerStudentCourseRequestSections = F, InitialSequenceNumber = F, IsAlternate = F, ModifiedTime = F, SchedulingMethodCode = F, SectionLengthSubsetsRequested = F, SectionLengthSubsetSummary = F, SequenceNumber = F, StudentAutoSchedulerCourseID = F, StudentAutoSchedulerSectionID = F, StudentAutoSchedulerStudentCourseRequestID = F, StudentAutoSchedulerStudentID = F, StudentCourseRequestID = F, UpdatedDuringThisSchedulingRun = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequest", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerStudentCourseRequest
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerStudentCourseRequest
	#' @param StudentAutoSchedulerStudentCourseRequestID The ID of the StudentAutoSchedulerStudentCourseRequest to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudentCourseRequest. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudentCourseRequest.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudentCourseRequest') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerStudentCourseRequest <- function(StudentAutoSchedulerStudentCourseRequestID, CourseConflictReason = F, CourseConflictReasonText = F, CreatedTime = F, EntityIDRequestedFrom = F, EntityRequestedFromEntityCode = F, HasRelatedStudentAutoSchedulerStudentCourseRequestSections = F, InitialSequenceNumber = F, IsAlternate = F, ModifiedTime = F, SchedulingMethodCode = F, SectionLengthSubsetsRequested = F, SectionLengthSubsetSummary = F, SequenceNumber = F, StudentAutoSchedulerCourseID = F, StudentAutoSchedulerSectionID = F, StudentAutoSchedulerStudentID = F, StudentCourseRequestID = F, UpdatedDuringThisSchedulingRun = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerStudentCourseRequestID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequest", objectId = StudentAutoSchedulerStudentCourseRequestID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerStudentCourseRequest
	#'
	#' This function deletes a StudentAutoSchedulerStudentCourseRequest
	#' @param StudentAutoSchedulerStudentCourseRequestID The ID of the StudentAutoSchedulerStudentCourseRequest to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerStudentCourseRequestID of the deleted StudentAutoSchedulerStudentCourseRequest.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerStudentCourseRequest <- function(StudentAutoSchedulerStudentCourseRequestID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequest", objectId = StudentAutoSchedulerStudentCourseRequestID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerStudentCourseRequest
	#'
	#' This function creates a StudentAutoSchedulerStudentCourseRequest
	#' @param fieldNames The field values to give the created StudentAutoSchedulerStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerStudentCourseRequest <- function(CourseConflictReason = NULL, CourseConflictReasonText = NULL, EntityIDRequestedFrom = NULL, EntityRequestedFromEntityCode = NULL, InitialSequenceNumber = NULL, IsAlternate = NULL, SchedulingMethodCode = NULL, SectionLengthSubsetsRequested = NULL, SectionLengthSubsetSummary = NULL, SequenceNumber = NULL, StudentAutoSchedulerCourseID = NULL, StudentAutoSchedulerSectionID = NULL, StudentAutoSchedulerStudentID = NULL, StudentCourseRequestID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequest", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerStudentCourseRequest
	#'
	#' This function modifies a StudentAutoSchedulerStudentCourseRequest
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerStudentCourseRequest <- function(StudentAutoSchedulerStudentCourseRequestID, CourseConflictReason = NULL, CourseConflictReasonText = NULL, EntityIDRequestedFrom = NULL, EntityRequestedFromEntityCode = NULL, InitialSequenceNumber = NULL, IsAlternate = NULL, SchedulingMethodCode = NULL, SectionLengthSubsetsRequested = NULL, SectionLengthSubsetSummary = NULL, SequenceNumber = NULL, StudentAutoSchedulerCourseID = NULL, StudentAutoSchedulerSectionID = NULL, StudentAutoSchedulerStudentID = NULL, StudentCourseRequestID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudentCourseRequest", objectId = StudentAutoSchedulerStudentCourseRequestID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerStudents
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerStudents <- function(searchConditionsList = NULL, BirthDate = F, CalendarCode = F, CreatedTime = F, EndTimeAnalysis = F, FullName = F, GenderCode = F, Grade = F, GradeReferenceID = F, HasConflict = F, ModifiedTime = F, NumberOfConflictedStudentCourseRequests = F, NumberOfScheduledStudentCourseRequests = F, ProcessedDuringThisSchedulingRun = F, RandomSchedulingInteger = F, RawPermutations = F, SchedulesConsidered = F, SchedulingCategories = F, SchedulingTeamCode = F, SequenceNumber = F, StartTimeAnalysis = F, StudentAutoSchedulerRunAnalysisID = F, StudentAutoSchedulerStudentID = F, StudentID = F, StudentNumber = F, StudentTypeCode = F, TotalNumberOfAlternateStudentCourseRequests = F, TotalNumberOfStudentCourseRequests = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerStudent
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerStudent
	#' @param StudentAutoSchedulerStudentID The ID of the StudentAutoSchedulerStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerStudent <- function(StudentAutoSchedulerStudentID, BirthDate = F, CalendarCode = F, CreatedTime = F, EndTimeAnalysis = F, FullName = F, GenderCode = F, Grade = F, GradeReferenceID = F, HasConflict = F, ModifiedTime = F, NumberOfConflictedStudentCourseRequests = F, NumberOfScheduledStudentCourseRequests = F, ProcessedDuringThisSchedulingRun = F, RandomSchedulingInteger = F, RawPermutations = F, SchedulesConsidered = F, SchedulingCategories = F, SchedulingTeamCode = F, SequenceNumber = F, StartTimeAnalysis = F, StudentAutoSchedulerRunAnalysisID = F, StudentID = F, StudentNumber = F, StudentTypeCode = F, TotalNumberOfAlternateStudentCourseRequests = F, TotalNumberOfStudentCourseRequests = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudent", objectId = StudentAutoSchedulerStudentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerStudent
	#'
	#' This function deletes a StudentAutoSchedulerStudent
	#' @param StudentAutoSchedulerStudentID The ID of the StudentAutoSchedulerStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerStudentID of the deleted StudentAutoSchedulerStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerStudent <- function(StudentAutoSchedulerStudentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudent", objectId = StudentAutoSchedulerStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerStudent
	#'
	#' This function creates a StudentAutoSchedulerStudent
	#' @param fieldNames The field values to give the created StudentAutoSchedulerStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerStudent <- function(BirthDate = NULL, CalendarCode = NULL, EndTimeAnalysis = NULL, FullName = NULL, GenderCode = NULL, Grade = NULL, GradeReferenceID = NULL, HasConflict = NULL, NumberOfConflictedStudentCourseRequests = NULL, NumberOfScheduledStudentCourseRequests = NULL, RandomSchedulingInteger = NULL, RawPermutations = NULL, SchedulesConsidered = NULL, SchedulingCategories = NULL, SchedulingTeamCode = NULL, SequenceNumber = NULL, StartTimeAnalysis = NULL, StudentAutoSchedulerRunAnalysisID = NULL, StudentID = NULL, StudentNumber = NULL, StudentTypeCode = NULL, TotalNumberOfAlternateStudentCourseRequests = NULL, TotalNumberOfStudentCourseRequests = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudent", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerStudent
	#'
	#' This function modifies a StudentAutoSchedulerStudent
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerStudent <- function(StudentAutoSchedulerStudentID, BirthDate = NULL, CalendarCode = NULL, EndTimeAnalysis = NULL, FullName = NULL, GenderCode = NULL, Grade = NULL, GradeReferenceID = NULL, HasConflict = NULL, NumberOfConflictedStudentCourseRequests = NULL, NumberOfScheduledStudentCourseRequests = NULL, RandomSchedulingInteger = NULL, RawPermutations = NULL, SchedulesConsidered = NULL, SchedulingCategories = NULL, SchedulingTeamCode = NULL, SequenceNumber = NULL, StartTimeAnalysis = NULL, StudentAutoSchedulerRunAnalysisID = NULL, StudentID = NULL, StudentNumber = NULL, StudentTypeCode = NULL, TotalNumberOfAlternateStudentCourseRequests = NULL, TotalNumberOfStudentCourseRequests = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerStudent", objectId = StudentAutoSchedulerStudentID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerRunAnalysisTotals
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerRunAnalysisTotals
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerRunAnalysisTotals. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerRunAnalysisTotals.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerRunAnalysisTotal') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerRunAnalysisTotals
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerRunAnalysisTotals <- function(searchConditionsList = NULL, ConflictedStudentCourseRequestPercent = F, ConflictPercent = F, CreatedTime = F, EndTimeAnalysis = F, GradeReferenceID = F, ModifiedTime = F, ScheduledStudentCourseRequestPercent = F, StartTimeAnalysis = F, StudentAutoSchedulerRunAnalysisID = F, StudentAutoSchedulerRunAnalysisTotalID = F, StudentCourseRequestsConflictedThisRun = F, StudentCourseRequestsProcessedThisRun = F, StudentCourseRequestsScheduledThisRun = F, StudentsWithConflicts = F, StudentsWithOneConflict = F, StudentsWithThreeOrMoreConflicts = F, StudentsWithTwoConflicts = F, TotalAlternateStudentCourseRequests = F, TotalConflictedStudentCourseRequests = F, TotalScheduledStudentCourseRequests = F, TotalStudentCourseRequests = F, TotalStudents = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisTotal", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerRunAnalysisTotal
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerRunAnalysisTotal
	#' @param StudentAutoSchedulerRunAnalysisTotalID The ID of the StudentAutoSchedulerRunAnalysisTotal to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerRunAnalysisTotal. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerRunAnalysisTotal.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerRunAnalysisTotal') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerRunAnalysisTotal
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerRunAnalysisTotal <- function(StudentAutoSchedulerRunAnalysisTotalID, ConflictedStudentCourseRequestPercent = F, ConflictPercent = F, CreatedTime = F, EndTimeAnalysis = F, GradeReferenceID = F, ModifiedTime = F, ScheduledStudentCourseRequestPercent = F, StartTimeAnalysis = F, StudentAutoSchedulerRunAnalysisID = F, StudentCourseRequestsConflictedThisRun = F, StudentCourseRequestsProcessedThisRun = F, StudentCourseRequestsScheduledThisRun = F, StudentsWithConflicts = F, StudentsWithOneConflict = F, StudentsWithThreeOrMoreConflicts = F, StudentsWithTwoConflicts = F, TotalAlternateStudentCourseRequests = F, TotalConflictedStudentCourseRequests = F, TotalScheduledStudentCourseRequests = F, TotalStudentCourseRequests = F, TotalStudents = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerRunAnalysisTotalID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisTotal", objectId = StudentAutoSchedulerRunAnalysisTotalID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerRunAnalysisTotal
	#'
	#' This function deletes a StudentAutoSchedulerRunAnalysisTotal
	#' @param StudentAutoSchedulerRunAnalysisTotalID The ID of the StudentAutoSchedulerRunAnalysisTotal to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerRunAnalysisTotalID of the deleted StudentAutoSchedulerRunAnalysisTotal.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerRunAnalysisTotal <- function(StudentAutoSchedulerRunAnalysisTotalID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisTotal", objectId = StudentAutoSchedulerRunAnalysisTotalID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerRunAnalysisTotal
	#'
	#' This function creates a StudentAutoSchedulerRunAnalysisTotal
	#' @param fieldNames The field values to give the created StudentAutoSchedulerRunAnalysisTotal. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerRunAnalysisTotal
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerRunAnalysisTotal <- function(ConflictPercent = NULL, EndTimeAnalysis = NULL, GradeReferenceID = NULL, StartTimeAnalysis = NULL, StudentAutoSchedulerRunAnalysisID = NULL, StudentCourseRequestsConflictedThisRun = NULL, StudentCourseRequestsProcessedThisRun = NULL, StudentCourseRequestsScheduledThisRun = NULL, StudentsWithConflicts = NULL, StudentsWithOneConflict = NULL, StudentsWithThreeOrMoreConflicts = NULL, StudentsWithTwoConflicts = NULL, TotalAlternateStudentCourseRequests = NULL, TotalConflictedStudentCourseRequests = NULL, TotalScheduledStudentCourseRequests = NULL, TotalStudentCourseRequests = NULL, TotalStudents = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisTotal", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerRunAnalysisTotalID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerRunAnalysisTotal
	#'
	#' This function modifies a StudentAutoSchedulerRunAnalysisTotal
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerRunAnalysisTotal. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerRunAnalysisTotal
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerRunAnalysisTotal <- function(StudentAutoSchedulerRunAnalysisTotalID, ConflictPercent = NULL, EndTimeAnalysis = NULL, GradeReferenceID = NULL, StartTimeAnalysis = NULL, StudentAutoSchedulerRunAnalysisID = NULL, StudentCourseRequestsConflictedThisRun = NULL, StudentCourseRequestsProcessedThisRun = NULL, StudentCourseRequestsScheduledThisRun = NULL, StudentsWithConflicts = NULL, StudentsWithOneConflict = NULL, StudentsWithThreeOrMoreConflicts = NULL, StudentsWithTwoConflicts = NULL, TotalAlternateStudentCourseRequests = NULL, TotalConflictedStudentCourseRequests = NULL, TotalScheduledStudentCourseRequests = NULL, TotalStudentCourseRequests = NULL, TotalStudents = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysisTotal", objectId = StudentAutoSchedulerRunAnalysisTotalID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerRunAnalysisTotalID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerRunAnalyses
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerRunAnalyses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerRunAnalyses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerRunAnalyses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerRunAnalysis') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerRunAnalyses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerRunAnalyses <- function(searchConditionsList = NULL, AnalysisDuration = F, AnalysisVersion = F, BaseRunAnalysisID = F, CloseSectionsWhenFilled = F, ConflictedStudentCourseRequestPercent = F, CountOfStudentAutoSchedulerCourseRecords = F, CountOfStudentAutoSchedulerRunAnalysisExceptionRecords = F, CountOfStudentAutoSchedulerStudentRecords = F, CourseConflictPercent = F, CreatedCourseAndSectionAnalysisDetails = F, CreatedStudentAnalysisDetails = F, CreatedTime = F, Description = F, EndTimeAnalysis = F, EndTimeFinalize = F, EvenlyDistributeSchedulingTeams = F, ExistsAutoScheduledCourses = F, ExistsAutoScheduledStudents = F, FailedScheduledStudentSectionsCount = F, FailedStudentCourseRequestsCount = F, FailedStudentSectionsCount = F, FailedStudentSectionTransactionsCount = F, FinalizeDuration = F, IncludedAbilityToAcceptProposedSchedules = F, ModifiedTime = F, OverallDuration = F, PersistentSchedulingRunDataIsNoLongerAcceptable = F, ProcessSpecialEdCourses = F, ProposedSchedulesAccepted = F, RunDescription = F, RunInformation = F, ScheduledStudentCourseRequestPercent = F, SendMessageOnComplete = F, StartTimeAnalysis = F, StartTimeFinalize = F, StudentAutoSchedulerMode = F, StudentAutoSchedulerRunAnalysisID = F, StudentConflictPercent = F, StudentCourseRequestOrder = F, StudentCourseRequestsConflictedThisRun = F, StudentCourseRequestsProcessedThisRun = F, StudentCourseRequestsScheduledThisRun = F, StudentDifficultyOrder = F, StudentEntityYearRangeXMLFilter = F, StudentInformation = F, SuccessfulStudentSectionsCount = F, TotalAlternateStudentCourseRequests = F, TotalConflictedStudentCourseRequests = F, TotalScheduledStudentCourseRequests = F, TotalStudentCourseRequests = F, TotalStudentCourseRequestsToFinalize = F, TotalStudents = F, TotalStudentSectionsToFinalize = F, TotalStudentSectionTransactionsToFinalize = F, TotalStudentsWithConflicts = F, TotalStudentsWithOneConflict = F, TotalStudentsWithThreeOrMoreConflicts = F, TotalStudentsWithTwoConflicts = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysis", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerRunAnalysis
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerRunAnalysis
	#' @param StudentAutoSchedulerRunAnalysisID The ID of the StudentAutoSchedulerRunAnalysis to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerRunAnalysis. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerRunAnalysis.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerRunAnalysis') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerRunAnalysis <- function(StudentAutoSchedulerRunAnalysisID, AnalysisDuration = F, AnalysisVersion = F, BaseRunAnalysisID = F, CloseSectionsWhenFilled = F, ConflictedStudentCourseRequestPercent = F, CountOfStudentAutoSchedulerCourseRecords = F, CountOfStudentAutoSchedulerRunAnalysisExceptionRecords = F, CountOfStudentAutoSchedulerStudentRecords = F, CourseConflictPercent = F, CreatedCourseAndSectionAnalysisDetails = F, CreatedStudentAnalysisDetails = F, CreatedTime = F, Description = F, EndTimeAnalysis = F, EndTimeFinalize = F, EvenlyDistributeSchedulingTeams = F, ExistsAutoScheduledCourses = F, ExistsAutoScheduledStudents = F, FailedScheduledStudentSectionsCount = F, FailedStudentCourseRequestsCount = F, FailedStudentSectionsCount = F, FailedStudentSectionTransactionsCount = F, FinalizeDuration = F, IncludedAbilityToAcceptProposedSchedules = F, ModifiedTime = F, OverallDuration = F, PersistentSchedulingRunDataIsNoLongerAcceptable = F, ProcessSpecialEdCourses = F, ProposedSchedulesAccepted = F, RunDescription = F, RunInformation = F, ScheduledStudentCourseRequestPercent = F, SendMessageOnComplete = F, StartTimeAnalysis = F, StartTimeFinalize = F, StudentAutoSchedulerMode = F, StudentConflictPercent = F, StudentCourseRequestOrder = F, StudentCourseRequestsConflictedThisRun = F, StudentCourseRequestsProcessedThisRun = F, StudentCourseRequestsScheduledThisRun = F, StudentDifficultyOrder = F, StudentEntityYearRangeXMLFilter = F, StudentInformation = F, SuccessfulStudentSectionsCount = F, TotalAlternateStudentCourseRequests = F, TotalConflictedStudentCourseRequests = F, TotalScheduledStudentCourseRequests = F, TotalStudentCourseRequests = F, TotalStudentCourseRequestsToFinalize = F, TotalStudents = F, TotalStudentSectionsToFinalize = F, TotalStudentSectionTransactionsToFinalize = F, TotalStudentsWithConflicts = F, TotalStudentsWithOneConflict = F, TotalStudentsWithThreeOrMoreConflicts = F, TotalStudentsWithTwoConflicts = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerRunAnalysisID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysis", objectId = StudentAutoSchedulerRunAnalysisID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerRunAnalysis
	#'
	#' This function deletes a StudentAutoSchedulerRunAnalysis
	#' @param StudentAutoSchedulerRunAnalysisID The ID of the StudentAutoSchedulerRunAnalysis to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerRunAnalysisID of the deleted StudentAutoSchedulerRunAnalysis.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerRunAnalysis <- function(StudentAutoSchedulerRunAnalysisID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysis", objectId = StudentAutoSchedulerRunAnalysisID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerRunAnalysis
	#'
	#' This function creates a StudentAutoSchedulerRunAnalysis
	#' @param fieldNames The field values to give the created StudentAutoSchedulerRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerRunAnalysis <- function(AnalysisVersion = NULL, BaseRunAnalysisID = NULL, CloseSectionsWhenFilled = NULL, CreatedCourseAndSectionAnalysisDetails = NULL, CreatedStudentAnalysisDetails = NULL, Description = NULL, EndTimeAnalysis = NULL, EndTimeFinalize = NULL, EvenlyDistributeSchedulingTeams = NULL, FailedScheduledStudentSectionsCount = NULL, FailedStudentCourseRequestsCount = NULL, FailedStudentSectionsCount = NULL, FailedStudentSectionTransactionsCount = NULL, IncludedAbilityToAcceptProposedSchedules = NULL, PersistentSchedulingRunDataIsNoLongerAcceptable = NULL, ProcessSpecialEdCourses = NULL, RunInformation = NULL, SendMessageOnComplete = NULL, StartTimeAnalysis = NULL, StartTimeFinalize = NULL, StudentAutoSchedulerMode = NULL, StudentCourseRequestOrder = NULL, StudentDifficultyOrder = NULL, StudentEntityYearRangeXMLFilter = NULL, StudentInformation = NULL, SuccessfulStudentSectionsCount = NULL, TotalAlternateStudentCourseRequests = NULL, TotalConflictedStudentCourseRequests = NULL, TotalScheduledStudentCourseRequests = NULL, TotalStudentCourseRequests = NULL, TotalStudentCourseRequestsToFinalize = NULL, TotalStudents = NULL, TotalStudentSectionsToFinalize = NULL, TotalStudentSectionTransactionsToFinalize = NULL, TotalStudentsWithConflicts = NULL, TotalStudentsWithOneConflict = NULL, TotalStudentsWithThreeOrMoreConflicts = NULL, TotalStudentsWithTwoConflicts = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysis", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerRunAnalysis
	#'
	#' This function modifies a StudentAutoSchedulerRunAnalysis
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerRunAnalysis <- function(StudentAutoSchedulerRunAnalysisID, AnalysisVersion = NULL, BaseRunAnalysisID = NULL, CloseSectionsWhenFilled = NULL, CreatedCourseAndSectionAnalysisDetails = NULL, CreatedStudentAnalysisDetails = NULL, Description = NULL, EndTimeAnalysis = NULL, EndTimeFinalize = NULL, EvenlyDistributeSchedulingTeams = NULL, FailedScheduledStudentSectionsCount = NULL, FailedStudentCourseRequestsCount = NULL, FailedStudentSectionsCount = NULL, FailedStudentSectionTransactionsCount = NULL, IncludedAbilityToAcceptProposedSchedules = NULL, PersistentSchedulingRunDataIsNoLongerAcceptable = NULL, ProcessSpecialEdCourses = NULL, RunInformation = NULL, SendMessageOnComplete = NULL, StartTimeAnalysis = NULL, StartTimeFinalize = NULL, StudentAutoSchedulerMode = NULL, StudentCourseRequestOrder = NULL, StudentDifficultyOrder = NULL, StudentEntityYearRangeXMLFilter = NULL, StudentInformation = NULL, SuccessfulStudentSectionsCount = NULL, TotalAlternateStudentCourseRequests = NULL, TotalConflictedStudentCourseRequests = NULL, TotalScheduledStudentCourseRequests = NULL, TotalStudentCourseRequests = NULL, TotalStudentCourseRequestsToFinalize = NULL, TotalStudents = NULL, TotalStudentSectionsToFinalize = NULL, TotalStudentSectionTransactionsToFinalize = NULL, TotalStudentsWithConflicts = NULL, TotalStudentsWithOneConflict = NULL, TotalStudentsWithThreeOrMoreConflicts = NULL, TotalStudentsWithTwoConflicts = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerRunAnalysis", objectId = StudentAutoSchedulerRunAnalysisID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentAutoSchedulerCourses
	#'
	#' This function returns a dataframe or json object of StudentAutoSchedulerCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentAutoSchedulerCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentAutoSchedulerCourses <- function(searchConditionsList = NULL, ActiveSections = F, CategoryCode = F, ConflictedRequestPercent = F, CourseCode = F, CourseDescription = F, CourseEntityCode = F, CourseEntityID = F, CourseID = F, CourseLengthCode = F, CourseSubjectCode = F, CreatedTime = F, DepartmentCode = F, IsActive = F, IsRequired = F, ModifiedTime = F, ScheduledRequestPercent = F, SchedulingPriorityCode = F, SchedulingTeamModeCode = F, SchedulingTypeCode = F, StudentAutoSchedulerCourseID = F, StudentAutoSchedulerRunAnalysisID = F, TotalAlternateRequests = F, TotalConflicts = F, TotalNumberScheduledThisRun = F, TotalRequests = F, TotalScheduled = F, TotalSeatsAvailable = F, TotalSeatsScheduled = F, TotalSections = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentAutoSchedulerCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentAutoSchedulerCourse
	#'
	#' This function returns a dataframe or json object of a StudentAutoSchedulerCourse
	#' @param StudentAutoSchedulerCourseID The ID of the StudentAutoSchedulerCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentAutoSchedulerCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentAutoSchedulerCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentAutoSchedulerCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentAutoSchedulerCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentAutoSchedulerCourse <- function(StudentAutoSchedulerCourseID, ActiveSections = F, CategoryCode = F, ConflictedRequestPercent = F, CourseCode = F, CourseDescription = F, CourseEntityCode = F, CourseEntityID = F, CourseID = F, CourseLengthCode = F, CourseSubjectCode = F, CreatedTime = F, DepartmentCode = F, IsActive = F, IsRequired = F, ModifiedTime = F, ScheduledRequestPercent = F, SchedulingPriorityCode = F, SchedulingTeamModeCode = F, SchedulingTypeCode = F, StudentAutoSchedulerRunAnalysisID = F, TotalAlternateRequests = F, TotalConflicts = F, TotalNumberScheduledThisRun = F, TotalRequests = F, TotalScheduled = F, TotalSeatsAvailable = F, TotalSeatsScheduled = F, TotalSections = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentAutoSchedulerCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerCourse", objectId = StudentAutoSchedulerCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentAutoSchedulerCourse
	#'
	#' This function deletes a StudentAutoSchedulerCourse
	#' @param StudentAutoSchedulerCourseID The ID of the StudentAutoSchedulerCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentAutoSchedulerCourseID of the deleted StudentAutoSchedulerCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentAutoSchedulerCourse <- function(StudentAutoSchedulerCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerCourse", objectId = StudentAutoSchedulerCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentAutoSchedulerCourse
	#'
	#' This function creates a StudentAutoSchedulerCourse
	#' @param fieldNames The field values to give the created StudentAutoSchedulerCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentAutoSchedulerCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentAutoSchedulerCourse <- function(CategoryCode = NULL, CourseCode = NULL, CourseDescription = NULL, CourseEntityCode = NULL, CourseEntityID = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseSubjectCode = NULL, DepartmentCode = NULL, IsActive = NULL, IsRequired = NULL, SchedulingPriorityCode = NULL, SchedulingTeamModeCode = NULL, SchedulingTypeCode = NULL, StudentAutoSchedulerRunAnalysisID = NULL, TotalAlternateRequests = NULL, TotalConflicts = NULL, TotalNumberScheduledThisRun = NULL, TotalRequests = NULL, TotalScheduled = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerCourse", body = list(DataObject = body), searchFields = append("StudentAutoSchedulerCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentAutoSchedulerCourse
	#'
	#' This function modifies a StudentAutoSchedulerCourse
	#' @param fieldNames The field values to give the modified StudentAutoSchedulerCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentAutoSchedulerCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentAutoSchedulerCourse <- function(StudentAutoSchedulerCourseID, CategoryCode = NULL, CourseCode = NULL, CourseDescription = NULL, CourseEntityCode = NULL, CourseEntityID = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseSubjectCode = NULL, DepartmentCode = NULL, IsActive = NULL, IsRequired = NULL, SchedulingPriorityCode = NULL, SchedulingTeamModeCode = NULL, SchedulingTypeCode = NULL, StudentAutoSchedulerRunAnalysisID = NULL, TotalAlternateRequests = NULL, TotalConflicts = NULL, TotalNumberScheduledThisRun = NULL, TotalRequests = NULL, TotalScheduled = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentAutoSchedulerCourse", objectId = StudentAutoSchedulerCourseID, body = list(DataObject = body), searchFields = append("StudentAutoSchedulerCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedStudentSections
	#'
	#' This function returns a dataframe or json object of TempFailedStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedStudentSections <- function(searchConditionsList = NULL, AutomaticRequestSetting = F, AutomaticScheduleSetting = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, EarlyExitReasonCodeDescription = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, GradeReferenceID = F, ModifiedTime = F, Note = F, RenderCheckbox = F, RowIsReadOnly = F, RowIsSelected = F, ScheduleAllSectionsInGroupOrNone = F, SchoolYearIDCourse = F, SectionCode = F, SectionCorequisiteGroupName = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentGenderCode = F, StudentGradeLevelCode = F, StudentGradYear = F, StudentID = F, StudentNameLFM = F, StudentNumber = F, StudentSectionID = F, StudentSectionTransactionIDToUpdate = F, TempFailedStudentSectionID = F, TempStudentID = F, TempStudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedStudentSection
	#'
	#' This function returns a dataframe or json object of a TempFailedStudentSection
	#' @param TempFailedStudentSectionID The ID of the TempFailedStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedStudentSection <- function(TempFailedStudentSectionID, AutomaticRequestSetting = F, AutomaticScheduleSetting = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, EarlyExitReasonCodeDescription = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, GradeReferenceID = F, ModifiedTime = F, Note = F, RenderCheckbox = F, RowIsReadOnly = F, RowIsSelected = F, ScheduleAllSectionsInGroupOrNone = F, SchoolYearIDCourse = F, SectionCode = F, SectionCorequisiteGroupName = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentGenderCode = F, StudentGradeLevelCode = F, StudentGradYear = F, StudentID = F, StudentNameLFM = F, StudentNumber = F, StudentSectionID = F, StudentSectionTransactionIDToUpdate = F, TempStudentID = F, TempStudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedStudentSection", objectId = TempFailedStudentSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedStudentSection
	#'
	#' This function deletes a TempFailedStudentSection
	#' @param TempFailedStudentSectionID The ID of the TempFailedStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedStudentSectionID of the deleted TempFailedStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedStudentSection <- function(TempFailedStudentSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedStudentSection", objectId = TempFailedStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedStudentSection
	#'
	#' This function creates a TempFailedStudentSection
	#' @param fieldNames The field values to give the created TempFailedStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedStudentSection <- function(AutomaticRequestSetting = NULL, AutomaticScheduleSetting = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, EarlyExitReasonCodeDescription = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, GradeReferenceID = NULL, Note = NULL, RenderCheckbox = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, ScheduleAllSectionsInGroupOrNone = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionCorequisiteGroupName = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentGenderCode = NULL, StudentGradeLevelCode = NULL, StudentGradYear = NULL, StudentID = NULL, StudentNameLFM = NULL, StudentNumber = NULL, StudentSectionID = NULL, StudentSectionTransactionIDToUpdate = NULL, TempStudentID = NULL, TempStudentSectionID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedStudentSection", body = list(DataObject = body), searchFields = append("TempFailedStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedStudentSection
	#'
	#' This function modifies a TempFailedStudentSection
	#' @param fieldNames The field values to give the modified TempFailedStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedStudentSection <- function(TempFailedStudentSectionID, AutomaticRequestSetting = NULL, AutomaticScheduleSetting = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, EarlyExitReasonCodeDescription = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, GradeReferenceID = NULL, Note = NULL, RenderCheckbox = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, ScheduleAllSectionsInGroupOrNone = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionCorequisiteGroupName = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentGenderCode = NULL, StudentGradeLevelCode = NULL, StudentGradYear = NULL, StudentID = NULL, StudentNameLFM = NULL, StudentNumber = NULL, StudentSectionID = NULL, StudentSectionTransactionIDToUpdate = NULL, TempStudentID = NULL, TempStudentSectionID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedStudentSection", objectId = TempFailedStudentSectionID, body = list(DataObject = body), searchFields = append("TempFailedStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentCourseRequestSectionLengthSubsets
	#'
	#' This function returns a dataframe or json object of StudentCourseRequestSectionLengthSubsets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCourseRequestSectionLengthSubsets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCourseRequestSectionLengthSubsets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCourseRequestSectionLengthSubset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentCourseRequestSectionLengthSubsets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentCourseRequestSectionLengthSubsets <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SectionLengthSubsetID = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentCourseRequestSectionLengthSubset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentCourseRequestSectionLengthSubset
	#'
	#' This function returns a dataframe or json object of a StudentCourseRequestSectionLengthSubset
	#' @param StudentCourseRequestSectionLengthSubsetID The ID of the StudentCourseRequestSectionLengthSubset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCourseRequestSectionLengthSubset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCourseRequestSectionLengthSubset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCourseRequestSectionLengthSubset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentCourseRequestSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentCourseRequestSectionLengthSubset <- function(StudentCourseRequestSectionLengthSubsetID, CreatedTime = F, ModifiedTime = F, SectionLengthSubsetID = F, StudentCourseRequestID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentCourseRequestSectionLengthSubsetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentCourseRequestSectionLengthSubset", objectId = StudentCourseRequestSectionLengthSubsetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentCourseRequestSectionLengthSubset
	#'
	#' This function deletes a StudentCourseRequestSectionLengthSubset
	#' @param StudentCourseRequestSectionLengthSubsetID The ID of the StudentCourseRequestSectionLengthSubset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentCourseRequestSectionLengthSubsetID of the deleted StudentCourseRequestSectionLengthSubset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentCourseRequestSectionLengthSubset <- function(StudentCourseRequestSectionLengthSubsetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentCourseRequestSectionLengthSubset", objectId = StudentCourseRequestSectionLengthSubsetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentCourseRequestSectionLengthSubset
	#'
	#' This function creates a StudentCourseRequestSectionLengthSubset
	#' @param fieldNames The field values to give the created StudentCourseRequestSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentCourseRequestSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentCourseRequestSectionLengthSubset <- function(SectionLengthSubsetID = NULL, StudentCourseRequestID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentCourseRequestSectionLengthSubset", body = list(DataObject = body), searchFields = append("StudentCourseRequestSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentCourseRequestSectionLengthSubset
	#'
	#' This function modifies a StudentCourseRequestSectionLengthSubset
	#' @param fieldNames The field values to give the modified StudentCourseRequestSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentCourseRequestSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentCourseRequestSectionLengthSubset <- function(StudentCourseRequestSectionLengthSubsetID, SectionLengthSubsetID = NULL, StudentCourseRequestID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentCourseRequestSectionLengthSubset", objectId = StudentCourseRequestSectionLengthSubsetID, body = list(DataObject = body), searchFields = append("StudentCourseRequestSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedStudentCourseRequests
	#'
	#' This function returns a dataframe or json object of TempFailedStudentCourseRequests
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentCourseRequests. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentCourseRequests.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentCourseRequest') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedStudentCourseRequests
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedStudentCourseRequests <- function(searchConditionsList = NULL, CourseCode = F, CourseDepartmentCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CourseLengthCode = F, CourseNumericSchoolYear = F, CourseSchoolYearDescription = F, CourseSubjectCodeDescription = F, CreatedTime = F, EarnedCredits = F, EntityIDRequestedFrom = F, ErrorMessage = F, Failed = F, FullStudentNameLFM = F, ModifiedTime = F, Note = F, SectionCode = F, SectionlengthSubsetCode = F, SectionLengthSubsetID = F, Selected = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, StudentID = F, StudentNumber = F, StudentSectionID = F, TempFailedStudentCourseRequestID = F, TempStudentCourseRequestID = F, TempStudentEnrollmentRecordID = F, TempStudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedStudentCourseRequest", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedStudentCourseRequest
	#'
	#' This function returns a dataframe or json object of a TempFailedStudentCourseRequest
	#' @param TempFailedStudentCourseRequestID The ID of the TempFailedStudentCourseRequest to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentCourseRequest. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentCourseRequest.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentCourseRequest') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedStudentCourseRequest <- function(TempFailedStudentCourseRequestID, CourseCode = F, CourseDepartmentCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CourseLengthCode = F, CourseNumericSchoolYear = F, CourseSchoolYearDescription = F, CourseSubjectCodeDescription = F, CreatedTime = F, EarnedCredits = F, EntityIDRequestedFrom = F, ErrorMessage = F, Failed = F, FullStudentNameLFM = F, ModifiedTime = F, Note = F, SectionCode = F, SectionlengthSubsetCode = F, SectionLengthSubsetID = F, Selected = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, StudentID = F, StudentNumber = F, StudentSectionID = F, TempStudentCourseRequestID = F, TempStudentEnrollmentRecordID = F, TempStudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedStudentCourseRequestID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequest", objectId = TempFailedStudentCourseRequestID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedStudentCourseRequest
	#'
	#' This function deletes a TempFailedStudentCourseRequest
	#' @param TempFailedStudentCourseRequestID The ID of the TempFailedStudentCourseRequest to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedStudentCourseRequestID of the deleted TempFailedStudentCourseRequest.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedStudentCourseRequest <- function(TempFailedStudentCourseRequestID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequest", objectId = TempFailedStudentCourseRequestID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedStudentCourseRequest
	#'
	#' This function creates a TempFailedStudentCourseRequest
	#' @param fieldNames The field values to give the created TempFailedStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedStudentCourseRequest <- function(CourseCode = NULL, CourseDepartmentCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseNumericSchoolYear = NULL, CourseSchoolYearDescription = NULL, CourseSubjectCodeDescription = NULL, EarnedCredits = NULL, EntityIDRequestedFrom = NULL, ErrorMessage = NULL, Failed = NULL, FullStudentNameLFM = NULL, Note = NULL, SectionCode = NULL, SectionlengthSubsetCode = NULL, SectionLengthSubsetID = NULL, Selected = NULL, StudentCourseRequestID = NULL, StudentCourseRequestSectionLengthSubsetID = NULL, StudentID = NULL, StudentNumber = NULL, StudentSectionID = NULL, TempStudentCourseRequestID = NULL, TempStudentEnrollmentRecordID = NULL, TempStudentID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequest", body = list(DataObject = body), searchFields = append("TempFailedStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedStudentCourseRequest
	#'
	#' This function modifies a TempFailedStudentCourseRequest
	#' @param fieldNames The field values to give the modified TempFailedStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedStudentCourseRequest <- function(TempFailedStudentCourseRequestID, CourseCode = NULL, CourseDepartmentCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseNumericSchoolYear = NULL, CourseSchoolYearDescription = NULL, CourseSubjectCodeDescription = NULL, EarnedCredits = NULL, EntityIDRequestedFrom = NULL, ErrorMessage = NULL, Failed = NULL, FullStudentNameLFM = NULL, Note = NULL, SectionCode = NULL, SectionlengthSubsetCode = NULL, SectionLengthSubsetID = NULL, Selected = NULL, StudentCourseRequestID = NULL, StudentCourseRequestSectionLengthSubsetID = NULL, StudentID = NULL, StudentNumber = NULL, StudentSectionID = NULL, TempStudentCourseRequestID = NULL, TempStudentEnrollmentRecordID = NULL, TempStudentID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequest", objectId = TempFailedStudentCourseRequestID, body = list(DataObject = body), searchFields = append("TempFailedStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentCourseRequests
	#'
	#' This function returns a dataframe or json object of TempStudentCourseRequests
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequests. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequests.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequest') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentCourseRequests
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentCourseRequests <- function(searchConditionsList = NULL, CourseCode = F, CourseDepartmentCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CourseLengthCode = F, CourseNumericSchoolYear = F, CourseSchoolYearDescription = F, CourseSubjectCodeDescription = F, CreatedTime = F, EarnedCredits = F, EntityIDRequestedFrom = F, ErrorMessage = F, Failed = F, FullStudentNameLFM = F, ModifiedTime = F, SectionCode = F, SectionlengthSubsetCode = F, SectionLengthSubsetID = F, Selected = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, StudentID = F, StudentNumber = F, StudentSectionID = F, TempStudentCourseRequestID = F, TempStudentEnrollmentRecordID = F, TempStudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentCourseRequest", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentCourseRequest
	#'
	#' This function returns a dataframe or json object of a TempStudentCourseRequest
	#' @param TempStudentCourseRequestID The ID of the TempStudentCourseRequest to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequest. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequest.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequest') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentCourseRequest <- function(TempStudentCourseRequestID, CourseCode = F, CourseDepartmentCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CourseLengthCode = F, CourseNumericSchoolYear = F, CourseSchoolYearDescription = F, CourseSubjectCodeDescription = F, CreatedTime = F, EarnedCredits = F, EntityIDRequestedFrom = F, ErrorMessage = F, Failed = F, FullStudentNameLFM = F, ModifiedTime = F, SectionCode = F, SectionlengthSubsetCode = F, SectionLengthSubsetID = F, Selected = F, StudentCourseRequestID = F, StudentCourseRequestSectionLengthSubsetID = F, StudentID = F, StudentNumber = F, StudentSectionID = F, TempStudentEnrollmentRecordID = F, TempStudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentCourseRequestID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequest", objectId = TempStudentCourseRequestID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentCourseRequest
	#'
	#' This function deletes a TempStudentCourseRequest
	#' @param TempStudentCourseRequestID The ID of the TempStudentCourseRequest to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentCourseRequestID of the deleted TempStudentCourseRequest.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentCourseRequest <- function(TempStudentCourseRequestID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequest", objectId = TempStudentCourseRequestID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentCourseRequest
	#'
	#' This function creates a TempStudentCourseRequest
	#' @param fieldNames The field values to give the created TempStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentCourseRequest <- function(CourseCode = NULL, CourseDepartmentCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseNumericSchoolYear = NULL, CourseSchoolYearDescription = NULL, CourseSubjectCodeDescription = NULL, EarnedCredits = NULL, EntityIDRequestedFrom = NULL, ErrorMessage = NULL, Failed = NULL, FullStudentNameLFM = NULL, SectionCode = NULL, SectionlengthSubsetCode = NULL, SectionLengthSubsetID = NULL, Selected = NULL, StudentCourseRequestID = NULL, StudentCourseRequestSectionLengthSubsetID = NULL, StudentID = NULL, StudentNumber = NULL, StudentSectionID = NULL, TempStudentEnrollmentRecordID = NULL, TempStudentID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequest", body = list(DataObject = body), searchFields = append("TempStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentCourseRequest
	#'
	#' This function modifies a TempStudentCourseRequest
	#' @param fieldNames The field values to give the modified TempStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentCourseRequest <- function(TempStudentCourseRequestID, CourseCode = NULL, CourseDepartmentCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CourseLengthCode = NULL, CourseNumericSchoolYear = NULL, CourseSchoolYearDescription = NULL, CourseSubjectCodeDescription = NULL, EarnedCredits = NULL, EntityIDRequestedFrom = NULL, ErrorMessage = NULL, Failed = NULL, FullStudentNameLFM = NULL, SectionCode = NULL, SectionlengthSubsetCode = NULL, SectionLengthSubsetID = NULL, Selected = NULL, StudentCourseRequestID = NULL, StudentCourseRequestSectionLengthSubsetID = NULL, StudentID = NULL, StudentNumber = NULL, StudentSectionID = NULL, TempStudentEnrollmentRecordID = NULL, TempStudentID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentCourseRequest", objectId = TempStudentCourseRequestID, body = list(DataObject = body), searchFields = append("TempStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionLengths
	#'
	#' This function returns a dataframe or json object of TempSectionLengths
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionLengths. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionLengths.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionLength') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempSectionLengths
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionLengths <- function(searchConditionsList = NULL, Code = F, CourseLengthCode = F, CreatedTime = F, EndDate = F, IsUpdated = F, ModifiedTime = F, OriginalEndDate = F, OriginalStartDate = F, ProcessAction = F, SectionLengthID = F, StartDate = F, TempSectionLengthID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempSectionLength", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionLength
	#'
	#' This function returns a dataframe or json object of a TempSectionLength
	#' @param TempSectionLengthID The ID of the TempSectionLength to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionLength. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionLength.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionLength') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempSectionLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionLength <- function(TempSectionLengthID, Code = F, CourseLengthCode = F, CreatedTime = F, EndDate = F, IsUpdated = F, ModifiedTime = F, OriginalEndDate = F, OriginalStartDate = F, ProcessAction = F, SectionLengthID = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionLengthID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempSectionLength", objectId = TempSectionLengthID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionLength
	#'
	#' This function deletes a TempSectionLength
	#' @param TempSectionLengthID The ID of the TempSectionLength to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempSectionLengthID of the deleted TempSectionLength.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionLength <- function(TempSectionLengthID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempSectionLength", objectId = TempSectionLengthID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionLength
	#'
	#' This function creates a TempSectionLength
	#' @param fieldNames The field values to give the created TempSectionLength. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempSectionLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionLength <- function(Code = NULL, CourseLengthCode = NULL, EndDate = NULL, OriginalEndDate = NULL, OriginalStartDate = NULL, ProcessAction = NULL, SectionLengthID = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempSectionLength", body = list(DataObject = body), searchFields = append("TempSectionLengthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionLength
	#'
	#' This function modifies a TempSectionLength
	#' @param fieldNames The field values to give the modified TempSectionLength. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempSectionLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionLength <- function(TempSectionLengthID, Code = NULL, CourseLengthCode = NULL, EndDate = NULL, OriginalEndDate = NULL, OriginalStartDate = NULL, ProcessAction = NULL, SectionLengthID = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempSectionLength", objectId = TempSectionLengthID, body = list(DataObject = body), searchFields = append("TempSectionLengthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGradeReferences
	#'
	#' This function returns a dataframe or json object of CourseGradeReferences
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradeReferences. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradeReferences.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradeReference') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseGradeReferences
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGradeReferences <- function(searchConditionsList = NULL, CourseGradeReferenceID = F, CourseGradeReferenceIDClonedFrom = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, GradeReferenceID = F, ModifiedTime = F, NumberOfAlternateCourseRequests = F, NumberOfAlternateCourseRequestsEntity = F, NumberOfCourseRequests = F, NumberOfCourseRequestsEntity = F, NumberOfCourseRequestsFemales = F, NumberOfCourseRequestsFemalesEntity = F, NumberOfCourseRequestsMales = F, NumberOfCourseRequestsMalesEntity = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseGradeReference", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGradeReference
	#'
	#' This function returns a dataframe or json object of a CourseGradeReference
	#' @param CourseGradeReferenceID The ID of the CourseGradeReference to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradeReference. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradeReference.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradeReference') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGradeReference <- function(CourseGradeReferenceID, CourseGradeReferenceIDClonedFrom = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, GradeReferenceID = F, ModifiedTime = F, NumberOfAlternateCourseRequests = F, NumberOfAlternateCourseRequestsEntity = F, NumberOfCourseRequests = F, NumberOfCourseRequestsEntity = F, NumberOfCourseRequestsFemales = F, NumberOfCourseRequestsFemalesEntity = F, NumberOfCourseRequestsMales = F, NumberOfCourseRequestsMalesEntity = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGradeReferenceID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseGradeReference", objectId = CourseGradeReferenceID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGradeReference
	#'
	#' This function deletes a CourseGradeReference
	#' @param CourseGradeReferenceID The ID of the CourseGradeReference to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseGradeReferenceID of the deleted CourseGradeReference.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGradeReference <- function(CourseGradeReferenceID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseGradeReference", objectId = CourseGradeReferenceID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGradeReference
	#'
	#' This function creates a CourseGradeReference
	#' @param fieldNames The field values to give the created CourseGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGradeReference <- function(CourseGradeReferenceIDClonedFrom = NULL, CourseID = NULL, EntityGroupKey = NULL, GradeReferenceID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseGradeReference", body = list(DataObject = body), searchFields = append("CourseGradeReferenceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGradeReference
	#'
	#' This function modifies a CourseGradeReference
	#' @param fieldNames The field values to give the modified CourseGradeReference. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseGradeReference
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGradeReference <- function(CourseGradeReferenceID, CourseGradeReferenceIDClonedFrom = NULL, CourseID = NULL, EntityGroupKey = NULL, GradeReferenceID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseGradeReference", objectId = CourseGradeReferenceID, body = list(DataObject = body), searchFields = append("CourseGradeReferenceID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSectionLengthSubsets
	#'
	#' This function returns a dataframe or json object of TempSectionLengthSubsets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionLengthSubsets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionLengthSubsets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionLengthSubset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempSectionLengthSubsets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSectionLengthSubsets <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseLengthCode = F, CourseLengthCodeDescription = F, CourseLengthID = F, CreatedTime = F, Description = F, EndDate = F, IsFullSectionLength = F, IsUpdated = F, ModifiedTime = F, ObjectIsDirty = F, OriginalEndDate = F, OriginalStartDate = F, ProcessAction = F, SectionLengthCode = F, SectionLengthCodeDescription = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, TempSectionLengthSubsetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempSectionLengthSubset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSectionLengthSubset
	#'
	#' This function returns a dataframe or json object of a TempSectionLengthSubset
	#' @param TempSectionLengthSubsetID The ID of the TempSectionLengthSubset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSectionLengthSubset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSectionLengthSubset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSectionLengthSubset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSectionLengthSubset <- function(TempSectionLengthSubsetID, Code = F, CodeDescription = F, CourseLengthCode = F, CourseLengthCodeDescription = F, CourseLengthID = F, CreatedTime = F, Description = F, EndDate = F, IsFullSectionLength = F, IsUpdated = F, ModifiedTime = F, ObjectIsDirty = F, OriginalEndDate = F, OriginalStartDate = F, ProcessAction = F, SectionLengthCode = F, SectionLengthCodeDescription = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSectionLengthSubsetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempSectionLengthSubset", objectId = TempSectionLengthSubsetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSectionLengthSubset
	#'
	#' This function deletes a TempSectionLengthSubset
	#' @param TempSectionLengthSubsetID The ID of the TempSectionLengthSubset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempSectionLengthSubsetID of the deleted TempSectionLengthSubset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSectionLengthSubset <- function(TempSectionLengthSubsetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempSectionLengthSubset", objectId = TempSectionLengthSubsetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSectionLengthSubset
	#'
	#' This function creates a TempSectionLengthSubset
	#' @param fieldNames The field values to give the created TempSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSectionLengthSubset <- function(Code = NULL, CodeDescription = NULL, CourseLengthCode = NULL, CourseLengthCodeDescription = NULL, CourseLengthID = NULL, Description = NULL, EndDate = NULL, IsFullSectionLength = NULL, ObjectIsDirty = NULL, OriginalEndDate = NULL, OriginalStartDate = NULL, ProcessAction = NULL, SectionLengthCode = NULL, SectionLengthCodeDescription = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempSectionLengthSubset", body = list(DataObject = body), searchFields = append("TempSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSectionLengthSubset
	#'
	#' This function modifies a TempSectionLengthSubset
	#' @param fieldNames The field values to give the modified TempSectionLengthSubset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSectionLengthSubset <- function(TempSectionLengthSubsetID, Code = NULL, CodeDescription = NULL, CourseLengthCode = NULL, CourseLengthCodeDescription = NULL, CourseLengthID = NULL, Description = NULL, EndDate = NULL, IsFullSectionLength = NULL, ObjectIsDirty = NULL, OriginalEndDate = NULL, OriginalStartDate = NULL, ProcessAction = NULL, SectionLengthCode = NULL, SectionLengthCodeDescription = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempSectionLengthSubset", objectId = TempSectionLengthSubsetID, body = list(DataObject = body), searchFields = append("TempSectionLengthSubsetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingTempStudentSections
	#'
	#' This function returns a dataframe or json object of SchedulingTempStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTempStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTempStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTempStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingTempStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingTempStudentSections <- function(searchConditionsList = NULL, AutomaticRequestSetting = F, AutomaticScheduleSetting = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, EarlyExitReasonCodeDescription = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, GradeReferenceID = F, ModifiedTime = F, Note = F, RenderCheckbox = F, RowIsReadOnly = F, RowIsSelected = F, ScheduleAllSectionsInGroupOrNone = F, SchoolYearIDCourse = F, SectionCode = F, SectionCorequisiteGroupName = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentGenderCode = F, StudentGradeLevelCode = F, StudentGradYear = F, StudentID = F, StudentNameLFM = F, StudentNumber = F, StudentSectionID = F, StudentSectionTransactionIDToUpdate = F, TempStudentID = F, TempStudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingTempStudentSection
	#'
	#' This function returns a dataframe or json object of a SchedulingTempStudentSection
	#' @param SchedulingTempStudentSectionID The ID of the SchedulingTempStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTempStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTempStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTempStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingTempStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingTempStudentSection <- function(SchedulingTempStudentSectionID, AutomaticRequestSetting = F, AutomaticScheduleSetting = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, EarlyExitReasonCodeDescription = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, GradeReferenceID = F, ModifiedTime = F, Note = F, RenderCheckbox = F, RowIsReadOnly = F, RowIsSelected = F, ScheduleAllSectionsInGroupOrNone = F, SchoolYearIDCourse = F, SectionCode = F, SectionCorequisiteGroupName = F, SectionID = F, SectionLengthCode = F, SectionLengthEndDate = F, SectionLengthID = F, SectionLengthStartDate = F, SectionLengthSubsetID = F, StartDate = F, StudentCourseRequestID = F, StudentGenderCode = F, StudentGradeLevelCode = F, StudentGradYear = F, StudentID = F, StudentNameLFM = F, StudentNumber = F, StudentSectionID = F, StudentSectionTransactionIDToUpdate = F, TempStudentID = F, TempStudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingTempStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentSection", objectId = SchedulingTempStudentSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingTempStudentSection
	#'
	#' This function deletes a SchedulingTempStudentSection
	#' @param SchedulingTempStudentSectionID The ID of the SchedulingTempStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingTempStudentSectionID of the deleted SchedulingTempStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingTempStudentSection <- function(SchedulingTempStudentSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentSection", objectId = SchedulingTempStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingTempStudentSection
	#'
	#' This function creates a SchedulingTempStudentSection
	#' @param fieldNames The field values to give the created SchedulingTempStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingTempStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingTempStudentSection <- function(AutomaticRequestSetting = NULL, AutomaticScheduleSetting = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, EarlyExitReasonCodeDescription = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, GradeReferenceID = NULL, Note = NULL, RenderCheckbox = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, ScheduleAllSectionsInGroupOrNone = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionCorequisiteGroupName = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentGenderCode = NULL, StudentGradeLevelCode = NULL, StudentGradYear = NULL, StudentID = NULL, StudentNameLFM = NULL, StudentNumber = NULL, StudentSectionID = NULL, StudentSectionTransactionIDToUpdate = NULL, TempStudentID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentSection", body = list(DataObject = body), searchFields = append("TempStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingTempStudentSection
	#'
	#' This function modifies a SchedulingTempStudentSection
	#' @param fieldNames The field values to give the modified SchedulingTempStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingTempStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingTempStudentSection <- function(TempStudentSectionID, AutomaticRequestSetting = NULL, AutomaticScheduleSetting = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, EarlyExitReasonCodeDescription = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, GradeReferenceID = NULL, Note = NULL, RenderCheckbox = NULL, RowIsReadOnly = NULL, RowIsSelected = NULL, ScheduleAllSectionsInGroupOrNone = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionCorequisiteGroupName = NULL, SectionID = NULL, SectionLengthCode = NULL, SectionLengthEndDate = NULL, SectionLengthID = NULL, SectionLengthStartDate = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentGenderCode = NULL, StudentGradeLevelCode = NULL, StudentGradYear = NULL, StudentID = NULL, StudentNameLFM = NULL, StudentNumber = NULL, StudentSectionID = NULL, StudentSectionTransactionIDToUpdate = NULL, TempStudentID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentSection", objectId = TempStudentSectionID, body = list(DataObject = body), searchFields = append("TempStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EarlyExitReasons
	#'
	#' This function returns a dataframe or json object of EarlyExitReasons
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarlyExitReasons. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarlyExitReasons.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarlyExitReason') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of EarlyExitReasons
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEarlyExitReasons <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EarlyExitReasonID = F, EarlyExitReasonIDClonedFrom = F, EntityGroupKey = F, EntityID = F, IsConsideredDrop = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "EarlyExitReason", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EarlyExitReason
	#'
	#' This function returns a dataframe or json object of an EarlyExitReason
	#' @param EarlyExitReasonID The ID of the EarlyExitReason to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EarlyExitReason. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EarlyExitReason.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EarlyExitReason') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of EarlyExitReason
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEarlyExitReason <- function(EarlyExitReasonID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EarlyExitReasonIDClonedFrom = F, EntityGroupKey = F, EntityID = F, IsConsideredDrop = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EarlyExitReasonID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "EarlyExitReason", objectId = EarlyExitReasonID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EarlyExitReason
	#'
	#' This function deletes an EarlyExitReason
	#' @param EarlyExitReasonID The ID of the EarlyExitReason to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The EarlyExitReasonID of the deleted EarlyExitReason.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEarlyExitReason <- function(EarlyExitReasonID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "EarlyExitReason", objectId = EarlyExitReasonID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EarlyExitReason
	#'
	#' This function creates an EarlyExitReason
	#' @param fieldNames The field values to give the created EarlyExitReason. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created EarlyExitReason
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEarlyExitReason <- function(Code = NULL, Description = NULL, EarlyExitReasonIDClonedFrom = NULL, EntityGroupKey = NULL, EntityID = NULL, IsConsideredDrop = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "EarlyExitReason", body = list(DataObject = body), searchFields = append("EarlyExitReasonID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EarlyExitReason
	#'
	#' This function modifies an EarlyExitReason
	#' @param fieldNames The field values to give the modified EarlyExitReason. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified EarlyExitReason
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEarlyExitReason <- function(EarlyExitReasonID, Code = NULL, Description = NULL, EarlyExitReasonIDClonedFrom = NULL, EntityGroupKey = NULL, EntityID = NULL, IsConsideredDrop = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "EarlyExitReason", objectId = EarlyExitReasonID, body = list(DataObject = body), searchFields = append("EarlyExitReasonID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulingTeams
	#'
	#' This function returns a dataframe or json object of SectionSchedulingTeams
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulingTeams. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulingTeams.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulingTeam') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulingTeams
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulingTeams <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SchedulingTeamID = F, SectionID = F, SectionSchedulingTeamID = F, SectionSchedulingTeamIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulingTeam", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulingTeam
	#'
	#' This function returns a dataframe or json object of a SectionSchedulingTeam
	#' @param SectionSchedulingTeamID The ID of the SectionSchedulingTeam to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulingTeam. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulingTeam.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulingTeam') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulingTeam <- function(SectionSchedulingTeamID, CreatedTime = F, ModifiedTime = F, SchedulingTeamID = F, SectionID = F, SectionSchedulingTeamIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulingTeamID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulingTeam", objectId = SectionSchedulingTeamID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulingTeam
	#'
	#' This function deletes a SectionSchedulingTeam
	#' @param SectionSchedulingTeamID The ID of the SectionSchedulingTeam to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulingTeamID of the deleted SectionSchedulingTeam.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulingTeam <- function(SectionSchedulingTeamID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulingTeam", objectId = SectionSchedulingTeamID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulingTeam
	#'
	#' This function creates a SectionSchedulingTeam
	#' @param fieldNames The field values to give the created SectionSchedulingTeam. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulingTeam <- function(SchedulingTeamID = NULL, SectionID = NULL, SectionSchedulingTeamIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulingTeam", body = list(DataObject = body), searchFields = append("SectionSchedulingTeamID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulingTeam
	#'
	#' This function modifies a SectionSchedulingTeam
	#' @param fieldNames The field values to give the modified SectionSchedulingTeam. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulingTeam <- function(SectionSchedulingTeamID, SchedulingTeamID = NULL, SectionID = NULL, SectionSchedulingTeamIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulingTeam", objectId = SectionSchedulingTeamID, body = list(DataObject = body), searchFields = append("SectionSchedulingTeamID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DateRangePresets
	#'
	#' This function returns a dataframe or json object of DateRangePresets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DateRangePresets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DateRangePresets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DateRangePreset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of DateRangePresets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDateRangePresets <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, DateRangePresetID = F, DateRangePresetIDClonedFrom = F, DateRangePresetIDClonedTo = F, Description = F, EntityGroupKey = F, EntityID = F, HighDate = F, LowDate = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "DateRangePreset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DateRangePreset
	#'
	#' This function returns a dataframe or json object of a DateRangePreset
	#' @param DateRangePresetID The ID of the DateRangePreset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DateRangePreset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DateRangePreset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DateRangePreset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of DateRangePreset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDateRangePreset <- function(DateRangePresetID, Code = F, CodeDescription = F, CreatedTime = F, DateRangePresetIDClonedFrom = F, DateRangePresetIDClonedTo = F, Description = F, EntityGroupKey = F, EntityID = F, HighDate = F, LowDate = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DateRangePresetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "DateRangePreset", objectId = DateRangePresetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DateRangePreset
	#'
	#' This function deletes a DateRangePreset
	#' @param DateRangePresetID The ID of the DateRangePreset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The DateRangePresetID of the deleted DateRangePreset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDateRangePreset <- function(DateRangePresetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "DateRangePreset", objectId = DateRangePresetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DateRangePreset
	#'
	#' This function creates a DateRangePreset
	#' @param fieldNames The field values to give the created DateRangePreset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created DateRangePreset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDateRangePreset <- function(Code = NULL, DateRangePresetIDClonedFrom = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, HighDate = NULL, LowDate = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "DateRangePreset", body = list(DataObject = body), searchFields = append("DateRangePresetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DateRangePreset
	#'
	#' This function modifies a DateRangePreset
	#' @param fieldNames The field values to give the modified DateRangePreset. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified DateRangePreset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDateRangePreset <- function(DateRangePresetID, Code = NULL, DateRangePresetIDClonedFrom = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, HighDate = NULL, LowDate = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "DateRangePreset", objectId = DateRangePresetID, body = list(DataObject = body), searchFields = append("DateRangePresetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityFilterCourseStudents
	#'
	#' This function returns a dataframe or json object of AvailabilityFilterCourseStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityFilterCourseStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityFilterCourseStudents <- function(searchConditionsList = NULL, ArenaSchedulingStartDate = F, AvailabilityCourseFilterID = F, AvailabilityFilterCourseStudentID = F, AvailabilityFilterCourseStudentIDClonedFrom = F, AvailabilityStudentFilterID = F, CourseRequestStartDate = F, CreatedTime = F, Description = F, ExcludeAvailabilityListBoolUpdatedManually = F, ExcludeAvailabilityListInNightlyUpdateTask = F, ModifiedTime = F, UseForArenaScheduling = F, UseForCourseRequests = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityFilterCourseStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityFilterCourseStudent
	#'
	#' This function returns a dataframe or json object of an AvailabilityFilterCourseStudent
	#' @param AvailabilityFilterCourseStudentID The ID of the AvailabilityFilterCourseStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityFilterCourseStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityFilterCourseStudent <- function(AvailabilityFilterCourseStudentID, ArenaSchedulingStartDate = F, AvailabilityCourseFilterID = F, AvailabilityFilterCourseStudentIDClonedFrom = F, AvailabilityStudentFilterID = F, CourseRequestStartDate = F, CreatedTime = F, Description = F, ExcludeAvailabilityListBoolUpdatedManually = F, ExcludeAvailabilityListInNightlyUpdateTask = F, ModifiedTime = F, UseForArenaScheduling = F, UseForCourseRequests = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityFilterCourseStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudent", objectId = AvailabilityFilterCourseStudentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityFilterCourseStudent
	#'
	#' This function deletes an AvailabilityFilterCourseStudent
	#' @param AvailabilityFilterCourseStudentID The ID of the AvailabilityFilterCourseStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityFilterCourseStudentID of the deleted AvailabilityFilterCourseStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityFilterCourseStudent <- function(AvailabilityFilterCourseStudentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudent", objectId = AvailabilityFilterCourseStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityFilterCourseStudent
	#'
	#' This function creates an AvailabilityFilterCourseStudent
	#' @param fieldNames The field values to give the created AvailabilityFilterCourseStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityFilterCourseStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityFilterCourseStudent <- function(AvailabilityCourseFilterID = NULL, AvailabilityFilterCourseStudentIDClonedFrom = NULL, AvailabilityStudentFilterID = NULL, Description = NULL, ExcludeAvailabilityListBoolUpdatedManually = NULL, ExcludeAvailabilityListInNightlyUpdateTask = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudent", body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityFilterCourseStudent
	#'
	#' This function modifies an AvailabilityFilterCourseStudent
	#' @param fieldNames The field values to give the modified AvailabilityFilterCourseStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityFilterCourseStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityFilterCourseStudent <- function(AvailabilityFilterCourseStudentID, AvailabilityCourseFilterID = NULL, AvailabilityFilterCourseStudentIDClonedFrom = NULL, AvailabilityStudentFilterID = NULL, Description = NULL, ExcludeAvailabilityListBoolUpdatedManually = NULL, ExcludeAvailabilityListInNightlyUpdateTask = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudent", objectId = AvailabilityFilterCourseStudentID, body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityStudentFilters
	#'
	#' This function returns a dataframe or json object of AvailabilityStudentFilters
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityStudentFilters. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityStudentFilters.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityStudentFilter') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityStudentFilters
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityStudentFilters <- function(searchConditionsList = NULL, AvailabilityStudentFilterID = F, AvailabilityStudentFilterIDClonedFrom = F, AvailabilityStudentFilterIDClonedTo = F, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityID = F, Filter = F, GradeReferenceIDInclusionList = F, ModifiedTime = F, SchoolYearID = F, SkywardID = F, StudentTypeIDInclusionList = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityStudentFilter", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityStudentFilter
	#'
	#' This function returns a dataframe or json object of an AvailabilityStudentFilter
	#' @param AvailabilityStudentFilterID The ID of the AvailabilityStudentFilter to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityStudentFilter. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityStudentFilter.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityStudentFilter') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityStudentFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityStudentFilter <- function(AvailabilityStudentFilterID, AvailabilityStudentFilterIDClonedFrom = F, AvailabilityStudentFilterIDClonedTo = F, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityID = F, Filter = F, GradeReferenceIDInclusionList = F, ModifiedTime = F, SchoolYearID = F, SkywardID = F, StudentTypeIDInclusionList = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityStudentFilterID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityStudentFilter", objectId = AvailabilityStudentFilterID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityStudentFilter
	#'
	#' This function deletes an AvailabilityStudentFilter
	#' @param AvailabilityStudentFilterID The ID of the AvailabilityStudentFilter to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityStudentFilterID of the deleted AvailabilityStudentFilter.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityStudentFilter <- function(AvailabilityStudentFilterID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityStudentFilter", objectId = AvailabilityStudentFilterID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityStudentFilter
	#'
	#' This function creates an AvailabilityStudentFilter
	#' @param fieldNames The field values to give the created AvailabilityStudentFilter. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityStudentFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityStudentFilter <- function(AvailabilityStudentFilterIDClonedFrom = NULL, Code = NULL, Description = NULL, EntityID = NULL, Filter = NULL, GradeReferenceIDInclusionList = NULL, SchoolYearID = NULL, StudentTypeIDInclusionList = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityStudentFilter", body = list(DataObject = body), searchFields = append("AvailabilityStudentFilterID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityStudentFilter
	#'
	#' This function modifies an AvailabilityStudentFilter
	#' @param fieldNames The field values to give the modified AvailabilityStudentFilter. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityStudentFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityStudentFilter <- function(AvailabilityStudentFilterID, AvailabilityStudentFilterIDClonedFrom = NULL, Code = NULL, Description = NULL, EntityID = NULL, Filter = NULL, GradeReferenceIDInclusionList = NULL, SchoolYearID = NULL, StudentTypeIDInclusionList = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityStudentFilter", objectId = AvailabilityStudentFilterID, body = list(DataObject = body), searchFields = append("AvailabilityStudentFilterID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityCourseFilters
	#'
	#' This function returns a dataframe or json object of AvailabilityCourseFilters
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityCourseFilters. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityCourseFilters.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityCourseFilter') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityCourseFilters
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityCourseFilters <- function(searchConditionsList = NULL, AvailabilityCourseFilterID = F, AvailabilityCourseFilterIDClonedFrom = F, AvailabilityCourseFilterIDClonedTo = F, Code = F, CodeDescription = F, CourseTypeIDInclusionList = F, CreatedTime = F, Description = F, EntityID = F, Filter = F, GradeLevelFilterType = F, GradeReferenceIDInclusionList = F, IncludeCategoryLunch = F, IncludeCategoryRegular = F, IncludeCategoryStudyHall = F, IncludeCoursesWithNoCourseType = F, IncludeElective = F, IncludeInactiveCourses = F, IncludeOfferedCourses = F, IncludeRequired = F, IncludeSchedulingTypeManuallyScheduled = F, IncludeSchedulingTypeNormal = F, IncludeSchedulingTypeSpecialEducation = F, ModifiedTime = F, SchoolYearID = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityCourseFilter", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityCourseFilter
	#'
	#' This function returns a dataframe or json object of an AvailabilityCourseFilter
	#' @param AvailabilityCourseFilterID The ID of the AvailabilityCourseFilter to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityCourseFilter. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityCourseFilter.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityCourseFilter') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityCourseFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityCourseFilter <- function(AvailabilityCourseFilterID, AvailabilityCourseFilterIDClonedFrom = F, AvailabilityCourseFilterIDClonedTo = F, Code = F, CodeDescription = F, CourseTypeIDInclusionList = F, CreatedTime = F, Description = F, EntityID = F, Filter = F, GradeLevelFilterType = F, GradeReferenceIDInclusionList = F, IncludeCategoryLunch = F, IncludeCategoryRegular = F, IncludeCategoryStudyHall = F, IncludeCoursesWithNoCourseType = F, IncludeElective = F, IncludeInactiveCourses = F, IncludeOfferedCourses = F, IncludeRequired = F, IncludeSchedulingTypeManuallyScheduled = F, IncludeSchedulingTypeNormal = F, IncludeSchedulingTypeSpecialEducation = F, ModifiedTime = F, SchoolYearID = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityCourseFilterID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityCourseFilter", objectId = AvailabilityCourseFilterID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityCourseFilter
	#'
	#' This function deletes an AvailabilityCourseFilter
	#' @param AvailabilityCourseFilterID The ID of the AvailabilityCourseFilter to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityCourseFilterID of the deleted AvailabilityCourseFilter.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityCourseFilter <- function(AvailabilityCourseFilterID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityCourseFilter", objectId = AvailabilityCourseFilterID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityCourseFilter
	#'
	#' This function creates an AvailabilityCourseFilter
	#' @param fieldNames The field values to give the created AvailabilityCourseFilter. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityCourseFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityCourseFilter <- function(AvailabilityCourseFilterIDClonedFrom = NULL, Code = NULL, Description = NULL, EntityID = NULL, Filter = NULL, GradeLevelFilterType = NULL, GradeReferenceIDInclusionList = NULL, IncludeCategoryLunch = NULL, IncludeCategoryRegular = NULL, IncludeCategoryStudyHall = NULL, IncludeCoursesWithNoCourseType = NULL, IncludeElective = NULL, IncludeInactiveCourses = NULL, IncludeOfferedCourses = NULL, IncludeRequired = NULL, IncludeSchedulingTypeManuallyScheduled = NULL, IncludeSchedulingTypeNormal = NULL, IncludeSchedulingTypeSpecialEducation = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityCourseFilter", body = list(DataObject = body), searchFields = append("AvailabilityCourseFilterID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityCourseFilter
	#'
	#' This function modifies an AvailabilityCourseFilter
	#' @param fieldNames The field values to give the modified AvailabilityCourseFilter. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityCourseFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityCourseFilter <- function(AvailabilityCourseFilterID, AvailabilityCourseFilterIDClonedFrom = NULL, Code = NULL, Description = NULL, EntityID = NULL, Filter = NULL, GradeLevelFilterType = NULL, GradeReferenceIDInclusionList = NULL, IncludeCategoryLunch = NULL, IncludeCategoryRegular = NULL, IncludeCategoryStudyHall = NULL, IncludeCoursesWithNoCourseType = NULL, IncludeElective = NULL, IncludeInactiveCourses = NULL, IncludeOfferedCourses = NULL, IncludeRequired = NULL, IncludeSchedulingTypeManuallyScheduled = NULL, IncludeSchedulingTypeNormal = NULL, IncludeSchedulingTypeSpecialEducation = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityCourseFilter", objectId = AvailabilityCourseFilterID, body = list(DataObject = body), searchFields = append("AvailabilityCourseFilterID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ScheduleRestorePoints
	#'
	#' This function returns a dataframe or json object of ScheduleRestorePoints
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ScheduleRestorePoints. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ScheduleRestorePoints.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ScheduleRestorePoint') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of ScheduleRestorePoints
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listScheduleRestorePoints <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, EntityID = F, ModifiedTime = F, Name = F, NameDescription = F, ReportQueueID = F, RestorePointDateTime = F, ScheduleRestorePointID = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "ScheduleRestorePoint", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ScheduleRestorePoint
	#'
	#' This function returns a dataframe or json object of a ScheduleRestorePoint
	#' @param ScheduleRestorePointID The ID of the ScheduleRestorePoint to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ScheduleRestorePoint. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ScheduleRestorePoint.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ScheduleRestorePoint') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of ScheduleRestorePoint
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getScheduleRestorePoint <- function(ScheduleRestorePointID, CreatedTime = F, Description = F, EntityID = F, ModifiedTime = F, Name = F, NameDescription = F, ReportQueueID = F, RestorePointDateTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ScheduleRestorePointID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "ScheduleRestorePoint", objectId = ScheduleRestorePointID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ScheduleRestorePoint
	#'
	#' This function deletes a ScheduleRestorePoint
	#' @param ScheduleRestorePointID The ID of the ScheduleRestorePoint to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The ScheduleRestorePointID of the deleted ScheduleRestorePoint.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteScheduleRestorePoint <- function(ScheduleRestorePointID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "ScheduleRestorePoint", objectId = ScheduleRestorePointID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ScheduleRestorePoint
	#'
	#' This function creates a ScheduleRestorePoint
	#' @param fieldNames The field values to give the created ScheduleRestorePoint. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created ScheduleRestorePoint
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createScheduleRestorePoint <- function(Description = NULL, EntityID = NULL, Name = NULL, ReportQueueID = NULL, RestorePointDateTime = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "ScheduleRestorePoint", body = list(DataObject = body), searchFields = append("ScheduleRestorePointID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ScheduleRestorePoint
	#'
	#' This function modifies a ScheduleRestorePoint
	#' @param fieldNames The field values to give the modified ScheduleRestorePoint. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified ScheduleRestorePoint
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyScheduleRestorePoint <- function(ScheduleRestorePointID, Description = NULL, EntityID = NULL, Name = NULL, ReportQueueID = NULL, RestorePointDateTime = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "ScheduleRestorePoint", objectId = ScheduleRestorePointID, body = list(DataObject = body), searchFields = append("ScheduleRestorePointID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseAlternates
	#'
	#' This function returns a dataframe or json object of CourseAlternates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseAlternates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseAlternates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseAlternate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseAlternates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseAlternates <- function(searchConditionsList = NULL, CourseAlternateID = F, CourseAlternateIDClonedFrom = F, CourseIDAlternate = F, CourseIDPrimary = F, CreatedTime = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseAlternate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseAlternate
	#'
	#' This function returns a dataframe or json object of a CourseAlternate
	#' @param CourseAlternateID The ID of the CourseAlternate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseAlternate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseAlternate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseAlternate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseAlternate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseAlternate <- function(CourseAlternateID, CourseAlternateIDClonedFrom = F, CourseIDAlternate = F, CourseIDPrimary = F, CreatedTime = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseAlternateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseAlternate", objectId = CourseAlternateID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseAlternate
	#'
	#' This function deletes a CourseAlternate
	#' @param CourseAlternateID The ID of the CourseAlternate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseAlternateID of the deleted CourseAlternate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseAlternate <- function(CourseAlternateID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseAlternate", objectId = CourseAlternateID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseAlternate
	#'
	#' This function creates a CourseAlternate
	#' @param fieldNames The field values to give the created CourseAlternate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseAlternate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseAlternate <- function(CourseAlternateIDClonedFrom = NULL, CourseIDAlternate = NULL, CourseIDPrimary = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseAlternate", body = list(DataObject = body), searchFields = append("CourseAlternateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseAlternate
	#'
	#' This function modifies a CourseAlternate
	#' @param fieldNames The field values to give the modified CourseAlternate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseAlternate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseAlternate <- function(CourseAlternateID, CourseAlternateIDClonedFrom = NULL, CourseIDAlternate = NULL, CourseIDPrimary = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseAlternate", objectId = CourseAlternateID, body = list(DataObject = body), searchFields = append("CourseAlternateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerCourseConstraints
	#'
	#' This function returns a dataframe or json object of SectionSchedulerCourseConstraints
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerCourseConstraints. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerCourseConstraints.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerCourseConstraint') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerCourseConstraints
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerCourseConstraints <- function(searchConditionsList = NULL, CourseID = F, CourseIDLinked = F, CreatedTime = F, CurrentCourseSection = F, CurrentCourseTopSectionCount = F, IsActive = F, LinkedCourse = F, LinkedCourseSection = F, LinkedCourseTopSectionCount = F, ModifiedTime = F, Rule = F, ScheduleFirst = F, ScheduleLinkedCourseFirst = F, SectionIDCurrentCourseSelected = F, SectionIDLinkedCourseSelected = F, SectionSchedulerCourseConstraintID = F, SectionSchedulerCourseConstraintIDClonedFrom = F, SectionSchedulerCourseConstraintIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerCourseConstraint", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerCourseConstraint
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerCourseConstraint
	#' @param SectionSchedulerCourseConstraintID The ID of the SectionSchedulerCourseConstraint to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerCourseConstraint. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerCourseConstraint.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerCourseConstraint') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerCourseConstraint
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerCourseConstraint <- function(SectionSchedulerCourseConstraintID, CourseID = F, CourseIDLinked = F, CreatedTime = F, CurrentCourseSection = F, CurrentCourseTopSectionCount = F, IsActive = F, LinkedCourse = F, LinkedCourseSection = F, LinkedCourseTopSectionCount = F, ModifiedTime = F, Rule = F, ScheduleFirst = F, ScheduleLinkedCourseFirst = F, SectionIDCurrentCourseSelected = F, SectionIDLinkedCourseSelected = F, SectionSchedulerCourseConstraintIDClonedFrom = F, SectionSchedulerCourseConstraintIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerCourseConstraintID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerCourseConstraint", objectId = SectionSchedulerCourseConstraintID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerCourseConstraint
	#'
	#' This function deletes a SectionSchedulerCourseConstraint
	#' @param SectionSchedulerCourseConstraintID The ID of the SectionSchedulerCourseConstraint to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerCourseConstraintID of the deleted SectionSchedulerCourseConstraint.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerCourseConstraint <- function(SectionSchedulerCourseConstraintID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerCourseConstraint", objectId = SectionSchedulerCourseConstraintID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerCourseConstraint
	#'
	#' This function creates a SectionSchedulerCourseConstraint
	#' @param fieldNames The field values to give the created SectionSchedulerCourseConstraint. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerCourseConstraint
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerCourseConstraint <- function(CourseID = NULL, CourseIDLinked = NULL, CurrentCourseSection = NULL, CurrentCourseTopSectionCount = NULL, IsActive = NULL, LinkedCourse = NULL, LinkedCourseSection = NULL, LinkedCourseTopSectionCount = NULL, Rule = NULL, ScheduleLinkedCourseFirst = NULL, SectionIDCurrentCourseSelected = NULL, SectionIDLinkedCourseSelected = NULL, SectionSchedulerCourseConstraintIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerCourseConstraint", body = list(DataObject = body), searchFields = append("SectionSchedulerCourseConstraintID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerCourseConstraint
	#'
	#' This function modifies a SectionSchedulerCourseConstraint
	#' @param fieldNames The field values to give the modified SectionSchedulerCourseConstraint. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerCourseConstraint
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerCourseConstraint <- function(SectionSchedulerCourseConstraintID, CourseID = NULL, CourseIDLinked = NULL, CurrentCourseSection = NULL, CurrentCourseTopSectionCount = NULL, IsActive = NULL, LinkedCourse = NULL, LinkedCourseSection = NULL, LinkedCourseTopSectionCount = NULL, Rule = NULL, ScheduleLinkedCourseFirst = NULL, SectionIDCurrentCourseSelected = NULL, SectionIDLinkedCourseSelected = NULL, SectionSchedulerCourseConstraintIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerCourseConstraint", objectId = SectionSchedulerCourseConstraintID, body = list(DataObject = body), searchFields = append("SectionSchedulerCourseConstraintID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MeetRequirements
	#'
	#' This function returns a dataframe or json object of MeetRequirements
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetRequirements. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetRequirements.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetRequirement') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MeetRequirements
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeetRequirements <- function(searchConditionsList = NULL, AllowMultiplePeriodsPerDayRotation = F, CourseID = F, CreatedTime = F, DaysPerRotation = F, ForceConsecutivePeriods = F, MeetRequirementID = F, MeetRequirementIDClonedFrom = F, MeetRequirementIDClonedTo = F, MinutesPerDay = F, ModifiedTime = F, TimeSpanAnalysisType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MeetRequirement", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MeetRequirement
	#'
	#' This function returns a dataframe or json object of a MeetRequirement
	#' @param MeetRequirementID The ID of the MeetRequirement to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetRequirement. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetRequirement.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetRequirement') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MeetRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeetRequirement <- function(MeetRequirementID, AllowMultiplePeriodsPerDayRotation = F, CourseID = F, CreatedTime = F, DaysPerRotation = F, ForceConsecutivePeriods = F, MeetRequirementIDClonedFrom = F, MeetRequirementIDClonedTo = F, MinutesPerDay = F, ModifiedTime = F, TimeSpanAnalysisType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetRequirementID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MeetRequirement", objectId = MeetRequirementID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MeetRequirement
	#'
	#' This function deletes a MeetRequirement
	#' @param MeetRequirementID The ID of the MeetRequirement to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetRequirementID of the deleted MeetRequirement.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeetRequirement <- function(MeetRequirementID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MeetRequirement", objectId = MeetRequirementID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MeetRequirement
	#'
	#' This function creates a MeetRequirement
	#' @param fieldNames The field values to give the created MeetRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MeetRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMeetRequirement <- function(AllowMultiplePeriodsPerDayRotation = NULL, CourseID = NULL, DaysPerRotation = NULL, ForceConsecutivePeriods = NULL, MeetRequirementIDClonedFrom = NULL, MinutesPerDay = NULL, TimeSpanAnalysisType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MeetRequirement", body = list(DataObject = body), searchFields = append("MeetRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MeetRequirement
	#'
	#' This function modifies a MeetRequirement
	#' @param fieldNames The field values to give the modified MeetRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MeetRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMeetRequirement <- function(MeetRequirementID, AllowMultiplePeriodsPerDayRotation = NULL, CourseID = NULL, DaysPerRotation = NULL, ForceConsecutivePeriods = NULL, MeetRequirementIDClonedFrom = NULL, MinutesPerDay = NULL, TimeSpanAnalysisType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MeetRequirement", objectId = MeetRequirementID, body = list(DataObject = body), searchFields = append("MeetRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentCourseRequests
	#'
	#' This function returns a dataframe or json object of StudentCourseRequests
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCourseRequests. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCourseRequests.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCourseRequest') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentCourseRequests
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentCourseRequests <- function(searchConditionsList = NULL, AlternateRank = F, CountsAgainstRequestLimit = F, CourseConflict = F, CourseEntityOfferedToID = F, CourseID = F, CourseIsNotDroppedManualOrTransfer = F, CourseNotScheduled = F, CourseNotScheduledAndIsRequestedFromViewingEntity = F, CourseRequested = F, CourseScheduled = F, CourseScheduledAndAllTransactionsCountTowardsViewingEntity = F, CourseScheduledAndAllTransactionsCountTowardsViewingEntityAndCourseIsNotDropOrTransfer = F, CourseScheduledAndCountsTowardsViewingEntity = F, CourseScheduledAndInProgress = F, CourseScheduledAndInProgressAndIsEffectiveToViewingEntity = F, CourseScheduledAndIsBeforeOrInProgress = F, CourseScheduledAndIsBeforeOrInProgressAndNotHasAtleastOneTransactionNotCountTowardsViewingEntity = F, CourseScheduledAndIsRequestedFromOfferingEntityAndAllTransactionsCountTowardsViewingEntity = F, CourseScheduledAndRequestedFromViewingEntityAndAllTransactionsCountTowardsViewingEntity = F, CreatedTime = F, CrsIsNotDrpOrTransferAndNotScheduledOrCrsScheduledAndRequestedFromViewingEntityAndAllTransactionsCntTowardsViewingEntity = F, DisplayToViewingEntity = F, EarnedCreditsPossibleAnticipated = F, EarnedCreditsRequested = F, EntityIDRequestedFrom = F, IsAlternate = F, ModifiedTime = F, PrerequisiteMet = F, RequestedFromOfferingEntity = F, RequestedFromViewingEntity = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetSummary = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentCourseRequest", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentCourseRequest
	#'
	#' This function returns a dataframe or json object of a StudentCourseRequest
	#' @param StudentCourseRequestID The ID of the StudentCourseRequest to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentCourseRequest. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentCourseRequest.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentCourseRequest') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentCourseRequest <- function(StudentCourseRequestID, AlternateRank = F, CountsAgainstRequestLimit = F, CourseConflict = F, CourseEntityOfferedToID = F, CourseID = F, CourseIsNotDroppedManualOrTransfer = F, CourseNotScheduled = F, CourseNotScheduledAndIsRequestedFromViewingEntity = F, CourseRequested = F, CourseScheduled = F, CourseScheduledAndAllTransactionsCountTowardsViewingEntity = F, CourseScheduledAndAllTransactionsCountTowardsViewingEntityAndCourseIsNotDropOrTransfer = F, CourseScheduledAndCountsTowardsViewingEntity = F, CourseScheduledAndInProgress = F, CourseScheduledAndInProgressAndIsEffectiveToViewingEntity = F, CourseScheduledAndIsBeforeOrInProgress = F, CourseScheduledAndIsBeforeOrInProgressAndNotHasAtleastOneTransactionNotCountTowardsViewingEntity = F, CourseScheduledAndIsRequestedFromOfferingEntityAndAllTransactionsCountTowardsViewingEntity = F, CourseScheduledAndRequestedFromViewingEntityAndAllTransactionsCountTowardsViewingEntity = F, CreatedTime = F, CrsIsNotDrpOrTransferAndNotScheduledOrCrsScheduledAndRequestedFromViewingEntityAndAllTransactionsCntTowardsViewingEntity = F, DisplayToViewingEntity = F, EarnedCreditsPossibleAnticipated = F, EarnedCreditsRequested = F, EntityIDRequestedFrom = F, IsAlternate = F, ModifiedTime = F, PrerequisiteMet = F, RequestedFromOfferingEntity = F, RequestedFromViewingEntity = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetSummary = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentCourseRequestID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentCourseRequest", objectId = StudentCourseRequestID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentCourseRequest
	#'
	#' This function deletes a StudentCourseRequest
	#' @param StudentCourseRequestID The ID of the StudentCourseRequest to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentCourseRequestID of the deleted StudentCourseRequest.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentCourseRequest <- function(StudentCourseRequestID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentCourseRequest", objectId = StudentCourseRequestID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentCourseRequest
	#'
	#' This function creates a StudentCourseRequest
	#' @param fieldNames The field values to give the created StudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentCourseRequest <- function(AlternateRank = NULL, CourseEntityOfferedToID = NULL, CourseID = NULL, EntityIDRequestedFrom = NULL, IsAlternate = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SectionLengthSubsetSummary = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentCourseRequest", body = list(DataObject = body), searchFields = append("StudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentCourseRequest
	#'
	#' This function modifies a StudentCourseRequest
	#' @param fieldNames The field values to give the modified StudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentCourseRequest <- function(StudentCourseRequestID, AlternateRank = NULL, CourseEntityOfferedToID = NULL, CourseID = NULL, EntityIDRequestedFrom = NULL, IsAlternate = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SectionLengthSubsetSummary = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentCourseRequest", objectId = StudentCourseRequestID, body = list(DataObject = body), searchFields = append("StudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseCorequisiteGroupCourses
	#'
	#' This function returns a dataframe or json object of CourseCorequisiteGroupCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCorequisiteGroupCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCorequisiteGroupCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCorequisiteGroupCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseCorequisiteGroupCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseCorequisiteGroupCourses <- function(searchConditionsList = NULL, CourseCorequisiteGroupCourseID = F, CourseCorequisiteGroupCourseIDClonedFrom = F, CourseCorequisiteGroupID = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseCorequisiteGroupCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseCorequisiteGroupCourse
	#'
	#' This function returns a dataframe or json object of a CourseCorequisiteGroupCourse
	#' @param CourseCorequisiteGroupCourseID The ID of the CourseCorequisiteGroupCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCorequisiteGroupCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCorequisiteGroupCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCorequisiteGroupCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseCorequisiteGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseCorequisiteGroupCourse <- function(CourseCorequisiteGroupCourseID, CourseCorequisiteGroupCourseIDClonedFrom = F, CourseCorequisiteGroupID = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseCorequisiteGroupCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroupCourse", objectId = CourseCorequisiteGroupCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseCorequisiteGroupCourse
	#'
	#' This function deletes a CourseCorequisiteGroupCourse
	#' @param CourseCorequisiteGroupCourseID The ID of the CourseCorequisiteGroupCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseCorequisiteGroupCourseID of the deleted CourseCorequisiteGroupCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseCorequisiteGroupCourse <- function(CourseCorequisiteGroupCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroupCourse", objectId = CourseCorequisiteGroupCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseCorequisiteGroupCourse
	#'
	#' This function creates a CourseCorequisiteGroupCourse
	#' @param fieldNames The field values to give the created CourseCorequisiteGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseCorequisiteGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseCorequisiteGroupCourse <- function(CourseCorequisiteGroupCourseIDClonedFrom = NULL, CourseCorequisiteGroupID = NULL, CourseID = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroupCourse", body = list(DataObject = body), searchFields = append("CourseCorequisiteGroupCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseCorequisiteGroupCourse
	#'
	#' This function modifies a CourseCorequisiteGroupCourse
	#' @param fieldNames The field values to give the modified CourseCorequisiteGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseCorequisiteGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseCorequisiteGroupCourse <- function(CourseCorequisiteGroupCourseID, CourseCorequisiteGroupCourseIDClonedFrom = NULL, CourseCorequisiteGroupID = NULL, CourseID = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroupCourse", objectId = CourseCorequisiteGroupCourseID, body = list(DataObject = body), searchFields = append("CourseCorequisiteGroupCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MeetSummaries
	#'
	#' This function returns a dataframe or json object of MeetSummaries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetSummaries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetSummaries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetSummary') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MeetSummaries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeetSummaries <- function(searchConditionsList = NULL, CalculatedDays = F, CalculatedDaysStudent = F, CalculatedPeriod = F, CalculatedPeriodStudent = F, CalendarID = F, CreatedTime = F, Days = F, EntityIDViewing = F, HasOnlyHiddenDetails = F, IsDefaultCalendar = F, IsPrimary = F, MeetID = F, MeetSummaryID = F, ModifiedTime = F, Period = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MeetSummary", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MeetSummary
	#'
	#' This function returns a dataframe or json object of a MeetSummary
	#' @param MeetSummaryID The ID of the MeetSummary to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetSummary. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetSummary.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetSummary') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MeetSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeetSummary <- function(MeetSummaryID, CalculatedDays = F, CalculatedDaysStudent = F, CalculatedPeriod = F, CalculatedPeriodStudent = F, CalendarID = F, CreatedTime = F, Days = F, EntityIDViewing = F, HasOnlyHiddenDetails = F, IsDefaultCalendar = F, IsPrimary = F, MeetID = F, ModifiedTime = F, Period = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetSummaryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MeetSummary", objectId = MeetSummaryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MeetSummary
	#'
	#' This function deletes a MeetSummary
	#' @param MeetSummaryID The ID of the MeetSummary to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetSummaryID of the deleted MeetSummary.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeetSummary <- function(MeetSummaryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MeetSummary", objectId = MeetSummaryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MeetSummary
	#'
	#' This function creates a MeetSummary
	#' @param fieldNames The field values to give the created MeetSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MeetSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMeetSummary <- function(CalendarID = NULL, Days = NULL, EntityIDViewing = NULL, HasOnlyHiddenDetails = NULL, IsDefaultCalendar = NULL, IsPrimary = NULL, MeetID = NULL, Period = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MeetSummary", body = list(DataObject = body), searchFields = append("MeetSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MeetSummary
	#'
	#' This function modifies a MeetSummary
	#' @param fieldNames The field values to give the modified MeetSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MeetSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMeetSummary <- function(MeetSummaryID, CalendarID = NULL, Days = NULL, EntityIDViewing = NULL, HasOnlyHiddenDetails = NULL, IsDefaultCalendar = NULL, IsPrimary = NULL, MeetID = NULL, Period = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MeetSummary", objectId = MeetSummaryID, body = list(DataObject = body), searchFields = append("MeetSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseCustomRequirements
	#'
	#' This function returns a dataframe or json object of CourseCustomRequirements
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCustomRequirements. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCustomRequirements.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCustomRequirement') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseCustomRequirements
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseCustomRequirements <- function(searchConditionsList = NULL, CourseCustomRequirementID = F, CourseCustomRequirementIDClonedFrom = F, CourseID = F, CreatedTime = F, CustomRequirementID = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseCustomRequirement", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseCustomRequirement
	#'
	#' This function returns a dataframe or json object of a CourseCustomRequirement
	#' @param CourseCustomRequirementID The ID of the CourseCustomRequirement to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCustomRequirement. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCustomRequirement.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCustomRequirement') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseCustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseCustomRequirement <- function(CourseCustomRequirementID, CourseCustomRequirementIDClonedFrom = F, CourseID = F, CreatedTime = F, CustomRequirementID = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseCustomRequirementID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseCustomRequirement", objectId = CourseCustomRequirementID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseCustomRequirement
	#'
	#' This function deletes a CourseCustomRequirement
	#' @param CourseCustomRequirementID The ID of the CourseCustomRequirement to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseCustomRequirementID of the deleted CourseCustomRequirement.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseCustomRequirement <- function(CourseCustomRequirementID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseCustomRequirement", objectId = CourseCustomRequirementID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseCustomRequirement
	#'
	#' This function creates a CourseCustomRequirement
	#' @param fieldNames The field values to give the created CourseCustomRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseCustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseCustomRequirement <- function(CourseCustomRequirementIDClonedFrom = NULL, CourseID = NULL, CustomRequirementID = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseCustomRequirement", body = list(DataObject = body), searchFields = append("CourseCustomRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseCustomRequirement
	#'
	#' This function modifies a CourseCustomRequirement
	#' @param fieldNames The field values to give the modified CourseCustomRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseCustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseCustomRequirement <- function(CourseCustomRequirementID, CourseCustomRequirementIDClonedFrom = NULL, CourseID = NULL, CustomRequirementID = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseCustomRequirement", objectId = CourseCustomRequirementID, body = list(DataObject = body), searchFields = append("CourseCustomRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseCorequisiteGroups
	#'
	#' This function returns a dataframe or json object of CourseCorequisiteGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCorequisiteGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCorequisiteGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCorequisiteGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseCorequisiteGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseCorequisiteGroups <- function(searchConditionsList = NULL, AutomaticRequestSetting = F, CourseCorequisiteGroupID = F, CourseCorequisiteGroupIDClonedFrom = F, CourseCorequisiteGroupIDClonedTo = F, CreatedTime = F, Description = F, DisplayPeriodMatch = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, Name = F, NameDescription = F, Overlap = F, ScheduleAllCoursesInGroupOrNone = F, SchoolYearID = F, StaffMatch = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseCorequisiteGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseCorequisiteGroup
	#'
	#' This function returns a dataframe or json object of a CourseCorequisiteGroup
	#' @param CourseCorequisiteGroupID The ID of the CourseCorequisiteGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCorequisiteGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCorequisiteGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCorequisiteGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseCorequisiteGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseCorequisiteGroup <- function(CourseCorequisiteGroupID, AutomaticRequestSetting = F, CourseCorequisiteGroupIDClonedFrom = F, CourseCorequisiteGroupIDClonedTo = F, CreatedTime = F, Description = F, DisplayPeriodMatch = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, Name = F, NameDescription = F, Overlap = F, ScheduleAllCoursesInGroupOrNone = F, SchoolYearID = F, StaffMatch = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseCorequisiteGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroup", objectId = CourseCorequisiteGroupID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseCorequisiteGroup
	#'
	#' This function deletes a CourseCorequisiteGroup
	#' @param CourseCorequisiteGroupID The ID of the CourseCorequisiteGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseCorequisiteGroupID of the deleted CourseCorequisiteGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseCorequisiteGroup <- function(CourseCorequisiteGroupID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroup", objectId = CourseCorequisiteGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseCorequisiteGroup
	#'
	#' This function creates a CourseCorequisiteGroup
	#' @param fieldNames The field values to give the created CourseCorequisiteGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseCorequisiteGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseCorequisiteGroup <- function(AutomaticRequestSetting = NULL, CourseCorequisiteGroupIDClonedFrom = NULL, Description = NULL, DisplayPeriodMatch = NULL, EntityGroupKey = NULL, EntityID = NULL, Name = NULL, Overlap = NULL, ScheduleAllCoursesInGroupOrNone = NULL, SchoolYearID = NULL, StaffMatch = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroup", body = list(DataObject = body), searchFields = append("CourseCorequisiteGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseCorequisiteGroup
	#'
	#' This function modifies a CourseCorequisiteGroup
	#' @param fieldNames The field values to give the modified CourseCorequisiteGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseCorequisiteGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseCorequisiteGroup <- function(CourseCorequisiteGroupID, AutomaticRequestSetting = NULL, CourseCorequisiteGroupIDClonedFrom = NULL, Description = NULL, DisplayPeriodMatch = NULL, EntityGroupKey = NULL, EntityID = NULL, Name = NULL, Overlap = NULL, ScheduleAllCoursesInGroupOrNone = NULL, SchoolYearID = NULL, StaffMatch = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseCorequisiteGroup", objectId = CourseCorequisiteGroupID, body = list(DataObject = body), searchFields = append("CourseCorequisiteGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGroups
	#'
	#' This function returns a dataframe or json object of CourseGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGroups <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseGroupID = F, CreatedTime = F, Description = F, DistrictGroupKey = F, DistrictID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGroup
	#'
	#' This function returns a dataframe or json object of a CourseGroup
	#' @param CourseGroupID The ID of the CourseGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGroup <- function(CourseGroupID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DistrictGroupKey = F, DistrictID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseGroup", objectId = CourseGroupID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGroup
	#'
	#' This function deletes a CourseGroup
	#' @param CourseGroupID The ID of the CourseGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseGroupID of the deleted CourseGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGroup <- function(CourseGroupID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseGroup", objectId = CourseGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGroup
	#'
	#' This function creates a CourseGroup
	#' @param fieldNames The field values to give the created CourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGroup <- function(Code = NULL, Description = NULL, DistrictGroupKey = NULL, DistrictID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseGroup", body = list(DataObject = body), searchFields = append("CourseGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGroup
	#'
	#' This function modifies a CourseGroup
	#' @param fieldNames The field values to give the modified CourseGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGroup <- function(CourseGroupID, Code = NULL, Description = NULL, DistrictGroupKey = NULL, DistrictID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseGroup", objectId = CourseGroupID, body = list(DataObject = body), searchFields = append("CourseGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionCorequisiteGroups
	#'
	#' This function returns a dataframe or json object of SectionCorequisiteGroups
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCorequisiteGroups. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCorequisiteGroups.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCorequisiteGroup') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionCorequisiteGroups
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionCorequisiteGroups <- function(searchConditionsList = NULL, AutomaticRequestSetting = F, AutomaticScheduleSetting = F, CreatedTime = F, Description = F, EntityID = F, ModifiedTime = F, Name = F, NameDescription = F, ScheduleAllSectionsInGroupOrNone = F, SectionCorequisiteGroupID = F, SectionCorequisiteGroupIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionCorequisiteGroup", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionCorequisiteGroup
	#'
	#' This function returns a dataframe or json object of a SectionCorequisiteGroup
	#' @param SectionCorequisiteGroupID The ID of the SectionCorequisiteGroup to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCorequisiteGroup. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCorequisiteGroup.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCorequisiteGroup') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionCorequisiteGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionCorequisiteGroup <- function(SectionCorequisiteGroupID, AutomaticRequestSetting = F, AutomaticScheduleSetting = F, CreatedTime = F, Description = F, EntityID = F, ModifiedTime = F, Name = F, NameDescription = F, ScheduleAllSectionsInGroupOrNone = F, SectionCorequisiteGroupIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionCorequisiteGroupID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroup", objectId = SectionCorequisiteGroupID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionCorequisiteGroup
	#'
	#' This function deletes a SectionCorequisiteGroup
	#' @param SectionCorequisiteGroupID The ID of the SectionCorequisiteGroup to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionCorequisiteGroupID of the deleted SectionCorequisiteGroup.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionCorequisiteGroup <- function(SectionCorequisiteGroupID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroup", objectId = SectionCorequisiteGroupID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionCorequisiteGroup
	#'
	#' This function creates a SectionCorequisiteGroup
	#' @param fieldNames The field values to give the created SectionCorequisiteGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionCorequisiteGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionCorequisiteGroup <- function(AutomaticRequestSetting = NULL, AutomaticScheduleSetting = NULL, Description = NULL, EntityID = NULL, Name = NULL, ScheduleAllSectionsInGroupOrNone = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroup", body = list(DataObject = body), searchFields = append("SectionCorequisiteGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionCorequisiteGroup
	#'
	#' This function modifies a SectionCorequisiteGroup
	#' @param fieldNames The field values to give the modified SectionCorequisiteGroup. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionCorequisiteGroup
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionCorequisiteGroup <- function(SectionCorequisiteGroupID, AutomaticRequestSetting = NULL, AutomaticScheduleSetting = NULL, Description = NULL, EntityID = NULL, Name = NULL, ScheduleAllSectionsInGroupOrNone = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroup", objectId = SectionCorequisiteGroupID, body = list(DataObject = body), searchFields = append("SectionCorequisiteGroupID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionCustomRequirements
	#'
	#' This function returns a dataframe or json object of SectionCustomRequirements
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCustomRequirements. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCustomRequirements.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCustomRequirement') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionCustomRequirements
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionCustomRequirements <- function(searchConditionsList = NULL, CreatedTime = F, CustomRequirementID = F, ModifiedTime = F, SectionCustomRequirementID = F, SectionCustomRequirementIDClonedFrom = F, SectionCustomRequirementIDClonedTo = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionCustomRequirement", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionCustomRequirement
	#'
	#' This function returns a dataframe or json object of a SectionCustomRequirement
	#' @param SectionCustomRequirementID The ID of the SectionCustomRequirement to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCustomRequirement. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCustomRequirement.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCustomRequirement') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionCustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionCustomRequirement <- function(SectionCustomRequirementID, CreatedTime = F, CustomRequirementID = F, ModifiedTime = F, SectionCustomRequirementIDClonedFrom = F, SectionCustomRequirementIDClonedTo = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionCustomRequirementID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionCustomRequirement", objectId = SectionCustomRequirementID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionCustomRequirement
	#'
	#' This function deletes a SectionCustomRequirement
	#' @param SectionCustomRequirementID The ID of the SectionCustomRequirement to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionCustomRequirementID of the deleted SectionCustomRequirement.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionCustomRequirement <- function(SectionCustomRequirementID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionCustomRequirement", objectId = SectionCustomRequirementID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionCustomRequirement
	#'
	#' This function creates a SectionCustomRequirement
	#' @param fieldNames The field values to give the created SectionCustomRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionCustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionCustomRequirement <- function(CustomRequirementID = NULL, SectionCustomRequirementIDClonedFrom = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionCustomRequirement", body = list(DataObject = body), searchFields = append("SectionCustomRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionCustomRequirement
	#'
	#' This function modifies a SectionCustomRequirement
	#' @param fieldNames The field values to give the modified SectionCustomRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionCustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionCustomRequirement <- function(SectionCustomRequirementID, CustomRequirementID = NULL, SectionCustomRequirementIDClonedFrom = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionCustomRequirement", objectId = SectionCustomRequirementID, body = list(DataObject = body), searchFields = append("SectionCustomRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionCorequisiteGroupSections
	#'
	#' This function returns a dataframe or json object of SectionCorequisiteGroupSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCorequisiteGroupSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCorequisiteGroupSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCorequisiteGroupSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionCorequisiteGroupSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionCorequisiteGroupSections <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SectionCorequisiteGroupID = F, SectionCorequisiteGroupSectionID = F, SectionCorequisiteGroupSectionIDClonedFrom = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionCorequisiteGroupSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionCorequisiteGroupSection
	#'
	#' This function returns a dataframe or json object of a SectionCorequisiteGroupSection
	#' @param SectionCorequisiteGroupSectionID The ID of the SectionCorequisiteGroupSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionCorequisiteGroupSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionCorequisiteGroupSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionCorequisiteGroupSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionCorequisiteGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionCorequisiteGroupSection <- function(SectionCorequisiteGroupSectionID, CreatedTime = F, ModifiedTime = F, SectionCorequisiteGroupID = F, SectionCorequisiteGroupSectionIDClonedFrom = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionCorequisiteGroupSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroupSection", objectId = SectionCorequisiteGroupSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionCorequisiteGroupSection
	#'
	#' This function deletes a SectionCorequisiteGroupSection
	#' @param SectionCorequisiteGroupSectionID The ID of the SectionCorequisiteGroupSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionCorequisiteGroupSectionID of the deleted SectionCorequisiteGroupSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionCorequisiteGroupSection <- function(SectionCorequisiteGroupSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroupSection", objectId = SectionCorequisiteGroupSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionCorequisiteGroupSection
	#'
	#' This function creates a SectionCorequisiteGroupSection
	#' @param fieldNames The field values to give the created SectionCorequisiteGroupSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionCorequisiteGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionCorequisiteGroupSection <- function(SectionCorequisiteGroupID = NULL, SectionCorequisiteGroupSectionIDClonedFrom = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroupSection", body = list(DataObject = body), searchFields = append("SectionCorequisiteGroupSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionCorequisiteGroupSection
	#'
	#' This function modifies a SectionCorequisiteGroupSection
	#' @param fieldNames The field values to give the modified SectionCorequisiteGroupSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionCorequisiteGroupSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionCorequisiteGroupSection <- function(SectionCorequisiteGroupSectionID, SectionCorequisiteGroupID = NULL, SectionCorequisiteGroupSectionIDClonedFrom = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionCorequisiteGroupSection", objectId = SectionCorequisiteGroupSectionID, body = list(DataObject = body), searchFields = append("SectionCorequisiteGroupSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGroupCourses
	#'
	#' This function returns a dataframe or json object of CourseGroupCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGroupCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGroupCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGroupCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseGroupCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGroupCourses <- function(searchConditionsList = NULL, CourseGroupCourseID = F, CourseGroupCourseIDClonedFrom = F, CourseGroupID = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseGroupCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGroupCourse
	#'
	#' This function returns a dataframe or json object of a CourseGroupCourse
	#' @param CourseGroupCourseID The ID of the CourseGroupCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGroupCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGroupCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGroupCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGroupCourse <- function(CourseGroupCourseID, CourseGroupCourseIDClonedFrom = F, CourseGroupID = F, CourseID = F, CreatedTime = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGroupCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseGroupCourse", objectId = CourseGroupCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGroupCourse
	#'
	#' This function deletes a CourseGroupCourse
	#' @param CourseGroupCourseID The ID of the CourseGroupCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseGroupCourseID of the deleted CourseGroupCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGroupCourse <- function(CourseGroupCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseGroupCourse", objectId = CourseGroupCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseGroupCourse
	#'
	#' This function creates a CourseGroupCourse
	#' @param fieldNames The field values to give the created CourseGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseGroupCourse <- function(CourseGroupCourseIDClonedFrom = NULL, CourseGroupID = NULL, CourseID = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseGroupCourse", body = list(DataObject = body), searchFields = append("CourseGroupCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseGroupCourse
	#'
	#' This function modifies a CourseGroupCourse
	#' @param fieldNames The field values to give the modified CourseGroupCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseGroupCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseGroupCourse <- function(CourseGroupCourseID, CourseGroupCourseIDClonedFrom = NULL, CourseGroupID = NULL, CourseID = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseGroupCourse", objectId = CourseGroupCourseID, body = list(DataObject = body), searchFields = append("CourseGroupCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CustomRequirements
	#'
	#' This function returns a dataframe or json object of CustomRequirements
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CustomRequirements. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CustomRequirements.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CustomRequirement') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CustomRequirements
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCustomRequirements <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, CustomRequirementID = F, Description = F, DistrictGroupKey = F, DistrictID = F, ModifiedTime = F, StudentCondition = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CustomRequirement", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CustomRequirement
	#'
	#' This function returns a dataframe or json object of a CustomRequirement
	#' @param CustomRequirementID The ID of the CustomRequirement to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CustomRequirement. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CustomRequirement.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CustomRequirement') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCustomRequirement <- function(CustomRequirementID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DistrictGroupKey = F, DistrictID = F, ModifiedTime = F, StudentCondition = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CustomRequirementID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CustomRequirement", objectId = CustomRequirementID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CustomRequirement
	#'
	#' This function deletes a CustomRequirement
	#' @param CustomRequirementID The ID of the CustomRequirement to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CustomRequirementID of the deleted CustomRequirement.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCustomRequirement <- function(CustomRequirementID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CustomRequirement", objectId = CustomRequirementID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CustomRequirement
	#'
	#' This function creates a CustomRequirement
	#' @param fieldNames The field values to give the created CustomRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCustomRequirement <- function(Code = NULL, Description = NULL, DistrictGroupKey = NULL, DistrictID = NULL, StudentCondition = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CustomRequirement", body = list(DataObject = body), searchFields = append("CustomRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CustomRequirement
	#'
	#' This function modifies a CustomRequirement
	#' @param fieldNames The field values to give the modified CustomRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CustomRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCustomRequirement <- function(CustomRequirementID, Code = NULL, Description = NULL, DistrictGroupKey = NULL, DistrictID = NULL, StudentCondition = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CustomRequirement", objectId = CustomRequirementID, body = list(DataObject = body), searchFields = append("CustomRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseSubjects
	#'
	#' This function returns a dataframe or json object of CourseSubjects
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseSubjects. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseSubjects.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseSubject') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseSubjects
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseSubjects <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseSubjectID = F, CourseSubjectIDClonedFrom = F, CourseSubjectIDClonedTo = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseSubject", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseSubject
	#'
	#' This function returns a dataframe or json object of a CourseSubject
	#' @param CourseSubjectID The ID of the CourseSubject to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseSubject. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseSubject.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseSubject') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseSubject <- function(CourseSubjectID, Code = F, CodeDescription = F, CourseSubjectIDClonedFrom = F, CourseSubjectIDClonedTo = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseSubjectID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseSubject", objectId = CourseSubjectID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseSubject
	#'
	#' This function deletes a CourseSubject
	#' @param CourseSubjectID The ID of the CourseSubject to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseSubjectID of the deleted CourseSubject.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseSubject <- function(CourseSubjectID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseSubject", objectId = CourseSubjectID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseSubject
	#'
	#' This function creates a CourseSubject
	#' @param fieldNames The field values to give the created CourseSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseSubject <- function(Code = NULL, CourseSubjectIDClonedFrom = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseSubject", body = list(DataObject = body), searchFields = append("CourseSubjectID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseSubject
	#'
	#' This function modifies a CourseSubject
	#' @param fieldNames The field values to give the modified CourseSubject. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseSubject
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseSubject <- function(CourseSubjectID, Code = NULL, CourseSubjectIDClonedFrom = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseSubject", objectId = CourseSubjectID, body = list(DataObject = body), searchFields = append("CourseSubjectID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseTypes
	#'
	#' This function returns a dataframe or json object of CourseTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseTypes <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseTypeID = F, CourseTypeIDClonedFrom = F, CourseTypeIDClonedTo = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseType
	#'
	#' This function returns a dataframe or json object of a CourseType
	#' @param CourseTypeID The ID of the CourseType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseType <- function(CourseTypeID, Code = F, CodeDescription = F, CourseTypeIDClonedFrom = F, CourseTypeIDClonedTo = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseType", objectId = CourseTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseType
	#'
	#' This function deletes a CourseType
	#' @param CourseTypeID The ID of the CourseType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseTypeID of the deleted CourseType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseType <- function(CourseTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseType", objectId = CourseTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseType
	#'
	#' This function creates a CourseType
	#' @param fieldNames The field values to give the created CourseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseType <- function(Code = NULL, CourseTypeIDClonedFrom = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseType", body = list(DataObject = body), searchFields = append("CourseTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseType
	#'
	#' This function modifies a CourseType
	#' @param fieldNames The field values to give the modified CourseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseType <- function(CourseTypeID, Code = NULL, CourseTypeIDClonedFrom = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseType", objectId = CourseTypeID, body = list(DataObject = body), searchFields = append("CourseTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseLengths
	#'
	#' This function returns a dataframe or json object of CourseLengths
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseLengths. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseLengths.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseLength') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseLengths
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseLengths <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CourseLengthID = F, CourseLengthIDClonedFrom = F, CourseLengthIDClonedTo = F, CreatedTime = F, DefaultEarnedCredits = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, SectionLengthCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseLength", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseLength
	#'
	#' This function returns a dataframe or json object of a CourseLength
	#' @param CourseLengthID The ID of the CourseLength to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseLength. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseLength.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseLength') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseLength <- function(CourseLengthID, Code = F, CodeDescription = F, CourseLengthIDClonedFrom = F, CourseLengthIDClonedTo = F, CreatedTime = F, DefaultEarnedCredits = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, SectionLengthCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseLengthID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseLength", objectId = CourseLengthID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseLength
	#'
	#' This function deletes a CourseLength
	#' @param CourseLengthID The ID of the CourseLength to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseLengthID of the deleted CourseLength.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseLength <- function(CourseLengthID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseLength", objectId = CourseLengthID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseLength
	#'
	#' This function creates a CourseLength
	#' @param fieldNames The field values to give the created CourseLength. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseLength <- function(Code = NULL, CourseLengthIDClonedFrom = NULL, DefaultEarnedCredits = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseLength", body = list(DataObject = body), searchFields = append("CourseLengthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseLength
	#'
	#' This function modifies a CourseLength
	#' @param fieldNames The field values to give the modified CourseLength. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseLength
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseLength <- function(CourseLengthID, Code = NULL, CourseLengthIDClonedFrom = NULL, DefaultEarnedCredits = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseLength", objectId = CourseLengthID, body = list(DataObject = body), searchFields = append("CourseLengthID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionDefaults
	#'
	#' This function returns a dataframe or json object of SectionDefaults
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionDefaults. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionDefaults.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionDefault') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionDefaults
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionDefaults <- function(searchConditionsList = NULL, CourseID = F, CreatedTime = F, MaximumStudentCount = F, MinimumStudentCount = F, ModifiedTime = F, OptimalStudentCount = F, SectionDefaultID = F, SectionDefaultIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionDefault", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionDefault
	#'
	#' This function returns a dataframe or json object of a SectionDefault
	#' @param SectionDefaultID The ID of the SectionDefault to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionDefault. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionDefault.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionDefault') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionDefault <- function(SectionDefaultID, CourseID = F, CreatedTime = F, MaximumStudentCount = F, MinimumStudentCount = F, ModifiedTime = F, OptimalStudentCount = F, SectionDefaultIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionDefaultID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionDefault", objectId = SectionDefaultID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionDefault
	#'
	#' This function deletes a SectionDefault
	#' @param SectionDefaultID The ID of the SectionDefault to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionDefaultID of the deleted SectionDefault.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionDefault <- function(SectionDefaultID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionDefault", objectId = SectionDefaultID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionDefault
	#'
	#' This function creates a SectionDefault
	#' @param fieldNames The field values to give the created SectionDefault. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionDefault <- function(CourseID = NULL, MaximumStudentCount = NULL, MinimumStudentCount = NULL, OptimalStudentCount = NULL, SectionDefaultIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionDefault", body = list(DataObject = body), searchFields = append("SectionDefaultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionDefault
	#'
	#' This function modifies a SectionDefault
	#' @param fieldNames The field values to give the modified SectionDefault. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionDefault
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionDefault <- function(SectionDefaultID, CourseID = NULL, MaximumStudentCount = NULL, MinimumStudentCount = NULL, OptimalStudentCount = NULL, SectionDefaultIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionDefault", objectId = SectionDefaultID, body = list(DataObject = body), searchFields = append("SectionDefaultID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Meets
	#'
	#' This function returns a dataframe or json object of Meets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Meets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Meets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Meet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of Meets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeets <- function(searchConditionsList = NULL, CalendarIDStaff = F, CreatedTime = F, DisplayPeriodRotationID = F, DurationInMinutes = F, EndDate = F, EntityID = F, HasDisplayPeriodRotationAssigned = F, IsAssignedToAHomeroomRoom = F, IsPrimary = F, LockAllMeetDayRotationsFromSectionScheduler = F, LockAllMeetDisplayPeriodsFromSectionScheduler = F, LockAllStaffMeetsFromSectionScheduler = F, LockRoomFromSectionScheduler = F, MeetID = F, MeetIDClonedFrom = F, MeetIDClonedTo = F, ModifiedTime = F, OverridePeriodEndTime = F, OverridePeriodStartTime = F, RoomID = F, RoomSeatsAvailable = F, SchoolYearID = F, SectionID = F, StartDate = F, StartDateEndDateBuildingCodeRoomNumber = F, TotalMeetDisplayPeriodCount = F, TotalStaffMeetCount = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "Meet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Meet
	#'
	#' This function returns a dataframe or json object of a Meet
	#' @param MeetID The ID of the Meet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Meet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Meet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Meet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of Meet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeet <- function(MeetID, CalendarIDStaff = F, CreatedTime = F, DisplayPeriodRotationID = F, DurationInMinutes = F, EndDate = F, EntityID = F, HasDisplayPeriodRotationAssigned = F, IsAssignedToAHomeroomRoom = F, IsPrimary = F, LockAllMeetDayRotationsFromSectionScheduler = F, LockAllMeetDisplayPeriodsFromSectionScheduler = F, LockAllStaffMeetsFromSectionScheduler = F, LockRoomFromSectionScheduler = F, MeetIDClonedFrom = F, MeetIDClonedTo = F, ModifiedTime = F, OverridePeriodEndTime = F, OverridePeriodStartTime = F, RoomID = F, RoomSeatsAvailable = F, SchoolYearID = F, SectionID = F, StartDate = F, StartDateEndDateBuildingCodeRoomNumber = F, TotalMeetDisplayPeriodCount = F, TotalStaffMeetCount = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "Meet", objectId = MeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Meet
	#'
	#' This function deletes a Meet
	#' @param MeetID The ID of the Meet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetID of the deleted Meet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeet <- function(MeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "Meet", objectId = MeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Meet
	#'
	#' This function creates a Meet
	#' @param fieldNames The field values to give the created Meet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created Meet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMeet <- function(CalendarIDStaff = NULL, DisplayPeriodRotationID = NULL, EndDate = NULL, EntityID = NULL, IsPrimary = NULL, LockAllMeetDayRotationsFromSectionScheduler = NULL, LockAllMeetDisplayPeriodsFromSectionScheduler = NULL, LockAllStaffMeetsFromSectionScheduler = NULL, LockRoomFromSectionScheduler = NULL, MeetIDClonedFrom = NULL, OverridePeriodEndTime = NULL, OverridePeriodStartTime = NULL, RoomID = NULL, SchoolYearID = NULL, SectionID = NULL, StartDate = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "Meet", body = list(DataObject = body), searchFields = append("MeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Meet
	#'
	#' This function modifies a Meet
	#' @param fieldNames The field values to give the modified Meet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified Meet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMeet <- function(MeetID, CalendarIDStaff = NULL, DisplayPeriodRotationID = NULL, EndDate = NULL, EntityID = NULL, IsPrimary = NULL, LockAllMeetDayRotationsFromSectionScheduler = NULL, LockAllMeetDisplayPeriodsFromSectionScheduler = NULL, LockAllStaffMeetsFromSectionScheduler = NULL, LockRoomFromSectionScheduler = NULL, MeetIDClonedFrom = NULL, OverridePeriodEndTime = NULL, OverridePeriodStartTime = NULL, RoomID = NULL, SchoolYearID = NULL, SectionID = NULL, StartDate = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "Meet", objectId = MeetID, body = list(DataObject = body), searchFields = append("MeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MeetDisplayPeriods
	#'
	#' This function returns a dataframe or json object of MeetDisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetDisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetDisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetDisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MeetDisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeetDisplayPeriods <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodID = F, HideMeetDisplayPeriod = F, IsPrimary = F, LockDisplayPeriod = F, MeetDisplayPeriodEndTime = F, MeetDisplayPeriodID = F, MeetDisplayPeriodIDClonedFrom = F, MeetDisplayPeriodStartTime = F, MeetID = F, ModifiedTime = F, ScheduledBySectionScheduler = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MeetDisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MeetDisplayPeriod
	#'
	#' This function returns a dataframe or json object of a MeetDisplayPeriod
	#' @param MeetDisplayPeriodID The ID of the MeetDisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetDisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetDisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetDisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeetDisplayPeriod <- function(MeetDisplayPeriodID, CreatedTime = F, DisplayPeriodID = F, HideMeetDisplayPeriod = F, IsPrimary = F, LockDisplayPeriod = F, MeetDisplayPeriodEndTime = F, MeetDisplayPeriodIDClonedFrom = F, MeetDisplayPeriodStartTime = F, MeetID = F, ModifiedTime = F, ScheduledBySectionScheduler = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetDisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MeetDisplayPeriod", objectId = MeetDisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MeetDisplayPeriod
	#'
	#' This function deletes a MeetDisplayPeriod
	#' @param MeetDisplayPeriodID The ID of the MeetDisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetDisplayPeriodID of the deleted MeetDisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeetDisplayPeriod <- function(MeetDisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MeetDisplayPeriod", objectId = MeetDisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MeetDisplayPeriod
	#'
	#' This function creates a MeetDisplayPeriod
	#' @param fieldNames The field values to give the created MeetDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMeetDisplayPeriod <- function(DisplayPeriodID = NULL, HideMeetDisplayPeriod = NULL, IsPrimary = NULL, LockDisplayPeriod = NULL, MeetDisplayPeriodIDClonedFrom = NULL, MeetID = NULL, ScheduledBySectionScheduler = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MeetDisplayPeriod", body = list(DataObject = body), searchFields = append("MeetDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MeetDisplayPeriod
	#'
	#' This function modifies a MeetDisplayPeriod
	#' @param fieldNames The field values to give the modified MeetDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMeetDisplayPeriod <- function(MeetDisplayPeriodID, DisplayPeriodID = NULL, HideMeetDisplayPeriod = NULL, IsPrimary = NULL, LockDisplayPeriod = NULL, MeetDisplayPeriodIDClonedFrom = NULL, MeetID = NULL, ScheduledBySectionScheduler = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MeetDisplayPeriod", objectId = MeetDisplayPeriodID, body = list(DataObject = body), searchFields = append("MeetDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StaffMeets
	#'
	#' This function returns a dataframe or json object of StaffMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StaffMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStaffMeets <- function(searchConditionsList = NULL, ApplyClosedGradingPeriodGradeChangePermission = F, AssignmentCount = F, CanMakeClosedGradingPeriodGradeChange = F, ClosedGradingPeriodGradeChangeCount = F, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, GradebookLastAccessedTime = F, HasAttendanceAccess = F, HasAttendanceAccessAsOfDate = F, HasGradebookAccess = F, IsLongTermSubstitute = F, IsPrimary = F, IsStaffCertified = F, IsSubstitute = F, LockStaffFromSectionScheduler = F, MeetID = F, MeetIsCurrent = F, MeetsToday = F, ModifiedTime = F, NameMeetDescription = F, ScheduledBySectionScheduler = F, SchoolYearID = F, SectionID = F, StaffID = F, StaffMeetID = F, StaffMeetIDClonedFrom = F, StaffMeetIDSubstituteFor = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StaffMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StaffMeet
	#'
	#' This function returns a dataframe or json object of a StaffMeet
	#' @param StaffMeetID The ID of the StaffMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStaffMeet <- function(StaffMeetID, ApplyClosedGradingPeriodGradeChangePermission = F, AssignmentCount = F, CanMakeClosedGradingPeriodGradeChange = F, ClosedGradingPeriodGradeChangeCount = F, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, GradebookLastAccessedTime = F, HasAttendanceAccess = F, HasAttendanceAccessAsOfDate = F, HasGradebookAccess = F, IsLongTermSubstitute = F, IsPrimary = F, IsStaffCertified = F, IsSubstitute = F, LockStaffFromSectionScheduler = F, MeetID = F, MeetIsCurrent = F, MeetsToday = F, ModifiedTime = F, NameMeetDescription = F, ScheduledBySectionScheduler = F, SchoolYearID = F, SectionID = F, StaffID = F, StaffMeetIDClonedFrom = F, StaffMeetIDSubstituteFor = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StaffMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StaffMeet", objectId = StaffMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StaffMeet
	#'
	#' This function deletes a StaffMeet
	#' @param StaffMeetID The ID of the StaffMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StaffMeetID of the deleted StaffMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStaffMeet <- function(StaffMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StaffMeet", objectId = StaffMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StaffMeet
	#'
	#' This function creates a StaffMeet
	#' @param fieldNames The field values to give the created StaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStaffMeet <- function(EffectiveEndDate = NULL, EffectiveStartDate = NULL, HasAttendanceAccess = NULL, HasGradebookAccess = NULL, IsLongTermSubstitute = NULL, IsPrimary = NULL, IsStaffCertified = NULL, IsSubstitute = NULL, LockStaffFromSectionScheduler = NULL, MeetID = NULL, ScheduledBySectionScheduler = NULL, SchoolYearID = NULL, SectionID = NULL, StaffID = NULL, StaffMeetIDClonedFrom = NULL, StaffMeetIDSubstituteFor = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StaffMeet", body = list(DataObject = body), searchFields = append("StaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StaffMeet
	#'
	#' This function modifies a StaffMeet
	#' @param fieldNames The field values to give the modified StaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStaffMeet <- function(StaffMeetID, EffectiveEndDate = NULL, EffectiveStartDate = NULL, HasAttendanceAccess = NULL, HasGradebookAccess = NULL, IsLongTermSubstitute = NULL, IsPrimary = NULL, IsStaffCertified = NULL, IsSubstitute = NULL, LockStaffFromSectionScheduler = NULL, MeetID = NULL, ScheduledBySectionScheduler = NULL, SchoolYearID = NULL, SectionID = NULL, StaffID = NULL, StaffMeetIDClonedFrom = NULL, StaffMeetIDSubstituteFor = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StaffMeet", objectId = StaffMeetID, body = list(DataObject = body), searchFields = append("StaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulingCategories
	#'
	#' This function returns a dataframe or json object of SectionSchedulingCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulingCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulingCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulingCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulingCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulingCategories <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SchedulingCategoryID = F, SectionID = F, SectionSchedulingCategoryID = F, SectionSchedulingCategoryIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulingCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulingCategory
	#'
	#' This function returns a dataframe or json object of a SectionSchedulingCategory
	#' @param SectionSchedulingCategoryID The ID of the SectionSchedulingCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulingCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulingCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulingCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulingCategory <- function(SectionSchedulingCategoryID, CreatedTime = F, ModifiedTime = F, SchedulingCategoryID = F, SectionID = F, SectionSchedulingCategoryIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulingCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulingCategory", objectId = SectionSchedulingCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulingCategory
	#'
	#' This function deletes a SectionSchedulingCategory
	#' @param SectionSchedulingCategoryID The ID of the SectionSchedulingCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulingCategoryID of the deleted SectionSchedulingCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulingCategory <- function(SectionSchedulingCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulingCategory", objectId = SectionSchedulingCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulingCategory
	#'
	#' This function creates a SectionSchedulingCategory
	#' @param fieldNames The field values to give the created SectionSchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulingCategory <- function(SchedulingCategoryID = NULL, SectionID = NULL, SectionSchedulingCategoryIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulingCategory", body = list(DataObject = body), searchFields = append("SectionSchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulingCategory
	#'
	#' This function modifies a SectionSchedulingCategory
	#' @param fieldNames The field values to give the modified SectionSchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulingCategory <- function(SectionSchedulingCategoryID, SchedulingCategoryID = NULL, SectionID = NULL, SectionSchedulingCategoryIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulingCategory", objectId = SectionSchedulingCategoryID, body = list(DataObject = body), searchFields = append("SectionSchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingPeriodDisplayPeriods
	#'
	#' This function returns a dataframe or json object of SchedulingPeriodDisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingPeriodDisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingPeriodDisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingPeriodDisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingPeriodDisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingPeriodDisplayPeriods <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodID = F, EntityGroupKey = F, ModifiedTime = F, SchedulingPeriodDisplayPeriodID = F, SchedulingPeriodDisplayPeriodIDClonedFrom = F, SchedulingPeriodID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingPeriodDisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingPeriodDisplayPeriod
	#'
	#' This function returns a dataframe or json object of a SchedulingPeriodDisplayPeriod
	#' @param SchedulingPeriodDisplayPeriodID The ID of the SchedulingPeriodDisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingPeriodDisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingPeriodDisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingPeriodDisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingPeriodDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingPeriodDisplayPeriod <- function(SchedulingPeriodDisplayPeriodID, CreatedTime = F, DisplayPeriodID = F, EntityGroupKey = F, ModifiedTime = F, SchedulingPeriodDisplayPeriodIDClonedFrom = F, SchedulingPeriodID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingPeriodDisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingPeriodDisplayPeriod", objectId = SchedulingPeriodDisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingPeriodDisplayPeriod
	#'
	#' This function deletes a SchedulingPeriodDisplayPeriod
	#' @param SchedulingPeriodDisplayPeriodID The ID of the SchedulingPeriodDisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingPeriodDisplayPeriodID of the deleted SchedulingPeriodDisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingPeriodDisplayPeriod <- function(SchedulingPeriodDisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingPeriodDisplayPeriod", objectId = SchedulingPeriodDisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingPeriodDisplayPeriod
	#'
	#' This function creates a SchedulingPeriodDisplayPeriod
	#' @param fieldNames The field values to give the created SchedulingPeriodDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingPeriodDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingPeriodDisplayPeriod <- function(DisplayPeriodID = NULL, EntityGroupKey = NULL, SchedulingPeriodDisplayPeriodIDClonedFrom = NULL, SchedulingPeriodID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingPeriodDisplayPeriod", body = list(DataObject = body), searchFields = append("SchedulingPeriodDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingPeriodDisplayPeriod
	#'
	#' This function modifies a SchedulingPeriodDisplayPeriod
	#' @param fieldNames The field values to give the modified SchedulingPeriodDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingPeriodDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingPeriodDisplayPeriod <- function(SchedulingPeriodDisplayPeriodID, DisplayPeriodID = NULL, EntityGroupKey = NULL, SchedulingPeriodDisplayPeriodIDClonedFrom = NULL, SchedulingPeriodID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingPeriodDisplayPeriod", objectId = SchedulingPeriodDisplayPeriodID, body = list(DataObject = body), searchFields = append("SchedulingPeriodDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentEntityYearSchedulingCategories
	#'
	#' This function returns a dataframe or json object of StudentEntityYearSchedulingCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentEntityYearSchedulingCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentEntityYearSchedulingCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentEntityYearSchedulingCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentEntityYearSchedulingCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentEntityYearSchedulingCategories <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, SchedulingCategoryID = F, StudentEntityYearID = F, StudentEntityYearSchedulingCategoryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentEntityYearSchedulingCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentEntityYearSchedulingCategory
	#'
	#' This function returns a dataframe or json object of a StudentEntityYearSchedulingCategory
	#' @param StudentEntityYearSchedulingCategoryID The ID of the StudentEntityYearSchedulingCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentEntityYearSchedulingCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentEntityYearSchedulingCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentEntityYearSchedulingCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentEntityYearSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentEntityYearSchedulingCategory <- function(StudentEntityYearSchedulingCategoryID, CreatedTime = F, ModifiedTime = F, SchedulingCategoryID = F, StudentEntityYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentEntityYearSchedulingCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentEntityYearSchedulingCategory", objectId = StudentEntityYearSchedulingCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentEntityYearSchedulingCategory
	#'
	#' This function deletes a StudentEntityYearSchedulingCategory
	#' @param StudentEntityYearSchedulingCategoryID The ID of the StudentEntityYearSchedulingCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentEntityYearSchedulingCategoryID of the deleted StudentEntityYearSchedulingCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentEntityYearSchedulingCategory <- function(StudentEntityYearSchedulingCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentEntityYearSchedulingCategory", objectId = StudentEntityYearSchedulingCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentEntityYearSchedulingCategory
	#'
	#' This function creates a StudentEntityYearSchedulingCategory
	#' @param fieldNames The field values to give the created StudentEntityYearSchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentEntityYearSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentEntityYearSchedulingCategory <- function(SchedulingCategoryID = NULL, StudentEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentEntityYearSchedulingCategory", body = list(DataObject = body), searchFields = append("StudentEntityYearSchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentEntityYearSchedulingCategory
	#'
	#' This function modifies a StudentEntityYearSchedulingCategory
	#' @param fieldNames The field values to give the modified StudentEntityYearSchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentEntityYearSchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentEntityYearSchedulingCategory <- function(StudentEntityYearSchedulingCategoryID, SchedulingCategoryID = NULL, StudentEntityYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentEntityYearSchedulingCategory", objectId = StudentEntityYearSchedulingCategoryID, body = list(DataObject = body), searchFields = append("StudentEntityYearSchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionTransactions
	#'
	#' This function returns a dataframe or json object of StudentSectionTransactions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionTransactions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionTransactions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionTransaction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentSectionTransactions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionTransactions <- function(searchConditionsList = NULL, CalendarID = F, CountsTowardsViewingEntity = F, CreatedTime = F, EarlyExitReasonID = F, EndDate = F, EndsAfterSectionLengthStartDate = F, EntityIDCountsAs = F, HideNewStudentButton = F, IsCECE = F, IsInProgress = F, IsInProgressAsOfToday = F, ModifiedTime = F, NameIDRequestedBy = F, OverlapsSectionLength = F, SectionID = F, SectionLengthSubsetID = F, StartDate = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentSectionTransaction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionTransaction
	#'
	#' This function returns a dataframe or json object of a StudentSectionTransaction
	#' @param StudentSectionTransactionID The ID of the StudentSectionTransaction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionTransaction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionTransaction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionTransaction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionTransaction <- function(StudentSectionTransactionID, CalendarID = F, CountsTowardsViewingEntity = F, CreatedTime = F, EarlyExitReasonID = F, EndDate = F, EndsAfterSectionLengthStartDate = F, EntityIDCountsAs = F, HideNewStudentButton = F, IsCECE = F, IsInProgress = F, IsInProgressAsOfToday = F, ModifiedTime = F, NameIDRequestedBy = F, OverlapsSectionLength = F, SectionID = F, SectionLengthSubsetID = F, StartDate = F, StudentID = F, StudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionTransactionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentSectionTransaction", objectId = StudentSectionTransactionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionTransaction
	#'
	#' This function deletes a StudentSectionTransaction
	#' @param StudentSectionTransactionID The ID of the StudentSectionTransaction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentSectionTransactionID of the deleted StudentSectionTransaction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionTransaction <- function(StudentSectionTransactionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentSectionTransaction", objectId = StudentSectionTransactionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSectionTransaction
	#'
	#' This function creates a StudentSectionTransaction
	#' @param fieldNames The field values to give the created StudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSectionTransaction <- function(EarlyExitReasonID = NULL, EndDate = NULL, EntityIDCountsAs = NULL, HideNewStudentButton = NULL, NameIDRequestedBy = NULL, SectionID = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentID = NULL, StudentSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentSectionTransaction", body = list(DataObject = body), searchFields = append("StudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSectionTransaction
	#'
	#' This function modifies a StudentSectionTransaction
	#' @param fieldNames The field values to give the modified StudentSectionTransaction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentSectionTransaction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSectionTransaction <- function(StudentSectionTransactionID, EarlyExitReasonID = NULL, EndDate = NULL, EntityIDCountsAs = NULL, HideNewStudentButton = NULL, NameIDRequestedBy = NULL, SectionID = NULL, SectionLengthSubsetID = NULL, StartDate = NULL, StudentID = NULL, StudentSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentSectionTransaction", objectId = StudentSectionTransactionID, body = list(DataObject = body), searchFields = append("StudentSectionTransactionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSections
	#'
	#' This function returns a dataframe or json object of StudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSections <- function(searchConditionsList = NULL, ActiveStudentGroups = F, AllTransactionsCountTowardsViewingEntity = F, AssignmentDueDateAttendance = F, ConversionKey = F, CountsForViewingEntity = F, CourseOrTransferDescription = F, CreatedTime = F, DisplayLinkedStudentSection = F, DisplayToViewingEntity = F, EarnedCreditAttempted = F, EarnedCreditAttemptedAllEntered = F, EarnedCreditAttemptedCompleted = F, EarnedCreditOverride = F, EarnedCredits = F, EarnedCreditsAllEntered = F, EarnedCreditsCompleted = F, EarnedCreditsPossible = F, EarnedCreditsPossibleAllEntered = F, EarnedCreditsPossibleCompleted = F, EndDate = F, EntityIDCourse = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, ExistsStudentSectionGPAMethods = F, FailedCredits = F, FailedCreditsAllEntered = F, FailedCreditsCompleted = F, FilteredGradedAssignmentCount = F, FilteredMissingAssignmentCount = F, FilteredUnGradedAssignmentCount = F, GradebookSortCode = F, GradebookStudentNameToUse = F, GradeReferenceID = F, HasAtLeastOneCrossEntityStudentSectionTransaction = F, HasAtleastOneTransactionNotCountTowardsViewingEntity = F, HasLinkingConflicts = F, HasStudentGradingScaleGroupForGradingPeriodGradeBucket = F, HasStudentSectionNoteForCurrentUser = F, InProgress = F, InProgressAndIsEffectiveForViewingEntity = F, IsActiveAsOfDate = F, IsActiveForTodayOrForSectionStartOrEnd = F, IsAlternativeCreditCourse = F, IsAvailableForAssignmentStudentGroup = F, IsBeforeOrInProgress = F, IsBeforeOrInProgressAndNotHasAtleastOneTransactionNotCountTowardsViewingEntity = F, IsCurrentStudentSection = F, IsEffectiveForViewingEntity = F, IsFlaggedAsMissingAssignmentCount = F, IsForCurrentSchoolYear = F, IsStudentSectionScheduledToMeet = F, IsTransferCourse = F, IsTSAProficient = F, IsWorkInProgress = F, LastStudentSectionTransactionConsideredDropped = F, LinkedStudentSectionsEarnedCredit = F, LinkedStudentSectionsEarnedCreditAttempted = F, LinkedStudentSectionsFailedCredit = F, MissingAssignmentCount = F, ModifiedTime = F, MultipleTransactions = F, RenderTransferGradesRowButton = F, RequestedFromOfferingEntityAndAllTransactionsCountTowardsViewingEntity = F, RequestedFromViewingEntityAndAllTransactionsCountTowardsViewingEntity = F, SchoolYearIDCourse = F, SectionID = F, SectionLengthSubsetSummary = F, StartDate = F, Status = F, StudentID = F, StudentSectionCode = F, StudentSectionID = F, StudentSectionLink = F, StudentSectionMNID = F, StudentSectionNoteCountForCurrentUser = F, StudentSectionTypeOverride = F, TotalDaysAbsent = F, TotalDaysExcused = F, TotalDaysOther = F, TotalDaysTardy = F, TotalDaysUnexcused = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransactionCountsForViewingEntity = F, TransferCourseName = F, TransferDistrictName = F, UnscoredPastDueAssignmentCount = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSection
	#'
	#' This function returns a dataframe or json object of a StudentSection
	#' @param StudentSectionID The ID of the StudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSection <- function(StudentSectionID, ActiveStudentGroups = F, AllTransactionsCountTowardsViewingEntity = F, AssignmentDueDateAttendance = F, ConversionKey = F, CountsForViewingEntity = F, CourseOrTransferDescription = F, CreatedTime = F, DisplayLinkedStudentSection = F, DisplayToViewingEntity = F, EarnedCreditAttempted = F, EarnedCreditAttemptedAllEntered = F, EarnedCreditAttemptedCompleted = F, EarnedCreditOverride = F, EarnedCredits = F, EarnedCreditsAllEntered = F, EarnedCreditsCompleted = F, EarnedCreditsPossible = F, EarnedCreditsPossibleAllEntered = F, EarnedCreditsPossibleCompleted = F, EndDate = F, EntityIDCourse = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, ExistsStudentSectionGPAMethods = F, FailedCredits = F, FailedCreditsAllEntered = F, FailedCreditsCompleted = F, FilteredGradedAssignmentCount = F, FilteredMissingAssignmentCount = F, FilteredUnGradedAssignmentCount = F, GradebookSortCode = F, GradebookStudentNameToUse = F, GradeReferenceID = F, HasAtLeastOneCrossEntityStudentSectionTransaction = F, HasAtleastOneTransactionNotCountTowardsViewingEntity = F, HasLinkingConflicts = F, HasStudentGradingScaleGroupForGradingPeriodGradeBucket = F, HasStudentSectionNoteForCurrentUser = F, InProgress = F, InProgressAndIsEffectiveForViewingEntity = F, IsActiveAsOfDate = F, IsActiveForTodayOrForSectionStartOrEnd = F, IsAlternativeCreditCourse = F, IsAvailableForAssignmentStudentGroup = F, IsBeforeOrInProgress = F, IsBeforeOrInProgressAndNotHasAtleastOneTransactionNotCountTowardsViewingEntity = F, IsCurrentStudentSection = F, IsEffectiveForViewingEntity = F, IsFlaggedAsMissingAssignmentCount = F, IsForCurrentSchoolYear = F, IsStudentSectionScheduledToMeet = F, IsTransferCourse = F, IsTSAProficient = F, IsWorkInProgress = F, LastStudentSectionTransactionConsideredDropped = F, LinkedStudentSectionsEarnedCredit = F, LinkedStudentSectionsEarnedCreditAttempted = F, LinkedStudentSectionsFailedCredit = F, MissingAssignmentCount = F, ModifiedTime = F, MultipleTransactions = F, RenderTransferGradesRowButton = F, RequestedFromOfferingEntityAndAllTransactionsCountTowardsViewingEntity = F, RequestedFromViewingEntityAndAllTransactionsCountTowardsViewingEntity = F, SchoolYearIDCourse = F, SectionID = F, SectionLengthSubsetSummary = F, StartDate = F, Status = F, StudentID = F, StudentSectionCode = F, StudentSectionLink = F, StudentSectionMNID = F, StudentSectionNoteCountForCurrentUser = F, StudentSectionTypeOverride = F, TotalDaysAbsent = F, TotalDaysExcused = F, TotalDaysOther = F, TotalDaysTardy = F, TotalDaysUnexcused = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransactionCountsForViewingEntity = F, TransferCourseName = F, TransferDistrictName = F, UnscoredPastDueAssignmentCount = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ViewingFromOfferingEntity = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentSection", objectId = StudentSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSection
	#'
	#' This function deletes a StudentSection
	#' @param StudentSectionID The ID of the StudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentSectionID of the deleted StudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSection <- function(StudentSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentSection", objectId = StudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StudentSection
	#'
	#' This function creates a StudentSection
	#' @param fieldNames The field values to give the created StudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created StudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStudentSection <- function(ConversionKey = NULL, EntityIDCourse = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsTSAProficient = NULL, IsWorkInProgress = NULL, SchoolYearIDCourse = NULL, SectionID = NULL, SectionLengthSubsetSummary = NULL, StudentID = NULL, StudentSectionLink = NULL, StudentSectionTypeOverride = NULL, TransferCourseName = NULL, TransferDistrictName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "StudentSection", body = list(DataObject = body), searchFields = append("StudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StudentSection
	#'
	#' This function modifies a StudentSection
	#' @param fieldNames The field values to give the modified StudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified StudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStudentSection <- function(StudentSectionID, ConversionKey = NULL, EntityIDCourse = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsTSAProficient = NULL, IsWorkInProgress = NULL, SchoolYearIDCourse = NULL, SectionID = NULL, SectionLengthSubsetSummary = NULL, StudentID = NULL, StudentSectionLink = NULL, StudentSectionTypeOverride = NULL, TransferCourseName = NULL, TransferDistrictName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "StudentSection", objectId = StudentSectionID, body = list(DataObject = body), searchFields = append("StudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingTeams
	#'
	#' This function returns a dataframe or json object of SchedulingTeams
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTeams. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTeams.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTeam') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingTeams
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingTeams <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchedulingTeamID = F, SchedulingTeamIDClonedFrom = F, SchedulingTeamIDClonedTo = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingTeam", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingTeam
	#'
	#' This function returns a dataframe or json object of a SchedulingTeam
	#' @param SchedulingTeamID The ID of the SchedulingTeam to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTeam. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTeam.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTeam') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingTeam <- function(SchedulingTeamID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchedulingTeamIDClonedFrom = F, SchedulingTeamIDClonedTo = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingTeamID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingTeam", objectId = SchedulingTeamID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingTeam
	#'
	#' This function deletes a SchedulingTeam
	#' @param SchedulingTeamID The ID of the SchedulingTeam to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingTeamID of the deleted SchedulingTeam.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingTeam <- function(SchedulingTeamID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingTeam", objectId = SchedulingTeamID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingTeam
	#'
	#' This function creates a SchedulingTeam
	#' @param fieldNames The field values to give the created SchedulingTeam. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingTeam <- function(Code = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchedulingTeamIDClonedFrom = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingTeam", body = list(DataObject = body), searchFields = append("SchedulingTeamID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingTeam
	#'
	#' This function modifies a SchedulingTeam
	#' @param fieldNames The field values to give the modified SchedulingTeam. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingTeam
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingTeam <- function(SchedulingTeamID, Code = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchedulingTeamIDClonedFrom = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingTeam", objectId = SchedulingTeamID, body = list(DataObject = body), searchFields = append("SchedulingTeamID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingPeriods
	#'
	#' This function returns a dataframe or json object of SchedulingPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingPeriods <- function(searchConditionsList = NULL, CodeNumber = F, CreatedTime = F, DayRotationID = F, DynamicRelationshipID = F, EntityGroupKey = F, ModifiedTime = F, SchedulingPeriodID = F, SchedulingPeriodIDClonedFrom = F, SchedulingPeriodIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingPeriod
	#'
	#' This function returns a dataframe or json object of a SchedulingPeriod
	#' @param SchedulingPeriodID The ID of the SchedulingPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingPeriod <- function(SchedulingPeriodID, CodeNumber = F, CreatedTime = F, DayRotationID = F, DynamicRelationshipID = F, EntityGroupKey = F, ModifiedTime = F, SchedulingPeriodIDClonedFrom = F, SchedulingPeriodIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingPeriod", objectId = SchedulingPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingPeriod
	#'
	#' This function deletes a SchedulingPeriod
	#' @param SchedulingPeriodID The ID of the SchedulingPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingPeriodID of the deleted SchedulingPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingPeriod <- function(SchedulingPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingPeriod", objectId = SchedulingPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingPeriod
	#'
	#' This function creates a SchedulingPeriod
	#' @param fieldNames The field values to give the created SchedulingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingPeriod <- function(CodeNumber = NULL, DayRotationID = NULL, EntityGroupKey = NULL, SchedulingPeriodIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingPeriod", body = list(DataObject = body), searchFields = append("SchedulingPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingPeriod
	#'
	#' This function modifies a SchedulingPeriod
	#' @param fieldNames The field values to give the modified SchedulingPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingPeriod <- function(SchedulingPeriodID, CodeNumber = NULL, DayRotationID = NULL, EntityGroupKey = NULL, SchedulingPeriodIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingPeriod", objectId = SchedulingPeriodID, body = list(DataObject = body), searchFields = append("SchedulingPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingCategories
	#'
	#' This function returns a dataframe or json object of SchedulingCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingCategories <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchedulingCategoryID = F, SchedulingCategoryIDClonedFrom = F, SchedulingCategoryIDClonedTo = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingCategory
	#'
	#' This function returns a dataframe or json object of a SchedulingCategory
	#' @param SchedulingCategoryID The ID of the SchedulingCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingCategory <- function(SchedulingCategoryID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EntityGroupKey = F, EntityID = F, ModifiedTime = F, SchedulingCategoryIDClonedFrom = F, SchedulingCategoryIDClonedTo = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingCategory", objectId = SchedulingCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingCategory
	#'
	#' This function deletes a SchedulingCategory
	#' @param SchedulingCategoryID The ID of the SchedulingCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingCategoryID of the deleted SchedulingCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingCategory <- function(SchedulingCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingCategory", objectId = SchedulingCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingCategory
	#'
	#' This function creates a SchedulingCategory
	#' @param fieldNames The field values to give the created SchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingCategory <- function(Code = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchedulingCategoryIDClonedFrom = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingCategory", body = list(DataObject = body), searchFields = append("SchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingCategory
	#'
	#' This function modifies a SchedulingCategory
	#' @param fieldNames The field values to give the modified SchedulingCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingCategory <- function(SchedulingCategoryID, Code = NULL, Description = NULL, EntityGroupKey = NULL, EntityID = NULL, SchedulingCategoryIDClonedFrom = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingCategory", objectId = SchedulingCategoryID, body = list(DataObject = body), searchFields = append("SchedulingCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisplayPeriodConflicts
	#'
	#' This function returns a dataframe or json object of DisplayPeriodConflicts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriodConflicts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriodConflicts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriodConflict') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of DisplayPeriodConflicts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisplayPeriodConflicts <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodConflictID = F, DisplayPeriodIDBase = F, DisplayPeriodIDConflicting = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "DisplayPeriodConflict", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisplayPeriodConflict
	#'
	#' This function returns a dataframe or json object of a DisplayPeriodConflict
	#' @param DisplayPeriodConflictID The ID of the DisplayPeriodConflict to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriodConflict. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriodConflict.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriodConflict') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of DisplayPeriodConflict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisplayPeriodConflict <- function(DisplayPeriodConflictID, CreatedTime = F, DisplayPeriodIDBase = F, DisplayPeriodIDConflicting = F, EntityGroupKey = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisplayPeriodConflictID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "DisplayPeriodConflict", objectId = DisplayPeriodConflictID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisplayPeriodConflict
	#'
	#' This function deletes a DisplayPeriodConflict
	#' @param DisplayPeriodConflictID The ID of the DisplayPeriodConflict to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The DisplayPeriodConflictID of the deleted DisplayPeriodConflict.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisplayPeriodConflict <- function(DisplayPeriodConflictID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "DisplayPeriodConflict", objectId = DisplayPeriodConflictID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisplayPeriodConflict
	#'
	#' This function creates a DisplayPeriodConflict
	#' @param fieldNames The field values to give the created DisplayPeriodConflict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created DisplayPeriodConflict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisplayPeriodConflict <- function(DisplayPeriodIDBase = NULL, DisplayPeriodIDConflicting = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "DisplayPeriodConflict", body = list(DataObject = body), searchFields = append("DisplayPeriodConflictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisplayPeriodConflict
	#'
	#' This function modifies a DisplayPeriodConflict
	#' @param fieldNames The field values to give the modified DisplayPeriodConflict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified DisplayPeriodConflict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisplayPeriodConflict <- function(DisplayPeriodConflictID, DisplayPeriodIDBase = NULL, DisplayPeriodIDConflicting = NULL, EntityGroupKey = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "DisplayPeriodConflict", objectId = DisplayPeriodConflictID, body = list(DataObject = body), searchFields = append("DisplayPeriodConflictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisplayPeriods
	#'
	#' This function returns a dataframe or json object of DisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of DisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisplayPeriods <- function(searchConditionsList = NULL, AttendancePeriodID = F, Code = F, CodeDescription = F, CodeDescriptionDayRotation = F, CreatedTime = F, DayRotationID = F, Description = F, DisplayPeriodCodeDayRotationCode = F, DisplayPeriodEndTime = F, DisplayPeriodID = F, DisplayPeriodIDClonedFrom = F, DisplayPeriodIDClonedTo = F, DisplayPeriodStartTime = F, EntityGroupKey = F, IsLunchPeriod = F, IsOutsideRegularSchoolDay = F, ModifiedTime = F, SortNumber = F, TeachingHourEquivalent = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "DisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisplayPeriod
	#'
	#' This function returns a dataframe or json object of a DisplayPeriod
	#' @param DisplayPeriodID The ID of the DisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of DisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisplayPeriod <- function(DisplayPeriodID, AttendancePeriodID = F, Code = F, CodeDescription = F, CodeDescriptionDayRotation = F, CreatedTime = F, DayRotationID = F, Description = F, DisplayPeriodCodeDayRotationCode = F, DisplayPeriodEndTime = F, DisplayPeriodIDClonedFrom = F, DisplayPeriodIDClonedTo = F, DisplayPeriodStartTime = F, EntityGroupKey = F, IsLunchPeriod = F, IsOutsideRegularSchoolDay = F, ModifiedTime = F, SortNumber = F, TeachingHourEquivalent = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "DisplayPeriod", objectId = DisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisplayPeriod
	#'
	#' This function deletes a DisplayPeriod
	#' @param DisplayPeriodID The ID of the DisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The DisplayPeriodID of the deleted DisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisplayPeriod <- function(DisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "DisplayPeriod", objectId = DisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisplayPeriod
	#'
	#' This function creates a DisplayPeriod
	#' @param fieldNames The field values to give the created DisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created DisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisplayPeriod <- function(AttendancePeriodID = NULL, Code = NULL, DayRotationID = NULL, Description = NULL, DisplayPeriodIDClonedFrom = NULL, EntityGroupKey = NULL, IsLunchPeriod = NULL, IsOutsideRegularSchoolDay = NULL, SortNumber = NULL, TeachingHourEquivalent = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "DisplayPeriod", body = list(DataObject = body), searchFields = append("DisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisplayPeriod
	#'
	#' This function modifies a DisplayPeriod
	#' @param fieldNames The field values to give the modified DisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified DisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisplayPeriod <- function(DisplayPeriodID, AttendancePeriodID = NULL, Code = NULL, DayRotationID = NULL, Description = NULL, DisplayPeriodIDClonedFrom = NULL, EntityGroupKey = NULL, IsLunchPeriod = NULL, IsOutsideRegularSchoolDay = NULL, SortNumber = NULL, TeachingHourEquivalent = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "DisplayPeriod", objectId = DisplayPeriodID, body = list(DataObject = body), searchFields = append("DisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DayRotations
	#'
	#' This function returns a dataframe or json object of DayRotations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DayRotations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DayRotations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DayRotation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of DayRotations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDayRotations <- function(searchConditionsList = NULL, Code = F, ConsecutiveTeachingHourLimit = F, CreatedTime = F, DayRotationID = F, DayRotationIDClonedFrom = F, DayRotationIDClonedTo = F, EntityGroupKey = F, EntityID = F, MaximumTeachingHourLimit = F, ModifiedTime = F, SchoolYearID = F, SortNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "DayRotation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DayRotation
	#'
	#' This function returns a dataframe or json object of a DayRotation
	#' @param DayRotationID The ID of the DayRotation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DayRotation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DayRotation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DayRotation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of DayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDayRotation <- function(DayRotationID, Code = F, ConsecutiveTeachingHourLimit = F, CreatedTime = F, DayRotationIDClonedFrom = F, DayRotationIDClonedTo = F, EntityGroupKey = F, EntityID = F, MaximumTeachingHourLimit = F, ModifiedTime = F, SchoolYearID = F, SortNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DayRotationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "DayRotation", objectId = DayRotationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DayRotation
	#'
	#' This function deletes a DayRotation
	#' @param DayRotationID The ID of the DayRotation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The DayRotationID of the deleted DayRotation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDayRotation <- function(DayRotationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "DayRotation", objectId = DayRotationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DayRotation
	#'
	#' This function creates a DayRotation
	#' @param fieldNames The field values to give the created DayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created DayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDayRotation <- function(Code = NULL, ConsecutiveTeachingHourLimit = NULL, DayRotationIDClonedFrom = NULL, EntityGroupKey = NULL, EntityID = NULL, MaximumTeachingHourLimit = NULL, SchoolYearID = NULL, SortNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "DayRotation", body = list(DataObject = body), searchFields = append("DayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DayRotation
	#'
	#' This function modifies a DayRotation
	#' @param fieldNames The field values to give the modified DayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified DayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDayRotation <- function(DayRotationID, Code = NULL, ConsecutiveTeachingHourLimit = NULL, DayRotationIDClonedFrom = NULL, EntityGroupKey = NULL, EntityID = NULL, MaximumTeachingHourLimit = NULL, SchoolYearID = NULL, SortNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "DayRotation", objectId = DayRotationID, body = list(DataObject = body), searchFields = append("DayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseSectionLengthExcludes
	#'
	#' This function returns a dataframe or json object of CourseSectionLengthExcludes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseSectionLengthExcludes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseSectionLengthExcludes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseSectionLengthExclude') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseSectionLengthExcludes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseSectionLengthExcludes <- function(searchConditionsList = NULL, CourseID = F, CourseSectionLengthExcludeID = F, CreatedTime = F, DistributionPercent = F, EntityGroupKey = F, ModifiedTime = F, SectionLengthID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseSectionLengthExclude", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseSectionLengthExclude
	#'
	#' This function returns a dataframe or json object of a CourseSectionLengthExclude
	#' @param CourseSectionLengthExcludeID The ID of the CourseSectionLengthExclude to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseSectionLengthExclude. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseSectionLengthExclude.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseSectionLengthExclude') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseSectionLengthExclude
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseSectionLengthExclude <- function(CourseSectionLengthExcludeID, CourseID = F, CreatedTime = F, DistributionPercent = F, EntityGroupKey = F, ModifiedTime = F, SectionLengthID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseSectionLengthExcludeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseSectionLengthExclude", objectId = CourseSectionLengthExcludeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseSectionLengthExclude
	#'
	#' This function deletes a CourseSectionLengthExclude
	#' @param CourseSectionLengthExcludeID The ID of the CourseSectionLengthExclude to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseSectionLengthExcludeID of the deleted CourseSectionLengthExclude.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseSectionLengthExclude <- function(CourseSectionLengthExcludeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseSectionLengthExclude", objectId = CourseSectionLengthExcludeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseSectionLengthExclude
	#'
	#' This function creates a CourseSectionLengthExclude
	#' @param fieldNames The field values to give the created CourseSectionLengthExclude. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseSectionLengthExclude
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseSectionLengthExclude <- function(CourseID = NULL, DistributionPercent = NULL, EntityGroupKey = NULL, SectionLengthID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseSectionLengthExclude", body = list(DataObject = body), searchFields = append("CourseSectionLengthExcludeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseSectionLengthExclude
	#'
	#' This function modifies a CourseSectionLengthExclude
	#' @param fieldNames The field values to give the modified CourseSectionLengthExclude. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseSectionLengthExclude
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseSectionLengthExclude <- function(CourseSectionLengthExcludeID, CourseID = NULL, DistributionPercent = NULL, EntityGroupKey = NULL, SectionLengthID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseSectionLengthExclude", objectId = CourseSectionLengthExcludeID, body = list(DataObject = body), searchFields = append("CourseSectionLengthExcludeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseEntityOfferedToSections
	#'
	#' This function returns a dataframe or json object of TempCourseEntityOfferedToSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseEntityOfferedToSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseEntityOfferedToSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseEntityOfferedToSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempCourseEntityOfferedToSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseEntityOfferedToSections <- function(searchConditionsList = NULL, CourseEntityOfferedToID = F, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionRecordExists = F, CourseID = F, CreatedTime = F, EntityIDOfferedTo = F, EntityOfferedToCode = F, EntityOfferedToName = F, IsActiveOverride = F, MaximumStudentCount = F, ModifiedTime = F, OriginalMaximumStudentCount = F, RowIsSelected = F, SchoolYearID = F, SeatsAvailable = F, SectionCode = F, SectionID = F, SectionIsActive = F, SectionMaximumStudentCount = F, SectionReservedSeatCount = F, SectionSectionLengthCode = F, TempCourseEntityOfferedToSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempCourseEntityOfferedToSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseEntityOfferedToSection
	#'
	#' This function returns a dataframe or json object of a TempCourseEntityOfferedToSection
	#' @param TempCourseEntityOfferedToSectionID The ID of the TempCourseEntityOfferedToSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseEntityOfferedToSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseEntityOfferedToSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseEntityOfferedToSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempCourseEntityOfferedToSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseEntityOfferedToSection <- function(TempCourseEntityOfferedToSectionID, CourseEntityOfferedToID = F, CourseEntityOfferedToSectionID = F, CourseEntityOfferedToSectionRecordExists = F, CourseID = F, CreatedTime = F, EntityIDOfferedTo = F, EntityOfferedToCode = F, EntityOfferedToName = F, IsActiveOverride = F, MaximumStudentCount = F, ModifiedTime = F, OriginalMaximumStudentCount = F, RowIsSelected = F, SchoolYearID = F, SeatsAvailable = F, SectionCode = F, SectionID = F, SectionIsActive = F, SectionMaximumStudentCount = F, SectionReservedSeatCount = F, SectionSectionLengthCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseEntityOfferedToSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempCourseEntityOfferedToSection", objectId = TempCourseEntityOfferedToSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseEntityOfferedToSection
	#'
	#' This function deletes a TempCourseEntityOfferedToSection
	#' @param TempCourseEntityOfferedToSectionID The ID of the TempCourseEntityOfferedToSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempCourseEntityOfferedToSectionID of the deleted TempCourseEntityOfferedToSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseEntityOfferedToSection <- function(TempCourseEntityOfferedToSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempCourseEntityOfferedToSection", objectId = TempCourseEntityOfferedToSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseEntityOfferedToSection
	#'
	#' This function creates a TempCourseEntityOfferedToSection
	#' @param fieldNames The field values to give the created TempCourseEntityOfferedToSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempCourseEntityOfferedToSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseEntityOfferedToSection <- function(CourseEntityOfferedToID = NULL, CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionRecordExists = NULL, CourseID = NULL, EntityIDOfferedTo = NULL, EntityOfferedToCode = NULL, EntityOfferedToName = NULL, IsActiveOverride = NULL, MaximumStudentCount = NULL, OriginalMaximumStudentCount = NULL, SchoolYearID = NULL, SeatsAvailable = NULL, SectionCode = NULL, SectionID = NULL, SectionIsActive = NULL, SectionMaximumStudentCount = NULL, SectionReservedSeatCount = NULL, SectionSectionLengthCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempCourseEntityOfferedToSection", body = list(DataObject = body), searchFields = append("TempCourseEntityOfferedToSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseEntityOfferedToSection
	#'
	#' This function modifies a TempCourseEntityOfferedToSection
	#' @param fieldNames The field values to give the modified TempCourseEntityOfferedToSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempCourseEntityOfferedToSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseEntityOfferedToSection <- function(TempCourseEntityOfferedToSectionID, CourseEntityOfferedToID = NULL, CourseEntityOfferedToSectionID = NULL, CourseEntityOfferedToSectionRecordExists = NULL, CourseID = NULL, EntityIDOfferedTo = NULL, EntityOfferedToCode = NULL, EntityOfferedToName = NULL, IsActiveOverride = NULL, MaximumStudentCount = NULL, OriginalMaximumStudentCount = NULL, SchoolYearID = NULL, SeatsAvailable = NULL, SectionCode = NULL, SectionID = NULL, SectionIsActive = NULL, SectionMaximumStudentCount = NULL, SectionReservedSeatCount = NULL, SectionSectionLengthCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempCourseEntityOfferedToSection", objectId = TempCourseEntityOfferedToSectionID, body = list(DataObject = body), searchFields = append("TempCourseEntityOfferedToSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MeetSummaryDetails
	#'
	#' This function returns a dataframe or json object of MeetSummaryDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetSummaryDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetSummaryDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetSummaryDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MeetSummaryDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeetSummaryDetails <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodID = F, IsPrimary = F, MeetSummaryDetailID = F, MeetSummaryID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MeetSummaryDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MeetSummaryDetail
	#'
	#' This function returns a dataframe or json object of a MeetSummaryDetail
	#' @param MeetSummaryDetailID The ID of the MeetSummaryDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetSummaryDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetSummaryDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetSummaryDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MeetSummaryDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeetSummaryDetail <- function(MeetSummaryDetailID, CreatedTime = F, DisplayPeriodID = F, IsPrimary = F, MeetSummaryID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetSummaryDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MeetSummaryDetail", objectId = MeetSummaryDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MeetSummaryDetail
	#'
	#' This function deletes a MeetSummaryDetail
	#' @param MeetSummaryDetailID The ID of the MeetSummaryDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetSummaryDetailID of the deleted MeetSummaryDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeetSummaryDetail <- function(MeetSummaryDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MeetSummaryDetail", objectId = MeetSummaryDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MeetSummaryDetail
	#'
	#' This function creates a MeetSummaryDetail
	#' @param fieldNames The field values to give the created MeetSummaryDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MeetSummaryDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMeetSummaryDetail <- function(DisplayPeriodID = NULL, IsPrimary = NULL, MeetSummaryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MeetSummaryDetail", body = list(DataObject = body), searchFields = append("MeetSummaryDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MeetSummaryDetail
	#'
	#' This function modifies a MeetSummaryDetail
	#' @param fieldNames The field values to give the modified MeetSummaryDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MeetSummaryDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMeetSummaryDetail <- function(MeetSummaryDetailID, DisplayPeriodID = NULL, IsPrimary = NULL, MeetSummaryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MeetSummaryDetail", objectId = MeetSummaryDetailID, body = list(DataObject = body), searchFields = append("MeetSummaryDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempSubstituteAssignments
	#'
	#' This function returns a dataframe or json object of TempSubstituteAssignments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSubstituteAssignments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSubstituteAssignments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSubstituteAssignment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempSubstituteAssignments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempSubstituteAssignments <- function(searchConditionsList = NULL, Conflicts = F, CourseCodeSectionCode = F, CreatedTime = F, Date = F, DisplayPeriodID = F, DisplayPeriodSortNumber = F, EntityID = F, HasAttendanceAccess = F, HasConflicts = F, HasGradebookAccess = F, IsLongTermSubstitute = F, MeetID = F, ModifiedTime = F, Period = F, SchoolYearID = F, SectionAlreadyCovered = F, SectionID = F, SourceStaffID = F, StaffMeetID = F, StaffName = F, SubstituteStaffID = F, TempSubstituteAssignmentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempSubstituteAssignment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempSubstituteAssignment
	#'
	#' This function returns a dataframe or json object of a TempSubstituteAssignment
	#' @param TempSubstituteAssignmentID The ID of the TempSubstituteAssignment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempSubstituteAssignment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempSubstituteAssignment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempSubstituteAssignment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempSubstituteAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempSubstituteAssignment <- function(TempSubstituteAssignmentID, Conflicts = F, CourseCodeSectionCode = F, CreatedTime = F, Date = F, DisplayPeriodID = F, DisplayPeriodSortNumber = F, EntityID = F, HasAttendanceAccess = F, HasConflicts = F, HasGradebookAccess = F, IsLongTermSubstitute = F, MeetID = F, ModifiedTime = F, Period = F, SchoolYearID = F, SectionAlreadyCovered = F, SectionID = F, SourceStaffID = F, StaffMeetID = F, StaffName = F, SubstituteStaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempSubstituteAssignmentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempSubstituteAssignment", objectId = TempSubstituteAssignmentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempSubstituteAssignment
	#'
	#' This function deletes a TempSubstituteAssignment
	#' @param TempSubstituteAssignmentID The ID of the TempSubstituteAssignment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempSubstituteAssignmentID of the deleted TempSubstituteAssignment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempSubstituteAssignment <- function(TempSubstituteAssignmentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempSubstituteAssignment", objectId = TempSubstituteAssignmentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempSubstituteAssignment
	#'
	#' This function creates a TempSubstituteAssignment
	#' @param fieldNames The field values to give the created TempSubstituteAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempSubstituteAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempSubstituteAssignment <- function(Conflicts = NULL, CourseCodeSectionCode = NULL, Date = NULL, DisplayPeriodID = NULL, DisplayPeriodSortNumber = NULL, EntityID = NULL, HasAttendanceAccess = NULL, HasGradebookAccess = NULL, IsLongTermSubstitute = NULL, MeetID = NULL, Period = NULL, SchoolYearID = NULL, SectionAlreadyCovered = NULL, SectionID = NULL, SourceStaffID = NULL, StaffMeetID = NULL, StaffName = NULL, SubstituteStaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempSubstituteAssignment", body = list(DataObject = body), searchFields = append("TempSubstituteAssignmentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempSubstituteAssignment
	#'
	#' This function modifies a TempSubstituteAssignment
	#' @param fieldNames The field values to give the modified TempSubstituteAssignment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempSubstituteAssignment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempSubstituteAssignment <- function(TempSubstituteAssignmentID, Conflicts = NULL, CourseCodeSectionCode = NULL, Date = NULL, DisplayPeriodID = NULL, DisplayPeriodSortNumber = NULL, EntityID = NULL, HasAttendanceAccess = NULL, HasGradebookAccess = NULL, IsLongTermSubstitute = NULL, MeetID = NULL, Period = NULL, SchoolYearID = NULL, SectionAlreadyCovered = NULL, SectionID = NULL, SourceStaffID = NULL, StaffMeetID = NULL, StaffName = NULL, SubstituteStaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempSubstituteAssignment", objectId = TempSubstituteAssignmentID, body = list(DataObject = body), searchFields = append("TempSubstituteAssignmentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisplayPeriodRotationDisplayPeriods
	#'
	#' This function returns a dataframe or json object of DisplayPeriodRotationDisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriodRotationDisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriodRotationDisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriodRotationDisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of DisplayPeriodRotationDisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisplayPeriodRotationDisplayPeriods <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodID = F, DisplayPeriodRotationDisplayPeriodID = F, DisplayPeriodRotationDisplayPeriodIDClonedFrom = F, DisplayPeriodRotationID = F, HideMeetDisplayPeriod = F, IsPrimary = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "DisplayPeriodRotationDisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisplayPeriodRotationDisplayPeriod
	#'
	#' This function returns a dataframe or json object of a DisplayPeriodRotationDisplayPeriod
	#' @param DisplayPeriodRotationDisplayPeriodID The ID of the DisplayPeriodRotationDisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriodRotationDisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriodRotationDisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriodRotationDisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of DisplayPeriodRotationDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisplayPeriodRotationDisplayPeriod <- function(DisplayPeriodRotationDisplayPeriodID, CreatedTime = F, DisplayPeriodID = F, DisplayPeriodRotationDisplayPeriodIDClonedFrom = F, DisplayPeriodRotationID = F, HideMeetDisplayPeriod = F, IsPrimary = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisplayPeriodRotationDisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "DisplayPeriodRotationDisplayPeriod", objectId = DisplayPeriodRotationDisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisplayPeriodRotationDisplayPeriod
	#'
	#' This function deletes a DisplayPeriodRotationDisplayPeriod
	#' @param DisplayPeriodRotationDisplayPeriodID The ID of the DisplayPeriodRotationDisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The DisplayPeriodRotationDisplayPeriodID of the deleted DisplayPeriodRotationDisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisplayPeriodRotationDisplayPeriod <- function(DisplayPeriodRotationDisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "DisplayPeriodRotationDisplayPeriod", objectId = DisplayPeriodRotationDisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisplayPeriodRotationDisplayPeriod
	#'
	#' This function creates a DisplayPeriodRotationDisplayPeriod
	#' @param fieldNames The field values to give the created DisplayPeriodRotationDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created DisplayPeriodRotationDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisplayPeriodRotationDisplayPeriod <- function(DisplayPeriodID = NULL, DisplayPeriodRotationDisplayPeriodIDClonedFrom = NULL, DisplayPeriodRotationID = NULL, HideMeetDisplayPeriod = NULL, IsPrimary = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "DisplayPeriodRotationDisplayPeriod", body = list(DataObject = body), searchFields = append("DisplayPeriodRotationDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisplayPeriodRotationDisplayPeriod
	#'
	#' This function modifies a DisplayPeriodRotationDisplayPeriod
	#' @param fieldNames The field values to give the modified DisplayPeriodRotationDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified DisplayPeriodRotationDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisplayPeriodRotationDisplayPeriod <- function(DisplayPeriodRotationDisplayPeriodID, DisplayPeriodID = NULL, DisplayPeriodRotationDisplayPeriodIDClonedFrom = NULL, DisplayPeriodRotationID = NULL, HideMeetDisplayPeriod = NULL, IsPrimary = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "DisplayPeriodRotationDisplayPeriod", objectId = DisplayPeriodRotationDisplayPeriodID, body = list(DataObject = body), searchFields = append("DisplayPeriodRotationDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List DisplayPeriodRotations
	#'
	#' This function returns a dataframe or json object of DisplayPeriodRotations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriodRotations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriodRotations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriodRotation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of DisplayPeriodRotations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listDisplayPeriodRotations <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DisplayPeriodRotationID = F, DisplayPeriodRotationIDClonedFrom = F, DisplayPeriodRotationIDClonedTo = F, DisplayPeriodSummary = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "DisplayPeriodRotation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a DisplayPeriodRotation
	#'
	#' This function returns a dataframe or json object of a DisplayPeriodRotation
	#' @param DisplayPeriodRotationID The ID of the DisplayPeriodRotation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given DisplayPeriodRotation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the DisplayPeriodRotation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('DisplayPeriodRotation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of DisplayPeriodRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getDisplayPeriodRotation <- function(DisplayPeriodRotationID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DisplayPeriodRotationIDClonedFrom = F, DisplayPeriodRotationIDClonedTo = F, DisplayPeriodSummary = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "DisplayPeriodRotationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "DisplayPeriodRotation", objectId = DisplayPeriodRotationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a DisplayPeriodRotation
	#'
	#' This function deletes a DisplayPeriodRotation
	#' @param DisplayPeriodRotationID The ID of the DisplayPeriodRotation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The DisplayPeriodRotationID of the deleted DisplayPeriodRotation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteDisplayPeriodRotation <- function(DisplayPeriodRotationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "DisplayPeriodRotation", objectId = DisplayPeriodRotationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a DisplayPeriodRotation
	#'
	#' This function creates a DisplayPeriodRotation
	#' @param fieldNames The field values to give the created DisplayPeriodRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created DisplayPeriodRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createDisplayPeriodRotation <- function(Code = NULL, Description = NULL, DisplayPeriodRotationIDClonedFrom = NULL, DisplayPeriodSummary = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "DisplayPeriodRotation", body = list(DataObject = body), searchFields = append("DisplayPeriodRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a DisplayPeriodRotation
	#'
	#' This function modifies a DisplayPeriodRotation
	#' @param fieldNames The field values to give the modified DisplayPeriodRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified DisplayPeriodRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyDisplayPeriodRotation <- function(DisplayPeriodRotationID, Code = NULL, Description = NULL, DisplayPeriodRotationIDClonedFrom = NULL, DisplayPeriodSummary = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "DisplayPeriodRotation", objectId = DisplayPeriodRotationID, body = list(DataObject = body), searchFields = append("DisplayPeriodRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityFilterCourseStudentStudents
	#'
	#' This function returns a dataframe or json object of AvailabilityFilterCourseStudentStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseStudentStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseStudentStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseStudentStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityFilterCourseStudentStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityFilterCourseStudentStudents <- function(searchConditionsList = NULL, ArenaSchedulingStatus = F, AvailabilityFilterCourseStudentID = F, AvailabilityFilterCourseStudentStudentID = F, CreatedTime = F, ModifiedTime = F, StudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityFilterCourseStudentStudent
	#'
	#' This function returns a dataframe or json object of an AvailabilityFilterCourseStudentStudent
	#' @param AvailabilityFilterCourseStudentStudentID The ID of the AvailabilityFilterCourseStudentStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseStudentStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseStudentStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseStudentStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityFilterCourseStudentStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityFilterCourseStudentStudent <- function(AvailabilityFilterCourseStudentStudentID, ArenaSchedulingStatus = F, AvailabilityFilterCourseStudentID = F, CreatedTime = F, ModifiedTime = F, StudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityFilterCourseStudentStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentStudent", objectId = AvailabilityFilterCourseStudentStudentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityFilterCourseStudentStudent
	#'
	#' This function deletes an AvailabilityFilterCourseStudentStudent
	#' @param AvailabilityFilterCourseStudentStudentID The ID of the AvailabilityFilterCourseStudentStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityFilterCourseStudentStudentID of the deleted AvailabilityFilterCourseStudentStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityFilterCourseStudentStudent <- function(AvailabilityFilterCourseStudentStudentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentStudent", objectId = AvailabilityFilterCourseStudentStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityFilterCourseStudentStudent
	#'
	#' This function creates an AvailabilityFilterCourseStudentStudent
	#' @param fieldNames The field values to give the created AvailabilityFilterCourseStudentStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityFilterCourseStudentStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityFilterCourseStudentStudent <- function(ArenaSchedulingStatus = NULL, AvailabilityFilterCourseStudentID = NULL, StudentID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentStudent", body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseStudentStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityFilterCourseStudentStudent
	#'
	#' This function modifies an AvailabilityFilterCourseStudentStudent
	#' @param fieldNames The field values to give the modified AvailabilityFilterCourseStudentStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityFilterCourseStudentStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityFilterCourseStudentStudent <- function(AvailabilityFilterCourseStudentStudentID, ArenaSchedulingStatus = NULL, AvailabilityFilterCourseStudentID = NULL, StudentID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentStudent", objectId = AvailabilityFilterCourseStudentStudentID, body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseStudentStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityFilterCourseStudentCourses
	#'
	#' This function returns a dataframe or json object of AvailabilityFilterCourseStudentCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseStudentCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseStudentCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseStudentCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityFilterCourseStudentCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityFilterCourseStudentCourses <- function(searchConditionsList = NULL, AvailabilityFilterCourseStudentCourseID = F, AvailabilityFilterCourseStudentID = F, CourseID = F, CreatedTime = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityFilterCourseStudentCourse
	#'
	#' This function returns a dataframe or json object of an AvailabilityFilterCourseStudentCourse
	#' @param AvailabilityFilterCourseStudentCourseID The ID of the AvailabilityFilterCourseStudentCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseStudentCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseStudentCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseStudentCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityFilterCourseStudentCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityFilterCourseStudentCourse <- function(AvailabilityFilterCourseStudentCourseID, AvailabilityFilterCourseStudentID = F, CourseID = F, CreatedTime = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityFilterCourseStudentCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentCourse", objectId = AvailabilityFilterCourseStudentCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityFilterCourseStudentCourse
	#'
	#' This function deletes an AvailabilityFilterCourseStudentCourse
	#' @param AvailabilityFilterCourseStudentCourseID The ID of the AvailabilityFilterCourseStudentCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityFilterCourseStudentCourseID of the deleted AvailabilityFilterCourseStudentCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityFilterCourseStudentCourse <- function(AvailabilityFilterCourseStudentCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentCourse", objectId = AvailabilityFilterCourseStudentCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityFilterCourseStudentCourse
	#'
	#' This function creates an AvailabilityFilterCourseStudentCourse
	#' @param fieldNames The field values to give the created AvailabilityFilterCourseStudentCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityFilterCourseStudentCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityFilterCourseStudentCourse <- function(AvailabilityFilterCourseStudentID = NULL, CourseID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentCourse", body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseStudentCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityFilterCourseStudentCourse
	#'
	#' This function modifies an AvailabilityFilterCourseStudentCourse
	#' @param fieldNames The field values to give the modified AvailabilityFilterCourseStudentCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityFilterCourseStudentCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityFilterCourseStudentCourse <- function(AvailabilityFilterCourseStudentCourseID, AvailabilityFilterCourseStudentID = NULL, CourseID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseStudentCourse", objectId = AvailabilityFilterCourseStudentCourseID, body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseStudentCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempDayRotations
	#'
	#' This function returns a dataframe or json object of TempDayRotations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempDayRotations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempDayRotations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempDayRotation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempDayRotations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempDayRotations <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DayRotationID = F, ModifiedTime = F, Note = F, ObjectIsDirty = F, ObjectName = F, RecordsUpdated = F, TempDayRotationID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempDayRotation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempDayRotation
	#'
	#' This function returns a dataframe or json object of a TempDayRotation
	#' @param TempDayRotationID The ID of the TempDayRotation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempDayRotation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempDayRotation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempDayRotation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempDayRotation <- function(TempDayRotationID, Code = F, CreatedTime = F, DayRotationID = F, ModifiedTime = F, Note = F, ObjectIsDirty = F, ObjectName = F, RecordsUpdated = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempDayRotationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempDayRotation", objectId = TempDayRotationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempDayRotation
	#'
	#' This function deletes a TempDayRotation
	#' @param TempDayRotationID The ID of the TempDayRotation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempDayRotationID of the deleted TempDayRotation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempDayRotation <- function(TempDayRotationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempDayRotation", objectId = TempDayRotationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempDayRotation
	#'
	#' This function creates a TempDayRotation
	#' @param fieldNames The field values to give the created TempDayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempDayRotation <- function(Code = NULL, DayRotationID = NULL, Note = NULL, ObjectIsDirty = NULL, ObjectName = NULL, RecordsUpdated = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempDayRotation", body = list(DataObject = body), searchFields = append("TempDayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempDayRotation
	#'
	#' This function modifies a TempDayRotation
	#' @param fieldNames The field values to give the modified TempDayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempDayRotation <- function(TempDayRotationID, Code = NULL, DayRotationID = NULL, Note = NULL, ObjectIsDirty = NULL, ObjectName = NULL, RecordsUpdated = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempDayRotation", objectId = TempDayRotationID, body = list(DataObject = body), searchFields = append("TempDayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedDayRotations
	#'
	#' This function returns a dataframe or json object of TempFailedDayRotations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedDayRotations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedDayRotations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedDayRotation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedDayRotations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedDayRotations <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DayRotationID = F, ModifiedTime = F, Note = F, ObjectIsDirty = F, ObjectName = F, RecordsUpdated = F, TempDayRotationID = F, TempFailedDayRotationID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedDayRotation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedDayRotation
	#'
	#' This function returns a dataframe or json object of a TempFailedDayRotation
	#' @param TempFailedDayRotationID The ID of the TempFailedDayRotation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedDayRotation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedDayRotation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedDayRotation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedDayRotation <- function(TempFailedDayRotationID, Code = F, CreatedTime = F, DayRotationID = F, ModifiedTime = F, Note = F, ObjectIsDirty = F, ObjectName = F, RecordsUpdated = F, TempDayRotationID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedDayRotationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedDayRotation", objectId = TempFailedDayRotationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedDayRotation
	#'
	#' This function deletes a TempFailedDayRotation
	#' @param TempFailedDayRotationID The ID of the TempFailedDayRotation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedDayRotationID of the deleted TempFailedDayRotation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedDayRotation <- function(TempFailedDayRotationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedDayRotation", objectId = TempFailedDayRotationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedDayRotation
	#'
	#' This function creates a TempFailedDayRotation
	#' @param fieldNames The field values to give the created TempFailedDayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedDayRotation <- function(Code = NULL, DayRotationID = NULL, Note = NULL, ObjectIsDirty = NULL, ObjectName = NULL, RecordsUpdated = NULL, TempDayRotationID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedDayRotation", body = list(DataObject = body), searchFields = append("TempFailedDayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedDayRotation
	#'
	#' This function modifies a TempFailedDayRotation
	#' @param fieldNames The field values to give the modified TempFailedDayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedDayRotation <- function(TempFailedDayRotationID, Code = NULL, DayRotationID = NULL, Note = NULL, ObjectIsDirty = NULL, ObjectName = NULL, RecordsUpdated = NULL, TempDayRotationID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedDayRotation", objectId = TempFailedDayRotationID, body = list(DataObject = body), searchFields = append("TempFailedDayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingConfigEntityYears
	#'
	#' This function returns a dataframe or json object of SchedulingConfigEntityYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingConfigEntityYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingConfigEntityYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingConfigEntityYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingConfigEntityYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingConfigEntityYears <- function(searchConditionsList = NULL, ArenaSchedulingConfirmationScreenMessage = F, ConfigEntityYearID = F, ConfigEntityYearIDClonedFrom = F, CreatedTime = F, EnableArenaSchedulingConfirmationScreen = F, EnableValidationOfRoomCapacityDuringStudentSectionEnrollment = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "ConfigEntityYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingConfigEntityYear
	#'
	#' This function returns a dataframe or json object of a SchedulingConfigEntityYear
	#' @param SchedulingConfigEntityYearID The ID of the SchedulingConfigEntityYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingConfigEntityYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingConfigEntityYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingConfigEntityYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingConfigEntityYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingConfigEntityYear <- function(SchedulingConfigEntityYearID, ArenaSchedulingConfirmationScreenMessage = F, ConfigEntityYearID = F, ConfigEntityYearIDClonedFrom = F, CreatedTime = F, EnableArenaSchedulingConfirmationScreen = F, EnableValidationOfRoomCapacityDuringStudentSectionEnrollment = F, EntityID = F, ModifiedTime = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingConfigEntityYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "ConfigEntityYear", objectId = SchedulingConfigEntityYearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingConfigEntityYear
	#'
	#' This function deletes a SchedulingConfigEntityYear
	#' @param SchedulingConfigEntityYearID The ID of the SchedulingConfigEntityYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingConfigEntityYearID of the deleted SchedulingConfigEntityYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingConfigEntityYear <- function(SchedulingConfigEntityYearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "ConfigEntityYear", objectId = SchedulingConfigEntityYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingConfigEntityYear
	#'
	#' This function creates a SchedulingConfigEntityYear
	#' @param fieldNames The field values to give the created SchedulingConfigEntityYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingConfigEntityYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingConfigEntityYear <- function(ArenaSchedulingConfirmationScreenMessage = NULL, ConfigEntityYearIDClonedFrom = NULL, EnableArenaSchedulingConfirmationScreen = NULL, EnableValidationOfRoomCapacityDuringStudentSectionEnrollment = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "ConfigEntityYear", body = list(DataObject = body), searchFields = append("ConfigEntityYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingConfigEntityYear
	#'
	#' This function modifies a SchedulingConfigEntityYear
	#' @param fieldNames The field values to give the modified SchedulingConfigEntityYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingConfigEntityYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingConfigEntityYear <- function(ConfigEntityYearID, ArenaSchedulingConfirmationScreenMessage = NULL, ConfigEntityYearIDClonedFrom = NULL, EnableArenaSchedulingConfirmationScreen = NULL, EnableValidationOfRoomCapacityDuringStudentSectionEnrollment = NULL, EntityID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "ConfigEntityYear", objectId = ConfigEntityYearID, body = list(DataObject = body), searchFields = append("ConfigEntityYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingBoardFilters
	#'
	#' This function returns a dataframe or json object of SchedulingBoardFilters
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingBoardFilters. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingBoardFilters.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingBoardFilter') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingBoardFilters
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingBoardFilters <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, DisplayOrder = F, ModifiedTime = F, SchedulingBoardFilterID = F, SkywardHash = F, SkywardID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SchedulingBoardFilter", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingBoardFilter
	#'
	#' This function returns a dataframe or json object of a SchedulingBoardFilter
	#' @param SchedulingBoardFilterID The ID of the SchedulingBoardFilter to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingBoardFilter. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingBoardFilter.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingBoardFilter') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingBoardFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingBoardFilter <- function(SchedulingBoardFilterID, CreatedTime = F, Description = F, DisplayOrder = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingBoardFilterID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SchedulingBoardFilter", objectId = SchedulingBoardFilterID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingBoardFilter
	#'
	#' This function deletes a SchedulingBoardFilter
	#' @param SchedulingBoardFilterID The ID of the SchedulingBoardFilter to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingBoardFilterID of the deleted SchedulingBoardFilter.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingBoardFilter <- function(SchedulingBoardFilterID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SchedulingBoardFilter", objectId = SchedulingBoardFilterID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingBoardFilter
	#'
	#' This function creates a SchedulingBoardFilter
	#' @param fieldNames The field values to give the created SchedulingBoardFilter. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingBoardFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingBoardFilter <- function(Description = NULL, DisplayOrder = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SchedulingBoardFilter", body = list(DataObject = body), searchFields = append("SchedulingBoardFilterID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingBoardFilter
	#'
	#' This function modifies a SchedulingBoardFilter
	#' @param fieldNames The field values to give the modified SchedulingBoardFilter. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingBoardFilter
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingBoardFilter <- function(SchedulingBoardFilterID, Description = NULL, DisplayOrder = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SchedulingBoardFilter", objectId = SchedulingBoardFilterID, body = list(DataObject = body), searchFields = append("SchedulingBoardFilterID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MassPrintStudentScheduleRunHistories
	#'
	#' This function returns a dataframe or json object of MassPrintStudentScheduleRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassPrintStudentScheduleRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassPrintStudentScheduleRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassPrintStudentScheduleRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MassPrintStudentScheduleRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMassPrintStudentScheduleRunHistories <- function(searchConditionsList = NULL, Comment = F, CreatedTime = F, EntityID = F, LockerInfoPrintType = F, MassPrintStudentScheduleRunHistoryID = F, MediaID = F, ModifiedTime = F, ParameterData = F, ParameterDescription = F, PrintAdvisorInfo = F, PrintHomeroomInfo = F, PrintLockerInfo = F, RequestIdentifier = F, RunDescription = F, SchoolYearID = F, SendMessageOnComplete = F, SortType = F, StudentSelectType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowInstanceID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MassPrintStudentScheduleRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MassPrintStudentScheduleRunHistory
	#'
	#' This function returns a dataframe or json object of a MassPrintStudentScheduleRunHistory
	#' @param MassPrintStudentScheduleRunHistoryID The ID of the MassPrintStudentScheduleRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassPrintStudentScheduleRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassPrintStudentScheduleRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassPrintStudentScheduleRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MassPrintStudentScheduleRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMassPrintStudentScheduleRunHistory <- function(MassPrintStudentScheduleRunHistoryID, Comment = F, CreatedTime = F, EntityID = F, LockerInfoPrintType = F, MediaID = F, ModifiedTime = F, ParameterData = F, ParameterDescription = F, PrintAdvisorInfo = F, PrintHomeroomInfo = F, PrintLockerInfo = F, RequestIdentifier = F, RunDescription = F, SchoolYearID = F, SendMessageOnComplete = F, SortType = F, StudentSelectType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowInstanceID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MassPrintStudentScheduleRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MassPrintStudentScheduleRunHistory", objectId = MassPrintStudentScheduleRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MassPrintStudentScheduleRunHistory
	#'
	#' This function deletes a MassPrintStudentScheduleRunHistory
	#' @param MassPrintStudentScheduleRunHistoryID The ID of the MassPrintStudentScheduleRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MassPrintStudentScheduleRunHistoryID of the deleted MassPrintStudentScheduleRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMassPrintStudentScheduleRunHistory <- function(MassPrintStudentScheduleRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MassPrintStudentScheduleRunHistory", objectId = MassPrintStudentScheduleRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MassPrintStudentScheduleRunHistory
	#'
	#' This function creates a MassPrintStudentScheduleRunHistory
	#' @param fieldNames The field values to give the created MassPrintStudentScheduleRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MassPrintStudentScheduleRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMassPrintStudentScheduleRunHistory <- function(Comment = NULL, EntityID = NULL, LockerInfoPrintType = NULL, MediaID = NULL, PrintAdvisorInfo = NULL, PrintHomeroomInfo = NULL, PrintLockerInfo = NULL, RunDescription = NULL, SchoolYearID = NULL, SendMessageOnComplete = NULL, SortType = NULL, StudentSelectType = NULL, WorkflowInstanceID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MassPrintStudentScheduleRunHistory", body = list(DataObject = body), searchFields = append("MassPrintStudentScheduleRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MassPrintStudentScheduleRunHistory
	#'
	#' This function modifies a MassPrintStudentScheduleRunHistory
	#' @param fieldNames The field values to give the modified MassPrintStudentScheduleRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MassPrintStudentScheduleRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMassPrintStudentScheduleRunHistory <- function(MassPrintStudentScheduleRunHistoryID, Comment = NULL, EntityID = NULL, LockerInfoPrintType = NULL, MediaID = NULL, PrintAdvisorInfo = NULL, PrintHomeroomInfo = NULL, PrintLockerInfo = NULL, RunDescription = NULL, SchoolYearID = NULL, SendMessageOnComplete = NULL, SortType = NULL, StudentSelectType = NULL, WorkflowInstanceID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MassPrintStudentScheduleRunHistory", objectId = MassPrintStudentScheduleRunHistoryID, body = list(DataObject = body), searchFields = append("MassPrintStudentScheduleRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SchedulingTempExceptions
	#'
	#' This function returns a dataframe or json object of SchedulingTempExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTempExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTempExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTempException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SchedulingTempExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSchedulingTempExceptions <- function(searchConditionsList = NULL, CreatedTime = F, ErrorDetail = F, ErrorFieldName = F, FailedRecordPrimaryKey = F, ModifiedTime = F, TempExceptionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SchedulingTempException
	#'
	#' This function returns a dataframe or json object of a SchedulingTempException
	#' @param SchedulingTempExceptionID The ID of the SchedulingTempException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SchedulingTempException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SchedulingTempException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SchedulingTempException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SchedulingTempException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSchedulingTempException <- function(SchedulingTempExceptionID, CreatedTime = F, ErrorDetail = F, ErrorFieldName = F, FailedRecordPrimaryKey = F, ModifiedTime = F, TempExceptionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SchedulingTempExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempException", objectId = SchedulingTempExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SchedulingTempException
	#'
	#' This function deletes a SchedulingTempException
	#' @param SchedulingTempExceptionID The ID of the SchedulingTempException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SchedulingTempExceptionID of the deleted SchedulingTempException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSchedulingTempException <- function(SchedulingTempExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempException", objectId = SchedulingTempExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SchedulingTempException
	#'
	#' This function creates a SchedulingTempException
	#' @param fieldNames The field values to give the created SchedulingTempException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SchedulingTempException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSchedulingTempException <- function(ErrorDetail = NULL, ErrorFieldName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempException", body = list(DataObject = body), searchFields = append("TempExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SchedulingTempException
	#'
	#' This function modifies a SchedulingTempException
	#' @param fieldNames The field values to give the modified SchedulingTempException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SchedulingTempException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySchedulingTempException <- function(TempExceptionID, ErrorDetail = NULL, ErrorFieldName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempException", objectId = TempExceptionID, body = list(DataObject = body), searchFields = append("TempExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempMeets
	#'
	#' This function returns a dataframe or json object of TempMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempMeets <- function(searchConditionsList = NULL, CourseCode = F, CourseDescription = F, CreatedTime = F, EndDate = F, MeetID = F, ModifiedTime = F, NewEndDate = F, NewSectionLengthCode = F, NewStartDate = F, PrimaryDays = F, PrimaryDisplayPeriod = F, PrimaryStaffFullNameFML = F, RoomNumberDescription = F, SectionCode = F, SectionID = F, SectionLengthCode = F, StartDate = F, TempMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempMeet
	#'
	#' This function returns a dataframe or json object of a TempMeet
	#' @param TempMeetID The ID of the TempMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempMeet <- function(TempMeetID, CourseCode = F, CourseDescription = F, CreatedTime = F, EndDate = F, MeetID = F, ModifiedTime = F, NewEndDate = F, NewSectionLengthCode = F, NewStartDate = F, PrimaryDays = F, PrimaryDisplayPeriod = F, PrimaryStaffFullNameFML = F, RoomNumberDescription = F, SectionCode = F, SectionID = F, SectionLengthCode = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempMeet", objectId = TempMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempMeet
	#'
	#' This function deletes a TempMeet
	#' @param TempMeetID The ID of the TempMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempMeetID of the deleted TempMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempMeet <- function(TempMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempMeet", objectId = TempMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempMeet
	#'
	#' This function creates a TempMeet
	#' @param fieldNames The field values to give the created TempMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempMeet <- function(CourseCode = NULL, CourseDescription = NULL, EndDate = NULL, MeetID = NULL, NewEndDate = NULL, NewSectionLengthCode = NULL, NewStartDate = NULL, PrimaryDays = NULL, PrimaryDisplayPeriod = NULL, PrimaryStaffFullNameFML = NULL, RoomNumberDescription = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempMeet", body = list(DataObject = body), searchFields = append("TempMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempMeet
	#'
	#' This function modifies a TempMeet
	#' @param fieldNames The field values to give the modified TempMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempMeet <- function(TempMeetID, CourseCode = NULL, CourseDescription = NULL, EndDate = NULL, MeetID = NULL, NewEndDate = NULL, NewSectionLengthCode = NULL, NewStartDate = NULL, PrimaryDays = NULL, PrimaryDisplayPeriod = NULL, PrimaryStaffFullNameFML = NULL, RoomNumberDescription = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthCode = NULL, StartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempMeet", objectId = TempMeetID, body = list(DataObject = body), searchFields = append("TempMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerProposedMeetDisplayPeriods
	#'
	#' This function returns a dataframe or json object of SectionSchedulerProposedMeetDisplayPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedMeetDisplayPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedMeetDisplayPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedMeetDisplayPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerProposedMeetDisplayPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerProposedMeetDisplayPeriods <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodID = F, ModifiedTime = F, SectionSchedulerProposedMeetDisplayPeriodID = F, SectionSchedulerProposedMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerProposedMeetDisplayPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerProposedMeetDisplayPeriod
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerProposedMeetDisplayPeriod
	#' @param SectionSchedulerProposedMeetDisplayPeriodID The ID of the SectionSchedulerProposedMeetDisplayPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedMeetDisplayPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedMeetDisplayPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedMeetDisplayPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerProposedMeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerProposedMeetDisplayPeriod <- function(SectionSchedulerProposedMeetDisplayPeriodID, CreatedTime = F, DisplayPeriodID = F, ModifiedTime = F, SectionSchedulerProposedMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerProposedMeetDisplayPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetDisplayPeriod", objectId = SectionSchedulerProposedMeetDisplayPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerProposedMeetDisplayPeriod
	#'
	#' This function deletes a SectionSchedulerProposedMeetDisplayPeriod
	#' @param SectionSchedulerProposedMeetDisplayPeriodID The ID of the SectionSchedulerProposedMeetDisplayPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerProposedMeetDisplayPeriodID of the deleted SectionSchedulerProposedMeetDisplayPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerProposedMeetDisplayPeriod <- function(SectionSchedulerProposedMeetDisplayPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetDisplayPeriod", objectId = SectionSchedulerProposedMeetDisplayPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerProposedMeetDisplayPeriod
	#'
	#' This function creates a SectionSchedulerProposedMeetDisplayPeriod
	#' @param fieldNames The field values to give the created SectionSchedulerProposedMeetDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerProposedMeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerProposedMeetDisplayPeriod <- function(DisplayPeriodID = NULL, SectionSchedulerProposedMeetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetDisplayPeriod", body = list(DataObject = body), searchFields = append("SectionSchedulerProposedMeetDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerProposedMeetDisplayPeriod
	#'
	#' This function modifies a SectionSchedulerProposedMeetDisplayPeriod
	#' @param fieldNames The field values to give the modified SectionSchedulerProposedMeetDisplayPeriod. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerProposedMeetDisplayPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerProposedMeetDisplayPeriod <- function(SectionSchedulerProposedMeetDisplayPeriodID, DisplayPeriodID = NULL, SectionSchedulerProposedMeetID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetDisplayPeriod", objectId = SectionSchedulerProposedMeetDisplayPeriodID, body = list(DataObject = body), searchFields = append("SectionSchedulerProposedMeetDisplayPeriodID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerProposedMeets
	#'
	#' This function returns a dataframe or json object of SectionSchedulerProposedMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerProposedMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerProposedMeets <- function(searchConditionsList = NULL, CreatedTime = F, DisplayPeriodRotationID = F, EndDate = F, MeetDisplayPeriodSummary = F, ModifiedTime = F, NumberOfActualConflicts = F, NumberOfEstimatedConflicts = F, NumberOfProposedMeetCourseConflicts = F, NumberOfProposedMeetRoomAndStaffConflicts = F, NumberOfProposedMeetRoomConflicts = F, NumberOfProposedMeetStaffConflicts = F, PrimaryStaffMeetFullNameLFM = F, Rank = F, RankValue = F, RoomID = F, SectionLengthID = F, SectionSchedulerProposedMeetID = F, SectionSchedulerRunAnalysisID = F, StartDate = F, SumTotalOfMaximumStudentCountForScheduledSections = F, SumTotalOfOptimalStudentCountForScheduledSections = F, TotalActualConflictsPointsEarned = F, TotalDisplayPeriodPointsEarned = F, TotalEstimatedConflictsPointsEarned = F, TotalRoomPointsEarned = F, TotalStaffPointsEarned = F, TotalSumOfMaximumStudentCountForScheduledSectionsPointsEarned = F, TotalSumOfOptimalStudentCountForScheduledSectionsPointsEarned = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerProposedMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerProposedMeet
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerProposedMeet
	#' @param SectionSchedulerProposedMeetID The ID of the SectionSchedulerProposedMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerProposedMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerProposedMeet <- function(SectionSchedulerProposedMeetID, CreatedTime = F, DisplayPeriodRotationID = F, EndDate = F, MeetDisplayPeriodSummary = F, ModifiedTime = F, NumberOfActualConflicts = F, NumberOfEstimatedConflicts = F, NumberOfProposedMeetCourseConflicts = F, NumberOfProposedMeetRoomAndStaffConflicts = F, NumberOfProposedMeetRoomConflicts = F, NumberOfProposedMeetStaffConflicts = F, PrimaryStaffMeetFullNameLFM = F, Rank = F, RankValue = F, RoomID = F, SectionLengthID = F, SectionSchedulerRunAnalysisID = F, StartDate = F, SumTotalOfMaximumStudentCountForScheduledSections = F, SumTotalOfOptimalStudentCountForScheduledSections = F, TotalActualConflictsPointsEarned = F, TotalDisplayPeriodPointsEarned = F, TotalEstimatedConflictsPointsEarned = F, TotalRoomPointsEarned = F, TotalStaffPointsEarned = F, TotalSumOfMaximumStudentCountForScheduledSectionsPointsEarned = F, TotalSumOfOptimalStudentCountForScheduledSectionsPointsEarned = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerProposedMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeet", objectId = SectionSchedulerProposedMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerProposedMeet
	#'
	#' This function deletes a SectionSchedulerProposedMeet
	#' @param SectionSchedulerProposedMeetID The ID of the SectionSchedulerProposedMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerProposedMeetID of the deleted SectionSchedulerProposedMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerProposedMeet <- function(SectionSchedulerProposedMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeet", objectId = SectionSchedulerProposedMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerProposedMeet
	#'
	#' This function creates a SectionSchedulerProposedMeet
	#' @param fieldNames The field values to give the created SectionSchedulerProposedMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerProposedMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerProposedMeet <- function(DisplayPeriodRotationID = NULL, EndDate = NULL, MeetDisplayPeriodSummary = NULL, NumberOfActualConflicts = NULL, NumberOfEstimatedConflicts = NULL, PrimaryStaffMeetFullNameLFM = NULL, Rank = NULL, RankValue = NULL, RoomID = NULL, SectionLengthID = NULL, SectionSchedulerRunAnalysisID = NULL, StartDate = NULL, SumTotalOfMaximumStudentCountForScheduledSections = NULL, SumTotalOfOptimalStudentCountForScheduledSections = NULL, TotalActualConflictsPointsEarned = NULL, TotalDisplayPeriodPointsEarned = NULL, TotalEstimatedConflictsPointsEarned = NULL, TotalRoomPointsEarned = NULL, TotalStaffPointsEarned = NULL, TotalSumOfMaximumStudentCountForScheduledSectionsPointsEarned = NULL, TotalSumOfOptimalStudentCountForScheduledSectionsPointsEarned = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeet", body = list(DataObject = body), searchFields = append("SectionSchedulerProposedMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerProposedMeet
	#'
	#' This function modifies a SectionSchedulerProposedMeet
	#' @param fieldNames The field values to give the modified SectionSchedulerProposedMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerProposedMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerProposedMeet <- function(SectionSchedulerProposedMeetID, DisplayPeriodRotationID = NULL, EndDate = NULL, MeetDisplayPeriodSummary = NULL, NumberOfActualConflicts = NULL, NumberOfEstimatedConflicts = NULL, PrimaryStaffMeetFullNameLFM = NULL, Rank = NULL, RankValue = NULL, RoomID = NULL, SectionLengthID = NULL, SectionSchedulerRunAnalysisID = NULL, StartDate = NULL, SumTotalOfMaximumStudentCountForScheduledSections = NULL, SumTotalOfOptimalStudentCountForScheduledSections = NULL, TotalActualConflictsPointsEarned = NULL, TotalDisplayPeriodPointsEarned = NULL, TotalEstimatedConflictsPointsEarned = NULL, TotalRoomPointsEarned = NULL, TotalStaffPointsEarned = NULL, TotalSumOfMaximumStudentCountForScheduledSectionsPointsEarned = NULL, TotalSumOfOptimalStudentCountForScheduledSectionsPointsEarned = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeet", objectId = SectionSchedulerProposedMeetID, body = list(DataObject = body), searchFields = append("SectionSchedulerProposedMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerRunAnalyses
	#'
	#' This function returns a dataframe or json object of SectionSchedulerRunAnalyses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerRunAnalyses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerRunAnalyses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerRunAnalysis') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerRunAnalyses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerRunAnalyses <- function(searchConditionsList = NULL, AcceptedMeetReverted = F, AnalysisDuration = F, AnalysisMethod = F, AnalyzeDayRotations = F, AnalyzeMeetDisplayPeriods = F, AnalyzeRoom = F, AnalyzeSectionLength = F, AnalyzeStaffMeets = F, CountOfSectionSchedulerProposedMeetRecords = F, CreatedTime = F, EndTimeAnalysis = F, EntityID = F, ExcludeMeetsPreviouslyScheduled = F, MeetID = F, ModifiedTime = F, PageStateID = F, RunReason = F, SchoolYearID = F, SectionSchedulerProposedMeetIDAccepted = F, SectionSchedulerRunAnalysisID = F, StartTimeAnalysis = F, TotalActualConflictsPointsPossible = F, TotalDisplayPeriodPointsPossible = F, TotalEstimatedConflictsPointsPossible = F, TotalRoomPointsPossible = F, TotalStaffPointsPossible = F, TotalSumOfMaximumStudentCountForScheduledSectionsPointsPossible = F, TotalSumOfOptimalStudentCountForScheduledSectionsPointsPossible = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDPerformer = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerRunAnalysis", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerRunAnalysis
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerRunAnalysis
	#' @param SectionSchedulerRunAnalysisID The ID of the SectionSchedulerRunAnalysis to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerRunAnalysis. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerRunAnalysis.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerRunAnalysis') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerRunAnalysis <- function(SectionSchedulerRunAnalysisID, AcceptedMeetReverted = F, AnalysisDuration = F, AnalysisMethod = F, AnalyzeDayRotations = F, AnalyzeMeetDisplayPeriods = F, AnalyzeRoom = F, AnalyzeSectionLength = F, AnalyzeStaffMeets = F, CountOfSectionSchedulerProposedMeetRecords = F, CreatedTime = F, EndTimeAnalysis = F, EntityID = F, ExcludeMeetsPreviouslyScheduled = F, MeetID = F, ModifiedTime = F, PageStateID = F, RunReason = F, SchoolYearID = F, SectionSchedulerProposedMeetIDAccepted = F, StartTimeAnalysis = F, TotalActualConflictsPointsPossible = F, TotalDisplayPeriodPointsPossible = F, TotalEstimatedConflictsPointsPossible = F, TotalRoomPointsPossible = F, TotalStaffPointsPossible = F, TotalSumOfMaximumStudentCountForScheduledSectionsPointsPossible = F, TotalSumOfOptimalStudentCountForScheduledSectionsPointsPossible = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDPerformer = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerRunAnalysisID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerRunAnalysis", objectId = SectionSchedulerRunAnalysisID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerRunAnalysis
	#'
	#' This function deletes a SectionSchedulerRunAnalysis
	#' @param SectionSchedulerRunAnalysisID The ID of the SectionSchedulerRunAnalysis to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerRunAnalysisID of the deleted SectionSchedulerRunAnalysis.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerRunAnalysis <- function(SectionSchedulerRunAnalysisID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerRunAnalysis", objectId = SectionSchedulerRunAnalysisID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerRunAnalysis
	#'
	#' This function creates a SectionSchedulerRunAnalysis
	#' @param fieldNames The field values to give the created SectionSchedulerRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerRunAnalysis <- function(AcceptedMeetReverted = NULL, AnalysisMethod = NULL, AnalyzeDayRotations = NULL, AnalyzeMeetDisplayPeriods = NULL, AnalyzeRoom = NULL, AnalyzeSectionLength = NULL, AnalyzeStaffMeets = NULL, EndTimeAnalysis = NULL, EntityID = NULL, ExcludeMeetsPreviouslyScheduled = NULL, MeetID = NULL, PageStateID = NULL, RunReason = NULL, SchoolYearID = NULL, SectionSchedulerProposedMeetIDAccepted = NULL, StartTimeAnalysis = NULL, TotalActualConflictsPointsPossible = NULL, TotalDisplayPeriodPointsPossible = NULL, TotalEstimatedConflictsPointsPossible = NULL, TotalRoomPointsPossible = NULL, TotalStaffPointsPossible = NULL, TotalSumOfMaximumStudentCountForScheduledSectionsPointsPossible = NULL, TotalSumOfOptimalStudentCountForScheduledSectionsPointsPossible = NULL, UserIDPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerRunAnalysis", body = list(DataObject = body), searchFields = append("SectionSchedulerRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerRunAnalysis
	#'
	#' This function modifies a SectionSchedulerRunAnalysis
	#' @param fieldNames The field values to give the modified SectionSchedulerRunAnalysis. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerRunAnalysis
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerRunAnalysis <- function(SectionSchedulerRunAnalysisID, AcceptedMeetReverted = NULL, AnalysisMethod = NULL, AnalyzeDayRotations = NULL, AnalyzeMeetDisplayPeriods = NULL, AnalyzeRoom = NULL, AnalyzeSectionLength = NULL, AnalyzeStaffMeets = NULL, EndTimeAnalysis = NULL, EntityID = NULL, ExcludeMeetsPreviouslyScheduled = NULL, MeetID = NULL, PageStateID = NULL, RunReason = NULL, SchoolYearID = NULL, SectionSchedulerProposedMeetIDAccepted = NULL, StartTimeAnalysis = NULL, TotalActualConflictsPointsPossible = NULL, TotalDisplayPeriodPointsPossible = NULL, TotalEstimatedConflictsPointsPossible = NULL, TotalRoomPointsPossible = NULL, TotalStaffPointsPossible = NULL, TotalSumOfMaximumStudentCountForScheduledSectionsPointsPossible = NULL, TotalSumOfOptimalStudentCountForScheduledSectionsPointsPossible = NULL, UserIDPerformer = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerRunAnalysis", objectId = SectionSchedulerRunAnalysisID, body = list(DataObject = body), searchFields = append("SectionSchedulerRunAnalysisID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerRoomTypeForCourses
	#'
	#' This function returns a dataframe or json object of SectionSchedulerRoomTypeForCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerRoomTypeForCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerRoomTypeForCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerRoomTypeForCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerRoomTypeForCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerRoomTypeForCourses <- function(searchConditionsList = NULL, CourseID = F, CreatedTime = F, ModifiedTime = F, RoomTypeID = F, SectionSchedulerRoomTypeForCourseID = F, SectionSchedulerRoomTypeForCourseIDClonedFrom = F, SectionSchedulerRoomTypeForCourseIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerRoomTypeForCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerRoomTypeForCourse
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerRoomTypeForCourse
	#' @param SectionSchedulerRoomTypeForCourseID The ID of the SectionSchedulerRoomTypeForCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerRoomTypeForCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerRoomTypeForCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerRoomTypeForCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerRoomTypeForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerRoomTypeForCourse <- function(SectionSchedulerRoomTypeForCourseID, CourseID = F, CreatedTime = F, ModifiedTime = F, RoomTypeID = F, SectionSchedulerRoomTypeForCourseIDClonedFrom = F, SectionSchedulerRoomTypeForCourseIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerRoomTypeForCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerRoomTypeForCourse", objectId = SectionSchedulerRoomTypeForCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerRoomTypeForCourse
	#'
	#' This function deletes a SectionSchedulerRoomTypeForCourse
	#' @param SectionSchedulerRoomTypeForCourseID The ID of the SectionSchedulerRoomTypeForCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerRoomTypeForCourseID of the deleted SectionSchedulerRoomTypeForCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerRoomTypeForCourse <- function(SectionSchedulerRoomTypeForCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerRoomTypeForCourse", objectId = SectionSchedulerRoomTypeForCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerRoomTypeForCourse
	#'
	#' This function creates a SectionSchedulerRoomTypeForCourse
	#' @param fieldNames The field values to give the created SectionSchedulerRoomTypeForCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerRoomTypeForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerRoomTypeForCourse <- function(CourseID = NULL, RoomTypeID = NULL, SectionSchedulerRoomTypeForCourseIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerRoomTypeForCourse", body = list(DataObject = body), searchFields = append("SectionSchedulerRoomTypeForCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerRoomTypeForCourse
	#'
	#' This function modifies a SectionSchedulerRoomTypeForCourse
	#' @param fieldNames The field values to give the modified SectionSchedulerRoomTypeForCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerRoomTypeForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerRoomTypeForCourse <- function(SectionSchedulerRoomTypeForCourseID, CourseID = NULL, RoomTypeID = NULL, SectionSchedulerRoomTypeForCourseIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerRoomTypeForCourse", objectId = SectionSchedulerRoomTypeForCourseID, body = list(DataObject = body), searchFields = append("SectionSchedulerRoomTypeForCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerStaffForCourses
	#'
	#' This function returns a dataframe or json object of SectionSchedulerStaffForCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerStaffForCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerStaffForCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerStaffForCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerStaffForCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerStaffForCourses <- function(searchConditionsList = NULL, CourseID = F, CreatedTime = F, ModifiedTime = F, SectionSchedulerStaffForCourseID = F, SectionSchedulerStaffForCourseIDClonedFrom = F, SectionSchedulerStaffForCourseIDClonedTo = F, StaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerStaffForCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerStaffForCourse
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerStaffForCourse
	#' @param SectionSchedulerStaffForCourseID The ID of the SectionSchedulerStaffForCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerStaffForCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerStaffForCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerStaffForCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerStaffForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerStaffForCourse <- function(SectionSchedulerStaffForCourseID, CourseID = F, CreatedTime = F, ModifiedTime = F, SectionSchedulerStaffForCourseIDClonedFrom = F, SectionSchedulerStaffForCourseIDClonedTo = F, StaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerStaffForCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerStaffForCourse", objectId = SectionSchedulerStaffForCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerStaffForCourse
	#'
	#' This function deletes a SectionSchedulerStaffForCourse
	#' @param SectionSchedulerStaffForCourseID The ID of the SectionSchedulerStaffForCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerStaffForCourseID of the deleted SectionSchedulerStaffForCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerStaffForCourse <- function(SectionSchedulerStaffForCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerStaffForCourse", objectId = SectionSchedulerStaffForCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerStaffForCourse
	#'
	#' This function creates a SectionSchedulerStaffForCourse
	#' @param fieldNames The field values to give the created SectionSchedulerStaffForCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerStaffForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerStaffForCourse <- function(CourseID = NULL, SectionSchedulerStaffForCourseIDClonedFrom = NULL, StaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerStaffForCourse", body = list(DataObject = body), searchFields = append("SectionSchedulerStaffForCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerStaffForCourse
	#'
	#' This function modifies a SectionSchedulerStaffForCourse
	#' @param fieldNames The field values to give the modified SectionSchedulerStaffForCourse. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerStaffForCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerStaffForCourse <- function(SectionSchedulerStaffForCourseID, CourseID = NULL, SectionSchedulerStaffForCourseIDClonedFrom = NULL, StaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerStaffForCourse", objectId = SectionSchedulerStaffForCourseID, body = list(DataObject = body), searchFields = append("SectionSchedulerStaffForCourseID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerProposedMeetConflicts
	#'
	#' This function returns a dataframe or json object of SectionSchedulerProposedMeetConflicts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedMeetConflicts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedMeetConflicts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedMeetConflict') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerProposedMeetConflicts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerProposedMeetConflicts <- function(searchConditionsList = NULL, ConflictType = F, CourseID = F, CreatedTime = F, Description = F, ModifiedTime = F, Name = F, NumberOfActualConflicts = F, NumberOfCommonRequests = F, NumberOfEstimatedConflicts = F, SectionSchedulerProposedMeetConflictID = F, SectionSchedulerProposedMeetID = F, Severity = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerProposedMeetConflict", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerProposedMeetConflict
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerProposedMeetConflict
	#' @param SectionSchedulerProposedMeetConflictID The ID of the SectionSchedulerProposedMeetConflict to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedMeetConflict. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedMeetConflict.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedMeetConflict') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerProposedMeetConflict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerProposedMeetConflict <- function(SectionSchedulerProposedMeetConflictID, ConflictType = F, CourseID = F, CreatedTime = F, Description = F, ModifiedTime = F, Name = F, NumberOfActualConflicts = F, NumberOfCommonRequests = F, NumberOfEstimatedConflicts = F, SectionSchedulerProposedMeetID = F, Severity = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerProposedMeetConflictID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetConflict", objectId = SectionSchedulerProposedMeetConflictID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerProposedMeetConflict
	#'
	#' This function deletes a SectionSchedulerProposedMeetConflict
	#' @param SectionSchedulerProposedMeetConflictID The ID of the SectionSchedulerProposedMeetConflict to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerProposedMeetConflictID of the deleted SectionSchedulerProposedMeetConflict.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerProposedMeetConflict <- function(SectionSchedulerProposedMeetConflictID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetConflict", objectId = SectionSchedulerProposedMeetConflictID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerProposedMeetConflict
	#'
	#' This function creates a SectionSchedulerProposedMeetConflict
	#' @param fieldNames The field values to give the created SectionSchedulerProposedMeetConflict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerProposedMeetConflict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerProposedMeetConflict <- function(ConflictType = NULL, CourseID = NULL, Description = NULL, Name = NULL, NumberOfActualConflicts = NULL, NumberOfCommonRequests = NULL, NumberOfEstimatedConflicts = NULL, SectionSchedulerProposedMeetID = NULL, Severity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetConflict", body = list(DataObject = body), searchFields = append("SectionSchedulerProposedMeetConflictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerProposedMeetConflict
	#'
	#' This function modifies a SectionSchedulerProposedMeetConflict
	#' @param fieldNames The field values to give the modified SectionSchedulerProposedMeetConflict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerProposedMeetConflict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerProposedMeetConflict <- function(SectionSchedulerProposedMeetConflictID, ConflictType = NULL, CourseID = NULL, Description = NULL, Name = NULL, NumberOfActualConflicts = NULL, NumberOfCommonRequests = NULL, NumberOfEstimatedConflicts = NULL, SectionSchedulerProposedMeetID = NULL, Severity = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedMeetConflict", objectId = SectionSchedulerProposedMeetConflictID, body = list(DataObject = body), searchFields = append("SectionSchedulerProposedMeetConflictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedStudentCourseRequestToReactivateDetails
	#'
	#' This function returns a dataframe or json object of TempFailedStudentCourseRequestToReactivateDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentCourseRequestToReactivateDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentCourseRequestToReactivateDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentCourseRequestToReactivateDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedStudentCourseRequestToReactivateDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedStudentCourseRequestToReactivateDetails <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, Note = F, TempFailedStudentCourseRequestToReactivateDetailID = F, TempRecordToReactivatePrimaryKeyValue = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivateDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedStudentCourseRequestToReactivateDetail
	#'
	#' This function returns a dataframe or json object of a TempFailedStudentCourseRequestToReactivateDetail
	#' @param TempFailedStudentCourseRequestToReactivateDetailID The ID of the TempFailedStudentCourseRequestToReactivateDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentCourseRequestToReactivateDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentCourseRequestToReactivateDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentCourseRequestToReactivateDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedStudentCourseRequestToReactivateDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedStudentCourseRequestToReactivateDetail <- function(TempFailedStudentCourseRequestToReactivateDetailID, CreatedTime = F, ModifiedTime = F, Note = F, TempRecordToReactivatePrimaryKeyValue = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedStudentCourseRequestToReactivateDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivateDetail", objectId = TempFailedStudentCourseRequestToReactivateDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedStudentCourseRequestToReactivateDetail
	#'
	#' This function deletes a TempFailedStudentCourseRequestToReactivateDetail
	#' @param TempFailedStudentCourseRequestToReactivateDetailID The ID of the TempFailedStudentCourseRequestToReactivateDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedStudentCourseRequestToReactivateDetailID of the deleted TempFailedStudentCourseRequestToReactivateDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedStudentCourseRequestToReactivateDetail <- function(TempFailedStudentCourseRequestToReactivateDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivateDetail", objectId = TempFailedStudentCourseRequestToReactivateDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedStudentCourseRequestToReactivateDetail
	#'
	#' This function creates a TempFailedStudentCourseRequestToReactivateDetail
	#' @param fieldNames The field values to give the created TempFailedStudentCourseRequestToReactivateDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedStudentCourseRequestToReactivateDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedStudentCourseRequestToReactivateDetail <- function(Note = NULL, TempRecordToReactivatePrimaryKeyValue = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivateDetail", body = list(DataObject = body), searchFields = append("TempFailedStudentCourseRequestToReactivateDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedStudentCourseRequestToReactivateDetail
	#'
	#' This function modifies a TempFailedStudentCourseRequestToReactivateDetail
	#' @param fieldNames The field values to give the modified TempFailedStudentCourseRequestToReactivateDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedStudentCourseRequestToReactivateDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedStudentCourseRequestToReactivateDetail <- function(TempFailedStudentCourseRequestToReactivateDetailID, Note = NULL, TempRecordToReactivatePrimaryKeyValue = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivateDetail", objectId = TempFailedStudentCourseRequestToReactivateDetailID, body = list(DataObject = body), searchFields = append("TempFailedStudentCourseRequestToReactivateDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedStudentCourseRequestToReactivates
	#'
	#' This function returns a dataframe or json object of TempFailedStudentCourseRequestToReactivates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentCourseRequestToReactivates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentCourseRequestToReactivates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentCourseRequestToReactivate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedStudentCourseRequestToReactivates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedStudentCourseRequestToReactivates <- function(searchConditionsList = NULL, AlternateRank = F, AuditRecordIsRequestable = F, AuditRecordIsSchedulable = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, CurrentEnrollment = F, DateFrom = F, DateTo = F, Days = F, EarlyExitReasonCodeDescription = F, EarlyExitReasonID = F, EarnedCreditOverride = F, EarnedCredits = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDRequestedFrom = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, GradeReferenceID = F, IsAlternate = F, IsTransferCourse = F, MaximumStudentCount = F, ModifiedTime = F, NameIDRequestedBy = F, NameRequestedByLFM = F, Note = F, Period = F, PreventReactivateCheckboxFromBeingRendered = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthID = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetID = F, StaffFullNameFML = F, StartDate = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, TempFailedStudentCourseRequestToReactivateID = F, TempRecordToReactivatePrimaryKeyValue = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransferCourseName = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedStudentCourseRequestToReactivate
	#'
	#' This function returns a dataframe or json object of a TempFailedStudentCourseRequestToReactivate
	#' @param TempFailedStudentCourseRequestToReactivateID The ID of the TempFailedStudentCourseRequestToReactivate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStudentCourseRequestToReactivate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStudentCourseRequestToReactivate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStudentCourseRequestToReactivate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedStudentCourseRequestToReactivate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedStudentCourseRequestToReactivate <- function(TempFailedStudentCourseRequestToReactivateID, AlternateRank = F, AuditRecordIsRequestable = F, AuditRecordIsSchedulable = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, CurrentEnrollment = F, DateFrom = F, DateTo = F, Days = F, EarlyExitReasonCodeDescription = F, EarlyExitReasonID = F, EarnedCreditOverride = F, EarnedCredits = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDRequestedFrom = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, GradeReferenceID = F, IsAlternate = F, IsTransferCourse = F, MaximumStudentCount = F, ModifiedTime = F, NameIDRequestedBy = F, NameRequestedByLFM = F, Note = F, Period = F, PreventReactivateCheckboxFromBeingRendered = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthID = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetID = F, StaffFullNameFML = F, StartDate = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, TempRecordToReactivatePrimaryKeyValue = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransferCourseName = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedStudentCourseRequestToReactivateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivate", objectId = TempFailedStudentCourseRequestToReactivateID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedStudentCourseRequestToReactivate
	#'
	#' This function deletes a TempFailedStudentCourseRequestToReactivate
	#' @param TempFailedStudentCourseRequestToReactivateID The ID of the TempFailedStudentCourseRequestToReactivate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedStudentCourseRequestToReactivateID of the deleted TempFailedStudentCourseRequestToReactivate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedStudentCourseRequestToReactivate <- function(TempFailedStudentCourseRequestToReactivateID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivate", objectId = TempFailedStudentCourseRequestToReactivateID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedStudentCourseRequestToReactivate
	#'
	#' This function creates a TempFailedStudentCourseRequestToReactivate
	#' @param fieldNames The field values to give the created TempFailedStudentCourseRequestToReactivate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedStudentCourseRequestToReactivate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedStudentCourseRequestToReactivate <- function(AlternateRank = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CurrentEnrollment = NULL, DateFrom = NULL, DateTo = NULL, Days = NULL, EarlyExitReasonCodeDescription = NULL, EarlyExitReasonID = NULL, EarnedCreditOverride = NULL, EarnedCredits = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, EntityIDRequestedFrom = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsAlternate = NULL, IsTransferCourse = NULL, MaximumStudentCount = NULL, NameIDRequestedBy = NULL, NameRequestedByLFM = NULL, Note = NULL, Period = NULL, PreventReactivateCheckboxFromBeingRendered = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthID = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, SectionLengthSubsetID = NULL, StaffFullNameFML = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, StudentSectionTransactionID = NULL, TempRecordToReactivatePrimaryKeyValue = NULL, TotalEarnedCreditOverride = NULL, TotalFailedCreditOverride = NULL, TransferCourseName = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditTotalOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivate", body = list(DataObject = body), searchFields = append("TempFailedStudentCourseRequestToReactivateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedStudentCourseRequestToReactivate
	#'
	#' This function modifies a TempFailedStudentCourseRequestToReactivate
	#' @param fieldNames The field values to give the modified TempFailedStudentCourseRequestToReactivate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedStudentCourseRequestToReactivate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedStudentCourseRequestToReactivate <- function(TempFailedStudentCourseRequestToReactivateID, AlternateRank = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CurrentEnrollment = NULL, DateFrom = NULL, DateTo = NULL, Days = NULL, EarlyExitReasonCodeDescription = NULL, EarlyExitReasonID = NULL, EarnedCreditOverride = NULL, EarnedCredits = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, EntityIDRequestedFrom = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsAlternate = NULL, IsTransferCourse = NULL, MaximumStudentCount = NULL, NameIDRequestedBy = NULL, NameRequestedByLFM = NULL, Note = NULL, Period = NULL, PreventReactivateCheckboxFromBeingRendered = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthID = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, SectionLengthSubsetID = NULL, StaffFullNameFML = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, StudentSectionTransactionID = NULL, TempRecordToReactivatePrimaryKeyValue = NULL, TotalEarnedCreditOverride = NULL, TotalFailedCreditOverride = NULL, TransferCourseName = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditTotalOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedStudentCourseRequestToReactivate", objectId = TempFailedStudentCourseRequestToReactivateID, body = list(DataObject = body), searchFields = append("TempFailedStudentCourseRequestToReactivateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudentCourseRequestToReactivateMNS
	#'
	#' This function returns a dataframe or json object of TempStudentCourseRequestToReactivateMNS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequestToReactivateMNS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequestToReactivateMNS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequestToReactivateMN') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudentCourseRequestToReactivateMNS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudentCourseRequestToReactivateMNS <- function(searchConditionsList = NULL, AlternateRank = F, AuditRecordIsRequestable = F, AuditRecordIsSchedulable = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, CurrentEnrollment = F, DateFrom = F, DateTo = F, Days = F, EarlyExitReasonCodeDescription = F, EarlyExitReasonID = F, EarnedCreditOverride = F, EarnedCredits = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDRequestedFrom = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, GradeReferenceID = F, IsAlternate = F, IsTransferCourse = F, IsTSAProficient = F, MaximumStudentCount = F, ModifiedTime = F, NameIDRequestedBy = F, NameRequestedByLFM = F, Period = F, PreventReactivateCheckboxFromBeingRendered = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthID = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetID = F, StaffFullNameFML = F, StartDate = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, TempStudentCourseRequestToReactivateMNID = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransferCourseName = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateMN", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudentCourseRequestToReactivateMN
	#'
	#' This function returns a dataframe or json object of a TempStudentCourseRequestToReactivateMN
	#' @param TempStudentCourseRequestToReactivateMNID The ID of the TempStudentCourseRequestToReactivateMN to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudentCourseRequestToReactivateMN. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudentCourseRequestToReactivateMN.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudentCourseRequestToReactivateMN') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudentCourseRequestToReactivateMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudentCourseRequestToReactivateMN <- function(TempStudentCourseRequestToReactivateMNID, AlternateRank = F, AuditRecordIsRequestable = F, AuditRecordIsSchedulable = F, CourseCode = F, CourseCodeDescription = F, CourseDescription = F, CourseEntityOfferedToID = F, CourseGradeLevelSummary = F, CourseID = F, CreatedTime = F, CurrentEnrollment = F, DateFrom = F, DateTo = F, Days = F, EarlyExitReasonCodeDescription = F, EarlyExitReasonID = F, EarnedCreditOverride = F, EarnedCredits = F, EndDate = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDRequestedFrom = F, ExcludeFromReportCardsAndTranscripts = F, ExcludeFromStudentSectionLink = F, GradeReferenceID = F, IsAlternate = F, IsTransferCourse = F, IsTSAProficient = F, MaximumStudentCount = F, ModifiedTime = F, NameIDRequestedBy = F, NameRequestedByLFM = F, Period = F, PreventReactivateCheckboxFromBeingRendered = F, RequestSource = F, RequestStatus = F, SchedulingMethod = F, SchoolYearIDCourse = F, SectionCode = F, SectionID = F, SectionLengthID = F, SectionLengthSubsetCode = F, SectionLengthSubsetDescription = F, SectionLengthSubsetID = F, StaffFullNameFML = F, StartDate = F, StudentCourseRequestID = F, StudentCourseRequestIDAlternateFor = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, TotalEarnedCreditOverride = F, TotalFailedCreditOverride = F, TransferCourseName = F, UseEarnedCreditOverride = F, UseEarnedCreditTotalOverride = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentCourseRequestToReactivateMNID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateMN", objectId = TempStudentCourseRequestToReactivateMNID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudentCourseRequestToReactivateMN
	#'
	#' This function deletes a TempStudentCourseRequestToReactivateMN
	#' @param TempStudentCourseRequestToReactivateMNID The ID of the TempStudentCourseRequestToReactivateMN to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentCourseRequestToReactivateMNID of the deleted TempStudentCourseRequestToReactivateMN.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudentCourseRequestToReactivateMN <- function(TempStudentCourseRequestToReactivateMNID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateMN", objectId = TempStudentCourseRequestToReactivateMNID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudentCourseRequestToReactivateMN
	#'
	#' This function creates a TempStudentCourseRequestToReactivateMN
	#' @param fieldNames The field values to give the created TempStudentCourseRequestToReactivateMN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudentCourseRequestToReactivateMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudentCourseRequestToReactivateMN <- function(AlternateRank = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CurrentEnrollment = NULL, DateFrom = NULL, DateTo = NULL, Days = NULL, EarlyExitReasonCodeDescription = NULL, EarlyExitReasonID = NULL, EarnedCreditOverride = NULL, EarnedCredits = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, EntityIDRequestedFrom = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsAlternate = NULL, IsTransferCourse = NULL, IsTSAProficient = NULL, MaximumStudentCount = NULL, NameIDRequestedBy = NULL, NameRequestedByLFM = NULL, Period = NULL, PreventReactivateCheckboxFromBeingRendered = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthID = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, SectionLengthSubsetID = NULL, StaffFullNameFML = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, StudentSectionTransactionID = NULL, TotalEarnedCreditOverride = NULL, TotalFailedCreditOverride = NULL, TransferCourseName = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditTotalOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateMN", body = list(DataObject = body), searchFields = append("TempStudentCourseRequestToReactivateMNID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudentCourseRequestToReactivateMN
	#'
	#' This function modifies a TempStudentCourseRequestToReactivateMN
	#' @param fieldNames The field values to give the modified TempStudentCourseRequestToReactivateMN. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudentCourseRequestToReactivateMN
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudentCourseRequestToReactivateMN <- function(TempStudentCourseRequestToReactivateMNID, AlternateRank = NULL, CourseCode = NULL, CourseCodeDescription = NULL, CourseDescription = NULL, CourseEntityOfferedToID = NULL, CourseGradeLevelSummary = NULL, CourseID = NULL, CurrentEnrollment = NULL, DateFrom = NULL, DateTo = NULL, Days = NULL, EarlyExitReasonCodeDescription = NULL, EarlyExitReasonID = NULL, EarnedCreditOverride = NULL, EarnedCredits = NULL, EndDate = NULL, EntityIDCountsAs = NULL, EntityIDCourse = NULL, EntityIDRequestedFrom = NULL, ExcludeFromReportCardsAndTranscripts = NULL, ExcludeFromStudentSectionLink = NULL, GradeReferenceID = NULL, IsAlternate = NULL, IsTransferCourse = NULL, IsTSAProficient = NULL, MaximumStudentCount = NULL, NameIDRequestedBy = NULL, NameRequestedByLFM = NULL, Period = NULL, PreventReactivateCheckboxFromBeingRendered = NULL, RequestSource = NULL, RequestStatus = NULL, SchedulingMethod = NULL, SchoolYearIDCourse = NULL, SectionCode = NULL, SectionID = NULL, SectionLengthID = NULL, SectionLengthSubsetCode = NULL, SectionLengthSubsetDescription = NULL, SectionLengthSubsetID = NULL, StaffFullNameFML = NULL, StartDate = NULL, StudentCourseRequestID = NULL, StudentCourseRequestIDAlternateFor = NULL, StudentID = NULL, StudentSectionID = NULL, StudentSectionTransactionID = NULL, TotalEarnedCreditOverride = NULL, TotalFailedCreditOverride = NULL, TransferCourseName = NULL, UseEarnedCreditOverride = NULL, UseEarnedCreditTotalOverride = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudentCourseRequestToReactivateMN", objectId = TempStudentCourseRequestToReactivateMNID, body = list(DataObject = body), searchFields = append("TempStudentCourseRequestToReactivateMNID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerPatternExcludedForMeetRequirements
	#'
	#' This function returns a dataframe or json object of SectionSchedulerPatternExcludedForMeetRequirements
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerPatternExcludedForMeetRequirements. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerPatternExcludedForMeetRequirements.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerPatternExcludedForMeetRequirement') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerPatternExcludedForMeetRequirements
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerPatternExcludedForMeetRequirements <- function(searchConditionsList = NULL, CreatedTime = F, MeetRequirementID = F, ModifiedTime = F, SectionSchedulerPatternExcludedForMeetRequirementID = F, SectionSchedulerPatternExcludedForMeetRequirementIDClonedFrom = F, SectionSchedulerPatternID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerPatternExcludedForMeetRequirement", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerPatternExcludedForMeetRequirement
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerPatternExcludedForMeetRequirement
	#' @param SectionSchedulerPatternExcludedForMeetRequirementID The ID of the SectionSchedulerPatternExcludedForMeetRequirement to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerPatternExcludedForMeetRequirement. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerPatternExcludedForMeetRequirement.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerPatternExcludedForMeetRequirement') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerPatternExcludedForMeetRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerPatternExcludedForMeetRequirement <- function(SectionSchedulerPatternExcludedForMeetRequirementID, CreatedTime = F, MeetRequirementID = F, ModifiedTime = F, SectionSchedulerPatternExcludedForMeetRequirementIDClonedFrom = F, SectionSchedulerPatternID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerPatternExcludedForMeetRequirementID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternExcludedForMeetRequirement", objectId = SectionSchedulerPatternExcludedForMeetRequirementID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerPatternExcludedForMeetRequirement
	#'
	#' This function deletes a SectionSchedulerPatternExcludedForMeetRequirement
	#' @param SectionSchedulerPatternExcludedForMeetRequirementID The ID of the SectionSchedulerPatternExcludedForMeetRequirement to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerPatternExcludedForMeetRequirementID of the deleted SectionSchedulerPatternExcludedForMeetRequirement.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerPatternExcludedForMeetRequirement <- function(SectionSchedulerPatternExcludedForMeetRequirementID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternExcludedForMeetRequirement", objectId = SectionSchedulerPatternExcludedForMeetRequirementID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerPatternExcludedForMeetRequirement
	#'
	#' This function creates a SectionSchedulerPatternExcludedForMeetRequirement
	#' @param fieldNames The field values to give the created SectionSchedulerPatternExcludedForMeetRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerPatternExcludedForMeetRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerPatternExcludedForMeetRequirement <- function(MeetRequirementID = NULL, SectionSchedulerPatternExcludedForMeetRequirementIDClonedFrom = NULL, SectionSchedulerPatternID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternExcludedForMeetRequirement", body = list(DataObject = body), searchFields = append("SectionSchedulerPatternExcludedForMeetRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerPatternExcludedForMeetRequirement
	#'
	#' This function modifies a SectionSchedulerPatternExcludedForMeetRequirement
	#' @param fieldNames The field values to give the modified SectionSchedulerPatternExcludedForMeetRequirement. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerPatternExcludedForMeetRequirement
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerPatternExcludedForMeetRequirement <- function(SectionSchedulerPatternExcludedForMeetRequirementID, MeetRequirementID = NULL, SectionSchedulerPatternExcludedForMeetRequirementIDClonedFrom = NULL, SectionSchedulerPatternID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternExcludedForMeetRequirement", objectId = SectionSchedulerPatternExcludedForMeetRequirementID, body = list(DataObject = body), searchFields = append("SectionSchedulerPatternExcludedForMeetRequirementID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerPatternDayRotations
	#'
	#' This function returns a dataframe or json object of SectionSchedulerPatternDayRotations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerPatternDayRotations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerPatternDayRotations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerPatternDayRotation') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerPatternDayRotations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerPatternDayRotations <- function(searchConditionsList = NULL, CreatedTime = F, DayRotationID = F, ModifiedTime = F, SectionSchedulerPatternDayRotationClonedTo = F, SectionSchedulerPatternDayRotationID = F, SectionSchedulerPatternDayRotationIDClonedFrom = F, SectionSchedulerPatternID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerPatternDayRotation", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerPatternDayRotation
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerPatternDayRotation
	#' @param SectionSchedulerPatternDayRotationID The ID of the SectionSchedulerPatternDayRotation to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerPatternDayRotation. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerPatternDayRotation.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerPatternDayRotation') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerPatternDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerPatternDayRotation <- function(SectionSchedulerPatternDayRotationID, CreatedTime = F, DayRotationID = F, ModifiedTime = F, SectionSchedulerPatternDayRotationClonedTo = F, SectionSchedulerPatternDayRotationIDClonedFrom = F, SectionSchedulerPatternID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerPatternDayRotationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternDayRotation", objectId = SectionSchedulerPatternDayRotationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerPatternDayRotation
	#'
	#' This function deletes a SectionSchedulerPatternDayRotation
	#' @param SectionSchedulerPatternDayRotationID The ID of the SectionSchedulerPatternDayRotation to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerPatternDayRotationID of the deleted SectionSchedulerPatternDayRotation.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerPatternDayRotation <- function(SectionSchedulerPatternDayRotationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternDayRotation", objectId = SectionSchedulerPatternDayRotationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerPatternDayRotation
	#'
	#' This function creates a SectionSchedulerPatternDayRotation
	#' @param fieldNames The field values to give the created SectionSchedulerPatternDayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerPatternDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerPatternDayRotation <- function(DayRotationID = NULL, SectionSchedulerPatternDayRotationIDClonedFrom = NULL, SectionSchedulerPatternID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternDayRotation", body = list(DataObject = body), searchFields = append("SectionSchedulerPatternDayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerPatternDayRotation
	#'
	#' This function modifies a SectionSchedulerPatternDayRotation
	#' @param fieldNames The field values to give the modified SectionSchedulerPatternDayRotation. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerPatternDayRotation
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerPatternDayRotation <- function(SectionSchedulerPatternDayRotationID, DayRotationID = NULL, SectionSchedulerPatternDayRotationIDClonedFrom = NULL, SectionSchedulerPatternID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerPatternDayRotation", objectId = SectionSchedulerPatternDayRotationID, body = list(DataObject = body), searchFields = append("SectionSchedulerPatternDayRotationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerPatterns
	#'
	#' This function returns a dataframe or json object of SectionSchedulerPatterns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerPatterns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerPatterns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerPattern') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerPatterns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerPatterns <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, DayRotationCount = F, DayRotationSummary = F, Description = F, EntityID = F, HasDayRotations = F, ModifiedTime = F, SchoolYearID = F, SectionSchedulerPatternID = F, SectionSchedulerPatternIDClonedFrom = F, SectionSchedulerPatternIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerPattern", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerPattern
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerPattern
	#' @param SectionSchedulerPatternID The ID of the SectionSchedulerPattern to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerPattern. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerPattern.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerPattern') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerPattern
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerPattern <- function(SectionSchedulerPatternID, Code = F, CodeDescription = F, CreatedTime = F, DayRotationCount = F, DayRotationSummary = F, Description = F, EntityID = F, HasDayRotations = F, ModifiedTime = F, SchoolYearID = F, SectionSchedulerPatternIDClonedFrom = F, SectionSchedulerPatternIDClonedTo = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerPatternID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerPattern", objectId = SectionSchedulerPatternID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerPattern
	#'
	#' This function deletes a SectionSchedulerPattern
	#' @param SectionSchedulerPatternID The ID of the SectionSchedulerPattern to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerPatternID of the deleted SectionSchedulerPattern.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerPattern <- function(SectionSchedulerPatternID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerPattern", objectId = SectionSchedulerPatternID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerPattern
	#'
	#' This function creates a SectionSchedulerPattern
	#' @param fieldNames The field values to give the created SectionSchedulerPattern. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerPattern
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerPattern <- function(Code = NULL, DayRotationSummary = NULL, Description = NULL, EntityID = NULL, SchoolYearID = NULL, SectionSchedulerPatternIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerPattern", body = list(DataObject = body), searchFields = append("SectionSchedulerPatternID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerPattern
	#'
	#' This function modifies a SectionSchedulerPattern
	#' @param fieldNames The field values to give the modified SectionSchedulerPattern. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerPattern
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerPattern <- function(SectionSchedulerPatternID, Code = NULL, DayRotationSummary = NULL, Description = NULL, EntityID = NULL, SchoolYearID = NULL, SectionSchedulerPatternIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerPattern", objectId = SectionSchedulerPatternID, body = list(DataObject = body), searchFields = append("SectionSchedulerPatternID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseMasterMassUpdates
	#'
	#' This function returns a dataframe or json object of CourseMasterMassUpdates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseMasterMassUpdates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseMasterMassUpdates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseMasterMassUpdate') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseMasterMassUpdates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseMasterMassUpdates <- function(searchConditionsList = NULL, CourseMasterMassUpdateID = F, CreatedTime = F, EntityID = F, FilterParameters = F, ModifiedTime = F, RunReason = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDRanBy = F, ValueParameters = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseMasterMassUpdate", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseMasterMassUpdate
	#'
	#' This function returns a dataframe or json object of a CourseMasterMassUpdate
	#' @param CourseMasterMassUpdateID The ID of the CourseMasterMassUpdate to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseMasterMassUpdate. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseMasterMassUpdate.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseMasterMassUpdate') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseMasterMassUpdate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseMasterMassUpdate <- function(CourseMasterMassUpdateID, CreatedTime = F, EntityID = F, FilterParameters = F, ModifiedTime = F, RunReason = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, UserIDRanBy = F, ValueParameters = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseMasterMassUpdateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseMasterMassUpdate", objectId = CourseMasterMassUpdateID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseMasterMassUpdate
	#'
	#' This function deletes a CourseMasterMassUpdate
	#' @param CourseMasterMassUpdateID The ID of the CourseMasterMassUpdate to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseMasterMassUpdateID of the deleted CourseMasterMassUpdate.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseMasterMassUpdate <- function(CourseMasterMassUpdateID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseMasterMassUpdate", objectId = CourseMasterMassUpdateID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CourseMasterMassUpdate
	#'
	#' This function creates a CourseMasterMassUpdate
	#' @param fieldNames The field values to give the created CourseMasterMassUpdate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CourseMasterMassUpdate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCourseMasterMassUpdate <- function(EntityID = NULL, FilterParameters = NULL, RunReason = NULL, SchoolYearID = NULL, UserIDRanBy = NULL, ValueParameters = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CourseMasterMassUpdate", body = list(DataObject = body), searchFields = append("CourseMasterMassUpdateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CourseMasterMassUpdate
	#'
	#' This function modifies a CourseMasterMassUpdate
	#' @param fieldNames The field values to give the modified CourseMasterMassUpdate. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CourseMasterMassUpdate
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCourseMasterMassUpdate <- function(CourseMasterMassUpdateID, EntityID = NULL, FilterParameters = NULL, RunReason = NULL, SchoolYearID = NULL, UserIDRanBy = NULL, ValueParameters = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CourseMasterMassUpdate", objectId = CourseMasterMassUpdateID, body = list(DataObject = body), searchFields = append("CourseMasterMassUpdateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseMasterMassUpdateErrors
	#'
	#' This function returns a dataframe or json object of TempCourseMasterMassUpdateErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMasterMassUpdateErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMasterMassUpdateErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMasterMassUpdateError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempCourseMasterMassUpdateErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseMasterMassUpdateErrors <- function(searchConditionsList = NULL, BaseTableName = F, CreatedTime = F, ModifiedTime = F, SourceDescription = F, TableName = F, TempCourseMasterMassUpdateErrorID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempCourseMasterMassUpdateError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseMasterMassUpdateError
	#'
	#' This function returns a dataframe or json object of a TempCourseMasterMassUpdateError
	#' @param TempCourseMasterMassUpdateErrorID The ID of the TempCourseMasterMassUpdateError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMasterMassUpdateError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMasterMassUpdateError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMasterMassUpdateError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempCourseMasterMassUpdateError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseMasterMassUpdateError <- function(TempCourseMasterMassUpdateErrorID, BaseTableName = F, CreatedTime = F, ModifiedTime = F, SourceDescription = F, TableName = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseMasterMassUpdateErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateError", objectId = TempCourseMasterMassUpdateErrorID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseMasterMassUpdateError
	#'
	#' This function deletes a TempCourseMasterMassUpdateError
	#' @param TempCourseMasterMassUpdateErrorID The ID of the TempCourseMasterMassUpdateError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempCourseMasterMassUpdateErrorID of the deleted TempCourseMasterMassUpdateError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseMasterMassUpdateError <- function(TempCourseMasterMassUpdateErrorID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateError", objectId = TempCourseMasterMassUpdateErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseMasterMassUpdateError
	#'
	#' This function creates a TempCourseMasterMassUpdateError
	#' @param fieldNames The field values to give the created TempCourseMasterMassUpdateError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempCourseMasterMassUpdateError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseMasterMassUpdateError <- function(BaseTableName = NULL, SourceDescription = NULL, TableName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateError", body = list(DataObject = body), searchFields = append("TempCourseMasterMassUpdateErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseMasterMassUpdateError
	#'
	#' This function modifies a TempCourseMasterMassUpdateError
	#' @param fieldNames The field values to give the modified TempCourseMasterMassUpdateError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempCourseMasterMassUpdateError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseMasterMassUpdateError <- function(TempCourseMasterMassUpdateErrorID, BaseTableName = NULL, SourceDescription = NULL, TableName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateError", objectId = TempCourseMasterMassUpdateErrorID, body = list(DataObject = body), searchFields = append("TempCourseMasterMassUpdateErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseMasterMassUpdateFields
	#'
	#' This function returns a dataframe or json object of TempCourseMasterMassUpdateFields
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMasterMassUpdateFields. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMasterMassUpdateFields.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMasterMassUpdateField') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempCourseMasterMassUpdateFields
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseMasterMassUpdateFields <- function(searchConditionsList = NULL, AffectedPrimaryKey = F, BaseTableName = F, CourseID = F, CreatedTime = F, FieldDisplayName = F, FieldName = F, FriendlyOriginalValue = F, FriendlyUpdatedValue = F, ModifiedTime = F, OriginalValue = F, SourceDescription = F, TableName = F, TempCourseMasterMassUpdateFieldID = F, UpdatedValue = F, UpdateRank = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempCourseMasterMassUpdateField", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseMasterMassUpdateField
	#'
	#' This function returns a dataframe or json object of a TempCourseMasterMassUpdateField
	#' @param TempCourseMasterMassUpdateFieldID The ID of the TempCourseMasterMassUpdateField to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMasterMassUpdateField. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMasterMassUpdateField.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMasterMassUpdateField') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempCourseMasterMassUpdateField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseMasterMassUpdateField <- function(TempCourseMasterMassUpdateFieldID, AffectedPrimaryKey = F, BaseTableName = F, CourseID = F, CreatedTime = F, FieldDisplayName = F, FieldName = F, FriendlyOriginalValue = F, FriendlyUpdatedValue = F, ModifiedTime = F, OriginalValue = F, SourceDescription = F, TableName = F, UpdatedValue = F, UpdateRank = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseMasterMassUpdateFieldID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateField", objectId = TempCourseMasterMassUpdateFieldID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseMasterMassUpdateField
	#'
	#' This function deletes a TempCourseMasterMassUpdateField
	#' @param TempCourseMasterMassUpdateFieldID The ID of the TempCourseMasterMassUpdateField to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempCourseMasterMassUpdateFieldID of the deleted TempCourseMasterMassUpdateField.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseMasterMassUpdateField <- function(TempCourseMasterMassUpdateFieldID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateField", objectId = TempCourseMasterMassUpdateFieldID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseMasterMassUpdateField
	#'
	#' This function creates a TempCourseMasterMassUpdateField
	#' @param fieldNames The field values to give the created TempCourseMasterMassUpdateField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempCourseMasterMassUpdateField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseMasterMassUpdateField <- function(AffectedPrimaryKey = NULL, BaseTableName = NULL, CourseID = NULL, FieldDisplayName = NULL, FieldName = NULL, FriendlyOriginalValue = NULL, FriendlyUpdatedValue = NULL, OriginalValue = NULL, SourceDescription = NULL, TableName = NULL, UpdatedValue = NULL, UpdateRank = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateField", body = list(DataObject = body), searchFields = append("TempCourseMasterMassUpdateFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseMasterMassUpdateField
	#'
	#' This function modifies a TempCourseMasterMassUpdateField
	#' @param fieldNames The field values to give the modified TempCourseMasterMassUpdateField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempCourseMasterMassUpdateField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseMasterMassUpdateField <- function(TempCourseMasterMassUpdateFieldID, AffectedPrimaryKey = NULL, BaseTableName = NULL, CourseID = NULL, FieldDisplayName = NULL, FieldName = NULL, FriendlyOriginalValue = NULL, FriendlyUpdatedValue = NULL, OriginalValue = NULL, SourceDescription = NULL, TableName = NULL, UpdatedValue = NULL, UpdateRank = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateField", objectId = TempCourseMasterMassUpdateFieldID, body = list(DataObject = body), searchFields = append("TempCourseMasterMassUpdateFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerProposedStaffMeets
	#'
	#' This function returns a dataframe or json object of SectionSchedulerProposedStaffMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedStaffMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedStaffMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedStaffMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerProposedStaffMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerProposedStaffMeets <- function(searchConditionsList = NULL, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, IsPrimary = F, ModifiedTime = F, SectionSchedulerProposedMeetID = F, SectionSchedulerProposedStaffMeetID = F, StaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerProposedStaffMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerProposedStaffMeet
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerProposedStaffMeet
	#' @param SectionSchedulerProposedStaffMeetID The ID of the SectionSchedulerProposedStaffMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerProposedStaffMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerProposedStaffMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerProposedStaffMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerProposedStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerProposedStaffMeet <- function(SectionSchedulerProposedStaffMeetID, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, IsPrimary = F, ModifiedTime = F, SectionSchedulerProposedMeetID = F, StaffID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerProposedStaffMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedStaffMeet", objectId = SectionSchedulerProposedStaffMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerProposedStaffMeet
	#'
	#' This function deletes a SectionSchedulerProposedStaffMeet
	#' @param SectionSchedulerProposedStaffMeetID The ID of the SectionSchedulerProposedStaffMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerProposedStaffMeetID of the deleted SectionSchedulerProposedStaffMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerProposedStaffMeet <- function(SectionSchedulerProposedStaffMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedStaffMeet", objectId = SectionSchedulerProposedStaffMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerProposedStaffMeet
	#'
	#' This function creates a SectionSchedulerProposedStaffMeet
	#' @param fieldNames The field values to give the created SectionSchedulerProposedStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerProposedStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerProposedStaffMeet <- function(EffectiveEndDate = NULL, EffectiveStartDate = NULL, IsPrimary = NULL, SectionSchedulerProposedMeetID = NULL, StaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedStaffMeet", body = list(DataObject = body), searchFields = append("SectionSchedulerProposedStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerProposedStaffMeet
	#'
	#' This function modifies a SectionSchedulerProposedStaffMeet
	#' @param fieldNames The field values to give the modified SectionSchedulerProposedStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerProposedStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerProposedStaffMeet <- function(SectionSchedulerProposedStaffMeetID, EffectiveEndDate = NULL, EffectiveStartDate = NULL, IsPrimary = NULL, SectionSchedulerProposedMeetID = NULL, StaffID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerProposedStaffMeet", objectId = SectionSchedulerProposedStaffMeetID, body = list(DataObject = body), searchFields = append("SectionSchedulerProposedStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StaffStudents
	#'
	#' This function returns a dataframe or json object of StaffStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StaffStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStaffStudents <- function(searchConditionsList = NULL, EntityID = F, SchoolYearID = F, StaffID = F, StudentID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StaffStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StaffStudent
	#'
	#' This function returns a dataframe or json object of a StaffStudent
	#' @param StaffStudentID The ID of the StaffStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StaffStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StaffStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StaffStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StaffStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStaffStudent <- function(StaffStudentID, EntityID = F, SchoolYearID = F, StaffID = F, StudentID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StaffStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StaffStudent", objectId = StaffStudentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StaffStudent
	#'
	#' This function deletes a StaffStudent
	#' @param StaffStudentID The ID of the StaffStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StaffStudentID of the deleted StaffStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStaffStudent <- function(StaffStudentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StaffStudent", objectId = StaffStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempCourseMasterMassUpdateErrorDetails
	#'
	#' This function returns a dataframe or json object of TempCourseMasterMassUpdateErrorDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMasterMassUpdateErrorDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMasterMassUpdateErrorDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMasterMassUpdateErrorDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempCourseMasterMassUpdateErrorDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempCourseMasterMassUpdateErrorDetails <- function(searchConditionsList = NULL, CreatedTime = F, Error = F, ErrorName = F, ModifiedTime = F, TempCourseMasterMassUpdateErrorDetailID = F, TempCourseMasterMassUpdateErrorID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempCourseMasterMassUpdateErrorDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempCourseMasterMassUpdateErrorDetail
	#'
	#' This function returns a dataframe or json object of a TempCourseMasterMassUpdateErrorDetail
	#' @param TempCourseMasterMassUpdateErrorDetailID The ID of the TempCourseMasterMassUpdateErrorDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempCourseMasterMassUpdateErrorDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempCourseMasterMassUpdateErrorDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempCourseMasterMassUpdateErrorDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempCourseMasterMassUpdateErrorDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempCourseMasterMassUpdateErrorDetail <- function(TempCourseMasterMassUpdateErrorDetailID, CreatedTime = F, Error = F, ErrorName = F, ModifiedTime = F, TempCourseMasterMassUpdateErrorID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempCourseMasterMassUpdateErrorDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateErrorDetail", objectId = TempCourseMasterMassUpdateErrorDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempCourseMasterMassUpdateErrorDetail
	#'
	#' This function deletes a TempCourseMasterMassUpdateErrorDetail
	#' @param TempCourseMasterMassUpdateErrorDetailID The ID of the TempCourseMasterMassUpdateErrorDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempCourseMasterMassUpdateErrorDetailID of the deleted TempCourseMasterMassUpdateErrorDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempCourseMasterMassUpdateErrorDetail <- function(TempCourseMasterMassUpdateErrorDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateErrorDetail", objectId = TempCourseMasterMassUpdateErrorDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempCourseMasterMassUpdateErrorDetail
	#'
	#' This function creates a TempCourseMasterMassUpdateErrorDetail
	#' @param fieldNames The field values to give the created TempCourseMasterMassUpdateErrorDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempCourseMasterMassUpdateErrorDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempCourseMasterMassUpdateErrorDetail <- function(Error = NULL, ErrorName = NULL, TempCourseMasterMassUpdateErrorID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateErrorDetail", body = list(DataObject = body), searchFields = append("TempCourseMasterMassUpdateErrorDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempCourseMasterMassUpdateErrorDetail
	#'
	#' This function modifies a TempCourseMasterMassUpdateErrorDetail
	#' @param fieldNames The field values to give the modified TempCourseMasterMassUpdateErrorDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempCourseMasterMassUpdateErrorDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempCourseMasterMassUpdateErrorDetail <- function(TempCourseMasterMassUpdateErrorDetailID, Error = NULL, ErrorName = NULL, TempCourseMasterMassUpdateErrorID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempCourseMasterMassUpdateErrorDetail", objectId = TempCourseMasterMassUpdateErrorDetailID, body = list(DataObject = body), searchFields = append("TempCourseMasterMassUpdateErrorDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionEnrollmentTotalForSectionLengthSubsets
	#'
	#' This function returns a dataframe or json object of SectionEnrollmentTotalForSectionLengthSubsets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionEnrollmentTotalForSectionLengthSubsets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionEnrollmentTotalForSectionLengthSubsets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionEnrollmentTotalForSectionLengthSubset') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionEnrollmentTotalForSectionLengthSubsets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionEnrollmentTotalForSectionLengthSubsets <- function(searchConditionsList = NULL, SectionID = F, SectionLengthSubsetID = F, TotalEnrollmentCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionEnrollmentTotalForSectionLengthSubset", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionEnrollmentTotalForSectionLengthSubset
	#'
	#' This function returns a dataframe or json object of a SectionEnrollmentTotalForSectionLengthSubset
	#' @param SectionEnrollmentTotalForSectionLengthSubsetID The ID of the SectionEnrollmentTotalForSectionLengthSubset to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionEnrollmentTotalForSectionLengthSubset. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionEnrollmentTotalForSectionLengthSubset.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionEnrollmentTotalForSectionLengthSubset') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionEnrollmentTotalForSectionLengthSubset
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionEnrollmentTotalForSectionLengthSubset <- function(SectionEnrollmentTotalForSectionLengthSubsetID, SectionID = F, SectionLengthSubsetID = F, TotalEnrollmentCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionEnrollmentTotalForSectionLengthSubsetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionEnrollmentTotalForSectionLengthSubset", objectId = SectionEnrollmentTotalForSectionLengthSubsetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionEnrollmentTotalForSectionLengthSubset
	#'
	#' This function deletes a SectionEnrollmentTotalForSectionLengthSubset
	#' @param SectionEnrollmentTotalForSectionLengthSubsetID The ID of the SectionEnrollmentTotalForSectionLengthSubset to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionEnrollmentTotalForSectionLengthSubsetID of the deleted SectionEnrollmentTotalForSectionLengthSubset.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionEnrollmentTotalForSectionLengthSubset <- function(SectionEnrollmentTotalForSectionLengthSubsetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionEnrollmentTotalForSectionLengthSubset", objectId = SectionEnrollmentTotalForSectionLengthSubsetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStaffMeets
	#'
	#' This function returns a dataframe or json object of TempStaffMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStaffMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStaffMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStaffMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStaffMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStaffMeets <- function(searchConditionsList = NULL, Conflicts = F, CourseCode = F, CourseDescription = F, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, HasAttendanceAccess = F, HasConflicts = F, HasGradebookAccess = F, IsChecked = F, IsLongTermSubstitute = F, IsPrimary = F, IsSubstitute = F, MeetID = F, ModifiedTime = F, NewEffectiveEndDate = F, NewEffectiveStartDate = F, NewStaffFullNameFML = F, NewStaffID = F, Note = F, SectionCode = F, SectionID = F, StaffFullNameFML = F, StaffID = F, StaffMeetID = F, TempStaffMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStaffMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStaffMeet
	#'
	#' This function returns a dataframe or json object of a TempStaffMeet
	#' @param TempStaffMeetID The ID of the TempStaffMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStaffMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStaffMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStaffMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStaffMeet <- function(TempStaffMeetID, Conflicts = F, CourseCode = F, CourseDescription = F, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, HasAttendanceAccess = F, HasConflicts = F, HasGradebookAccess = F, IsChecked = F, IsLongTermSubstitute = F, IsPrimary = F, IsSubstitute = F, MeetID = F, ModifiedTime = F, NewEffectiveEndDate = F, NewEffectiveStartDate = F, NewStaffFullNameFML = F, NewStaffID = F, Note = F, SectionCode = F, SectionID = F, StaffFullNameFML = F, StaffID = F, StaffMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStaffMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStaffMeet", objectId = TempStaffMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStaffMeet
	#'
	#' This function deletes a TempStaffMeet
	#' @param TempStaffMeetID The ID of the TempStaffMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStaffMeetID of the deleted TempStaffMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStaffMeet <- function(TempStaffMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStaffMeet", objectId = TempStaffMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStaffMeet
	#'
	#' This function creates a TempStaffMeet
	#' @param fieldNames The field values to give the created TempStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStaffMeet <- function(Conflicts = NULL, CourseCode = NULL, CourseDescription = NULL, EffectiveEndDate = NULL, EffectiveStartDate = NULL, HasAttendanceAccess = NULL, HasGradebookAccess = NULL, IsChecked = NULL, IsLongTermSubstitute = NULL, IsPrimary = NULL, IsSubstitute = NULL, MeetID = NULL, NewEffectiveEndDate = NULL, NewEffectiveStartDate = NULL, NewStaffFullNameFML = NULL, NewStaffID = NULL, Note = NULL, SectionCode = NULL, SectionID = NULL, StaffFullNameFML = NULL, StaffID = NULL, StaffMeetID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStaffMeet", body = list(DataObject = body), searchFields = append("TempStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStaffMeet
	#'
	#' This function modifies a TempStaffMeet
	#' @param fieldNames The field values to give the modified TempStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStaffMeet <- function(TempStaffMeetID, Conflicts = NULL, CourseCode = NULL, CourseDescription = NULL, EffectiveEndDate = NULL, EffectiveStartDate = NULL, HasAttendanceAccess = NULL, HasGradebookAccess = NULL, IsChecked = NULL, IsLongTermSubstitute = NULL, IsPrimary = NULL, IsSubstitute = NULL, MeetID = NULL, NewEffectiveEndDate = NULL, NewEffectiveStartDate = NULL, NewStaffFullNameFML = NULL, NewStaffID = NULL, Note = NULL, SectionCode = NULL, SectionID = NULL, StaffFullNameFML = NULL, StaffID = NULL, StaffMeetID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStaffMeet", objectId = TempStaffMeetID, body = list(DataObject = body), searchFields = append("TempStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionEnrollmentTotalForSectionLengthSubsetAndEntities
	#'
	#' This function returns a dataframe or json object of SectionEnrollmentTotalForSectionLengthSubsetAndEntities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionEnrollmentTotalForSectionLengthSubsetAndEntities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionEnrollmentTotalForSectionLengthSubsetAndEntities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionEnrollmentTotalForSectionLengthSubsetAndEntity') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionEnrollmentTotalForSectionLengthSubsetAndEntities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionEnrollmentTotalForSectionLengthSubsetAndEntities <- function(searchConditionsList = NULL, EntityIDCountsAs = F, SectionID = F, SectionLengthSubsetID = F, TotalEnrollmentCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionEnrollmentTotalForSectionLengthSubsetAndEntity", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionEnrollmentTotalForSectionLengthSubsetAndEntity
	#'
	#' This function returns a dataframe or json object of a SectionEnrollmentTotalForSectionLengthSubsetAndEntity
	#' @param SectionEnrollmentTotalForSectionLengthSubsetAndEntityID The ID of the SectionEnrollmentTotalForSectionLengthSubsetAndEntity to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionEnrollmentTotalForSectionLengthSubsetAndEntity. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionEnrollmentTotalForSectionLengthSubsetAndEntity.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionEnrollmentTotalForSectionLengthSubsetAndEntity') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionEnrollmentTotalForSectionLengthSubsetAndEntity
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionEnrollmentTotalForSectionLengthSubsetAndEntity <- function(SectionEnrollmentTotalForSectionLengthSubsetAndEntityID, EntityIDCountsAs = F, SectionID = F, SectionLengthSubsetID = F, TotalEnrollmentCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionEnrollmentTotalForSectionLengthSubsetAndEntityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionEnrollmentTotalForSectionLengthSubsetAndEntity", objectId = SectionEnrollmentTotalForSectionLengthSubsetAndEntityID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionEnrollmentTotalForSectionLengthSubsetAndEntity
	#'
	#' This function deletes a SectionEnrollmentTotalForSectionLengthSubsetAndEntity
	#' @param SectionEnrollmentTotalForSectionLengthSubsetAndEntityID The ID of the SectionEnrollmentTotalForSectionLengthSubsetAndEntity to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionEnrollmentTotalForSectionLengthSubsetAndEntityID of the deleted SectionEnrollmentTotalForSectionLengthSubsetAndEntity.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionEnrollmentTotalForSectionLengthSubsetAndEntity <- function(SectionEnrollmentTotalForSectionLengthSubsetAndEntityID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionEnrollmentTotalForSectionLengthSubsetAndEntity", objectId = SectionEnrollmentTotalForSectionLengthSubsetAndEntityID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionMeetSummaries
	#'
	#' This function returns a dataframe or json object of SectionMeetSummaries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionMeetSummaries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionMeetSummaries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionMeetSummary') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionMeetSummaries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionMeetSummaries <- function(searchConditionsList = NULL, CalendarID = F, CreatedTime = F, EntityIDViewing = F, IsDefaultCalendar = F, ModifiedTime = F, PeriodDaySummary = F, SectionID = F, SectionMeetSummaryID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionMeetSummary", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionMeetSummary
	#'
	#' This function returns a dataframe or json object of a SectionMeetSummary
	#' @param SectionMeetSummaryID The ID of the SectionMeetSummary to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionMeetSummary. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionMeetSummary.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionMeetSummary') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionMeetSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionMeetSummary <- function(SectionMeetSummaryID, CalendarID = F, CreatedTime = F, EntityIDViewing = F, IsDefaultCalendar = F, ModifiedTime = F, PeriodDaySummary = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionMeetSummaryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionMeetSummary", objectId = SectionMeetSummaryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionMeetSummary
	#'
	#' This function deletes a SectionMeetSummary
	#' @param SectionMeetSummaryID The ID of the SectionMeetSummary to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionMeetSummaryID of the deleted SectionMeetSummary.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionMeetSummary <- function(SectionMeetSummaryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionMeetSummary", objectId = SectionMeetSummaryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionMeetSummary
	#'
	#' This function creates a SectionMeetSummary
	#' @param fieldNames The field values to give the created SectionMeetSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionMeetSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionMeetSummary <- function(CalendarID = NULL, EntityIDViewing = NULL, IsDefaultCalendar = NULL, PeriodDaySummary = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionMeetSummary", body = list(DataObject = body), searchFields = append("SectionMeetSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionMeetSummary
	#'
	#' This function modifies a SectionMeetSummary
	#' @param fieldNames The field values to give the modified SectionMeetSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionMeetSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionMeetSummary <- function(SectionMeetSummaryID, CalendarID = NULL, EntityIDViewing = NULL, IsDefaultCalendar = NULL, PeriodDaySummary = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionMeetSummary", objectId = SectionMeetSummaryID, body = list(DataObject = body), searchFields = append("SectionMeetSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempStudents
	#'
	#' This function returns a dataframe or json object of TempStudents
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudents. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudents.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudent') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempStudents
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempStudents <- function(searchConditionsList = NULL, CourseRequestCount = F, CreatedTime = F, CurrentActive = F, FullNameLFM = F, GenderCode = F, GradeLevelCode = F, GradeReferenceID = F, GradYear = F, HasConflictedStudentCourseRequest = F, HasFailedUpdate = F, HomeRoomCode = F, IsSelected = F, ModifiedTime = F, SchoolName = F, StudentID = F, StudentNumber = F, StudentSectionCount = F, StudentTypeCodeDescription = F, TempStudentID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempStudent", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempStudent
	#'
	#' This function returns a dataframe or json object of a TempStudent
	#' @param TempStudentID The ID of the TempStudent to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempStudent. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempStudent.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempStudent') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempStudent <- function(TempStudentID, CourseRequestCount = F, CreatedTime = F, CurrentActive = F, FullNameLFM = F, GenderCode = F, GradeLevelCode = F, GradeReferenceID = F, GradYear = F, HasConflictedStudentCourseRequest = F, HasFailedUpdate = F, HomeRoomCode = F, IsSelected = F, ModifiedTime = F, SchoolName = F, StudentID = F, StudentNumber = F, StudentSectionCount = F, StudentTypeCodeDescription = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempStudentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempStudent", objectId = TempStudentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempStudent
	#'
	#' This function deletes a TempStudent
	#' @param TempStudentID The ID of the TempStudent to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempStudentID of the deleted TempStudent.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempStudent <- function(TempStudentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempStudent", objectId = TempStudentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempStudent
	#'
	#' This function creates a TempStudent
	#' @param fieldNames The field values to give the created TempStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempStudent <- function(CourseRequestCount = NULL, CurrentActive = NULL, FullNameLFM = NULL, GenderCode = NULL, GradeLevelCode = NULL, GradeReferenceID = NULL, GradYear = NULL, HasConflictedStudentCourseRequest = NULL, HomeRoomCode = NULL, SchoolName = NULL, StudentID = NULL, StudentNumber = NULL, StudentSectionCount = NULL, StudentTypeCodeDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempStudent", body = list(DataObject = body), searchFields = append("TempStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempStudent
	#'
	#' This function modifies a TempStudent
	#' @param fieldNames The field values to give the modified TempStudent. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempStudent
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempStudent <- function(TempStudentID, CourseRequestCount = NULL, CurrentActive = NULL, FullNameLFM = NULL, GenderCode = NULL, GradeLevelCode = NULL, GradeReferenceID = NULL, GradYear = NULL, HasConflictedStudentCourseRequest = NULL, HomeRoomCode = NULL, SchoolName = NULL, StudentID = NULL, StudentNumber = NULL, StudentSectionCount = NULL, StudentTypeCodeDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempStudent", objectId = TempStudentID, body = list(DataObject = body), searchFields = append("TempStudentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List SectionSchedulerDayRotationForMeets
	#'
	#' This function returns a dataframe or json object of SectionSchedulerDayRotationForMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerDayRotationForMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerDayRotationForMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerDayRotationForMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of SectionSchedulerDayRotationForMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listSectionSchedulerDayRotationForMeets <- function(searchConditionsList = NULL, CreatedTime = F, DayRotationID = F, MeetID = F, ModifiedTime = F, SectionSchedulerDayRotationForMeetClonedTo = F, SectionSchedulerDayRotationForMeetID = F, SectionSchedulerDayRotationForMeetIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "SectionSchedulerDayRotationForMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a SectionSchedulerDayRotationForMeet
	#'
	#' This function returns a dataframe or json object of a SectionSchedulerDayRotationForMeet
	#' @param SectionSchedulerDayRotationForMeetID The ID of the SectionSchedulerDayRotationForMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given SectionSchedulerDayRotationForMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the SectionSchedulerDayRotationForMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('SectionSchedulerDayRotationForMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of SectionSchedulerDayRotationForMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getSectionSchedulerDayRotationForMeet <- function(SectionSchedulerDayRotationForMeetID, CreatedTime = F, DayRotationID = F, MeetID = F, ModifiedTime = F, SectionSchedulerDayRotationForMeetClonedTo = F, SectionSchedulerDayRotationForMeetIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "SectionSchedulerDayRotationForMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "SectionSchedulerDayRotationForMeet", objectId = SectionSchedulerDayRotationForMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a SectionSchedulerDayRotationForMeet
	#'
	#' This function deletes a SectionSchedulerDayRotationForMeet
	#' @param SectionSchedulerDayRotationForMeetID The ID of the SectionSchedulerDayRotationForMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The SectionSchedulerDayRotationForMeetID of the deleted SectionSchedulerDayRotationForMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteSectionSchedulerDayRotationForMeet <- function(SectionSchedulerDayRotationForMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "SectionSchedulerDayRotationForMeet", objectId = SectionSchedulerDayRotationForMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a SectionSchedulerDayRotationForMeet
	#'
	#' This function creates a SectionSchedulerDayRotationForMeet
	#' @param fieldNames The field values to give the created SectionSchedulerDayRotationForMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created SectionSchedulerDayRotationForMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createSectionSchedulerDayRotationForMeet <- function(DayRotationID = NULL, MeetID = NULL, SectionSchedulerDayRotationForMeetIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "SectionSchedulerDayRotationForMeet", body = list(DataObject = body), searchFields = append("SectionSchedulerDayRotationForMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a SectionSchedulerDayRotationForMeet
	#'
	#' This function modifies a SectionSchedulerDayRotationForMeet
	#' @param fieldNames The field values to give the modified SectionSchedulerDayRotationForMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified SectionSchedulerDayRotationForMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifySectionSchedulerDayRotationForMeet <- function(SectionSchedulerDayRotationForMeetID, DayRotationID = NULL, MeetID = NULL, SectionSchedulerDayRotationForMeetIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "SectionSchedulerDayRotationForMeet", objectId = SectionSchedulerDayRotationForMeetID, body = list(DataObject = body), searchFields = append("SectionSchedulerDayRotationForMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AppleSchoolManagerConfigs
	#'
	#' This function returns a dataframe or json object of AppleSchoolManagerConfigs
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AppleSchoolManagerConfigs. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AppleSchoolManagerConfigs.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AppleSchoolManagerConfig') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AppleSchoolManagerConfigs
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAppleSchoolManagerConfigs <- function(searchConditionsList = NULL, AppleSchoolManagerConfigID = F, AsOfDateSetting = F, CreatedTime = F, DistrictID = F, EntityIDSelectedList = F, FileDestinationIDSelectedList = F, MediaID = F, ModifiedTime = F, SchoolYearID = F, SpecifiedAsOfDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipFileName = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AppleSchoolManagerConfig", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AppleSchoolManagerConfig
	#'
	#' This function returns a dataframe or json object of an AppleSchoolManagerConfig
	#' @param AppleSchoolManagerConfigID The ID of the AppleSchoolManagerConfig to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AppleSchoolManagerConfig. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AppleSchoolManagerConfig.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AppleSchoolManagerConfig') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AppleSchoolManagerConfig
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAppleSchoolManagerConfig <- function(AppleSchoolManagerConfigID, AsOfDateSetting = F, CreatedTime = F, DistrictID = F, EntityIDSelectedList = F, FileDestinationIDSelectedList = F, MediaID = F, ModifiedTime = F, SchoolYearID = F, SpecifiedAsOfDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipFileName = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AppleSchoolManagerConfigID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfig", objectId = AppleSchoolManagerConfigID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AppleSchoolManagerConfig
	#'
	#' This function deletes an AppleSchoolManagerConfig
	#' @param AppleSchoolManagerConfigID The ID of the AppleSchoolManagerConfig to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AppleSchoolManagerConfigID of the deleted AppleSchoolManagerConfig.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAppleSchoolManagerConfig <- function(AppleSchoolManagerConfigID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfig", objectId = AppleSchoolManagerConfigID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AppleSchoolManagerConfig
	#'
	#' This function creates an AppleSchoolManagerConfig
	#' @param fieldNames The field values to give the created AppleSchoolManagerConfig. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AppleSchoolManagerConfig
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAppleSchoolManagerConfig <- function(AsOfDateSetting = NULL, DistrictID = NULL, EntityIDSelectedList = NULL, MediaID = NULL, SchoolYearID = NULL, SpecifiedAsOfDate = NULL, ZipFileName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfig", body = list(DataObject = body), searchFields = append("AppleSchoolManagerConfigID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AppleSchoolManagerConfig
	#'
	#' This function modifies an AppleSchoolManagerConfig
	#' @param fieldNames The field values to give the modified AppleSchoolManagerConfig. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AppleSchoolManagerConfig
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAppleSchoolManagerConfig <- function(AppleSchoolManagerConfigID, AsOfDateSetting = NULL, DistrictID = NULL, EntityIDSelectedList = NULL, MediaID = NULL, SchoolYearID = NULL, SpecifiedAsOfDate = NULL, ZipFileName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfig", objectId = AppleSchoolManagerConfigID, body = list(DataObject = body), searchFields = append("AppleSchoolManagerConfigID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempFailedStaffMeets
	#'
	#' This function returns a dataframe or json object of TempFailedStaffMeets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStaffMeets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStaffMeets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStaffMeet') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempFailedStaffMeets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempFailedStaffMeets <- function(searchConditionsList = NULL, Conflicts = F, CourseCode = F, CourseDescription = F, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, ErrorCount = F, HasAttendanceAccess = F, HasConflicts = F, HasErrors = F, HasGradebookAccess = F, IsChecked = F, IsLongTermSubstitute = F, IsPrimary = F, IsSubstitute = F, MeetID = F, ModifiedTime = F, NewEffectiveEndDate = F, NewEffectiveStartDate = F, NewStaffFullNameFML = F, NewStaffID = F, Note = F, SectionCode = F, SectionID = F, StaffFullNameFML = F, StaffID = F, StaffMeetID = F, TempFailedStaffMeetID = F, TempStaffMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempFailedStaffMeet", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempFailedStaffMeet
	#'
	#' This function returns a dataframe or json object of a TempFailedStaffMeet
	#' @param TempFailedStaffMeetID The ID of the TempFailedStaffMeet to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempFailedStaffMeet. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempFailedStaffMeet.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempFailedStaffMeet') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempFailedStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempFailedStaffMeet <- function(TempFailedStaffMeetID, Conflicts = F, CourseCode = F, CourseDescription = F, CreatedTime = F, EffectiveEndDate = F, EffectiveStartDate = F, ErrorCount = F, HasAttendanceAccess = F, HasConflicts = F, HasErrors = F, HasGradebookAccess = F, IsChecked = F, IsLongTermSubstitute = F, IsPrimary = F, IsSubstitute = F, MeetID = F, ModifiedTime = F, NewEffectiveEndDate = F, NewEffectiveStartDate = F, NewStaffFullNameFML = F, NewStaffID = F, Note = F, SectionCode = F, SectionID = F, StaffFullNameFML = F, StaffID = F, StaffMeetID = F, TempStaffMeetID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WorkflowAction = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempFailedStaffMeetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempFailedStaffMeet", objectId = TempFailedStaffMeetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempFailedStaffMeet
	#'
	#' This function deletes a TempFailedStaffMeet
	#' @param TempFailedStaffMeetID The ID of the TempFailedStaffMeet to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempFailedStaffMeetID of the deleted TempFailedStaffMeet.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempFailedStaffMeet <- function(TempFailedStaffMeetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempFailedStaffMeet", objectId = TempFailedStaffMeetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempFailedStaffMeet
	#'
	#' This function creates a TempFailedStaffMeet
	#' @param fieldNames The field values to give the created TempFailedStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempFailedStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempFailedStaffMeet <- function(Conflicts = NULL, CourseCode = NULL, CourseDescription = NULL, EffectiveEndDate = NULL, EffectiveStartDate = NULL, ErrorCount = NULL, HasAttendanceAccess = NULL, HasErrors = NULL, HasGradebookAccess = NULL, IsChecked = NULL, IsLongTermSubstitute = NULL, IsPrimary = NULL, IsSubstitute = NULL, MeetID = NULL, NewEffectiveEndDate = NULL, NewEffectiveStartDate = NULL, NewStaffFullNameFML = NULL, NewStaffID = NULL, Note = NULL, SectionCode = NULL, SectionID = NULL, StaffFullNameFML = NULL, StaffID = NULL, StaffMeetID = NULL, TempStaffMeetID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempFailedStaffMeet", body = list(DataObject = body), searchFields = append("TempFailedStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempFailedStaffMeet
	#'
	#' This function modifies a TempFailedStaffMeet
	#' @param fieldNames The field values to give the modified TempFailedStaffMeet. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempFailedStaffMeet
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempFailedStaffMeet <- function(TempFailedStaffMeetID, Conflicts = NULL, CourseCode = NULL, CourseDescription = NULL, EffectiveEndDate = NULL, EffectiveStartDate = NULL, ErrorCount = NULL, HasAttendanceAccess = NULL, HasErrors = NULL, HasGradebookAccess = NULL, IsChecked = NULL, IsLongTermSubstitute = NULL, IsPrimary = NULL, IsSubstitute = NULL, MeetID = NULL, NewEffectiveEndDate = NULL, NewEffectiveStartDate = NULL, NewStaffFullNameFML = NULL, NewStaffID = NULL, Note = NULL, SectionCode = NULL, SectionID = NULL, StaffFullNameFML = NULL, StaffID = NULL, StaffMeetID = NULL, TempStaffMeetID = NULL, WorkflowAction = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempFailedStaffMeet", objectId = TempFailedStaffMeetID, body = list(DataObject = body), searchFields = append("TempFailedStaffMeetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List QueuedStudentSections
	#'
	#' This function returns a dataframe or json object of QueuedStudentSections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QueuedStudentSections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QueuedStudentSections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QueuedStudentSection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of QueuedStudentSections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listQueuedStudentSections <- function(searchConditionsList = NULL, CreatedTime = F, IsComplete = F, ModifiedTime = F, QueuedStudentSectionID = F, SourceProcess = F, StudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "QueuedStudentSection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a QueuedStudentSection
	#'
	#' This function returns a dataframe or json object of a QueuedStudentSection
	#' @param QueuedStudentSectionID The ID of the QueuedStudentSection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given QueuedStudentSection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the QueuedStudentSection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('QueuedStudentSection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of QueuedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getQueuedStudentSection <- function(QueuedStudentSectionID, CreatedTime = F, IsComplete = F, ModifiedTime = F, SourceProcess = F, StudentSectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "QueuedStudentSectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "QueuedStudentSection", objectId = QueuedStudentSectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a QueuedStudentSection
	#'
	#' This function deletes a QueuedStudentSection
	#' @param QueuedStudentSectionID The ID of the QueuedStudentSection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The QueuedStudentSectionID of the deleted QueuedStudentSection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteQueuedStudentSection <- function(QueuedStudentSectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "QueuedStudentSection", objectId = QueuedStudentSectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a QueuedStudentSection
	#'
	#' This function creates a QueuedStudentSection
	#' @param fieldNames The field values to give the created QueuedStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created QueuedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createQueuedStudentSection <- function(IsComplete = NULL, SourceProcess = NULL, StudentSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "QueuedStudentSection", body = list(DataObject = body), searchFields = append("QueuedStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a QueuedStudentSection
	#'
	#' This function modifies a QueuedStudentSection
	#' @param fieldNames The field values to give the modified QueuedStudentSection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified QueuedStudentSection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyQueuedStudentSection <- function(QueuedStudentSectionID, IsComplete = NULL, SourceProcess = NULL, StudentSectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "QueuedStudentSection", objectId = QueuedStudentSectionID, body = list(DataObject = body), searchFields = append("QueuedStudentSectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StudentSectionTransactionRoomPeriods
	#'
	#' This function returns a dataframe or json object of StudentSectionTransactionRoomPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionTransactionRoomPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionTransactionRoomPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionTransactionRoomPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of StudentSectionTransactionRoomPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStudentSectionTransactionRoomPeriods <- function(searchConditionsList = NULL, CalendarID = F, DisplayPeriodID = F, EndDateMeet = F, EndDateSectionSectionTransaction = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDFor = F, EntityIDViewingCalculated = F, EntityIDViewingMeetSummary = F, IsDefaultCalendar = F, MeetID = F, MeetSummaryID = F, RoomID = F, SchedulingPeriodID = F, SchoolYearID = F, SectionID = F, StartDateMeet = F, StartDateStudentSectionTransaction = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, UseRoomOverride = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "StudentSectionTransactionRoomPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StudentSectionTransactionRoomPeriod
	#'
	#' This function returns a dataframe or json object of a StudentSectionTransactionRoomPeriod
	#' @param StudentSectionTransactionRoomPeriodID The ID of the StudentSectionTransactionRoomPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StudentSectionTransactionRoomPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StudentSectionTransactionRoomPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StudentSectionTransactionRoomPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of StudentSectionTransactionRoomPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStudentSectionTransactionRoomPeriod <- function(StudentSectionTransactionRoomPeriodID, CalendarID = F, DisplayPeriodID = F, EndDateMeet = F, EndDateSectionSectionTransaction = F, EntityIDCountsAs = F, EntityIDCourse = F, EntityIDFor = F, EntityIDViewingCalculated = F, EntityIDViewingMeetSummary = F, IsDefaultCalendar = F, MeetID = F, MeetSummaryID = F, RoomID = F, SchedulingPeriodID = F, SchoolYearID = F, SectionID = F, StartDateMeet = F, StartDateStudentSectionTransaction = F, StudentID = F, StudentSectionID = F, StudentSectionTransactionID = F, UseRoomOverride = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StudentSectionTransactionRoomPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "StudentSectionTransactionRoomPeriod", objectId = StudentSectionTransactionRoomPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StudentSectionTransactionRoomPeriod
	#'
	#' This function deletes a StudentSectionTransactionRoomPeriod
	#' @param StudentSectionTransactionRoomPeriodID The ID of the StudentSectionTransactionRoomPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The StudentSectionTransactionRoomPeriodID of the deleted StudentSectionTransactionRoomPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStudentSectionTransactionRoomPeriod <- function(StudentSectionTransactionRoomPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "StudentSectionTransactionRoomPeriod", objectId = StudentSectionTransactionRoomPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MeetEntityRoomPeriods
	#'
	#' This function returns a dataframe or json object of MeetEntityRoomPeriods
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetEntityRoomPeriods. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetEntityRoomPeriods.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetEntityRoomPeriod') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MeetEntityRoomPeriods
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeetEntityRoomPeriods <- function(searchConditionsList = NULL, CalendarID = F, DisplayPeriodID = F, EndDate = F, EntityIDCourse = F, EntityIDFor = F, EntityIDViewingCalculated = F, EntityIDViewingMeetSummary = F, IsDefaultCalendar = F, MeetID = F, MeetSummaryID = F, RoomID = F, SchedulingPeriodID = F, SchoolYearID = F, SectionID = F, StartDate = F, UseRoomOverride = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MeetEntityRoomPeriod", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MeetEntityRoomPeriod
	#'
	#' This function returns a dataframe or json object of a MeetEntityRoomPeriod
	#' @param MeetEntityRoomPeriodID The ID of the MeetEntityRoomPeriod to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetEntityRoomPeriod. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetEntityRoomPeriod.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetEntityRoomPeriod') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MeetEntityRoomPeriod
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeetEntityRoomPeriod <- function(MeetEntityRoomPeriodID, CalendarID = F, DisplayPeriodID = F, EndDate = F, EntityIDCourse = F, EntityIDFor = F, EntityIDViewingCalculated = F, EntityIDViewingMeetSummary = F, IsDefaultCalendar = F, MeetID = F, MeetSummaryID = F, RoomID = F, SchedulingPeriodID = F, SchoolYearID = F, SectionID = F, StartDate = F, UseRoomOverride = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetEntityRoomPeriodID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MeetEntityRoomPeriod", objectId = MeetEntityRoomPeriodID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MeetEntityRoomPeriod
	#'
	#' This function deletes a MeetEntityRoomPeriod
	#' @param MeetEntityRoomPeriodID The ID of the MeetEntityRoomPeriod to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetEntityRoomPeriodID of the deleted MeetEntityRoomPeriod.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeetEntityRoomPeriod <- function(MeetEntityRoomPeriodID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MeetEntityRoomPeriod", objectId = MeetEntityRoomPeriodID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MeetEntityRoomPeriodDateSeatsAvailables
	#'
	#' This function returns a dataframe or json object of MeetEntityRoomPeriodDateSeatsAvailables
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetEntityRoomPeriodDateSeatsAvailables. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetEntityRoomPeriodDateSeatsAvailables.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetEntityRoomPeriodDateSeatsAvailable') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MeetEntityRoomPeriodDateSeatsAvailables
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMeetEntityRoomPeriodDateSeatsAvailables <- function(searchConditionsList = NULL, CalendarID = F, Date = F, DisplayPeriodID = F, EndDate = F, EntityIDCourse = F, EntityIDFor = F, EntityIDViewingCalculated = F, EntityIDViewingMeetSummary = F, IsOfferedMeet = F, MaxSeats = F, MaxSeatsMeet = F, MaxSeatsMeetOverride = F, MeetID = F, MeetSummaryID = F, RoomID = F, RoomIDMeet = F, RoomIDMeetOverride = F, RoomSeatsAvailable = F, SchedulingPeriodID = F, SchoolYearID = F, SectionID = F, StartDate = F, UseRoomOverride = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MeetEntityRoomPeriodDateSeatsAvailable", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MeetEntityRoomPeriodDateSeatsAvailable
	#'
	#' This function returns a dataframe or json object of a MeetEntityRoomPeriodDateSeatsAvailable
	#' @param MeetEntityRoomPeriodDateSeatsAvailableID The ID of the MeetEntityRoomPeriodDateSeatsAvailable to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MeetEntityRoomPeriodDateSeatsAvailable. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MeetEntityRoomPeriodDateSeatsAvailable.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MeetEntityRoomPeriodDateSeatsAvailable') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MeetEntityRoomPeriodDateSeatsAvailable
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMeetEntityRoomPeriodDateSeatsAvailable <- function(MeetEntityRoomPeriodDateSeatsAvailableID, CalendarID = F, Date = F, DisplayPeriodID = F, EndDate = F, EntityIDCourse = F, EntityIDFor = F, EntityIDViewingCalculated = F, EntityIDViewingMeetSummary = F, IsOfferedMeet = F, MaxSeats = F, MaxSeatsMeet = F, MaxSeatsMeetOverride = F, MeetID = F, MeetSummaryID = F, RoomID = F, RoomIDMeet = F, RoomIDMeetOverride = F, RoomSeatsAvailable = F, SchedulingPeriodID = F, SchoolYearID = F, SectionID = F, StartDate = F, UseRoomOverride = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MeetEntityRoomPeriodDateSeatsAvailableID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MeetEntityRoomPeriodDateSeatsAvailable", objectId = MeetEntityRoomPeriodDateSeatsAvailableID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MeetEntityRoomPeriodDateSeatsAvailable
	#'
	#' This function deletes a MeetEntityRoomPeriodDateSeatsAvailable
	#' @param MeetEntityRoomPeriodDateSeatsAvailableID The ID of the MeetEntityRoomPeriodDateSeatsAvailable to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MeetEntityRoomPeriodDateSeatsAvailableID of the deleted MeetEntityRoomPeriodDateSeatsAvailable.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMeetEntityRoomPeriodDateSeatsAvailable <- function(MeetEntityRoomPeriodDateSeatsAvailableID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MeetEntityRoomPeriodDateSeatsAvailable", objectId = MeetEntityRoomPeriodDateSeatsAvailableID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CoursePrerequisiteCurriculumCourseStudentCourseRequests
	#'
	#' This function returns a dataframe or json object of CoursePrerequisiteCurriculumCourseStudentCourseRequests
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CoursePrerequisiteCurriculumCourseStudentCourseRequests. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CoursePrerequisiteCurriculumCourseStudentCourseRequests.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CoursePrerequisiteCurriculumCourseStudentCourseRequest') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CoursePrerequisiteCurriculumCourseStudentCourseRequests
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCoursePrerequisiteCurriculumCourseStudentCourseRequests <- function(searchConditionsList = NULL, CourseIDFor = F, CourseIDRequired = F, CoursePrerequisiteCurriculumCourseStudentCourseRequestID = F, CurriculumIDFor = F, CurriculumIDRequired = F, PrerequisiteID = F, Status = F, StudentCourseRequestID = F, StudentID = F, StudentSectionID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourseStudentCourseRequest", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#'
	#' This function returns a dataframe or json object of a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' @param CoursePrerequisiteCurriculumCourseStudentCourseRequestID The ID of the CoursePrerequisiteCurriculumCourseStudentCourseRequest to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CoursePrerequisiteCurriculumCourseStudentCourseRequest. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CoursePrerequisiteCurriculumCourseStudentCourseRequest.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CoursePrerequisiteCurriculumCourseStudentCourseRequest') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCoursePrerequisiteCurriculumCourseStudentCourseRequest <- function(CoursePrerequisiteCurriculumCourseStudentCourseRequestID, CourseIDFor = F, CourseIDRequired = F, CurriculumIDFor = F, CurriculumIDRequired = F, PrerequisiteID = F, Status = F, StudentCourseRequestID = F, StudentID = F, StudentSectionID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CoursePrerequisiteCurriculumCourseStudentCourseRequestID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourseStudentCourseRequest", objectId = CoursePrerequisiteCurriculumCourseStudentCourseRequestID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#'
	#' This function deletes a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' @param CoursePrerequisiteCurriculumCourseStudentCourseRequestID The ID of the CoursePrerequisiteCurriculumCourseStudentCourseRequest to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CoursePrerequisiteCurriculumCourseStudentCourseRequestID of the deleted CoursePrerequisiteCurriculumCourseStudentCourseRequest.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCoursePrerequisiteCurriculumCourseStudentCourseRequest <- function(CoursePrerequisiteCurriculumCourseStudentCourseRequestID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourseStudentCourseRequest", objectId = CoursePrerequisiteCurriculumCourseStudentCourseRequestID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#'
	#' This function creates a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' @param fieldNames The field values to give the created CoursePrerequisiteCurriculumCourseStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCoursePrerequisiteCurriculumCourseStudentCourseRequest <- function(Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourseStudentCourseRequest", body = list(DataObject = body), searchFields = append("CoursePrerequisiteCurriculumCourseStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#'
	#' This function modifies a CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' @param fieldNames The field values to give the modified CoursePrerequisiteCurriculumCourseStudentCourseRequest. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified CoursePrerequisiteCurriculumCourseStudentCourseRequest
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCoursePrerequisiteCurriculumCourseStudentCourseRequest <- function(CoursePrerequisiteCurriculumCourseStudentCourseRequestID, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourseStudentCourseRequest", objectId = CoursePrerequisiteCurriculumCourseStudentCourseRequestID, body = list(DataObject = body), searchFields = append("CoursePrerequisiteCurriculumCourseStudentCourseRequestID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CoursePrerequisiteCurriculumCourses
	#'
	#' This function returns a dataframe or json object of CoursePrerequisiteCurriculumCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CoursePrerequisiteCurriculumCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CoursePrerequisiteCurriculumCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CoursePrerequisiteCurriculumCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CoursePrerequisiteCurriculumCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCoursePrerequisiteCurriculumCourses <- function(searchConditionsList = NULL, CourseIDFor = F, CourseIDRequired = F, CoursePrerequisiteCurriculumCourseID = F, CurriculumIDFor = F, CurriculumIDRequired = F, EntityIDFor = F, EntityIDRequired = F, NumericYearCourse = F, NumericYearCurrentFor = F, NumericYearRequired = F, PrerequisiteCurriculumID = F, PrerequisiteID = F, SchoolYearIDFor = F, SchoolYearIDRequired = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CoursePrerequisiteCurriculumCourse
	#'
	#' This function returns a dataframe or json object of a CoursePrerequisiteCurriculumCourse
	#' @param CoursePrerequisiteCurriculumCourseID The ID of the CoursePrerequisiteCurriculumCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CoursePrerequisiteCurriculumCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CoursePrerequisiteCurriculumCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CoursePrerequisiteCurriculumCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CoursePrerequisiteCurriculumCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCoursePrerequisiteCurriculumCourse <- function(CoursePrerequisiteCurriculumCourseID, CourseIDFor = F, CourseIDRequired = F, CurriculumIDFor = F, CurriculumIDRequired = F, EntityIDFor = F, EntityIDRequired = F, NumericYearCourse = F, NumericYearCurrentFor = F, NumericYearRequired = F, PrerequisiteCurriculumID = F, PrerequisiteID = F, SchoolYearIDFor = F, SchoolYearIDRequired = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CoursePrerequisiteCurriculumCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourse", objectId = CoursePrerequisiteCurriculumCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CoursePrerequisiteCurriculumCourse
	#'
	#' This function deletes a CoursePrerequisiteCurriculumCourse
	#' @param CoursePrerequisiteCurriculumCourseID The ID of the CoursePrerequisiteCurriculumCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CoursePrerequisiteCurriculumCourseID of the deleted CoursePrerequisiteCurriculumCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCoursePrerequisiteCurriculumCourse <- function(CoursePrerequisiteCurriculumCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CoursePrerequisiteCurriculumCourse", objectId = CoursePrerequisiteCurriculumCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CoursePrerequisites
	#'
	#' This function returns a dataframe or json object of CoursePrerequisites
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CoursePrerequisites. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CoursePrerequisites.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CoursePrerequisite') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CoursePrerequisites
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCoursePrerequisites <- function(searchConditionsList = NULL, CourseID = F, CoursePrerequisiteID = F, CurriculumCode = F, CurriculumDescription = F, CurriculumID = F, EarnedCredits = F, EntityID = F, HasPrequisiteCurriculums = F, NumericYearCourse = F, NumericYearCurrent = F, PrerequisiteCode = F, PrerequisiteID = F, SchoolYearHigh = F, SchoolYearID = F, SchoolYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CoursePrerequisite", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CoursePrerequisite
	#'
	#' This function returns a dataframe or json object of a CoursePrerequisite
	#' @param CoursePrerequisiteID The ID of the CoursePrerequisite to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CoursePrerequisite. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CoursePrerequisite.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CoursePrerequisite') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CoursePrerequisite
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCoursePrerequisite <- function(CoursePrerequisiteID, CourseID = F, CurriculumCode = F, CurriculumDescription = F, CurriculumID = F, EarnedCredits = F, EntityID = F, HasPrequisiteCurriculums = F, NumericYearCourse = F, NumericYearCurrent = F, PrerequisiteCode = F, PrerequisiteID = F, SchoolYearHigh = F, SchoolYearID = F, SchoolYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CoursePrerequisiteID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CoursePrerequisite", objectId = CoursePrerequisiteID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CoursePrerequisite
	#'
	#' This function deletes a CoursePrerequisite
	#' @param CoursePrerequisiteID The ID of the CoursePrerequisite to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CoursePrerequisiteID of the deleted CoursePrerequisite.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCoursePrerequisite <- function(CoursePrerequisiteID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CoursePrerequisite", objectId = CoursePrerequisiteID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List PrerequisiteCurriculumCourses
	#'
	#' This function returns a dataframe or json object of PrerequisiteCurriculumCourses
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given PrerequisiteCurriculumCourses. Defaults to FALSE for all return fields which, for convenience, returns all fields for the PrerequisiteCurriculumCourses.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('PrerequisiteCurriculumCourse') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of PrerequisiteCurriculumCourses
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listPrerequisiteCurriculumCourses <- function(searchConditionsList = NULL, CourseID = F, CurriculumIDFor = F, CurriculumIDRequired = F, EntityID = F, NumericYear = F, PrerequisiteCurriculumCourseID = F, PrerequisiteCurriculumID = F, PrerequisiteID = F, SchoolYearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "PrerequisiteCurriculumCourse", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a PrerequisiteCurriculumCourse
	#'
	#' This function returns a dataframe or json object of a PrerequisiteCurriculumCourse
	#' @param PrerequisiteCurriculumCourseID The ID of the PrerequisiteCurriculumCourse to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given PrerequisiteCurriculumCourse. Defaults to FALSE for all return fields which, for convenience, returns all fields for the PrerequisiteCurriculumCourse.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('PrerequisiteCurriculumCourse') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of PrerequisiteCurriculumCourse
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getPrerequisiteCurriculumCourse <- function(PrerequisiteCurriculumCourseID, CourseID = F, CurriculumIDFor = F, CurriculumIDRequired = F, EntityID = F, NumericYear = F, PrerequisiteCurriculumID = F, PrerequisiteID = F, SchoolYearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "PrerequisiteCurriculumCourseID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "PrerequisiteCurriculumCourse", objectId = PrerequisiteCurriculumCourseID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a PrerequisiteCurriculumCourse
	#'
	#' This function deletes a PrerequisiteCurriculumCourse
	#' @param PrerequisiteCurriculumCourseID The ID of the PrerequisiteCurriculumCourse to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The PrerequisiteCurriculumCourseID of the deleted PrerequisiteCurriculumCourse.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deletePrerequisiteCurriculumCourse <- function(PrerequisiteCurriculumCourseID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "PrerequisiteCurriculumCourse", objectId = PrerequisiteCurriculumCourseID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityFilterArenaSchedulingSettings
	#'
	#' This function returns a dataframe or json object of AvailabilityFilterArenaSchedulingSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterArenaSchedulingSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterArenaSchedulingSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterArenaSchedulingSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityFilterArenaSchedulingSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityFilterArenaSchedulingSettings <- function(searchConditionsList = NULL, AutoSchedulerCourseSelection = F, AvailabilityFilterArenaSchedulingSettingID = F, AvailabilityFilterCourseStudentID = F, AvailableEndDate = F, AvailableStartDate = F, CreatedTime = F, EnableAutoSchedule = F, EvenlyDistributeSchedulingTeams = F, HideRoom = F, HideStaff = F, LockAfterSubmission = F, ModifiedTime = F, SchedulingEntryEndDateTime = F, SchedulingEntryStartDateTime = F, ShowAllClasses = F, ShowMyAlternateRequests = F, ShowMyRequests = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityFilterArenaSchedulingSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityFilterArenaSchedulingSetting
	#'
	#' This function returns a dataframe or json object of an AvailabilityFilterArenaSchedulingSetting
	#' @param AvailabilityFilterArenaSchedulingSettingID The ID of the AvailabilityFilterArenaSchedulingSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterArenaSchedulingSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterArenaSchedulingSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterArenaSchedulingSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityFilterArenaSchedulingSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityFilterArenaSchedulingSetting <- function(AvailabilityFilterArenaSchedulingSettingID, AutoSchedulerCourseSelection = F, AvailabilityFilterCourseStudentID = F, AvailableEndDate = F, AvailableStartDate = F, CreatedTime = F, EnableAutoSchedule = F, EvenlyDistributeSchedulingTeams = F, HideRoom = F, HideStaff = F, LockAfterSubmission = F, ModifiedTime = F, SchedulingEntryEndDateTime = F, SchedulingEntryStartDateTime = F, ShowAllClasses = F, ShowMyAlternateRequests = F, ShowMyRequests = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityFilterArenaSchedulingSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityFilterArenaSchedulingSetting", objectId = AvailabilityFilterArenaSchedulingSettingID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityFilterArenaSchedulingSetting
	#'
	#' This function deletes an AvailabilityFilterArenaSchedulingSetting
	#' @param AvailabilityFilterArenaSchedulingSettingID The ID of the AvailabilityFilterArenaSchedulingSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityFilterArenaSchedulingSettingID of the deleted AvailabilityFilterArenaSchedulingSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityFilterArenaSchedulingSetting <- function(AvailabilityFilterArenaSchedulingSettingID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityFilterArenaSchedulingSetting", objectId = AvailabilityFilterArenaSchedulingSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityFilterArenaSchedulingSetting
	#'
	#' This function creates an AvailabilityFilterArenaSchedulingSetting
	#' @param fieldNames The field values to give the created AvailabilityFilterArenaSchedulingSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityFilterArenaSchedulingSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityFilterArenaSchedulingSetting <- function(AutoSchedulerCourseSelection = NULL, AvailabilityFilterCourseStudentID = NULL, AvailableEndDate = NULL, AvailableStartDate = NULL, EnableAutoSchedule = NULL, EvenlyDistributeSchedulingTeams = NULL, HideRoom = NULL, HideStaff = NULL, LockAfterSubmission = NULL, SchedulingEntryEndDateTime = NULL, SchedulingEntryStartDateTime = NULL, ShowAllClasses = NULL, ShowMyAlternateRequests = NULL, ShowMyRequests = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityFilterArenaSchedulingSetting", body = list(DataObject = body), searchFields = append("AvailabilityFilterArenaSchedulingSettingID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityFilterArenaSchedulingSetting
	#'
	#' This function modifies an AvailabilityFilterArenaSchedulingSetting
	#' @param fieldNames The field values to give the modified AvailabilityFilterArenaSchedulingSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityFilterArenaSchedulingSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityFilterArenaSchedulingSetting <- function(AvailabilityFilterArenaSchedulingSettingID, AutoSchedulerCourseSelection = NULL, AvailabilityFilterCourseStudentID = NULL, AvailableEndDate = NULL, AvailableStartDate = NULL, EnableAutoSchedule = NULL, EvenlyDistributeSchedulingTeams = NULL, HideRoom = NULL, HideStaff = NULL, LockAfterSubmission = NULL, SchedulingEntryEndDateTime = NULL, SchedulingEntryStartDateTime = NULL, ShowAllClasses = NULL, ShowMyAlternateRequests = NULL, ShowMyRequests = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityFilterArenaSchedulingSetting", objectId = AvailabilityFilterArenaSchedulingSettingID, body = list(DataObject = body), searchFields = append("AvailabilityFilterArenaSchedulingSettingID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AvailabilityFilterCourseRequestSettings
	#'
	#' This function returns a dataframe or json object of AvailabilityFilterCourseRequestSettings
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseRequestSettings. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseRequestSettings.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseRequestSetting') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AvailabilityFilterCourseRequestSettings
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAvailabilityFilterCourseRequestSettings <- function(searchConditionsList = NULL, AvailabilityFilterCourseRequestSettingID = F, AvailabilityFilterCourseStudentID = F, AvailableEndDate = F, AvailableStartDate = F, CreatedTime = F, MaximumAlternateCourseRequests = F, MaximumAlternateCredits = F, MaximumCourseRequests = F, MaximumCredits = F, ModifiedTime = F, RequestEntryEndDateTime = F, RequestEntryStartDateTime = F, UseCourseRequestCountMaximum = F, UseCreditsMaximum = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AvailabilityFilterCourseRequestSetting", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AvailabilityFilterCourseRequestSetting
	#'
	#' This function returns a dataframe or json object of an AvailabilityFilterCourseRequestSetting
	#' @param AvailabilityFilterCourseRequestSettingID The ID of the AvailabilityFilterCourseRequestSetting to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AvailabilityFilterCourseRequestSetting. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AvailabilityFilterCourseRequestSetting.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AvailabilityFilterCourseRequestSetting') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AvailabilityFilterCourseRequestSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAvailabilityFilterCourseRequestSetting <- function(AvailabilityFilterCourseRequestSettingID, AvailabilityFilterCourseStudentID = F, AvailableEndDate = F, AvailableStartDate = F, CreatedTime = F, MaximumAlternateCourseRequests = F, MaximumAlternateCredits = F, MaximumCourseRequests = F, MaximumCredits = F, ModifiedTime = F, RequestEntryEndDateTime = F, RequestEntryStartDateTime = F, UseCourseRequestCountMaximum = F, UseCreditsMaximum = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AvailabilityFilterCourseRequestSettingID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseRequestSetting", objectId = AvailabilityFilterCourseRequestSettingID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AvailabilityFilterCourseRequestSetting
	#'
	#' This function deletes an AvailabilityFilterCourseRequestSetting
	#' @param AvailabilityFilterCourseRequestSettingID The ID of the AvailabilityFilterCourseRequestSetting to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AvailabilityFilterCourseRequestSettingID of the deleted AvailabilityFilterCourseRequestSetting.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAvailabilityFilterCourseRequestSetting <- function(AvailabilityFilterCourseRequestSettingID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseRequestSetting", objectId = AvailabilityFilterCourseRequestSettingID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AvailabilityFilterCourseRequestSetting
	#'
	#' This function creates an AvailabilityFilterCourseRequestSetting
	#' @param fieldNames The field values to give the created AvailabilityFilterCourseRequestSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AvailabilityFilterCourseRequestSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAvailabilityFilterCourseRequestSetting <- function(AvailabilityFilterCourseStudentID = NULL, AvailableEndDate = NULL, AvailableStartDate = NULL, MaximumAlternateCourseRequests = NULL, MaximumAlternateCredits = NULL, MaximumCourseRequests = NULL, MaximumCredits = NULL, RequestEntryEndDateTime = NULL, RequestEntryStartDateTime = NULL, UseCourseRequestCountMaximum = NULL, UseCreditsMaximum = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseRequestSetting", body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseRequestSettingID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AvailabilityFilterCourseRequestSetting
	#'
	#' This function modifies an AvailabilityFilterCourseRequestSetting
	#' @param fieldNames The field values to give the modified AvailabilityFilterCourseRequestSetting. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AvailabilityFilterCourseRequestSetting
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAvailabilityFilterCourseRequestSetting <- function(AvailabilityFilterCourseRequestSettingID, AvailabilityFilterCourseStudentID = NULL, AvailableEndDate = NULL, AvailableStartDate = NULL, MaximumAlternateCourseRequests = NULL, MaximumAlternateCredits = NULL, MaximumCourseRequests = NULL, MaximumCredits = NULL, RequestEntryEndDateTime = NULL, RequestEntryStartDateTime = NULL, UseCourseRequestCountMaximum = NULL, UseCreditsMaximum = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AvailabilityFilterCourseRequestSetting", objectId = AvailabilityFilterCourseRequestSettingID, body = list(DataObject = body), searchFields = append("AvailabilityFilterCourseRequestSettingID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MassUpdateSystemDateRunHistoryDetails
	#'
	#' This function returns a dataframe or json object of MassUpdateSystemDateRunHistoryDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateSystemDateRunHistoryDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateSystemDateRunHistoryDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateSystemDateRunHistoryDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MassUpdateSystemDateRunHistoryDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMassUpdateSystemDateRunHistoryDetails <- function(searchConditionsList = NULL, CreatedTime = F, EndDateFailedCount = F, EndDateRecordsProcessedCount = F, EndDateSuccessCount = F, MassUpdateSystemDateRunHistoryDetailID = F, MassUpdateSystemDateRunHistoryID = F, ModifiedTime = F, SortNumber = F, StartDateFailedCount = F, StartDateRecordsProcessedCount = F, StartDateSuccessCount = F, TableType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MassUpdateSystemDateRunHistoryDetail
	#'
	#' This function returns a dataframe or json object of a MassUpdateSystemDateRunHistoryDetail
	#' @param MassUpdateSystemDateRunHistoryDetailID The ID of the MassUpdateSystemDateRunHistoryDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateSystemDateRunHistoryDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateSystemDateRunHistoryDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateSystemDateRunHistoryDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MassUpdateSystemDateRunHistoryDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMassUpdateSystemDateRunHistoryDetail <- function(MassUpdateSystemDateRunHistoryDetailID, CreatedTime = F, EndDateFailedCount = F, EndDateRecordsProcessedCount = F, EndDateSuccessCount = F, MassUpdateSystemDateRunHistoryID = F, ModifiedTime = F, SortNumber = F, StartDateFailedCount = F, StartDateRecordsProcessedCount = F, StartDateSuccessCount = F, TableType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MassUpdateSystemDateRunHistoryDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryDetail", objectId = MassUpdateSystemDateRunHistoryDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MassUpdateSystemDateRunHistoryDetail
	#'
	#' This function deletes a MassUpdateSystemDateRunHistoryDetail
	#' @param MassUpdateSystemDateRunHistoryDetailID The ID of the MassUpdateSystemDateRunHistoryDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MassUpdateSystemDateRunHistoryDetailID of the deleted MassUpdateSystemDateRunHistoryDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMassUpdateSystemDateRunHistoryDetail <- function(MassUpdateSystemDateRunHistoryDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryDetail", objectId = MassUpdateSystemDateRunHistoryDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MassUpdateSystemDateRunHistoryDetail
	#'
	#' This function creates a MassUpdateSystemDateRunHistoryDetail
	#' @param fieldNames The field values to give the created MassUpdateSystemDateRunHistoryDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MassUpdateSystemDateRunHistoryDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMassUpdateSystemDateRunHistoryDetail <- function(EndDateFailedCount = NULL, EndDateRecordsProcessedCount = NULL, EndDateSuccessCount = NULL, MassUpdateSystemDateRunHistoryID = NULL, SortNumber = NULL, StartDateFailedCount = NULL, StartDateRecordsProcessedCount = NULL, StartDateSuccessCount = NULL, TableType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryDetail", body = list(DataObject = body), searchFields = append("MassUpdateSystemDateRunHistoryDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MassUpdateSystemDateRunHistoryDetail
	#'
	#' This function modifies a MassUpdateSystemDateRunHistoryDetail
	#' @param fieldNames The field values to give the modified MassUpdateSystemDateRunHistoryDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MassUpdateSystemDateRunHistoryDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMassUpdateSystemDateRunHistoryDetail <- function(MassUpdateSystemDateRunHistoryDetailID, EndDateFailedCount = NULL, EndDateRecordsProcessedCount = NULL, EndDateSuccessCount = NULL, MassUpdateSystemDateRunHistoryID = NULL, SortNumber = NULL, StartDateFailedCount = NULL, StartDateRecordsProcessedCount = NULL, StartDateSuccessCount = NULL, TableType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryDetail", objectId = MassUpdateSystemDateRunHistoryDetailID, body = list(DataObject = body), searchFields = append("MassUpdateSystemDateRunHistoryDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MassUpdateSystemDateRunHistoryErrors
	#'
	#' This function returns a dataframe or json object of MassUpdateSystemDateRunHistoryErrors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateSystemDateRunHistoryErrors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateSystemDateRunHistoryErrors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateSystemDateRunHistoryError') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MassUpdateSystemDateRunHistoryErrors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMassUpdateSystemDateRunHistoryErrors <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, ErrorDetail = F, MassUpdateSystemDateRunHistoryErrorID = F, MassUpdateSystemDateRunHistoryID = F, ModifiedTime = F, ObjectID = F, ParentObjectID = F, TableType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryError", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MassUpdateSystemDateRunHistoryError
	#'
	#' This function returns a dataframe or json object of a MassUpdateSystemDateRunHistoryError
	#' @param MassUpdateSystemDateRunHistoryErrorID The ID of the MassUpdateSystemDateRunHistoryError to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateSystemDateRunHistoryError. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateSystemDateRunHistoryError.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateSystemDateRunHistoryError') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MassUpdateSystemDateRunHistoryError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMassUpdateSystemDateRunHistoryError <- function(MassUpdateSystemDateRunHistoryErrorID, CreatedTime = F, Description = F, ErrorDetail = F, MassUpdateSystemDateRunHistoryID = F, ModifiedTime = F, ObjectID = F, ParentObjectID = F, TableType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MassUpdateSystemDateRunHistoryErrorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryError", objectId = MassUpdateSystemDateRunHistoryErrorID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MassUpdateSystemDateRunHistoryError
	#'
	#' This function deletes a MassUpdateSystemDateRunHistoryError
	#' @param MassUpdateSystemDateRunHistoryErrorID The ID of the MassUpdateSystemDateRunHistoryError to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MassUpdateSystemDateRunHistoryErrorID of the deleted MassUpdateSystemDateRunHistoryError.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMassUpdateSystemDateRunHistoryError <- function(MassUpdateSystemDateRunHistoryErrorID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryError", objectId = MassUpdateSystemDateRunHistoryErrorID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MassUpdateSystemDateRunHistoryError
	#'
	#' This function creates a MassUpdateSystemDateRunHistoryError
	#' @param fieldNames The field values to give the created MassUpdateSystemDateRunHistoryError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MassUpdateSystemDateRunHistoryError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMassUpdateSystemDateRunHistoryError <- function(Description = NULL, ErrorDetail = NULL, MassUpdateSystemDateRunHistoryID = NULL, ObjectID = NULL, ParentObjectID = NULL, TableType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryError", body = list(DataObject = body), searchFields = append("MassUpdateSystemDateRunHistoryErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MassUpdateSystemDateRunHistoryError
	#'
	#' This function modifies a MassUpdateSystemDateRunHistoryError
	#' @param fieldNames The field values to give the modified MassUpdateSystemDateRunHistoryError. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MassUpdateSystemDateRunHistoryError
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMassUpdateSystemDateRunHistoryError <- function(MassUpdateSystemDateRunHistoryErrorID, Description = NULL, ErrorDetail = NULL, MassUpdateSystemDateRunHistoryID = NULL, ObjectID = NULL, ParentObjectID = NULL, TableType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistoryError", objectId = MassUpdateSystemDateRunHistoryErrorID, body = list(DataObject = body), searchFields = append("MassUpdateSystemDateRunHistoryErrorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List MassUpdateSystemDateRunHistories
	#'
	#' This function returns a dataframe or json object of MassUpdateSystemDateRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateSystemDateRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateSystemDateRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateSystemDateRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of MassUpdateSystemDateRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listMassUpdateSystemDateRunHistories <- function(searchConditionsList = NULL, CreatedTime = F, DateTemplateXML = F, Duration = F, EndTime = F, EntityID = F, ErrorCount = F, HasErrors = F, Location = F, MassUpdateSystemDateRunHistoryID = F, ModifiedTime = F, RunDescription = F, RunReason = F, SchoolYearID = F, SourceID = F, StartTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a MassUpdateSystemDateRunHistory
	#'
	#' This function returns a dataframe or json object of a MassUpdateSystemDateRunHistory
	#' @param MassUpdateSystemDateRunHistoryID The ID of the MassUpdateSystemDateRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given MassUpdateSystemDateRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the MassUpdateSystemDateRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('MassUpdateSystemDateRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of MassUpdateSystemDateRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getMassUpdateSystemDateRunHistory <- function(MassUpdateSystemDateRunHistoryID, CreatedTime = F, DateTemplateXML = F, Duration = F, EndTime = F, EntityID = F, ErrorCount = F, HasErrors = F, Location = F, ModifiedTime = F, RunDescription = F, RunReason = F, SchoolYearID = F, SourceID = F, StartTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "MassUpdateSystemDateRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistory", objectId = MassUpdateSystemDateRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a MassUpdateSystemDateRunHistory
	#'
	#' This function deletes a MassUpdateSystemDateRunHistory
	#' @param MassUpdateSystemDateRunHistoryID The ID of the MassUpdateSystemDateRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The MassUpdateSystemDateRunHistoryID of the deleted MassUpdateSystemDateRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteMassUpdateSystemDateRunHistory <- function(MassUpdateSystemDateRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistory", objectId = MassUpdateSystemDateRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a MassUpdateSystemDateRunHistory
	#'
	#' This function creates a MassUpdateSystemDateRunHistory
	#' @param fieldNames The field values to give the created MassUpdateSystemDateRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created MassUpdateSystemDateRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createMassUpdateSystemDateRunHistory <- function(DateTemplateXML = NULL, EndTime = NULL, EntityID = NULL, Location = NULL, RunDescription = NULL, RunReason = NULL, SchoolYearID = NULL, SourceID = NULL, StartTime = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistory", body = list(DataObject = body), searchFields = append("MassUpdateSystemDateRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a MassUpdateSystemDateRunHistory
	#'
	#' This function modifies a MassUpdateSystemDateRunHistory
	#' @param fieldNames The field values to give the modified MassUpdateSystemDateRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified MassUpdateSystemDateRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyMassUpdateSystemDateRunHistory <- function(MassUpdateSystemDateRunHistoryID, DateTemplateXML = NULL, EndTime = NULL, EntityID = NULL, Location = NULL, RunDescription = NULL, RunReason = NULL, SchoolYearID = NULL, SourceID = NULL, StartTime = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "MassUpdateSystemDateRunHistory", objectId = MassUpdateSystemDateRunHistoryID, body = list(DataObject = body), searchFields = append("MassUpdateSystemDateRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List TempMasterDateChangeDetails
	#'
	#' This function returns a dataframe or json object of TempMasterDateChangeDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMasterDateChangeDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMasterDateChangeDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMasterDateChangeDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of TempMasterDateChangeDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listTempMasterDateChangeDetails <- function(searchConditionsList = NULL, CreatedTime = F, CurrentDate = F, DateDescriptor = F, DisplayLowHighDates = F, HighDate = F, LowDate = F, ModifiedTime = F, NewDate = F, TempMasterDateChangeDetailID = F, UsedBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "TempMasterDateChangeDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a TempMasterDateChangeDetail
	#'
	#' This function returns a dataframe or json object of a TempMasterDateChangeDetail
	#' @param TempMasterDateChangeDetailID The ID of the TempMasterDateChangeDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given TempMasterDateChangeDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the TempMasterDateChangeDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('TempMasterDateChangeDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of TempMasterDateChangeDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getTempMasterDateChangeDetail <- function(TempMasterDateChangeDetailID, CreatedTime = F, CurrentDate = F, DateDescriptor = F, DisplayLowHighDates = F, HighDate = F, LowDate = F, ModifiedTime = F, NewDate = F, UsedBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "TempMasterDateChangeDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "TempMasterDateChangeDetail", objectId = TempMasterDateChangeDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a TempMasterDateChangeDetail
	#'
	#' This function deletes a TempMasterDateChangeDetail
	#' @param TempMasterDateChangeDetailID The ID of the TempMasterDateChangeDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The TempMasterDateChangeDetailID of the deleted TempMasterDateChangeDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteTempMasterDateChangeDetail <- function(TempMasterDateChangeDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "TempMasterDateChangeDetail", objectId = TempMasterDateChangeDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a TempMasterDateChangeDetail
	#'
	#' This function creates a TempMasterDateChangeDetail
	#' @param fieldNames The field values to give the created TempMasterDateChangeDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created TempMasterDateChangeDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createTempMasterDateChangeDetail <- function(CurrentDate = NULL, DateDescriptor = NULL, HighDate = NULL, LowDate = NULL, NewDate = NULL, UsedBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "TempMasterDateChangeDetail", body = list(DataObject = body), searchFields = append("TempMasterDateChangeDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a TempMasterDateChangeDetail
	#'
	#' This function modifies a TempMasterDateChangeDetail
	#' @param fieldNames The field values to give the modified TempMasterDateChangeDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified TempMasterDateChangeDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyTempMasterDateChangeDetail <- function(TempMasterDateChangeDetailID, CurrentDate = NULL, DateDescriptor = NULL, HighDate = NULL, LowDate = NULL, NewDate = NULL, UsedBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "TempMasterDateChangeDetail", objectId = TempMasterDateChangeDetailID, body = list(DataObject = body), searchFields = append("TempMasterDateChangeDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseGradeReferenceSummaries
	#'
	#' This function returns a dataframe or json object of CourseGradeReferenceSummaries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradeReferenceSummaries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradeReferenceSummaries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradeReferenceSummary') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseGradeReferenceSummaries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseGradeReferenceSummaries <- function(searchConditionsList = NULL, CourseID = F, GradeLevelIDSummary = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseGradeReferenceSummary", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseGradeReferenceSummary
	#'
	#' This function returns a dataframe or json object of a CourseGradeReferenceSummary
	#' @param CourseGradeReferenceSummaryID The ID of the CourseGradeReferenceSummary to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseGradeReferenceSummary. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseGradeReferenceSummary.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseGradeReferenceSummary') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseGradeReferenceSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseGradeReferenceSummary <- function(CourseGradeReferenceSummaryID, CourseID = F, GradeLevelIDSummary = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseGradeReferenceSummaryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseGradeReferenceSummary", objectId = CourseGradeReferenceSummaryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseGradeReferenceSummary
	#'
	#' This function deletes a CourseGradeReferenceSummary
	#' @param CourseGradeReferenceSummaryID The ID of the CourseGradeReferenceSummary to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseGradeReferenceSummaryID of the deleted CourseGradeReferenceSummary.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseGradeReferenceSummary <- function(CourseGradeReferenceSummaryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseGradeReferenceSummary", objectId = CourseGradeReferenceSummaryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CourseCorequisites
	#'
	#' This function returns a dataframe or json object of CourseCorequisites
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCorequisites. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCorequisites.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCorequisite') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of CourseCorequisites
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCourseCorequisites <- function(searchConditionsList = NULL, AutomaticRequestSetting = F, CourseCorequisiteGroupID = F, CourseCorequisiteID = F, CourseIDMember = F, CourseIDParent = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "CourseCorequisite", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CourseCorequisite
	#'
	#' This function returns a dataframe or json object of a CourseCorequisite
	#' @param CourseCorequisiteID The ID of the CourseCorequisite to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CourseCorequisite. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CourseCorequisite.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CourseCorequisite') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of CourseCorequisite
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCourseCorequisite <- function(CourseCorequisiteID, AutomaticRequestSetting = F, CourseCorequisiteGroupID = F, CourseIDMember = F, CourseIDParent = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CourseCorequisiteID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "CourseCorequisite", objectId = CourseCorequisiteID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CourseCorequisite
	#'
	#' This function deletes a CourseCorequisite
	#' @param CourseCorequisiteID The ID of the CourseCorequisite to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The CourseCorequisiteID of the deleted CourseCorequisite.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCourseCorequisite <- function(CourseCorequisiteID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "CourseCorequisite", objectId = CourseCorequisiteID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AppleSchoolManagerConfigFileDestinations
	#'
	#' This function returns a dataframe or json object of AppleSchoolManagerConfigFileDestinations
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AppleSchoolManagerConfigFileDestinations. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AppleSchoolManagerConfigFileDestinations.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AppleSchoolManagerConfigFileDestination') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A list of AppleSchoolManagerConfigFileDestinations
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAppleSchoolManagerConfigFileDestinations <- function(searchConditionsList = NULL, AppleSchoolManagerConfigFileDestinationID = F, AppleSchoolManagerConfigID = F, CreatedTime = F, FileDestinationID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "Scheduling", objectName = "AppleSchoolManagerConfigFileDestination", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AppleSchoolManagerConfigFileDestination
	#'
	#' This function returns a dataframe or json object of an AppleSchoolManagerConfigFileDestination
	#' @param AppleSchoolManagerConfigFileDestinationID The ID of the AppleSchoolManagerConfigFileDestination to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AppleSchoolManagerConfigFileDestination. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AppleSchoolManagerConfigFileDestination.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AppleSchoolManagerConfigFileDestination') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A dataframe or of AppleSchoolManagerConfigFileDestination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAppleSchoolManagerConfigFileDestination <- function(AppleSchoolManagerConfigFileDestinationID, AppleSchoolManagerConfigID = F, CreatedTime = F, FileDestinationID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AppleSchoolManagerConfigFileDestinationID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfigFileDestination", objectId = AppleSchoolManagerConfigFileDestinationID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AppleSchoolManagerConfigFileDestination
	#'
	#' This function deletes an AppleSchoolManagerConfigFileDestination
	#' @param AppleSchoolManagerConfigFileDestinationID The ID of the AppleSchoolManagerConfigFileDestination to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The AppleSchoolManagerConfigFileDestinationID of the deleted AppleSchoolManagerConfigFileDestination.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAppleSchoolManagerConfigFileDestination <- function(AppleSchoolManagerConfigFileDestinationID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfigFileDestination", objectId = AppleSchoolManagerConfigFileDestinationID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AppleSchoolManagerConfigFileDestination
	#'
	#' This function creates an AppleSchoolManagerConfigFileDestination
	#' @param fieldNames The field values to give the created AppleSchoolManagerConfigFileDestination. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return A newly created AppleSchoolManagerConfigFileDestination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAppleSchoolManagerConfigFileDestination <- function(AppleSchoolManagerConfigID = NULL, FileDestinationID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfigFileDestination", body = list(DataObject = body), searchFields = append("AppleSchoolManagerConfigFileDestinationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AppleSchoolManagerConfigFileDestination
	#'
	#' This function modifies an AppleSchoolManagerConfigFileDestination
	#' @param fieldNames The field values to give the modified AppleSchoolManagerConfigFileDestination. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept Scheduling
	#' @return The modified AppleSchoolManagerConfigFileDestination
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAppleSchoolManagerConfigFileDestination <- function(AppleSchoolManagerConfigFileDestinationID, AppleSchoolManagerConfigID = NULL, FileDestinationID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "Scheduling", objectName = "AppleSchoolManagerConfigFileDestination", objectId = AppleSchoolManagerConfigFileDestinationID, body = list(DataObject = body), searchFields = append("AppleSchoolManagerConfigFileDestinationID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}
