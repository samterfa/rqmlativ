
	#' List CivilRightsDataCollectionExceptions
	#'
	#' This function returns a dataframe or json object of CivilRightsDataCollectionExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CivilRightsDataCollectionExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CivilRightsDataCollectionExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CivilRightsDataCollectionException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CivilRightsDataCollectionExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCivilRightsDataCollectionExceptions <- function(searchConditionsList = NULL, CivilRightsDataCollectionExceptionID = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, SchoolID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CivilRightsDataCollectionException
	#'
	#' This function returns a dataframe or json object of a CivilRightsDataCollectionException
	#' @param CivilRightsDataCollectionExceptionID The ID of the CivilRightsDataCollectionException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CivilRightsDataCollectionException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CivilRightsDataCollectionException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CivilRightsDataCollectionException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CivilRightsDataCollectionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCivilRightsDataCollectionException <- function(CivilRightsDataCollectionExceptionID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, SchoolID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CivilRightsDataCollectionExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionException", objectId = CivilRightsDataCollectionExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CivilRightsDataCollectionException
	#'
	#' This function deletes a CivilRightsDataCollectionException
	#' @param CivilRightsDataCollectionExceptionID The ID of the CivilRightsDataCollectionException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CivilRightsDataCollectionExceptionID of the deleted CivilRightsDataCollectionException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCivilRightsDataCollectionException <- function(CivilRightsDataCollectionExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionException", objectId = CivilRightsDataCollectionExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CivilRightsDataCollectionException
	#'
	#' This function creates a CivilRightsDataCollectionException
	#' @param fieldNames The field values to give the created CivilRightsDataCollectionException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CivilRightsDataCollectionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCivilRightsDataCollectionException <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, Message = NULL, MessageType = NULL, SchoolID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionException", body = list(DataObject = body), searchFields = append("CivilRightsDataCollectionExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CivilRightsDataCollectionException
	#'
	#' This function modifies a CivilRightsDataCollectionException
	#' @param fieldNames The field values to give the modified CivilRightsDataCollectionException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CivilRightsDataCollectionException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCivilRightsDataCollectionException <- function(CivilRightsDataCollectionExceptionID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, Message = NULL, MessageType = NULL, SchoolID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionException", objectId = CivilRightsDataCollectionExceptionID, body = list(DataObject = body), searchFields = append("CivilRightsDataCollectionExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalOffenseCategories
	#'
	#' This function returns a dataframe or json object of FederalOffenseCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalOffenseCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalOffenseCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalOffenseCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalOffenseCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalOffenseCategories <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, FederalOffenseCategoryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalOffenseCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalOffenseCategory
	#'
	#' This function returns a dataframe or json object of a FederalOffenseCategory
	#' @param FederalOffenseCategoryID The ID of the FederalOffenseCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalOffenseCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalOffenseCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalOffenseCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalOffenseCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalOffenseCategory <- function(FederalOffenseCategoryID, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalOffenseCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalOffenseCategory", objectId = FederalOffenseCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalOffenseCategory
	#'
	#' This function deletes a FederalOffenseCategory
	#' @param FederalOffenseCategoryID The ID of the FederalOffenseCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalOffenseCategoryID of the deleted FederalOffenseCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalOffenseCategory <- function(FederalOffenseCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalOffenseCategory", objectId = FederalOffenseCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalOffenseCategory
	#'
	#' This function creates a FederalOffenseCategory
	#' @param fieldNames The field values to give the created FederalOffenseCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalOffenseCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalOffenseCategory <- function(Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalOffenseCategory", body = list(DataObject = body), searchFields = append("FederalOffenseCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalOffenseCategory
	#'
	#' This function modifies a FederalOffenseCategory
	#' @param fieldNames The field values to give the modified FederalOffenseCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalOffenseCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalOffenseCategory <- function(FederalOffenseCategoryID, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalOffenseCategory", objectId = FederalOffenseCategoryID, body = list(DataObject = body), searchFields = append("FederalOffenseCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalDisciplineCategories
	#'
	#' This function returns a dataframe or json object of FederalDisciplineCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalDisciplineCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalDisciplineCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalDisciplineCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalDisciplineCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalDisciplineCategories <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, FederalDisciplineCategoryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalDisciplineCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalDisciplineCategory
	#'
	#' This function returns a dataframe or json object of a FederalDisciplineCategory
	#' @param FederalDisciplineCategoryID The ID of the FederalDisciplineCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalDisciplineCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalDisciplineCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalDisciplineCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalDisciplineCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalDisciplineCategory <- function(FederalDisciplineCategoryID, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalDisciplineCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalDisciplineCategory", objectId = FederalDisciplineCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalDisciplineCategory
	#'
	#' This function deletes a FederalDisciplineCategory
	#' @param FederalDisciplineCategoryID The ID of the FederalDisciplineCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalDisciplineCategoryID of the deleted FederalDisciplineCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalDisciplineCategory <- function(FederalDisciplineCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalDisciplineCategory", objectId = FederalDisciplineCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalDisciplineCategory
	#'
	#' This function creates a FederalDisciplineCategory
	#' @param fieldNames The field values to give the created FederalDisciplineCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalDisciplineCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalDisciplineCategory <- function(Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalDisciplineCategory", body = list(DataObject = body), searchFields = append("FederalDisciplineCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalDisciplineCategory
	#'
	#' This function modifies a FederalDisciplineCategory
	#' @param fieldNames The field values to give the modified FederalDisciplineCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalDisciplineCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalDisciplineCategory <- function(FederalDisciplineCategoryID, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalDisciplineCategory", objectId = FederalDisciplineCategoryID, body = list(DataObject = body), searchFields = append("FederalDisciplineCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalAdvancedPlacementCourseTypes
	#'
	#' This function returns a dataframe or json object of FederalAdvancedPlacementCourseTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalAdvancedPlacementCourseTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalAdvancedPlacementCourseTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalAdvancedPlacementCourseType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalAdvancedPlacementCourseTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalAdvancedPlacementCourseTypes <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, FederalAdvancedPlacementCourseTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalAdvancedPlacementCourseType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalAdvancedPlacementCourseType
	#'
	#' This function returns a dataframe or json object of a FederalAdvancedPlacementCourseType
	#' @param FederalAdvancedPlacementCourseTypeID The ID of the FederalAdvancedPlacementCourseType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalAdvancedPlacementCourseType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalAdvancedPlacementCourseType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalAdvancedPlacementCourseType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalAdvancedPlacementCourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalAdvancedPlacementCourseType <- function(FederalAdvancedPlacementCourseTypeID, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalAdvancedPlacementCourseTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalAdvancedPlacementCourseType", objectId = FederalAdvancedPlacementCourseTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalAdvancedPlacementCourseType
	#'
	#' This function deletes a FederalAdvancedPlacementCourseType
	#' @param FederalAdvancedPlacementCourseTypeID The ID of the FederalAdvancedPlacementCourseType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalAdvancedPlacementCourseTypeID of the deleted FederalAdvancedPlacementCourseType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalAdvancedPlacementCourseType <- function(FederalAdvancedPlacementCourseTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalAdvancedPlacementCourseType", objectId = FederalAdvancedPlacementCourseTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalAdvancedPlacementCourseType
	#'
	#' This function creates a FederalAdvancedPlacementCourseType
	#' @param fieldNames The field values to give the created FederalAdvancedPlacementCourseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalAdvancedPlacementCourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalAdvancedPlacementCourseType <- function(Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalAdvancedPlacementCourseType", body = list(DataObject = body), searchFields = append("FederalAdvancedPlacementCourseTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalAdvancedPlacementCourseType
	#'
	#' This function modifies a FederalAdvancedPlacementCourseType
	#' @param fieldNames The field values to give the modified FederalAdvancedPlacementCourseType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalAdvancedPlacementCourseType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalAdvancedPlacementCourseType <- function(FederalAdvancedPlacementCourseTypeID, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalAdvancedPlacementCourseType", objectId = FederalAdvancedPlacementCourseTypeID, body = list(DataObject = body), searchFields = append("FederalAdvancedPlacementCourseTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalSubjectTypes
	#'
	#' This function returns a dataframe or json object of FederalSubjectTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalSubjectTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalSubjectTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalSubjectType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalSubjectTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalSubjectTypes <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, FederalSubjectTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalSubjectType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalSubjectType
	#'
	#' This function returns a dataframe or json object of a FederalSubjectType
	#' @param FederalSubjectTypeID The ID of the FederalSubjectType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalSubjectType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalSubjectType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalSubjectType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalSubjectType <- function(FederalSubjectTypeID, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalSubjectTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalSubjectType", objectId = FederalSubjectTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalSubjectType
	#'
	#' This function deletes a FederalSubjectType
	#' @param FederalSubjectTypeID The ID of the FederalSubjectType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalSubjectTypeID of the deleted FederalSubjectType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalSubjectType <- function(FederalSubjectTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalSubjectType", objectId = FederalSubjectTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalSubjectType
	#'
	#' This function creates a FederalSubjectType
	#' @param fieldNames The field values to give the created FederalSubjectType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalSubjectType <- function(Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalSubjectType", body = list(DataObject = body), searchFields = append("FederalSubjectTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalSubjectType
	#'
	#' This function modifies a FederalSubjectType
	#' @param fieldNames The field values to give the modified FederalSubjectType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalSubjectType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalSubjectType <- function(FederalSubjectTypeID, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalSubjectType", objectId = FederalSubjectTypeID, body = list(DataObject = body), searchFields = append("FederalSubjectTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearExceptions
	#'
	#' This function returns a dataframe or json object of ACAYearExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearExceptions <- function(searchConditionsList = NULL, ACAYearEmployeeV1ID = F, ACAYearExceptionID = F, ACAYearID = F, CreatedTime = F, Description = F, EmployeeID = F, ExceptionType = F, IsFatalException = F, ModifiedTime = F, SourceType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearException
	#'
	#' This function returns a dataframe or json object of an ACAYearException
	#' @param ACAYearExceptionID The ID of the ACAYearException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearException <- function(ACAYearExceptionID, ACAYearEmployeeV1ID = F, ACAYearID = F, CreatedTime = F, Description = F, EmployeeID = F, ExceptionType = F, IsFatalException = F, ModifiedTime = F, SourceType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearException", objectId = ACAYearExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearException
	#'
	#' This function deletes an ACAYearException
	#' @param ACAYearExceptionID The ID of the ACAYearException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearExceptionID of the deleted ACAYearException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearException <- function(ACAYearExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearException", objectId = ACAYearExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearException
	#'
	#' This function creates an ACAYearException
	#' @param fieldNames The field values to give the created ACAYearException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearException <- function(ACAYearEmployeeV1ID = NULL, ACAYearID = NULL, Description = NULL, EmployeeID = NULL, ExceptionType = NULL, IsFatalException = NULL, SourceType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearException", body = list(DataObject = body), searchFields = append("ACAYearExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearException
	#'
	#' This function modifies an ACAYearException
	#' @param fieldNames The field values to give the modified ACAYearException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearException <- function(ACAYearExceptionID, ACAYearEmployeeV1ID = NULL, ACAYearID = NULL, Description = NULL, EmployeeID = NULL, ExceptionType = NULL, IsFatalException = NULL, SourceType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearException", objectId = ACAYearExceptionID, body = list(DataObject = body), searchFields = append("ACAYearExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CivilRightsDataCollections
	#'
	#' This function returns a dataframe or json object of CivilRightsDataCollections
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CivilRightsDataCollections. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CivilRightsDataCollections.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CivilRightsDataCollection') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CivilRightsDataCollections
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCivilRightsDataCollections <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CreatedTime = F, DistrictID = F, HasLEAFile = F, HasSchoolFile = F, ModifiedTime = F, NumericYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CivilRightsDataCollection", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CivilRightsDataCollection
	#'
	#' This function returns a dataframe or json object of a CivilRightsDataCollection
	#' @param CivilRightsDataCollectionID The ID of the CivilRightsDataCollection to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CivilRightsDataCollection. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CivilRightsDataCollection.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CivilRightsDataCollection') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CivilRightsDataCollection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCivilRightsDataCollection <- function(CivilRightsDataCollectionID, CreatedTime = F, DistrictID = F, HasLEAFile = F, HasSchoolFile = F, ModifiedTime = F, NumericYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CivilRightsDataCollectionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollection", objectId = CivilRightsDataCollectionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CivilRightsDataCollection
	#'
	#' This function deletes a CivilRightsDataCollection
	#' @param CivilRightsDataCollectionID The ID of the CivilRightsDataCollection to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CivilRightsDataCollectionID of the deleted CivilRightsDataCollection.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCivilRightsDataCollection <- function(CivilRightsDataCollectionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollection", objectId = CivilRightsDataCollectionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CivilRightsDataCollection
	#'
	#' This function creates a CivilRightsDataCollection
	#' @param fieldNames The field values to give the created CivilRightsDataCollection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CivilRightsDataCollection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCivilRightsDataCollection <- function(DistrictID = NULL, NumericYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollection", body = list(DataObject = body), searchFields = append("CivilRightsDataCollectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CivilRightsDataCollection
	#'
	#' This function modifies a CivilRightsDataCollection
	#' @param fieldNames The field values to give the modified CivilRightsDataCollection. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CivilRightsDataCollection
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCivilRightsDataCollection <- function(CivilRightsDataCollectionID, DistrictID = NULL, NumericYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollection", objectId = CivilRightsDataCollectionID, body = list(DataObject = body), searchFields = append("CivilRightsDataCollectionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CivilRightsDataCollectionRunHistories
	#'
	#' This function returns a dataframe or json object of CivilRightsDataCollectionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CivilRightsDataCollectionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CivilRightsDataCollectionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CivilRightsDataCollectionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CivilRightsDataCollectionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCivilRightsDataCollectionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CollectionType = F, CreatedTime = F, EndDateTime = F, HasMediaID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CivilRightsDataCollectionRunHistory
	#'
	#' This function returns a dataframe or json object of a CivilRightsDataCollectionRunHistory
	#' @param CivilRightsDataCollectionRunHistoryID The ID of the CivilRightsDataCollectionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CivilRightsDataCollectionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CivilRightsDataCollectionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CivilRightsDataCollectionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CivilRightsDataCollectionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCivilRightsDataCollectionRunHistory <- function(CivilRightsDataCollectionRunHistoryID, CanAccessMedia = F, CivilRightsDataCollectionID = F, CollectionType = F, CreatedTime = F, EndDateTime = F, HasMediaID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CivilRightsDataCollectionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionRunHistory", objectId = CivilRightsDataCollectionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CivilRightsDataCollectionRunHistory
	#'
	#' This function deletes a CivilRightsDataCollectionRunHistory
	#' @param CivilRightsDataCollectionRunHistoryID The ID of the CivilRightsDataCollectionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CivilRightsDataCollectionRunHistoryID of the deleted CivilRightsDataCollectionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCivilRightsDataCollectionRunHistory <- function(CivilRightsDataCollectionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionRunHistory", objectId = CivilRightsDataCollectionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CivilRightsDataCollectionRunHistory
	#'
	#' This function creates a CivilRightsDataCollectionRunHistory
	#' @param fieldNames The field values to give the created CivilRightsDataCollectionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CivilRightsDataCollectionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCivilRightsDataCollectionRunHistory <- function(CivilRightsDataCollectionID = NULL, CollectionType = NULL, EndDateTime = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionRunHistory", body = list(DataObject = body), searchFields = append("CivilRightsDataCollectionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CivilRightsDataCollectionRunHistory
	#'
	#' This function modifies a CivilRightsDataCollectionRunHistory
	#' @param fieldNames The field values to give the modified CivilRightsDataCollectionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CivilRightsDataCollectionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCivilRightsDataCollectionRunHistory <- function(CivilRightsDataCollectionRunHistoryID, CivilRightsDataCollectionID = NULL, CollectionType = NULL, EndDateTime = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CivilRightsDataCollectionRunHistory", objectId = CivilRightsDataCollectionRunHistoryID, body = list(DataObject = body), searchFields = append("CivilRightsDataCollectionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEACoordinatorV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEACoordinatorV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEACoordinatorV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEACoordinatorV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEACoordinatorV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEACoordinatorV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEACoordinatorV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEACoordinatorV1ID = F, CreatedTime = F, DisabilityEmail = F, DisabilityFirstName = F, DisabilityLastName = F, DisabilityPhone = F, DistrictID = F, DistrictSchoolYearID = F, FormattedDisabilityPhoneNumber = F, FormattedRacePhoneNumber = F, FormattedSexPhoneNumber = F, HasDisability = F, HasGender = F, HasRace = F, ModifiedTime = F, NameIDDisability = F, NameIDRace = F, NameIDSex = F, RaceEmail = F, RaceFirstName = F, RaceLastName = F, RacePhone = F, SexEmail = F, SexFirstName = F, SexLastName = F, SexPhone = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEACoordinatorV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEACoordinatorV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEACoordinatorV1
	#' @param CRDCLEACoordinatorV1ID The ID of the CRDCLEACoordinatorV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEACoordinatorV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEACoordinatorV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEACoordinatorV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEACoordinatorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEACoordinatorV1 <- function(CRDCLEACoordinatorV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DisabilityEmail = F, DisabilityFirstName = F, DisabilityLastName = F, DisabilityPhone = F, DistrictID = F, DistrictSchoolYearID = F, FormattedDisabilityPhoneNumber = F, FormattedRacePhoneNumber = F, FormattedSexPhoneNumber = F, HasDisability = F, HasGender = F, HasRace = F, ModifiedTime = F, NameIDDisability = F, NameIDRace = F, NameIDSex = F, RaceEmail = F, RaceFirstName = F, RaceLastName = F, RacePhone = F, SexEmail = F, SexFirstName = F, SexLastName = F, SexPhone = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEACoordinatorV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACoordinatorV1", objectId = CRDCLEACoordinatorV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEACoordinatorV1
	#'
	#' This function deletes a CRDCLEACoordinatorV1
	#' @param CRDCLEACoordinatorV1ID The ID of the CRDCLEACoordinatorV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEACoordinatorV1ID of the deleted CRDCLEACoordinatorV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEACoordinatorV1 <- function(CRDCLEACoordinatorV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACoordinatorV1", objectId = CRDCLEACoordinatorV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEACoordinatorV1
	#'
	#' This function creates a CRDCLEACoordinatorV1
	#' @param fieldNames The field values to give the created CRDCLEACoordinatorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEACoordinatorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEACoordinatorV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityEmail = NULL, DisabilityFirstName = NULL, DisabilityLastName = NULL, DisabilityPhone = NULL, DistrictID = NULL, DistrictSchoolYearID = NULL, HasDisability = NULL, HasGender = NULL, HasRace = NULL, NameIDDisability = NULL, NameIDRace = NULL, NameIDSex = NULL, RaceEmail = NULL, RaceFirstName = NULL, RaceLastName = NULL, RacePhone = NULL, SexEmail = NULL, SexFirstName = NULL, SexLastName = NULL, SexPhone = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACoordinatorV1", body = list(DataObject = body), searchFields = append("CRDCLEACoordinatorV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEACoordinatorV1
	#'
	#' This function modifies a CRDCLEACoordinatorV1
	#' @param fieldNames The field values to give the modified CRDCLEACoordinatorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEACoordinatorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEACoordinatorV1 <- function(CRDCLEACoordinatorV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityEmail = NULL, DisabilityFirstName = NULL, DisabilityLastName = NULL, DisabilityPhone = NULL, DistrictID = NULL, DistrictSchoolYearID = NULL, HasDisability = NULL, HasGender = NULL, HasRace = NULL, NameIDDisability = NULL, NameIDRace = NULL, NameIDSex = NULL, RaceEmail = NULL, RaceFirstName = NULL, RaceLastName = NULL, RacePhone = NULL, SexEmail = NULL, SexFirstName = NULL, SexLastName = NULL, SexPhone = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEACoordinatorV1", objectId = CRDCLEACoordinatorV1ID, body = list(DataObject = body), searchFields = append("CRDCLEACoordinatorV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEACountV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEACountV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEACountV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEACountV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEACountV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEACountV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEACountV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEACountV1ID = F, CreatedTime = F, DistrictID = F, DistrictSchoolYearID = F, ModifiedTime = F, NCESIDCode = F, SchoolCount = F, StudentNonLEACount = F, StudentOverallCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEACountV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEACountV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEACountV1
	#' @param CRDCLEACountV1ID The ID of the CRDCLEACountV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEACountV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEACountV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEACountV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEACountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEACountV1 <- function(CRDCLEACountV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, DistrictSchoolYearID = F, ModifiedTime = F, NCESIDCode = F, SchoolCount = F, StudentNonLEACount = F, StudentOverallCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEACountV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACountV1", objectId = CRDCLEACountV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEACountV1
	#'
	#' This function deletes a CRDCLEACountV1
	#' @param CRDCLEACountV1ID The ID of the CRDCLEACountV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEACountV1ID of the deleted CRDCLEACountV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEACountV1 <- function(CRDCLEACountV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACountV1", objectId = CRDCLEACountV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEACountV1
	#'
	#' This function creates a CRDCLEACountV1
	#' @param fieldNames The field values to give the created CRDCLEACountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEACountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEACountV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, DistrictSchoolYearID = NULL, NCESIDCode = NULL, SchoolCount = NULL, StudentNonLEACount = NULL, StudentOverallCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACountV1", body = list(DataObject = body), searchFields = append("CRDCLEACountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEACountV1
	#'
	#' This function modifies a CRDCLEACountV1
	#' @param fieldNames The field values to give the modified CRDCLEACountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEACountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEACountV1 <- function(CRDCLEACountV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, DistrictSchoolYearID = NULL, NCESIDCode = NULL, SchoolCount = NULL, StudentNonLEACount = NULL, StudentOverallCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEACountV1", objectId = CRDCLEACountV1ID, body = list(DataObject = body), searchFields = append("CRDCLEACountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEAHarassmentV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEAHarassmentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAHarassmentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAHarassmentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAHarassmentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEAHarassmentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEAHarassmentV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEAHarassmentV1ID = F, CreatedTime = F, DistrictID = F, DistrictSchoolYearID = F, HarassmentPolicyWebLink = F, HasDesegregationPlan = F, HasHarassmentPolicy = F, HasHarassmentPolicyWebLink = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEAHarassmentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEAHarassmentV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEAHarassmentV1
	#' @param CRDCLEAHarassmentV1ID The ID of the CRDCLEAHarassmentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAHarassmentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAHarassmentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAHarassmentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEAHarassmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEAHarassmentV1 <- function(CRDCLEAHarassmentV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, DistrictSchoolYearID = F, HarassmentPolicyWebLink = F, HasDesegregationPlan = F, HasHarassmentPolicy = F, HasHarassmentPolicyWebLink = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEAHarassmentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAHarassmentV1", objectId = CRDCLEAHarassmentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEAHarassmentV1
	#'
	#' This function deletes a CRDCLEAHarassmentV1
	#' @param CRDCLEAHarassmentV1ID The ID of the CRDCLEAHarassmentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEAHarassmentV1ID of the deleted CRDCLEAHarassmentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEAHarassmentV1 <- function(CRDCLEAHarassmentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAHarassmentV1", objectId = CRDCLEAHarassmentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEAHarassmentV1
	#'
	#' This function creates a CRDCLEAHarassmentV1
	#' @param fieldNames The field values to give the created CRDCLEAHarassmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEAHarassmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEAHarassmentV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, DistrictSchoolYearID = NULL, HarassmentPolicyWebLink = NULL, HasDesegregationPlan = NULL, HasHarassmentPolicy = NULL, HasHarassmentPolicyWebLink = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAHarassmentV1", body = list(DataObject = body), searchFields = append("CRDCLEAHarassmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEAHarassmentV1
	#'
	#' This function modifies a CRDCLEAHarassmentV1
	#' @param fieldNames The field values to give the modified CRDCLEAHarassmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEAHarassmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEAHarassmentV1 <- function(CRDCLEAHarassmentV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, DistrictSchoolYearID = NULL, HarassmentPolicyWebLink = NULL, HasDesegregationPlan = NULL, HasHarassmentPolicy = NULL, HasHarassmentPolicyWebLink = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEAHarassmentV1", objectId = CRDCLEAHarassmentV1ID, body = list(DataObject = body), searchFields = append("CRDCLEAHarassmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAlternativeTransferWithDisabilityV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAlternativeTransferWithDisabilityV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAlternativeTransferWithDisabilityV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAlternativeTransferWithDisabilityV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAlternativeTransferWithDisabilityV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAlternativeTransferWithDisabilityV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAlternativeTransferWithDisabilityV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAlternativeTransferWithDisabilityV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithDisabilityV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAlternativeTransferWithDisabilityV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAlternativeTransferWithDisabilityV1
	#' @param CRDCSchoolAlternativeTransferWithDisabilityV1ID The ID of the CRDCSchoolAlternativeTransferWithDisabilityV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAlternativeTransferWithDisabilityV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAlternativeTransferWithDisabilityV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAlternativeTransferWithDisabilityV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAlternativeTransferWithDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAlternativeTransferWithDisabilityV1 <- function(CRDCSchoolAlternativeTransferWithDisabilityV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAlternativeTransferWithDisabilityV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithDisabilityV1", objectId = CRDCSchoolAlternativeTransferWithDisabilityV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAlternativeTransferWithDisabilityV1
	#'
	#' This function deletes a CRDCSchoolAlternativeTransferWithDisabilityV1
	#' @param CRDCSchoolAlternativeTransferWithDisabilityV1ID The ID of the CRDCSchoolAlternativeTransferWithDisabilityV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAlternativeTransferWithDisabilityV1ID of the deleted CRDCSchoolAlternativeTransferWithDisabilityV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAlternativeTransferWithDisabilityV1 <- function(CRDCSchoolAlternativeTransferWithDisabilityV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithDisabilityV1", objectId = CRDCSchoolAlternativeTransferWithDisabilityV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAlternativeTransferWithDisabilityV1
	#'
	#' This function creates a CRDCSchoolAlternativeTransferWithDisabilityV1
	#' @param fieldNames The field values to give the created CRDCSchoolAlternativeTransferWithDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAlternativeTransferWithDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAlternativeTransferWithDisabilityV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithDisabilityV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAlternativeTransferWithDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAlternativeTransferWithDisabilityV1
	#'
	#' This function modifies a CRDCSchoolAlternativeTransferWithDisabilityV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAlternativeTransferWithDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAlternativeTransferWithDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAlternativeTransferWithDisabilityV1 <- function(CRDCSchoolAlternativeTransferWithDisabilityV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithDisabilityV1", objectId = CRDCSchoolAlternativeTransferWithDisabilityV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAlternativeTransferWithDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAlternativeTransferWithoutDisabilityV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAlternativeTransferWithoutDisabilityV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAlternativeTransferWithoutDisabilityV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAlternativeTransferWithoutDisabilityV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAlternativeTransferWithoutDisabilityV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAlternativeTransferWithoutDisabilityV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAlternativeTransferWithoutDisabilityV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAlternativeTransferWithoutDisabilityV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithoutDisabilityV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' @param CRDCSchoolAlternativeTransferWithoutDisabilityV1ID The ID of the CRDCSchoolAlternativeTransferWithoutDisabilityV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAlternativeTransferWithoutDisabilityV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAlternativeTransferWithoutDisabilityV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAlternativeTransferWithoutDisabilityV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAlternativeTransferWithoutDisabilityV1 <- function(CRDCSchoolAlternativeTransferWithoutDisabilityV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAlternativeTransferWithoutDisabilityV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithoutDisabilityV1", objectId = CRDCSchoolAlternativeTransferWithoutDisabilityV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#'
	#' This function deletes a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' @param CRDCSchoolAlternativeTransferWithoutDisabilityV1ID The ID of the CRDCSchoolAlternativeTransferWithoutDisabilityV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAlternativeTransferWithoutDisabilityV1ID of the deleted CRDCSchoolAlternativeTransferWithoutDisabilityV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAlternativeTransferWithoutDisabilityV1 <- function(CRDCSchoolAlternativeTransferWithoutDisabilityV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithoutDisabilityV1", objectId = CRDCSchoolAlternativeTransferWithoutDisabilityV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#'
	#' This function creates a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' @param fieldNames The field values to give the created CRDCSchoolAlternativeTransferWithoutDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAlternativeTransferWithoutDisabilityV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithoutDisabilityV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAlternativeTransferWithoutDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#'
	#' This function modifies a CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAlternativeTransferWithoutDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAlternativeTransferWithoutDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAlternativeTransferWithoutDisabilityV1 <- function(CRDCSchoolAlternativeTransferWithoutDisabilityV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAlternativeTransferWithoutDisabilityV1", objectId = CRDCSchoolAlternativeTransferWithoutDisabilityV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAlternativeTransferWithoutDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPCourseV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPCourseV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPCourseV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPCourseV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPCourseV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPCourseV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPCourseV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CourseCount = F, CRDCSchoolAPCourseV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasSelfSelection = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPCourseV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPCourseV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPCourseV1
	#' @param CRDCSchoolAPCourseV1ID The ID of the CRDCSchoolAPCourseV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPCourseV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPCourseV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPCourseV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPCourseV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPCourseV1 <- function(CRDCSchoolAPCourseV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CourseCount = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasSelfSelection = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPCourseV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPCourseV1", objectId = CRDCSchoolAPCourseV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPCourseV1
	#'
	#' This function deletes a CRDCSchoolAPCourseV1
	#' @param CRDCSchoolAPCourseV1ID The ID of the CRDCSchoolAPCourseV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPCourseV1ID of the deleted CRDCSchoolAPCourseV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPCourseV1 <- function(CRDCSchoolAPCourseV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPCourseV1", objectId = CRDCSchoolAPCourseV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPCourseV1
	#'
	#' This function creates a CRDCSchoolAPCourseV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPCourseV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPCourseV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPCourseV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CourseCount = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasSelfSelection = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPCourseV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPCourseV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPCourseV1
	#'
	#' This function modifies a CRDCSchoolAPCourseV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPCourseV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPCourseV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPCourseV1 <- function(CRDCSchoolAPCourseV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CourseCount = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasSelfSelection = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPCourseV1", objectId = CRDCSchoolAPCourseV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPCourseV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPMathV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPMathV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPMathV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPMathV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPMathV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPMathV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPMathV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAPMathV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPMathV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPMathV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPMathV1
	#' @param CRDCSchoolAPMathV1ID The ID of the CRDCSchoolAPMathV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPMathV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPMathV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPMathV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPMathV1 <- function(CRDCSchoolAPMathV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPMathV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPMathV1", objectId = CRDCSchoolAPMathV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPMathV1
	#'
	#' This function deletes a CRDCSchoolAPMathV1
	#' @param CRDCSchoolAPMathV1ID The ID of the CRDCSchoolAPMathV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPMathV1ID of the deleted CRDCSchoolAPMathV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPMathV1 <- function(CRDCSchoolAPMathV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPMathV1", objectId = CRDCSchoolAPMathV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPMathV1
	#'
	#' This function creates a CRDCSchoolAPMathV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPMathV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPMathV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPMathV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPMathV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPMathV1
	#'
	#' This function modifies a CRDCSchoolAPMathV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPMathV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPMathV1 <- function(CRDCSchoolAPMathV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPMathV1", objectId = CRDCSchoolAPMathV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPMathV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPOtherV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPOtherV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPOtherV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPOtherV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPOtherV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPOtherV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPOtherV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAPOtherV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPOtherV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPOtherV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPOtherV1
	#' @param CRDCSchoolAPOtherV1ID The ID of the CRDCSchoolAPOtherV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPOtherV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPOtherV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPOtherV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPOtherV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPOtherV1 <- function(CRDCSchoolAPOtherV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPOtherV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPOtherV1", objectId = CRDCSchoolAPOtherV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPOtherV1
	#'
	#' This function deletes a CRDCSchoolAPOtherV1
	#' @param CRDCSchoolAPOtherV1ID The ID of the CRDCSchoolAPOtherV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPOtherV1ID of the deleted CRDCSchoolAPOtherV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPOtherV1 <- function(CRDCSchoolAPOtherV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPOtherV1", objectId = CRDCSchoolAPOtherV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPOtherV1
	#'
	#' This function creates a CRDCSchoolAPOtherV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPOtherV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPOtherV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPOtherV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPOtherV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPOtherV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPOtherV1
	#'
	#' This function modifies a CRDCSchoolAPOtherV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPOtherV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPOtherV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPOtherV1 <- function(CRDCSchoolAPOtherV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPOtherV1", objectId = CRDCSchoolAPOtherV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPOtherV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPScienceV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPScienceV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPScienceV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPScienceV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPScienceV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPScienceV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPScienceV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAPScienceV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPScienceV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPScienceV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPScienceV1
	#' @param CRDCSchoolAPScienceV1ID The ID of the CRDCSchoolAPScienceV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPScienceV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPScienceV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPScienceV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPScienceV1 <- function(CRDCSchoolAPScienceV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPScienceV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPScienceV1", objectId = CRDCSchoolAPScienceV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPScienceV1
	#'
	#' This function deletes a CRDCSchoolAPScienceV1
	#' @param CRDCSchoolAPScienceV1ID The ID of the CRDCSchoolAPScienceV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPScienceV1ID of the deleted CRDCSchoolAPScienceV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPScienceV1 <- function(CRDCSchoolAPScienceV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPScienceV1", objectId = CRDCSchoolAPScienceV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPScienceV1
	#'
	#' This function creates a CRDCSchoolAPScienceV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPScienceV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPScienceV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPScienceV1
	#'
	#' This function modifies a CRDCSchoolAPScienceV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPScienceV1 <- function(CRDCSchoolAPScienceV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPScienceV1", objectId = CRDCSchoolAPScienceV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolDisciplineWithoutDisabilityV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolDisciplineWithoutDisabilityV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDisciplineWithoutDisabilityV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDisciplineWithoutDisabilityV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDisciplineWithoutDisabilityV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolDisciplineWithoutDisabilityV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolDisciplineWithoutDisabilityV1s <- function(searchConditionsList = NULL, ArrestFemaleAlaskanCount = F, ArrestFemaleAsianCount = F, ArrestFemaleBlackCount = F, ArrestFemaleHawaiianCount = F, ArrestFemaleHispanicCount = F, ArrestFemaleLEPCount = F, ArrestFemaleMultiRaceCount = F, ArrestFemaleWhiteCount = F, ArrestMaleAlaskanCount = F, ArrestMaleAsianCount = F, ArrestMaleBlackCount = F, ArrestMaleHawaiianCount = F, ArrestMaleHispanicCount = F, ArrestMaleLEPCount = F, ArrestMaleMultiRaceCount = F, ArrestMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CorporalFemaleAlaskanCount = F, CorporalFemaleAsianCount = F, CorporalFemaleBlackCount = F, CorporalFemaleHawaiianCount = F, CorporalFemaleHispanicCount = F, CorporalFemaleLEPCount = F, CorporalFemaleMultiRaceCount = F, CorporalFemaleWhiteCount = F, CorporalMaleAlaskanCount = F, CorporalMaleAsianCount = F, CorporalMaleBlackCount = F, CorporalMaleHawaiianCount = F, CorporalMaleHispanicCount = F, CorporalMaleLEPCount = F, CorporalMaleMultiRaceCount = F, CorporalMaleWhiteCount = F, CRDCSchoolDisciplineWithoutDisabilityV1ID = F, CreatedTime = F, ExpulsionNoServicesFemaleAlaskanCount = F, ExpulsionNoServicesFemaleAsianCount = F, ExpulsionNoServicesFemaleBlackCount = F, ExpulsionNoServicesFemaleHawaiianCount = F, ExpulsionNoServicesFemaleHispanicCount = F, ExpulsionNoServicesFemaleLEPCount = F, ExpulsionNoServicesFemaleMultiRaceCount = F, ExpulsionNoServicesFemaleWhiteCount = F, ExpulsionNoServicesMaleAlaskanCount = F, ExpulsionNoServicesMaleAsianCount = F, ExpulsionNoServicesMaleBlackCount = F, ExpulsionNoServicesMaleHawaiianCount = F, ExpulsionNoServicesMaleHispanicCount = F, ExpulsionNoServicesMaleLEPCount = F, ExpulsionNoServicesMaleMultiRaceCount = F, ExpulsionNoServicesMaleWhiteCount = F, ExpulsionWithServicesFemaleAlaskanCount = F, ExpulsionWithServicesFemaleAsianCount = F, ExpulsionWithServicesFemaleBlackCount = F, ExpulsionWithServicesFemaleHawaiianCount = F, ExpulsionWithServicesFemaleHispanicCount = F, ExpulsionWithServicesFemaleLEPCount = F, ExpulsionWithServicesFemaleMultiRaceCount = F, ExpulsionWithServicesFemaleWhiteCount = F, ExpulsionWithServicesMaleAlaskanCount = F, ExpulsionWithServicesMaleAsianCount = F, ExpulsionWithServicesMaleBlackCount = F, ExpulsionWithServicesMaleHawaiianCount = F, ExpulsionWithServicesMaleHispanicCount = F, ExpulsionWithServicesMaleLEPCount = F, ExpulsionWithServicesMaleMultiRaceCount = F, ExpulsionWithServicesMaleWhiteCount = F, FederalNCESSchoolID = F, HasCorporalPunishment = F, InSchoolFemaleAlaskanCount = F, InSchoolFemaleAsianCount = F, InSchoolFemaleBlackCount = F, InSchoolFemaleHawaiianCount = F, InSchoolFemaleHispanicCount = F, InSchoolFemaleLEPCount = F, InSchoolFemaleMultiRaceCount = F, InSchoolFemaleWhiteCount = F, InSchoolMaleAlaskanCount = F, InSchoolMaleAsianCount = F, InSchoolMaleBlackCount = F, InSchoolMaleHawaiianCount = F, InSchoolMaleHispanicCount = F, InSchoolMaleLEPCount = F, InSchoolMaleMultiRaceCount = F, InSchoolMaleWhiteCount = F, LawEnforcementFemaleAlaskanCount = F, LawEnforcementFemaleAsianCount = F, LawEnforcementFemaleBlackCount = F, LawEnforcementFemaleHawaiianCount = F, LawEnforcementFemaleHispanicCount = F, LawEnforcementFemaleLEPCount = F, LawEnforcementFemaleMultiRaceCount = F, LawEnforcementFemaleWhiteCount = F, LawEnforcementMaleAlaskanCount = F, LawEnforcementMaleAsianCount = F, LawEnforcementMaleBlackCount = F, LawEnforcementMaleHawaiianCount = F, LawEnforcementMaleHispanicCount = F, LawEnforcementMaleLEPCount = F, LawEnforcementMaleMultiRaceCount = F, LawEnforcementMaleWhiteCount = F, ModifiedTime = F, MultipleOutOfSchoolFemaleAlaskanCount = F, MultipleOutOfSchoolFemaleAsianCount = F, MultipleOutOfSchoolFemaleBlackCount = F, MultipleOutOfSchoolFemaleHawaiianCount = F, MultipleOutOfSchoolFemaleHispanicCount = F, MultipleOutOfSchoolFemaleLEPCount = F, MultipleOutOfSchoolFemaleMultiRaceCount = F, MultipleOutOfSchoolFemaleWhiteCount = F, MultipleOutOfSchoolMaleAlaskanCount = F, MultipleOutOfSchoolMaleAsianCount = F, MultipleOutOfSchoolMaleBlackCount = F, MultipleOutOfSchoolMaleHawaiianCount = F, MultipleOutOfSchoolMaleHispanicCount = F, MultipleOutOfSchoolMaleLEPCount = F, MultipleOutOfSchoolMaleMultiRaceCount = F, MultipleOutOfSchoolMaleWhiteCount = F, OneOutOfSchoolFemaleAlaskanCount = F, OneOutOfSchoolFemaleAsianCount = F, OneOutOfSchoolFemaleBlackCount = F, OneOutOfSchoolFemaleHawaiianCount = F, OneOutOfSchoolFemaleHispanicCount = F, OneOutOfSchoolFemaleLEPCount = F, OneOutOfSchoolFemaleMultiRaceCount = F, OneOutOfSchoolFemaleWhiteCount = F, OneOutOfSchoolMaleAlaskanCount = F, OneOutOfSchoolMaleAsianCount = F, OneOutOfSchoolMaleBlackCount = F, OneOutOfSchoolMaleHawaiianCount = F, OneOutOfSchoolMaleHispanicCount = F, OneOutOfSchoolMaleLEPCount = F, OneOutOfSchoolMaleMultiRaceCount = F, OneOutOfSchoolMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZeroToleranceFemaleAlaskanCount = F, ZeroToleranceFemaleAsianCount = F, ZeroToleranceFemaleBlackCount = F, ZeroToleranceFemaleHawaiianCount = F, ZeroToleranceFemaleHispanicCount = F, ZeroToleranceFemaleLEPCount = F, ZeroToleranceFemaleMultiRaceCount = F, ZeroToleranceFemaleWhiteCount = F, ZeroToleranceMaleAlaskanCount = F, ZeroToleranceMaleAsianCount = F, ZeroToleranceMaleBlackCount = F, ZeroToleranceMaleHawaiianCount = F, ZeroToleranceMaleHispanicCount = F, ZeroToleranceMaleLEPCount = F, ZeroToleranceMaleMultiRaceCount = F, ZeroToleranceMaleWhiteCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithoutDisabilityV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolDisciplineWithoutDisabilityV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolDisciplineWithoutDisabilityV1
	#' @param CRDCSchoolDisciplineWithoutDisabilityV1ID The ID of the CRDCSchoolDisciplineWithoutDisabilityV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDisciplineWithoutDisabilityV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDisciplineWithoutDisabilityV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDisciplineWithoutDisabilityV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolDisciplineWithoutDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolDisciplineWithoutDisabilityV1 <- function(CRDCSchoolDisciplineWithoutDisabilityV1ID, ArrestFemaleAlaskanCount = F, ArrestFemaleAsianCount = F, ArrestFemaleBlackCount = F, ArrestFemaleHawaiianCount = F, ArrestFemaleHispanicCount = F, ArrestFemaleLEPCount = F, ArrestFemaleMultiRaceCount = F, ArrestFemaleWhiteCount = F, ArrestMaleAlaskanCount = F, ArrestMaleAsianCount = F, ArrestMaleBlackCount = F, ArrestMaleHawaiianCount = F, ArrestMaleHispanicCount = F, ArrestMaleLEPCount = F, ArrestMaleMultiRaceCount = F, ArrestMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CorporalFemaleAlaskanCount = F, CorporalFemaleAsianCount = F, CorporalFemaleBlackCount = F, CorporalFemaleHawaiianCount = F, CorporalFemaleHispanicCount = F, CorporalFemaleLEPCount = F, CorporalFemaleMultiRaceCount = F, CorporalFemaleWhiteCount = F, CorporalMaleAlaskanCount = F, CorporalMaleAsianCount = F, CorporalMaleBlackCount = F, CorporalMaleHawaiianCount = F, CorporalMaleHispanicCount = F, CorporalMaleLEPCount = F, CorporalMaleMultiRaceCount = F, CorporalMaleWhiteCount = F, CreatedTime = F, ExpulsionNoServicesFemaleAlaskanCount = F, ExpulsionNoServicesFemaleAsianCount = F, ExpulsionNoServicesFemaleBlackCount = F, ExpulsionNoServicesFemaleHawaiianCount = F, ExpulsionNoServicesFemaleHispanicCount = F, ExpulsionNoServicesFemaleLEPCount = F, ExpulsionNoServicesFemaleMultiRaceCount = F, ExpulsionNoServicesFemaleWhiteCount = F, ExpulsionNoServicesMaleAlaskanCount = F, ExpulsionNoServicesMaleAsianCount = F, ExpulsionNoServicesMaleBlackCount = F, ExpulsionNoServicesMaleHawaiianCount = F, ExpulsionNoServicesMaleHispanicCount = F, ExpulsionNoServicesMaleLEPCount = F, ExpulsionNoServicesMaleMultiRaceCount = F, ExpulsionNoServicesMaleWhiteCount = F, ExpulsionWithServicesFemaleAlaskanCount = F, ExpulsionWithServicesFemaleAsianCount = F, ExpulsionWithServicesFemaleBlackCount = F, ExpulsionWithServicesFemaleHawaiianCount = F, ExpulsionWithServicesFemaleHispanicCount = F, ExpulsionWithServicesFemaleLEPCount = F, ExpulsionWithServicesFemaleMultiRaceCount = F, ExpulsionWithServicesFemaleWhiteCount = F, ExpulsionWithServicesMaleAlaskanCount = F, ExpulsionWithServicesMaleAsianCount = F, ExpulsionWithServicesMaleBlackCount = F, ExpulsionWithServicesMaleHawaiianCount = F, ExpulsionWithServicesMaleHispanicCount = F, ExpulsionWithServicesMaleLEPCount = F, ExpulsionWithServicesMaleMultiRaceCount = F, ExpulsionWithServicesMaleWhiteCount = F, FederalNCESSchoolID = F, HasCorporalPunishment = F, InSchoolFemaleAlaskanCount = F, InSchoolFemaleAsianCount = F, InSchoolFemaleBlackCount = F, InSchoolFemaleHawaiianCount = F, InSchoolFemaleHispanicCount = F, InSchoolFemaleLEPCount = F, InSchoolFemaleMultiRaceCount = F, InSchoolFemaleWhiteCount = F, InSchoolMaleAlaskanCount = F, InSchoolMaleAsianCount = F, InSchoolMaleBlackCount = F, InSchoolMaleHawaiianCount = F, InSchoolMaleHispanicCount = F, InSchoolMaleLEPCount = F, InSchoolMaleMultiRaceCount = F, InSchoolMaleWhiteCount = F, LawEnforcementFemaleAlaskanCount = F, LawEnforcementFemaleAsianCount = F, LawEnforcementFemaleBlackCount = F, LawEnforcementFemaleHawaiianCount = F, LawEnforcementFemaleHispanicCount = F, LawEnforcementFemaleLEPCount = F, LawEnforcementFemaleMultiRaceCount = F, LawEnforcementFemaleWhiteCount = F, LawEnforcementMaleAlaskanCount = F, LawEnforcementMaleAsianCount = F, LawEnforcementMaleBlackCount = F, LawEnforcementMaleHawaiianCount = F, LawEnforcementMaleHispanicCount = F, LawEnforcementMaleLEPCount = F, LawEnforcementMaleMultiRaceCount = F, LawEnforcementMaleWhiteCount = F, ModifiedTime = F, MultipleOutOfSchoolFemaleAlaskanCount = F, MultipleOutOfSchoolFemaleAsianCount = F, MultipleOutOfSchoolFemaleBlackCount = F, MultipleOutOfSchoolFemaleHawaiianCount = F, MultipleOutOfSchoolFemaleHispanicCount = F, MultipleOutOfSchoolFemaleLEPCount = F, MultipleOutOfSchoolFemaleMultiRaceCount = F, MultipleOutOfSchoolFemaleWhiteCount = F, MultipleOutOfSchoolMaleAlaskanCount = F, MultipleOutOfSchoolMaleAsianCount = F, MultipleOutOfSchoolMaleBlackCount = F, MultipleOutOfSchoolMaleHawaiianCount = F, MultipleOutOfSchoolMaleHispanicCount = F, MultipleOutOfSchoolMaleLEPCount = F, MultipleOutOfSchoolMaleMultiRaceCount = F, MultipleOutOfSchoolMaleWhiteCount = F, OneOutOfSchoolFemaleAlaskanCount = F, OneOutOfSchoolFemaleAsianCount = F, OneOutOfSchoolFemaleBlackCount = F, OneOutOfSchoolFemaleHawaiianCount = F, OneOutOfSchoolFemaleHispanicCount = F, OneOutOfSchoolFemaleLEPCount = F, OneOutOfSchoolFemaleMultiRaceCount = F, OneOutOfSchoolFemaleWhiteCount = F, OneOutOfSchoolMaleAlaskanCount = F, OneOutOfSchoolMaleAsianCount = F, OneOutOfSchoolMaleBlackCount = F, OneOutOfSchoolMaleHawaiianCount = F, OneOutOfSchoolMaleHispanicCount = F, OneOutOfSchoolMaleLEPCount = F, OneOutOfSchoolMaleMultiRaceCount = F, OneOutOfSchoolMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZeroToleranceFemaleAlaskanCount = F, ZeroToleranceFemaleAsianCount = F, ZeroToleranceFemaleBlackCount = F, ZeroToleranceFemaleHawaiianCount = F, ZeroToleranceFemaleHispanicCount = F, ZeroToleranceFemaleLEPCount = F, ZeroToleranceFemaleMultiRaceCount = F, ZeroToleranceFemaleWhiteCount = F, ZeroToleranceMaleAlaskanCount = F, ZeroToleranceMaleAsianCount = F, ZeroToleranceMaleBlackCount = F, ZeroToleranceMaleHawaiianCount = F, ZeroToleranceMaleHispanicCount = F, ZeroToleranceMaleLEPCount = F, ZeroToleranceMaleMultiRaceCount = F, ZeroToleranceMaleWhiteCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolDisciplineWithoutDisabilityV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithoutDisabilityV1", objectId = CRDCSchoolDisciplineWithoutDisabilityV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolDisciplineWithoutDisabilityV1
	#'
	#' This function deletes a CRDCSchoolDisciplineWithoutDisabilityV1
	#' @param CRDCSchoolDisciplineWithoutDisabilityV1ID The ID of the CRDCSchoolDisciplineWithoutDisabilityV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolDisciplineWithoutDisabilityV1ID of the deleted CRDCSchoolDisciplineWithoutDisabilityV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolDisciplineWithoutDisabilityV1 <- function(CRDCSchoolDisciplineWithoutDisabilityV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithoutDisabilityV1", objectId = CRDCSchoolDisciplineWithoutDisabilityV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolDisciplineWithoutDisabilityV1
	#'
	#' This function creates a CRDCSchoolDisciplineWithoutDisabilityV1
	#' @param fieldNames The field values to give the created CRDCSchoolDisciplineWithoutDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolDisciplineWithoutDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolDisciplineWithoutDisabilityV1 <- function(ArrestFemaleAlaskanCount = NULL, ArrestFemaleAsianCount = NULL, ArrestFemaleBlackCount = NULL, ArrestFemaleHawaiianCount = NULL, ArrestFemaleHispanicCount = NULL, ArrestFemaleLEPCount = NULL, ArrestFemaleMultiRaceCount = NULL, ArrestFemaleWhiteCount = NULL, ArrestMaleAlaskanCount = NULL, ArrestMaleAsianCount = NULL, ArrestMaleBlackCount = NULL, ArrestMaleHawaiianCount = NULL, ArrestMaleHispanicCount = NULL, ArrestMaleLEPCount = NULL, ArrestMaleMultiRaceCount = NULL, ArrestMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CorporalFemaleAlaskanCount = NULL, CorporalFemaleAsianCount = NULL, CorporalFemaleBlackCount = NULL, CorporalFemaleHawaiianCount = NULL, CorporalFemaleHispanicCount = NULL, CorporalFemaleLEPCount = NULL, CorporalFemaleMultiRaceCount = NULL, CorporalFemaleWhiteCount = NULL, CorporalMaleAlaskanCount = NULL, CorporalMaleAsianCount = NULL, CorporalMaleBlackCount = NULL, CorporalMaleHawaiianCount = NULL, CorporalMaleHispanicCount = NULL, CorporalMaleLEPCount = NULL, CorporalMaleMultiRaceCount = NULL, CorporalMaleWhiteCount = NULL, ExpulsionNoServicesFemaleAlaskanCount = NULL, ExpulsionNoServicesFemaleAsianCount = NULL, ExpulsionNoServicesFemaleBlackCount = NULL, ExpulsionNoServicesFemaleHawaiianCount = NULL, ExpulsionNoServicesFemaleHispanicCount = NULL, ExpulsionNoServicesFemaleLEPCount = NULL, ExpulsionNoServicesFemaleMultiRaceCount = NULL, ExpulsionNoServicesFemaleWhiteCount = NULL, ExpulsionNoServicesMaleAlaskanCount = NULL, ExpulsionNoServicesMaleAsianCount = NULL, ExpulsionNoServicesMaleBlackCount = NULL, ExpulsionNoServicesMaleHawaiianCount = NULL, ExpulsionNoServicesMaleHispanicCount = NULL, ExpulsionNoServicesMaleLEPCount = NULL, ExpulsionNoServicesMaleMultiRaceCount = NULL, ExpulsionNoServicesMaleWhiteCount = NULL, ExpulsionWithServicesFemaleAlaskanCount = NULL, ExpulsionWithServicesFemaleAsianCount = NULL, ExpulsionWithServicesFemaleBlackCount = NULL, ExpulsionWithServicesFemaleHawaiianCount = NULL, ExpulsionWithServicesFemaleHispanicCount = NULL, ExpulsionWithServicesFemaleLEPCount = NULL, ExpulsionWithServicesFemaleMultiRaceCount = NULL, ExpulsionWithServicesFemaleWhiteCount = NULL, ExpulsionWithServicesMaleAlaskanCount = NULL, ExpulsionWithServicesMaleAsianCount = NULL, ExpulsionWithServicesMaleBlackCount = NULL, ExpulsionWithServicesMaleHawaiianCount = NULL, ExpulsionWithServicesMaleHispanicCount = NULL, ExpulsionWithServicesMaleLEPCount = NULL, ExpulsionWithServicesMaleMultiRaceCount = NULL, ExpulsionWithServicesMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, HasCorporalPunishment = NULL, InSchoolFemaleAlaskanCount = NULL, InSchoolFemaleAsianCount = NULL, InSchoolFemaleBlackCount = NULL, InSchoolFemaleHawaiianCount = NULL, InSchoolFemaleHispanicCount = NULL, InSchoolFemaleLEPCount = NULL, InSchoolFemaleMultiRaceCount = NULL, InSchoolFemaleWhiteCount = NULL, InSchoolMaleAlaskanCount = NULL, InSchoolMaleAsianCount = NULL, InSchoolMaleBlackCount = NULL, InSchoolMaleHawaiianCount = NULL, InSchoolMaleHispanicCount = NULL, InSchoolMaleLEPCount = NULL, InSchoolMaleMultiRaceCount = NULL, InSchoolMaleWhiteCount = NULL, LawEnforcementFemaleAlaskanCount = NULL, LawEnforcementFemaleAsianCount = NULL, LawEnforcementFemaleBlackCount = NULL, LawEnforcementFemaleHawaiianCount = NULL, LawEnforcementFemaleHispanicCount = NULL, LawEnforcementFemaleLEPCount = NULL, LawEnforcementFemaleMultiRaceCount = NULL, LawEnforcementFemaleWhiteCount = NULL, LawEnforcementMaleAlaskanCount = NULL, LawEnforcementMaleAsianCount = NULL, LawEnforcementMaleBlackCount = NULL, LawEnforcementMaleHawaiianCount = NULL, LawEnforcementMaleHispanicCount = NULL, LawEnforcementMaleLEPCount = NULL, LawEnforcementMaleMultiRaceCount = NULL, LawEnforcementMaleWhiteCount = NULL, MultipleOutOfSchoolFemaleAlaskanCount = NULL, MultipleOutOfSchoolFemaleAsianCount = NULL, MultipleOutOfSchoolFemaleBlackCount = NULL, MultipleOutOfSchoolFemaleHawaiianCount = NULL, MultipleOutOfSchoolFemaleHispanicCount = NULL, MultipleOutOfSchoolFemaleLEPCount = NULL, MultipleOutOfSchoolFemaleMultiRaceCount = NULL, MultipleOutOfSchoolFemaleWhiteCount = NULL, MultipleOutOfSchoolMaleAlaskanCount = NULL, MultipleOutOfSchoolMaleAsianCount = NULL, MultipleOutOfSchoolMaleBlackCount = NULL, MultipleOutOfSchoolMaleHawaiianCount = NULL, MultipleOutOfSchoolMaleHispanicCount = NULL, MultipleOutOfSchoolMaleLEPCount = NULL, MultipleOutOfSchoolMaleMultiRaceCount = NULL, MultipleOutOfSchoolMaleWhiteCount = NULL, OneOutOfSchoolFemaleAlaskanCount = NULL, OneOutOfSchoolFemaleAsianCount = NULL, OneOutOfSchoolFemaleBlackCount = NULL, OneOutOfSchoolFemaleHawaiianCount = NULL, OneOutOfSchoolFemaleHispanicCount = NULL, OneOutOfSchoolFemaleLEPCount = NULL, OneOutOfSchoolFemaleMultiRaceCount = NULL, OneOutOfSchoolFemaleWhiteCount = NULL, OneOutOfSchoolMaleAlaskanCount = NULL, OneOutOfSchoolMaleAsianCount = NULL, OneOutOfSchoolMaleBlackCount = NULL, OneOutOfSchoolMaleHawaiianCount = NULL, OneOutOfSchoolMaleHispanicCount = NULL, OneOutOfSchoolMaleLEPCount = NULL, OneOutOfSchoolMaleMultiRaceCount = NULL, OneOutOfSchoolMaleWhiteCount = NULL, ZeroToleranceFemaleAlaskanCount = NULL, ZeroToleranceFemaleAsianCount = NULL, ZeroToleranceFemaleBlackCount = NULL, ZeroToleranceFemaleHawaiianCount = NULL, ZeroToleranceFemaleHispanicCount = NULL, ZeroToleranceFemaleLEPCount = NULL, ZeroToleranceFemaleMultiRaceCount = NULL, ZeroToleranceFemaleWhiteCount = NULL, ZeroToleranceMaleAlaskanCount = NULL, ZeroToleranceMaleAsianCount = NULL, ZeroToleranceMaleBlackCount = NULL, ZeroToleranceMaleHawaiianCount = NULL, ZeroToleranceMaleHispanicCount = NULL, ZeroToleranceMaleLEPCount = NULL, ZeroToleranceMaleMultiRaceCount = NULL, ZeroToleranceMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithoutDisabilityV1", body = list(DataObject = body), searchFields = append("CRDCSchoolDisciplineWithoutDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolDisciplineWithoutDisabilityV1
	#'
	#' This function modifies a CRDCSchoolDisciplineWithoutDisabilityV1
	#' @param fieldNames The field values to give the modified CRDCSchoolDisciplineWithoutDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolDisciplineWithoutDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolDisciplineWithoutDisabilityV1 <- function(CRDCSchoolDisciplineWithoutDisabilityV1ID, ArrestFemaleAlaskanCount = NULL, ArrestFemaleAsianCount = NULL, ArrestFemaleBlackCount = NULL, ArrestFemaleHawaiianCount = NULL, ArrestFemaleHispanicCount = NULL, ArrestFemaleLEPCount = NULL, ArrestFemaleMultiRaceCount = NULL, ArrestFemaleWhiteCount = NULL, ArrestMaleAlaskanCount = NULL, ArrestMaleAsianCount = NULL, ArrestMaleBlackCount = NULL, ArrestMaleHawaiianCount = NULL, ArrestMaleHispanicCount = NULL, ArrestMaleLEPCount = NULL, ArrestMaleMultiRaceCount = NULL, ArrestMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CorporalFemaleAlaskanCount = NULL, CorporalFemaleAsianCount = NULL, CorporalFemaleBlackCount = NULL, CorporalFemaleHawaiianCount = NULL, CorporalFemaleHispanicCount = NULL, CorporalFemaleLEPCount = NULL, CorporalFemaleMultiRaceCount = NULL, CorporalFemaleWhiteCount = NULL, CorporalMaleAlaskanCount = NULL, CorporalMaleAsianCount = NULL, CorporalMaleBlackCount = NULL, CorporalMaleHawaiianCount = NULL, CorporalMaleHispanicCount = NULL, CorporalMaleLEPCount = NULL, CorporalMaleMultiRaceCount = NULL, CorporalMaleWhiteCount = NULL, ExpulsionNoServicesFemaleAlaskanCount = NULL, ExpulsionNoServicesFemaleAsianCount = NULL, ExpulsionNoServicesFemaleBlackCount = NULL, ExpulsionNoServicesFemaleHawaiianCount = NULL, ExpulsionNoServicesFemaleHispanicCount = NULL, ExpulsionNoServicesFemaleLEPCount = NULL, ExpulsionNoServicesFemaleMultiRaceCount = NULL, ExpulsionNoServicesFemaleWhiteCount = NULL, ExpulsionNoServicesMaleAlaskanCount = NULL, ExpulsionNoServicesMaleAsianCount = NULL, ExpulsionNoServicesMaleBlackCount = NULL, ExpulsionNoServicesMaleHawaiianCount = NULL, ExpulsionNoServicesMaleHispanicCount = NULL, ExpulsionNoServicesMaleLEPCount = NULL, ExpulsionNoServicesMaleMultiRaceCount = NULL, ExpulsionNoServicesMaleWhiteCount = NULL, ExpulsionWithServicesFemaleAlaskanCount = NULL, ExpulsionWithServicesFemaleAsianCount = NULL, ExpulsionWithServicesFemaleBlackCount = NULL, ExpulsionWithServicesFemaleHawaiianCount = NULL, ExpulsionWithServicesFemaleHispanicCount = NULL, ExpulsionWithServicesFemaleLEPCount = NULL, ExpulsionWithServicesFemaleMultiRaceCount = NULL, ExpulsionWithServicesFemaleWhiteCount = NULL, ExpulsionWithServicesMaleAlaskanCount = NULL, ExpulsionWithServicesMaleAsianCount = NULL, ExpulsionWithServicesMaleBlackCount = NULL, ExpulsionWithServicesMaleHawaiianCount = NULL, ExpulsionWithServicesMaleHispanicCount = NULL, ExpulsionWithServicesMaleLEPCount = NULL, ExpulsionWithServicesMaleMultiRaceCount = NULL, ExpulsionWithServicesMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, HasCorporalPunishment = NULL, InSchoolFemaleAlaskanCount = NULL, InSchoolFemaleAsianCount = NULL, InSchoolFemaleBlackCount = NULL, InSchoolFemaleHawaiianCount = NULL, InSchoolFemaleHispanicCount = NULL, InSchoolFemaleLEPCount = NULL, InSchoolFemaleMultiRaceCount = NULL, InSchoolFemaleWhiteCount = NULL, InSchoolMaleAlaskanCount = NULL, InSchoolMaleAsianCount = NULL, InSchoolMaleBlackCount = NULL, InSchoolMaleHawaiianCount = NULL, InSchoolMaleHispanicCount = NULL, InSchoolMaleLEPCount = NULL, InSchoolMaleMultiRaceCount = NULL, InSchoolMaleWhiteCount = NULL, LawEnforcementFemaleAlaskanCount = NULL, LawEnforcementFemaleAsianCount = NULL, LawEnforcementFemaleBlackCount = NULL, LawEnforcementFemaleHawaiianCount = NULL, LawEnforcementFemaleHispanicCount = NULL, LawEnforcementFemaleLEPCount = NULL, LawEnforcementFemaleMultiRaceCount = NULL, LawEnforcementFemaleWhiteCount = NULL, LawEnforcementMaleAlaskanCount = NULL, LawEnforcementMaleAsianCount = NULL, LawEnforcementMaleBlackCount = NULL, LawEnforcementMaleHawaiianCount = NULL, LawEnforcementMaleHispanicCount = NULL, LawEnforcementMaleLEPCount = NULL, LawEnforcementMaleMultiRaceCount = NULL, LawEnforcementMaleWhiteCount = NULL, MultipleOutOfSchoolFemaleAlaskanCount = NULL, MultipleOutOfSchoolFemaleAsianCount = NULL, MultipleOutOfSchoolFemaleBlackCount = NULL, MultipleOutOfSchoolFemaleHawaiianCount = NULL, MultipleOutOfSchoolFemaleHispanicCount = NULL, MultipleOutOfSchoolFemaleLEPCount = NULL, MultipleOutOfSchoolFemaleMultiRaceCount = NULL, MultipleOutOfSchoolFemaleWhiteCount = NULL, MultipleOutOfSchoolMaleAlaskanCount = NULL, MultipleOutOfSchoolMaleAsianCount = NULL, MultipleOutOfSchoolMaleBlackCount = NULL, MultipleOutOfSchoolMaleHawaiianCount = NULL, MultipleOutOfSchoolMaleHispanicCount = NULL, MultipleOutOfSchoolMaleLEPCount = NULL, MultipleOutOfSchoolMaleMultiRaceCount = NULL, MultipleOutOfSchoolMaleWhiteCount = NULL, OneOutOfSchoolFemaleAlaskanCount = NULL, OneOutOfSchoolFemaleAsianCount = NULL, OneOutOfSchoolFemaleBlackCount = NULL, OneOutOfSchoolFemaleHawaiianCount = NULL, OneOutOfSchoolFemaleHispanicCount = NULL, OneOutOfSchoolFemaleLEPCount = NULL, OneOutOfSchoolFemaleMultiRaceCount = NULL, OneOutOfSchoolFemaleWhiteCount = NULL, OneOutOfSchoolMaleAlaskanCount = NULL, OneOutOfSchoolMaleAsianCount = NULL, OneOutOfSchoolMaleBlackCount = NULL, OneOutOfSchoolMaleHawaiianCount = NULL, OneOutOfSchoolMaleHispanicCount = NULL, OneOutOfSchoolMaleLEPCount = NULL, OneOutOfSchoolMaleMultiRaceCount = NULL, OneOutOfSchoolMaleWhiteCount = NULL, ZeroToleranceFemaleAlaskanCount = NULL, ZeroToleranceFemaleAsianCount = NULL, ZeroToleranceFemaleBlackCount = NULL, ZeroToleranceFemaleHawaiianCount = NULL, ZeroToleranceFemaleHispanicCount = NULL, ZeroToleranceFemaleLEPCount = NULL, ZeroToleranceFemaleMultiRaceCount = NULL, ZeroToleranceFemaleWhiteCount = NULL, ZeroToleranceMaleAlaskanCount = NULL, ZeroToleranceMaleAsianCount = NULL, ZeroToleranceMaleBlackCount = NULL, ZeroToleranceMaleHawaiianCount = NULL, ZeroToleranceMaleHispanicCount = NULL, ZeroToleranceMaleLEPCount = NULL, ZeroToleranceMaleMultiRaceCount = NULL, ZeroToleranceMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithoutDisabilityV1", objectId = CRDCSchoolDisciplineWithoutDisabilityV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolDisciplineWithoutDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolIDEARestraintV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolIDEARestraintV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolIDEARestraintV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolIDEARestraintV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolIDEARestraintV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolIDEARestraintV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolIDEARestraintV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolIDEARestraintV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, MechanicalFemaleAlaskanCount = F, MechanicalFemaleAsianCount = F, MechanicalFemaleBlackCount = F, MechanicalFemaleHawaiianCount = F, MechanicalFemaleHispanicCount = F, MechanicalFemaleLEPCount = F, MechanicalFemaleMultiRaceCount = F, MechanicalFemaleSection504Count = F, MechanicalFemaleWhiteCount = F, MechanicalMaleAlaskanCount = F, MechanicalMaleAsianCount = F, MechanicalMaleBlackCount = F, MechanicalMaleHawaiianCount = F, MechanicalMaleHispanicCount = F, MechanicalMaleLEPCount = F, MechanicalMaleMultiRaceCount = F, MechanicalMaleSection504Count = F, MechanicalMaleWhiteCount = F, ModifiedTime = F, PhysicalFemaleAlaskanCount = F, PhysicalFemaleAsianCount = F, PhysicalFemaleBlackCount = F, PhysicalFemaleHawaiianCount = F, PhysicalFemaleHispanicCount = F, PhysicalFemaleLEPCount = F, PhysicalFemaleMultiRaceCount = F, PhysicalFemaleSection504Count = F, PhysicalFemaleWhiteCount = F, PhysicalMaleAlaskanCount = F, PhysicalMaleAsianCount = F, PhysicalMaleBlackCount = F, PhysicalMaleHawaiianCount = F, PhysicalMaleHispanicCount = F, PhysicalMaleLEPCount = F, PhysicalMaleMultiRaceCount = F, PhysicalMaleSection504Count = F, PhysicalMaleWhiteCount = F, SeclusionFemaleAlaskanCount = F, SeclusionFemaleAsianCount = F, SeclusionFemaleBlackCount = F, SeclusionFemaleHawaiianCount = F, SeclusionFemaleHispanicCount = F, SeclusionFemaleLEPCount = F, SeclusionFemaleMultiRaceCount = F, SeclusionFemaleSection504Count = F, SeclusionFemaleWhiteCount = F, SeclusionMaleAlaskanCount = F, SeclusionMaleAsianCount = F, SeclusionMaleBlackCount = F, SeclusionMaleHawaiianCount = F, SeclusionMaleHispanicCount = F, SeclusionMaleLEPCount = F, SeclusionMaleMultiRaceCount = F, SeclusionMaleSection504Count = F, SeclusionMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolIDEARestraintV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolIDEARestraintV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolIDEARestraintV1
	#' @param CRDCSchoolIDEARestraintV1ID The ID of the CRDCSchoolIDEARestraintV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolIDEARestraintV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolIDEARestraintV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolIDEARestraintV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolIDEARestraintV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolIDEARestraintV1 <- function(CRDCSchoolIDEARestraintV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, MechanicalFemaleAlaskanCount = F, MechanicalFemaleAsianCount = F, MechanicalFemaleBlackCount = F, MechanicalFemaleHawaiianCount = F, MechanicalFemaleHispanicCount = F, MechanicalFemaleLEPCount = F, MechanicalFemaleMultiRaceCount = F, MechanicalFemaleSection504Count = F, MechanicalFemaleWhiteCount = F, MechanicalMaleAlaskanCount = F, MechanicalMaleAsianCount = F, MechanicalMaleBlackCount = F, MechanicalMaleHawaiianCount = F, MechanicalMaleHispanicCount = F, MechanicalMaleLEPCount = F, MechanicalMaleMultiRaceCount = F, MechanicalMaleSection504Count = F, MechanicalMaleWhiteCount = F, ModifiedTime = F, PhysicalFemaleAlaskanCount = F, PhysicalFemaleAsianCount = F, PhysicalFemaleBlackCount = F, PhysicalFemaleHawaiianCount = F, PhysicalFemaleHispanicCount = F, PhysicalFemaleLEPCount = F, PhysicalFemaleMultiRaceCount = F, PhysicalFemaleSection504Count = F, PhysicalFemaleWhiteCount = F, PhysicalMaleAlaskanCount = F, PhysicalMaleAsianCount = F, PhysicalMaleBlackCount = F, PhysicalMaleHawaiianCount = F, PhysicalMaleHispanicCount = F, PhysicalMaleLEPCount = F, PhysicalMaleMultiRaceCount = F, PhysicalMaleSection504Count = F, PhysicalMaleWhiteCount = F, SeclusionFemaleAlaskanCount = F, SeclusionFemaleAsianCount = F, SeclusionFemaleBlackCount = F, SeclusionFemaleHawaiianCount = F, SeclusionFemaleHispanicCount = F, SeclusionFemaleLEPCount = F, SeclusionFemaleMultiRaceCount = F, SeclusionFemaleSection504Count = F, SeclusionFemaleWhiteCount = F, SeclusionMaleAlaskanCount = F, SeclusionMaleAsianCount = F, SeclusionMaleBlackCount = F, SeclusionMaleHawaiianCount = F, SeclusionMaleHispanicCount = F, SeclusionMaleLEPCount = F, SeclusionMaleMultiRaceCount = F, SeclusionMaleSection504Count = F, SeclusionMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolIDEARestraintV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEARestraintV1", objectId = CRDCSchoolIDEARestraintV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolIDEARestraintV1
	#'
	#' This function deletes a CRDCSchoolIDEARestraintV1
	#' @param CRDCSchoolIDEARestraintV1ID The ID of the CRDCSchoolIDEARestraintV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolIDEARestraintV1ID of the deleted CRDCSchoolIDEARestraintV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolIDEARestraintV1 <- function(CRDCSchoolIDEARestraintV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEARestraintV1", objectId = CRDCSchoolIDEARestraintV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolIDEARestraintV1
	#'
	#' This function creates a CRDCSchoolIDEARestraintV1
	#' @param fieldNames The field values to give the created CRDCSchoolIDEARestraintV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolIDEARestraintV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolIDEARestraintV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, MechanicalFemaleAlaskanCount = NULL, MechanicalFemaleAsianCount = NULL, MechanicalFemaleBlackCount = NULL, MechanicalFemaleHawaiianCount = NULL, MechanicalFemaleHispanicCount = NULL, MechanicalFemaleLEPCount = NULL, MechanicalFemaleMultiRaceCount = NULL, MechanicalFemaleSection504Count = NULL, MechanicalFemaleWhiteCount = NULL, MechanicalMaleAlaskanCount = NULL, MechanicalMaleAsianCount = NULL, MechanicalMaleBlackCount = NULL, MechanicalMaleHawaiianCount = NULL, MechanicalMaleHispanicCount = NULL, MechanicalMaleLEPCount = NULL, MechanicalMaleMultiRaceCount = NULL, MechanicalMaleSection504Count = NULL, MechanicalMaleWhiteCount = NULL, PhysicalFemaleAlaskanCount = NULL, PhysicalFemaleAsianCount = NULL, PhysicalFemaleBlackCount = NULL, PhysicalFemaleHawaiianCount = NULL, PhysicalFemaleHispanicCount = NULL, PhysicalFemaleLEPCount = NULL, PhysicalFemaleMultiRaceCount = NULL, PhysicalFemaleSection504Count = NULL, PhysicalFemaleWhiteCount = NULL, PhysicalMaleAlaskanCount = NULL, PhysicalMaleAsianCount = NULL, PhysicalMaleBlackCount = NULL, PhysicalMaleHawaiianCount = NULL, PhysicalMaleHispanicCount = NULL, PhysicalMaleLEPCount = NULL, PhysicalMaleMultiRaceCount = NULL, PhysicalMaleSection504Count = NULL, PhysicalMaleWhiteCount = NULL, SeclusionFemaleAlaskanCount = NULL, SeclusionFemaleAsianCount = NULL, SeclusionFemaleBlackCount = NULL, SeclusionFemaleHawaiianCount = NULL, SeclusionFemaleHispanicCount = NULL, SeclusionFemaleLEPCount = NULL, SeclusionFemaleMultiRaceCount = NULL, SeclusionFemaleSection504Count = NULL, SeclusionFemaleWhiteCount = NULL, SeclusionMaleAlaskanCount = NULL, SeclusionMaleAsianCount = NULL, SeclusionMaleBlackCount = NULL, SeclusionMaleHawaiianCount = NULL, SeclusionMaleHispanicCount = NULL, SeclusionMaleLEPCount = NULL, SeclusionMaleMultiRaceCount = NULL, SeclusionMaleSection504Count = NULL, SeclusionMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEARestraintV1", body = list(DataObject = body), searchFields = append("CRDCSchoolIDEARestraintV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolIDEARestraintV1
	#'
	#' This function modifies a CRDCSchoolIDEARestraintV1
	#' @param fieldNames The field values to give the modified CRDCSchoolIDEARestraintV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolIDEARestraintV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolIDEARestraintV1 <- function(CRDCSchoolIDEARestraintV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, MechanicalFemaleAlaskanCount = NULL, MechanicalFemaleAsianCount = NULL, MechanicalFemaleBlackCount = NULL, MechanicalFemaleHawaiianCount = NULL, MechanicalFemaleHispanicCount = NULL, MechanicalFemaleLEPCount = NULL, MechanicalFemaleMultiRaceCount = NULL, MechanicalFemaleSection504Count = NULL, MechanicalFemaleWhiteCount = NULL, MechanicalMaleAlaskanCount = NULL, MechanicalMaleAsianCount = NULL, MechanicalMaleBlackCount = NULL, MechanicalMaleHawaiianCount = NULL, MechanicalMaleHispanicCount = NULL, MechanicalMaleLEPCount = NULL, MechanicalMaleMultiRaceCount = NULL, MechanicalMaleSection504Count = NULL, MechanicalMaleWhiteCount = NULL, PhysicalFemaleAlaskanCount = NULL, PhysicalFemaleAsianCount = NULL, PhysicalFemaleBlackCount = NULL, PhysicalFemaleHawaiianCount = NULL, PhysicalFemaleHispanicCount = NULL, PhysicalFemaleLEPCount = NULL, PhysicalFemaleMultiRaceCount = NULL, PhysicalFemaleSection504Count = NULL, PhysicalFemaleWhiteCount = NULL, PhysicalMaleAlaskanCount = NULL, PhysicalMaleAsianCount = NULL, PhysicalMaleBlackCount = NULL, PhysicalMaleHawaiianCount = NULL, PhysicalMaleHispanicCount = NULL, PhysicalMaleLEPCount = NULL, PhysicalMaleMultiRaceCount = NULL, PhysicalMaleSection504Count = NULL, PhysicalMaleWhiteCount = NULL, SeclusionFemaleAlaskanCount = NULL, SeclusionFemaleAsianCount = NULL, SeclusionFemaleBlackCount = NULL, SeclusionFemaleHawaiianCount = NULL, SeclusionFemaleHispanicCount = NULL, SeclusionFemaleLEPCount = NULL, SeclusionFemaleMultiRaceCount = NULL, SeclusionFemaleSection504Count = NULL, SeclusionFemaleWhiteCount = NULL, SeclusionMaleAlaskanCount = NULL, SeclusionMaleAsianCount = NULL, SeclusionMaleBlackCount = NULL, SeclusionMaleHawaiianCount = NULL, SeclusionMaleHispanicCount = NULL, SeclusionMaleLEPCount = NULL, SeclusionMaleMultiRaceCount = NULL, SeclusionMaleSection504Count = NULL, SeclusionMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEARestraintV1", objectId = CRDCSchoolIDEARestraintV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolIDEARestraintV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolNonIDEARestraintV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolNonIDEARestraintV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolNonIDEARestraintV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolNonIDEARestraintV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolNonIDEARestraintV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolNonIDEARestraintV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolNonIDEARestraintV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolNonIDEARestraintV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, MechanicalFemaleAlaskanCount = F, MechanicalFemaleAsianCount = F, MechanicalFemaleBlackCount = F, MechanicalFemaleHawaiianCount = F, MechanicalFemaleHispanicCount = F, MechanicalFemaleLEPCount = F, MechanicalFemaleMultiRaceCount = F, MechanicalFemaleSection504Count = F, MechanicalFemaleWhiteCount = F, MechanicalMaleAlaskanCount = F, MechanicalMaleAsianCount = F, MechanicalMaleBlackCount = F, MechanicalMaleHawaiianCount = F, MechanicalMaleHispanicCount = F, MechanicalMaleLEPCount = F, MechanicalMaleMultiRaceCount = F, MechanicalMaleSection504Count = F, MechanicalMaleWhiteCount = F, ModifiedTime = F, PhysicalFemaleAlaskanCount = F, PhysicalFemaleAsianCount = F, PhysicalFemaleBlackCount = F, PhysicalFemaleHawaiianCount = F, PhysicalFemaleHispanicCount = F, PhysicalFemaleLEPCount = F, PhysicalFemaleMultiRaceCount = F, PhysicalFemaleSection504Count = F, PhysicalFemaleWhiteCount = F, PhysicalMaleAlaskanCount = F, PhysicalMaleAsianCount = F, PhysicalMaleBlackCount = F, PhysicalMaleHawaiianCount = F, PhysicalMaleHispanicCount = F, PhysicalMaleLEPCount = F, PhysicalMaleMultiRaceCount = F, PhysicalMaleSection504Count = F, PhysicalMaleWhiteCount = F, SeclusionFemaleAlaskanCount = F, SeclusionFemaleAsianCount = F, SeclusionFemaleBlackCount = F, SeclusionFemaleHawaiianCount = F, SeclusionFemaleHispanicCount = F, SeclusionFemaleLEPCount = F, SeclusionFemaleMultiRaceCount = F, SeclusionFemaleSection504Count = F, SeclusionFemaleWhiteCount = F, SeclusionMaleAlaskanCount = F, SeclusionMaleAsianCount = F, SeclusionMaleBlackCount = F, SeclusionMaleHawaiianCount = F, SeclusionMaleHispanicCount = F, SeclusionMaleLEPCount = F, SeclusionMaleMultiRaceCount = F, SeclusionMaleSection504Count = F, SeclusionMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolNonIDEARestraintV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolNonIDEARestraintV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolNonIDEARestraintV1
	#' @param CRDCSchoolNonIDEARestraintV1ID The ID of the CRDCSchoolNonIDEARestraintV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolNonIDEARestraintV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolNonIDEARestraintV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolNonIDEARestraintV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolNonIDEARestraintV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolNonIDEARestraintV1 <- function(CRDCSchoolNonIDEARestraintV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, MechanicalFemaleAlaskanCount = F, MechanicalFemaleAsianCount = F, MechanicalFemaleBlackCount = F, MechanicalFemaleHawaiianCount = F, MechanicalFemaleHispanicCount = F, MechanicalFemaleLEPCount = F, MechanicalFemaleMultiRaceCount = F, MechanicalFemaleSection504Count = F, MechanicalFemaleWhiteCount = F, MechanicalMaleAlaskanCount = F, MechanicalMaleAsianCount = F, MechanicalMaleBlackCount = F, MechanicalMaleHawaiianCount = F, MechanicalMaleHispanicCount = F, MechanicalMaleLEPCount = F, MechanicalMaleMultiRaceCount = F, MechanicalMaleSection504Count = F, MechanicalMaleWhiteCount = F, ModifiedTime = F, PhysicalFemaleAlaskanCount = F, PhysicalFemaleAsianCount = F, PhysicalFemaleBlackCount = F, PhysicalFemaleHawaiianCount = F, PhysicalFemaleHispanicCount = F, PhysicalFemaleLEPCount = F, PhysicalFemaleMultiRaceCount = F, PhysicalFemaleSection504Count = F, PhysicalFemaleWhiteCount = F, PhysicalMaleAlaskanCount = F, PhysicalMaleAsianCount = F, PhysicalMaleBlackCount = F, PhysicalMaleHawaiianCount = F, PhysicalMaleHispanicCount = F, PhysicalMaleLEPCount = F, PhysicalMaleMultiRaceCount = F, PhysicalMaleSection504Count = F, PhysicalMaleWhiteCount = F, SeclusionFemaleAlaskanCount = F, SeclusionFemaleAsianCount = F, SeclusionFemaleBlackCount = F, SeclusionFemaleHawaiianCount = F, SeclusionFemaleHispanicCount = F, SeclusionFemaleLEPCount = F, SeclusionFemaleMultiRaceCount = F, SeclusionFemaleSection504Count = F, SeclusionFemaleWhiteCount = F, SeclusionMaleAlaskanCount = F, SeclusionMaleAsianCount = F, SeclusionMaleBlackCount = F, SeclusionMaleHawaiianCount = F, SeclusionMaleHispanicCount = F, SeclusionMaleLEPCount = F, SeclusionMaleMultiRaceCount = F, SeclusionMaleSection504Count = F, SeclusionMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolNonIDEARestraintV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolNonIDEARestraintV1", objectId = CRDCSchoolNonIDEARestraintV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolNonIDEARestraintV1
	#'
	#' This function deletes a CRDCSchoolNonIDEARestraintV1
	#' @param CRDCSchoolNonIDEARestraintV1ID The ID of the CRDCSchoolNonIDEARestraintV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolNonIDEARestraintV1ID of the deleted CRDCSchoolNonIDEARestraintV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolNonIDEARestraintV1 <- function(CRDCSchoolNonIDEARestraintV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolNonIDEARestraintV1", objectId = CRDCSchoolNonIDEARestraintV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolNonIDEARestraintV1
	#'
	#' This function creates a CRDCSchoolNonIDEARestraintV1
	#' @param fieldNames The field values to give the created CRDCSchoolNonIDEARestraintV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolNonIDEARestraintV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolNonIDEARestraintV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, MechanicalFemaleAlaskanCount = NULL, MechanicalFemaleAsianCount = NULL, MechanicalFemaleBlackCount = NULL, MechanicalFemaleHawaiianCount = NULL, MechanicalFemaleHispanicCount = NULL, MechanicalFemaleLEPCount = NULL, MechanicalFemaleMultiRaceCount = NULL, MechanicalFemaleSection504Count = NULL, MechanicalFemaleWhiteCount = NULL, MechanicalMaleAlaskanCount = NULL, MechanicalMaleAsianCount = NULL, MechanicalMaleBlackCount = NULL, MechanicalMaleHawaiianCount = NULL, MechanicalMaleHispanicCount = NULL, MechanicalMaleLEPCount = NULL, MechanicalMaleMultiRaceCount = NULL, MechanicalMaleSection504Count = NULL, MechanicalMaleWhiteCount = NULL, PhysicalFemaleAlaskanCount = NULL, PhysicalFemaleAsianCount = NULL, PhysicalFemaleBlackCount = NULL, PhysicalFemaleHawaiianCount = NULL, PhysicalFemaleHispanicCount = NULL, PhysicalFemaleLEPCount = NULL, PhysicalFemaleMultiRaceCount = NULL, PhysicalFemaleSection504Count = NULL, PhysicalFemaleWhiteCount = NULL, PhysicalMaleAlaskanCount = NULL, PhysicalMaleAsianCount = NULL, PhysicalMaleBlackCount = NULL, PhysicalMaleHawaiianCount = NULL, PhysicalMaleHispanicCount = NULL, PhysicalMaleLEPCount = NULL, PhysicalMaleMultiRaceCount = NULL, PhysicalMaleSection504Count = NULL, PhysicalMaleWhiteCount = NULL, SeclusionFemaleAlaskanCount = NULL, SeclusionFemaleAsianCount = NULL, SeclusionFemaleBlackCount = NULL, SeclusionFemaleHawaiianCount = NULL, SeclusionFemaleHispanicCount = NULL, SeclusionFemaleLEPCount = NULL, SeclusionFemaleMultiRaceCount = NULL, SeclusionFemaleSection504Count = NULL, SeclusionFemaleWhiteCount = NULL, SeclusionMaleAlaskanCount = NULL, SeclusionMaleAsianCount = NULL, SeclusionMaleBlackCount = NULL, SeclusionMaleHawaiianCount = NULL, SeclusionMaleHispanicCount = NULL, SeclusionMaleLEPCount = NULL, SeclusionMaleMultiRaceCount = NULL, SeclusionMaleSection504Count = NULL, SeclusionMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolNonIDEARestraintV1", body = list(DataObject = body), searchFields = append("CRDCSchoolNonIDEARestraintV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolNonIDEARestraintV1
	#'
	#' This function modifies a CRDCSchoolNonIDEARestraintV1
	#' @param fieldNames The field values to give the modified CRDCSchoolNonIDEARestraintV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolNonIDEARestraintV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolNonIDEARestraintV1 <- function(CRDCSchoolNonIDEARestraintV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, MechanicalFemaleAlaskanCount = NULL, MechanicalFemaleAsianCount = NULL, MechanicalFemaleBlackCount = NULL, MechanicalFemaleHawaiianCount = NULL, MechanicalFemaleHispanicCount = NULL, MechanicalFemaleLEPCount = NULL, MechanicalFemaleMultiRaceCount = NULL, MechanicalFemaleSection504Count = NULL, MechanicalFemaleWhiteCount = NULL, MechanicalMaleAlaskanCount = NULL, MechanicalMaleAsianCount = NULL, MechanicalMaleBlackCount = NULL, MechanicalMaleHawaiianCount = NULL, MechanicalMaleHispanicCount = NULL, MechanicalMaleLEPCount = NULL, MechanicalMaleMultiRaceCount = NULL, MechanicalMaleSection504Count = NULL, MechanicalMaleWhiteCount = NULL, PhysicalFemaleAlaskanCount = NULL, PhysicalFemaleAsianCount = NULL, PhysicalFemaleBlackCount = NULL, PhysicalFemaleHawaiianCount = NULL, PhysicalFemaleHispanicCount = NULL, PhysicalFemaleLEPCount = NULL, PhysicalFemaleMultiRaceCount = NULL, PhysicalFemaleSection504Count = NULL, PhysicalFemaleWhiteCount = NULL, PhysicalMaleAlaskanCount = NULL, PhysicalMaleAsianCount = NULL, PhysicalMaleBlackCount = NULL, PhysicalMaleHawaiianCount = NULL, PhysicalMaleHispanicCount = NULL, PhysicalMaleLEPCount = NULL, PhysicalMaleMultiRaceCount = NULL, PhysicalMaleSection504Count = NULL, PhysicalMaleWhiteCount = NULL, SeclusionFemaleAlaskanCount = NULL, SeclusionFemaleAsianCount = NULL, SeclusionFemaleBlackCount = NULL, SeclusionFemaleHawaiianCount = NULL, SeclusionFemaleHispanicCount = NULL, SeclusionFemaleLEPCount = NULL, SeclusionFemaleMultiRaceCount = NULL, SeclusionFemaleSection504Count = NULL, SeclusionFemaleWhiteCount = NULL, SeclusionMaleAlaskanCount = NULL, SeclusionMaleAsianCount = NULL, SeclusionMaleBlackCount = NULL, SeclusionMaleHawaiianCount = NULL, SeclusionMaleHispanicCount = NULL, SeclusionMaleLEPCount = NULL, SeclusionMaleMultiRaceCount = NULL, SeclusionMaleSection504Count = NULL, SeclusionMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolNonIDEARestraintV1", objectId = CRDCSchoolNonIDEARestraintV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolNonIDEARestraintV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolFederalSalaryV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolFederalSalaryV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolFederalSalaryV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolFederalSalaryV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolFederalSalaryV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolFederalSalaryV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolFederalSalaryV1s <- function(searchConditionsList = NULL, AdministrationAmount = F, AdministrationFTE = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolFederalSalaryV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, InstructionalAideAmount = F, InstructionalAideFTE = F, ModifiedTime = F, NonPersonnelAmount = F, SupportStaffAmount = F, SupportStaffFTE = F, TeacherAmount = F, TotalPersonnelAmount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolFederalSalaryV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolFederalSalaryV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolFederalSalaryV1
	#' @param CRDCSchoolFederalSalaryV1ID The ID of the CRDCSchoolFederalSalaryV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolFederalSalaryV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolFederalSalaryV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolFederalSalaryV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolFederalSalaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolFederalSalaryV1 <- function(CRDCSchoolFederalSalaryV1ID, AdministrationAmount = F, AdministrationFTE = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, InstructionalAideAmount = F, InstructionalAideFTE = F, ModifiedTime = F, NonPersonnelAmount = F, SupportStaffAmount = F, SupportStaffFTE = F, TeacherAmount = F, TotalPersonnelAmount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolFederalSalaryV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolFederalSalaryV1", objectId = CRDCSchoolFederalSalaryV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolFederalSalaryV1
	#'
	#' This function deletes a CRDCSchoolFederalSalaryV1
	#' @param CRDCSchoolFederalSalaryV1ID The ID of the CRDCSchoolFederalSalaryV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolFederalSalaryV1ID of the deleted CRDCSchoolFederalSalaryV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolFederalSalaryV1 <- function(CRDCSchoolFederalSalaryV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolFederalSalaryV1", objectId = CRDCSchoolFederalSalaryV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolFederalSalaryV1
	#'
	#' This function creates a CRDCSchoolFederalSalaryV1
	#' @param fieldNames The field values to give the created CRDCSchoolFederalSalaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolFederalSalaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolFederalSalaryV1 <- function(AdministrationAmount = NULL, AdministrationFTE = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, InstructionalAideAmount = NULL, InstructionalAideFTE = NULL, NonPersonnelAmount = NULL, SupportStaffAmount = NULL, SupportStaffFTE = NULL, TeacherAmount = NULL, TotalPersonnelAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolFederalSalaryV1", body = list(DataObject = body), searchFields = append("CRDCSchoolFederalSalaryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolFederalSalaryV1
	#'
	#' This function modifies a CRDCSchoolFederalSalaryV1
	#' @param fieldNames The field values to give the modified CRDCSchoolFederalSalaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolFederalSalaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolFederalSalaryV1 <- function(CRDCSchoolFederalSalaryV1ID, AdministrationAmount = NULL, AdministrationFTE = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, InstructionalAideAmount = NULL, InstructionalAideFTE = NULL, NonPersonnelAmount = NULL, SupportStaffAmount = NULL, SupportStaffFTE = NULL, TeacherAmount = NULL, TotalPersonnelAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolFederalSalaryV1", objectId = CRDCSchoolFederalSalaryV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolFederalSalaryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolJusticeFacilityV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolJusticeFacilityV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolJusticeFacilityV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolJusticeFacilityV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolJusticeFacilityV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolJusticeFacilityV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolJusticeFacilityV1s <- function(searchConditionsList = NULL, Between15To30DaysStudentCount = F, Between31To90DaysStudentCount = F, Between91To180DaysStudentCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolJusticeFacilityV1ID = F, CreatedTime = F, DaysInSchoolYear = F, FacilityType = F, FederalJusticeFacilityTypeID = F, FederalNCESSchoolID = F, HoursPerWeek = F, LessThan15DaysStudentCount = F, ModifiedTime = F, MoreThan180DaysStudentCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolJusticeFacilityV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolJusticeFacilityV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolJusticeFacilityV1
	#' @param CRDCSchoolJusticeFacilityV1ID The ID of the CRDCSchoolJusticeFacilityV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolJusticeFacilityV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolJusticeFacilityV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolJusticeFacilityV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolJusticeFacilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolJusticeFacilityV1 <- function(CRDCSchoolJusticeFacilityV1ID, Between15To30DaysStudentCount = F, Between31To90DaysStudentCount = F, Between91To180DaysStudentCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DaysInSchoolYear = F, FacilityType = F, FederalJusticeFacilityTypeID = F, FederalNCESSchoolID = F, HoursPerWeek = F, LessThan15DaysStudentCount = F, ModifiedTime = F, MoreThan180DaysStudentCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolJusticeFacilityV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolJusticeFacilityV1", objectId = CRDCSchoolJusticeFacilityV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolJusticeFacilityV1
	#'
	#' This function deletes a CRDCSchoolJusticeFacilityV1
	#' @param CRDCSchoolJusticeFacilityV1ID The ID of the CRDCSchoolJusticeFacilityV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolJusticeFacilityV1ID of the deleted CRDCSchoolJusticeFacilityV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolJusticeFacilityV1 <- function(CRDCSchoolJusticeFacilityV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolJusticeFacilityV1", objectId = CRDCSchoolJusticeFacilityV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolJusticeFacilityV1
	#'
	#' This function creates a CRDCSchoolJusticeFacilityV1
	#' @param fieldNames The field values to give the created CRDCSchoolJusticeFacilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolJusticeFacilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolJusticeFacilityV1 <- function(Between15To30DaysStudentCount = NULL, Between31To90DaysStudentCount = NULL, Between91To180DaysStudentCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DaysInSchoolYear = NULL, FacilityType = NULL, FederalJusticeFacilityTypeID = NULL, FederalNCESSchoolID = NULL, HoursPerWeek = NULL, LessThan15DaysStudentCount = NULL, MoreThan180DaysStudentCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolJusticeFacilityV1", body = list(DataObject = body), searchFields = append("CRDCSchoolJusticeFacilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolJusticeFacilityV1
	#'
	#' This function modifies a CRDCSchoolJusticeFacilityV1
	#' @param fieldNames The field values to give the modified CRDCSchoolJusticeFacilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolJusticeFacilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolJusticeFacilityV1 <- function(CRDCSchoolJusticeFacilityV1ID, Between15To30DaysStudentCount = NULL, Between31To90DaysStudentCount = NULL, Between91To180DaysStudentCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DaysInSchoolYear = NULL, FacilityType = NULL, FederalJusticeFacilityTypeID = NULL, FederalNCESSchoolID = NULL, HoursPerWeek = NULL, LessThan15DaysStudentCount = NULL, MoreThan180DaysStudentCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolJusticeFacilityV1", objectId = CRDCSchoolJusticeFacilityV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolJusticeFacilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolPKCorporalPunishmentV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolPKCorporalPunishmentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolPKCorporalPunishmentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolPKCorporalPunishmentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolPKCorporalPunishmentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolPKCorporalPunishmentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolPKCorporalPunishmentV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolPKCorporalPunishmentV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, IDEANumberOfInstances = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, NumberOfInstances = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolPKCorporalPunishmentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolPKCorporalPunishmentV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolPKCorporalPunishmentV1
	#' @param CRDCSchoolPKCorporalPunishmentV1ID The ID of the CRDCSchoolPKCorporalPunishmentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolPKCorporalPunishmentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolPKCorporalPunishmentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolPKCorporalPunishmentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolPKCorporalPunishmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolPKCorporalPunishmentV1 <- function(CRDCSchoolPKCorporalPunishmentV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, IDEANumberOfInstances = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, NumberOfInstances = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolPKCorporalPunishmentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKCorporalPunishmentV1", objectId = CRDCSchoolPKCorporalPunishmentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolPKCorporalPunishmentV1
	#'
	#' This function deletes a CRDCSchoolPKCorporalPunishmentV1
	#' @param CRDCSchoolPKCorporalPunishmentV1ID The ID of the CRDCSchoolPKCorporalPunishmentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolPKCorporalPunishmentV1ID of the deleted CRDCSchoolPKCorporalPunishmentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolPKCorporalPunishmentV1 <- function(CRDCSchoolPKCorporalPunishmentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKCorporalPunishmentV1", objectId = CRDCSchoolPKCorporalPunishmentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolPKCorporalPunishmentV1
	#'
	#' This function creates a CRDCSchoolPKCorporalPunishmentV1
	#' @param fieldNames The field values to give the created CRDCSchoolPKCorporalPunishmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolPKCorporalPunishmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolPKCorporalPunishmentV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, IDEANumberOfInstances = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, NumberOfInstances = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKCorporalPunishmentV1", body = list(DataObject = body), searchFields = append("CRDCSchoolPKCorporalPunishmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolPKCorporalPunishmentV1
	#'
	#' This function modifies a CRDCSchoolPKCorporalPunishmentV1
	#' @param fieldNames The field values to give the modified CRDCSchoolPKCorporalPunishmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolPKCorporalPunishmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolPKCorporalPunishmentV1 <- function(CRDCSchoolPKCorporalPunishmentV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, IDEANumberOfInstances = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, NumberOfInstances = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKCorporalPunishmentV1", objectId = CRDCSchoolPKCorporalPunishmentV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolPKCorporalPunishmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolPKSuspensionExpulsionV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolPKSuspensionExpulsionV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolPKSuspensionExpulsionV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolPKSuspensionExpulsionV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolPKSuspensionExpulsionV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolPKSuspensionExpulsionV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolPKSuspensionExpulsionV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolPKSuspensionExpulsionV1ID = F, CreatedTime = F, ExpulsionFemaleAlaskanCount = F, ExpulsionFemaleAsianCount = F, ExpulsionFemaleBlackCount = F, ExpulsionFemaleHawaiianCount = F, ExpulsionFemaleHispanicCount = F, ExpulsionFemaleIDEACount = F, ExpulsionFemaleLEPCount = F, ExpulsionFemaleMultiRaceCount = F, ExpulsionFemaleWhiteCount = F, ExpulsionMaleAlaskanCount = F, ExpulsionMaleAsianCount = F, ExpulsionMaleBlackCount = F, ExpulsionMaleHawaiianCount = F, ExpulsionMaleHispanicCount = F, ExpulsionMaleIDEACount = F, ExpulsionMaleLEPCount = F, ExpulsionMaleMultiRaceCount = F, ExpulsionMaleWhiteCount = F, FederalNCESSchoolID = F, IDEANumberOfInstances = F, ModifiedTime = F, MultipleSuspensionsFemaleAlaskanCount = F, MultipleSuspensionsFemaleAsianCount = F, MultipleSuspensionsFemaleBlackCount = F, MultipleSuspensionsFemaleHawaiianCount = F, MultipleSuspensionsFemaleHispanicCount = F, MultipleSuspensionsFemaleIDEACount = F, MultipleSuspensionsFemaleLEPCount = F, MultipleSuspensionsFemaleMultiRaceCount = F, MultipleSuspensionsFemaleWhiteCount = F, MultipleSuspensionsMaleAlaskanCount = F, MultipleSuspensionsMaleAsianCount = F, MultipleSuspensionsMaleBlackCount = F, MultipleSuspensionsMaleHawaiianCount = F, MultipleSuspensionsMaleHispanicCount = F, MultipleSuspensionsMaleIDEACount = F, MultipleSuspensionsMaleLEPCount = F, MultipleSuspensionsMaleMultiRaceCount = F, MultipleSuspensionsMaleWhiteCount = F, NumberOfInstances = F, OneSuspensionFemaleAlaskanCount = F, OneSuspensionFemaleAsianCount = F, OneSuspensionFemaleBlackCount = F, OneSuspensionFemaleHawaiianCount = F, OneSuspensionFemaleHispanicCount = F, OneSuspensionFemaleIDEACount = F, OneSuspensionFemaleLEPCount = F, OneSuspensionFemaleMultiRaceCount = F, OneSuspensionFemaleWhiteCount = F, OneSuspensionMaleAlaskanCount = F, OneSuspensionMaleAsianCount = F, OneSuspensionMaleBlackCount = F, OneSuspensionMaleHawaiianCount = F, OneSuspensionMaleHispanicCount = F, OneSuspensionMaleIDEACount = F, OneSuspensionMaleLEPCount = F, OneSuspensionMaleMultiRaceCount = F, OneSuspensionMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolPKSuspensionExpulsionV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolPKSuspensionExpulsionV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolPKSuspensionExpulsionV1
	#' @param CRDCSchoolPKSuspensionExpulsionV1ID The ID of the CRDCSchoolPKSuspensionExpulsionV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolPKSuspensionExpulsionV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolPKSuspensionExpulsionV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolPKSuspensionExpulsionV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolPKSuspensionExpulsionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolPKSuspensionExpulsionV1 <- function(CRDCSchoolPKSuspensionExpulsionV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, ExpulsionFemaleAlaskanCount = F, ExpulsionFemaleAsianCount = F, ExpulsionFemaleBlackCount = F, ExpulsionFemaleHawaiianCount = F, ExpulsionFemaleHispanicCount = F, ExpulsionFemaleIDEACount = F, ExpulsionFemaleLEPCount = F, ExpulsionFemaleMultiRaceCount = F, ExpulsionFemaleWhiteCount = F, ExpulsionMaleAlaskanCount = F, ExpulsionMaleAsianCount = F, ExpulsionMaleBlackCount = F, ExpulsionMaleHawaiianCount = F, ExpulsionMaleHispanicCount = F, ExpulsionMaleIDEACount = F, ExpulsionMaleLEPCount = F, ExpulsionMaleMultiRaceCount = F, ExpulsionMaleWhiteCount = F, FederalNCESSchoolID = F, IDEANumberOfInstances = F, ModifiedTime = F, MultipleSuspensionsFemaleAlaskanCount = F, MultipleSuspensionsFemaleAsianCount = F, MultipleSuspensionsFemaleBlackCount = F, MultipleSuspensionsFemaleHawaiianCount = F, MultipleSuspensionsFemaleHispanicCount = F, MultipleSuspensionsFemaleIDEACount = F, MultipleSuspensionsFemaleLEPCount = F, MultipleSuspensionsFemaleMultiRaceCount = F, MultipleSuspensionsFemaleWhiteCount = F, MultipleSuspensionsMaleAlaskanCount = F, MultipleSuspensionsMaleAsianCount = F, MultipleSuspensionsMaleBlackCount = F, MultipleSuspensionsMaleHawaiianCount = F, MultipleSuspensionsMaleHispanicCount = F, MultipleSuspensionsMaleIDEACount = F, MultipleSuspensionsMaleLEPCount = F, MultipleSuspensionsMaleMultiRaceCount = F, MultipleSuspensionsMaleWhiteCount = F, NumberOfInstances = F, OneSuspensionFemaleAlaskanCount = F, OneSuspensionFemaleAsianCount = F, OneSuspensionFemaleBlackCount = F, OneSuspensionFemaleHawaiianCount = F, OneSuspensionFemaleHispanicCount = F, OneSuspensionFemaleIDEACount = F, OneSuspensionFemaleLEPCount = F, OneSuspensionFemaleMultiRaceCount = F, OneSuspensionFemaleWhiteCount = F, OneSuspensionMaleAlaskanCount = F, OneSuspensionMaleAsianCount = F, OneSuspensionMaleBlackCount = F, OneSuspensionMaleHawaiianCount = F, OneSuspensionMaleHispanicCount = F, OneSuspensionMaleIDEACount = F, OneSuspensionMaleLEPCount = F, OneSuspensionMaleMultiRaceCount = F, OneSuspensionMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolPKSuspensionExpulsionV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKSuspensionExpulsionV1", objectId = CRDCSchoolPKSuspensionExpulsionV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolPKSuspensionExpulsionV1
	#'
	#' This function deletes a CRDCSchoolPKSuspensionExpulsionV1
	#' @param CRDCSchoolPKSuspensionExpulsionV1ID The ID of the CRDCSchoolPKSuspensionExpulsionV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolPKSuspensionExpulsionV1ID of the deleted CRDCSchoolPKSuspensionExpulsionV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolPKSuspensionExpulsionV1 <- function(CRDCSchoolPKSuspensionExpulsionV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKSuspensionExpulsionV1", objectId = CRDCSchoolPKSuspensionExpulsionV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolPKSuspensionExpulsionV1
	#'
	#' This function creates a CRDCSchoolPKSuspensionExpulsionV1
	#' @param fieldNames The field values to give the created CRDCSchoolPKSuspensionExpulsionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolPKSuspensionExpulsionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolPKSuspensionExpulsionV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, ExpulsionFemaleAlaskanCount = NULL, ExpulsionFemaleAsianCount = NULL, ExpulsionFemaleBlackCount = NULL, ExpulsionFemaleHawaiianCount = NULL, ExpulsionFemaleHispanicCount = NULL, ExpulsionFemaleIDEACount = NULL, ExpulsionFemaleLEPCount = NULL, ExpulsionFemaleMultiRaceCount = NULL, ExpulsionFemaleWhiteCount = NULL, ExpulsionMaleAlaskanCount = NULL, ExpulsionMaleAsianCount = NULL, ExpulsionMaleBlackCount = NULL, ExpulsionMaleHawaiianCount = NULL, ExpulsionMaleHispanicCount = NULL, ExpulsionMaleIDEACount = NULL, ExpulsionMaleLEPCount = NULL, ExpulsionMaleMultiRaceCount = NULL, ExpulsionMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, IDEANumberOfInstances = NULL, MultipleSuspensionsFemaleAlaskanCount = NULL, MultipleSuspensionsFemaleAsianCount = NULL, MultipleSuspensionsFemaleBlackCount = NULL, MultipleSuspensionsFemaleHawaiianCount = NULL, MultipleSuspensionsFemaleHispanicCount = NULL, MultipleSuspensionsFemaleIDEACount = NULL, MultipleSuspensionsFemaleLEPCount = NULL, MultipleSuspensionsFemaleMultiRaceCount = NULL, MultipleSuspensionsFemaleWhiteCount = NULL, MultipleSuspensionsMaleAlaskanCount = NULL, MultipleSuspensionsMaleAsianCount = NULL, MultipleSuspensionsMaleBlackCount = NULL, MultipleSuspensionsMaleHawaiianCount = NULL, MultipleSuspensionsMaleHispanicCount = NULL, MultipleSuspensionsMaleIDEACount = NULL, MultipleSuspensionsMaleLEPCount = NULL, MultipleSuspensionsMaleMultiRaceCount = NULL, MultipleSuspensionsMaleWhiteCount = NULL, NumberOfInstances = NULL, OneSuspensionFemaleAlaskanCount = NULL, OneSuspensionFemaleAsianCount = NULL, OneSuspensionFemaleBlackCount = NULL, OneSuspensionFemaleHawaiianCount = NULL, OneSuspensionFemaleHispanicCount = NULL, OneSuspensionFemaleIDEACount = NULL, OneSuspensionFemaleLEPCount = NULL, OneSuspensionFemaleMultiRaceCount = NULL, OneSuspensionFemaleWhiteCount = NULL, OneSuspensionMaleAlaskanCount = NULL, OneSuspensionMaleAsianCount = NULL, OneSuspensionMaleBlackCount = NULL, OneSuspensionMaleHawaiianCount = NULL, OneSuspensionMaleHispanicCount = NULL, OneSuspensionMaleIDEACount = NULL, OneSuspensionMaleLEPCount = NULL, OneSuspensionMaleMultiRaceCount = NULL, OneSuspensionMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKSuspensionExpulsionV1", body = list(DataObject = body), searchFields = append("CRDCSchoolPKSuspensionExpulsionV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolPKSuspensionExpulsionV1
	#'
	#' This function modifies a CRDCSchoolPKSuspensionExpulsionV1
	#' @param fieldNames The field values to give the modified CRDCSchoolPKSuspensionExpulsionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolPKSuspensionExpulsionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolPKSuspensionExpulsionV1 <- function(CRDCSchoolPKSuspensionExpulsionV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, ExpulsionFemaleAlaskanCount = NULL, ExpulsionFemaleAsianCount = NULL, ExpulsionFemaleBlackCount = NULL, ExpulsionFemaleHawaiianCount = NULL, ExpulsionFemaleHispanicCount = NULL, ExpulsionFemaleIDEACount = NULL, ExpulsionFemaleLEPCount = NULL, ExpulsionFemaleMultiRaceCount = NULL, ExpulsionFemaleWhiteCount = NULL, ExpulsionMaleAlaskanCount = NULL, ExpulsionMaleAsianCount = NULL, ExpulsionMaleBlackCount = NULL, ExpulsionMaleHawaiianCount = NULL, ExpulsionMaleHispanicCount = NULL, ExpulsionMaleIDEACount = NULL, ExpulsionMaleLEPCount = NULL, ExpulsionMaleMultiRaceCount = NULL, ExpulsionMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, IDEANumberOfInstances = NULL, MultipleSuspensionsFemaleAlaskanCount = NULL, MultipleSuspensionsFemaleAsianCount = NULL, MultipleSuspensionsFemaleBlackCount = NULL, MultipleSuspensionsFemaleHawaiianCount = NULL, MultipleSuspensionsFemaleHispanicCount = NULL, MultipleSuspensionsFemaleIDEACount = NULL, MultipleSuspensionsFemaleLEPCount = NULL, MultipleSuspensionsFemaleMultiRaceCount = NULL, MultipleSuspensionsFemaleWhiteCount = NULL, MultipleSuspensionsMaleAlaskanCount = NULL, MultipleSuspensionsMaleAsianCount = NULL, MultipleSuspensionsMaleBlackCount = NULL, MultipleSuspensionsMaleHawaiianCount = NULL, MultipleSuspensionsMaleHispanicCount = NULL, MultipleSuspensionsMaleIDEACount = NULL, MultipleSuspensionsMaleLEPCount = NULL, MultipleSuspensionsMaleMultiRaceCount = NULL, MultipleSuspensionsMaleWhiteCount = NULL, NumberOfInstances = NULL, OneSuspensionFemaleAlaskanCount = NULL, OneSuspensionFemaleAsianCount = NULL, OneSuspensionFemaleBlackCount = NULL, OneSuspensionFemaleHawaiianCount = NULL, OneSuspensionFemaleHispanicCount = NULL, OneSuspensionFemaleIDEACount = NULL, OneSuspensionFemaleLEPCount = NULL, OneSuspensionFemaleMultiRaceCount = NULL, OneSuspensionFemaleWhiteCount = NULL, OneSuspensionMaleAlaskanCount = NULL, OneSuspensionMaleAsianCount = NULL, OneSuspensionMaleBlackCount = NULL, OneSuspensionMaleHawaiianCount = NULL, OneSuspensionMaleHispanicCount = NULL, OneSuspensionMaleIDEACount = NULL, OneSuspensionMaleLEPCount = NULL, OneSuspensionMaleMultiRaceCount = NULL, OneSuspensionMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPKSuspensionExpulsionV1", objectId = CRDCSchoolPKSuspensionExpulsionV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolPKSuspensionExpulsionV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolDaysMissedSuspensionV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolDaysMissedSuspensionV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDaysMissedSuspensionV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDaysMissedSuspensionV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDaysMissedSuspensionV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolDaysMissedSuspensionV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolDaysMissedSuspensionV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolDaysMissedSuspensionV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolDaysMissedSuspensionV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolDaysMissedSuspensionV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolDaysMissedSuspensionV1
	#' @param CRDCSchoolDaysMissedSuspensionV1ID The ID of the CRDCSchoolDaysMissedSuspensionV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDaysMissedSuspensionV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDaysMissedSuspensionV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDaysMissedSuspensionV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolDaysMissedSuspensionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolDaysMissedSuspensionV1 <- function(CRDCSchoolDaysMissedSuspensionV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolDaysMissedSuspensionV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDaysMissedSuspensionV1", objectId = CRDCSchoolDaysMissedSuspensionV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolDaysMissedSuspensionV1
	#'
	#' This function deletes a CRDCSchoolDaysMissedSuspensionV1
	#' @param CRDCSchoolDaysMissedSuspensionV1ID The ID of the CRDCSchoolDaysMissedSuspensionV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolDaysMissedSuspensionV1ID of the deleted CRDCSchoolDaysMissedSuspensionV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolDaysMissedSuspensionV1 <- function(CRDCSchoolDaysMissedSuspensionV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDaysMissedSuspensionV1", objectId = CRDCSchoolDaysMissedSuspensionV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolDaysMissedSuspensionV1
	#'
	#' This function creates a CRDCSchoolDaysMissedSuspensionV1
	#' @param fieldNames The field values to give the created CRDCSchoolDaysMissedSuspensionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolDaysMissedSuspensionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolDaysMissedSuspensionV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDaysMissedSuspensionV1", body = list(DataObject = body), searchFields = append("CRDCSchoolDaysMissedSuspensionV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolDaysMissedSuspensionV1
	#'
	#' This function modifies a CRDCSchoolDaysMissedSuspensionV1
	#' @param fieldNames The field values to give the modified CRDCSchoolDaysMissedSuspensionV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolDaysMissedSuspensionV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolDaysMissedSuspensionV1 <- function(CRDCSchoolDaysMissedSuspensionV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDaysMissedSuspensionV1", objectId = CRDCSchoolDaysMissedSuspensionV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolDaysMissedSuspensionV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolDisciplineCountV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolDisciplineCountV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDisciplineCountV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDisciplineCountV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDisciplineCountV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolDisciplineCountV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolDisciplineCountV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolDisciplineCountV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, IDEAOutOfSchoolCount = F, ModifiedTime = F, Section504OutOfSchoolCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WithDisabilityCorporalCount = F, WithoutDisabilityCorporalCount = F, WithoutDisabilityOutOfSchoolCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineCountV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolDisciplineCountV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolDisciplineCountV1
	#' @param CRDCSchoolDisciplineCountV1ID The ID of the CRDCSchoolDisciplineCountV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDisciplineCountV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDisciplineCountV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDisciplineCountV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolDisciplineCountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolDisciplineCountV1 <- function(CRDCSchoolDisciplineCountV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, IDEAOutOfSchoolCount = F, ModifiedTime = F, Section504OutOfSchoolCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WithDisabilityCorporalCount = F, WithoutDisabilityCorporalCount = F, WithoutDisabilityOutOfSchoolCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolDisciplineCountV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineCountV1", objectId = CRDCSchoolDisciplineCountV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolDisciplineCountV1
	#'
	#' This function deletes a CRDCSchoolDisciplineCountV1
	#' @param CRDCSchoolDisciplineCountV1ID The ID of the CRDCSchoolDisciplineCountV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolDisciplineCountV1ID of the deleted CRDCSchoolDisciplineCountV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolDisciplineCountV1 <- function(CRDCSchoolDisciplineCountV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineCountV1", objectId = CRDCSchoolDisciplineCountV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolDisciplineCountV1
	#'
	#' This function creates a CRDCSchoolDisciplineCountV1
	#' @param fieldNames The field values to give the created CRDCSchoolDisciplineCountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolDisciplineCountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolDisciplineCountV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, IDEAOutOfSchoolCount = NULL, Section504OutOfSchoolCount = NULL, WithDisabilityCorporalCount = NULL, WithoutDisabilityCorporalCount = NULL, WithoutDisabilityOutOfSchoolCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineCountV1", body = list(DataObject = body), searchFields = append("CRDCSchoolDisciplineCountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolDisciplineCountV1
	#'
	#' This function modifies a CRDCSchoolDisciplineCountV1
	#' @param fieldNames The field values to give the modified CRDCSchoolDisciplineCountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolDisciplineCountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolDisciplineCountV1 <- function(CRDCSchoolDisciplineCountV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, IDEAOutOfSchoolCount = NULL, Section504OutOfSchoolCount = NULL, WithDisabilityCorporalCount = NULL, WithoutDisabilityCorporalCount = NULL, WithoutDisabilityOutOfSchoolCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineCountV1", objectId = CRDCSchoolDisciplineCountV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolDisciplineCountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolDisciplineWithDisabilityV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolDisciplineWithDisabilityV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDisciplineWithDisabilityV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDisciplineWithDisabilityV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDisciplineWithDisabilityV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolDisciplineWithDisabilityV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolDisciplineWithDisabilityV1s <- function(searchConditionsList = NULL, ArrestFemaleAlaskanCount = F, ArrestFemaleAsianCount = F, ArrestFemaleBlackCount = F, ArrestFemaleHawaiianCount = F, ArrestFemaleHispanicCount = F, ArrestFemaleLEPCount = F, ArrestFemaleMultiRaceCount = F, ArrestFemaleSection504Count = F, ArrestFemaleWhiteCount = F, ArrestMaleAlaskanCount = F, ArrestMaleAsianCount = F, ArrestMaleBlackCount = F, ArrestMaleHawaiianCount = F, ArrestMaleHispanicCount = F, ArrestMaleLEPCount = F, ArrestMaleMultiRaceCount = F, ArrestMaleSection504Count = F, ArrestMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CorporalFemaleAlaskanCount = F, CorporalFemaleAsianCount = F, CorporalFemaleBlackCount = F, CorporalFemaleHawaiianCount = F, CorporalFemaleHispanicCount = F, CorporalFemaleLEPCount = F, CorporalFemaleMultiRaceCount = F, CorporalFemaleSection504Count = F, CorporalFemaleWhiteCount = F, CorporalMaleAlaskanCount = F, CorporalMaleAsianCount = F, CorporalMaleBlackCount = F, CorporalMaleHawaiianCount = F, CorporalMaleHispanicCount = F, CorporalMaleLEPCount = F, CorporalMaleMultiRaceCount = F, CorporalMaleSection504Count = F, CorporalMaleWhiteCount = F, CRDCSchoolDisciplineWithDisabilityV1ID = F, CreatedTime = F, ExpulsionNoServicesFemaleAlaskanCount = F, ExpulsionNoServicesFemaleAsianCount = F, ExpulsionNoServicesFemaleBlackCount = F, ExpulsionNoServicesFemaleHawaiianCount = F, ExpulsionNoServicesFemaleHispanicCount = F, ExpulsionNoServicesFemaleLEPCount = F, ExpulsionNoServicesFemaleMultiRaceCount = F, ExpulsionNoServicesFemaleSection504Count = F, ExpulsionNoServicesFemaleWhiteCount = F, ExpulsionNoServicesMaleAlaskanCount = F, ExpulsionNoServicesMaleAsianCount = F, ExpulsionNoServicesMaleBlackCount = F, ExpulsionNoServicesMaleHawaiianCount = F, ExpulsionNoServicesMaleHispanicCount = F, ExpulsionNoServicesMaleLEPCount = F, ExpulsionNoServicesMaleMultiRaceCount = F, ExpulsionNoServicesMaleSection504Count = F, ExpulsionNoServicesMaleWhiteCount = F, ExpulsionWithServicesFemaleAlaskanCount = F, ExpulsionWithServicesFemaleAsianCount = F, ExpulsionWithServicesFemaleBlackCount = F, ExpulsionWithServicesFemaleHawaiianCount = F, ExpulsionWithServicesFemaleHispanicCount = F, ExpulsionWithServicesFemaleLEPCount = F, ExpulsionWithServicesFemaleMultiRaceCount = F, ExpulsionWithServicesFemaleSection504Count = F, ExpulsionWithServicesFemaleWhiteCount = F, ExpulsionWithServicesMaleAlaskanCount = F, ExpulsionWithServicesMaleAsianCount = F, ExpulsionWithServicesMaleBlackCount = F, ExpulsionWithServicesMaleHawaiianCount = F, ExpulsionWithServicesMaleHispanicCount = F, ExpulsionWithServicesMaleLEPCount = F, ExpulsionWithServicesMaleMultiRaceCount = F, ExpulsionWithServicesMaleSection504Count = F, ExpulsionWithServicesMaleWhiteCount = F, FederalNCESSchoolID = F, InSchoolFemaleAlaskanCount = F, InSchoolFemaleAsianCount = F, InSchoolFemaleBlackCount = F, InSchoolFemaleHawaiianCount = F, InSchoolFemaleHispanicCount = F, InSchoolFemaleLEPCount = F, InSchoolFemaleMultiRaceCount = F, InSchoolFemaleSection504Count = F, InSchoolFemaleWhiteCount = F, InSchoolMaleAlaskanCount = F, InSchoolMaleAsianCount = F, InSchoolMaleBlackCount = F, InSchoolMaleHawaiianCount = F, InSchoolMaleHispanicCount = F, InSchoolMaleLEPCount = F, InSchoolMaleMultiRaceCount = F, InSchoolMaleSection504Count = F, InSchoolMaleWhiteCount = F, LawEnforcementFemaleAlaskanCount = F, LawEnforcementFemaleAsianCount = F, LawEnforcementFemaleBlackCount = F, LawEnforcementFemaleHawaiianCount = F, LawEnforcementFemaleHispanicCount = F, LawEnforcementFemaleLEPCount = F, LawEnforcementFemaleMultiRaceCount = F, LawEnforcementFemaleSection504Count = F, LawEnforcementFemaleWhiteCount = F, LawEnforcementMaleAlaskanCount = F, LawEnforcementMaleAsianCount = F, LawEnforcementMaleBlackCount = F, LawEnforcementMaleHawaiianCount = F, LawEnforcementMaleHispanicCount = F, LawEnforcementMaleLEPCount = F, LawEnforcementMaleMultiRaceCount = F, LawEnforcementMaleSection504Count = F, LawEnforcementMaleWhiteCount = F, ModifiedTime = F, MultipleOutOfSchoolFemaleAlaskanCount = F, MultipleOutOfSchoolFemaleAsianCount = F, MultipleOutOfSchoolFemaleBlackCount = F, MultipleOutOfSchoolFemaleHawaiianCount = F, MultipleOutOfSchoolFemaleHispanicCount = F, MultipleOutOfSchoolFemaleLEPCount = F, MultipleOutOfSchoolFemaleMultiRaceCount = F, MultipleOutOfSchoolFemaleSection504Count = F, MultipleOutOfSchoolFemaleWhiteCount = F, MultipleOutOfSchoolMaleAlaskanCount = F, MultipleOutOfSchoolMaleAsianCount = F, MultipleOutOfSchoolMaleBlackCount = F, MultipleOutOfSchoolMaleHawaiianCount = F, MultipleOutOfSchoolMaleHispanicCount = F, MultipleOutOfSchoolMaleLEPCount = F, MultipleOutOfSchoolMaleMultiRaceCount = F, MultipleOutOfSchoolMaleSection504Count = F, MultipleOutOfSchoolMaleWhiteCount = F, OneOutOfSchoolFemaleAlaskanCount = F, OneOutOfSchoolFemaleAsianCount = F, OneOutOfSchoolFemaleBlackCount = F, OneOutOfSchoolFemaleHawaiianCount = F, OneOutOfSchoolFemaleHispanicCount = F, OneOutOfSchoolFemaleLEPCount = F, OneOutOfSchoolFemaleMultiRaceCount = F, OneOutOfSchoolFemaleSection504Count = F, OneOutOfSchoolFemaleWhiteCount = F, OneOutOfSchoolMaleAlaskanCount = F, OneOutOfSchoolMaleAsianCount = F, OneOutOfSchoolMaleBlackCount = F, OneOutOfSchoolMaleHawaiianCount = F, OneOutOfSchoolMaleHispanicCount = F, OneOutOfSchoolMaleLEPCount = F, OneOutOfSchoolMaleMultiRaceCount = F, OneOutOfSchoolMaleSection504Count = F, OneOutOfSchoolMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZeroToleranceFemaleAlaskanCount = F, ZeroToleranceFemaleAsianCount = F, ZeroToleranceFemaleBlackCount = F, ZeroToleranceFemaleHawaiianCount = F, ZeroToleranceFemaleHispanicCount = F, ZeroToleranceFemaleLEPCount = F, ZeroToleranceFemaleMultiRaceCount = F, ZeroToleranceFemaleSection504Count = F, ZeroToleranceFemaleWhiteCount = F, ZeroToleranceMaleAlaskanCount = F, ZeroToleranceMaleAsianCount = F, ZeroToleranceMaleBlackCount = F, ZeroToleranceMaleHawaiianCount = F, ZeroToleranceMaleHispanicCount = F, ZeroToleranceMaleLEPCount = F, ZeroToleranceMaleMultiRaceCount = F, ZeroToleranceMaleSection504Count = F, ZeroToleranceMaleWhiteCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithDisabilityV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolDisciplineWithDisabilityV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolDisciplineWithDisabilityV1
	#' @param CRDCSchoolDisciplineWithDisabilityV1ID The ID of the CRDCSchoolDisciplineWithDisabilityV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDisciplineWithDisabilityV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDisciplineWithDisabilityV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDisciplineWithDisabilityV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolDisciplineWithDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolDisciplineWithDisabilityV1 <- function(CRDCSchoolDisciplineWithDisabilityV1ID, ArrestFemaleAlaskanCount = F, ArrestFemaleAsianCount = F, ArrestFemaleBlackCount = F, ArrestFemaleHawaiianCount = F, ArrestFemaleHispanicCount = F, ArrestFemaleLEPCount = F, ArrestFemaleMultiRaceCount = F, ArrestFemaleSection504Count = F, ArrestFemaleWhiteCount = F, ArrestMaleAlaskanCount = F, ArrestMaleAsianCount = F, ArrestMaleBlackCount = F, ArrestMaleHawaiianCount = F, ArrestMaleHispanicCount = F, ArrestMaleLEPCount = F, ArrestMaleMultiRaceCount = F, ArrestMaleSection504Count = F, ArrestMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CorporalFemaleAlaskanCount = F, CorporalFemaleAsianCount = F, CorporalFemaleBlackCount = F, CorporalFemaleHawaiianCount = F, CorporalFemaleHispanicCount = F, CorporalFemaleLEPCount = F, CorporalFemaleMultiRaceCount = F, CorporalFemaleSection504Count = F, CorporalFemaleWhiteCount = F, CorporalMaleAlaskanCount = F, CorporalMaleAsianCount = F, CorporalMaleBlackCount = F, CorporalMaleHawaiianCount = F, CorporalMaleHispanicCount = F, CorporalMaleLEPCount = F, CorporalMaleMultiRaceCount = F, CorporalMaleSection504Count = F, CorporalMaleWhiteCount = F, CreatedTime = F, ExpulsionNoServicesFemaleAlaskanCount = F, ExpulsionNoServicesFemaleAsianCount = F, ExpulsionNoServicesFemaleBlackCount = F, ExpulsionNoServicesFemaleHawaiianCount = F, ExpulsionNoServicesFemaleHispanicCount = F, ExpulsionNoServicesFemaleLEPCount = F, ExpulsionNoServicesFemaleMultiRaceCount = F, ExpulsionNoServicesFemaleSection504Count = F, ExpulsionNoServicesFemaleWhiteCount = F, ExpulsionNoServicesMaleAlaskanCount = F, ExpulsionNoServicesMaleAsianCount = F, ExpulsionNoServicesMaleBlackCount = F, ExpulsionNoServicesMaleHawaiianCount = F, ExpulsionNoServicesMaleHispanicCount = F, ExpulsionNoServicesMaleLEPCount = F, ExpulsionNoServicesMaleMultiRaceCount = F, ExpulsionNoServicesMaleSection504Count = F, ExpulsionNoServicesMaleWhiteCount = F, ExpulsionWithServicesFemaleAlaskanCount = F, ExpulsionWithServicesFemaleAsianCount = F, ExpulsionWithServicesFemaleBlackCount = F, ExpulsionWithServicesFemaleHawaiianCount = F, ExpulsionWithServicesFemaleHispanicCount = F, ExpulsionWithServicesFemaleLEPCount = F, ExpulsionWithServicesFemaleMultiRaceCount = F, ExpulsionWithServicesFemaleSection504Count = F, ExpulsionWithServicesFemaleWhiteCount = F, ExpulsionWithServicesMaleAlaskanCount = F, ExpulsionWithServicesMaleAsianCount = F, ExpulsionWithServicesMaleBlackCount = F, ExpulsionWithServicesMaleHawaiianCount = F, ExpulsionWithServicesMaleHispanicCount = F, ExpulsionWithServicesMaleLEPCount = F, ExpulsionWithServicesMaleMultiRaceCount = F, ExpulsionWithServicesMaleSection504Count = F, ExpulsionWithServicesMaleWhiteCount = F, FederalNCESSchoolID = F, InSchoolFemaleAlaskanCount = F, InSchoolFemaleAsianCount = F, InSchoolFemaleBlackCount = F, InSchoolFemaleHawaiianCount = F, InSchoolFemaleHispanicCount = F, InSchoolFemaleLEPCount = F, InSchoolFemaleMultiRaceCount = F, InSchoolFemaleSection504Count = F, InSchoolFemaleWhiteCount = F, InSchoolMaleAlaskanCount = F, InSchoolMaleAsianCount = F, InSchoolMaleBlackCount = F, InSchoolMaleHawaiianCount = F, InSchoolMaleHispanicCount = F, InSchoolMaleLEPCount = F, InSchoolMaleMultiRaceCount = F, InSchoolMaleSection504Count = F, InSchoolMaleWhiteCount = F, LawEnforcementFemaleAlaskanCount = F, LawEnforcementFemaleAsianCount = F, LawEnforcementFemaleBlackCount = F, LawEnforcementFemaleHawaiianCount = F, LawEnforcementFemaleHispanicCount = F, LawEnforcementFemaleLEPCount = F, LawEnforcementFemaleMultiRaceCount = F, LawEnforcementFemaleSection504Count = F, LawEnforcementFemaleWhiteCount = F, LawEnforcementMaleAlaskanCount = F, LawEnforcementMaleAsianCount = F, LawEnforcementMaleBlackCount = F, LawEnforcementMaleHawaiianCount = F, LawEnforcementMaleHispanicCount = F, LawEnforcementMaleLEPCount = F, LawEnforcementMaleMultiRaceCount = F, LawEnforcementMaleSection504Count = F, LawEnforcementMaleWhiteCount = F, ModifiedTime = F, MultipleOutOfSchoolFemaleAlaskanCount = F, MultipleOutOfSchoolFemaleAsianCount = F, MultipleOutOfSchoolFemaleBlackCount = F, MultipleOutOfSchoolFemaleHawaiianCount = F, MultipleOutOfSchoolFemaleHispanicCount = F, MultipleOutOfSchoolFemaleLEPCount = F, MultipleOutOfSchoolFemaleMultiRaceCount = F, MultipleOutOfSchoolFemaleSection504Count = F, MultipleOutOfSchoolFemaleWhiteCount = F, MultipleOutOfSchoolMaleAlaskanCount = F, MultipleOutOfSchoolMaleAsianCount = F, MultipleOutOfSchoolMaleBlackCount = F, MultipleOutOfSchoolMaleHawaiianCount = F, MultipleOutOfSchoolMaleHispanicCount = F, MultipleOutOfSchoolMaleLEPCount = F, MultipleOutOfSchoolMaleMultiRaceCount = F, MultipleOutOfSchoolMaleSection504Count = F, MultipleOutOfSchoolMaleWhiteCount = F, OneOutOfSchoolFemaleAlaskanCount = F, OneOutOfSchoolFemaleAsianCount = F, OneOutOfSchoolFemaleBlackCount = F, OneOutOfSchoolFemaleHawaiianCount = F, OneOutOfSchoolFemaleHispanicCount = F, OneOutOfSchoolFemaleLEPCount = F, OneOutOfSchoolFemaleMultiRaceCount = F, OneOutOfSchoolFemaleSection504Count = F, OneOutOfSchoolFemaleWhiteCount = F, OneOutOfSchoolMaleAlaskanCount = F, OneOutOfSchoolMaleAsianCount = F, OneOutOfSchoolMaleBlackCount = F, OneOutOfSchoolMaleHawaiianCount = F, OneOutOfSchoolMaleHispanicCount = F, OneOutOfSchoolMaleLEPCount = F, OneOutOfSchoolMaleMultiRaceCount = F, OneOutOfSchoolMaleSection504Count = F, OneOutOfSchoolMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZeroToleranceFemaleAlaskanCount = F, ZeroToleranceFemaleAsianCount = F, ZeroToleranceFemaleBlackCount = F, ZeroToleranceFemaleHawaiianCount = F, ZeroToleranceFemaleHispanicCount = F, ZeroToleranceFemaleLEPCount = F, ZeroToleranceFemaleMultiRaceCount = F, ZeroToleranceFemaleSection504Count = F, ZeroToleranceFemaleWhiteCount = F, ZeroToleranceMaleAlaskanCount = F, ZeroToleranceMaleAsianCount = F, ZeroToleranceMaleBlackCount = F, ZeroToleranceMaleHawaiianCount = F, ZeroToleranceMaleHispanicCount = F, ZeroToleranceMaleLEPCount = F, ZeroToleranceMaleMultiRaceCount = F, ZeroToleranceMaleSection504Count = F, ZeroToleranceMaleWhiteCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolDisciplineWithDisabilityV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithDisabilityV1", objectId = CRDCSchoolDisciplineWithDisabilityV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolDisciplineWithDisabilityV1
	#'
	#' This function deletes a CRDCSchoolDisciplineWithDisabilityV1
	#' @param CRDCSchoolDisciplineWithDisabilityV1ID The ID of the CRDCSchoolDisciplineWithDisabilityV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolDisciplineWithDisabilityV1ID of the deleted CRDCSchoolDisciplineWithDisabilityV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolDisciplineWithDisabilityV1 <- function(CRDCSchoolDisciplineWithDisabilityV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithDisabilityV1", objectId = CRDCSchoolDisciplineWithDisabilityV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolDisciplineWithDisabilityV1
	#'
	#' This function creates a CRDCSchoolDisciplineWithDisabilityV1
	#' @param fieldNames The field values to give the created CRDCSchoolDisciplineWithDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolDisciplineWithDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolDisciplineWithDisabilityV1 <- function(ArrestFemaleAlaskanCount = NULL, ArrestFemaleAsianCount = NULL, ArrestFemaleBlackCount = NULL, ArrestFemaleHawaiianCount = NULL, ArrestFemaleHispanicCount = NULL, ArrestFemaleLEPCount = NULL, ArrestFemaleMultiRaceCount = NULL, ArrestFemaleSection504Count = NULL, ArrestFemaleWhiteCount = NULL, ArrestMaleAlaskanCount = NULL, ArrestMaleAsianCount = NULL, ArrestMaleBlackCount = NULL, ArrestMaleHawaiianCount = NULL, ArrestMaleHispanicCount = NULL, ArrestMaleLEPCount = NULL, ArrestMaleMultiRaceCount = NULL, ArrestMaleSection504Count = NULL, ArrestMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CorporalFemaleAlaskanCount = NULL, CorporalFemaleAsianCount = NULL, CorporalFemaleBlackCount = NULL, CorporalFemaleHawaiianCount = NULL, CorporalFemaleHispanicCount = NULL, CorporalFemaleLEPCount = NULL, CorporalFemaleMultiRaceCount = NULL, CorporalFemaleSection504Count = NULL, CorporalFemaleWhiteCount = NULL, CorporalMaleAlaskanCount = NULL, CorporalMaleAsianCount = NULL, CorporalMaleBlackCount = NULL, CorporalMaleHawaiianCount = NULL, CorporalMaleHispanicCount = NULL, CorporalMaleLEPCount = NULL, CorporalMaleMultiRaceCount = NULL, CorporalMaleSection504Count = NULL, CorporalMaleWhiteCount = NULL, ExpulsionNoServicesFemaleAlaskanCount = NULL, ExpulsionNoServicesFemaleAsianCount = NULL, ExpulsionNoServicesFemaleBlackCount = NULL, ExpulsionNoServicesFemaleHawaiianCount = NULL, ExpulsionNoServicesFemaleHispanicCount = NULL, ExpulsionNoServicesFemaleLEPCount = NULL, ExpulsionNoServicesFemaleMultiRaceCount = NULL, ExpulsionNoServicesFemaleSection504Count = NULL, ExpulsionNoServicesFemaleWhiteCount = NULL, ExpulsionNoServicesMaleAlaskanCount = NULL, ExpulsionNoServicesMaleAsianCount = NULL, ExpulsionNoServicesMaleBlackCount = NULL, ExpulsionNoServicesMaleHawaiianCount = NULL, ExpulsionNoServicesMaleHispanicCount = NULL, ExpulsionNoServicesMaleLEPCount = NULL, ExpulsionNoServicesMaleMultiRaceCount = NULL, ExpulsionNoServicesMaleSection504Count = NULL, ExpulsionNoServicesMaleWhiteCount = NULL, ExpulsionWithServicesFemaleAlaskanCount = NULL, ExpulsionWithServicesFemaleAsianCount = NULL, ExpulsionWithServicesFemaleBlackCount = NULL, ExpulsionWithServicesFemaleHawaiianCount = NULL, ExpulsionWithServicesFemaleHispanicCount = NULL, ExpulsionWithServicesFemaleLEPCount = NULL, ExpulsionWithServicesFemaleMultiRaceCount = NULL, ExpulsionWithServicesFemaleSection504Count = NULL, ExpulsionWithServicesFemaleWhiteCount = NULL, ExpulsionWithServicesMaleAlaskanCount = NULL, ExpulsionWithServicesMaleAsianCount = NULL, ExpulsionWithServicesMaleBlackCount = NULL, ExpulsionWithServicesMaleHawaiianCount = NULL, ExpulsionWithServicesMaleHispanicCount = NULL, ExpulsionWithServicesMaleLEPCount = NULL, ExpulsionWithServicesMaleMultiRaceCount = NULL, ExpulsionWithServicesMaleSection504Count = NULL, ExpulsionWithServicesMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, InSchoolFemaleAlaskanCount = NULL, InSchoolFemaleAsianCount = NULL, InSchoolFemaleBlackCount = NULL, InSchoolFemaleHawaiianCount = NULL, InSchoolFemaleHispanicCount = NULL, InSchoolFemaleLEPCount = NULL, InSchoolFemaleMultiRaceCount = NULL, InSchoolFemaleSection504Count = NULL, InSchoolFemaleWhiteCount = NULL, InSchoolMaleAlaskanCount = NULL, InSchoolMaleAsianCount = NULL, InSchoolMaleBlackCount = NULL, InSchoolMaleHawaiianCount = NULL, InSchoolMaleHispanicCount = NULL, InSchoolMaleLEPCount = NULL, InSchoolMaleMultiRaceCount = NULL, InSchoolMaleSection504Count = NULL, InSchoolMaleWhiteCount = NULL, LawEnforcementFemaleAlaskanCount = NULL, LawEnforcementFemaleAsianCount = NULL, LawEnforcementFemaleBlackCount = NULL, LawEnforcementFemaleHawaiianCount = NULL, LawEnforcementFemaleHispanicCount = NULL, LawEnforcementFemaleLEPCount = NULL, LawEnforcementFemaleMultiRaceCount = NULL, LawEnforcementFemaleSection504Count = NULL, LawEnforcementFemaleWhiteCount = NULL, LawEnforcementMaleAlaskanCount = NULL, LawEnforcementMaleAsianCount = NULL, LawEnforcementMaleBlackCount = NULL, LawEnforcementMaleHawaiianCount = NULL, LawEnforcementMaleHispanicCount = NULL, LawEnforcementMaleLEPCount = NULL, LawEnforcementMaleMultiRaceCount = NULL, LawEnforcementMaleSection504Count = NULL, LawEnforcementMaleWhiteCount = NULL, MultipleOutOfSchoolFemaleAlaskanCount = NULL, MultipleOutOfSchoolFemaleAsianCount = NULL, MultipleOutOfSchoolFemaleBlackCount = NULL, MultipleOutOfSchoolFemaleHawaiianCount = NULL, MultipleOutOfSchoolFemaleHispanicCount = NULL, MultipleOutOfSchoolFemaleLEPCount = NULL, MultipleOutOfSchoolFemaleMultiRaceCount = NULL, MultipleOutOfSchoolFemaleSection504Count = NULL, MultipleOutOfSchoolFemaleWhiteCount = NULL, MultipleOutOfSchoolMaleAlaskanCount = NULL, MultipleOutOfSchoolMaleAsianCount = NULL, MultipleOutOfSchoolMaleBlackCount = NULL, MultipleOutOfSchoolMaleHawaiianCount = NULL, MultipleOutOfSchoolMaleHispanicCount = NULL, MultipleOutOfSchoolMaleLEPCount = NULL, MultipleOutOfSchoolMaleMultiRaceCount = NULL, MultipleOutOfSchoolMaleSection504Count = NULL, MultipleOutOfSchoolMaleWhiteCount = NULL, OneOutOfSchoolFemaleAlaskanCount = NULL, OneOutOfSchoolFemaleAsianCount = NULL, OneOutOfSchoolFemaleBlackCount = NULL, OneOutOfSchoolFemaleHawaiianCount = NULL, OneOutOfSchoolFemaleHispanicCount = NULL, OneOutOfSchoolFemaleLEPCount = NULL, OneOutOfSchoolFemaleMultiRaceCount = NULL, OneOutOfSchoolFemaleSection504Count = NULL, OneOutOfSchoolFemaleWhiteCount = NULL, OneOutOfSchoolMaleAlaskanCount = NULL, OneOutOfSchoolMaleAsianCount = NULL, OneOutOfSchoolMaleBlackCount = NULL, OneOutOfSchoolMaleHawaiianCount = NULL, OneOutOfSchoolMaleHispanicCount = NULL, OneOutOfSchoolMaleLEPCount = NULL, OneOutOfSchoolMaleMultiRaceCount = NULL, OneOutOfSchoolMaleSection504Count = NULL, OneOutOfSchoolMaleWhiteCount = NULL, ZeroToleranceFemaleAlaskanCount = NULL, ZeroToleranceFemaleAsianCount = NULL, ZeroToleranceFemaleBlackCount = NULL, ZeroToleranceFemaleHawaiianCount = NULL, ZeroToleranceFemaleHispanicCount = NULL, ZeroToleranceFemaleLEPCount = NULL, ZeroToleranceFemaleMultiRaceCount = NULL, ZeroToleranceFemaleSection504Count = NULL, ZeroToleranceFemaleWhiteCount = NULL, ZeroToleranceMaleAlaskanCount = NULL, ZeroToleranceMaleAsianCount = NULL, ZeroToleranceMaleBlackCount = NULL, ZeroToleranceMaleHawaiianCount = NULL, ZeroToleranceMaleHispanicCount = NULL, ZeroToleranceMaleLEPCount = NULL, ZeroToleranceMaleMultiRaceCount = NULL, ZeroToleranceMaleSection504Count = NULL, ZeroToleranceMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithDisabilityV1", body = list(DataObject = body), searchFields = append("CRDCSchoolDisciplineWithDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolDisciplineWithDisabilityV1
	#'
	#' This function modifies a CRDCSchoolDisciplineWithDisabilityV1
	#' @param fieldNames The field values to give the modified CRDCSchoolDisciplineWithDisabilityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolDisciplineWithDisabilityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolDisciplineWithDisabilityV1 <- function(CRDCSchoolDisciplineWithDisabilityV1ID, ArrestFemaleAlaskanCount = NULL, ArrestFemaleAsianCount = NULL, ArrestFemaleBlackCount = NULL, ArrestFemaleHawaiianCount = NULL, ArrestFemaleHispanicCount = NULL, ArrestFemaleLEPCount = NULL, ArrestFemaleMultiRaceCount = NULL, ArrestFemaleSection504Count = NULL, ArrestFemaleWhiteCount = NULL, ArrestMaleAlaskanCount = NULL, ArrestMaleAsianCount = NULL, ArrestMaleBlackCount = NULL, ArrestMaleHawaiianCount = NULL, ArrestMaleHispanicCount = NULL, ArrestMaleLEPCount = NULL, ArrestMaleMultiRaceCount = NULL, ArrestMaleSection504Count = NULL, ArrestMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CorporalFemaleAlaskanCount = NULL, CorporalFemaleAsianCount = NULL, CorporalFemaleBlackCount = NULL, CorporalFemaleHawaiianCount = NULL, CorporalFemaleHispanicCount = NULL, CorporalFemaleLEPCount = NULL, CorporalFemaleMultiRaceCount = NULL, CorporalFemaleSection504Count = NULL, CorporalFemaleWhiteCount = NULL, CorporalMaleAlaskanCount = NULL, CorporalMaleAsianCount = NULL, CorporalMaleBlackCount = NULL, CorporalMaleHawaiianCount = NULL, CorporalMaleHispanicCount = NULL, CorporalMaleLEPCount = NULL, CorporalMaleMultiRaceCount = NULL, CorporalMaleSection504Count = NULL, CorporalMaleWhiteCount = NULL, ExpulsionNoServicesFemaleAlaskanCount = NULL, ExpulsionNoServicesFemaleAsianCount = NULL, ExpulsionNoServicesFemaleBlackCount = NULL, ExpulsionNoServicesFemaleHawaiianCount = NULL, ExpulsionNoServicesFemaleHispanicCount = NULL, ExpulsionNoServicesFemaleLEPCount = NULL, ExpulsionNoServicesFemaleMultiRaceCount = NULL, ExpulsionNoServicesFemaleSection504Count = NULL, ExpulsionNoServicesFemaleWhiteCount = NULL, ExpulsionNoServicesMaleAlaskanCount = NULL, ExpulsionNoServicesMaleAsianCount = NULL, ExpulsionNoServicesMaleBlackCount = NULL, ExpulsionNoServicesMaleHawaiianCount = NULL, ExpulsionNoServicesMaleHispanicCount = NULL, ExpulsionNoServicesMaleLEPCount = NULL, ExpulsionNoServicesMaleMultiRaceCount = NULL, ExpulsionNoServicesMaleSection504Count = NULL, ExpulsionNoServicesMaleWhiteCount = NULL, ExpulsionWithServicesFemaleAlaskanCount = NULL, ExpulsionWithServicesFemaleAsianCount = NULL, ExpulsionWithServicesFemaleBlackCount = NULL, ExpulsionWithServicesFemaleHawaiianCount = NULL, ExpulsionWithServicesFemaleHispanicCount = NULL, ExpulsionWithServicesFemaleLEPCount = NULL, ExpulsionWithServicesFemaleMultiRaceCount = NULL, ExpulsionWithServicesFemaleSection504Count = NULL, ExpulsionWithServicesFemaleWhiteCount = NULL, ExpulsionWithServicesMaleAlaskanCount = NULL, ExpulsionWithServicesMaleAsianCount = NULL, ExpulsionWithServicesMaleBlackCount = NULL, ExpulsionWithServicesMaleHawaiianCount = NULL, ExpulsionWithServicesMaleHispanicCount = NULL, ExpulsionWithServicesMaleLEPCount = NULL, ExpulsionWithServicesMaleMultiRaceCount = NULL, ExpulsionWithServicesMaleSection504Count = NULL, ExpulsionWithServicesMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, InSchoolFemaleAlaskanCount = NULL, InSchoolFemaleAsianCount = NULL, InSchoolFemaleBlackCount = NULL, InSchoolFemaleHawaiianCount = NULL, InSchoolFemaleHispanicCount = NULL, InSchoolFemaleLEPCount = NULL, InSchoolFemaleMultiRaceCount = NULL, InSchoolFemaleSection504Count = NULL, InSchoolFemaleWhiteCount = NULL, InSchoolMaleAlaskanCount = NULL, InSchoolMaleAsianCount = NULL, InSchoolMaleBlackCount = NULL, InSchoolMaleHawaiianCount = NULL, InSchoolMaleHispanicCount = NULL, InSchoolMaleLEPCount = NULL, InSchoolMaleMultiRaceCount = NULL, InSchoolMaleSection504Count = NULL, InSchoolMaleWhiteCount = NULL, LawEnforcementFemaleAlaskanCount = NULL, LawEnforcementFemaleAsianCount = NULL, LawEnforcementFemaleBlackCount = NULL, LawEnforcementFemaleHawaiianCount = NULL, LawEnforcementFemaleHispanicCount = NULL, LawEnforcementFemaleLEPCount = NULL, LawEnforcementFemaleMultiRaceCount = NULL, LawEnforcementFemaleSection504Count = NULL, LawEnforcementFemaleWhiteCount = NULL, LawEnforcementMaleAlaskanCount = NULL, LawEnforcementMaleAsianCount = NULL, LawEnforcementMaleBlackCount = NULL, LawEnforcementMaleHawaiianCount = NULL, LawEnforcementMaleHispanicCount = NULL, LawEnforcementMaleLEPCount = NULL, LawEnforcementMaleMultiRaceCount = NULL, LawEnforcementMaleSection504Count = NULL, LawEnforcementMaleWhiteCount = NULL, MultipleOutOfSchoolFemaleAlaskanCount = NULL, MultipleOutOfSchoolFemaleAsianCount = NULL, MultipleOutOfSchoolFemaleBlackCount = NULL, MultipleOutOfSchoolFemaleHawaiianCount = NULL, MultipleOutOfSchoolFemaleHispanicCount = NULL, MultipleOutOfSchoolFemaleLEPCount = NULL, MultipleOutOfSchoolFemaleMultiRaceCount = NULL, MultipleOutOfSchoolFemaleSection504Count = NULL, MultipleOutOfSchoolFemaleWhiteCount = NULL, MultipleOutOfSchoolMaleAlaskanCount = NULL, MultipleOutOfSchoolMaleAsianCount = NULL, MultipleOutOfSchoolMaleBlackCount = NULL, MultipleOutOfSchoolMaleHawaiianCount = NULL, MultipleOutOfSchoolMaleHispanicCount = NULL, MultipleOutOfSchoolMaleLEPCount = NULL, MultipleOutOfSchoolMaleMultiRaceCount = NULL, MultipleOutOfSchoolMaleSection504Count = NULL, MultipleOutOfSchoolMaleWhiteCount = NULL, OneOutOfSchoolFemaleAlaskanCount = NULL, OneOutOfSchoolFemaleAsianCount = NULL, OneOutOfSchoolFemaleBlackCount = NULL, OneOutOfSchoolFemaleHawaiianCount = NULL, OneOutOfSchoolFemaleHispanicCount = NULL, OneOutOfSchoolFemaleLEPCount = NULL, OneOutOfSchoolFemaleMultiRaceCount = NULL, OneOutOfSchoolFemaleSection504Count = NULL, OneOutOfSchoolFemaleWhiteCount = NULL, OneOutOfSchoolMaleAlaskanCount = NULL, OneOutOfSchoolMaleAsianCount = NULL, OneOutOfSchoolMaleBlackCount = NULL, OneOutOfSchoolMaleHawaiianCount = NULL, OneOutOfSchoolMaleHispanicCount = NULL, OneOutOfSchoolMaleLEPCount = NULL, OneOutOfSchoolMaleMultiRaceCount = NULL, OneOutOfSchoolMaleSection504Count = NULL, OneOutOfSchoolMaleWhiteCount = NULL, ZeroToleranceFemaleAlaskanCount = NULL, ZeroToleranceFemaleAsianCount = NULL, ZeroToleranceFemaleBlackCount = NULL, ZeroToleranceFemaleHawaiianCount = NULL, ZeroToleranceFemaleHispanicCount = NULL, ZeroToleranceFemaleLEPCount = NULL, ZeroToleranceFemaleMultiRaceCount = NULL, ZeroToleranceFemaleSection504Count = NULL, ZeroToleranceFemaleWhiteCount = NULL, ZeroToleranceMaleAlaskanCount = NULL, ZeroToleranceMaleAsianCount = NULL, ZeroToleranceMaleBlackCount = NULL, ZeroToleranceMaleHawaiianCount = NULL, ZeroToleranceMaleHispanicCount = NULL, ZeroToleranceMaleLEPCount = NULL, ZeroToleranceMaleMultiRaceCount = NULL, ZeroToleranceMaleSection504Count = NULL, ZeroToleranceMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDisciplineWithDisabilityV1", objectId = CRDCSchoolDisciplineWithDisabilityV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolDisciplineWithDisabilityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolStateSalaryV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolStateSalaryV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolStateSalaryV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolStateSalaryV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolStateSalaryV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolStateSalaryV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolStateSalaryV1s <- function(searchConditionsList = NULL, AdministrationAmount = F, AdministrationFTE = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolStateSalaryV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, InstructionalAideAmount = F, InstructionalAideFTE = F, ModifiedTime = F, NonPersonnelAmount = F, SupportStaffAmount = F, SupportStaffFTE = F, TeacherAmount = F, TeacherFTE = F, TotalPersonnelAmount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolStateSalaryV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolStateSalaryV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolStateSalaryV1
	#' @param CRDCSchoolStateSalaryV1ID The ID of the CRDCSchoolStateSalaryV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolStateSalaryV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolStateSalaryV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolStateSalaryV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolStateSalaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolStateSalaryV1 <- function(CRDCSchoolStateSalaryV1ID, AdministrationAmount = F, AdministrationFTE = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, InstructionalAideAmount = F, InstructionalAideFTE = F, ModifiedTime = F, NonPersonnelAmount = F, SupportStaffAmount = F, SupportStaffFTE = F, TeacherAmount = F, TeacherFTE = F, TotalPersonnelAmount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolStateSalaryV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolStateSalaryV1", objectId = CRDCSchoolStateSalaryV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolStateSalaryV1
	#'
	#' This function deletes a CRDCSchoolStateSalaryV1
	#' @param CRDCSchoolStateSalaryV1ID The ID of the CRDCSchoolStateSalaryV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolStateSalaryV1ID of the deleted CRDCSchoolStateSalaryV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolStateSalaryV1 <- function(CRDCSchoolStateSalaryV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolStateSalaryV1", objectId = CRDCSchoolStateSalaryV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolStateSalaryV1
	#'
	#' This function creates a CRDCSchoolStateSalaryV1
	#' @param fieldNames The field values to give the created CRDCSchoolStateSalaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolStateSalaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolStateSalaryV1 <- function(AdministrationAmount = NULL, AdministrationFTE = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, InstructionalAideAmount = NULL, InstructionalAideFTE = NULL, NonPersonnelAmount = NULL, SupportStaffAmount = NULL, SupportStaffFTE = NULL, TeacherAmount = NULL, TeacherFTE = NULL, TotalPersonnelAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolStateSalaryV1", body = list(DataObject = body), searchFields = append("CRDCSchoolStateSalaryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolStateSalaryV1
	#'
	#' This function modifies a CRDCSchoolStateSalaryV1
	#' @param fieldNames The field values to give the modified CRDCSchoolStateSalaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolStateSalaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolStateSalaryV1 <- function(CRDCSchoolStateSalaryV1ID, AdministrationAmount = NULL, AdministrationFTE = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, InstructionalAideAmount = NULL, InstructionalAideFTE = NULL, NonPersonnelAmount = NULL, SupportStaffAmount = NULL, SupportStaffFTE = NULL, TeacherAmount = NULL, TeacherFTE = NULL, TotalPersonnelAmount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolStateSalaryV1", objectId = CRDCSchoolStateSalaryV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolStateSalaryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolTeacherAbsenteeismV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolTeacherAbsenteeismV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolTeacherAbsenteeismV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolTeacherAbsenteeismV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolTeacherAbsenteeismV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolTeacherAbsenteeismV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolTeacherAbsenteeismV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolTeacherAbsenteeismV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FTE = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherAbsenteeismV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolTeacherAbsenteeismV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolTeacherAbsenteeismV1
	#' @param CRDCSchoolTeacherAbsenteeismV1ID The ID of the CRDCSchoolTeacherAbsenteeismV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolTeacherAbsenteeismV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolTeacherAbsenteeismV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolTeacherAbsenteeismV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolTeacherAbsenteeismV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolTeacherAbsenteeismV1 <- function(CRDCSchoolTeacherAbsenteeismV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FTE = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolTeacherAbsenteeismV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherAbsenteeismV1", objectId = CRDCSchoolTeacherAbsenteeismV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolTeacherAbsenteeismV1
	#'
	#' This function deletes a CRDCSchoolTeacherAbsenteeismV1
	#' @param CRDCSchoolTeacherAbsenteeismV1ID The ID of the CRDCSchoolTeacherAbsenteeismV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolTeacherAbsenteeismV1ID of the deleted CRDCSchoolTeacherAbsenteeismV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolTeacherAbsenteeismV1 <- function(CRDCSchoolTeacherAbsenteeismV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherAbsenteeismV1", objectId = CRDCSchoolTeacherAbsenteeismV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolTeacherAbsenteeismV1
	#'
	#' This function creates a CRDCSchoolTeacherAbsenteeismV1
	#' @param fieldNames The field values to give the created CRDCSchoolTeacherAbsenteeismV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolTeacherAbsenteeismV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolTeacherAbsenteeismV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FTE = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherAbsenteeismV1", body = list(DataObject = body), searchFields = append("CRDCSchoolTeacherAbsenteeismV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolTeacherAbsenteeismV1
	#'
	#' This function modifies a CRDCSchoolTeacherAbsenteeismV1
	#' @param fieldNames The field values to give the modified CRDCSchoolTeacherAbsenteeismV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolTeacherAbsenteeismV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolTeacherAbsenteeismV1 <- function(CRDCSchoolTeacherAbsenteeismV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FTE = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherAbsenteeismV1", objectId = CRDCSchoolTeacherAbsenteeismV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolTeacherAbsenteeismV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolOffenseSummaryV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolOffenseSummaryV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolOffenseSummaryV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolOffenseSummaryV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolOffenseSummaryV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolOffenseSummaryV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolOffenseSummaryV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolOffenseSummaryV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FightWithFirearmIncidents = F, FightWithoutWeaponIncidents = F, FightWithWeaponIncidents = F, HadFirearmUse = F, HadHomicide = F, ModifiedTime = F, PossessionOfFirearmIncidents = F, RapeIncidents = F, RobberyWithFirearmIncidents = F, RobberyWithoutWeaponIncidents = F, RobberyWithWeaponIncidents = F, SexualAssaultIncidents = F, ThreatWithFirearmIncidents = F, ThreatWithoutWeaponIncidents = F, ThreatWithWeaponIncidents = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolOffenseSummaryV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolOffenseSummaryV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolOffenseSummaryV1
	#' @param CRDCSchoolOffenseSummaryV1ID The ID of the CRDCSchoolOffenseSummaryV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolOffenseSummaryV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolOffenseSummaryV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolOffenseSummaryV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolOffenseSummaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolOffenseSummaryV1 <- function(CRDCSchoolOffenseSummaryV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FightWithFirearmIncidents = F, FightWithoutWeaponIncidents = F, FightWithWeaponIncidents = F, HadFirearmUse = F, HadHomicide = F, ModifiedTime = F, PossessionOfFirearmIncidents = F, RapeIncidents = F, RobberyWithFirearmIncidents = F, RobberyWithoutWeaponIncidents = F, RobberyWithWeaponIncidents = F, SexualAssaultIncidents = F, ThreatWithFirearmIncidents = F, ThreatWithoutWeaponIncidents = F, ThreatWithWeaponIncidents = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolOffenseSummaryV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolOffenseSummaryV1", objectId = CRDCSchoolOffenseSummaryV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolOffenseSummaryV1
	#'
	#' This function deletes a CRDCSchoolOffenseSummaryV1
	#' @param CRDCSchoolOffenseSummaryV1ID The ID of the CRDCSchoolOffenseSummaryV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolOffenseSummaryV1ID of the deleted CRDCSchoolOffenseSummaryV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolOffenseSummaryV1 <- function(CRDCSchoolOffenseSummaryV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolOffenseSummaryV1", objectId = CRDCSchoolOffenseSummaryV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolOffenseSummaryV1
	#'
	#' This function creates a CRDCSchoolOffenseSummaryV1
	#' @param fieldNames The field values to give the created CRDCSchoolOffenseSummaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolOffenseSummaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolOffenseSummaryV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FightWithFirearmIncidents = NULL, FightWithoutWeaponIncidents = NULL, FightWithWeaponIncidents = NULL, HadFirearmUse = NULL, HadHomicide = NULL, PossessionOfFirearmIncidents = NULL, RapeIncidents = NULL, RobberyWithFirearmIncidents = NULL, RobberyWithoutWeaponIncidents = NULL, RobberyWithWeaponIncidents = NULL, SexualAssaultIncidents = NULL, ThreatWithFirearmIncidents = NULL, ThreatWithoutWeaponIncidents = NULL, ThreatWithWeaponIncidents = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolOffenseSummaryV1", body = list(DataObject = body), searchFields = append("CRDCSchoolOffenseSummaryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolOffenseSummaryV1
	#'
	#' This function modifies a CRDCSchoolOffenseSummaryV1
	#' @param fieldNames The field values to give the modified CRDCSchoolOffenseSummaryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolOffenseSummaryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolOffenseSummaryV1 <- function(CRDCSchoolOffenseSummaryV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FightWithFirearmIncidents = NULL, FightWithoutWeaponIncidents = NULL, FightWithWeaponIncidents = NULL, HadFirearmUse = NULL, HadHomicide = NULL, PossessionOfFirearmIncidents = NULL, RapeIncidents = NULL, RobberyWithFirearmIncidents = NULL, RobberyWithoutWeaponIncidents = NULL, RobberyWithWeaponIncidents = NULL, SexualAssaultIncidents = NULL, ThreatWithFirearmIncidents = NULL, ThreatWithoutWeaponIncidents = NULL, ThreatWithWeaponIncidents = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolOffenseSummaryV1", objectId = CRDCSchoolOffenseSummaryV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolOffenseSummaryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolHarassmentBullyingAllegationV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolHarassmentBullyingAllegationV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHarassmentBullyingAllegationV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHarassmentBullyingAllegationV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHarassmentBullyingAllegationV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolHarassmentBullyingAllegationV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolHarassmentBullyingAllegationV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolHarassmentBullyingAllegationV1ID = F, CreatedTime = F, DisabilityBasedAllegationCount = F, FederalNCESSchoolID = F, ModifiedTime = F, OrientationBasedAllegationCount = F, RaceBasedAllegationCount = F, ReligionBasedAllegationCount = F, SexBasedAllegationCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingAllegationV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolHarassmentBullyingAllegationV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolHarassmentBullyingAllegationV1
	#' @param CRDCSchoolHarassmentBullyingAllegationV1ID The ID of the CRDCSchoolHarassmentBullyingAllegationV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHarassmentBullyingAllegationV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHarassmentBullyingAllegationV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHarassmentBullyingAllegationV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolHarassmentBullyingAllegationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolHarassmentBullyingAllegationV1 <- function(CRDCSchoolHarassmentBullyingAllegationV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DisabilityBasedAllegationCount = F, FederalNCESSchoolID = F, ModifiedTime = F, OrientationBasedAllegationCount = F, RaceBasedAllegationCount = F, ReligionBasedAllegationCount = F, SexBasedAllegationCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolHarassmentBullyingAllegationV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingAllegationV1", objectId = CRDCSchoolHarassmentBullyingAllegationV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolHarassmentBullyingAllegationV1
	#'
	#' This function deletes a CRDCSchoolHarassmentBullyingAllegationV1
	#' @param CRDCSchoolHarassmentBullyingAllegationV1ID The ID of the CRDCSchoolHarassmentBullyingAllegationV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolHarassmentBullyingAllegationV1ID of the deleted CRDCSchoolHarassmentBullyingAllegationV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolHarassmentBullyingAllegationV1 <- function(CRDCSchoolHarassmentBullyingAllegationV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingAllegationV1", objectId = CRDCSchoolHarassmentBullyingAllegationV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolHarassmentBullyingAllegationV1
	#'
	#' This function creates a CRDCSchoolHarassmentBullyingAllegationV1
	#' @param fieldNames The field values to give the created CRDCSchoolHarassmentBullyingAllegationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolHarassmentBullyingAllegationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolHarassmentBullyingAllegationV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityBasedAllegationCount = NULL, FederalNCESSchoolID = NULL, OrientationBasedAllegationCount = NULL, RaceBasedAllegationCount = NULL, ReligionBasedAllegationCount = NULL, SexBasedAllegationCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingAllegationV1", body = list(DataObject = body), searchFields = append("CRDCSchoolHarassmentBullyingAllegationV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolHarassmentBullyingAllegationV1
	#'
	#' This function modifies a CRDCSchoolHarassmentBullyingAllegationV1
	#' @param fieldNames The field values to give the modified CRDCSchoolHarassmentBullyingAllegationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolHarassmentBullyingAllegationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolHarassmentBullyingAllegationV1 <- function(CRDCSchoolHarassmentBullyingAllegationV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityBasedAllegationCount = NULL, FederalNCESSchoolID = NULL, OrientationBasedAllegationCount = NULL, RaceBasedAllegationCount = NULL, ReligionBasedAllegationCount = NULL, SexBasedAllegationCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingAllegationV1", objectId = CRDCSchoolHarassmentBullyingAllegationV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolHarassmentBullyingAllegationV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolHarassmentBullyingDisciplineV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolHarassmentBullyingDisciplineV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHarassmentBullyingDisciplineV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHarassmentBullyingDisciplineV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHarassmentBullyingDisciplineV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolHarassmentBullyingDisciplineV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolHarassmentBullyingDisciplineV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolHarassmentBullyingDisciplineV1ID = F, CreatedTime = F, DisabilityFemaleAlaskanCount = F, DisabilityFemaleAsianCount = F, DisabilityFemaleBlackCount = F, DisabilityFemaleHawaiianCount = F, DisabilityFemaleHispanicCount = F, DisabilityFemaleIDEACount = F, DisabilityFemaleLEPCount = F, DisabilityFemaleMultiRaceCount = F, DisabilityFemaleSection504Count = F, DisabilityFemaleWhiteCount = F, DisabilityMaleAlaskanCount = F, DisabilityMaleAsianCount = F, DisabilityMaleBlackCount = F, DisabilityMaleHawaiianCount = F, DisabilityMaleHispanicCount = F, DisabilityMaleIDEACount = F, DisabilityMaleLEPCount = F, DisabilityMaleMultiRaceCount = F, DisabilityMaleSection504Count = F, DisabilityMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, RaceFemaleAlaskanCount = F, RaceFemaleAsianCount = F, RaceFemaleBlackCount = F, RaceFemaleHawaiianCount = F, RaceFemaleHispanicCount = F, RaceFemaleIDEACount = F, RaceFemaleLEPCount = F, RaceFemaleMultiRaceCount = F, RaceFemaleSection504Count = F, RaceFemaleWhiteCount = F, RaceMaleAlaskanCount = F, RaceMaleAsianCount = F, RaceMaleBlackCount = F, RaceMaleHawaiianCount = F, RaceMaleHispanicCount = F, RaceMaleIDEACount = F, RaceMaleLEPCount = F, RaceMaleMultiRaceCount = F, RaceMaleSection504Count = F, RaceMaleWhiteCount = F, SexFemaleAlaskanCount = F, SexFemaleAsianCount = F, SexFemaleBlackCount = F, SexFemaleHawaiianCount = F, SexFemaleHispanicCount = F, SexFemaleIDEACount = F, SexFemaleLEPCount = F, SexFemaleMultiRaceCount = F, SexFemaleSection504Count = F, SexFemaleWhiteCount = F, SexMaleAlaskanCount = F, SexMaleAsianCount = F, SexMaleBlackCount = F, SexMaleHawaiianCount = F, SexMaleHispanicCount = F, SexMaleIDEACount = F, SexMaleLEPCount = F, SexMaleMultiRaceCount = F, SexMaleSection504Count = F, SexMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingDisciplineV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolHarassmentBullyingDisciplineV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolHarassmentBullyingDisciplineV1
	#' @param CRDCSchoolHarassmentBullyingDisciplineV1ID The ID of the CRDCSchoolHarassmentBullyingDisciplineV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHarassmentBullyingDisciplineV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHarassmentBullyingDisciplineV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHarassmentBullyingDisciplineV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolHarassmentBullyingDisciplineV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolHarassmentBullyingDisciplineV1 <- function(CRDCSchoolHarassmentBullyingDisciplineV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DisabilityFemaleAlaskanCount = F, DisabilityFemaleAsianCount = F, DisabilityFemaleBlackCount = F, DisabilityFemaleHawaiianCount = F, DisabilityFemaleHispanicCount = F, DisabilityFemaleIDEACount = F, DisabilityFemaleLEPCount = F, DisabilityFemaleMultiRaceCount = F, DisabilityFemaleSection504Count = F, DisabilityFemaleWhiteCount = F, DisabilityMaleAlaskanCount = F, DisabilityMaleAsianCount = F, DisabilityMaleBlackCount = F, DisabilityMaleHawaiianCount = F, DisabilityMaleHispanicCount = F, DisabilityMaleIDEACount = F, DisabilityMaleLEPCount = F, DisabilityMaleMultiRaceCount = F, DisabilityMaleSection504Count = F, DisabilityMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, RaceFemaleAlaskanCount = F, RaceFemaleAsianCount = F, RaceFemaleBlackCount = F, RaceFemaleHawaiianCount = F, RaceFemaleHispanicCount = F, RaceFemaleIDEACount = F, RaceFemaleLEPCount = F, RaceFemaleMultiRaceCount = F, RaceFemaleSection504Count = F, RaceFemaleWhiteCount = F, RaceMaleAlaskanCount = F, RaceMaleAsianCount = F, RaceMaleBlackCount = F, RaceMaleHawaiianCount = F, RaceMaleHispanicCount = F, RaceMaleIDEACount = F, RaceMaleLEPCount = F, RaceMaleMultiRaceCount = F, RaceMaleSection504Count = F, RaceMaleWhiteCount = F, SexFemaleAlaskanCount = F, SexFemaleAsianCount = F, SexFemaleBlackCount = F, SexFemaleHawaiianCount = F, SexFemaleHispanicCount = F, SexFemaleIDEACount = F, SexFemaleLEPCount = F, SexFemaleMultiRaceCount = F, SexFemaleSection504Count = F, SexFemaleWhiteCount = F, SexMaleAlaskanCount = F, SexMaleAsianCount = F, SexMaleBlackCount = F, SexMaleHawaiianCount = F, SexMaleHispanicCount = F, SexMaleIDEACount = F, SexMaleLEPCount = F, SexMaleMultiRaceCount = F, SexMaleSection504Count = F, SexMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolHarassmentBullyingDisciplineV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingDisciplineV1", objectId = CRDCSchoolHarassmentBullyingDisciplineV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolHarassmentBullyingDisciplineV1
	#'
	#' This function deletes a CRDCSchoolHarassmentBullyingDisciplineV1
	#' @param CRDCSchoolHarassmentBullyingDisciplineV1ID The ID of the CRDCSchoolHarassmentBullyingDisciplineV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolHarassmentBullyingDisciplineV1ID of the deleted CRDCSchoolHarassmentBullyingDisciplineV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolHarassmentBullyingDisciplineV1 <- function(CRDCSchoolHarassmentBullyingDisciplineV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingDisciplineV1", objectId = CRDCSchoolHarassmentBullyingDisciplineV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolHarassmentBullyingDisciplineV1
	#'
	#' This function creates a CRDCSchoolHarassmentBullyingDisciplineV1
	#' @param fieldNames The field values to give the created CRDCSchoolHarassmentBullyingDisciplineV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolHarassmentBullyingDisciplineV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolHarassmentBullyingDisciplineV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityFemaleAlaskanCount = NULL, DisabilityFemaleAsianCount = NULL, DisabilityFemaleBlackCount = NULL, DisabilityFemaleHawaiianCount = NULL, DisabilityFemaleHispanicCount = NULL, DisabilityFemaleIDEACount = NULL, DisabilityFemaleLEPCount = NULL, DisabilityFemaleMultiRaceCount = NULL, DisabilityFemaleSection504Count = NULL, DisabilityFemaleWhiteCount = NULL, DisabilityMaleAlaskanCount = NULL, DisabilityMaleAsianCount = NULL, DisabilityMaleBlackCount = NULL, DisabilityMaleHawaiianCount = NULL, DisabilityMaleHispanicCount = NULL, DisabilityMaleIDEACount = NULL, DisabilityMaleLEPCount = NULL, DisabilityMaleMultiRaceCount = NULL, DisabilityMaleSection504Count = NULL, DisabilityMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, RaceFemaleAlaskanCount = NULL, RaceFemaleAsianCount = NULL, RaceFemaleBlackCount = NULL, RaceFemaleHawaiianCount = NULL, RaceFemaleHispanicCount = NULL, RaceFemaleIDEACount = NULL, RaceFemaleLEPCount = NULL, RaceFemaleMultiRaceCount = NULL, RaceFemaleSection504Count = NULL, RaceFemaleWhiteCount = NULL, RaceMaleAlaskanCount = NULL, RaceMaleAsianCount = NULL, RaceMaleBlackCount = NULL, RaceMaleHawaiianCount = NULL, RaceMaleHispanicCount = NULL, RaceMaleIDEACount = NULL, RaceMaleLEPCount = NULL, RaceMaleMultiRaceCount = NULL, RaceMaleSection504Count = NULL, RaceMaleWhiteCount = NULL, SexFemaleAlaskanCount = NULL, SexFemaleAsianCount = NULL, SexFemaleBlackCount = NULL, SexFemaleHawaiianCount = NULL, SexFemaleHispanicCount = NULL, SexFemaleIDEACount = NULL, SexFemaleLEPCount = NULL, SexFemaleMultiRaceCount = NULL, SexFemaleSection504Count = NULL, SexFemaleWhiteCount = NULL, SexMaleAlaskanCount = NULL, SexMaleAsianCount = NULL, SexMaleBlackCount = NULL, SexMaleHawaiianCount = NULL, SexMaleHispanicCount = NULL, SexMaleIDEACount = NULL, SexMaleLEPCount = NULL, SexMaleMultiRaceCount = NULL, SexMaleSection504Count = NULL, SexMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingDisciplineV1", body = list(DataObject = body), searchFields = append("CRDCSchoolHarassmentBullyingDisciplineV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolHarassmentBullyingDisciplineV1
	#'
	#' This function modifies a CRDCSchoolHarassmentBullyingDisciplineV1
	#' @param fieldNames The field values to give the modified CRDCSchoolHarassmentBullyingDisciplineV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolHarassmentBullyingDisciplineV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolHarassmentBullyingDisciplineV1 <- function(CRDCSchoolHarassmentBullyingDisciplineV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityFemaleAlaskanCount = NULL, DisabilityFemaleAsianCount = NULL, DisabilityFemaleBlackCount = NULL, DisabilityFemaleHawaiianCount = NULL, DisabilityFemaleHispanicCount = NULL, DisabilityFemaleIDEACount = NULL, DisabilityFemaleLEPCount = NULL, DisabilityFemaleMultiRaceCount = NULL, DisabilityFemaleSection504Count = NULL, DisabilityFemaleWhiteCount = NULL, DisabilityMaleAlaskanCount = NULL, DisabilityMaleAsianCount = NULL, DisabilityMaleBlackCount = NULL, DisabilityMaleHawaiianCount = NULL, DisabilityMaleHispanicCount = NULL, DisabilityMaleIDEACount = NULL, DisabilityMaleLEPCount = NULL, DisabilityMaleMultiRaceCount = NULL, DisabilityMaleSection504Count = NULL, DisabilityMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, RaceFemaleAlaskanCount = NULL, RaceFemaleAsianCount = NULL, RaceFemaleBlackCount = NULL, RaceFemaleHawaiianCount = NULL, RaceFemaleHispanicCount = NULL, RaceFemaleIDEACount = NULL, RaceFemaleLEPCount = NULL, RaceFemaleMultiRaceCount = NULL, RaceFemaleSection504Count = NULL, RaceFemaleWhiteCount = NULL, RaceMaleAlaskanCount = NULL, RaceMaleAsianCount = NULL, RaceMaleBlackCount = NULL, RaceMaleHawaiianCount = NULL, RaceMaleHispanicCount = NULL, RaceMaleIDEACount = NULL, RaceMaleLEPCount = NULL, RaceMaleMultiRaceCount = NULL, RaceMaleSection504Count = NULL, RaceMaleWhiteCount = NULL, SexFemaleAlaskanCount = NULL, SexFemaleAsianCount = NULL, SexFemaleBlackCount = NULL, SexFemaleHawaiianCount = NULL, SexFemaleHispanicCount = NULL, SexFemaleIDEACount = NULL, SexFemaleLEPCount = NULL, SexFemaleMultiRaceCount = NULL, SexFemaleSection504Count = NULL, SexFemaleWhiteCount = NULL, SexMaleAlaskanCount = NULL, SexMaleAsianCount = NULL, SexMaleBlackCount = NULL, SexMaleHawaiianCount = NULL, SexMaleHispanicCount = NULL, SexMaleIDEACount = NULL, SexMaleLEPCount = NULL, SexMaleMultiRaceCount = NULL, SexMaleSection504Count = NULL, SexMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingDisciplineV1", objectId = CRDCSchoolHarassmentBullyingDisciplineV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolHarassmentBullyingDisciplineV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolHarassmentBullyingReportedV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolHarassmentBullyingReportedV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHarassmentBullyingReportedV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHarassmentBullyingReportedV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHarassmentBullyingReportedV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolHarassmentBullyingReportedV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolHarassmentBullyingReportedV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolHarassmentBullyingReportedV1ID = F, CreatedTime = F, DisabilityFemaleAlaskanCount = F, DisabilityFemaleAsianCount = F, DisabilityFemaleBlackCount = F, DisabilityFemaleHawaiianCount = F, DisabilityFemaleHispanicCount = F, DisabilityFemaleIDEACount = F, DisabilityFemaleLEPCount = F, DisabilityFemaleMultiRaceCount = F, DisabilityFemaleSection504Count = F, DisabilityFemaleWhiteCount = F, DisabilityMaleAlaskanCount = F, DisabilityMaleAsianCount = F, DisabilityMaleBlackCount = F, DisabilityMaleHawaiianCount = F, DisabilityMaleHispanicCount = F, DisabilityMaleIDEACount = F, DisabilityMaleLEPCount = F, DisabilityMaleMultiRaceCount = F, DisabilityMaleSection504Count = F, DisabilityMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, RaceFemaleAlaskanCount = F, RaceFemaleAsianCount = F, RaceFemaleBlackCount = F, RaceFemaleHawaiianCount = F, RaceFemaleHispanicCount = F, RaceFemaleIDEACount = F, RaceFemaleLEPCount = F, RaceFemaleMultiRaceCount = F, RaceFemaleSection504Count = F, RaceFemaleWhiteCount = F, RaceMaleAlaskanCount = F, RaceMaleAsianCount = F, RaceMaleBlackCount = F, RaceMaleHawaiianCount = F, RaceMaleHispanicCount = F, RaceMaleIDEACount = F, RaceMaleLEPCount = F, RaceMaleMultiRaceCount = F, RaceMaleSection504Count = F, RaceMaleWhiteCount = F, SexFemaleAlaskanCount = F, SexFemaleAsianCount = F, SexFemaleBlackCount = F, SexFemaleHawaiianCount = F, SexFemaleHispanicCount = F, SexFemaleIDEACount = F, SexFemaleLEPCount = F, SexFemaleMultiRaceCount = F, SexFemaleSection504Count = F, SexFemaleWhiteCount = F, SexMaleAlaskanCount = F, SexMaleAsianCount = F, SexMaleBlackCount = F, SexMaleHawaiianCount = F, SexMaleHispanicCount = F, SexMaleIDEACount = F, SexMaleLEPCount = F, SexMaleMultiRaceCount = F, SexMaleSection504Count = F, SexMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingReportedV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolHarassmentBullyingReportedV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolHarassmentBullyingReportedV1
	#' @param CRDCSchoolHarassmentBullyingReportedV1ID The ID of the CRDCSchoolHarassmentBullyingReportedV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHarassmentBullyingReportedV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHarassmentBullyingReportedV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHarassmentBullyingReportedV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolHarassmentBullyingReportedV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolHarassmentBullyingReportedV1 <- function(CRDCSchoolHarassmentBullyingReportedV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DisabilityFemaleAlaskanCount = F, DisabilityFemaleAsianCount = F, DisabilityFemaleBlackCount = F, DisabilityFemaleHawaiianCount = F, DisabilityFemaleHispanicCount = F, DisabilityFemaleIDEACount = F, DisabilityFemaleLEPCount = F, DisabilityFemaleMultiRaceCount = F, DisabilityFemaleSection504Count = F, DisabilityFemaleWhiteCount = F, DisabilityMaleAlaskanCount = F, DisabilityMaleAsianCount = F, DisabilityMaleBlackCount = F, DisabilityMaleHawaiianCount = F, DisabilityMaleHispanicCount = F, DisabilityMaleIDEACount = F, DisabilityMaleLEPCount = F, DisabilityMaleMultiRaceCount = F, DisabilityMaleSection504Count = F, DisabilityMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, RaceFemaleAlaskanCount = F, RaceFemaleAsianCount = F, RaceFemaleBlackCount = F, RaceFemaleHawaiianCount = F, RaceFemaleHispanicCount = F, RaceFemaleIDEACount = F, RaceFemaleLEPCount = F, RaceFemaleMultiRaceCount = F, RaceFemaleSection504Count = F, RaceFemaleWhiteCount = F, RaceMaleAlaskanCount = F, RaceMaleAsianCount = F, RaceMaleBlackCount = F, RaceMaleHawaiianCount = F, RaceMaleHispanicCount = F, RaceMaleIDEACount = F, RaceMaleLEPCount = F, RaceMaleMultiRaceCount = F, RaceMaleSection504Count = F, RaceMaleWhiteCount = F, SexFemaleAlaskanCount = F, SexFemaleAsianCount = F, SexFemaleBlackCount = F, SexFemaleHawaiianCount = F, SexFemaleHispanicCount = F, SexFemaleIDEACount = F, SexFemaleLEPCount = F, SexFemaleMultiRaceCount = F, SexFemaleSection504Count = F, SexFemaleWhiteCount = F, SexMaleAlaskanCount = F, SexMaleAsianCount = F, SexMaleBlackCount = F, SexMaleHawaiianCount = F, SexMaleHispanicCount = F, SexMaleIDEACount = F, SexMaleLEPCount = F, SexMaleMultiRaceCount = F, SexMaleSection504Count = F, SexMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolHarassmentBullyingReportedV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingReportedV1", objectId = CRDCSchoolHarassmentBullyingReportedV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolHarassmentBullyingReportedV1
	#'
	#' This function deletes a CRDCSchoolHarassmentBullyingReportedV1
	#' @param CRDCSchoolHarassmentBullyingReportedV1ID The ID of the CRDCSchoolHarassmentBullyingReportedV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolHarassmentBullyingReportedV1ID of the deleted CRDCSchoolHarassmentBullyingReportedV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolHarassmentBullyingReportedV1 <- function(CRDCSchoolHarassmentBullyingReportedV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingReportedV1", objectId = CRDCSchoolHarassmentBullyingReportedV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolHarassmentBullyingReportedV1
	#'
	#' This function creates a CRDCSchoolHarassmentBullyingReportedV1
	#' @param fieldNames The field values to give the created CRDCSchoolHarassmentBullyingReportedV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolHarassmentBullyingReportedV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolHarassmentBullyingReportedV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityFemaleAlaskanCount = NULL, DisabilityFemaleAsianCount = NULL, DisabilityFemaleBlackCount = NULL, DisabilityFemaleHawaiianCount = NULL, DisabilityFemaleHispanicCount = NULL, DisabilityFemaleIDEACount = NULL, DisabilityFemaleLEPCount = NULL, DisabilityFemaleMultiRaceCount = NULL, DisabilityFemaleSection504Count = NULL, DisabilityFemaleWhiteCount = NULL, DisabilityMaleAlaskanCount = NULL, DisabilityMaleAsianCount = NULL, DisabilityMaleBlackCount = NULL, DisabilityMaleHawaiianCount = NULL, DisabilityMaleHispanicCount = NULL, DisabilityMaleIDEACount = NULL, DisabilityMaleLEPCount = NULL, DisabilityMaleMultiRaceCount = NULL, DisabilityMaleSection504Count = NULL, DisabilityMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, RaceFemaleAlaskanCount = NULL, RaceFemaleAsianCount = NULL, RaceFemaleBlackCount = NULL, RaceFemaleHawaiianCount = NULL, RaceFemaleHispanicCount = NULL, RaceFemaleIDEACount = NULL, RaceFemaleLEPCount = NULL, RaceFemaleMultiRaceCount = NULL, RaceFemaleSection504Count = NULL, RaceFemaleWhiteCount = NULL, RaceMaleAlaskanCount = NULL, RaceMaleAsianCount = NULL, RaceMaleBlackCount = NULL, RaceMaleHawaiianCount = NULL, RaceMaleHispanicCount = NULL, RaceMaleIDEACount = NULL, RaceMaleLEPCount = NULL, RaceMaleMultiRaceCount = NULL, RaceMaleSection504Count = NULL, RaceMaleWhiteCount = NULL, SexFemaleAlaskanCount = NULL, SexFemaleAsianCount = NULL, SexFemaleBlackCount = NULL, SexFemaleHawaiianCount = NULL, SexFemaleHispanicCount = NULL, SexFemaleIDEACount = NULL, SexFemaleLEPCount = NULL, SexFemaleMultiRaceCount = NULL, SexFemaleSection504Count = NULL, SexFemaleWhiteCount = NULL, SexMaleAlaskanCount = NULL, SexMaleAsianCount = NULL, SexMaleBlackCount = NULL, SexMaleHawaiianCount = NULL, SexMaleHispanicCount = NULL, SexMaleIDEACount = NULL, SexMaleLEPCount = NULL, SexMaleMultiRaceCount = NULL, SexMaleSection504Count = NULL, SexMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingReportedV1", body = list(DataObject = body), searchFields = append("CRDCSchoolHarassmentBullyingReportedV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolHarassmentBullyingReportedV1
	#'
	#' This function modifies a CRDCSchoolHarassmentBullyingReportedV1
	#' @param fieldNames The field values to give the modified CRDCSchoolHarassmentBullyingReportedV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolHarassmentBullyingReportedV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolHarassmentBullyingReportedV1 <- function(CRDCSchoolHarassmentBullyingReportedV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DisabilityFemaleAlaskanCount = NULL, DisabilityFemaleAsianCount = NULL, DisabilityFemaleBlackCount = NULL, DisabilityFemaleHawaiianCount = NULL, DisabilityFemaleHispanicCount = NULL, DisabilityFemaleIDEACount = NULL, DisabilityFemaleLEPCount = NULL, DisabilityFemaleMultiRaceCount = NULL, DisabilityFemaleSection504Count = NULL, DisabilityFemaleWhiteCount = NULL, DisabilityMaleAlaskanCount = NULL, DisabilityMaleAsianCount = NULL, DisabilityMaleBlackCount = NULL, DisabilityMaleHawaiianCount = NULL, DisabilityMaleHispanicCount = NULL, DisabilityMaleIDEACount = NULL, DisabilityMaleLEPCount = NULL, DisabilityMaleMultiRaceCount = NULL, DisabilityMaleSection504Count = NULL, DisabilityMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, RaceFemaleAlaskanCount = NULL, RaceFemaleAsianCount = NULL, RaceFemaleBlackCount = NULL, RaceFemaleHawaiianCount = NULL, RaceFemaleHispanicCount = NULL, RaceFemaleIDEACount = NULL, RaceFemaleLEPCount = NULL, RaceFemaleMultiRaceCount = NULL, RaceFemaleSection504Count = NULL, RaceFemaleWhiteCount = NULL, RaceMaleAlaskanCount = NULL, RaceMaleAsianCount = NULL, RaceMaleBlackCount = NULL, RaceMaleHawaiianCount = NULL, RaceMaleHispanicCount = NULL, RaceMaleIDEACount = NULL, RaceMaleLEPCount = NULL, RaceMaleMultiRaceCount = NULL, RaceMaleSection504Count = NULL, RaceMaleWhiteCount = NULL, SexFemaleAlaskanCount = NULL, SexFemaleAsianCount = NULL, SexFemaleBlackCount = NULL, SexFemaleHawaiianCount = NULL, SexFemaleHispanicCount = NULL, SexFemaleIDEACount = NULL, SexFemaleLEPCount = NULL, SexFemaleMultiRaceCount = NULL, SexFemaleSection504Count = NULL, SexFemaleWhiteCount = NULL, SexMaleAlaskanCount = NULL, SexMaleAsianCount = NULL, SexMaleBlackCount = NULL, SexMaleHawaiianCount = NULL, SexMaleHispanicCount = NULL, SexMaleIDEACount = NULL, SexMaleLEPCount = NULL, SexMaleMultiRaceCount = NULL, SexMaleSection504Count = NULL, SexMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHarassmentBullyingReportedV1", objectId = CRDCSchoolHarassmentBullyingReportedV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolHarassmentBullyingReportedV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolRestraintInstanceV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolRestraintInstanceV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolRestraintInstanceV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolRestraintInstanceV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolRestraintInstanceV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolRestraintInstanceV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolRestraintInstanceV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolRestraintInstanceV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, MechanicalIDEAInstances = F, MechanicalSection504Instances = F, MechanicalWithoutDisabilityInstances = F, ModifiedTime = F, PhysicalIDEAInstances = F, PhysicalSection504Instances = F, PhysicalWithoutDisabilityInstances = F, SeclusionIDEAInstances = F, SeclusionSection504Instances = F, SeclusionWithoutDisabilityInstances = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolRestraintInstanceV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolRestraintInstanceV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolRestraintInstanceV1
	#' @param CRDCSchoolRestraintInstanceV1ID The ID of the CRDCSchoolRestraintInstanceV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolRestraintInstanceV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolRestraintInstanceV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolRestraintInstanceV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolRestraintInstanceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolRestraintInstanceV1 <- function(CRDCSchoolRestraintInstanceV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, MechanicalIDEAInstances = F, MechanicalSection504Instances = F, MechanicalWithoutDisabilityInstances = F, ModifiedTime = F, PhysicalIDEAInstances = F, PhysicalSection504Instances = F, PhysicalWithoutDisabilityInstances = F, SeclusionIDEAInstances = F, SeclusionSection504Instances = F, SeclusionWithoutDisabilityInstances = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolRestraintInstanceV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRestraintInstanceV1", objectId = CRDCSchoolRestraintInstanceV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolRestraintInstanceV1
	#'
	#' This function deletes a CRDCSchoolRestraintInstanceV1
	#' @param CRDCSchoolRestraintInstanceV1ID The ID of the CRDCSchoolRestraintInstanceV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolRestraintInstanceV1ID of the deleted CRDCSchoolRestraintInstanceV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolRestraintInstanceV1 <- function(CRDCSchoolRestraintInstanceV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRestraintInstanceV1", objectId = CRDCSchoolRestraintInstanceV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolRestraintInstanceV1
	#'
	#' This function creates a CRDCSchoolRestraintInstanceV1
	#' @param fieldNames The field values to give the created CRDCSchoolRestraintInstanceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolRestraintInstanceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolRestraintInstanceV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, MechanicalIDEAInstances = NULL, MechanicalSection504Instances = NULL, MechanicalWithoutDisabilityInstances = NULL, PhysicalIDEAInstances = NULL, PhysicalSection504Instances = NULL, PhysicalWithoutDisabilityInstances = NULL, SeclusionIDEAInstances = NULL, SeclusionSection504Instances = NULL, SeclusionWithoutDisabilityInstances = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRestraintInstanceV1", body = list(DataObject = body), searchFields = append("CRDCSchoolRestraintInstanceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolRestraintInstanceV1
	#'
	#' This function modifies a CRDCSchoolRestraintInstanceV1
	#' @param fieldNames The field values to give the modified CRDCSchoolRestraintInstanceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolRestraintInstanceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolRestraintInstanceV1 <- function(CRDCSchoolRestraintInstanceV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, MechanicalIDEAInstances = NULL, MechanicalSection504Instances = NULL, MechanicalWithoutDisabilityInstances = NULL, PhysicalIDEAInstances = NULL, PhysicalSection504Instances = NULL, PhysicalWithoutDisabilityInstances = NULL, SeclusionIDEAInstances = NULL, SeclusionSection504Instances = NULL, SeclusionWithoutDisabilityInstances = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRestraintInstanceV1", objectId = CRDCSchoolRestraintInstanceV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolRestraintInstanceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCStudentDetails
	#'
	#' This function returns a dataframe or json object of CRDCStudentDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCStudentDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCStudentDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCStudentDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCStudentDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCStudentDetails <- function(searchConditionsList = NULL, Age = F, ArrestCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CorporalPunishmentIncidents = F, CRDCStudentDetailID = F, CreatedTime = F, CumulativeEnrollmentPromotionStatusCode = F, DisciplinedBullyingDisability = F, DisciplinedBullyingRace = F, DisciplinedBullyingSex = F, DistrictID = F, EnrollmentType = F, ExpulsionWithoutServicesCount = F, ExpulsionWithServicesCount = F, FederalNCESSchoolID = F, FullName = F, Gender = F, GradeLevel = F, HasAdvancedMathGrade0912 = F, HasAlgebraIGrade07 = F, HasAlgebraIGrade08 = F, HasAlgebraIGrade0910 = F, HasAlgebraIGrade1112 = F, HasAlgebraIIGrade0912 = F, HasAPComputerScienceGrade0912 = F, HasAPCourseGrade0912 = F, HasAPMathGrade0912 = F, HasAPOtherGrade0912 = F, HasAPScienceGrade0912 = F, HasBiologyGrade0912 = F, HasCalculusGrade0912 = F, HasChemistryGrade0912 = F, HasComputerScienceGrade0912 = F, HasCreditRecovery = F, HasGeometryGrade08 = F, HasGeometryGrade0912 = F, HasPassedAlgebraIGrade07 = F, HasPassedAlgebraIGrade08 = F, HasPassedAlgebraIGrade0910 = F, HasPassedAlgebraIGrade1112 = F, HasPhysicsGrade0912 = F, HasTakenACT0912 = F, HasTakenSAT0912 = F, IDEADate = F, InSchoolSuspensionCount = F, IsAlaskan = F, IsAsian = F, IsBlack = F, IsChronicallyAbsent = F, IsEnrolled = F, IsFederalDistanceEducation = F, IsFederalDualEnrollment = F, IsGiftedTalented = F, IsHawaiian = F, IsHispanic = F, IsIDEA = F, IsIDEASnapshot = F, IsLEP = F, IsLEPSnapshot = F, IsMultiRace = F, IsNonLEA = F, IsSection504 = F, IsSection504Snapshot = F, IsWhite = F, LawEnforcementReferralCount = F, MechanicalRestraintCount = F, ModifiedTime = F, OutOfSchoolSuspensionCount = F, OutOfSchoolSuspensionMissedDays = F, PhysicalRestraintCount = F, ReportDate = F, ReportedBulliedDisability = F, ReportedBulliedRace = F, ReportedBulliedSex = F, SchoolID = F, SeclusionCount = F, SingleSexAthleticCount = F, StudentID = F, TransferToAlternativeSchool = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZeroToleranceWithoutServicesCount = F, ZeroToleranceWithServicesCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCStudentDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCStudentDetail
	#'
	#' This function returns a dataframe or json object of a CRDCStudentDetail
	#' @param CRDCStudentDetailID The ID of the CRDCStudentDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCStudentDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCStudentDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCStudentDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCStudentDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCStudentDetail <- function(CRDCStudentDetailID, Age = F, ArrestCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CorporalPunishmentIncidents = F, CreatedTime = F, CumulativeEnrollmentPromotionStatusCode = F, DisciplinedBullyingDisability = F, DisciplinedBullyingRace = F, DisciplinedBullyingSex = F, DistrictID = F, EnrollmentType = F, ExpulsionWithoutServicesCount = F, ExpulsionWithServicesCount = F, FederalNCESSchoolID = F, FullName = F, Gender = F, GradeLevel = F, HasAdvancedMathGrade0912 = F, HasAlgebraIGrade07 = F, HasAlgebraIGrade08 = F, HasAlgebraIGrade0910 = F, HasAlgebraIGrade1112 = F, HasAlgebraIIGrade0912 = F, HasAPComputerScienceGrade0912 = F, HasAPCourseGrade0912 = F, HasAPMathGrade0912 = F, HasAPOtherGrade0912 = F, HasAPScienceGrade0912 = F, HasBiologyGrade0912 = F, HasCalculusGrade0912 = F, HasChemistryGrade0912 = F, HasComputerScienceGrade0912 = F, HasCreditRecovery = F, HasGeometryGrade08 = F, HasGeometryGrade0912 = F, HasPassedAlgebraIGrade07 = F, HasPassedAlgebraIGrade08 = F, HasPassedAlgebraIGrade0910 = F, HasPassedAlgebraIGrade1112 = F, HasPhysicsGrade0912 = F, HasTakenACT0912 = F, HasTakenSAT0912 = F, IDEADate = F, InSchoolSuspensionCount = F, IsAlaskan = F, IsAsian = F, IsBlack = F, IsChronicallyAbsent = F, IsEnrolled = F, IsFederalDistanceEducation = F, IsFederalDualEnrollment = F, IsGiftedTalented = F, IsHawaiian = F, IsHispanic = F, IsIDEA = F, IsIDEASnapshot = F, IsLEP = F, IsLEPSnapshot = F, IsMultiRace = F, IsNonLEA = F, IsSection504 = F, IsSection504Snapshot = F, IsWhite = F, LawEnforcementReferralCount = F, MechanicalRestraintCount = F, ModifiedTime = F, OutOfSchoolSuspensionCount = F, OutOfSchoolSuspensionMissedDays = F, PhysicalRestraintCount = F, ReportDate = F, ReportedBulliedDisability = F, ReportedBulliedRace = F, ReportedBulliedSex = F, SchoolID = F, SeclusionCount = F, SingleSexAthleticCount = F, StudentID = F, TransferToAlternativeSchool = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZeroToleranceWithoutServicesCount = F, ZeroToleranceWithServicesCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCStudentDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCStudentDetail", objectId = CRDCStudentDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCStudentDetail
	#'
	#' This function deletes a CRDCStudentDetail
	#' @param CRDCStudentDetailID The ID of the CRDCStudentDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCStudentDetailID of the deleted CRDCStudentDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCStudentDetail <- function(CRDCStudentDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCStudentDetail", objectId = CRDCStudentDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCStudentDetail
	#'
	#' This function creates a CRDCStudentDetail
	#' @param fieldNames The field values to give the created CRDCStudentDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCStudentDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCStudentDetail <- function(Age = NULL, ArrestCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CorporalPunishmentIncidents = NULL, CumulativeEnrollmentPromotionStatusCode = NULL, DisciplinedBullyingDisability = NULL, DisciplinedBullyingRace = NULL, DisciplinedBullyingSex = NULL, DistrictID = NULL, EnrollmentType = NULL, ExpulsionWithoutServicesCount = NULL, ExpulsionWithServicesCount = NULL, FederalNCESSchoolID = NULL, FullName = NULL, Gender = NULL, GradeLevel = NULL, HasAdvancedMathGrade0912 = NULL, HasAlgebraIGrade07 = NULL, HasAlgebraIGrade08 = NULL, HasAlgebraIGrade0910 = NULL, HasAlgebraIGrade1112 = NULL, HasAlgebraIIGrade0912 = NULL, HasAPComputerScienceGrade0912 = NULL, HasAPCourseGrade0912 = NULL, HasAPMathGrade0912 = NULL, HasAPOtherGrade0912 = NULL, HasAPScienceGrade0912 = NULL, HasBiologyGrade0912 = NULL, HasCalculusGrade0912 = NULL, HasChemistryGrade0912 = NULL, HasComputerScienceGrade0912 = NULL, HasCreditRecovery = NULL, HasGeometryGrade08 = NULL, HasGeometryGrade0912 = NULL, HasPassedAlgebraIGrade07 = NULL, HasPassedAlgebraIGrade08 = NULL, HasPassedAlgebraIGrade0910 = NULL, HasPassedAlgebraIGrade1112 = NULL, HasPhysicsGrade0912 = NULL, HasTakenACT0912 = NULL, HasTakenSAT0912 = NULL, IDEADate = NULL, InSchoolSuspensionCount = NULL, IsAlaskan = NULL, IsAsian = NULL, IsBlack = NULL, IsChronicallyAbsent = NULL, IsFederalDistanceEducation = NULL, IsFederalDualEnrollment = NULL, IsGiftedTalented = NULL, IsHawaiian = NULL, IsHispanic = NULL, IsIDEA = NULL, IsIDEASnapshot = NULL, IsLEP = NULL, IsLEPSnapshot = NULL, IsMultiRace = NULL, IsNonLEA = NULL, IsSection504 = NULL, IsSection504Snapshot = NULL, IsWhite = NULL, LawEnforcementReferralCount = NULL, MechanicalRestraintCount = NULL, OutOfSchoolSuspensionCount = NULL, OutOfSchoolSuspensionMissedDays = NULL, PhysicalRestraintCount = NULL, ReportDate = NULL, ReportedBulliedDisability = NULL, ReportedBulliedRace = NULL, ReportedBulliedSex = NULL, SchoolID = NULL, SeclusionCount = NULL, SingleSexAthleticCount = NULL, StudentID = NULL, TransferToAlternativeSchool = NULL, ZeroToleranceWithoutServicesCount = NULL, ZeroToleranceWithServicesCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCStudentDetail", body = list(DataObject = body), searchFields = append("CRDCStudentDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCStudentDetail
	#'
	#' This function modifies a CRDCStudentDetail
	#' @param fieldNames The field values to give the modified CRDCStudentDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCStudentDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCStudentDetail <- function(CRDCStudentDetailID, Age = NULL, ArrestCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CorporalPunishmentIncidents = NULL, CumulativeEnrollmentPromotionStatusCode = NULL, DisciplinedBullyingDisability = NULL, DisciplinedBullyingRace = NULL, DisciplinedBullyingSex = NULL, DistrictID = NULL, EnrollmentType = NULL, ExpulsionWithoutServicesCount = NULL, ExpulsionWithServicesCount = NULL, FederalNCESSchoolID = NULL, FullName = NULL, Gender = NULL, GradeLevel = NULL, HasAdvancedMathGrade0912 = NULL, HasAlgebraIGrade07 = NULL, HasAlgebraIGrade08 = NULL, HasAlgebraIGrade0910 = NULL, HasAlgebraIGrade1112 = NULL, HasAlgebraIIGrade0912 = NULL, HasAPComputerScienceGrade0912 = NULL, HasAPCourseGrade0912 = NULL, HasAPMathGrade0912 = NULL, HasAPOtherGrade0912 = NULL, HasAPScienceGrade0912 = NULL, HasBiologyGrade0912 = NULL, HasCalculusGrade0912 = NULL, HasChemistryGrade0912 = NULL, HasComputerScienceGrade0912 = NULL, HasCreditRecovery = NULL, HasGeometryGrade08 = NULL, HasGeometryGrade0912 = NULL, HasPassedAlgebraIGrade07 = NULL, HasPassedAlgebraIGrade08 = NULL, HasPassedAlgebraIGrade0910 = NULL, HasPassedAlgebraIGrade1112 = NULL, HasPhysicsGrade0912 = NULL, HasTakenACT0912 = NULL, HasTakenSAT0912 = NULL, IDEADate = NULL, InSchoolSuspensionCount = NULL, IsAlaskan = NULL, IsAsian = NULL, IsBlack = NULL, IsChronicallyAbsent = NULL, IsFederalDistanceEducation = NULL, IsFederalDualEnrollment = NULL, IsGiftedTalented = NULL, IsHawaiian = NULL, IsHispanic = NULL, IsIDEA = NULL, IsIDEASnapshot = NULL, IsLEP = NULL, IsLEPSnapshot = NULL, IsMultiRace = NULL, IsNonLEA = NULL, IsSection504 = NULL, IsSection504Snapshot = NULL, IsWhite = NULL, LawEnforcementReferralCount = NULL, MechanicalRestraintCount = NULL, OutOfSchoolSuspensionCount = NULL, OutOfSchoolSuspensionMissedDays = NULL, PhysicalRestraintCount = NULL, ReportDate = NULL, ReportedBulliedDisability = NULL, ReportedBulliedRace = NULL, ReportedBulliedSex = NULL, SchoolID = NULL, SeclusionCount = NULL, SingleSexAthleticCount = NULL, StudentID = NULL, TransferToAlternativeSchool = NULL, ZeroToleranceWithoutServicesCount = NULL, ZeroToleranceWithServicesCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCStudentDetail", objectId = CRDCStudentDetailID, body = list(DataObject = body), searchFields = append("CRDCStudentDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolIDEASection504V1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolIDEASection504V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolIDEASection504V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolIDEASection504V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolIDEASection504V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolIDEASection504V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolIDEASection504V1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolIDEASection504V1ID = F, CreatedTime = F, FederalNCESSchoolID = F, IDEAFemaleAlaskanCount = F, IDEAFemaleAsianCount = F, IDEAFemaleBlackCount = F, IDEAFemaleHawaiianCount = F, IDEAFemaleHispanicCount = F, IDEAFemaleLEPCount = F, IDEAFemaleMultiRaceCount = F, IDEAFemaleWhiteCount = F, IDEAMaleAlaskanCount = F, IDEAMaleAsianCount = F, IDEAMaleBlackCount = F, IDEAMaleHawaiianCount = F, IDEAMaleHispanicCount = F, IDEAMaleLEPCount = F, IDEAMaleMultiRaceCount = F, IDEAMaleWhiteCount = F, ModifiedTime = F, Section504FemaleAlaskanCount = F, Section504FemaleAsianCount = F, Section504FemaleBlackCount = F, Section504FemaleHawaiianCount = F, Section504FemaleHispanicCount = F, Section504FemaleLEPCount = F, Section504FemaleMultiRaceCount = F, Section504FemaleWhiteCount = F, Section504MaleAlaskanCount = F, Section504MaleAsianCount = F, Section504MaleBlackCount = F, Section504MaleHawaiianCount = F, Section504MaleHispanicCount = F, Section504MaleLEPCount = F, Section504MaleMultiRaceCount = F, Section504MaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolIDEASection504V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolIDEASection504V1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolIDEASection504V1
	#' @param CRDCSchoolIDEASection504V1ID The ID of the CRDCSchoolIDEASection504V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolIDEASection504V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolIDEASection504V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolIDEASection504V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolIDEASection504V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolIDEASection504V1 <- function(CRDCSchoolIDEASection504V1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, IDEAFemaleAlaskanCount = F, IDEAFemaleAsianCount = F, IDEAFemaleBlackCount = F, IDEAFemaleHawaiianCount = F, IDEAFemaleHispanicCount = F, IDEAFemaleLEPCount = F, IDEAFemaleMultiRaceCount = F, IDEAFemaleWhiteCount = F, IDEAMaleAlaskanCount = F, IDEAMaleAsianCount = F, IDEAMaleBlackCount = F, IDEAMaleHawaiianCount = F, IDEAMaleHispanicCount = F, IDEAMaleLEPCount = F, IDEAMaleMultiRaceCount = F, IDEAMaleWhiteCount = F, ModifiedTime = F, Section504FemaleAlaskanCount = F, Section504FemaleAsianCount = F, Section504FemaleBlackCount = F, Section504FemaleHawaiianCount = F, Section504FemaleHispanicCount = F, Section504FemaleLEPCount = F, Section504FemaleMultiRaceCount = F, Section504FemaleWhiteCount = F, Section504MaleAlaskanCount = F, Section504MaleAsianCount = F, Section504MaleBlackCount = F, Section504MaleHawaiianCount = F, Section504MaleHispanicCount = F, Section504MaleLEPCount = F, Section504MaleMultiRaceCount = F, Section504MaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolIDEASection504V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEASection504V1", objectId = CRDCSchoolIDEASection504V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolIDEASection504V1
	#'
	#' This function deletes a CRDCSchoolIDEASection504V1
	#' @param CRDCSchoolIDEASection504V1ID The ID of the CRDCSchoolIDEASection504V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolIDEASection504V1ID of the deleted CRDCSchoolIDEASection504V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolIDEASection504V1 <- function(CRDCSchoolIDEASection504V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEASection504V1", objectId = CRDCSchoolIDEASection504V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolIDEASection504V1
	#'
	#' This function creates a CRDCSchoolIDEASection504V1
	#' @param fieldNames The field values to give the created CRDCSchoolIDEASection504V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolIDEASection504V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolIDEASection504V1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, IDEAFemaleAlaskanCount = NULL, IDEAFemaleAsianCount = NULL, IDEAFemaleBlackCount = NULL, IDEAFemaleHawaiianCount = NULL, IDEAFemaleHispanicCount = NULL, IDEAFemaleLEPCount = NULL, IDEAFemaleMultiRaceCount = NULL, IDEAFemaleWhiteCount = NULL, IDEAMaleAlaskanCount = NULL, IDEAMaleAsianCount = NULL, IDEAMaleBlackCount = NULL, IDEAMaleHawaiianCount = NULL, IDEAMaleHispanicCount = NULL, IDEAMaleLEPCount = NULL, IDEAMaleMultiRaceCount = NULL, IDEAMaleWhiteCount = NULL, Section504FemaleAlaskanCount = NULL, Section504FemaleAsianCount = NULL, Section504FemaleBlackCount = NULL, Section504FemaleHawaiianCount = NULL, Section504FemaleHispanicCount = NULL, Section504FemaleLEPCount = NULL, Section504FemaleMultiRaceCount = NULL, Section504FemaleWhiteCount = NULL, Section504MaleAlaskanCount = NULL, Section504MaleAsianCount = NULL, Section504MaleBlackCount = NULL, Section504MaleHawaiianCount = NULL, Section504MaleHispanicCount = NULL, Section504MaleLEPCount = NULL, Section504MaleMultiRaceCount = NULL, Section504MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEASection504V1", body = list(DataObject = body), searchFields = append("CRDCSchoolIDEASection504V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolIDEASection504V1
	#'
	#' This function modifies a CRDCSchoolIDEASection504V1
	#' @param fieldNames The field values to give the modified CRDCSchoolIDEASection504V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolIDEASection504V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolIDEASection504V1 <- function(CRDCSchoolIDEASection504V1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, IDEAFemaleAlaskanCount = NULL, IDEAFemaleAsianCount = NULL, IDEAFemaleBlackCount = NULL, IDEAFemaleHawaiianCount = NULL, IDEAFemaleHispanicCount = NULL, IDEAFemaleLEPCount = NULL, IDEAFemaleMultiRaceCount = NULL, IDEAFemaleWhiteCount = NULL, IDEAMaleAlaskanCount = NULL, IDEAMaleAsianCount = NULL, IDEAMaleBlackCount = NULL, IDEAMaleHawaiianCount = NULL, IDEAMaleHispanicCount = NULL, IDEAMaleLEPCount = NULL, IDEAMaleMultiRaceCount = NULL, IDEAMaleWhiteCount = NULL, Section504FemaleAlaskanCount = NULL, Section504FemaleAsianCount = NULL, Section504FemaleBlackCount = NULL, Section504FemaleHawaiianCount = NULL, Section504FemaleHispanicCount = NULL, Section504FemaleLEPCount = NULL, Section504FemaleMultiRaceCount = NULL, Section504FemaleWhiteCount = NULL, Section504MaleAlaskanCount = NULL, Section504MaleAsianCount = NULL, Section504MaleBlackCount = NULL, Section504MaleHawaiianCount = NULL, Section504MaleHispanicCount = NULL, Section504MaleLEPCount = NULL, Section504MaleMultiRaceCount = NULL, Section504MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIDEASection504V1", objectId = CRDCSchoolIDEASection504V1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolIDEASection504V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolLEPV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolLEPV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolLEPV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolLEPV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolLEPV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolLEPV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolLEPV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolLEPV1ID = F, CreatedTime = F, EnrolledFemaleAlaskanCount = F, EnrolledFemaleAsianCount = F, EnrolledFemaleBlackCount = F, EnrolledFemaleHawaiianCount = F, EnrolledFemaleHispanicCount = F, EnrolledFemaleIDEACount = F, EnrolledFemaleMultiRaceCount = F, EnrolledFemaleWhiteCount = F, EnrolledMaleAlaskanCount = F, EnrolledMaleAsianCount = F, EnrolledMaleBlackCount = F, EnrolledMaleHawaiianCount = F, EnrolledMaleHispanicCount = F, EnrolledMaleIDEACount = F, EnrolledMaleMultiRaceCount = F, EnrolledMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, StudentsFemaleAlaskanCount = F, StudentsFemaleAsianCount = F, StudentsFemaleBlackCount = F, StudentsFemaleHawaiianCount = F, StudentsFemaleHispanicCount = F, StudentsFemaleMultiRaceCount = F, StudentsFemaleWhiteCount = F, StudentsMaleAlaskanCount = F, StudentsMaleAsianCount = F, StudentsMaleBlackCount = F, StudentsMaleHawaiianCount = F, StudentsMaleHispanicCount = F, StudentsMaleMultiRaceCount = F, StudentsMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolLEPV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolLEPV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolLEPV1
	#' @param CRDCSchoolLEPV1ID The ID of the CRDCSchoolLEPV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolLEPV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolLEPV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolLEPV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolLEPV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolLEPV1 <- function(CRDCSchoolLEPV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, EnrolledFemaleAlaskanCount = F, EnrolledFemaleAsianCount = F, EnrolledFemaleBlackCount = F, EnrolledFemaleHawaiianCount = F, EnrolledFemaleHispanicCount = F, EnrolledFemaleIDEACount = F, EnrolledFemaleMultiRaceCount = F, EnrolledFemaleWhiteCount = F, EnrolledMaleAlaskanCount = F, EnrolledMaleAsianCount = F, EnrolledMaleBlackCount = F, EnrolledMaleHawaiianCount = F, EnrolledMaleHispanicCount = F, EnrolledMaleIDEACount = F, EnrolledMaleMultiRaceCount = F, EnrolledMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, StudentsFemaleAlaskanCount = F, StudentsFemaleAsianCount = F, StudentsFemaleBlackCount = F, StudentsFemaleHawaiianCount = F, StudentsFemaleHispanicCount = F, StudentsFemaleMultiRaceCount = F, StudentsFemaleWhiteCount = F, StudentsMaleAlaskanCount = F, StudentsMaleAsianCount = F, StudentsMaleBlackCount = F, StudentsMaleHawaiianCount = F, StudentsMaleHispanicCount = F, StudentsMaleMultiRaceCount = F, StudentsMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolLEPV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolLEPV1", objectId = CRDCSchoolLEPV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolLEPV1
	#'
	#' This function deletes a CRDCSchoolLEPV1
	#' @param CRDCSchoolLEPV1ID The ID of the CRDCSchoolLEPV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolLEPV1ID of the deleted CRDCSchoolLEPV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolLEPV1 <- function(CRDCSchoolLEPV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolLEPV1", objectId = CRDCSchoolLEPV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolLEPV1
	#'
	#' This function creates a CRDCSchoolLEPV1
	#' @param fieldNames The field values to give the created CRDCSchoolLEPV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolLEPV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolLEPV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EnrolledFemaleAlaskanCount = NULL, EnrolledFemaleAsianCount = NULL, EnrolledFemaleBlackCount = NULL, EnrolledFemaleHawaiianCount = NULL, EnrolledFemaleHispanicCount = NULL, EnrolledFemaleIDEACount = NULL, EnrolledFemaleMultiRaceCount = NULL, EnrolledFemaleWhiteCount = NULL, EnrolledMaleAlaskanCount = NULL, EnrolledMaleAsianCount = NULL, EnrolledMaleBlackCount = NULL, EnrolledMaleHawaiianCount = NULL, EnrolledMaleHispanicCount = NULL, EnrolledMaleIDEACount = NULL, EnrolledMaleMultiRaceCount = NULL, EnrolledMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, StudentsFemaleAlaskanCount = NULL, StudentsFemaleAsianCount = NULL, StudentsFemaleBlackCount = NULL, StudentsFemaleHawaiianCount = NULL, StudentsFemaleHispanicCount = NULL, StudentsFemaleMultiRaceCount = NULL, StudentsFemaleWhiteCount = NULL, StudentsMaleAlaskanCount = NULL, StudentsMaleAsianCount = NULL, StudentsMaleBlackCount = NULL, StudentsMaleHawaiianCount = NULL, StudentsMaleHispanicCount = NULL, StudentsMaleMultiRaceCount = NULL, StudentsMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolLEPV1", body = list(DataObject = body), searchFields = append("CRDCSchoolLEPV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolLEPV1
	#'
	#' This function modifies a CRDCSchoolLEPV1
	#' @param fieldNames The field values to give the modified CRDCSchoolLEPV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolLEPV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolLEPV1 <- function(CRDCSchoolLEPV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EnrolledFemaleAlaskanCount = NULL, EnrolledFemaleAsianCount = NULL, EnrolledFemaleBlackCount = NULL, EnrolledFemaleHawaiianCount = NULL, EnrolledFemaleHispanicCount = NULL, EnrolledFemaleIDEACount = NULL, EnrolledFemaleMultiRaceCount = NULL, EnrolledFemaleWhiteCount = NULL, EnrolledMaleAlaskanCount = NULL, EnrolledMaleAsianCount = NULL, EnrolledMaleBlackCount = NULL, EnrolledMaleHawaiianCount = NULL, EnrolledMaleHispanicCount = NULL, EnrolledMaleIDEACount = NULL, EnrolledMaleMultiRaceCount = NULL, EnrolledMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, StudentsFemaleAlaskanCount = NULL, StudentsFemaleAsianCount = NULL, StudentsFemaleBlackCount = NULL, StudentsFemaleHawaiianCount = NULL, StudentsFemaleHispanicCount = NULL, StudentsFemaleMultiRaceCount = NULL, StudentsFemaleWhiteCount = NULL, StudentsMaleAlaskanCount = NULL, StudentsMaleAsianCount = NULL, StudentsMaleBlackCount = NULL, StudentsMaleHawaiianCount = NULL, StudentsMaleHispanicCount = NULL, StudentsMaleMultiRaceCount = NULL, StudentsMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolLEPV1", objectId = CRDCSchoolLEPV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolLEPV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolEnrollmentV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolEnrollmentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolEnrollmentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolEnrollmentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolEnrollmentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolEnrollmentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolEnrollmentV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolEnrollmentV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolEnrollmentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolEnrollmentV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolEnrollmentV1
	#' @param CRDCSchoolEnrollmentV1ID The ID of the CRDCSchoolEnrollmentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolEnrollmentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolEnrollmentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolEnrollmentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolEnrollmentV1 <- function(CRDCSchoolEnrollmentV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolEnrollmentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolEnrollmentV1", objectId = CRDCSchoolEnrollmentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolEnrollmentV1
	#'
	#' This function deletes a CRDCSchoolEnrollmentV1
	#' @param CRDCSchoolEnrollmentV1ID The ID of the CRDCSchoolEnrollmentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolEnrollmentV1ID of the deleted CRDCSchoolEnrollmentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolEnrollmentV1 <- function(CRDCSchoolEnrollmentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolEnrollmentV1", objectId = CRDCSchoolEnrollmentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolEnrollmentV1
	#'
	#' This function creates a CRDCSchoolEnrollmentV1
	#' @param fieldNames The field values to give the created CRDCSchoolEnrollmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolEnrollmentV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolEnrollmentV1", body = list(DataObject = body), searchFields = append("CRDCSchoolEnrollmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolEnrollmentV1
	#'
	#' This function modifies a CRDCSchoolEnrollmentV1
	#' @param fieldNames The field values to give the modified CRDCSchoolEnrollmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolEnrollmentV1 <- function(CRDCSchoolEnrollmentV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolEnrollmentV1", objectId = CRDCSchoolEnrollmentV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolEnrollmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolHSAlgebraGeometryV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolHSAlgebraGeometryV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHSAlgebraGeometryV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHSAlgebraGeometryV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHSAlgebraGeometryV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolHSAlgebraGeometryV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolHSAlgebraGeometryV1s <- function(searchConditionsList = NULL, AlgebraIClassCount = F, AlgebraIGrade0910FemaleAlaskanCount = F, AlgebraIGrade0910FemaleAsianCount = F, AlgebraIGrade0910FemaleBlackCount = F, AlgebraIGrade0910FemaleHawaiianCount = F, AlgebraIGrade0910FemaleHispanicCount = F, AlgebraIGrade0910FemaleIDEACount = F, AlgebraIGrade0910FemaleLEPCount = F, AlgebraIGrade0910FemaleMultiRaceCount = F, AlgebraIGrade0910FemaleWhiteCount = F, AlgebraIGrade0910MaleAlaskanCount = F, AlgebraIGrade0910MaleAsianCount = F, AlgebraIGrade0910MaleBlackCount = F, AlgebraIGrade0910MaleHawaiianCount = F, AlgebraIGrade0910MaleHispanicCount = F, AlgebraIGrade0910MaleIDEACount = F, AlgebraIGrade0910MaleLEPCount = F, AlgebraIGrade0910MaleMultiRaceCount = F, AlgebraIGrade0910MaleWhiteCount = F, AlgebraIGrade1112FemaleAlaskanCount = F, AlgebraIGrade1112FemaleAsianCount = F, AlgebraIGrade1112FemaleBlackCount = F, AlgebraIGrade1112FemaleHawaiianCount = F, AlgebraIGrade1112FemaleHispanicCount = F, AlgebraIGrade1112FemaleIDEACount = F, AlgebraIGrade1112FemaleLEPCount = F, AlgebraIGrade1112FemaleMultiRaceCount = F, AlgebraIGrade1112FemaleWhiteCount = F, AlgebraIGrade1112MaleAlaskanCount = F, AlgebraIGrade1112MaleAsianCount = F, AlgebraIGrade1112MaleBlackCount = F, AlgebraIGrade1112MaleHawaiianCount = F, AlgebraIGrade1112MaleHispanicCount = F, AlgebraIGrade1112MaleIDEACount = F, AlgebraIGrade1112MaleLEPCount = F, AlgebraIGrade1112MaleMultiRaceCount = F, AlgebraIGrade1112MaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolHSAlgebraGeometryV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, GeometryClassCount = F, GeometryFemaleAlaskanCount = F, GeometryFemaleAsianCount = F, GeometryFemaleBlackCount = F, GeometryFemaleHawaiianCount = F, GeometryFemaleHispanicCount = F, GeometryFemaleIDEACount = F, GeometryFemaleLEPCount = F, GeometryFemaleMultiRaceCount = F, GeometryFemaleWhiteCount = F, GeometryMaleAlaskanCount = F, GeometryMaleAsianCount = F, GeometryMaleBlackCount = F, GeometryMaleHawaiianCount = F, GeometryMaleHispanicCount = F, GeometryMaleIDEACount = F, GeometryMaleLEPCount = F, GeometryMaleMultiRaceCount = F, GeometryMaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolHSAlgebraGeometryV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolHSAlgebraGeometryV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolHSAlgebraGeometryV1
	#' @param CRDCSchoolHSAlgebraGeometryV1ID The ID of the CRDCSchoolHSAlgebraGeometryV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHSAlgebraGeometryV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHSAlgebraGeometryV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHSAlgebraGeometryV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolHSAlgebraGeometryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolHSAlgebraGeometryV1 <- function(CRDCSchoolHSAlgebraGeometryV1ID, AlgebraIClassCount = F, AlgebraIGrade0910FemaleAlaskanCount = F, AlgebraIGrade0910FemaleAsianCount = F, AlgebraIGrade0910FemaleBlackCount = F, AlgebraIGrade0910FemaleHawaiianCount = F, AlgebraIGrade0910FemaleHispanicCount = F, AlgebraIGrade0910FemaleIDEACount = F, AlgebraIGrade0910FemaleLEPCount = F, AlgebraIGrade0910FemaleMultiRaceCount = F, AlgebraIGrade0910FemaleWhiteCount = F, AlgebraIGrade0910MaleAlaskanCount = F, AlgebraIGrade0910MaleAsianCount = F, AlgebraIGrade0910MaleBlackCount = F, AlgebraIGrade0910MaleHawaiianCount = F, AlgebraIGrade0910MaleHispanicCount = F, AlgebraIGrade0910MaleIDEACount = F, AlgebraIGrade0910MaleLEPCount = F, AlgebraIGrade0910MaleMultiRaceCount = F, AlgebraIGrade0910MaleWhiteCount = F, AlgebraIGrade1112FemaleAlaskanCount = F, AlgebraIGrade1112FemaleAsianCount = F, AlgebraIGrade1112FemaleBlackCount = F, AlgebraIGrade1112FemaleHawaiianCount = F, AlgebraIGrade1112FemaleHispanicCount = F, AlgebraIGrade1112FemaleIDEACount = F, AlgebraIGrade1112FemaleLEPCount = F, AlgebraIGrade1112FemaleMultiRaceCount = F, AlgebraIGrade1112FemaleWhiteCount = F, AlgebraIGrade1112MaleAlaskanCount = F, AlgebraIGrade1112MaleAsianCount = F, AlgebraIGrade1112MaleBlackCount = F, AlgebraIGrade1112MaleHawaiianCount = F, AlgebraIGrade1112MaleHispanicCount = F, AlgebraIGrade1112MaleIDEACount = F, AlgebraIGrade1112MaleLEPCount = F, AlgebraIGrade1112MaleMultiRaceCount = F, AlgebraIGrade1112MaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, GeometryClassCount = F, GeometryFemaleAlaskanCount = F, GeometryFemaleAsianCount = F, GeometryFemaleBlackCount = F, GeometryFemaleHawaiianCount = F, GeometryFemaleHispanicCount = F, GeometryFemaleIDEACount = F, GeometryFemaleLEPCount = F, GeometryFemaleMultiRaceCount = F, GeometryFemaleWhiteCount = F, GeometryMaleAlaskanCount = F, GeometryMaleAsianCount = F, GeometryMaleBlackCount = F, GeometryMaleHawaiianCount = F, GeometryMaleHispanicCount = F, GeometryMaleIDEACount = F, GeometryMaleLEPCount = F, GeometryMaleMultiRaceCount = F, GeometryMaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolHSAlgebraGeometryV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSAlgebraGeometryV1", objectId = CRDCSchoolHSAlgebraGeometryV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolHSAlgebraGeometryV1
	#'
	#' This function deletes a CRDCSchoolHSAlgebraGeometryV1
	#' @param CRDCSchoolHSAlgebraGeometryV1ID The ID of the CRDCSchoolHSAlgebraGeometryV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolHSAlgebraGeometryV1ID of the deleted CRDCSchoolHSAlgebraGeometryV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolHSAlgebraGeometryV1 <- function(CRDCSchoolHSAlgebraGeometryV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSAlgebraGeometryV1", objectId = CRDCSchoolHSAlgebraGeometryV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolHSAlgebraGeometryV1
	#'
	#' This function creates a CRDCSchoolHSAlgebraGeometryV1
	#' @param fieldNames The field values to give the created CRDCSchoolHSAlgebraGeometryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolHSAlgebraGeometryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolHSAlgebraGeometryV1 <- function(AlgebraIClassCount = NULL, AlgebraIGrade0910FemaleAlaskanCount = NULL, AlgebraIGrade0910FemaleAsianCount = NULL, AlgebraIGrade0910FemaleBlackCount = NULL, AlgebraIGrade0910FemaleHawaiianCount = NULL, AlgebraIGrade0910FemaleHispanicCount = NULL, AlgebraIGrade0910FemaleIDEACount = NULL, AlgebraIGrade0910FemaleLEPCount = NULL, AlgebraIGrade0910FemaleMultiRaceCount = NULL, AlgebraIGrade0910FemaleWhiteCount = NULL, AlgebraIGrade0910MaleAlaskanCount = NULL, AlgebraIGrade0910MaleAsianCount = NULL, AlgebraIGrade0910MaleBlackCount = NULL, AlgebraIGrade0910MaleHawaiianCount = NULL, AlgebraIGrade0910MaleHispanicCount = NULL, AlgebraIGrade0910MaleIDEACount = NULL, AlgebraIGrade0910MaleLEPCount = NULL, AlgebraIGrade0910MaleMultiRaceCount = NULL, AlgebraIGrade0910MaleWhiteCount = NULL, AlgebraIGrade1112FemaleAlaskanCount = NULL, AlgebraIGrade1112FemaleAsianCount = NULL, AlgebraIGrade1112FemaleBlackCount = NULL, AlgebraIGrade1112FemaleHawaiianCount = NULL, AlgebraIGrade1112FemaleHispanicCount = NULL, AlgebraIGrade1112FemaleIDEACount = NULL, AlgebraIGrade1112FemaleLEPCount = NULL, AlgebraIGrade1112FemaleMultiRaceCount = NULL, AlgebraIGrade1112FemaleWhiteCount = NULL, AlgebraIGrade1112MaleAlaskanCount = NULL, AlgebraIGrade1112MaleAsianCount = NULL, AlgebraIGrade1112MaleBlackCount = NULL, AlgebraIGrade1112MaleHawaiianCount = NULL, AlgebraIGrade1112MaleHispanicCount = NULL, AlgebraIGrade1112MaleIDEACount = NULL, AlgebraIGrade1112MaleLEPCount = NULL, AlgebraIGrade1112MaleMultiRaceCount = NULL, AlgebraIGrade1112MaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, GeometryClassCount = NULL, GeometryFemaleAlaskanCount = NULL, GeometryFemaleAsianCount = NULL, GeometryFemaleBlackCount = NULL, GeometryFemaleHawaiianCount = NULL, GeometryFemaleHispanicCount = NULL, GeometryFemaleIDEACount = NULL, GeometryFemaleLEPCount = NULL, GeometryFemaleMultiRaceCount = NULL, GeometryFemaleWhiteCount = NULL, GeometryMaleAlaskanCount = NULL, GeometryMaleAsianCount = NULL, GeometryMaleBlackCount = NULL, GeometryMaleHawaiianCount = NULL, GeometryMaleHispanicCount = NULL, GeometryMaleIDEACount = NULL, GeometryMaleLEPCount = NULL, GeometryMaleMultiRaceCount = NULL, GeometryMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSAlgebraGeometryV1", body = list(DataObject = body), searchFields = append("CRDCSchoolHSAlgebraGeometryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolHSAlgebraGeometryV1
	#'
	#' This function modifies a CRDCSchoolHSAlgebraGeometryV1
	#' @param fieldNames The field values to give the modified CRDCSchoolHSAlgebraGeometryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolHSAlgebraGeometryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolHSAlgebraGeometryV1 <- function(CRDCSchoolHSAlgebraGeometryV1ID, AlgebraIClassCount = NULL, AlgebraIGrade0910FemaleAlaskanCount = NULL, AlgebraIGrade0910FemaleAsianCount = NULL, AlgebraIGrade0910FemaleBlackCount = NULL, AlgebraIGrade0910FemaleHawaiianCount = NULL, AlgebraIGrade0910FemaleHispanicCount = NULL, AlgebraIGrade0910FemaleIDEACount = NULL, AlgebraIGrade0910FemaleLEPCount = NULL, AlgebraIGrade0910FemaleMultiRaceCount = NULL, AlgebraIGrade0910FemaleWhiteCount = NULL, AlgebraIGrade0910MaleAlaskanCount = NULL, AlgebraIGrade0910MaleAsianCount = NULL, AlgebraIGrade0910MaleBlackCount = NULL, AlgebraIGrade0910MaleHawaiianCount = NULL, AlgebraIGrade0910MaleHispanicCount = NULL, AlgebraIGrade0910MaleIDEACount = NULL, AlgebraIGrade0910MaleLEPCount = NULL, AlgebraIGrade0910MaleMultiRaceCount = NULL, AlgebraIGrade0910MaleWhiteCount = NULL, AlgebraIGrade1112FemaleAlaskanCount = NULL, AlgebraIGrade1112FemaleAsianCount = NULL, AlgebraIGrade1112FemaleBlackCount = NULL, AlgebraIGrade1112FemaleHawaiianCount = NULL, AlgebraIGrade1112FemaleHispanicCount = NULL, AlgebraIGrade1112FemaleIDEACount = NULL, AlgebraIGrade1112FemaleLEPCount = NULL, AlgebraIGrade1112FemaleMultiRaceCount = NULL, AlgebraIGrade1112FemaleWhiteCount = NULL, AlgebraIGrade1112MaleAlaskanCount = NULL, AlgebraIGrade1112MaleAsianCount = NULL, AlgebraIGrade1112MaleBlackCount = NULL, AlgebraIGrade1112MaleHawaiianCount = NULL, AlgebraIGrade1112MaleHispanicCount = NULL, AlgebraIGrade1112MaleIDEACount = NULL, AlgebraIGrade1112MaleLEPCount = NULL, AlgebraIGrade1112MaleMultiRaceCount = NULL, AlgebraIGrade1112MaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, GeometryClassCount = NULL, GeometryFemaleAlaskanCount = NULL, GeometryFemaleAsianCount = NULL, GeometryFemaleBlackCount = NULL, GeometryFemaleHawaiianCount = NULL, GeometryFemaleHispanicCount = NULL, GeometryFemaleIDEACount = NULL, GeometryFemaleLEPCount = NULL, GeometryFemaleMultiRaceCount = NULL, GeometryFemaleWhiteCount = NULL, GeometryMaleAlaskanCount = NULL, GeometryMaleAsianCount = NULL, GeometryMaleBlackCount = NULL, GeometryMaleHawaiianCount = NULL, GeometryMaleHispanicCount = NULL, GeometryMaleIDEACount = NULL, GeometryMaleLEPCount = NULL, GeometryMaleMultiRaceCount = NULL, GeometryMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSAlgebraGeometryV1", objectId = CRDCSchoolHSAlgebraGeometryV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolHSAlgebraGeometryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEACredentialV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEACredentialV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEACredentialV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEACredentialV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEACredentialV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEACredentialV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEACredentialV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEACredentialV1ID = F, CreatedTime = F, DistrictID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEACredentialV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEACredentialV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEACredentialV1
	#' @param CRDCLEACredentialV1ID The ID of the CRDCLEACredentialV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEACredentialV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEACredentialV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEACredentialV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEACredentialV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEACredentialV1 <- function(CRDCLEACredentialV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEACredentialV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACredentialV1", objectId = CRDCLEACredentialV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEACredentialV1
	#'
	#' This function deletes a CRDCLEACredentialV1
	#' @param CRDCLEACredentialV1ID The ID of the CRDCLEACredentialV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEACredentialV1ID of the deleted CRDCLEACredentialV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEACredentialV1 <- function(CRDCLEACredentialV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACredentialV1", objectId = CRDCLEACredentialV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEACredentialV1
	#'
	#' This function creates a CRDCLEACredentialV1
	#' @param fieldNames The field values to give the created CRDCLEACredentialV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEACredentialV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEACredentialV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEACredentialV1", body = list(DataObject = body), searchFields = append("CRDCLEACredentialV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEACredentialV1
	#'
	#' This function modifies a CRDCLEACredentialV1
	#' @param fieldNames The field values to give the modified CRDCLEACredentialV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEACredentialV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEACredentialV1 <- function(CRDCLEACredentialV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEACredentialV1", objectId = CRDCLEACredentialV1ID, body = list(DataObject = body), searchFields = append("CRDCLEACredentialV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEAEquivalencyV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEAEquivalencyV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAEquivalencyV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAEquivalencyV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAEquivalencyV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEAEquivalencyV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEAEquivalencyV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEAEquivalencyV1ID = F, CreatedTime = F, DistrictID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasPreparationProgram = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEAEquivalencyV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEAEquivalencyV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEAEquivalencyV1
	#' @param CRDCLEAEquivalencyV1ID The ID of the CRDCLEAEquivalencyV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAEquivalencyV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAEquivalencyV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAEquivalencyV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEAEquivalencyV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEAEquivalencyV1 <- function(CRDCLEAEquivalencyV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasPreparationProgram = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEAEquivalencyV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEquivalencyV1", objectId = CRDCLEAEquivalencyV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEAEquivalencyV1
	#'
	#' This function deletes a CRDCLEAEquivalencyV1
	#' @param CRDCLEAEquivalencyV1ID The ID of the CRDCLEAEquivalencyV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEAEquivalencyV1ID of the deleted CRDCLEAEquivalencyV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEAEquivalencyV1 <- function(CRDCLEAEquivalencyV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEquivalencyV1", objectId = CRDCLEAEquivalencyV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEAEquivalencyV1
	#'
	#' This function creates a CRDCLEAEquivalencyV1
	#' @param fieldNames The field values to give the created CRDCLEAEquivalencyV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEAEquivalencyV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEAEquivalencyV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasPreparationProgram = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEquivalencyV1", body = list(DataObject = body), searchFields = append("CRDCLEAEquivalencyV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEAEquivalencyV1
	#'
	#' This function modifies a CRDCLEAEquivalencyV1
	#' @param fieldNames The field values to give the modified CRDCLEAEquivalencyV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEAEquivalencyV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEAEquivalencyV1 <- function(CRDCLEAEquivalencyV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasPreparationProgram = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEquivalencyV1", objectId = CRDCLEAEquivalencyV1ID, body = list(DataObject = body), searchFields = append("CRDCLEAEquivalencyV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolCharacteristicV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolCharacteristicV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolCharacteristicV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolCharacteristicV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolCharacteristicV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolCharacteristicV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolCharacteristicV1s <- function(searchConditionsList = NULL, AlternativeSchoolFocus = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolCharacteristicV1ID = F, CreatedTime = F, FederalAlternativeSchoolDetailID = F, FederalNCESSchoolID = F, HasPreschoolNonIDEAAge3 = F, HasPreschoolNonIDEAAge4 = F, HasPreschoolNonIDEAAge5 = F, HasUngradedElementary = F, HasUngradedElementaryMiddleSchool = F, HasUngradedElementaryMiddleSchoolHighSchool = F, HasUngradedHighSchool = F, HasUngradedMiddleSchool = F, HasUngradedMiddleSchoolHighSchool = F, IsAlternative = F, IsAlternativeAcademic = F, IsAlternativeAcademicDiscipline = F, IsAlternativeDiscipline = F, IsCharter = F, IsEntireSchoolMagnet = F, IsMagnet = F, IsSpecialEducation = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolCharacteristicV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolCharacteristicV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolCharacteristicV1
	#' @param CRDCSchoolCharacteristicV1ID The ID of the CRDCSchoolCharacteristicV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolCharacteristicV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolCharacteristicV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolCharacteristicV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolCharacteristicV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolCharacteristicV1 <- function(CRDCSchoolCharacteristicV1ID, AlternativeSchoolFocus = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalAlternativeSchoolDetailID = F, FederalNCESSchoolID = F, HasPreschoolNonIDEAAge3 = F, HasPreschoolNonIDEAAge4 = F, HasPreschoolNonIDEAAge5 = F, HasUngradedElementary = F, HasUngradedElementaryMiddleSchool = F, HasUngradedElementaryMiddleSchoolHighSchool = F, HasUngradedHighSchool = F, HasUngradedMiddleSchool = F, HasUngradedMiddleSchoolHighSchool = F, IsAlternative = F, IsAlternativeAcademic = F, IsAlternativeAcademicDiscipline = F, IsAlternativeDiscipline = F, IsCharter = F, IsEntireSchoolMagnet = F, IsMagnet = F, IsSpecialEducation = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolCharacteristicV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCharacteristicV1", objectId = CRDCSchoolCharacteristicV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolCharacteristicV1
	#'
	#' This function deletes a CRDCSchoolCharacteristicV1
	#' @param CRDCSchoolCharacteristicV1ID The ID of the CRDCSchoolCharacteristicV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolCharacteristicV1ID of the deleted CRDCSchoolCharacteristicV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolCharacteristicV1 <- function(CRDCSchoolCharacteristicV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCharacteristicV1", objectId = CRDCSchoolCharacteristicV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolCharacteristicV1
	#'
	#' This function creates a CRDCSchoolCharacteristicV1
	#' @param fieldNames The field values to give the created CRDCSchoolCharacteristicV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolCharacteristicV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolCharacteristicV1 <- function(AlternativeSchoolFocus = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalAlternativeSchoolDetailID = NULL, FederalNCESSchoolID = NULL, HasPreschoolNonIDEAAge3 = NULL, HasPreschoolNonIDEAAge4 = NULL, HasPreschoolNonIDEAAge5 = NULL, HasUngradedElementary = NULL, HasUngradedHighSchool = NULL, HasUngradedMiddleSchool = NULL, IsAlternative = NULL, IsCharter = NULL, IsEntireSchoolMagnet = NULL, IsMagnet = NULL, IsSpecialEducation = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCharacteristicV1", body = list(DataObject = body), searchFields = append("CRDCSchoolCharacteristicV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolCharacteristicV1
	#'
	#' This function modifies a CRDCSchoolCharacteristicV1
	#' @param fieldNames The field values to give the modified CRDCSchoolCharacteristicV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolCharacteristicV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolCharacteristicV1 <- function(CRDCSchoolCharacteristicV1ID, AlternativeSchoolFocus = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalAlternativeSchoolDetailID = NULL, FederalNCESSchoolID = NULL, HasPreschoolNonIDEAAge3 = NULL, HasPreschoolNonIDEAAge4 = NULL, HasPreschoolNonIDEAAge5 = NULL, HasUngradedElementary = NULL, HasUngradedHighSchool = NULL, HasUngradedMiddleSchool = NULL, IsAlternative = NULL, IsCharter = NULL, IsEntireSchoolMagnet = NULL, IsMagnet = NULL, IsSpecialEducation = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCharacteristicV1", objectId = CRDCSchoolCharacteristicV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolCharacteristicV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolGradeV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolGradeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolGradeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolGradeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolGradeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolGradeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolGradeV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolGradeV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, HasGrade01 = F, HasGrade02 = F, HasGrade03 = F, HasGrade04 = F, HasGrade05 = F, HasGrade06 = F, HasGrade07 = F, HasGrade08 = F, HasGrade09 = F, HasGrade10 = F, HasGrade11 = F, HasGrade12 = F, HasKindergarten = F, HasPreschool = F, HasUngraded = F, ModifiedTime = F, NCESIDCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolGradeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolGradeV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolGradeV1
	#' @param CRDCSchoolGradeV1ID The ID of the CRDCSchoolGradeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolGradeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolGradeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolGradeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolGradeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolGradeV1 <- function(CRDCSchoolGradeV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, HasGrade01 = F, HasGrade02 = F, HasGrade03 = F, HasGrade04 = F, HasGrade05 = F, HasGrade06 = F, HasGrade07 = F, HasGrade08 = F, HasGrade09 = F, HasGrade10 = F, HasGrade11 = F, HasGrade12 = F, HasKindergarten = F, HasPreschool = F, HasUngraded = F, ModifiedTime = F, NCESIDCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolGradeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGradeV1", objectId = CRDCSchoolGradeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolGradeV1
	#'
	#' This function deletes a CRDCSchoolGradeV1
	#' @param CRDCSchoolGradeV1ID The ID of the CRDCSchoolGradeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolGradeV1ID of the deleted CRDCSchoolGradeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolGradeV1 <- function(CRDCSchoolGradeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGradeV1", objectId = CRDCSchoolGradeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolGradeV1
	#'
	#' This function creates a CRDCSchoolGradeV1
	#' @param fieldNames The field values to give the created CRDCSchoolGradeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolGradeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolGradeV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasGrade01 = NULL, HasGrade02 = NULL, HasGrade03 = NULL, HasGrade04 = NULL, HasGrade05 = NULL, HasGrade06 = NULL, HasGrade07 = NULL, HasGrade08 = NULL, HasGrade09 = NULL, HasGrade10 = NULL, HasGrade11 = NULL, HasGrade12 = NULL, HasKindergarten = NULL, HasPreschool = NULL, HasUngraded = NULL, NCESIDCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGradeV1", body = list(DataObject = body), searchFields = append("CRDCSchoolGradeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolGradeV1
	#'
	#' This function modifies a CRDCSchoolGradeV1
	#' @param fieldNames The field values to give the modified CRDCSchoolGradeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolGradeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolGradeV1 <- function(CRDCSchoolGradeV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasGrade01 = NULL, HasGrade02 = NULL, HasGrade03 = NULL, HasGrade04 = NULL, HasGrade05 = NULL, HasGrade06 = NULL, HasGrade07 = NULL, HasGrade08 = NULL, HasGrade09 = NULL, HasGrade10 = NULL, HasGrade11 = NULL, HasGrade12 = NULL, HasKindergarten = NULL, HasPreschool = NULL, HasUngraded = NULL, NCESIDCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGradeV1", objectId = CRDCSchoolGradeV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolGradeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolPreschoolV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolPreschoolV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolPreschoolV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolPreschoolV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolPreschoolV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolPreschoolV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolPreschoolV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolPreschoolV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolPreschoolV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolPreschoolV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolPreschoolV1
	#' @param CRDCSchoolPreschoolV1ID The ID of the CRDCSchoolPreschoolV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolPreschoolV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolPreschoolV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolPreschoolV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolPreschoolV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolPreschoolV1 <- function(CRDCSchoolPreschoolV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolPreschoolV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPreschoolV1", objectId = CRDCSchoolPreschoolV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolPreschoolV1
	#'
	#' This function deletes a CRDCSchoolPreschoolV1
	#' @param CRDCSchoolPreschoolV1ID The ID of the CRDCSchoolPreschoolV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolPreschoolV1ID of the deleted CRDCSchoolPreschoolV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolPreschoolV1 <- function(CRDCSchoolPreschoolV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPreschoolV1", objectId = CRDCSchoolPreschoolV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolPreschoolV1
	#'
	#' This function creates a CRDCSchoolPreschoolV1
	#' @param fieldNames The field values to give the created CRDCSchoolPreschoolV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolPreschoolV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolPreschoolV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPreschoolV1", body = list(DataObject = body), searchFields = append("CRDCSchoolPreschoolV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolPreschoolV1
	#'
	#' This function modifies a CRDCSchoolPreschoolV1
	#' @param fieldNames The field values to give the modified CRDCSchoolPreschoolV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolPreschoolV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolPreschoolV1 <- function(CRDCSchoolPreschoolV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolPreschoolV1", objectId = CRDCSchoolPreschoolV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolPreschoolV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolGiftedEnrollmentV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolGiftedEnrollmentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolGiftedEnrollmentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolGiftedEnrollmentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolGiftedEnrollmentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolGiftedEnrollmentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolGiftedEnrollmentV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolGiftedEnrollmentV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolGiftedEnrollmentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolGiftedEnrollmentV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolGiftedEnrollmentV1
	#' @param CRDCSchoolGiftedEnrollmentV1ID The ID of the CRDCSchoolGiftedEnrollmentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolGiftedEnrollmentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolGiftedEnrollmentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolGiftedEnrollmentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolGiftedEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolGiftedEnrollmentV1 <- function(CRDCSchoolGiftedEnrollmentV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolGiftedEnrollmentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGiftedEnrollmentV1", objectId = CRDCSchoolGiftedEnrollmentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolGiftedEnrollmentV1
	#'
	#' This function deletes a CRDCSchoolGiftedEnrollmentV1
	#' @param CRDCSchoolGiftedEnrollmentV1ID The ID of the CRDCSchoolGiftedEnrollmentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolGiftedEnrollmentV1ID of the deleted CRDCSchoolGiftedEnrollmentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolGiftedEnrollmentV1 <- function(CRDCSchoolGiftedEnrollmentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGiftedEnrollmentV1", objectId = CRDCSchoolGiftedEnrollmentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolGiftedEnrollmentV1
	#'
	#' This function creates a CRDCSchoolGiftedEnrollmentV1
	#' @param fieldNames The field values to give the created CRDCSchoolGiftedEnrollmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolGiftedEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolGiftedEnrollmentV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGiftedEnrollmentV1", body = list(DataObject = body), searchFields = append("CRDCSchoolGiftedEnrollmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolGiftedEnrollmentV1
	#'
	#' This function modifies a CRDCSchoolGiftedEnrollmentV1
	#' @param fieldNames The field values to give the modified CRDCSchoolGiftedEnrollmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolGiftedEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolGiftedEnrollmentV1 <- function(CRDCSchoolGiftedEnrollmentV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolGiftedEnrollmentV1", objectId = CRDCSchoolGiftedEnrollmentV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolGiftedEnrollmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolMSMathV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolMSMathV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolMSMathV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolMSMathV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolMSMathV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolMSMathV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolMSMathV1s <- function(searchConditionsList = NULL, AlgebraICertifiedClassCount = F, AlgebraIClassCount = F, AlgebraIGrade07StudentCount = F, AlgebraIGrade08FemaleAlaskanCount = F, AlgebraIGrade08FemaleAsianCount = F, AlgebraIGrade08FemaleBlackCount = F, AlgebraIGrade08FemaleHawaiianCount = F, AlgebraIGrade08FemaleHispanicCount = F, AlgebraIGrade08FemaleIDEACount = F, AlgebraIGrade08FemaleLEPCount = F, AlgebraIGrade08FemaleMultiRaceCount = F, AlgebraIGrade08FemaleWhiteCount = F, AlgebraIGrade08MaleAlaskanCount = F, AlgebraIGrade08MaleAsianCount = F, AlgebraIGrade08MaleBlackCount = F, AlgebraIGrade08MaleHawaiianCount = F, AlgebraIGrade08MaleHispanicCount = F, AlgebraIGrade08MaleIDEACount = F, AlgebraIGrade08MaleLEPCount = F, AlgebraIGrade08MaleMultiRaceCount = F, AlgebraIGrade08MaleWhiteCount = F, AlgebraIGrade08StudentCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolMSMathV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, GeometryGrade08StudentCount = F, HasAlgebraIGrade07 = F, HasAlgebraIGrade08 = F, HasGeometryGrade08 = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolMSMathV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolMSMathV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolMSMathV1
	#' @param CRDCSchoolMSMathV1ID The ID of the CRDCSchoolMSMathV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolMSMathV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolMSMathV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolMSMathV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolMSMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolMSMathV1 <- function(CRDCSchoolMSMathV1ID, AlgebraICertifiedClassCount = F, AlgebraIClassCount = F, AlgebraIGrade07StudentCount = F, AlgebraIGrade08FemaleAlaskanCount = F, AlgebraIGrade08FemaleAsianCount = F, AlgebraIGrade08FemaleBlackCount = F, AlgebraIGrade08FemaleHawaiianCount = F, AlgebraIGrade08FemaleHispanicCount = F, AlgebraIGrade08FemaleIDEACount = F, AlgebraIGrade08FemaleLEPCount = F, AlgebraIGrade08FemaleMultiRaceCount = F, AlgebraIGrade08FemaleWhiteCount = F, AlgebraIGrade08MaleAlaskanCount = F, AlgebraIGrade08MaleAsianCount = F, AlgebraIGrade08MaleBlackCount = F, AlgebraIGrade08MaleHawaiianCount = F, AlgebraIGrade08MaleHispanicCount = F, AlgebraIGrade08MaleIDEACount = F, AlgebraIGrade08MaleLEPCount = F, AlgebraIGrade08MaleMultiRaceCount = F, AlgebraIGrade08MaleWhiteCount = F, AlgebraIGrade08StudentCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, GeometryGrade08StudentCount = F, HasAlgebraIGrade07 = F, HasAlgebraIGrade08 = F, HasGeometryGrade08 = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolMSMathV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSMathV1", objectId = CRDCSchoolMSMathV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolMSMathV1
	#'
	#' This function deletes a CRDCSchoolMSMathV1
	#' @param CRDCSchoolMSMathV1ID The ID of the CRDCSchoolMSMathV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolMSMathV1ID of the deleted CRDCSchoolMSMathV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolMSMathV1 <- function(CRDCSchoolMSMathV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSMathV1", objectId = CRDCSchoolMSMathV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolMSMathV1
	#'
	#' This function creates a CRDCSchoolMSMathV1
	#' @param fieldNames The field values to give the created CRDCSchoolMSMathV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolMSMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolMSMathV1 <- function(AlgebraICertifiedClassCount = NULL, AlgebraIClassCount = NULL, AlgebraIGrade07StudentCount = NULL, AlgebraIGrade08FemaleAlaskanCount = NULL, AlgebraIGrade08FemaleAsianCount = NULL, AlgebraIGrade08FemaleBlackCount = NULL, AlgebraIGrade08FemaleHawaiianCount = NULL, AlgebraIGrade08FemaleHispanicCount = NULL, AlgebraIGrade08FemaleIDEACount = NULL, AlgebraIGrade08FemaleLEPCount = NULL, AlgebraIGrade08FemaleMultiRaceCount = NULL, AlgebraIGrade08FemaleWhiteCount = NULL, AlgebraIGrade08MaleAlaskanCount = NULL, AlgebraIGrade08MaleAsianCount = NULL, AlgebraIGrade08MaleBlackCount = NULL, AlgebraIGrade08MaleHawaiianCount = NULL, AlgebraIGrade08MaleHispanicCount = NULL, AlgebraIGrade08MaleIDEACount = NULL, AlgebraIGrade08MaleLEPCount = NULL, AlgebraIGrade08MaleMultiRaceCount = NULL, AlgebraIGrade08MaleWhiteCount = NULL, AlgebraIGrade08StudentCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, GeometryGrade08StudentCount = NULL, HasAlgebraIGrade07 = NULL, HasAlgebraIGrade08 = NULL, HasGeometryGrade08 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSMathV1", body = list(DataObject = body), searchFields = append("CRDCSchoolMSMathV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolMSMathV1
	#'
	#' This function modifies a CRDCSchoolMSMathV1
	#' @param fieldNames The field values to give the modified CRDCSchoolMSMathV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolMSMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolMSMathV1 <- function(CRDCSchoolMSMathV1ID, AlgebraICertifiedClassCount = NULL, AlgebraIClassCount = NULL, AlgebraIGrade07StudentCount = NULL, AlgebraIGrade08FemaleAlaskanCount = NULL, AlgebraIGrade08FemaleAsianCount = NULL, AlgebraIGrade08FemaleBlackCount = NULL, AlgebraIGrade08FemaleHawaiianCount = NULL, AlgebraIGrade08FemaleHispanicCount = NULL, AlgebraIGrade08FemaleIDEACount = NULL, AlgebraIGrade08FemaleLEPCount = NULL, AlgebraIGrade08FemaleMultiRaceCount = NULL, AlgebraIGrade08FemaleWhiteCount = NULL, AlgebraIGrade08MaleAlaskanCount = NULL, AlgebraIGrade08MaleAsianCount = NULL, AlgebraIGrade08MaleBlackCount = NULL, AlgebraIGrade08MaleHawaiianCount = NULL, AlgebraIGrade08MaleHispanicCount = NULL, AlgebraIGrade08MaleIDEACount = NULL, AlgebraIGrade08MaleLEPCount = NULL, AlgebraIGrade08MaleMultiRaceCount = NULL, AlgebraIGrade08MaleWhiteCount = NULL, AlgebraIGrade08StudentCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, GeometryGrade08StudentCount = NULL, HasAlgebraIGrade07 = NULL, HasAlgebraIGrade08 = NULL, HasGeometryGrade08 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSMathV1", objectId = CRDCSchoolMSMathV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolMSMathV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAdvancedMathV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAdvancedMathV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAdvancedMathV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAdvancedMathV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAdvancedMathV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAdvancedMathV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAdvancedMathV1s <- function(searchConditionsList = NULL, AdvancedCertifiedClassCount = F, AdvancedClassCount = F, AdvancedFemaleAlaskanCount = F, AdvancedFemaleAsianCount = F, AdvancedFemaleBlackCount = F, AdvancedFemaleHawaiianCount = F, AdvancedFemaleHispanicCount = F, AdvancedFemaleIDEACount = F, AdvancedFemaleLEPCount = F, AdvancedFemaleMultiRaceCount = F, AdvancedFemaleWhiteCount = F, AdvancedMaleAlaskanCount = F, AdvancedMaleAsianCount = F, AdvancedMaleBlackCount = F, AdvancedMaleHawaiianCount = F, AdvancedMaleHispanicCount = F, AdvancedMaleIDEACount = F, AdvancedMaleLEPCount = F, AdvancedMaleMultiRaceCount = F, AdvancedMaleWhiteCount = F, AlgebraICertifiedClassCount = F, AlgebraIICertifiedClassCount = F, AlgebraIIClassCount = F, AlgebraIIFemaleAlaskanCount = F, AlgebraIIFemaleAsianCount = F, AlgebraIIFemaleBlackCount = F, AlgebraIIFemaleHawaiianCount = F, AlgebraIIFemaleHispanicCount = F, AlgebraIIFemaleIDEACount = F, AlgebraIIFemaleLEPCount = F, AlgebraIIFemaleMultiRaceCount = F, AlgebraIIFemaleWhiteCount = F, AlgebraIIMaleAlaskanCount = F, AlgebraIIMaleAsianCount = F, AlgebraIIMaleBlackCount = F, AlgebraIIMaleHawaiianCount = F, AlgebraIIMaleHispanicCount = F, AlgebraIIMaleIDEACount = F, AlgebraIIMaleLEPCount = F, AlgebraIIMaleMultiRaceCount = F, AlgebraIIMaleWhiteCount = F, CalculusCertifiedClassCount = F, CalculusClassCount = F, CalculusFemaleAlaskanCount = F, CalculusFemaleAsianCount = F, CalculusFemaleBlackCount = F, CalculusFemaleHawaiianCount = F, CalculusFemaleHispanicCount = F, CalculusFemaleIDEACount = F, CalculusFemaleLEPCount = F, CalculusFemaleMultiRaceCount = F, CalculusFemaleWhiteCount = F, CalculusMaleAlaskanCount = F, CalculusMaleAsianCount = F, CalculusMaleBlackCount = F, CalculusMaleHawaiianCount = F, CalculusMaleHispanicCount = F, CalculusMaleIDEACount = F, CalculusMaleLEPCount = F, CalculusMaleMultiRaceCount = F, CalculusMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAdvancedMathV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, GeometryCertifiedClassCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAdvancedMathV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAdvancedMathV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAdvancedMathV1
	#' @param CRDCSchoolAdvancedMathV1ID The ID of the CRDCSchoolAdvancedMathV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAdvancedMathV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAdvancedMathV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAdvancedMathV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAdvancedMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAdvancedMathV1 <- function(CRDCSchoolAdvancedMathV1ID, AdvancedCertifiedClassCount = F, AdvancedClassCount = F, AdvancedFemaleAlaskanCount = F, AdvancedFemaleAsianCount = F, AdvancedFemaleBlackCount = F, AdvancedFemaleHawaiianCount = F, AdvancedFemaleHispanicCount = F, AdvancedFemaleIDEACount = F, AdvancedFemaleLEPCount = F, AdvancedFemaleMultiRaceCount = F, AdvancedFemaleWhiteCount = F, AdvancedMaleAlaskanCount = F, AdvancedMaleAsianCount = F, AdvancedMaleBlackCount = F, AdvancedMaleHawaiianCount = F, AdvancedMaleHispanicCount = F, AdvancedMaleIDEACount = F, AdvancedMaleLEPCount = F, AdvancedMaleMultiRaceCount = F, AdvancedMaleWhiteCount = F, AlgebraICertifiedClassCount = F, AlgebraIICertifiedClassCount = F, AlgebraIIClassCount = F, AlgebraIIFemaleAlaskanCount = F, AlgebraIIFemaleAsianCount = F, AlgebraIIFemaleBlackCount = F, AlgebraIIFemaleHawaiianCount = F, AlgebraIIFemaleHispanicCount = F, AlgebraIIFemaleIDEACount = F, AlgebraIIFemaleLEPCount = F, AlgebraIIFemaleMultiRaceCount = F, AlgebraIIFemaleWhiteCount = F, AlgebraIIMaleAlaskanCount = F, AlgebraIIMaleAsianCount = F, AlgebraIIMaleBlackCount = F, AlgebraIIMaleHawaiianCount = F, AlgebraIIMaleHispanicCount = F, AlgebraIIMaleIDEACount = F, AlgebraIIMaleLEPCount = F, AlgebraIIMaleMultiRaceCount = F, AlgebraIIMaleWhiteCount = F, CalculusCertifiedClassCount = F, CalculusClassCount = F, CalculusFemaleAlaskanCount = F, CalculusFemaleAsianCount = F, CalculusFemaleBlackCount = F, CalculusFemaleHawaiianCount = F, CalculusFemaleHispanicCount = F, CalculusFemaleIDEACount = F, CalculusFemaleLEPCount = F, CalculusFemaleMultiRaceCount = F, CalculusFemaleWhiteCount = F, CalculusMaleAlaskanCount = F, CalculusMaleAsianCount = F, CalculusMaleBlackCount = F, CalculusMaleHawaiianCount = F, CalculusMaleHispanicCount = F, CalculusMaleIDEACount = F, CalculusMaleLEPCount = F, CalculusMaleMultiRaceCount = F, CalculusMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, GeometryCertifiedClassCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAdvancedMathV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAdvancedMathV1", objectId = CRDCSchoolAdvancedMathV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAdvancedMathV1
	#'
	#' This function deletes a CRDCSchoolAdvancedMathV1
	#' @param CRDCSchoolAdvancedMathV1ID The ID of the CRDCSchoolAdvancedMathV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAdvancedMathV1ID of the deleted CRDCSchoolAdvancedMathV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAdvancedMathV1 <- function(CRDCSchoolAdvancedMathV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAdvancedMathV1", objectId = CRDCSchoolAdvancedMathV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAdvancedMathV1
	#'
	#' This function creates a CRDCSchoolAdvancedMathV1
	#' @param fieldNames The field values to give the created CRDCSchoolAdvancedMathV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAdvancedMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAdvancedMathV1 <- function(AdvancedCertifiedClassCount = NULL, AdvancedClassCount = NULL, AdvancedFemaleAlaskanCount = NULL, AdvancedFemaleAsianCount = NULL, AdvancedFemaleBlackCount = NULL, AdvancedFemaleHawaiianCount = NULL, AdvancedFemaleHispanicCount = NULL, AdvancedFemaleIDEACount = NULL, AdvancedFemaleLEPCount = NULL, AdvancedFemaleMultiRaceCount = NULL, AdvancedFemaleWhiteCount = NULL, AdvancedMaleAlaskanCount = NULL, AdvancedMaleAsianCount = NULL, AdvancedMaleBlackCount = NULL, AdvancedMaleHawaiianCount = NULL, AdvancedMaleHispanicCount = NULL, AdvancedMaleIDEACount = NULL, AdvancedMaleLEPCount = NULL, AdvancedMaleMultiRaceCount = NULL, AdvancedMaleWhiteCount = NULL, AlgebraICertifiedClassCount = NULL, AlgebraIICertifiedClassCount = NULL, AlgebraIIClassCount = NULL, AlgebraIIFemaleAlaskanCount = NULL, AlgebraIIFemaleAsianCount = NULL, AlgebraIIFemaleBlackCount = NULL, AlgebraIIFemaleHawaiianCount = NULL, AlgebraIIFemaleHispanicCount = NULL, AlgebraIIFemaleIDEACount = NULL, AlgebraIIFemaleLEPCount = NULL, AlgebraIIFemaleMultiRaceCount = NULL, AlgebraIIFemaleWhiteCount = NULL, AlgebraIIMaleAlaskanCount = NULL, AlgebraIIMaleAsianCount = NULL, AlgebraIIMaleBlackCount = NULL, AlgebraIIMaleHawaiianCount = NULL, AlgebraIIMaleHispanicCount = NULL, AlgebraIIMaleIDEACount = NULL, AlgebraIIMaleLEPCount = NULL, AlgebraIIMaleMultiRaceCount = NULL, AlgebraIIMaleWhiteCount = NULL, CalculusCertifiedClassCount = NULL, CalculusClassCount = NULL, CalculusFemaleAlaskanCount = NULL, CalculusFemaleAsianCount = NULL, CalculusFemaleBlackCount = NULL, CalculusFemaleHawaiianCount = NULL, CalculusFemaleHispanicCount = NULL, CalculusFemaleIDEACount = NULL, CalculusFemaleLEPCount = NULL, CalculusFemaleMultiRaceCount = NULL, CalculusFemaleWhiteCount = NULL, CalculusMaleAlaskanCount = NULL, CalculusMaleAsianCount = NULL, CalculusMaleBlackCount = NULL, CalculusMaleHawaiianCount = NULL, CalculusMaleHispanicCount = NULL, CalculusMaleIDEACount = NULL, CalculusMaleLEPCount = NULL, CalculusMaleMultiRaceCount = NULL, CalculusMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, GeometryCertifiedClassCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAdvancedMathV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAdvancedMathV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAdvancedMathV1
	#'
	#' This function modifies a CRDCSchoolAdvancedMathV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAdvancedMathV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAdvancedMathV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAdvancedMathV1 <- function(CRDCSchoolAdvancedMathV1ID, AdvancedCertifiedClassCount = NULL, AdvancedClassCount = NULL, AdvancedFemaleAlaskanCount = NULL, AdvancedFemaleAsianCount = NULL, AdvancedFemaleBlackCount = NULL, AdvancedFemaleHawaiianCount = NULL, AdvancedFemaleHispanicCount = NULL, AdvancedFemaleIDEACount = NULL, AdvancedFemaleLEPCount = NULL, AdvancedFemaleMultiRaceCount = NULL, AdvancedFemaleWhiteCount = NULL, AdvancedMaleAlaskanCount = NULL, AdvancedMaleAsianCount = NULL, AdvancedMaleBlackCount = NULL, AdvancedMaleHawaiianCount = NULL, AdvancedMaleHispanicCount = NULL, AdvancedMaleIDEACount = NULL, AdvancedMaleLEPCount = NULL, AdvancedMaleMultiRaceCount = NULL, AdvancedMaleWhiteCount = NULL, AlgebraICertifiedClassCount = NULL, AlgebraIICertifiedClassCount = NULL, AlgebraIIClassCount = NULL, AlgebraIIFemaleAlaskanCount = NULL, AlgebraIIFemaleAsianCount = NULL, AlgebraIIFemaleBlackCount = NULL, AlgebraIIFemaleHawaiianCount = NULL, AlgebraIIFemaleHispanicCount = NULL, AlgebraIIFemaleIDEACount = NULL, AlgebraIIFemaleLEPCount = NULL, AlgebraIIFemaleMultiRaceCount = NULL, AlgebraIIFemaleWhiteCount = NULL, AlgebraIIMaleAlaskanCount = NULL, AlgebraIIMaleAsianCount = NULL, AlgebraIIMaleBlackCount = NULL, AlgebraIIMaleHawaiianCount = NULL, AlgebraIIMaleHispanicCount = NULL, AlgebraIIMaleIDEACount = NULL, AlgebraIIMaleLEPCount = NULL, AlgebraIIMaleMultiRaceCount = NULL, AlgebraIIMaleWhiteCount = NULL, CalculusCertifiedClassCount = NULL, CalculusClassCount = NULL, CalculusFemaleAlaskanCount = NULL, CalculusFemaleAsianCount = NULL, CalculusFemaleBlackCount = NULL, CalculusFemaleHawaiianCount = NULL, CalculusFemaleHispanicCount = NULL, CalculusFemaleIDEACount = NULL, CalculusFemaleLEPCount = NULL, CalculusFemaleMultiRaceCount = NULL, CalculusFemaleWhiteCount = NULL, CalculusMaleAlaskanCount = NULL, CalculusMaleAsianCount = NULL, CalculusMaleBlackCount = NULL, CalculusMaleHawaiianCount = NULL, CalculusMaleHispanicCount = NULL, CalculusMaleIDEACount = NULL, CalculusMaleLEPCount = NULL, CalculusMaleMultiRaceCount = NULL, CalculusMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, GeometryCertifiedClassCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAdvancedMathV1", objectId = CRDCSchoolAdvancedMathV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAdvancedMathV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolRetentionCountV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolRetentionCountV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolRetentionCountV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolRetentionCountV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolRetentionCountV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolRetentionCountV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolRetentionCountV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolRetentionCountV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, Grade01FemaleAlaskanCount = F, Grade01FemaleAsianCount = F, Grade01FemaleBlackCount = F, Grade01FemaleHawaiianCount = F, Grade01FemaleHispanicCount = F, Grade01FemaleIDEACount = F, Grade01FemaleLEPCount = F, Grade01FemaleMultiRaceCount = F, Grade01FemaleSection504Count = F, Grade01FemaleWhiteCount = F, Grade01MaleAlaskanCount = F, Grade01MaleAsianCount = F, Grade01MaleBlackCount = F, Grade01MaleHawaiianCount = F, Grade01MaleHispanicCount = F, Grade01MaleIDEACount = F, Grade01MaleLEPCount = F, Grade01MaleMultiRaceCount = F, Grade01MaleSection504Count = F, Grade01MaleWhiteCount = F, Grade02FemaleAlaskanCount = F, Grade02FemaleAsianCount = F, Grade02FemaleBlackCount = F, Grade02FemaleHawaiianCount = F, Grade02FemaleHispanicCount = F, Grade02FemaleIDEACount = F, Grade02FemaleLEPCount = F, Grade02FemaleMultiRaceCount = F, Grade02FemaleSection504Count = F, Grade02FemaleWhiteCount = F, Grade02MaleAlaskanCount = F, Grade02MaleAsianCount = F, Grade02MaleBlackCount = F, Grade02MaleHawaiianCount = F, Grade02MaleHispanicCount = F, Grade02MaleIDEACount = F, Grade02MaleLEPCount = F, Grade02MaleMultiRaceCount = F, Grade02MaleSection504Count = F, Grade02MaleWhiteCount = F, Grade03FemaleAlaskanCount = F, Grade03FemaleAsianCount = F, Grade03FemaleBlackCount = F, Grade03FemaleHawaiianCount = F, Grade03FemaleHispanicCount = F, Grade03FemaleIDEACount = F, Grade03FemaleLEPCount = F, Grade03FemaleMultiRaceCount = F, Grade03FemaleSection504Count = F, Grade03FemaleWhiteCount = F, Grade03MaleAlaskanCount = F, Grade03MaleAsianCount = F, Grade03MaleBlackCount = F, Grade03MaleHawaiianCount = F, Grade03MaleHispanicCount = F, Grade03MaleIDEACount = F, Grade03MaleLEPCount = F, Grade03MaleMultiRaceCount = F, Grade03MaleSection504Count = F, Grade03MaleWhiteCount = F, Grade04FemaleAlaskanCount = F, Grade04FemaleAsianCount = F, Grade04FemaleBlackCount = F, Grade04FemaleHawaiianCount = F, Grade04FemaleHispanicCount = F, Grade04FemaleIDEACount = F, Grade04FemaleLEPCount = F, Grade04FemaleMultiRaceCount = F, Grade04FemaleSection504Count = F, Grade04FemaleWhiteCount = F, Grade04MaleAlaskanCount = F, Grade04MaleAsianCount = F, Grade04MaleBlackCount = F, Grade04MaleHawaiianCount = F, Grade04MaleHispanicCount = F, Grade04MaleIDEACount = F, Grade04MaleLEPCount = F, Grade04MaleMultiRaceCount = F, Grade04MaleSection504Count = F, Grade04MaleWhiteCount = F, Grade05FemaleAlaskanCount = F, Grade05FemaleAsianCount = F, Grade05FemaleBlackCount = F, Grade05FemaleHawaiianCount = F, Grade05FemaleHispanicCount = F, Grade05FemaleIDEACount = F, Grade05FemaleLEPCount = F, Grade05FemaleMultiRaceCount = F, Grade05FemaleSection504Count = F, Grade05FemaleWhiteCount = F, Grade05MaleAlaskanCount = F, Grade05MaleAsianCount = F, Grade05MaleBlackCount = F, Grade05MaleHawaiianCount = F, Grade05MaleHispanicCount = F, Grade05MaleIDEACount = F, Grade05MaleLEPCount = F, Grade05MaleMultiRaceCount = F, Grade05MaleSection504Count = F, Grade05MaleWhiteCount = F, Grade06FemaleAlaskanCount = F, Grade06FemaleAsianCount = F, Grade06FemaleBlackCount = F, Grade06FemaleHawaiianCount = F, Grade06FemaleHispanicCount = F, Grade06FemaleIDEACount = F, Grade06FemaleLEPCount = F, Grade06FemaleMultiRaceCount = F, Grade06FemaleSection504Count = F, Grade06FemaleWhiteCount = F, Grade06MaleAlaskanCount = F, Grade06MaleAsianCount = F, Grade06MaleBlackCount = F, Grade06MaleHawaiianCount = F, Grade06MaleHispanicCount = F, Grade06MaleIDEACount = F, Grade06MaleLEPCount = F, Grade06MaleMultiRaceCount = F, Grade06MaleSection504Count = F, Grade06MaleWhiteCount = F, Grade07FemaleAlaskanCount = F, Grade07FemaleAsianCount = F, Grade07FemaleBlackCount = F, Grade07FemaleHawaiianCount = F, Grade07FemaleHispanicCount = F, Grade07FemaleIDEACount = F, Grade07FemaleLEPCount = F, Grade07FemaleMultiRaceCount = F, Grade07FemaleSection504Count = F, Grade07FemaleWhiteCount = F, Grade07MaleAlaskanCount = F, Grade07MaleAsianCount = F, Grade07MaleBlackCount = F, Grade07MaleHawaiianCount = F, Grade07MaleHispanicCount = F, Grade07MaleIDEACount = F, Grade07MaleLEPCount = F, Grade07MaleMultiRaceCount = F, Grade07MaleSection504Count = F, Grade07MaleWhiteCount = F, Grade08FemaleAlaskanCount = F, Grade08FemaleAsianCount = F, Grade08FemaleBlackCount = F, Grade08FemaleHawaiianCount = F, Grade08FemaleHispanicCount = F, Grade08FemaleIDEACount = F, Grade08FemaleLEPCount = F, Grade08FemaleMultiRaceCount = F, Grade08FemaleSection504Count = F, Grade08FemaleWhiteCount = F, Grade08MaleAlaskanCount = F, Grade08MaleAsianCount = F, Grade08MaleBlackCount = F, Grade08MaleHawaiianCount = F, Grade08MaleHispanicCount = F, Grade08MaleIDEACount = F, Grade08MaleLEPCount = F, Grade08MaleMultiRaceCount = F, Grade08MaleSection504Count = F, Grade08MaleWhiteCount = F, Grade09FemaleAlaskanCount = F, Grade09FemaleAsianCount = F, Grade09FemaleBlackCount = F, Grade09FemaleHawaiianCount = F, Grade09FemaleHispanicCount = F, Grade09FemaleIDEACount = F, Grade09FemaleLEPCount = F, Grade09FemaleMultiRaceCount = F, Grade09FemaleSection504Count = F, Grade09FemaleWhiteCount = F, Grade09MaleAlaskanCount = F, Grade09MaleAsianCount = F, Grade09MaleBlackCount = F, Grade09MaleHawaiianCount = F, Grade09MaleHispanicCount = F, Grade09MaleIDEACount = F, Grade09MaleLEPCount = F, Grade09MaleMultiRaceCount = F, Grade09MaleSection504Count = F, Grade09MaleWhiteCount = F, Grade10FemaleAlaskanCount = F, Grade10FemaleAsianCount = F, Grade10FemaleBlackCount = F, Grade10FemaleHawaiianCount = F, Grade10FemaleHispanicCount = F, Grade10FemaleIDEACount = F, Grade10FemaleLEPCount = F, Grade10FemaleMultiRaceCount = F, Grade10FemaleSection504Count = F, Grade10FemaleWhiteCount = F, Grade10MaleAlaskanCount = F, Grade10MaleAsianCount = F, Grade10MaleBlackCount = F, Grade10MaleHawaiianCount = F, Grade10MaleHispanicCount = F, Grade10MaleIDEACount = F, Grade10MaleLEPCount = F, Grade10MaleMultiRaceCount = F, Grade10MaleSection504Count = F, Grade10MaleWhiteCount = F, Grade11FemaleAlaskanCount = F, Grade11FemaleAsianCount = F, Grade11FemaleBlackCount = F, Grade11FemaleHawaiianCount = F, Grade11FemaleHispanicCount = F, Grade11FemaleIDEACount = F, Grade11FemaleLEPCount = F, Grade11FemaleMultiRaceCount = F, Grade11FemaleSection504Count = F, Grade11FemaleWhiteCount = F, Grade11MaleAlaskanCount = F, Grade11MaleAsianCount = F, Grade11MaleBlackCount = F, Grade11MaleHawaiianCount = F, Grade11MaleHispanicCount = F, Grade11MaleIDEACount = F, Grade11MaleLEPCount = F, Grade11MaleMultiRaceCount = F, Grade11MaleSection504Count = F, Grade11MaleWhiteCount = F, Grade12FemaleAlaskanCount = F, Grade12FemaleAsianCount = F, Grade12FemaleBlackCount = F, Grade12FemaleHawaiianCount = F, Grade12FemaleHispanicCount = F, Grade12FemaleIDEACount = F, Grade12FemaleLEPCount = F, Grade12FemaleMultiRaceCount = F, Grade12FemaleSection504Count = F, Grade12FemaleWhiteCount = F, Grade12MaleAlaskanCount = F, Grade12MaleAsianCount = F, Grade12MaleBlackCount = F, Grade12MaleHawaiianCount = F, Grade12MaleHispanicCount = F, Grade12MaleIDEACount = F, Grade12MaleLEPCount = F, Grade12MaleMultiRaceCount = F, Grade12MaleSection504Count = F, Grade12MaleWhiteCount = F, KindergartenFemaleAlaskanCount = F, KindergartenFemaleAsianCount = F, KindergartenFemaleBlackCount = F, KindergartenFemaleHawaiianCount = F, KindergartenFemaleHispanicCount = F, KindergartenFemaleIDEACount = F, KindergartenFemaleLEPCount = F, KindergartenFemaleMultiRaceCount = F, KindergartenFemaleSection504Count = F, KindergartenFemaleWhiteCount = F, KindergartenMaleAlaskanCount = F, KindergartenMaleAsianCount = F, KindergartenMaleBlackCount = F, KindergartenMaleHawaiianCount = F, KindergartenMaleHispanicCount = F, KindergartenMaleIDEACount = F, KindergartenMaleLEPCount = F, KindergartenMaleMultiRaceCount = F, KindergartenMaleSection504Count = F, KindergartenMaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionCountV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolRetentionCountV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolRetentionCountV1
	#' @param CRDCSchoolRetentionCountV1ID The ID of the CRDCSchoolRetentionCountV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolRetentionCountV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolRetentionCountV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolRetentionCountV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolRetentionCountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolRetentionCountV1 <- function(CRDCSchoolRetentionCountV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, Grade01FemaleAlaskanCount = F, Grade01FemaleAsianCount = F, Grade01FemaleBlackCount = F, Grade01FemaleHawaiianCount = F, Grade01FemaleHispanicCount = F, Grade01FemaleIDEACount = F, Grade01FemaleLEPCount = F, Grade01FemaleMultiRaceCount = F, Grade01FemaleSection504Count = F, Grade01FemaleWhiteCount = F, Grade01MaleAlaskanCount = F, Grade01MaleAsianCount = F, Grade01MaleBlackCount = F, Grade01MaleHawaiianCount = F, Grade01MaleHispanicCount = F, Grade01MaleIDEACount = F, Grade01MaleLEPCount = F, Grade01MaleMultiRaceCount = F, Grade01MaleSection504Count = F, Grade01MaleWhiteCount = F, Grade02FemaleAlaskanCount = F, Grade02FemaleAsianCount = F, Grade02FemaleBlackCount = F, Grade02FemaleHawaiianCount = F, Grade02FemaleHispanicCount = F, Grade02FemaleIDEACount = F, Grade02FemaleLEPCount = F, Grade02FemaleMultiRaceCount = F, Grade02FemaleSection504Count = F, Grade02FemaleWhiteCount = F, Grade02MaleAlaskanCount = F, Grade02MaleAsianCount = F, Grade02MaleBlackCount = F, Grade02MaleHawaiianCount = F, Grade02MaleHispanicCount = F, Grade02MaleIDEACount = F, Grade02MaleLEPCount = F, Grade02MaleMultiRaceCount = F, Grade02MaleSection504Count = F, Grade02MaleWhiteCount = F, Grade03FemaleAlaskanCount = F, Grade03FemaleAsianCount = F, Grade03FemaleBlackCount = F, Grade03FemaleHawaiianCount = F, Grade03FemaleHispanicCount = F, Grade03FemaleIDEACount = F, Grade03FemaleLEPCount = F, Grade03FemaleMultiRaceCount = F, Grade03FemaleSection504Count = F, Grade03FemaleWhiteCount = F, Grade03MaleAlaskanCount = F, Grade03MaleAsianCount = F, Grade03MaleBlackCount = F, Grade03MaleHawaiianCount = F, Grade03MaleHispanicCount = F, Grade03MaleIDEACount = F, Grade03MaleLEPCount = F, Grade03MaleMultiRaceCount = F, Grade03MaleSection504Count = F, Grade03MaleWhiteCount = F, Grade04FemaleAlaskanCount = F, Grade04FemaleAsianCount = F, Grade04FemaleBlackCount = F, Grade04FemaleHawaiianCount = F, Grade04FemaleHispanicCount = F, Grade04FemaleIDEACount = F, Grade04FemaleLEPCount = F, Grade04FemaleMultiRaceCount = F, Grade04FemaleSection504Count = F, Grade04FemaleWhiteCount = F, Grade04MaleAlaskanCount = F, Grade04MaleAsianCount = F, Grade04MaleBlackCount = F, Grade04MaleHawaiianCount = F, Grade04MaleHispanicCount = F, Grade04MaleIDEACount = F, Grade04MaleLEPCount = F, Grade04MaleMultiRaceCount = F, Grade04MaleSection504Count = F, Grade04MaleWhiteCount = F, Grade05FemaleAlaskanCount = F, Grade05FemaleAsianCount = F, Grade05FemaleBlackCount = F, Grade05FemaleHawaiianCount = F, Grade05FemaleHispanicCount = F, Grade05FemaleIDEACount = F, Grade05FemaleLEPCount = F, Grade05FemaleMultiRaceCount = F, Grade05FemaleSection504Count = F, Grade05FemaleWhiteCount = F, Grade05MaleAlaskanCount = F, Grade05MaleAsianCount = F, Grade05MaleBlackCount = F, Grade05MaleHawaiianCount = F, Grade05MaleHispanicCount = F, Grade05MaleIDEACount = F, Grade05MaleLEPCount = F, Grade05MaleMultiRaceCount = F, Grade05MaleSection504Count = F, Grade05MaleWhiteCount = F, Grade06FemaleAlaskanCount = F, Grade06FemaleAsianCount = F, Grade06FemaleBlackCount = F, Grade06FemaleHawaiianCount = F, Grade06FemaleHispanicCount = F, Grade06FemaleIDEACount = F, Grade06FemaleLEPCount = F, Grade06FemaleMultiRaceCount = F, Grade06FemaleSection504Count = F, Grade06FemaleWhiteCount = F, Grade06MaleAlaskanCount = F, Grade06MaleAsianCount = F, Grade06MaleBlackCount = F, Grade06MaleHawaiianCount = F, Grade06MaleHispanicCount = F, Grade06MaleIDEACount = F, Grade06MaleLEPCount = F, Grade06MaleMultiRaceCount = F, Grade06MaleSection504Count = F, Grade06MaleWhiteCount = F, Grade07FemaleAlaskanCount = F, Grade07FemaleAsianCount = F, Grade07FemaleBlackCount = F, Grade07FemaleHawaiianCount = F, Grade07FemaleHispanicCount = F, Grade07FemaleIDEACount = F, Grade07FemaleLEPCount = F, Grade07FemaleMultiRaceCount = F, Grade07FemaleSection504Count = F, Grade07FemaleWhiteCount = F, Grade07MaleAlaskanCount = F, Grade07MaleAsianCount = F, Grade07MaleBlackCount = F, Grade07MaleHawaiianCount = F, Grade07MaleHispanicCount = F, Grade07MaleIDEACount = F, Grade07MaleLEPCount = F, Grade07MaleMultiRaceCount = F, Grade07MaleSection504Count = F, Grade07MaleWhiteCount = F, Grade08FemaleAlaskanCount = F, Grade08FemaleAsianCount = F, Grade08FemaleBlackCount = F, Grade08FemaleHawaiianCount = F, Grade08FemaleHispanicCount = F, Grade08FemaleIDEACount = F, Grade08FemaleLEPCount = F, Grade08FemaleMultiRaceCount = F, Grade08FemaleSection504Count = F, Grade08FemaleWhiteCount = F, Grade08MaleAlaskanCount = F, Grade08MaleAsianCount = F, Grade08MaleBlackCount = F, Grade08MaleHawaiianCount = F, Grade08MaleHispanicCount = F, Grade08MaleIDEACount = F, Grade08MaleLEPCount = F, Grade08MaleMultiRaceCount = F, Grade08MaleSection504Count = F, Grade08MaleWhiteCount = F, Grade09FemaleAlaskanCount = F, Grade09FemaleAsianCount = F, Grade09FemaleBlackCount = F, Grade09FemaleHawaiianCount = F, Grade09FemaleHispanicCount = F, Grade09FemaleIDEACount = F, Grade09FemaleLEPCount = F, Grade09FemaleMultiRaceCount = F, Grade09FemaleSection504Count = F, Grade09FemaleWhiteCount = F, Grade09MaleAlaskanCount = F, Grade09MaleAsianCount = F, Grade09MaleBlackCount = F, Grade09MaleHawaiianCount = F, Grade09MaleHispanicCount = F, Grade09MaleIDEACount = F, Grade09MaleLEPCount = F, Grade09MaleMultiRaceCount = F, Grade09MaleSection504Count = F, Grade09MaleWhiteCount = F, Grade10FemaleAlaskanCount = F, Grade10FemaleAsianCount = F, Grade10FemaleBlackCount = F, Grade10FemaleHawaiianCount = F, Grade10FemaleHispanicCount = F, Grade10FemaleIDEACount = F, Grade10FemaleLEPCount = F, Grade10FemaleMultiRaceCount = F, Grade10FemaleSection504Count = F, Grade10FemaleWhiteCount = F, Grade10MaleAlaskanCount = F, Grade10MaleAsianCount = F, Grade10MaleBlackCount = F, Grade10MaleHawaiianCount = F, Grade10MaleHispanicCount = F, Grade10MaleIDEACount = F, Grade10MaleLEPCount = F, Grade10MaleMultiRaceCount = F, Grade10MaleSection504Count = F, Grade10MaleWhiteCount = F, Grade11FemaleAlaskanCount = F, Grade11FemaleAsianCount = F, Grade11FemaleBlackCount = F, Grade11FemaleHawaiianCount = F, Grade11FemaleHispanicCount = F, Grade11FemaleIDEACount = F, Grade11FemaleLEPCount = F, Grade11FemaleMultiRaceCount = F, Grade11FemaleSection504Count = F, Grade11FemaleWhiteCount = F, Grade11MaleAlaskanCount = F, Grade11MaleAsianCount = F, Grade11MaleBlackCount = F, Grade11MaleHawaiianCount = F, Grade11MaleHispanicCount = F, Grade11MaleIDEACount = F, Grade11MaleLEPCount = F, Grade11MaleMultiRaceCount = F, Grade11MaleSection504Count = F, Grade11MaleWhiteCount = F, Grade12FemaleAlaskanCount = F, Grade12FemaleAsianCount = F, Grade12FemaleBlackCount = F, Grade12FemaleHawaiianCount = F, Grade12FemaleHispanicCount = F, Grade12FemaleIDEACount = F, Grade12FemaleLEPCount = F, Grade12FemaleMultiRaceCount = F, Grade12FemaleSection504Count = F, Grade12FemaleWhiteCount = F, Grade12MaleAlaskanCount = F, Grade12MaleAsianCount = F, Grade12MaleBlackCount = F, Grade12MaleHawaiianCount = F, Grade12MaleHispanicCount = F, Grade12MaleIDEACount = F, Grade12MaleLEPCount = F, Grade12MaleMultiRaceCount = F, Grade12MaleSection504Count = F, Grade12MaleWhiteCount = F, KindergartenFemaleAlaskanCount = F, KindergartenFemaleAsianCount = F, KindergartenFemaleBlackCount = F, KindergartenFemaleHawaiianCount = F, KindergartenFemaleHispanicCount = F, KindergartenFemaleIDEACount = F, KindergartenFemaleLEPCount = F, KindergartenFemaleMultiRaceCount = F, KindergartenFemaleSection504Count = F, KindergartenFemaleWhiteCount = F, KindergartenMaleAlaskanCount = F, KindergartenMaleAsianCount = F, KindergartenMaleBlackCount = F, KindergartenMaleHawaiianCount = F, KindergartenMaleHispanicCount = F, KindergartenMaleIDEACount = F, KindergartenMaleLEPCount = F, KindergartenMaleMultiRaceCount = F, KindergartenMaleSection504Count = F, KindergartenMaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolRetentionCountV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionCountV1", objectId = CRDCSchoolRetentionCountV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolRetentionCountV1
	#'
	#' This function deletes a CRDCSchoolRetentionCountV1
	#' @param CRDCSchoolRetentionCountV1ID The ID of the CRDCSchoolRetentionCountV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolRetentionCountV1ID of the deleted CRDCSchoolRetentionCountV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolRetentionCountV1 <- function(CRDCSchoolRetentionCountV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionCountV1", objectId = CRDCSchoolRetentionCountV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolRetentionCountV1
	#'
	#' This function creates a CRDCSchoolRetentionCountV1
	#' @param fieldNames The field values to give the created CRDCSchoolRetentionCountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolRetentionCountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolRetentionCountV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, Grade01FemaleAlaskanCount = NULL, Grade01FemaleAsianCount = NULL, Grade01FemaleBlackCount = NULL, Grade01FemaleHawaiianCount = NULL, Grade01FemaleHispanicCount = NULL, Grade01FemaleIDEACount = NULL, Grade01FemaleLEPCount = NULL, Grade01FemaleMultiRaceCount = NULL, Grade01FemaleSection504Count = NULL, Grade01FemaleWhiteCount = NULL, Grade01MaleAlaskanCount = NULL, Grade01MaleAsianCount = NULL, Grade01MaleBlackCount = NULL, Grade01MaleHawaiianCount = NULL, Grade01MaleHispanicCount = NULL, Grade01MaleIDEACount = NULL, Grade01MaleLEPCount = NULL, Grade01MaleMultiRaceCount = NULL, Grade01MaleSection504Count = NULL, Grade01MaleWhiteCount = NULL, Grade02FemaleAlaskanCount = NULL, Grade02FemaleAsianCount = NULL, Grade02FemaleBlackCount = NULL, Grade02FemaleHawaiianCount = NULL, Grade02FemaleHispanicCount = NULL, Grade02FemaleIDEACount = NULL, Grade02FemaleLEPCount = NULL, Grade02FemaleMultiRaceCount = NULL, Grade02FemaleSection504Count = NULL, Grade02FemaleWhiteCount = NULL, Grade02MaleAlaskanCount = NULL, Grade02MaleAsianCount = NULL, Grade02MaleBlackCount = NULL, Grade02MaleHawaiianCount = NULL, Grade02MaleHispanicCount = NULL, Grade02MaleIDEACount = NULL, Grade02MaleLEPCount = NULL, Grade02MaleMultiRaceCount = NULL, Grade02MaleSection504Count = NULL, Grade02MaleWhiteCount = NULL, Grade03FemaleAlaskanCount = NULL, Grade03FemaleAsianCount = NULL, Grade03FemaleBlackCount = NULL, Grade03FemaleHawaiianCount = NULL, Grade03FemaleHispanicCount = NULL, Grade03FemaleIDEACount = NULL, Grade03FemaleLEPCount = NULL, Grade03FemaleMultiRaceCount = NULL, Grade03FemaleSection504Count = NULL, Grade03FemaleWhiteCount = NULL, Grade03MaleAlaskanCount = NULL, Grade03MaleAsianCount = NULL, Grade03MaleBlackCount = NULL, Grade03MaleHawaiianCount = NULL, Grade03MaleHispanicCount = NULL, Grade03MaleIDEACount = NULL, Grade03MaleLEPCount = NULL, Grade03MaleMultiRaceCount = NULL, Grade03MaleSection504Count = NULL, Grade03MaleWhiteCount = NULL, Grade04FemaleAlaskanCount = NULL, Grade04FemaleAsianCount = NULL, Grade04FemaleBlackCount = NULL, Grade04FemaleHawaiianCount = NULL, Grade04FemaleHispanicCount = NULL, Grade04FemaleIDEACount = NULL, Grade04FemaleLEPCount = NULL, Grade04FemaleMultiRaceCount = NULL, Grade04FemaleSection504Count = NULL, Grade04FemaleWhiteCount = NULL, Grade04MaleAlaskanCount = NULL, Grade04MaleAsianCount = NULL, Grade04MaleBlackCount = NULL, Grade04MaleHawaiianCount = NULL, Grade04MaleHispanicCount = NULL, Grade04MaleIDEACount = NULL, Grade04MaleLEPCount = NULL, Grade04MaleMultiRaceCount = NULL, Grade04MaleSection504Count = NULL, Grade04MaleWhiteCount = NULL, Grade05FemaleAlaskanCount = NULL, Grade05FemaleAsianCount = NULL, Grade05FemaleBlackCount = NULL, Grade05FemaleHawaiianCount = NULL, Grade05FemaleHispanicCount = NULL, Grade05FemaleIDEACount = NULL, Grade05FemaleLEPCount = NULL, Grade05FemaleMultiRaceCount = NULL, Grade05FemaleSection504Count = NULL, Grade05FemaleWhiteCount = NULL, Grade05MaleAlaskanCount = NULL, Grade05MaleAsianCount = NULL, Grade05MaleBlackCount = NULL, Grade05MaleHawaiianCount = NULL, Grade05MaleHispanicCount = NULL, Grade05MaleIDEACount = NULL, Grade05MaleLEPCount = NULL, Grade05MaleMultiRaceCount = NULL, Grade05MaleSection504Count = NULL, Grade05MaleWhiteCount = NULL, Grade06FemaleAlaskanCount = NULL, Grade06FemaleAsianCount = NULL, Grade06FemaleBlackCount = NULL, Grade06FemaleHawaiianCount = NULL, Grade06FemaleHispanicCount = NULL, Grade06FemaleIDEACount = NULL, Grade06FemaleLEPCount = NULL, Grade06FemaleMultiRaceCount = NULL, Grade06FemaleSection504Count = NULL, Grade06FemaleWhiteCount = NULL, Grade06MaleAlaskanCount = NULL, Grade06MaleAsianCount = NULL, Grade06MaleBlackCount = NULL, Grade06MaleHawaiianCount = NULL, Grade06MaleHispanicCount = NULL, Grade06MaleIDEACount = NULL, Grade06MaleLEPCount = NULL, Grade06MaleMultiRaceCount = NULL, Grade06MaleSection504Count = NULL, Grade06MaleWhiteCount = NULL, Grade07FemaleAlaskanCount = NULL, Grade07FemaleAsianCount = NULL, Grade07FemaleBlackCount = NULL, Grade07FemaleHawaiianCount = NULL, Grade07FemaleHispanicCount = NULL, Grade07FemaleIDEACount = NULL, Grade07FemaleLEPCount = NULL, Grade07FemaleMultiRaceCount = NULL, Grade07FemaleSection504Count = NULL, Grade07FemaleWhiteCount = NULL, Grade07MaleAlaskanCount = NULL, Grade07MaleAsianCount = NULL, Grade07MaleBlackCount = NULL, Grade07MaleHawaiianCount = NULL, Grade07MaleHispanicCount = NULL, Grade07MaleIDEACount = NULL, Grade07MaleLEPCount = NULL, Grade07MaleMultiRaceCount = NULL, Grade07MaleSection504Count = NULL, Grade07MaleWhiteCount = NULL, Grade08FemaleAlaskanCount = NULL, Grade08FemaleAsianCount = NULL, Grade08FemaleBlackCount = NULL, Grade08FemaleHawaiianCount = NULL, Grade08FemaleHispanicCount = NULL, Grade08FemaleIDEACount = NULL, Grade08FemaleLEPCount = NULL, Grade08FemaleMultiRaceCount = NULL, Grade08FemaleSection504Count = NULL, Grade08FemaleWhiteCount = NULL, Grade08MaleAlaskanCount = NULL, Grade08MaleAsianCount = NULL, Grade08MaleBlackCount = NULL, Grade08MaleHawaiianCount = NULL, Grade08MaleHispanicCount = NULL, Grade08MaleIDEACount = NULL, Grade08MaleLEPCount = NULL, Grade08MaleMultiRaceCount = NULL, Grade08MaleSection504Count = NULL, Grade08MaleWhiteCount = NULL, Grade09FemaleAlaskanCount = NULL, Grade09FemaleAsianCount = NULL, Grade09FemaleBlackCount = NULL, Grade09FemaleHawaiianCount = NULL, Grade09FemaleHispanicCount = NULL, Grade09FemaleIDEACount = NULL, Grade09FemaleLEPCount = NULL, Grade09FemaleMultiRaceCount = NULL, Grade09FemaleSection504Count = NULL, Grade09FemaleWhiteCount = NULL, Grade09MaleAlaskanCount = NULL, Grade09MaleAsianCount = NULL, Grade09MaleBlackCount = NULL, Grade09MaleHawaiianCount = NULL, Grade09MaleHispanicCount = NULL, Grade09MaleIDEACount = NULL, Grade09MaleLEPCount = NULL, Grade09MaleMultiRaceCount = NULL, Grade09MaleSection504Count = NULL, Grade09MaleWhiteCount = NULL, Grade10FemaleAlaskanCount = NULL, Grade10FemaleAsianCount = NULL, Grade10FemaleBlackCount = NULL, Grade10FemaleHawaiianCount = NULL, Grade10FemaleHispanicCount = NULL, Grade10FemaleIDEACount = NULL, Grade10FemaleLEPCount = NULL, Grade10FemaleMultiRaceCount = NULL, Grade10FemaleSection504Count = NULL, Grade10FemaleWhiteCount = NULL, Grade10MaleAlaskanCount = NULL, Grade10MaleAsianCount = NULL, Grade10MaleBlackCount = NULL, Grade10MaleHawaiianCount = NULL, Grade10MaleHispanicCount = NULL, Grade10MaleIDEACount = NULL, Grade10MaleLEPCount = NULL, Grade10MaleMultiRaceCount = NULL, Grade10MaleSection504Count = NULL, Grade10MaleWhiteCount = NULL, Grade11FemaleAlaskanCount = NULL, Grade11FemaleAsianCount = NULL, Grade11FemaleBlackCount = NULL, Grade11FemaleHawaiianCount = NULL, Grade11FemaleHispanicCount = NULL, Grade11FemaleIDEACount = NULL, Grade11FemaleLEPCount = NULL, Grade11FemaleMultiRaceCount = NULL, Grade11FemaleSection504Count = NULL, Grade11FemaleWhiteCount = NULL, Grade11MaleAlaskanCount = NULL, Grade11MaleAsianCount = NULL, Grade11MaleBlackCount = NULL, Grade11MaleHawaiianCount = NULL, Grade11MaleHispanicCount = NULL, Grade11MaleIDEACount = NULL, Grade11MaleLEPCount = NULL, Grade11MaleMultiRaceCount = NULL, Grade11MaleSection504Count = NULL, Grade11MaleWhiteCount = NULL, Grade12FemaleAlaskanCount = NULL, Grade12FemaleAsianCount = NULL, Grade12FemaleBlackCount = NULL, Grade12FemaleHawaiianCount = NULL, Grade12FemaleHispanicCount = NULL, Grade12FemaleIDEACount = NULL, Grade12FemaleLEPCount = NULL, Grade12FemaleMultiRaceCount = NULL, Grade12FemaleSection504Count = NULL, Grade12FemaleWhiteCount = NULL, Grade12MaleAlaskanCount = NULL, Grade12MaleAsianCount = NULL, Grade12MaleBlackCount = NULL, Grade12MaleHawaiianCount = NULL, Grade12MaleHispanicCount = NULL, Grade12MaleIDEACount = NULL, Grade12MaleLEPCount = NULL, Grade12MaleMultiRaceCount = NULL, Grade12MaleSection504Count = NULL, Grade12MaleWhiteCount = NULL, KindergartenFemaleAlaskanCount = NULL, KindergartenFemaleAsianCount = NULL, KindergartenFemaleBlackCount = NULL, KindergartenFemaleHawaiianCount = NULL, KindergartenFemaleHispanicCount = NULL, KindergartenFemaleIDEACount = NULL, KindergartenFemaleLEPCount = NULL, KindergartenFemaleMultiRaceCount = NULL, KindergartenFemaleSection504Count = NULL, KindergartenFemaleWhiteCount = NULL, KindergartenMaleAlaskanCount = NULL, KindergartenMaleAsianCount = NULL, KindergartenMaleBlackCount = NULL, KindergartenMaleHawaiianCount = NULL, KindergartenMaleHispanicCount = NULL, KindergartenMaleIDEACount = NULL, KindergartenMaleLEPCount = NULL, KindergartenMaleMultiRaceCount = NULL, KindergartenMaleSection504Count = NULL, KindergartenMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionCountV1", body = list(DataObject = body), searchFields = append("CRDCSchoolRetentionCountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolRetentionCountV1
	#'
	#' This function modifies a CRDCSchoolRetentionCountV1
	#' @param fieldNames The field values to give the modified CRDCSchoolRetentionCountV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolRetentionCountV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolRetentionCountV1 <- function(CRDCSchoolRetentionCountV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, Grade01FemaleAlaskanCount = NULL, Grade01FemaleAsianCount = NULL, Grade01FemaleBlackCount = NULL, Grade01FemaleHawaiianCount = NULL, Grade01FemaleHispanicCount = NULL, Grade01FemaleIDEACount = NULL, Grade01FemaleLEPCount = NULL, Grade01FemaleMultiRaceCount = NULL, Grade01FemaleSection504Count = NULL, Grade01FemaleWhiteCount = NULL, Grade01MaleAlaskanCount = NULL, Grade01MaleAsianCount = NULL, Grade01MaleBlackCount = NULL, Grade01MaleHawaiianCount = NULL, Grade01MaleHispanicCount = NULL, Grade01MaleIDEACount = NULL, Grade01MaleLEPCount = NULL, Grade01MaleMultiRaceCount = NULL, Grade01MaleSection504Count = NULL, Grade01MaleWhiteCount = NULL, Grade02FemaleAlaskanCount = NULL, Grade02FemaleAsianCount = NULL, Grade02FemaleBlackCount = NULL, Grade02FemaleHawaiianCount = NULL, Grade02FemaleHispanicCount = NULL, Grade02FemaleIDEACount = NULL, Grade02FemaleLEPCount = NULL, Grade02FemaleMultiRaceCount = NULL, Grade02FemaleSection504Count = NULL, Grade02FemaleWhiteCount = NULL, Grade02MaleAlaskanCount = NULL, Grade02MaleAsianCount = NULL, Grade02MaleBlackCount = NULL, Grade02MaleHawaiianCount = NULL, Grade02MaleHispanicCount = NULL, Grade02MaleIDEACount = NULL, Grade02MaleLEPCount = NULL, Grade02MaleMultiRaceCount = NULL, Grade02MaleSection504Count = NULL, Grade02MaleWhiteCount = NULL, Grade03FemaleAlaskanCount = NULL, Grade03FemaleAsianCount = NULL, Grade03FemaleBlackCount = NULL, Grade03FemaleHawaiianCount = NULL, Grade03FemaleHispanicCount = NULL, Grade03FemaleIDEACount = NULL, Grade03FemaleLEPCount = NULL, Grade03FemaleMultiRaceCount = NULL, Grade03FemaleSection504Count = NULL, Grade03FemaleWhiteCount = NULL, Grade03MaleAlaskanCount = NULL, Grade03MaleAsianCount = NULL, Grade03MaleBlackCount = NULL, Grade03MaleHawaiianCount = NULL, Grade03MaleHispanicCount = NULL, Grade03MaleIDEACount = NULL, Grade03MaleLEPCount = NULL, Grade03MaleMultiRaceCount = NULL, Grade03MaleSection504Count = NULL, Grade03MaleWhiteCount = NULL, Grade04FemaleAlaskanCount = NULL, Grade04FemaleAsianCount = NULL, Grade04FemaleBlackCount = NULL, Grade04FemaleHawaiianCount = NULL, Grade04FemaleHispanicCount = NULL, Grade04FemaleIDEACount = NULL, Grade04FemaleLEPCount = NULL, Grade04FemaleMultiRaceCount = NULL, Grade04FemaleSection504Count = NULL, Grade04FemaleWhiteCount = NULL, Grade04MaleAlaskanCount = NULL, Grade04MaleAsianCount = NULL, Grade04MaleBlackCount = NULL, Grade04MaleHawaiianCount = NULL, Grade04MaleHispanicCount = NULL, Grade04MaleIDEACount = NULL, Grade04MaleLEPCount = NULL, Grade04MaleMultiRaceCount = NULL, Grade04MaleSection504Count = NULL, Grade04MaleWhiteCount = NULL, Grade05FemaleAlaskanCount = NULL, Grade05FemaleAsianCount = NULL, Grade05FemaleBlackCount = NULL, Grade05FemaleHawaiianCount = NULL, Grade05FemaleHispanicCount = NULL, Grade05FemaleIDEACount = NULL, Grade05FemaleLEPCount = NULL, Grade05FemaleMultiRaceCount = NULL, Grade05FemaleSection504Count = NULL, Grade05FemaleWhiteCount = NULL, Grade05MaleAlaskanCount = NULL, Grade05MaleAsianCount = NULL, Grade05MaleBlackCount = NULL, Grade05MaleHawaiianCount = NULL, Grade05MaleHispanicCount = NULL, Grade05MaleIDEACount = NULL, Grade05MaleLEPCount = NULL, Grade05MaleMultiRaceCount = NULL, Grade05MaleSection504Count = NULL, Grade05MaleWhiteCount = NULL, Grade06FemaleAlaskanCount = NULL, Grade06FemaleAsianCount = NULL, Grade06FemaleBlackCount = NULL, Grade06FemaleHawaiianCount = NULL, Grade06FemaleHispanicCount = NULL, Grade06FemaleIDEACount = NULL, Grade06FemaleLEPCount = NULL, Grade06FemaleMultiRaceCount = NULL, Grade06FemaleSection504Count = NULL, Grade06FemaleWhiteCount = NULL, Grade06MaleAlaskanCount = NULL, Grade06MaleAsianCount = NULL, Grade06MaleBlackCount = NULL, Grade06MaleHawaiianCount = NULL, Grade06MaleHispanicCount = NULL, Grade06MaleIDEACount = NULL, Grade06MaleLEPCount = NULL, Grade06MaleMultiRaceCount = NULL, Grade06MaleSection504Count = NULL, Grade06MaleWhiteCount = NULL, Grade07FemaleAlaskanCount = NULL, Grade07FemaleAsianCount = NULL, Grade07FemaleBlackCount = NULL, Grade07FemaleHawaiianCount = NULL, Grade07FemaleHispanicCount = NULL, Grade07FemaleIDEACount = NULL, Grade07FemaleLEPCount = NULL, Grade07FemaleMultiRaceCount = NULL, Grade07FemaleSection504Count = NULL, Grade07FemaleWhiteCount = NULL, Grade07MaleAlaskanCount = NULL, Grade07MaleAsianCount = NULL, Grade07MaleBlackCount = NULL, Grade07MaleHawaiianCount = NULL, Grade07MaleHispanicCount = NULL, Grade07MaleIDEACount = NULL, Grade07MaleLEPCount = NULL, Grade07MaleMultiRaceCount = NULL, Grade07MaleSection504Count = NULL, Grade07MaleWhiteCount = NULL, Grade08FemaleAlaskanCount = NULL, Grade08FemaleAsianCount = NULL, Grade08FemaleBlackCount = NULL, Grade08FemaleHawaiianCount = NULL, Grade08FemaleHispanicCount = NULL, Grade08FemaleIDEACount = NULL, Grade08FemaleLEPCount = NULL, Grade08FemaleMultiRaceCount = NULL, Grade08FemaleSection504Count = NULL, Grade08FemaleWhiteCount = NULL, Grade08MaleAlaskanCount = NULL, Grade08MaleAsianCount = NULL, Grade08MaleBlackCount = NULL, Grade08MaleHawaiianCount = NULL, Grade08MaleHispanicCount = NULL, Grade08MaleIDEACount = NULL, Grade08MaleLEPCount = NULL, Grade08MaleMultiRaceCount = NULL, Grade08MaleSection504Count = NULL, Grade08MaleWhiteCount = NULL, Grade09FemaleAlaskanCount = NULL, Grade09FemaleAsianCount = NULL, Grade09FemaleBlackCount = NULL, Grade09FemaleHawaiianCount = NULL, Grade09FemaleHispanicCount = NULL, Grade09FemaleIDEACount = NULL, Grade09FemaleLEPCount = NULL, Grade09FemaleMultiRaceCount = NULL, Grade09FemaleSection504Count = NULL, Grade09FemaleWhiteCount = NULL, Grade09MaleAlaskanCount = NULL, Grade09MaleAsianCount = NULL, Grade09MaleBlackCount = NULL, Grade09MaleHawaiianCount = NULL, Grade09MaleHispanicCount = NULL, Grade09MaleIDEACount = NULL, Grade09MaleLEPCount = NULL, Grade09MaleMultiRaceCount = NULL, Grade09MaleSection504Count = NULL, Grade09MaleWhiteCount = NULL, Grade10FemaleAlaskanCount = NULL, Grade10FemaleAsianCount = NULL, Grade10FemaleBlackCount = NULL, Grade10FemaleHawaiianCount = NULL, Grade10FemaleHispanicCount = NULL, Grade10FemaleIDEACount = NULL, Grade10FemaleLEPCount = NULL, Grade10FemaleMultiRaceCount = NULL, Grade10FemaleSection504Count = NULL, Grade10FemaleWhiteCount = NULL, Grade10MaleAlaskanCount = NULL, Grade10MaleAsianCount = NULL, Grade10MaleBlackCount = NULL, Grade10MaleHawaiianCount = NULL, Grade10MaleHispanicCount = NULL, Grade10MaleIDEACount = NULL, Grade10MaleLEPCount = NULL, Grade10MaleMultiRaceCount = NULL, Grade10MaleSection504Count = NULL, Grade10MaleWhiteCount = NULL, Grade11FemaleAlaskanCount = NULL, Grade11FemaleAsianCount = NULL, Grade11FemaleBlackCount = NULL, Grade11FemaleHawaiianCount = NULL, Grade11FemaleHispanicCount = NULL, Grade11FemaleIDEACount = NULL, Grade11FemaleLEPCount = NULL, Grade11FemaleMultiRaceCount = NULL, Grade11FemaleSection504Count = NULL, Grade11FemaleWhiteCount = NULL, Grade11MaleAlaskanCount = NULL, Grade11MaleAsianCount = NULL, Grade11MaleBlackCount = NULL, Grade11MaleHawaiianCount = NULL, Grade11MaleHispanicCount = NULL, Grade11MaleIDEACount = NULL, Grade11MaleLEPCount = NULL, Grade11MaleMultiRaceCount = NULL, Grade11MaleSection504Count = NULL, Grade11MaleWhiteCount = NULL, Grade12FemaleAlaskanCount = NULL, Grade12FemaleAsianCount = NULL, Grade12FemaleBlackCount = NULL, Grade12FemaleHawaiianCount = NULL, Grade12FemaleHispanicCount = NULL, Grade12FemaleIDEACount = NULL, Grade12FemaleLEPCount = NULL, Grade12FemaleMultiRaceCount = NULL, Grade12FemaleSection504Count = NULL, Grade12FemaleWhiteCount = NULL, Grade12MaleAlaskanCount = NULL, Grade12MaleAsianCount = NULL, Grade12MaleBlackCount = NULL, Grade12MaleHawaiianCount = NULL, Grade12MaleHispanicCount = NULL, Grade12MaleIDEACount = NULL, Grade12MaleLEPCount = NULL, Grade12MaleMultiRaceCount = NULL, Grade12MaleSection504Count = NULL, Grade12MaleWhiteCount = NULL, KindergartenFemaleAlaskanCount = NULL, KindergartenFemaleAsianCount = NULL, KindergartenFemaleBlackCount = NULL, KindergartenFemaleHawaiianCount = NULL, KindergartenFemaleHispanicCount = NULL, KindergartenFemaleIDEACount = NULL, KindergartenFemaleLEPCount = NULL, KindergartenFemaleMultiRaceCount = NULL, KindergartenFemaleSection504Count = NULL, KindergartenFemaleWhiteCount = NULL, KindergartenMaleAlaskanCount = NULL, KindergartenMaleAsianCount = NULL, KindergartenMaleBlackCount = NULL, KindergartenMaleHawaiianCount = NULL, KindergartenMaleHispanicCount = NULL, KindergartenMaleIDEACount = NULL, KindergartenMaleLEPCount = NULL, KindergartenMaleMultiRaceCount = NULL, KindergartenMaleSection504Count = NULL, KindergartenMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionCountV1", objectId = CRDCSchoolRetentionCountV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolRetentionCountV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolRetentionIndicatorV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolRetentionIndicatorV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolRetentionIndicatorV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolRetentionIndicatorV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolRetentionIndicatorV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolRetentionIndicatorV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolRetentionIndicatorV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolRetentionIndicatorV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, HasGrade01 = F, HasGrade02 = F, HasGrade03 = F, HasGrade04 = F, HasGrade05 = F, HasGrade06 = F, HasGrade07 = F, HasGrade08 = F, HasGrade09 = F, HasGrade10 = F, HasGrade11 = F, HasGrade12 = F, HasKindergarten = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionIndicatorV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolRetentionIndicatorV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolRetentionIndicatorV1
	#' @param CRDCSchoolRetentionIndicatorV1ID The ID of the CRDCSchoolRetentionIndicatorV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolRetentionIndicatorV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolRetentionIndicatorV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolRetentionIndicatorV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolRetentionIndicatorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolRetentionIndicatorV1 <- function(CRDCSchoolRetentionIndicatorV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, HasGrade01 = F, HasGrade02 = F, HasGrade03 = F, HasGrade04 = F, HasGrade05 = F, HasGrade06 = F, HasGrade07 = F, HasGrade08 = F, HasGrade09 = F, HasGrade10 = F, HasGrade11 = F, HasGrade12 = F, HasKindergarten = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolRetentionIndicatorV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionIndicatorV1", objectId = CRDCSchoolRetentionIndicatorV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolRetentionIndicatorV1
	#'
	#' This function deletes a CRDCSchoolRetentionIndicatorV1
	#' @param CRDCSchoolRetentionIndicatorV1ID The ID of the CRDCSchoolRetentionIndicatorV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolRetentionIndicatorV1ID of the deleted CRDCSchoolRetentionIndicatorV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolRetentionIndicatorV1 <- function(CRDCSchoolRetentionIndicatorV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionIndicatorV1", objectId = CRDCSchoolRetentionIndicatorV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolRetentionIndicatorV1
	#'
	#' This function creates a CRDCSchoolRetentionIndicatorV1
	#' @param fieldNames The field values to give the created CRDCSchoolRetentionIndicatorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolRetentionIndicatorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolRetentionIndicatorV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasGrade01 = NULL, HasGrade02 = NULL, HasGrade03 = NULL, HasGrade04 = NULL, HasGrade05 = NULL, HasGrade06 = NULL, HasGrade07 = NULL, HasGrade08 = NULL, HasGrade09 = NULL, HasGrade10 = NULL, HasGrade11 = NULL, HasGrade12 = NULL, HasKindergarten = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionIndicatorV1", body = list(DataObject = body), searchFields = append("CRDCSchoolRetentionIndicatorV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolRetentionIndicatorV1
	#'
	#' This function modifies a CRDCSchoolRetentionIndicatorV1
	#' @param fieldNames The field values to give the modified CRDCSchoolRetentionIndicatorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolRetentionIndicatorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolRetentionIndicatorV1 <- function(CRDCSchoolRetentionIndicatorV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasGrade01 = NULL, HasGrade02 = NULL, HasGrade03 = NULL, HasGrade04 = NULL, HasGrade05 = NULL, HasGrade06 = NULL, HasGrade07 = NULL, HasGrade08 = NULL, HasGrade09 = NULL, HasGrade10 = NULL, HasGrade11 = NULL, HasGrade12 = NULL, HasKindergarten = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolRetentionIndicatorV1", objectId = CRDCSchoolRetentionIndicatorV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolRetentionIndicatorV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolHSScienceV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolHSScienceV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHSScienceV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHSScienceV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHSScienceV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolHSScienceV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolHSScienceV1s <- function(searchConditionsList = NULL, BiologyCertifiedClassCount = F, BiologyClassCount = F, BiologyFemaleAlaskanCount = F, BiologyFemaleAsianCount = F, BiologyFemaleBlackCount = F, BiologyFemaleHawaiianCount = F, BiologyFemaleHispanicCount = F, BiologyFemaleIDEACount = F, BiologyFemaleLEPCount = F, BiologyFemaleMultiRaceCount = F, BiologyFemaleWhiteCount = F, BiologyMaleAlaskanCount = F, BiologyMaleAsianCount = F, BiologyMaleBlackCount = F, BiologyMaleHawaiianCount = F, BiologyMaleHispanicCount = F, BiologyMaleIDEACount = F, BiologyMaleLEPCount = F, BiologyMaleMultiRaceCount = F, BiologyMaleWhiteCount = F, ChemistryCertifiedClassCount = F, ChemistryClassCount = F, ChemistryFemaleAlaskanCount = F, ChemistryFemaleAsianCount = F, ChemistryFemaleBlackCount = F, ChemistryFemaleHawaiianCount = F, ChemistryFemaleHispanicCount = F, ChemistryFemaleIDEACount = F, ChemistryFemaleLEPCount = F, ChemistryFemaleMultiRaceCount = F, ChemistryFemaleWhiteCount = F, ChemistryMaleAlaskanCount = F, ChemistryMaleAsianCount = F, ChemistryMaleBlackCount = F, ChemistryMaleHawaiianCount = F, ChemistryMaleHispanicCount = F, ChemistryMaleIDEACount = F, ChemistryMaleLEPCount = F, ChemistryMaleMultiRaceCount = F, ChemistryMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolHSScienceV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, ModifiedTime = F, PhysicsCertifiedClassCount = F, PhysicsClassCount = F, PhysicsFemaleAlaskanCount = F, PhysicsFemaleAsianCount = F, PhysicsFemaleBlackCount = F, PhysicsFemaleHawaiianCount = F, PhysicsFemaleHispanicCount = F, PhysicsFemaleIDEACount = F, PhysicsFemaleLEPCount = F, PhysicsFemaleMultiRaceCount = F, PhysicsFemaleWhiteCount = F, PhysicsMaleAlaskanCount = F, PhysicsMaleAsianCount = F, PhysicsMaleBlackCount = F, PhysicsMaleHawaiianCount = F, PhysicsMaleHispanicCount = F, PhysicsMaleIDEACount = F, PhysicsMaleLEPCount = F, PhysicsMaleMultiRaceCount = F, PhysicsMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolHSScienceV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolHSScienceV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolHSScienceV1
	#' @param CRDCSchoolHSScienceV1ID The ID of the CRDCSchoolHSScienceV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHSScienceV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHSScienceV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHSScienceV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolHSScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolHSScienceV1 <- function(CRDCSchoolHSScienceV1ID, BiologyCertifiedClassCount = F, BiologyClassCount = F, BiologyFemaleAlaskanCount = F, BiologyFemaleAsianCount = F, BiologyFemaleBlackCount = F, BiologyFemaleHawaiianCount = F, BiologyFemaleHispanicCount = F, BiologyFemaleIDEACount = F, BiologyFemaleLEPCount = F, BiologyFemaleMultiRaceCount = F, BiologyFemaleWhiteCount = F, BiologyMaleAlaskanCount = F, BiologyMaleAsianCount = F, BiologyMaleBlackCount = F, BiologyMaleHawaiianCount = F, BiologyMaleHispanicCount = F, BiologyMaleIDEACount = F, BiologyMaleLEPCount = F, BiologyMaleMultiRaceCount = F, BiologyMaleWhiteCount = F, ChemistryCertifiedClassCount = F, ChemistryClassCount = F, ChemistryFemaleAlaskanCount = F, ChemistryFemaleAsianCount = F, ChemistryFemaleBlackCount = F, ChemistryFemaleHawaiianCount = F, ChemistryFemaleHispanicCount = F, ChemistryFemaleIDEACount = F, ChemistryFemaleLEPCount = F, ChemistryFemaleMultiRaceCount = F, ChemistryFemaleWhiteCount = F, ChemistryMaleAlaskanCount = F, ChemistryMaleAsianCount = F, ChemistryMaleBlackCount = F, ChemistryMaleHawaiianCount = F, ChemistryMaleHispanicCount = F, ChemistryMaleIDEACount = F, ChemistryMaleLEPCount = F, ChemistryMaleMultiRaceCount = F, ChemistryMaleWhiteCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, ModifiedTime = F, PhysicsCertifiedClassCount = F, PhysicsClassCount = F, PhysicsFemaleAlaskanCount = F, PhysicsFemaleAsianCount = F, PhysicsFemaleBlackCount = F, PhysicsFemaleHawaiianCount = F, PhysicsFemaleHispanicCount = F, PhysicsFemaleIDEACount = F, PhysicsFemaleLEPCount = F, PhysicsFemaleMultiRaceCount = F, PhysicsFemaleWhiteCount = F, PhysicsMaleAlaskanCount = F, PhysicsMaleAsianCount = F, PhysicsMaleBlackCount = F, PhysicsMaleHawaiianCount = F, PhysicsMaleHispanicCount = F, PhysicsMaleIDEACount = F, PhysicsMaleLEPCount = F, PhysicsMaleMultiRaceCount = F, PhysicsMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolHSScienceV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSScienceV1", objectId = CRDCSchoolHSScienceV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolHSScienceV1
	#'
	#' This function deletes a CRDCSchoolHSScienceV1
	#' @param CRDCSchoolHSScienceV1ID The ID of the CRDCSchoolHSScienceV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolHSScienceV1ID of the deleted CRDCSchoolHSScienceV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolHSScienceV1 <- function(CRDCSchoolHSScienceV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSScienceV1", objectId = CRDCSchoolHSScienceV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolHSScienceV1
	#'
	#' This function creates a CRDCSchoolHSScienceV1
	#' @param fieldNames The field values to give the created CRDCSchoolHSScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolHSScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolHSScienceV1 <- function(BiologyCertifiedClassCount = NULL, BiologyClassCount = NULL, BiologyFemaleAlaskanCount = NULL, BiologyFemaleAsianCount = NULL, BiologyFemaleBlackCount = NULL, BiologyFemaleHawaiianCount = NULL, BiologyFemaleHispanicCount = NULL, BiologyFemaleIDEACount = NULL, BiologyFemaleLEPCount = NULL, BiologyFemaleMultiRaceCount = NULL, BiologyFemaleWhiteCount = NULL, BiologyMaleAlaskanCount = NULL, BiologyMaleAsianCount = NULL, BiologyMaleBlackCount = NULL, BiologyMaleHawaiianCount = NULL, BiologyMaleHispanicCount = NULL, BiologyMaleIDEACount = NULL, BiologyMaleLEPCount = NULL, BiologyMaleMultiRaceCount = NULL, BiologyMaleWhiteCount = NULL, ChemistryCertifiedClassCount = NULL, ChemistryClassCount = NULL, ChemistryFemaleAlaskanCount = NULL, ChemistryFemaleAsianCount = NULL, ChemistryFemaleBlackCount = NULL, ChemistryFemaleHawaiianCount = NULL, ChemistryFemaleHispanicCount = NULL, ChemistryFemaleIDEACount = NULL, ChemistryFemaleLEPCount = NULL, ChemistryFemaleMultiRaceCount = NULL, ChemistryFemaleWhiteCount = NULL, ChemistryMaleAlaskanCount = NULL, ChemistryMaleAsianCount = NULL, ChemistryMaleBlackCount = NULL, ChemistryMaleHawaiianCount = NULL, ChemistryMaleHispanicCount = NULL, ChemistryMaleIDEACount = NULL, ChemistryMaleLEPCount = NULL, ChemistryMaleMultiRaceCount = NULL, ChemistryMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, PhysicsCertifiedClassCount = NULL, PhysicsClassCount = NULL, PhysicsFemaleAlaskanCount = NULL, PhysicsFemaleAsianCount = NULL, PhysicsFemaleBlackCount = NULL, PhysicsFemaleHawaiianCount = NULL, PhysicsFemaleHispanicCount = NULL, PhysicsFemaleIDEACount = NULL, PhysicsFemaleLEPCount = NULL, PhysicsFemaleMultiRaceCount = NULL, PhysicsFemaleWhiteCount = NULL, PhysicsMaleAlaskanCount = NULL, PhysicsMaleAsianCount = NULL, PhysicsMaleBlackCount = NULL, PhysicsMaleHawaiianCount = NULL, PhysicsMaleHispanicCount = NULL, PhysicsMaleIDEACount = NULL, PhysicsMaleLEPCount = NULL, PhysicsMaleMultiRaceCount = NULL, PhysicsMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSScienceV1", body = list(DataObject = body), searchFields = append("CRDCSchoolHSScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolHSScienceV1
	#'
	#' This function modifies a CRDCSchoolHSScienceV1
	#' @param fieldNames The field values to give the modified CRDCSchoolHSScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolHSScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolHSScienceV1 <- function(CRDCSchoolHSScienceV1ID, BiologyCertifiedClassCount = NULL, BiologyClassCount = NULL, BiologyFemaleAlaskanCount = NULL, BiologyFemaleAsianCount = NULL, BiologyFemaleBlackCount = NULL, BiologyFemaleHawaiianCount = NULL, BiologyFemaleHispanicCount = NULL, BiologyFemaleIDEACount = NULL, BiologyFemaleLEPCount = NULL, BiologyFemaleMultiRaceCount = NULL, BiologyFemaleWhiteCount = NULL, BiologyMaleAlaskanCount = NULL, BiologyMaleAsianCount = NULL, BiologyMaleBlackCount = NULL, BiologyMaleHawaiianCount = NULL, BiologyMaleHispanicCount = NULL, BiologyMaleIDEACount = NULL, BiologyMaleLEPCount = NULL, BiologyMaleMultiRaceCount = NULL, BiologyMaleWhiteCount = NULL, ChemistryCertifiedClassCount = NULL, ChemistryClassCount = NULL, ChemistryFemaleAlaskanCount = NULL, ChemistryFemaleAsianCount = NULL, ChemistryFemaleBlackCount = NULL, ChemistryFemaleHawaiianCount = NULL, ChemistryFemaleHispanicCount = NULL, ChemistryFemaleIDEACount = NULL, ChemistryFemaleLEPCount = NULL, ChemistryFemaleMultiRaceCount = NULL, ChemistryFemaleWhiteCount = NULL, ChemistryMaleAlaskanCount = NULL, ChemistryMaleAsianCount = NULL, ChemistryMaleBlackCount = NULL, ChemistryMaleHawaiianCount = NULL, ChemistryMaleHispanicCount = NULL, ChemistryMaleIDEACount = NULL, ChemistryMaleLEPCount = NULL, ChemistryMaleMultiRaceCount = NULL, ChemistryMaleWhiteCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, PhysicsCertifiedClassCount = NULL, PhysicsClassCount = NULL, PhysicsFemaleAlaskanCount = NULL, PhysicsFemaleAsianCount = NULL, PhysicsFemaleBlackCount = NULL, PhysicsFemaleHawaiianCount = NULL, PhysicsFemaleHispanicCount = NULL, PhysicsFemaleIDEACount = NULL, PhysicsFemaleLEPCount = NULL, PhysicsFemaleMultiRaceCount = NULL, PhysicsFemaleWhiteCount = NULL, PhysicsMaleAlaskanCount = NULL, PhysicsMaleAsianCount = NULL, PhysicsMaleBlackCount = NULL, PhysicsMaleHawaiianCount = NULL, PhysicsMaleHispanicCount = NULL, PhysicsMaleIDEACount = NULL, PhysicsMaleLEPCount = NULL, PhysicsMaleMultiRaceCount = NULL, PhysicsMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSScienceV1", objectId = CRDCSchoolHSScienceV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolHSScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolDualEnrollmentV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolDualEnrollmentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDualEnrollmentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDualEnrollmentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDualEnrollmentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolDualEnrollmentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolDualEnrollmentV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolDualEnrollmentV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolDualEnrollmentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolDualEnrollmentV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolDualEnrollmentV1
	#' @param CRDCSchoolDualEnrollmentV1ID The ID of the CRDCSchoolDualEnrollmentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolDualEnrollmentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolDualEnrollmentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolDualEnrollmentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolDualEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolDualEnrollmentV1 <- function(CRDCSchoolDualEnrollmentV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolDualEnrollmentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDualEnrollmentV1", objectId = CRDCSchoolDualEnrollmentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolDualEnrollmentV1
	#'
	#' This function deletes a CRDCSchoolDualEnrollmentV1
	#' @param CRDCSchoolDualEnrollmentV1ID The ID of the CRDCSchoolDualEnrollmentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolDualEnrollmentV1ID of the deleted CRDCSchoolDualEnrollmentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolDualEnrollmentV1 <- function(CRDCSchoolDualEnrollmentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDualEnrollmentV1", objectId = CRDCSchoolDualEnrollmentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolDualEnrollmentV1
	#'
	#' This function creates a CRDCSchoolDualEnrollmentV1
	#' @param fieldNames The field values to give the created CRDCSchoolDualEnrollmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolDualEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolDualEnrollmentV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDualEnrollmentV1", body = list(DataObject = body), searchFields = append("CRDCSchoolDualEnrollmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolDualEnrollmentV1
	#'
	#' This function modifies a CRDCSchoolDualEnrollmentV1
	#' @param fieldNames The field values to give the modified CRDCSchoolDualEnrollmentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolDualEnrollmentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolDualEnrollmentV1 <- function(CRDCSchoolDualEnrollmentV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolDualEnrollmentV1", objectId = CRDCSchoolDualEnrollmentV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolDualEnrollmentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolIBDiplomaV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolIBDiplomaV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolIBDiplomaV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolIBDiplomaV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolIBDiplomaV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolIBDiplomaV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolIBDiplomaV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolIBDiplomaV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolIBDiplomaV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolIBDiplomaV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolIBDiplomaV1
	#' @param CRDCSchoolIBDiplomaV1ID The ID of the CRDCSchoolIBDiplomaV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolIBDiplomaV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolIBDiplomaV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolIBDiplomaV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolIBDiplomaV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolIBDiplomaV1 <- function(CRDCSchoolIBDiplomaV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolIBDiplomaV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIBDiplomaV1", objectId = CRDCSchoolIBDiplomaV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolIBDiplomaV1
	#'
	#' This function deletes a CRDCSchoolIBDiplomaV1
	#' @param CRDCSchoolIBDiplomaV1ID The ID of the CRDCSchoolIBDiplomaV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolIBDiplomaV1ID of the deleted CRDCSchoolIBDiplomaV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolIBDiplomaV1 <- function(CRDCSchoolIBDiplomaV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIBDiplomaV1", objectId = CRDCSchoolIBDiplomaV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolIBDiplomaV1
	#'
	#' This function creates a CRDCSchoolIBDiplomaV1
	#' @param fieldNames The field values to give the created CRDCSchoolIBDiplomaV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolIBDiplomaV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolIBDiplomaV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIBDiplomaV1", body = list(DataObject = body), searchFields = append("CRDCSchoolIBDiplomaV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolIBDiplomaV1
	#'
	#' This function modifies a CRDCSchoolIBDiplomaV1
	#' @param fieldNames The field values to give the modified CRDCSchoolIBDiplomaV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolIBDiplomaV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolIBDiplomaV1 <- function(CRDCSchoolIBDiplomaV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolIBDiplomaV1", objectId = CRDCSchoolIBDiplomaV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolIBDiplomaV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolSingleSexClassV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolSingleSexClassV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSingleSexClassV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSingleSexClassV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSingleSexClassV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolSingleSexClassV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolSingleSexClassV1s <- function(searchConditionsList = NULL, AlgebraGeometryFemaleCount = F, AlgebraGeometryMaleCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolSingleSexClassV1ID = F, CreatedTime = F, EnglishFemaleCount = F, EnglishMaleCount = F, FederalNCESSchoolID = F, HasStudents = F, ModifiedTime = F, OtherMathFemaleCount = F, OtherMathMaleCount = F, OtherSubjectFemaleCount = F, OtherSubjectMaleCount = F, ScienceFemaleCount = F, ScienceMaleCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexClassV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolSingleSexClassV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolSingleSexClassV1
	#' @param CRDCSchoolSingleSexClassV1ID The ID of the CRDCSchoolSingleSexClassV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSingleSexClassV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSingleSexClassV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSingleSexClassV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolSingleSexClassV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolSingleSexClassV1 <- function(CRDCSchoolSingleSexClassV1ID, AlgebraGeometryFemaleCount = F, AlgebraGeometryMaleCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, EnglishFemaleCount = F, EnglishMaleCount = F, FederalNCESSchoolID = F, HasStudents = F, ModifiedTime = F, OtherMathFemaleCount = F, OtherMathMaleCount = F, OtherSubjectFemaleCount = F, OtherSubjectMaleCount = F, ScienceFemaleCount = F, ScienceMaleCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolSingleSexClassV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexClassV1", objectId = CRDCSchoolSingleSexClassV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolSingleSexClassV1
	#'
	#' This function deletes a CRDCSchoolSingleSexClassV1
	#' @param CRDCSchoolSingleSexClassV1ID The ID of the CRDCSchoolSingleSexClassV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolSingleSexClassV1ID of the deleted CRDCSchoolSingleSexClassV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolSingleSexClassV1 <- function(CRDCSchoolSingleSexClassV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexClassV1", objectId = CRDCSchoolSingleSexClassV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolSingleSexClassV1
	#'
	#' This function creates a CRDCSchoolSingleSexClassV1
	#' @param fieldNames The field values to give the created CRDCSchoolSingleSexClassV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolSingleSexClassV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolSingleSexClassV1 <- function(AlgebraGeometryFemaleCount = NULL, AlgebraGeometryMaleCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EnglishFemaleCount = NULL, EnglishMaleCount = NULL, FederalNCESSchoolID = NULL, HasStudents = NULL, OtherMathFemaleCount = NULL, OtherMathMaleCount = NULL, OtherSubjectFemaleCount = NULL, OtherSubjectMaleCount = NULL, ScienceFemaleCount = NULL, ScienceMaleCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexClassV1", body = list(DataObject = body), searchFields = append("CRDCSchoolSingleSexClassV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolSingleSexClassV1
	#'
	#' This function modifies a CRDCSchoolSingleSexClassV1
	#' @param fieldNames The field values to give the modified CRDCSchoolSingleSexClassV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolSingleSexClassV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolSingleSexClassV1 <- function(CRDCSchoolSingleSexClassV1ID, AlgebraGeometryFemaleCount = NULL, AlgebraGeometryMaleCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EnglishFemaleCount = NULL, EnglishMaleCount = NULL, FederalNCESSchoolID = NULL, HasStudents = NULL, OtherMathFemaleCount = NULL, OtherMathMaleCount = NULL, OtherSubjectFemaleCount = NULL, OtherSubjectMaleCount = NULL, ScienceFemaleCount = NULL, ScienceMaleCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexClassV1", objectId = CRDCSchoolSingleSexClassV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolSingleSexClassV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolSupportStaffV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolSupportStaffV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSupportStaffV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSupportStaffV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSupportStaffV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolSupportStaffV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolSupportStaffV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CounselorFTETotal = F, CRDCSchoolSupportStaffV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, ModifiedTime = F, NurseFTETotal = F, OfficerFTETotal = F, PsychologistFTETotal = F, SecurityGuardFTETotal = F, SocialWorkerFTETotal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolSupportStaffV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolSupportStaffV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolSupportStaffV1
	#' @param CRDCSchoolSupportStaffV1ID The ID of the CRDCSchoolSupportStaffV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSupportStaffV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSupportStaffV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSupportStaffV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolSupportStaffV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolSupportStaffV1 <- function(CRDCSchoolSupportStaffV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CounselorFTETotal = F, CreatedTime = F, FederalNCESSchoolID = F, ModifiedTime = F, NurseFTETotal = F, OfficerFTETotal = F, PsychologistFTETotal = F, SecurityGuardFTETotal = F, SocialWorkerFTETotal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolSupportStaffV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSupportStaffV1", objectId = CRDCSchoolSupportStaffV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolSupportStaffV1
	#'
	#' This function deletes a CRDCSchoolSupportStaffV1
	#' @param CRDCSchoolSupportStaffV1ID The ID of the CRDCSchoolSupportStaffV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolSupportStaffV1ID of the deleted CRDCSchoolSupportStaffV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolSupportStaffV1 <- function(CRDCSchoolSupportStaffV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSupportStaffV1", objectId = CRDCSchoolSupportStaffV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolSupportStaffV1
	#'
	#' This function creates a CRDCSchoolSupportStaffV1
	#' @param fieldNames The field values to give the created CRDCSchoolSupportStaffV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolSupportStaffV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolSupportStaffV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CounselorFTETotal = NULL, FederalNCESSchoolID = NULL, NurseFTETotal = NULL, OfficerFTETotal = NULL, PsychologistFTETotal = NULL, SecurityGuardFTETotal = NULL, SocialWorkerFTETotal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSupportStaffV1", body = list(DataObject = body), searchFields = append("CRDCSchoolSupportStaffV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolSupportStaffV1
	#'
	#' This function modifies a CRDCSchoolSupportStaffV1
	#' @param fieldNames The field values to give the modified CRDCSchoolSupportStaffV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolSupportStaffV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolSupportStaffV1 <- function(CRDCSchoolSupportStaffV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CounselorFTETotal = NULL, FederalNCESSchoolID = NULL, NurseFTETotal = NULL, OfficerFTETotal = NULL, PsychologistFTETotal = NULL, SecurityGuardFTETotal = NULL, SocialWorkerFTETotal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSupportStaffV1", objectId = CRDCSchoolSupportStaffV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolSupportStaffV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolTeacherV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolTeacherV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolTeacherV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolTeacherV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolTeacherV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolTeacherV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolTeacherV1s <- function(searchConditionsList = NULL, CertifiedFTETotal = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolTeacherV1ID = F, CreatedTime = F, CurrentYearCount = F, FederalNCESSchoolID = F, FirstYearFTETotal = F, FTETotal = F, ModifiedTime = F, NotCertifiedFTETotal = F, PreviousYearCount = F, SecondYearFTETotal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolTeacherV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolTeacherV1
	#' @param CRDCSchoolTeacherV1ID The ID of the CRDCSchoolTeacherV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolTeacherV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolTeacherV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolTeacherV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolTeacherV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolTeacherV1 <- function(CRDCSchoolTeacherV1ID, CertifiedFTETotal = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, CurrentYearCount = F, FederalNCESSchoolID = F, FirstYearFTETotal = F, FTETotal = F, ModifiedTime = F, NotCertifiedFTETotal = F, PreviousYearCount = F, SecondYearFTETotal = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolTeacherV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherV1", objectId = CRDCSchoolTeacherV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolTeacherV1
	#'
	#' This function deletes a CRDCSchoolTeacherV1
	#' @param CRDCSchoolTeacherV1ID The ID of the CRDCSchoolTeacherV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolTeacherV1ID of the deleted CRDCSchoolTeacherV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolTeacherV1 <- function(CRDCSchoolTeacherV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherV1", objectId = CRDCSchoolTeacherV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolTeacherV1
	#'
	#' This function creates a CRDCSchoolTeacherV1
	#' @param fieldNames The field values to give the created CRDCSchoolTeacherV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolTeacherV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolTeacherV1 <- function(CertifiedFTETotal = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CurrentYearCount = NULL, FederalNCESSchoolID = NULL, FirstYearFTETotal = NULL, FTETotal = NULL, NotCertifiedFTETotal = NULL, PreviousYearCount = NULL, SecondYearFTETotal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherV1", body = list(DataObject = body), searchFields = append("CRDCSchoolTeacherV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolTeacherV1
	#'
	#' This function modifies a CRDCSchoolTeacherV1
	#' @param fieldNames The field values to give the modified CRDCSchoolTeacherV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolTeacherV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolTeacherV1 <- function(CRDCSchoolTeacherV1ID, CertifiedFTETotal = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CurrentYearCount = NULL, FederalNCESSchoolID = NULL, FirstYearFTETotal = NULL, FTETotal = NULL, NotCertifiedFTETotal = NULL, PreviousYearCount = NULL, SecondYearFTETotal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolTeacherV1", objectId = CRDCSchoolTeacherV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolTeacherV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolHSPassedAlgebraIV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolHSPassedAlgebraIV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHSPassedAlgebraIV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHSPassedAlgebraIV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHSPassedAlgebraIV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolHSPassedAlgebraIV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolHSPassedAlgebraIV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolHSPassedAlgebraIV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, Grade0910FemaleAlaskanCount = F, Grade0910FemaleAsianCount = F, Grade0910FemaleBlackCount = F, Grade0910FemaleHawaiianCount = F, Grade0910FemaleHispanicCount = F, Grade0910FemaleIDEACount = F, Grade0910FemaleLEPCount = F, Grade0910FemaleMultiRaceCount = F, Grade0910FemaleWhiteCount = F, Grade0910MaleAlaskanCount = F, Grade0910MaleAsianCount = F, Grade0910MaleBlackCount = F, Grade0910MaleHawaiianCount = F, Grade0910MaleHispanicCount = F, Grade0910MaleIDEACount = F, Grade0910MaleLEPCount = F, Grade0910MaleMultiRaceCount = F, Grade0910MaleWhiteCount = F, Grade1112FemaleAlaskanCount = F, Grade1112FemaleAsianCount = F, Grade1112FemaleBlackCount = F, Grade1112FemaleHawaiianCount = F, Grade1112FemaleHispanicCount = F, Grade1112FemaleIDEACount = F, Grade1112FemaleLEPCount = F, Grade1112FemaleMultiRaceCount = F, Grade1112FemaleWhiteCount = F, Grade1112MaleAlaskanCount = F, Grade1112MaleAsianCount = F, Grade1112MaleBlackCount = F, Grade1112MaleHawaiianCount = F, Grade1112MaleHispanicCount = F, Grade1112MaleIDEACount = F, Grade1112MaleLEPCount = F, Grade1112MaleMultiRaceCount = F, Grade1112MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolHSPassedAlgebraIV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolHSPassedAlgebraIV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolHSPassedAlgebraIV1
	#' @param CRDCSchoolHSPassedAlgebraIV1ID The ID of the CRDCSchoolHSPassedAlgebraIV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolHSPassedAlgebraIV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolHSPassedAlgebraIV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolHSPassedAlgebraIV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolHSPassedAlgebraIV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolHSPassedAlgebraIV1 <- function(CRDCSchoolHSPassedAlgebraIV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, Grade0910FemaleAlaskanCount = F, Grade0910FemaleAsianCount = F, Grade0910FemaleBlackCount = F, Grade0910FemaleHawaiianCount = F, Grade0910FemaleHispanicCount = F, Grade0910FemaleIDEACount = F, Grade0910FemaleLEPCount = F, Grade0910FemaleMultiRaceCount = F, Grade0910FemaleWhiteCount = F, Grade0910MaleAlaskanCount = F, Grade0910MaleAsianCount = F, Grade0910MaleBlackCount = F, Grade0910MaleHawaiianCount = F, Grade0910MaleHispanicCount = F, Grade0910MaleIDEACount = F, Grade0910MaleLEPCount = F, Grade0910MaleMultiRaceCount = F, Grade0910MaleWhiteCount = F, Grade1112FemaleAlaskanCount = F, Grade1112FemaleAsianCount = F, Grade1112FemaleBlackCount = F, Grade1112FemaleHawaiianCount = F, Grade1112FemaleHispanicCount = F, Grade1112FemaleIDEACount = F, Grade1112FemaleLEPCount = F, Grade1112FemaleMultiRaceCount = F, Grade1112FemaleWhiteCount = F, Grade1112MaleAlaskanCount = F, Grade1112MaleAsianCount = F, Grade1112MaleBlackCount = F, Grade1112MaleHawaiianCount = F, Grade1112MaleHispanicCount = F, Grade1112MaleIDEACount = F, Grade1112MaleLEPCount = F, Grade1112MaleMultiRaceCount = F, Grade1112MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolHSPassedAlgebraIV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSPassedAlgebraIV1", objectId = CRDCSchoolHSPassedAlgebraIV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolHSPassedAlgebraIV1
	#'
	#' This function deletes a CRDCSchoolHSPassedAlgebraIV1
	#' @param CRDCSchoolHSPassedAlgebraIV1ID The ID of the CRDCSchoolHSPassedAlgebraIV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolHSPassedAlgebraIV1ID of the deleted CRDCSchoolHSPassedAlgebraIV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolHSPassedAlgebraIV1 <- function(CRDCSchoolHSPassedAlgebraIV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSPassedAlgebraIV1", objectId = CRDCSchoolHSPassedAlgebraIV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolHSPassedAlgebraIV1
	#'
	#' This function creates a CRDCSchoolHSPassedAlgebraIV1
	#' @param fieldNames The field values to give the created CRDCSchoolHSPassedAlgebraIV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolHSPassedAlgebraIV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolHSPassedAlgebraIV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, Grade0910FemaleAlaskanCount = NULL, Grade0910FemaleAsianCount = NULL, Grade0910FemaleBlackCount = NULL, Grade0910FemaleHawaiianCount = NULL, Grade0910FemaleHispanicCount = NULL, Grade0910FemaleIDEACount = NULL, Grade0910FemaleLEPCount = NULL, Grade0910FemaleMultiRaceCount = NULL, Grade0910FemaleWhiteCount = NULL, Grade0910MaleAlaskanCount = NULL, Grade0910MaleAsianCount = NULL, Grade0910MaleBlackCount = NULL, Grade0910MaleHawaiianCount = NULL, Grade0910MaleHispanicCount = NULL, Grade0910MaleIDEACount = NULL, Grade0910MaleLEPCount = NULL, Grade0910MaleMultiRaceCount = NULL, Grade0910MaleWhiteCount = NULL, Grade1112FemaleAlaskanCount = NULL, Grade1112FemaleAsianCount = NULL, Grade1112FemaleBlackCount = NULL, Grade1112FemaleHawaiianCount = NULL, Grade1112FemaleHispanicCount = NULL, Grade1112FemaleIDEACount = NULL, Grade1112FemaleLEPCount = NULL, Grade1112FemaleMultiRaceCount = NULL, Grade1112FemaleWhiteCount = NULL, Grade1112MaleAlaskanCount = NULL, Grade1112MaleAsianCount = NULL, Grade1112MaleBlackCount = NULL, Grade1112MaleHawaiianCount = NULL, Grade1112MaleHispanicCount = NULL, Grade1112MaleIDEACount = NULL, Grade1112MaleLEPCount = NULL, Grade1112MaleMultiRaceCount = NULL, Grade1112MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSPassedAlgebraIV1", body = list(DataObject = body), searchFields = append("CRDCSchoolHSPassedAlgebraIV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolHSPassedAlgebraIV1
	#'
	#' This function modifies a CRDCSchoolHSPassedAlgebraIV1
	#' @param fieldNames The field values to give the modified CRDCSchoolHSPassedAlgebraIV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolHSPassedAlgebraIV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolHSPassedAlgebraIV1 <- function(CRDCSchoolHSPassedAlgebraIV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, Grade0910FemaleAlaskanCount = NULL, Grade0910FemaleAsianCount = NULL, Grade0910FemaleBlackCount = NULL, Grade0910FemaleHawaiianCount = NULL, Grade0910FemaleHispanicCount = NULL, Grade0910FemaleIDEACount = NULL, Grade0910FemaleLEPCount = NULL, Grade0910FemaleMultiRaceCount = NULL, Grade0910FemaleWhiteCount = NULL, Grade0910MaleAlaskanCount = NULL, Grade0910MaleAsianCount = NULL, Grade0910MaleBlackCount = NULL, Grade0910MaleHawaiianCount = NULL, Grade0910MaleHispanicCount = NULL, Grade0910MaleIDEACount = NULL, Grade0910MaleLEPCount = NULL, Grade0910MaleMultiRaceCount = NULL, Grade0910MaleWhiteCount = NULL, Grade1112FemaleAlaskanCount = NULL, Grade1112FemaleAsianCount = NULL, Grade1112FemaleBlackCount = NULL, Grade1112FemaleHawaiianCount = NULL, Grade1112FemaleHispanicCount = NULL, Grade1112FemaleIDEACount = NULL, Grade1112FemaleLEPCount = NULL, Grade1112FemaleMultiRaceCount = NULL, Grade1112FemaleWhiteCount = NULL, Grade1112MaleAlaskanCount = NULL, Grade1112MaleAsianCount = NULL, Grade1112MaleBlackCount = NULL, Grade1112MaleHawaiianCount = NULL, Grade1112MaleHispanicCount = NULL, Grade1112MaleIDEACount = NULL, Grade1112MaleLEPCount = NULL, Grade1112MaleMultiRaceCount = NULL, Grade1112MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolHSPassedAlgebraIV1", objectId = CRDCSchoolHSPassedAlgebraIV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolHSPassedAlgebraIV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolMSPassedAlgebraIV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolMSPassedAlgebraIV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolMSPassedAlgebraIV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolMSPassedAlgebraIV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolMSPassedAlgebraIV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolMSPassedAlgebraIV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolMSPassedAlgebraIV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolMSPassedAlgebraIV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, Grade07StudentCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolMSPassedAlgebraIV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolMSPassedAlgebraIV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolMSPassedAlgebraIV1
	#' @param CRDCSchoolMSPassedAlgebraIV1ID The ID of the CRDCSchoolMSPassedAlgebraIV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolMSPassedAlgebraIV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolMSPassedAlgebraIV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolMSPassedAlgebraIV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolMSPassedAlgebraIV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolMSPassedAlgebraIV1 <- function(CRDCSchoolMSPassedAlgebraIV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, Grade07StudentCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolMSPassedAlgebraIV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSPassedAlgebraIV1", objectId = CRDCSchoolMSPassedAlgebraIV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolMSPassedAlgebraIV1
	#'
	#' This function deletes a CRDCSchoolMSPassedAlgebraIV1
	#' @param CRDCSchoolMSPassedAlgebraIV1ID The ID of the CRDCSchoolMSPassedAlgebraIV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolMSPassedAlgebraIV1ID of the deleted CRDCSchoolMSPassedAlgebraIV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolMSPassedAlgebraIV1 <- function(CRDCSchoolMSPassedAlgebraIV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSPassedAlgebraIV1", objectId = CRDCSchoolMSPassedAlgebraIV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolMSPassedAlgebraIV1
	#'
	#' This function creates a CRDCSchoolMSPassedAlgebraIV1
	#' @param fieldNames The field values to give the created CRDCSchoolMSPassedAlgebraIV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolMSPassedAlgebraIV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolMSPassedAlgebraIV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, Grade07StudentCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSPassedAlgebraIV1", body = list(DataObject = body), searchFields = append("CRDCSchoolMSPassedAlgebraIV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolMSPassedAlgebraIV1
	#'
	#' This function modifies a CRDCSchoolMSPassedAlgebraIV1
	#' @param fieldNames The field values to give the modified CRDCSchoolMSPassedAlgebraIV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolMSPassedAlgebraIV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolMSPassedAlgebraIV1 <- function(CRDCSchoolMSPassedAlgebraIV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, Grade07StudentCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolMSPassedAlgebraIV1", objectId = CRDCSchoolMSPassedAlgebraIV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolMSPassedAlgebraIV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPExamScoreV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPExamScoreV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPExamScoreV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPExamScoreV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPExamScoreV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPExamScoreV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPExamScoreV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAPExamScoreV1ID = F, CreatedTime = F, FailedFemaleAlaskanCount = F, FailedFemaleAsianCount = F, FailedFemaleBlackCount = F, FailedFemaleHawaiianCount = F, FailedFemaleHispanicCount = F, FailedFemaleIDEACount = F, FailedFemaleLEPCount = F, FailedFemaleMultiRaceCount = F, FailedFemaleWhiteCount = F, FailedMaleAlaskanCount = F, FailedMaleAsianCount = F, FailedMaleBlackCount = F, FailedMaleHawaiianCount = F, FailedMaleHispanicCount = F, FailedMaleIDEACount = F, FailedMaleLEPCount = F, FailedMaleMultiRaceCount = F, FailedMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, PassedFemaleAlaskanCount = F, PassedFemaleAsianCount = F, PassedFemaleBlackCount = F, PassedFemaleHawaiianCount = F, PassedFemaleHispanicCount = F, PassedFemaleIDEACount = F, PassedFemaleLEPCount = F, PassedFemaleMultiRaceCount = F, PassedFemaleWhiteCount = F, PassedMaleAlaskanCount = F, PassedMaleAsianCount = F, PassedMaleBlackCount = F, PassedMaleHawaiianCount = F, PassedMaleHispanicCount = F, PassedMaleIDEACount = F, PassedMaleLEPCount = F, PassedMaleMultiRaceCount = F, PassedMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamScoreV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPExamScoreV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPExamScoreV1
	#' @param CRDCSchoolAPExamScoreV1ID The ID of the CRDCSchoolAPExamScoreV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPExamScoreV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPExamScoreV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPExamScoreV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPExamScoreV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPExamScoreV1 <- function(CRDCSchoolAPExamScoreV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FailedFemaleAlaskanCount = F, FailedFemaleAsianCount = F, FailedFemaleBlackCount = F, FailedFemaleHawaiianCount = F, FailedFemaleHispanicCount = F, FailedFemaleIDEACount = F, FailedFemaleLEPCount = F, FailedFemaleMultiRaceCount = F, FailedFemaleWhiteCount = F, FailedMaleAlaskanCount = F, FailedMaleAsianCount = F, FailedMaleBlackCount = F, FailedMaleHawaiianCount = F, FailedMaleHispanicCount = F, FailedMaleIDEACount = F, FailedMaleLEPCount = F, FailedMaleMultiRaceCount = F, FailedMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, PassedFemaleAlaskanCount = F, PassedFemaleAsianCount = F, PassedFemaleBlackCount = F, PassedFemaleHawaiianCount = F, PassedFemaleHispanicCount = F, PassedFemaleIDEACount = F, PassedFemaleLEPCount = F, PassedFemaleMultiRaceCount = F, PassedFemaleWhiteCount = F, PassedMaleAlaskanCount = F, PassedMaleAsianCount = F, PassedMaleBlackCount = F, PassedMaleHawaiianCount = F, PassedMaleHispanicCount = F, PassedMaleIDEACount = F, PassedMaleLEPCount = F, PassedMaleMultiRaceCount = F, PassedMaleWhiteCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPExamScoreV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamScoreV1", objectId = CRDCSchoolAPExamScoreV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPExamScoreV1
	#'
	#' This function deletes a CRDCSchoolAPExamScoreV1
	#' @param CRDCSchoolAPExamScoreV1ID The ID of the CRDCSchoolAPExamScoreV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPExamScoreV1ID of the deleted CRDCSchoolAPExamScoreV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPExamScoreV1 <- function(CRDCSchoolAPExamScoreV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamScoreV1", objectId = CRDCSchoolAPExamScoreV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPExamScoreV1
	#'
	#' This function creates a CRDCSchoolAPExamScoreV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPExamScoreV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPExamScoreV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPExamScoreV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FailedFemaleAlaskanCount = NULL, FailedFemaleAsianCount = NULL, FailedFemaleBlackCount = NULL, FailedFemaleHawaiianCount = NULL, FailedFemaleHispanicCount = NULL, FailedFemaleIDEACount = NULL, FailedFemaleLEPCount = NULL, FailedFemaleMultiRaceCount = NULL, FailedFemaleWhiteCount = NULL, FailedMaleAlaskanCount = NULL, FailedMaleAsianCount = NULL, FailedMaleBlackCount = NULL, FailedMaleHawaiianCount = NULL, FailedMaleHispanicCount = NULL, FailedMaleIDEACount = NULL, FailedMaleLEPCount = NULL, FailedMaleMultiRaceCount = NULL, FailedMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, PassedFemaleAlaskanCount = NULL, PassedFemaleAsianCount = NULL, PassedFemaleBlackCount = NULL, PassedFemaleHawaiianCount = NULL, PassedFemaleHispanicCount = NULL, PassedFemaleIDEACount = NULL, PassedFemaleLEPCount = NULL, PassedFemaleMultiRaceCount = NULL, PassedFemaleWhiteCount = NULL, PassedMaleAlaskanCount = NULL, PassedMaleAsianCount = NULL, PassedMaleBlackCount = NULL, PassedMaleHawaiianCount = NULL, PassedMaleHispanicCount = NULL, PassedMaleIDEACount = NULL, PassedMaleLEPCount = NULL, PassedMaleMultiRaceCount = NULL, PassedMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamScoreV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPExamScoreV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPExamScoreV1
	#'
	#' This function modifies a CRDCSchoolAPExamScoreV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPExamScoreV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPExamScoreV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPExamScoreV1 <- function(CRDCSchoolAPExamScoreV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FailedFemaleAlaskanCount = NULL, FailedFemaleAsianCount = NULL, FailedFemaleBlackCount = NULL, FailedFemaleHawaiianCount = NULL, FailedFemaleHispanicCount = NULL, FailedFemaleIDEACount = NULL, FailedFemaleLEPCount = NULL, FailedFemaleMultiRaceCount = NULL, FailedFemaleWhiteCount = NULL, FailedMaleAlaskanCount = NULL, FailedMaleAsianCount = NULL, FailedMaleBlackCount = NULL, FailedMaleHawaiianCount = NULL, FailedMaleHispanicCount = NULL, FailedMaleIDEACount = NULL, FailedMaleLEPCount = NULL, FailedMaleMultiRaceCount = NULL, FailedMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, PassedFemaleAlaskanCount = NULL, PassedFemaleAsianCount = NULL, PassedFemaleBlackCount = NULL, PassedFemaleHawaiianCount = NULL, PassedFemaleHispanicCount = NULL, PassedFemaleIDEACount = NULL, PassedFemaleLEPCount = NULL, PassedFemaleMultiRaceCount = NULL, PassedFemaleWhiteCount = NULL, PassedMaleAlaskanCount = NULL, PassedMaleAsianCount = NULL, PassedMaleBlackCount = NULL, PassedMaleHawaiianCount = NULL, PassedMaleHispanicCount = NULL, PassedMaleIDEACount = NULL, PassedMaleLEPCount = NULL, PassedMaleMultiRaceCount = NULL, PassedMaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamScoreV1", objectId = CRDCSchoolAPExamScoreV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPExamScoreV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPExamV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPExamV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPExamV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPExamV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPExamV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPExamV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPExamV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAPExamV1ID = F, CreatedTime = F, EnrolledFemaleAlaskanCount = F, EnrolledFemaleAsianCount = F, EnrolledFemaleBlackCount = F, EnrolledFemaleHawaiianCount = F, EnrolledFemaleHispanicCount = F, EnrolledFemaleIDEACount = F, EnrolledFemaleLEPCount = F, EnrolledFemaleMultiRaceCount = F, EnrolledFemaleWhiteCount = F, EnrolledMaleAlaskanCount = F, EnrolledMaleAsianCount = F, EnrolledMaleBlackCount = F, EnrolledMaleHawaiianCount = F, EnrolledMaleHispanicCount = F, EnrolledMaleIDEACount = F, EnrolledMaleLEPCount = F, EnrolledMaleMultiRaceCount = F, EnrolledMaleWhiteCount = F, ExamTakenFemaleAlaskanCount = F, ExamTakenFemaleAsianCount = F, ExamTakenFemaleBlackCount = F, ExamTakenFemaleHawaiianCount = F, ExamTakenFemaleHispanicCount = F, ExamTakenFemaleIDEACount = F, ExamTakenFemaleLEPCount = F, ExamTakenFemaleMultiRaceCount = F, ExamTakenFemaleWhiteCount = F, ExamTakenMaleAlaskanCount = F, ExamTakenMaleAsianCount = F, ExamTakenMaleBlackCount = F, ExamTakenMaleHawaiianCount = F, ExamTakenMaleHispanicCount = F, ExamTakenMaleIDEACount = F, ExamTakenMaleLEPCount = F, ExamTakenMaleMultiRaceCount = F, ExamTakenMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPExamV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPExamV1
	#' @param CRDCSchoolAPExamV1ID The ID of the CRDCSchoolAPExamV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPExamV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPExamV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPExamV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPExamV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPExamV1 <- function(CRDCSchoolAPExamV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, EnrolledFemaleAlaskanCount = F, EnrolledFemaleAsianCount = F, EnrolledFemaleBlackCount = F, EnrolledFemaleHawaiianCount = F, EnrolledFemaleHispanicCount = F, EnrolledFemaleIDEACount = F, EnrolledFemaleLEPCount = F, EnrolledFemaleMultiRaceCount = F, EnrolledFemaleWhiteCount = F, EnrolledMaleAlaskanCount = F, EnrolledMaleAsianCount = F, EnrolledMaleBlackCount = F, EnrolledMaleHawaiianCount = F, EnrolledMaleHispanicCount = F, EnrolledMaleIDEACount = F, EnrolledMaleLEPCount = F, EnrolledMaleMultiRaceCount = F, EnrolledMaleWhiteCount = F, ExamTakenFemaleAlaskanCount = F, ExamTakenFemaleAsianCount = F, ExamTakenFemaleBlackCount = F, ExamTakenFemaleHawaiianCount = F, ExamTakenFemaleHispanicCount = F, ExamTakenFemaleIDEACount = F, ExamTakenFemaleLEPCount = F, ExamTakenFemaleMultiRaceCount = F, ExamTakenFemaleWhiteCount = F, ExamTakenMaleAlaskanCount = F, ExamTakenMaleAsianCount = F, ExamTakenMaleBlackCount = F, ExamTakenMaleHawaiianCount = F, ExamTakenMaleHispanicCount = F, ExamTakenMaleIDEACount = F, ExamTakenMaleLEPCount = F, ExamTakenMaleMultiRaceCount = F, ExamTakenMaleWhiteCount = F, FederalNCESSchoolID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPExamV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamV1", objectId = CRDCSchoolAPExamV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPExamV1
	#'
	#' This function deletes a CRDCSchoolAPExamV1
	#' @param CRDCSchoolAPExamV1ID The ID of the CRDCSchoolAPExamV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPExamV1ID of the deleted CRDCSchoolAPExamV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPExamV1 <- function(CRDCSchoolAPExamV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamV1", objectId = CRDCSchoolAPExamV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPExamV1
	#'
	#' This function creates a CRDCSchoolAPExamV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPExamV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPExamV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPExamV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EnrolledFemaleAlaskanCount = NULL, EnrolledFemaleAsianCount = NULL, EnrolledFemaleBlackCount = NULL, EnrolledFemaleHawaiianCount = NULL, EnrolledFemaleHispanicCount = NULL, EnrolledFemaleIDEACount = NULL, EnrolledFemaleLEPCount = NULL, EnrolledFemaleMultiRaceCount = NULL, EnrolledFemaleWhiteCount = NULL, EnrolledMaleAlaskanCount = NULL, EnrolledMaleAsianCount = NULL, EnrolledMaleBlackCount = NULL, EnrolledMaleHawaiianCount = NULL, EnrolledMaleHispanicCount = NULL, EnrolledMaleIDEACount = NULL, EnrolledMaleLEPCount = NULL, EnrolledMaleMultiRaceCount = NULL, EnrolledMaleWhiteCount = NULL, ExamTakenFemaleAlaskanCount = NULL, ExamTakenFemaleAsianCount = NULL, ExamTakenFemaleBlackCount = NULL, ExamTakenFemaleHawaiianCount = NULL, ExamTakenFemaleHispanicCount = NULL, ExamTakenFemaleIDEACount = NULL, ExamTakenFemaleLEPCount = NULL, ExamTakenFemaleMultiRaceCount = NULL, ExamTakenFemaleWhiteCount = NULL, ExamTakenMaleAlaskanCount = NULL, ExamTakenMaleAsianCount = NULL, ExamTakenMaleBlackCount = NULL, ExamTakenMaleHawaiianCount = NULL, ExamTakenMaleHispanicCount = NULL, ExamTakenMaleIDEACount = NULL, ExamTakenMaleLEPCount = NULL, ExamTakenMaleMultiRaceCount = NULL, ExamTakenMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPExamV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPExamV1
	#'
	#' This function modifies a CRDCSchoolAPExamV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPExamV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPExamV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPExamV1 <- function(CRDCSchoolAPExamV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EnrolledFemaleAlaskanCount = NULL, EnrolledFemaleAsianCount = NULL, EnrolledFemaleBlackCount = NULL, EnrolledFemaleHawaiianCount = NULL, EnrolledFemaleHispanicCount = NULL, EnrolledFemaleIDEACount = NULL, EnrolledFemaleLEPCount = NULL, EnrolledFemaleMultiRaceCount = NULL, EnrolledFemaleWhiteCount = NULL, EnrolledMaleAlaskanCount = NULL, EnrolledMaleAsianCount = NULL, EnrolledMaleBlackCount = NULL, EnrolledMaleHawaiianCount = NULL, EnrolledMaleHispanicCount = NULL, EnrolledMaleIDEACount = NULL, EnrolledMaleLEPCount = NULL, EnrolledMaleMultiRaceCount = NULL, EnrolledMaleWhiteCount = NULL, ExamTakenFemaleAlaskanCount = NULL, ExamTakenFemaleAsianCount = NULL, ExamTakenFemaleBlackCount = NULL, ExamTakenFemaleHawaiianCount = NULL, ExamTakenFemaleHispanicCount = NULL, ExamTakenFemaleIDEACount = NULL, ExamTakenFemaleLEPCount = NULL, ExamTakenFemaleMultiRaceCount = NULL, ExamTakenFemaleWhiteCount = NULL, ExamTakenMaleAlaskanCount = NULL, ExamTakenMaleAsianCount = NULL, ExamTakenMaleBlackCount = NULL, ExamTakenMaleHawaiianCount = NULL, ExamTakenMaleHispanicCount = NULL, ExamTakenMaleIDEACount = NULL, ExamTakenMaleLEPCount = NULL, ExamTakenMaleMultiRaceCount = NULL, ExamTakenMaleWhiteCount = NULL, FederalNCESSchoolID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPExamV1", objectId = CRDCSchoolAPExamV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPExamV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolCreditRecoveryV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolCreditRecoveryV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolCreditRecoveryV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolCreditRecoveryV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolCreditRecoveryV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolCreditRecoveryV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolCreditRecoveryV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolCreditRecoveryV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, HasProgram = F, ModifiedTime = F, StudentCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolCreditRecoveryV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolCreditRecoveryV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolCreditRecoveryV1
	#' @param CRDCSchoolCreditRecoveryV1ID The ID of the CRDCSchoolCreditRecoveryV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolCreditRecoveryV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolCreditRecoveryV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolCreditRecoveryV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolCreditRecoveryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolCreditRecoveryV1 <- function(CRDCSchoolCreditRecoveryV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, HasProgram = F, ModifiedTime = F, StudentCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolCreditRecoveryV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCreditRecoveryV1", objectId = CRDCSchoolCreditRecoveryV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolCreditRecoveryV1
	#'
	#' This function deletes a CRDCSchoolCreditRecoveryV1
	#' @param CRDCSchoolCreditRecoveryV1ID The ID of the CRDCSchoolCreditRecoveryV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolCreditRecoveryV1ID of the deleted CRDCSchoolCreditRecoveryV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolCreditRecoveryV1 <- function(CRDCSchoolCreditRecoveryV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCreditRecoveryV1", objectId = CRDCSchoolCreditRecoveryV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolCreditRecoveryV1
	#'
	#' This function creates a CRDCSchoolCreditRecoveryV1
	#' @param fieldNames The field values to give the created CRDCSchoolCreditRecoveryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolCreditRecoveryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolCreditRecoveryV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasProgram = NULL, StudentCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCreditRecoveryV1", body = list(DataObject = body), searchFields = append("CRDCSchoolCreditRecoveryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolCreditRecoveryV1
	#'
	#' This function modifies a CRDCSchoolCreditRecoveryV1
	#' @param fieldNames The field values to give the modified CRDCSchoolCreditRecoveryV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolCreditRecoveryV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolCreditRecoveryV1 <- function(CRDCSchoolCreditRecoveryV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasProgram = NULL, StudentCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolCreditRecoveryV1", objectId = CRDCSchoolCreditRecoveryV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolCreditRecoveryV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolSATACTV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolSATACTV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSATACTV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSATACTV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSATACTV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolSATACTV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolSATACTV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolSATACTV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolSATACTV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolSATACTV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolSATACTV1
	#' @param CRDCSchoolSATACTV1ID The ID of the CRDCSchoolSATACTV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSATACTV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSATACTV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSATACTV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolSATACTV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolSATACTV1 <- function(CRDCSchoolSATACTV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolSATACTV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSATACTV1", objectId = CRDCSchoolSATACTV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolSATACTV1
	#'
	#' This function deletes a CRDCSchoolSATACTV1
	#' @param CRDCSchoolSATACTV1ID The ID of the CRDCSchoolSATACTV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolSATACTV1ID of the deleted CRDCSchoolSATACTV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolSATACTV1 <- function(CRDCSchoolSATACTV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSATACTV1", objectId = CRDCSchoolSATACTV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolSATACTV1
	#'
	#' This function creates a CRDCSchoolSATACTV1
	#' @param fieldNames The field values to give the created CRDCSchoolSATACTV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolSATACTV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolSATACTV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSATACTV1", body = list(DataObject = body), searchFields = append("CRDCSchoolSATACTV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolSATACTV1
	#'
	#' This function modifies a CRDCSchoolSATACTV1
	#' @param fieldNames The field values to give the modified CRDCSchoolSATACTV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolSATACTV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolSATACTV1 <- function(CRDCSchoolSATACTV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSATACTV1", objectId = CRDCSchoolSATACTV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolSATACTV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAbsenteeismV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAbsenteeismV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAbsenteeismV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAbsenteeismV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAbsenteeismV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAbsenteeismV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAbsenteeismV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAbsenteeismV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAbsenteeismV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAbsenteeismV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAbsenteeismV1
	#' @param CRDCSchoolAbsenteeismV1ID The ID of the CRDCSchoolAbsenteeismV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAbsenteeismV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAbsenteeismV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAbsenteeismV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAbsenteeismV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAbsenteeismV1 <- function(CRDCSchoolAbsenteeismV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleSection504Count = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleSection504Count = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAbsenteeismV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAbsenteeismV1", objectId = CRDCSchoolAbsenteeismV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAbsenteeismV1
	#'
	#' This function deletes a CRDCSchoolAbsenteeismV1
	#' @param CRDCSchoolAbsenteeismV1ID The ID of the CRDCSchoolAbsenteeismV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAbsenteeismV1ID of the deleted CRDCSchoolAbsenteeismV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAbsenteeismV1 <- function(CRDCSchoolAbsenteeismV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAbsenteeismV1", objectId = CRDCSchoolAbsenteeismV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAbsenteeismV1
	#'
	#' This function creates a CRDCSchoolAbsenteeismV1
	#' @param fieldNames The field values to give the created CRDCSchoolAbsenteeismV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAbsenteeismV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAbsenteeismV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAbsenteeismV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAbsenteeismV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAbsenteeismV1
	#'
	#' This function modifies a CRDCSchoolAbsenteeismV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAbsenteeismV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAbsenteeismV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAbsenteeismV1 <- function(CRDCSchoolAbsenteeismV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleSection504Count = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleSection504Count = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAbsenteeismV1", objectId = CRDCSchoolAbsenteeismV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAbsenteeismV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolSingleSexAthleticV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolSingleSexAthleticV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSingleSexAthleticV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSingleSexAthleticV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSingleSexAthleticV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolSingleSexAthleticV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolSingleSexAthleticV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolSingleSexAthleticV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleOnlyParticipantCount = F, FemaleOnlySportsCount = F, FemaleOnlyTeamsCount = F, HasStudents = F, MaleOnlyParticipantCount = F, MaleOnlySportsCount = F, MaleOnlyTeamsCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexAthleticV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolSingleSexAthleticV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolSingleSexAthleticV1
	#' @param CRDCSchoolSingleSexAthleticV1ID The ID of the CRDCSchoolSingleSexAthleticV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolSingleSexAthleticV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolSingleSexAthleticV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolSingleSexAthleticV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolSingleSexAthleticV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolSingleSexAthleticV1 <- function(CRDCSchoolSingleSexAthleticV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleOnlyParticipantCount = F, FemaleOnlySportsCount = F, FemaleOnlyTeamsCount = F, HasStudents = F, MaleOnlyParticipantCount = F, MaleOnlySportsCount = F, MaleOnlyTeamsCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolSingleSexAthleticV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexAthleticV1", objectId = CRDCSchoolSingleSexAthleticV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolSingleSexAthleticV1
	#'
	#' This function deletes a CRDCSchoolSingleSexAthleticV1
	#' @param CRDCSchoolSingleSexAthleticV1ID The ID of the CRDCSchoolSingleSexAthleticV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolSingleSexAthleticV1ID of the deleted CRDCSchoolSingleSexAthleticV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolSingleSexAthleticV1 <- function(CRDCSchoolSingleSexAthleticV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexAthleticV1", objectId = CRDCSchoolSingleSexAthleticV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolSingleSexAthleticV1
	#'
	#' This function creates a CRDCSchoolSingleSexAthleticV1
	#' @param fieldNames The field values to give the created CRDCSchoolSingleSexAthleticV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolSingleSexAthleticV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolSingleSexAthleticV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleOnlyParticipantCount = NULL, FemaleOnlySportsCount = NULL, FemaleOnlyTeamsCount = NULL, HasStudents = NULL, MaleOnlyParticipantCount = NULL, MaleOnlySportsCount = NULL, MaleOnlyTeamsCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexAthleticV1", body = list(DataObject = body), searchFields = append("CRDCSchoolSingleSexAthleticV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolSingleSexAthleticV1
	#'
	#' This function modifies a CRDCSchoolSingleSexAthleticV1
	#' @param fieldNames The field values to give the modified CRDCSchoolSingleSexAthleticV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolSingleSexAthleticV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolSingleSexAthleticV1 <- function(CRDCSchoolSingleSexAthleticV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleOnlyParticipantCount = NULL, FemaleOnlySportsCount = NULL, FemaleOnlyTeamsCount = NULL, HasStudents = NULL, MaleOnlyParticipantCount = NULL, MaleOnlySportsCount = NULL, MaleOnlyTeamsCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolSingleSexAthleticV1", objectId = CRDCSchoolSingleSexAthleticV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolSingleSexAthleticV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalAlternativeSchoolDetails
	#'
	#' This function returns a dataframe or json object of FederalAlternativeSchoolDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalAlternativeSchoolDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalAlternativeSchoolDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalAlternativeSchoolDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalAlternativeSchoolDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalAlternativeSchoolDetails <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, FederalAlternativeSchoolDetailID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalAlternativeSchoolDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalAlternativeSchoolDetail
	#'
	#' This function returns a dataframe or json object of a FederalAlternativeSchoolDetail
	#' @param FederalAlternativeSchoolDetailID The ID of the FederalAlternativeSchoolDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalAlternativeSchoolDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalAlternativeSchoolDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalAlternativeSchoolDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalAlternativeSchoolDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalAlternativeSchoolDetail <- function(FederalAlternativeSchoolDetailID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalAlternativeSchoolDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalAlternativeSchoolDetail", objectId = FederalAlternativeSchoolDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalAlternativeSchoolDetail
	#'
	#' This function deletes a FederalAlternativeSchoolDetail
	#' @param FederalAlternativeSchoolDetailID The ID of the FederalAlternativeSchoolDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalAlternativeSchoolDetailID of the deleted FederalAlternativeSchoolDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalAlternativeSchoolDetail <- function(FederalAlternativeSchoolDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalAlternativeSchoolDetail", objectId = FederalAlternativeSchoolDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalAlternativeSchoolDetail
	#'
	#' This function creates a FederalAlternativeSchoolDetail
	#' @param fieldNames The field values to give the created FederalAlternativeSchoolDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalAlternativeSchoolDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalAlternativeSchoolDetail <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalAlternativeSchoolDetail", body = list(DataObject = body), searchFields = append("FederalAlternativeSchoolDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalAlternativeSchoolDetail
	#'
	#' This function modifies a FederalAlternativeSchoolDetail
	#' @param fieldNames The field values to give the modified FederalAlternativeSchoolDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalAlternativeSchoolDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalAlternativeSchoolDetail <- function(FederalAlternativeSchoolDetailID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalAlternativeSchoolDetail", objectId = FederalAlternativeSchoolDetailID, body = list(DataObject = body), searchFields = append("FederalAlternativeSchoolDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalJusticeFacilityTypes
	#'
	#' This function returns a dataframe or json object of FederalJusticeFacilityTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalJusticeFacilityTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalJusticeFacilityTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalJusticeFacilityType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalJusticeFacilityTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalJusticeFacilityTypes <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, FederalJusticeFacilityTypeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalJusticeFacilityType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalJusticeFacilityType
	#'
	#' This function returns a dataframe or json object of a FederalJusticeFacilityType
	#' @param FederalJusticeFacilityTypeID The ID of the FederalJusticeFacilityType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalJusticeFacilityType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalJusticeFacilityType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalJusticeFacilityType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalJusticeFacilityType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalJusticeFacilityType <- function(FederalJusticeFacilityTypeID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalJusticeFacilityTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalJusticeFacilityType", objectId = FederalJusticeFacilityTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalJusticeFacilityType
	#'
	#' This function deletes a FederalJusticeFacilityType
	#' @param FederalJusticeFacilityTypeID The ID of the FederalJusticeFacilityType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalJusticeFacilityTypeID of the deleted FederalJusticeFacilityType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalJusticeFacilityType <- function(FederalJusticeFacilityTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalJusticeFacilityType", objectId = FederalJusticeFacilityTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalJusticeFacilityType
	#'
	#' This function creates a FederalJusticeFacilityType
	#' @param fieldNames The field values to give the created FederalJusticeFacilityType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalJusticeFacilityType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalJusticeFacilityType <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalJusticeFacilityType", body = list(DataObject = body), searchFields = append("FederalJusticeFacilityTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalJusticeFacilityType
	#'
	#' This function modifies a FederalJusticeFacilityType
	#' @param fieldNames The field values to give the modified FederalJusticeFacilityType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalJusticeFacilityType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalJusticeFacilityType <- function(FederalJusticeFacilityTypeID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalJusticeFacilityType", objectId = FederalJusticeFacilityTypeID, body = list(DataObject = body), searchFields = append("FederalJusticeFacilityTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEAEarlyLearningV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEAEarlyLearningV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAEarlyLearningV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAEarlyLearningV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAEarlyLearningV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEAEarlyLearningV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEAEarlyLearningV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEAEarlyLearningV1ID = F, CreatedTime = F, DistrictID = F, HasEarlyChildhood = F, HasEarlyChildhoodNonIDEA = F, HasKindergarten = F, HasKindergartenFullDayCost = F, HasKindergartenFullDayFree = F, HasKindergartenPartDayCost = F, HasKindergartenPartDayFree = F, HasPreschool = F, HasPreschoolAllChildren = F, HasPreschoolFullDayCost = F, HasPreschoolFullDayFree = F, HasPreschoolIDEA = F, HasPreschoolLowIncome = F, HasPreschoolNonIDEAAge3 = F, HasPreschoolNonIDEAAge4 = F, HasPreschoolNonIDEAAge5 = F, HasPreschoolPartDayCost = F, HasPreschoolPartDayFree = F, HasPreschoolTitleI = F, ModifiedTime = F, PreschoolChildrenServedAge2 = F, PreschoolChildrenServedAge3 = F, PreschoolChildrenServedAge4 = F, PreschoolChildrenServedAge5 = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEAEarlyLearningV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEAEarlyLearningV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEAEarlyLearningV1
	#' @param CRDCLEAEarlyLearningV1ID The ID of the CRDCLEAEarlyLearningV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAEarlyLearningV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAEarlyLearningV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAEarlyLearningV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEAEarlyLearningV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEAEarlyLearningV1 <- function(CRDCLEAEarlyLearningV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, HasEarlyChildhood = F, HasEarlyChildhoodNonIDEA = F, HasKindergarten = F, HasKindergartenFullDayCost = F, HasKindergartenFullDayFree = F, HasKindergartenPartDayCost = F, HasKindergartenPartDayFree = F, HasPreschool = F, HasPreschoolAllChildren = F, HasPreschoolFullDayCost = F, HasPreschoolFullDayFree = F, HasPreschoolIDEA = F, HasPreschoolLowIncome = F, HasPreschoolNonIDEAAge3 = F, HasPreschoolNonIDEAAge4 = F, HasPreschoolNonIDEAAge5 = F, HasPreschoolPartDayCost = F, HasPreschoolPartDayFree = F, HasPreschoolTitleI = F, ModifiedTime = F, PreschoolChildrenServedAge2 = F, PreschoolChildrenServedAge3 = F, PreschoolChildrenServedAge4 = F, PreschoolChildrenServedAge5 = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEAEarlyLearningV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEarlyLearningV1", objectId = CRDCLEAEarlyLearningV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEAEarlyLearningV1
	#'
	#' This function deletes a CRDCLEAEarlyLearningV1
	#' @param CRDCLEAEarlyLearningV1ID The ID of the CRDCLEAEarlyLearningV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEAEarlyLearningV1ID of the deleted CRDCLEAEarlyLearningV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEAEarlyLearningV1 <- function(CRDCLEAEarlyLearningV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEarlyLearningV1", objectId = CRDCLEAEarlyLearningV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEAEarlyLearningV1
	#'
	#' This function creates a CRDCLEAEarlyLearningV1
	#' @param fieldNames The field values to give the created CRDCLEAEarlyLearningV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEAEarlyLearningV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEAEarlyLearningV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, HasEarlyChildhood = NULL, HasEarlyChildhoodNonIDEA = NULL, HasKindergarten = NULL, HasKindergartenFullDayCost = NULL, HasKindergartenFullDayFree = NULL, HasKindergartenPartDayCost = NULL, HasKindergartenPartDayFree = NULL, HasPreschool = NULL, HasPreschoolAllChildren = NULL, HasPreschoolFullDayCost = NULL, HasPreschoolFullDayFree = NULL, HasPreschoolIDEA = NULL, HasPreschoolLowIncome = NULL, HasPreschoolNonIDEAAge3 = NULL, HasPreschoolNonIDEAAge4 = NULL, HasPreschoolNonIDEAAge5 = NULL, HasPreschoolPartDayCost = NULL, HasPreschoolPartDayFree = NULL, HasPreschoolTitleI = NULL, PreschoolChildrenServedAge2 = NULL, PreschoolChildrenServedAge3 = NULL, PreschoolChildrenServedAge4 = NULL, PreschoolChildrenServedAge5 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEarlyLearningV1", body = list(DataObject = body), searchFields = append("CRDCLEAEarlyLearningV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEAEarlyLearningV1
	#'
	#' This function modifies a CRDCLEAEarlyLearningV1
	#' @param fieldNames The field values to give the modified CRDCLEAEarlyLearningV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEAEarlyLearningV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEAEarlyLearningV1 <- function(CRDCLEAEarlyLearningV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, HasEarlyChildhood = NULL, HasEarlyChildhoodNonIDEA = NULL, HasKindergarten = NULL, HasKindergartenFullDayCost = NULL, HasKindergartenFullDayFree = NULL, HasKindergartenPartDayCost = NULL, HasKindergartenPartDayFree = NULL, HasPreschool = NULL, HasPreschoolAllChildren = NULL, HasPreschoolFullDayCost = NULL, HasPreschoolFullDayFree = NULL, HasPreschoolIDEA = NULL, HasPreschoolLowIncome = NULL, HasPreschoolNonIDEAAge3 = NULL, HasPreschoolNonIDEAAge4 = NULL, HasPreschoolNonIDEAAge5 = NULL, HasPreschoolPartDayCost = NULL, HasPreschoolPartDayFree = NULL, HasPreschoolTitleI = NULL, PreschoolChildrenServedAge2 = NULL, PreschoolChildrenServedAge3 = NULL, PreschoolChildrenServedAge4 = NULL, PreschoolChildrenServedAge5 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEAEarlyLearningV1", objectId = CRDCLEAEarlyLearningV1ID, body = list(DataObject = body), searchFields = append("CRDCLEAEarlyLearningV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEADistanceEducationV1s
	#'
	#' This function returns a dataframe or json object of CRDCLEADistanceEducationV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEADistanceEducationV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEADistanceEducationV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEADistanceEducationV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEADistanceEducationV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEADistanceEducationV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCLEADistanceEducationV1ID = F, CreatedTime = F, DistrictID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasDistanceEducation = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEADistanceEducationV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEADistanceEducationV1
	#'
	#' This function returns a dataframe or json object of a CRDCLEADistanceEducationV1
	#' @param CRDCLEADistanceEducationV1ID The ID of the CRDCLEADistanceEducationV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEADistanceEducationV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEADistanceEducationV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEADistanceEducationV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEADistanceEducationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEADistanceEducationV1 <- function(CRDCLEADistanceEducationV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, DistrictID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasDistanceEducation = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEADistanceEducationV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEADistanceEducationV1", objectId = CRDCLEADistanceEducationV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEADistanceEducationV1
	#'
	#' This function deletes a CRDCLEADistanceEducationV1
	#' @param CRDCLEADistanceEducationV1ID The ID of the CRDCLEADistanceEducationV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEADistanceEducationV1ID of the deleted CRDCLEADistanceEducationV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEADistanceEducationV1 <- function(CRDCLEADistanceEducationV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEADistanceEducationV1", objectId = CRDCLEADistanceEducationV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEADistanceEducationV1
	#'
	#' This function creates a CRDCLEADistanceEducationV1
	#' @param fieldNames The field values to give the created CRDCLEADistanceEducationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEADistanceEducationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEADistanceEducationV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasDistanceEducation = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEADistanceEducationV1", body = list(DataObject = body), searchFields = append("CRDCLEADistanceEducationV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEADistanceEducationV1
	#'
	#' This function modifies a CRDCLEADistanceEducationV1
	#' @param fieldNames The field values to give the modified CRDCLEADistanceEducationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEADistanceEducationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEADistanceEducationV1 <- function(CRDCLEADistanceEducationV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, DistrictID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasDistanceEducation = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEADistanceEducationV1", objectId = CRDCLEADistanceEducationV1ID, body = list(DataObject = body), searchFields = append("CRDCLEADistanceEducationV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearExportPrintFiles
	#'
	#' This function returns a dataframe or json object of ACAYearExportPrintFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearExportPrintFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearExportPrintFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearExportPrintFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearExportPrintFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearExportPrintFiles <- function(searchConditionsList = NULL, ACAYearExportPrintFileID = F, ACAYearID = F, CreatedTime = F, MediaID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearExportPrintFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearExportPrintFile
	#'
	#' This function returns a dataframe or json object of an ACAYearExportPrintFile
	#' @param ACAYearExportPrintFileID The ID of the ACAYearExportPrintFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearExportPrintFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearExportPrintFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearExportPrintFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearExportPrintFile <- function(ACAYearExportPrintFileID, ACAYearID = F, CreatedTime = F, MediaID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearExportPrintFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearExportPrintFile", objectId = ACAYearExportPrintFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearExportPrintFile
	#'
	#' This function deletes an ACAYearExportPrintFile
	#' @param ACAYearExportPrintFileID The ID of the ACAYearExportPrintFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearExportPrintFileID of the deleted ACAYearExportPrintFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearExportPrintFile <- function(ACAYearExportPrintFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearExportPrintFile", objectId = ACAYearExportPrintFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearExportPrintFile
	#'
	#' This function creates an ACAYearExportPrintFile
	#' @param fieldNames The field values to give the created ACAYearExportPrintFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearExportPrintFile <- function(ACAYearID = NULL, MediaID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearExportPrintFile", body = list(DataObject = body), searchFields = append("ACAYearExportPrintFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearExportPrintFile
	#'
	#' This function modifies an ACAYearExportPrintFile
	#' @param fieldNames The field values to give the modified ACAYearExportPrintFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearExportPrintFile <- function(ACAYearExportPrintFileID, ACAYearID = NULL, MediaID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearExportPrintFile", objectId = ACAYearExportPrintFileID, body = list(DataObject = body), searchFields = append("ACAYearExportPrintFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MYearExceptions
	#'
	#' This function returns a dataframe or json object of Form1099MYearExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MYearExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MYearExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, ExceptionType = F, Form1099MYearExceptionID = F, Form1099MYearID = F, IsFatalException = F, ModifiedTime = F, PreserveException = F, SourceType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VendorID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MYearException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MYearException
	#'
	#' This function returns a dataframe or json object of a Form1099MYearException
	#' @param Form1099MYearExceptionID The ID of the Form1099MYearException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MYearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MYearException <- function(Form1099MYearExceptionID, CreatedTime = F, Description = F, ExceptionType = F, Form1099MYearID = F, IsFatalException = F, ModifiedTime = F, PreserveException = F, SourceType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VendorID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MYearExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearException", objectId = Form1099MYearExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MYearException
	#'
	#' This function deletes a Form1099MYearException
	#' @param Form1099MYearExceptionID The ID of the Form1099MYearException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MYearExceptionID of the deleted Form1099MYearException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MYearException <- function(Form1099MYearExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearException", objectId = Form1099MYearExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MYearException
	#'
	#' This function creates a Form1099MYearException
	#' @param fieldNames The field values to give the created Form1099MYearException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MYearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MYearException <- function(Description = NULL, ExceptionType = NULL, Form1099MYearID = NULL, IsFatalException = NULL, PreserveException = NULL, SourceType = NULL, VendorID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearException", body = list(DataObject = body), searchFields = append("Form1099MYearExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MYearException
	#'
	#' This function modifies a Form1099MYearException
	#' @param fieldNames The field values to give the modified Form1099MYearException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MYearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MYearException <- function(Form1099MYearExceptionID, Description = NULL, ExceptionType = NULL, Form1099MYearID = NULL, IsFatalException = NULL, PreserveException = NULL, SourceType = NULL, VendorID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MYearException", objectId = Form1099MYearExceptionID, body = list(DataObject = body), searchFields = append("Form1099MYearExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MExportPrintFiles
	#'
	#' This function returns a dataframe or json object of Form1099MExportPrintFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MExportPrintFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MExportPrintFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MExportPrintFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MExportPrintFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MExportPrintFiles <- function(searchConditionsList = NULL, CreatedTime = F, Form1099MExportPrintFileID = F, Form1099MYearID = F, MediaID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MExportPrintFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MExportPrintFile
	#'
	#' This function returns a dataframe or json object of a Form1099MExportPrintFile
	#' @param Form1099MExportPrintFileID The ID of the Form1099MExportPrintFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MExportPrintFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MExportPrintFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MExportPrintFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MExportPrintFile <- function(Form1099MExportPrintFileID, CreatedTime = F, Form1099MYearID = F, MediaID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MExportPrintFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportPrintFile", objectId = Form1099MExportPrintFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MExportPrintFile
	#'
	#' This function deletes a Form1099MExportPrintFile
	#' @param Form1099MExportPrintFileID The ID of the Form1099MExportPrintFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MExportPrintFileID of the deleted Form1099MExportPrintFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MExportPrintFile <- function(Form1099MExportPrintFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportPrintFile", objectId = Form1099MExportPrintFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MExportPrintFile
	#'
	#' This function creates a Form1099MExportPrintFile
	#' @param fieldNames The field values to give the created Form1099MExportPrintFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MExportPrintFile <- function(Form1099MYearID = NULL, MediaID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportPrintFile", body = list(DataObject = body), searchFields = append("Form1099MExportPrintFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MExportPrintFile
	#'
	#' This function modifies a Form1099MExportPrintFile
	#' @param fieldNames The field values to give the modified Form1099MExportPrintFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MExportPrintFile <- function(Form1099MExportPrintFileID, Form1099MYearID = NULL, MediaID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MExportPrintFile", objectId = Form1099MExportPrintFileID, body = list(DataObject = body), searchFields = append("Form1099MExportPrintFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearALEMemberMonthV1s
	#'
	#' This function returns a dataframe or json object of ACAYearALEMemberMonthV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearALEMemberMonthV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearALEMemberMonthV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearALEMemberMonthV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearALEMemberMonthV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearALEMemberMonthV1s <- function(searchConditionsList = NULL, ACAYearALEMemberMonthV1ID = F, ACAYearID = F, CreatedTime = F, FullTimeEmployeeCount = F, IsAggregatedGroup = F, ModifiedTime = F, Month = F, OffersMinimumEssentialCoverage = F, Section4980HTransitionRelief = F, SkywardID = F, TotalEmployeeCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearALEMemberMonthV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearALEMemberMonthV1
	#'
	#' This function returns a dataframe or json object of an ACAYearALEMemberMonthV1
	#' @param ACAYearALEMemberMonthV1ID The ID of the ACAYearALEMemberMonthV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearALEMemberMonthV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearALEMemberMonthV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearALEMemberMonthV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearALEMemberMonthV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearALEMemberMonthV1 <- function(ACAYearALEMemberMonthV1ID, ACAYearID = F, CreatedTime = F, FullTimeEmployeeCount = F, IsAggregatedGroup = F, ModifiedTime = F, Month = F, OffersMinimumEssentialCoverage = F, Section4980HTransitionRelief = F, SkywardID = F, TotalEmployeeCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearALEMemberMonthV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearALEMemberMonthV1", objectId = ACAYearALEMemberMonthV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearALEMemberMonthV1
	#'
	#' This function deletes an ACAYearALEMemberMonthV1
	#' @param ACAYearALEMemberMonthV1ID The ID of the ACAYearALEMemberMonthV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearALEMemberMonthV1ID of the deleted ACAYearALEMemberMonthV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearALEMemberMonthV1 <- function(ACAYearALEMemberMonthV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearALEMemberMonthV1", objectId = ACAYearALEMemberMonthV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearALEMemberMonthV1
	#'
	#' This function creates an ACAYearALEMemberMonthV1
	#' @param fieldNames The field values to give the created ACAYearALEMemberMonthV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearALEMemberMonthV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearALEMemberMonthV1 <- function(ACAYearID = NULL, FullTimeEmployeeCount = NULL, IsAggregatedGroup = NULL, Month = NULL, OffersMinimumEssentialCoverage = NULL, Section4980HTransitionRelief = NULL, TotalEmployeeCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearALEMemberMonthV1", body = list(DataObject = body), searchFields = append("ACAYearALEMemberMonthV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearALEMemberMonthV1
	#'
	#' This function modifies an ACAYearALEMemberMonthV1
	#' @param fieldNames The field values to give the modified ACAYearALEMemberMonthV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearALEMemberMonthV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearALEMemberMonthV1 <- function(ACAYearALEMemberMonthV1ID, ACAYearID = NULL, FullTimeEmployeeCount = NULL, IsAggregatedGroup = NULL, Month = NULL, OffersMinimumEssentialCoverage = NULL, Section4980HTransitionRelief = NULL, TotalEmployeeCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearALEMemberMonthV1", objectId = ACAYearALEMemberMonthV1ID, body = list(DataObject = body), searchFields = append("ACAYearALEMemberMonthV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearOtherALEMemberOfAggregatedGroupV1s
	#'
	#' This function returns a dataframe or json object of ACAYearOtherALEMemberOfAggregatedGroupV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearOtherALEMemberOfAggregatedGroupV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearOtherALEMemberOfAggregatedGroupV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearOtherALEMemberOfAggregatedGroupV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearOtherALEMemberOfAggregatedGroupV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearOtherALEMemberOfAggregatedGroupV1s <- function(searchConditionsList = NULL, ACAYearID = F, ACAYearOtherALEMemberOfAggregatedGroupV1ID = F, CreatedTime = F, EmployerBusinessNameLine1ElectronicFile = F, EmployerBusinessNameLine2ElectronicFile = F, EmployerBusinessNamePrinted = F, EmployerID = F, EmployerIdentificationNumber = F, ModifiedTime = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearOtherALEMemberOfAggregatedGroupV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearOtherALEMemberOfAggregatedGroupV1
	#'
	#' This function returns a dataframe or json object of an ACAYearOtherALEMemberOfAggregatedGroupV1
	#' @param ACAYearOtherALEMemberOfAggregatedGroupV1ID The ID of the ACAYearOtherALEMemberOfAggregatedGroupV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearOtherALEMemberOfAggregatedGroupV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearOtherALEMemberOfAggregatedGroupV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearOtherALEMemberOfAggregatedGroupV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearOtherALEMemberOfAggregatedGroupV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearOtherALEMemberOfAggregatedGroupV1 <- function(ACAYearOtherALEMemberOfAggregatedGroupV1ID, ACAYearID = F, CreatedTime = F, EmployerBusinessNameLine1ElectronicFile = F, EmployerBusinessNameLine2ElectronicFile = F, EmployerBusinessNamePrinted = F, EmployerID = F, EmployerIdentificationNumber = F, ModifiedTime = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearOtherALEMemberOfAggregatedGroupV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearOtherALEMemberOfAggregatedGroupV1", objectId = ACAYearOtherALEMemberOfAggregatedGroupV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearOtherALEMemberOfAggregatedGroupV1
	#'
	#' This function deletes an ACAYearOtherALEMemberOfAggregatedGroupV1
	#' @param ACAYearOtherALEMemberOfAggregatedGroupV1ID The ID of the ACAYearOtherALEMemberOfAggregatedGroupV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearOtherALEMemberOfAggregatedGroupV1ID of the deleted ACAYearOtherALEMemberOfAggregatedGroupV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearOtherALEMemberOfAggregatedGroupV1 <- function(ACAYearOtherALEMemberOfAggregatedGroupV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearOtherALEMemberOfAggregatedGroupV1", objectId = ACAYearOtherALEMemberOfAggregatedGroupV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearOtherALEMemberOfAggregatedGroupV1
	#'
	#' This function creates an ACAYearOtherALEMemberOfAggregatedGroupV1
	#' @param fieldNames The field values to give the created ACAYearOtherALEMemberOfAggregatedGroupV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearOtherALEMemberOfAggregatedGroupV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearOtherALEMemberOfAggregatedGroupV1 <- function(ACAYearID = NULL, EmployerBusinessNameLine1ElectronicFile = NULL, EmployerBusinessNameLine2ElectronicFile = NULL, EmployerBusinessNamePrinted = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearOtherALEMemberOfAggregatedGroupV1", body = list(DataObject = body), searchFields = append("ACAYearOtherALEMemberOfAggregatedGroupV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearOtherALEMemberOfAggregatedGroupV1
	#'
	#' This function modifies an ACAYearOtherALEMemberOfAggregatedGroupV1
	#' @param fieldNames The field values to give the modified ACAYearOtherALEMemberOfAggregatedGroupV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearOtherALEMemberOfAggregatedGroupV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearOtherALEMemberOfAggregatedGroupV1 <- function(ACAYearOtherALEMemberOfAggregatedGroupV1ID, ACAYearID = NULL, EmployerBusinessNameLine1ElectronicFile = NULL, EmployerBusinessNameLine2ElectronicFile = NULL, EmployerBusinessNamePrinted = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearOtherALEMemberOfAggregatedGroupV1", objectId = ACAYearOtherALEMemberOfAggregatedGroupV1ID, body = list(DataObject = body), searchFields = append("ACAYearOtherALEMemberOfAggregatedGroupV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateCalendarYears
	#'
	#' This function returns a dataframe or json object of StateCalendarYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateCalendarYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateCalendarYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateCalendarYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of StateCalendarYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateCalendarYears <- function(searchConditionsList = NULL, CalendarYear = F, CombinedStateFederalFilingCode = F, CreatedTime = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateCalendarYearID = F, StateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "StateCalendarYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateCalendarYear
	#'
	#' This function returns a dataframe or json object of a StateCalendarYear
	#' @param StateCalendarYearID The ID of the StateCalendarYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateCalendarYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateCalendarYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateCalendarYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of StateCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateCalendarYear <- function(StateCalendarYearID, CalendarYear = F, CombinedStateFederalFilingCode = F, CreatedTime = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, StateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateCalendarYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "StateCalendarYear", objectId = StateCalendarYearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateCalendarYear
	#'
	#' This function deletes a StateCalendarYear
	#' @param StateCalendarYearID The ID of the StateCalendarYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The StateCalendarYearID of the deleted StateCalendarYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateCalendarYear <- function(StateCalendarYearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "StateCalendarYear", objectId = StateCalendarYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateCalendarYear
	#'
	#' This function creates a StateCalendarYear
	#' @param fieldNames The field values to give the created StateCalendarYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created StateCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateCalendarYear <- function(CalendarYear = NULL, CombinedStateFederalFilingCode = NULL, StateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "StateCalendarYear", body = list(DataObject = body), searchFields = append("StateCalendarYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateCalendarYear
	#'
	#' This function modifies a StateCalendarYear
	#' @param fieldNames The field values to give the modified StateCalendarYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified StateCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateCalendarYear <- function(StateCalendarYearID, CalendarYear = NULL, CombinedStateFederalFilingCode = NULL, StateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "StateCalendarYear", objectId = StateCalendarYearID, body = list(DataObject = body), searchFields = append("StateCalendarYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2ExportPrintFiles
	#'
	#' This function returns a dataframe or json object of W2ExportPrintFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2ExportPrintFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2ExportPrintFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2ExportPrintFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2ExportPrintFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2ExportPrintFiles <- function(searchConditionsList = NULL, CreatedTime = F, MediaID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2ExportPrintFileID = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2ExportPrintFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2ExportPrintFile
	#'
	#' This function returns a dataframe or json object of a W2ExportPrintFile
	#' @param W2ExportPrintFileID The ID of the W2ExportPrintFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2ExportPrintFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2ExportPrintFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2ExportPrintFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2ExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2ExportPrintFile <- function(W2ExportPrintFileID, CreatedTime = F, MediaID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2ExportPrintFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2ExportPrintFile", objectId = W2ExportPrintFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2ExportPrintFile
	#'
	#' This function deletes a W2ExportPrintFile
	#' @param W2ExportPrintFileID The ID of the W2ExportPrintFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2ExportPrintFileID of the deleted W2ExportPrintFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2ExportPrintFile <- function(W2ExportPrintFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2ExportPrintFile", objectId = W2ExportPrintFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2ExportPrintFile
	#'
	#' This function creates a W2ExportPrintFile
	#' @param fieldNames The field values to give the created W2ExportPrintFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2ExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2ExportPrintFile <- function(MediaID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2ExportPrintFile", body = list(DataObject = body), searchFields = append("W2ExportPrintFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2ExportPrintFile
	#'
	#' This function modifies a W2ExportPrintFile
	#' @param fieldNames The field values to give the modified W2ExportPrintFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2ExportPrintFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2ExportPrintFile <- function(W2ExportPrintFileID, MediaID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2ExportPrintFile", objectId = W2ExportPrintFileID, body = list(DataObject = body), searchFields = append("W2ExportPrintFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MExportElectronicFiles
	#'
	#' This function returns a dataframe or json object of Form1099MExportElectronicFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MExportElectronicFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MExportElectronicFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MExportElectronicFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MExportElectronicFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MExportElectronicFiles <- function(searchConditionsList = NULL, CreatedTime = F, Form1099MExportElectronicFileID = F, Form1099MYearID = F, IsCombinedStateFederal = F, IsTestFile = F, ModifiedTime = F, TransmitterControlCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MExportElectronicFile
	#'
	#' This function returns a dataframe or json object of a Form1099MExportElectronicFile
	#' @param Form1099MExportElectronicFileID The ID of the Form1099MExportElectronicFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MExportElectronicFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MExportElectronicFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MExportElectronicFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MExportElectronicFile <- function(Form1099MExportElectronicFileID, CreatedTime = F, Form1099MYearID = F, IsCombinedStateFederal = F, IsTestFile = F, ModifiedTime = F, TransmitterControlCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MExportElectronicFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFile", objectId = Form1099MExportElectronicFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MExportElectronicFile
	#'
	#' This function deletes a Form1099MExportElectronicFile
	#' @param Form1099MExportElectronicFileID The ID of the Form1099MExportElectronicFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MExportElectronicFileID of the deleted Form1099MExportElectronicFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MExportElectronicFile <- function(Form1099MExportElectronicFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFile", objectId = Form1099MExportElectronicFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MExportElectronicFile
	#'
	#' This function creates a Form1099MExportElectronicFile
	#' @param fieldNames The field values to give the created Form1099MExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MExportElectronicFile <- function(Form1099MYearID = NULL, IsCombinedStateFederal = NULL, IsTestFile = NULL, TransmitterControlCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFile", body = list(DataObject = body), searchFields = append("Form1099MExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MExportElectronicFile
	#'
	#' This function modifies a Form1099MExportElectronicFile
	#' @param fieldNames The field values to give the modified Form1099MExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MExportElectronicFile <- function(Form1099MExportElectronicFileID, Form1099MYearID = NULL, IsCombinedStateFederal = NULL, IsTestFile = NULL, TransmitterControlCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFile", objectId = Form1099MExportElectronicFileID, body = list(DataObject = body), searchFields = append("Form1099MExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYears
	#'
	#' This function returns a dataframe or json object of ACAYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYears <- function(searchConditionsList = NULL, ACAYearID = F, CalendarYear = F, CreatedTime = F, DesignatedGovernmentEntityAddressLine1 = F, DesignatedGovernmentEntityAddressLine2 = F, DesignatedGovernmentEntityBusinessNameLine1 = F, DesignatedGovernmentEntityBusinessNameLine2 = F, DesignatedGovernmentEntityCity = F, DesignatedGovernmentEntityContactPersonFirstName = F, DesignatedGovernmentEntityContactPersonLastName = F, DesignatedGovernmentEntityContactPersonMiddleName = F, DesignatedGovernmentEntityContactPersonSuffix = F, DesignatedGovernmentEntityContactPhoneNumber = F, DesignatedGovernmentEntityEmployerIdentificationNumber = F, DesignatedGovernmentEntityStateCode = F, DesignatedGovernmentEntityStateID = F, DesignatedGovernmentEntityZipCode = F, DesignatedGovernmentEntityZipCodeExtension = F, DistrictID = F, EmployerAddressLine1Printed = F, EmployerAddressLine2Printed = F, EmployerBusinessNamePrinted = F, EmployerCityPrinted = F, EmployerContactPersonFirstName = F, EmployerContactPersonLastName = F, EmployerContactPersonMiddleName = F, EmployerContactPersonSuffix = F, EmployerContactTelephoneNumber = F, EmployerID = F, EmployerIdentificationNumber = F, EmployerStateCodePrinted = F, EmployerStateIDPrinted = F, EmployerZipCodeExtensionPrinted = F, EmployerZipCodePrinted = F, FullTimeEmployeeCount = F, Is98PercentOfferMethod = F, IsAggregatedGroup = F, IsALEMemberPartOfAggregatedALEGroup = F, IsAvailableInEmployeeAccess = F, IsQualifyingOfferMethod = F, IsQualifyingOfferMethodTransitionRelief = F, IsSection4980HTransitionRelief = F, IsTransmittalForThisALEMember = F, ModifiedTime = F, OffersMinimumEssentialCoverage = F, Section4980HTransitionReliefCode = F, SkywardID = F, TestScenarioIdentification = F, TotalEmployeeCount = F, TotalForm1095CFiledByALEMember = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYear
	#'
	#' This function returns a dataframe or json object of an ACAYear
	#' @param ACAYearID The ID of the ACAYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYear <- function(ACAYearID, CalendarYear = F, CreatedTime = F, DesignatedGovernmentEntityAddressLine1 = F, DesignatedGovernmentEntityAddressLine2 = F, DesignatedGovernmentEntityBusinessNameLine1 = F, DesignatedGovernmentEntityBusinessNameLine2 = F, DesignatedGovernmentEntityCity = F, DesignatedGovernmentEntityContactPersonFirstName = F, DesignatedGovernmentEntityContactPersonLastName = F, DesignatedGovernmentEntityContactPersonMiddleName = F, DesignatedGovernmentEntityContactPersonSuffix = F, DesignatedGovernmentEntityContactPhoneNumber = F, DesignatedGovernmentEntityEmployerIdentificationNumber = F, DesignatedGovernmentEntityStateCode = F, DesignatedGovernmentEntityStateID = F, DesignatedGovernmentEntityZipCode = F, DesignatedGovernmentEntityZipCodeExtension = F, DistrictID = F, EmployerAddressLine1Printed = F, EmployerAddressLine2Printed = F, EmployerBusinessNamePrinted = F, EmployerCityPrinted = F, EmployerContactPersonFirstName = F, EmployerContactPersonLastName = F, EmployerContactPersonMiddleName = F, EmployerContactPersonSuffix = F, EmployerContactTelephoneNumber = F, EmployerID = F, EmployerIdentificationNumber = F, EmployerStateCodePrinted = F, EmployerStateIDPrinted = F, EmployerZipCodeExtensionPrinted = F, EmployerZipCodePrinted = F, FullTimeEmployeeCount = F, Is98PercentOfferMethod = F, IsAggregatedGroup = F, IsALEMemberPartOfAggregatedALEGroup = F, IsAvailableInEmployeeAccess = F, IsQualifyingOfferMethod = F, IsQualifyingOfferMethodTransitionRelief = F, IsSection4980HTransitionRelief = F, IsTransmittalForThisALEMember = F, ModifiedTime = F, OffersMinimumEssentialCoverage = F, Section4980HTransitionReliefCode = F, SkywardID = F, TestScenarioIdentification = F, TotalEmployeeCount = F, TotalForm1095CFiledByALEMember = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYear", objectId = ACAYearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYear
	#'
	#' This function deletes an ACAYear
	#' @param ACAYearID The ID of the ACAYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearID of the deleted ACAYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYear <- function(ACAYearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYear", objectId = ACAYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYear
	#'
	#' This function creates an ACAYear
	#' @param fieldNames The field values to give the created ACAYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYear <- function(CalendarYear = NULL, DesignatedGovernmentEntityAddressLine1 = NULL, DesignatedGovernmentEntityAddressLine2 = NULL, DesignatedGovernmentEntityBusinessNameLine1 = NULL, DesignatedGovernmentEntityBusinessNameLine2 = NULL, DesignatedGovernmentEntityCity = NULL, DesignatedGovernmentEntityContactPersonFirstName = NULL, DesignatedGovernmentEntityContactPersonLastName = NULL, DesignatedGovernmentEntityContactPersonMiddleName = NULL, DesignatedGovernmentEntityContactPersonSuffix = NULL, DesignatedGovernmentEntityContactPhoneNumber = NULL, DesignatedGovernmentEntityEmployerIdentificationNumber = NULL, DesignatedGovernmentEntityStateCode = NULL, DesignatedGovernmentEntityStateID = NULL, DesignatedGovernmentEntityZipCode = NULL, DesignatedGovernmentEntityZipCodeExtension = NULL, DistrictID = NULL, EmployerAddressLine1Printed = NULL, EmployerAddressLine2Printed = NULL, EmployerBusinessNamePrinted = NULL, EmployerCityPrinted = NULL, EmployerContactPersonFirstName = NULL, EmployerContactPersonLastName = NULL, EmployerContactPersonMiddleName = NULL, EmployerContactPersonSuffix = NULL, EmployerContactTelephoneNumber = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, EmployerStateCodePrinted = NULL, EmployerStateIDPrinted = NULL, EmployerZipCodeExtensionPrinted = NULL, EmployerZipCodePrinted = NULL, Is98PercentOfferMethod = NULL, IsALEMemberPartOfAggregatedALEGroup = NULL, IsAvailableInEmployeeAccess = NULL, IsQualifyingOfferMethod = NULL, IsQualifyingOfferMethodTransitionRelief = NULL, IsSection4980HTransitionRelief = NULL, IsTransmittalForThisALEMember = NULL, TestScenarioIdentification = NULL, TotalForm1095CFiledByALEMember = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYear", body = list(DataObject = body), searchFields = append("ACAYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYear
	#'
	#' This function modifies an ACAYear
	#' @param fieldNames The field values to give the modified ACAYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYear <- function(ACAYearID, CalendarYear = NULL, DesignatedGovernmentEntityAddressLine1 = NULL, DesignatedGovernmentEntityAddressLine2 = NULL, DesignatedGovernmentEntityBusinessNameLine1 = NULL, DesignatedGovernmentEntityBusinessNameLine2 = NULL, DesignatedGovernmentEntityCity = NULL, DesignatedGovernmentEntityContactPersonFirstName = NULL, DesignatedGovernmentEntityContactPersonLastName = NULL, DesignatedGovernmentEntityContactPersonMiddleName = NULL, DesignatedGovernmentEntityContactPersonSuffix = NULL, DesignatedGovernmentEntityContactPhoneNumber = NULL, DesignatedGovernmentEntityEmployerIdentificationNumber = NULL, DesignatedGovernmentEntityStateCode = NULL, DesignatedGovernmentEntityStateID = NULL, DesignatedGovernmentEntityZipCode = NULL, DesignatedGovernmentEntityZipCodeExtension = NULL, DistrictID = NULL, EmployerAddressLine1Printed = NULL, EmployerAddressLine2Printed = NULL, EmployerBusinessNamePrinted = NULL, EmployerCityPrinted = NULL, EmployerContactPersonFirstName = NULL, EmployerContactPersonLastName = NULL, EmployerContactPersonMiddleName = NULL, EmployerContactPersonSuffix = NULL, EmployerContactTelephoneNumber = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, EmployerStateCodePrinted = NULL, EmployerStateIDPrinted = NULL, EmployerZipCodeExtensionPrinted = NULL, EmployerZipCodePrinted = NULL, Is98PercentOfferMethod = NULL, IsALEMemberPartOfAggregatedALEGroup = NULL, IsAvailableInEmployeeAccess = NULL, IsQualifyingOfferMethod = NULL, IsQualifyingOfferMethodTransitionRelief = NULL, IsSection4980HTransitionRelief = NULL, IsTransmittalForThisALEMember = NULL, TestScenarioIdentification = NULL, TotalForm1095CFiledByALEMember = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYear", objectId = ACAYearID, body = list(DataObject = body), searchFields = append("ACAYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearEmployeeCoveredIndividualV1s
	#'
	#' This function returns a dataframe or json object of ACAYearEmployeeCoveredIndividualV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearEmployeeCoveredIndividualV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearEmployeeCoveredIndividualV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearEmployeeCoveredIndividualV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearEmployeeCoveredIndividualV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearEmployeeCoveredIndividualV1s <- function(searchConditionsList = NULL, ACAYearEmployeeCoveredIndividualV1ID = F, ACAYearEmployeeV1ID = F, BirthDate = F, CoveredIndividualFirstNameElectronicFile = F, CoveredIndividualLastNameElectronicFile = F, CoveredIndividualMiddleNameElectronicFile = F, CoveredIndividualNamePrinted = F, CoveredIndividualSuffixElectronicFile = F, CreatedTime = F, IsCoveredApril = F, IsCoveredAugust = F, IsCoveredDecember = F, IsCoveredFebruary = F, IsCoveredJanuary = F, IsCoveredJuly = F, IsCoveredJune = F, IsCoveredMarch = F, IsCoveredMay = F, IsCoveredNovember = F, IsCoveredOctober = F, IsCoveredSeptember = F, ModifiedTime = F, NameID = F, SkywardID = F, SocialSecurityNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearEmployeeCoveredIndividualV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearEmployeeCoveredIndividualV1
	#'
	#' This function returns a dataframe or json object of an ACAYearEmployeeCoveredIndividualV1
	#' @param ACAYearEmployeeCoveredIndividualV1ID The ID of the ACAYearEmployeeCoveredIndividualV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearEmployeeCoveredIndividualV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearEmployeeCoveredIndividualV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearEmployeeCoveredIndividualV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearEmployeeCoveredIndividualV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearEmployeeCoveredIndividualV1 <- function(ACAYearEmployeeCoveredIndividualV1ID, ACAYearEmployeeV1ID = F, BirthDate = F, CoveredIndividualFirstNameElectronicFile = F, CoveredIndividualLastNameElectronicFile = F, CoveredIndividualMiddleNameElectronicFile = F, CoveredIndividualNamePrinted = F, CoveredIndividualSuffixElectronicFile = F, CreatedTime = F, IsCoveredApril = F, IsCoveredAugust = F, IsCoveredDecember = F, IsCoveredFebruary = F, IsCoveredJanuary = F, IsCoveredJuly = F, IsCoveredJune = F, IsCoveredMarch = F, IsCoveredMay = F, IsCoveredNovember = F, IsCoveredOctober = F, IsCoveredSeptember = F, ModifiedTime = F, NameID = F, SkywardID = F, SocialSecurityNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearEmployeeCoveredIndividualV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeCoveredIndividualV1", objectId = ACAYearEmployeeCoveredIndividualV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearEmployeeCoveredIndividualV1
	#'
	#' This function deletes an ACAYearEmployeeCoveredIndividualV1
	#' @param ACAYearEmployeeCoveredIndividualV1ID The ID of the ACAYearEmployeeCoveredIndividualV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearEmployeeCoveredIndividualV1ID of the deleted ACAYearEmployeeCoveredIndividualV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearEmployeeCoveredIndividualV1 <- function(ACAYearEmployeeCoveredIndividualV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeCoveredIndividualV1", objectId = ACAYearEmployeeCoveredIndividualV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearEmployeeCoveredIndividualV1
	#'
	#' This function creates an ACAYearEmployeeCoveredIndividualV1
	#' @param fieldNames The field values to give the created ACAYearEmployeeCoveredIndividualV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearEmployeeCoveredIndividualV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearEmployeeCoveredIndividualV1 <- function(ACAYearEmployeeV1ID = NULL, BirthDate = NULL, CoveredIndividualFirstNameElectronicFile = NULL, CoveredIndividualLastNameElectronicFile = NULL, CoveredIndividualMiddleNameElectronicFile = NULL, CoveredIndividualNamePrinted = NULL, CoveredIndividualSuffixElectronicFile = NULL, IsCoveredApril = NULL, IsCoveredAugust = NULL, IsCoveredDecember = NULL, IsCoveredFebruary = NULL, IsCoveredJanuary = NULL, IsCoveredJuly = NULL, IsCoveredJune = NULL, IsCoveredMarch = NULL, IsCoveredMay = NULL, IsCoveredNovember = NULL, IsCoveredOctober = NULL, IsCoveredSeptember = NULL, NameID = NULL, SocialSecurityNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeCoveredIndividualV1", body = list(DataObject = body), searchFields = append("ACAYearEmployeeCoveredIndividualV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearEmployeeCoveredIndividualV1
	#'
	#' This function modifies an ACAYearEmployeeCoveredIndividualV1
	#' @param fieldNames The field values to give the modified ACAYearEmployeeCoveredIndividualV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearEmployeeCoveredIndividualV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearEmployeeCoveredIndividualV1 <- function(ACAYearEmployeeCoveredIndividualV1ID, ACAYearEmployeeV1ID = NULL, BirthDate = NULL, CoveredIndividualFirstNameElectronicFile = NULL, CoveredIndividualLastNameElectronicFile = NULL, CoveredIndividualMiddleNameElectronicFile = NULL, CoveredIndividualNamePrinted = NULL, CoveredIndividualSuffixElectronicFile = NULL, IsCoveredApril = NULL, IsCoveredAugust = NULL, IsCoveredDecember = NULL, IsCoveredFebruary = NULL, IsCoveredJanuary = NULL, IsCoveredJuly = NULL, IsCoveredJune = NULL, IsCoveredMarch = NULL, IsCoveredMay = NULL, IsCoveredNovember = NULL, IsCoveredOctober = NULL, IsCoveredSeptember = NULL, NameID = NULL, SocialSecurityNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeCoveredIndividualV1", objectId = ACAYearEmployeeCoveredIndividualV1ID, body = list(DataObject = body), searchFields = append("ACAYearEmployeeCoveredIndividualV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearEmployeeOfferAndCoverageV1s
	#'
	#' This function returns a dataframe or json object of ACAYearEmployeeOfferAndCoverageV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearEmployeeOfferAndCoverageV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearEmployeeOfferAndCoverageV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearEmployeeOfferAndCoverageV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearEmployeeOfferAndCoverageV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearEmployeeOfferAndCoverageV1s <- function(searchConditionsList = NULL, ACAYearEmployeeOfferAndCoverageV1ID = F, ACAYearEmployeeV1ID = F, CreatedTime = F, EmployeeShareOfLowestCostMonthlyPremium = F, FederalACAOfferAndCoverageCode = F, FederalACAOfferAndCoverageID = F, FederalACASafeHarborCode = F, FederalACASafeHarborID = F, ModifiedTime = F, Month = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearEmployeeOfferAndCoverageV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearEmployeeOfferAndCoverageV1
	#'
	#' This function returns a dataframe or json object of an ACAYearEmployeeOfferAndCoverageV1
	#' @param ACAYearEmployeeOfferAndCoverageV1ID The ID of the ACAYearEmployeeOfferAndCoverageV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearEmployeeOfferAndCoverageV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearEmployeeOfferAndCoverageV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearEmployeeOfferAndCoverageV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearEmployeeOfferAndCoverageV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearEmployeeOfferAndCoverageV1 <- function(ACAYearEmployeeOfferAndCoverageV1ID, ACAYearEmployeeV1ID = F, CreatedTime = F, EmployeeShareOfLowestCostMonthlyPremium = F, FederalACAOfferAndCoverageCode = F, FederalACAOfferAndCoverageID = F, FederalACASafeHarborCode = F, FederalACASafeHarborID = F, ModifiedTime = F, Month = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearEmployeeOfferAndCoverageV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeOfferAndCoverageV1", objectId = ACAYearEmployeeOfferAndCoverageV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearEmployeeOfferAndCoverageV1
	#'
	#' This function deletes an ACAYearEmployeeOfferAndCoverageV1
	#' @param ACAYearEmployeeOfferAndCoverageV1ID The ID of the ACAYearEmployeeOfferAndCoverageV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearEmployeeOfferAndCoverageV1ID of the deleted ACAYearEmployeeOfferAndCoverageV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearEmployeeOfferAndCoverageV1 <- function(ACAYearEmployeeOfferAndCoverageV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeOfferAndCoverageV1", objectId = ACAYearEmployeeOfferAndCoverageV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearEmployeeOfferAndCoverageV1
	#'
	#' This function creates an ACAYearEmployeeOfferAndCoverageV1
	#' @param fieldNames The field values to give the created ACAYearEmployeeOfferAndCoverageV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearEmployeeOfferAndCoverageV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearEmployeeOfferAndCoverageV1 <- function(ACAYearEmployeeV1ID = NULL, EmployeeShareOfLowestCostMonthlyPremium = NULL, FederalACAOfferAndCoverageCode = NULL, FederalACAOfferAndCoverageID = NULL, FederalACASafeHarborCode = NULL, FederalACASafeHarborID = NULL, Month = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeOfferAndCoverageV1", body = list(DataObject = body), searchFields = append("ACAYearEmployeeOfferAndCoverageV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearEmployeeOfferAndCoverageV1
	#'
	#' This function modifies an ACAYearEmployeeOfferAndCoverageV1
	#' @param fieldNames The field values to give the modified ACAYearEmployeeOfferAndCoverageV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearEmployeeOfferAndCoverageV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearEmployeeOfferAndCoverageV1 <- function(ACAYearEmployeeOfferAndCoverageV1ID, ACAYearEmployeeV1ID = NULL, EmployeeShareOfLowestCostMonthlyPremium = NULL, FederalACAOfferAndCoverageCode = NULL, FederalACAOfferAndCoverageID = NULL, FederalACASafeHarborCode = NULL, FederalACASafeHarborID = NULL, Month = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeOfferAndCoverageV1", objectId = ACAYearEmployeeOfferAndCoverageV1ID, body = list(DataObject = body), searchFields = append("ACAYearEmployeeOfferAndCoverageV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearEmployeeV1s
	#'
	#' This function returns a dataframe or json object of ACAYearEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearEmployeeV1s <- function(searchConditionsList = NULL, ACAYearEmployeeV1ID = F, ACAYearID = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, AttachmentCount = F, CityElectronicFile = F, CityPrinted = F, ContactPhoneNumber = F, CreatedTime = F, EmployeeID = F, EmployeeShareOfLowestCostMonthlyPremium = F, EmployerOfferedSelfInsuredCoverage = F, FederalACAOfferAndCoverageCode = F, FederalACAOfferAndCoverageID = F, FederalACASafeHarborCode = F, FederalACASafeHarborID = F, FirstNameElectronicFile = F, LastNameElectronicFile = F, MediaIDFullSSN = F, MediaIDMaskedSSN = F, MiddleNameElectronicFile = F, ModifiedTime = F, NamePrinted = F, PlanStartMonth = F, SkywardID = F, SocialSecurityNumber = F, StateCode = F, StateID = F, SuffixElectronicFile = F, TestScenarioIdentification = F, UserIDCreatedBy = F, UserIDModifiedBy = F, XMLData = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearEmployeeV1
	#'
	#' This function returns a dataframe or json object of an ACAYearEmployeeV1
	#' @param ACAYearEmployeeV1ID The ID of the ACAYearEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearEmployeeV1 <- function(ACAYearEmployeeV1ID, ACAYearID = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, AttachmentCount = F, CityElectronicFile = F, CityPrinted = F, ContactPhoneNumber = F, CreatedTime = F, EmployeeID = F, EmployeeShareOfLowestCostMonthlyPremium = F, EmployerOfferedSelfInsuredCoverage = F, FederalACAOfferAndCoverageCode = F, FederalACAOfferAndCoverageID = F, FederalACASafeHarborCode = F, FederalACASafeHarborID = F, FirstNameElectronicFile = F, LastNameElectronicFile = F, MediaIDFullSSN = F, MediaIDMaskedSSN = F, MiddleNameElectronicFile = F, ModifiedTime = F, NamePrinted = F, PlanStartMonth = F, SkywardID = F, SocialSecurityNumber = F, StateCode = F, StateID = F, SuffixElectronicFile = F, TestScenarioIdentification = F, UserIDCreatedBy = F, UserIDModifiedBy = F, XMLData = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeV1", objectId = ACAYearEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearEmployeeV1
	#'
	#' This function deletes an ACAYearEmployeeV1
	#' @param ACAYearEmployeeV1ID The ID of the ACAYearEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearEmployeeV1ID of the deleted ACAYearEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearEmployeeV1 <- function(ACAYearEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeV1", objectId = ACAYearEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearEmployeeV1
	#'
	#' This function creates an ACAYearEmployeeV1
	#' @param fieldNames The field values to give the created ACAYearEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearEmployeeV1 <- function(ACAYearID = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, CityElectronicFile = NULL, CityPrinted = NULL, ContactPhoneNumber = NULL, EmployeeID = NULL, EmployeeShareOfLowestCostMonthlyPremium = NULL, EmployerOfferedSelfInsuredCoverage = NULL, FederalACAOfferAndCoverageCode = NULL, FederalACAOfferAndCoverageID = NULL, FederalACASafeHarborCode = NULL, FederalACASafeHarborID = NULL, FirstNameElectronicFile = NULL, LastNameElectronicFile = NULL, MediaIDFullSSN = NULL, MediaIDMaskedSSN = NULL, MiddleNameElectronicFile = NULL, NamePrinted = NULL, PlanStartMonth = NULL, SocialSecurityNumber = NULL, StateCode = NULL, StateID = NULL, SuffixElectronicFile = NULL, TestScenarioIdentification = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeV1", body = list(DataObject = body), searchFields = append("ACAYearEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearEmployeeV1
	#'
	#' This function modifies an ACAYearEmployeeV1
	#' @param fieldNames The field values to give the modified ACAYearEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearEmployeeV1 <- function(ACAYearEmployeeV1ID, ACAYearID = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, CityElectronicFile = NULL, CityPrinted = NULL, ContactPhoneNumber = NULL, EmployeeID = NULL, EmployeeShareOfLowestCostMonthlyPremium = NULL, EmployerOfferedSelfInsuredCoverage = NULL, FederalACAOfferAndCoverageCode = NULL, FederalACAOfferAndCoverageID = NULL, FederalACASafeHarborCode = NULL, FederalACASafeHarborID = NULL, FirstNameElectronicFile = NULL, LastNameElectronicFile = NULL, MediaIDFullSSN = NULL, MediaIDMaskedSSN = NULL, MiddleNameElectronicFile = NULL, NamePrinted = NULL, PlanStartMonth = NULL, SocialSecurityNumber = NULL, StateCode = NULL, StateID = NULL, SuffixElectronicFile = NULL, TestScenarioIdentification = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearEmployeeV1", objectId = ACAYearEmployeeV1ID, body = list(DataObject = body), searchFields = append("ACAYearEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearExceptions
	#'
	#' This function returns a dataframe or json object of W2YearExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Description = F, EmployeeID = F, ExceptionType = F, IsFatalException = F, ModifiedTime = F, SourceType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeV1ID = F, W2YearExceptionID = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearException
	#'
	#' This function returns a dataframe or json object of a W2YearException
	#' @param W2YearExceptionID The ID of the W2YearException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearException <- function(W2YearExceptionID, CreatedTime = F, Description = F, EmployeeID = F, ExceptionType = F, IsFatalException = F, ModifiedTime = F, SourceType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeV1ID = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearException", objectId = W2YearExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearException
	#'
	#' This function deletes a W2YearException
	#' @param W2YearExceptionID The ID of the W2YearException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearExceptionID of the deleted W2YearException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearException <- function(W2YearExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearException", objectId = W2YearExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearException
	#'
	#' This function creates a W2YearException
	#' @param fieldNames The field values to give the created W2YearException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearException <- function(Description = NULL, EmployeeID = NULL, ExceptionType = NULL, IsFatalException = NULL, SourceType = NULL, W2YearEmployeeV1ID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearException", body = list(DataObject = body), searchFields = append("W2YearExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearException
	#'
	#' This function modifies a W2YearException
	#' @param fieldNames The field values to give the modified W2YearException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearException <- function(W2YearExceptionID, Description = NULL, EmployeeID = NULL, ExceptionType = NULL, IsFatalException = NULL, SourceType = NULL, W2YearEmployeeV1ID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearException", objectId = W2YearExceptionID, body = list(DataObject = body), searchFields = append("W2YearExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAYearExportElectronicFiles
	#'
	#' This function returns a dataframe or json object of ACAYearExportElectronicFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearExportElectronicFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearExportElectronicFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearExportElectronicFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAYearExportElectronicFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAYearExportElectronicFiles <- function(searchConditionsList = NULL, ACAYearExportElectronicFileID = F, ACAYearID = F, CompanyAddressLine1 = F, CompanyAddressLine2 = F, CompanyCity = F, CompanyContactPersonFirstName = F, CompanyContactPersonLastName = F, CompanyContactPersonMiddleName = F, CompanyContactPersonSuffix = F, CompanyContactPhoneNumber = F, CompanyName = F, CompanyStateCode = F, CompanyStateID = F, CompanyZipCode = F, CompanyZipCodeExtension = F, CreatedTime = F, EmployerAddressLine1 = F, EmployerAddressLine2 = F, EmployerBusinessNameLine1 = F, EmployerBusinessNameLine2 = F, EmployerCity = F, EmployerStateCode = F, EmployerStateID = F, EmployerZipCode = F, EmployerZipCodeExtension = F, IsPriorYearData = F, MediaID = F, MediaIDManifest = F, ModifiedTime = F, OriginalReceiptIdentification = F, Status = F, TransmissionType = F, TransmitterBusinessNameLine1 = F, TransmitterBusinessNameLine2 = F, TransmitterControlCode = F, TransmitterEmployerIdentificationNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAYearExportElectronicFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAYearExportElectronicFile
	#'
	#' This function returns a dataframe or json object of an ACAYearExportElectronicFile
	#' @param ACAYearExportElectronicFileID The ID of the ACAYearExportElectronicFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAYearExportElectronicFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAYearExportElectronicFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAYearExportElectronicFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAYearExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAYearExportElectronicFile <- function(ACAYearExportElectronicFileID, ACAYearID = F, CompanyAddressLine1 = F, CompanyAddressLine2 = F, CompanyCity = F, CompanyContactPersonFirstName = F, CompanyContactPersonLastName = F, CompanyContactPersonMiddleName = F, CompanyContactPersonSuffix = F, CompanyContactPhoneNumber = F, CompanyName = F, CompanyStateCode = F, CompanyStateID = F, CompanyZipCode = F, CompanyZipCodeExtension = F, CreatedTime = F, EmployerAddressLine1 = F, EmployerAddressLine2 = F, EmployerBusinessNameLine1 = F, EmployerBusinessNameLine2 = F, EmployerCity = F, EmployerStateCode = F, EmployerStateID = F, EmployerZipCode = F, EmployerZipCodeExtension = F, IsPriorYearData = F, MediaID = F, MediaIDManifest = F, ModifiedTime = F, OriginalReceiptIdentification = F, Status = F, TransmissionType = F, TransmitterBusinessNameLine1 = F, TransmitterBusinessNameLine2 = F, TransmitterControlCode = F, TransmitterEmployerIdentificationNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAYearExportElectronicFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAYearExportElectronicFile", objectId = ACAYearExportElectronicFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAYearExportElectronicFile
	#'
	#' This function deletes an ACAYearExportElectronicFile
	#' @param ACAYearExportElectronicFileID The ID of the ACAYearExportElectronicFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAYearExportElectronicFileID of the deleted ACAYearExportElectronicFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAYearExportElectronicFile <- function(ACAYearExportElectronicFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAYearExportElectronicFile", objectId = ACAYearExportElectronicFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAYearExportElectronicFile
	#'
	#' This function creates an ACAYearExportElectronicFile
	#' @param fieldNames The field values to give the created ACAYearExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAYearExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAYearExportElectronicFile <- function(ACAYearID = NULL, CompanyAddressLine1 = NULL, CompanyAddressLine2 = NULL, CompanyCity = NULL, CompanyContactPersonFirstName = NULL, CompanyContactPersonLastName = NULL, CompanyContactPersonMiddleName = NULL, CompanyContactPersonSuffix = NULL, CompanyContactPhoneNumber = NULL, CompanyName = NULL, CompanyStateCode = NULL, CompanyStateID = NULL, CompanyZipCode = NULL, CompanyZipCodeExtension = NULL, EmployerAddressLine1 = NULL, EmployerAddressLine2 = NULL, EmployerBusinessNameLine1 = NULL, EmployerBusinessNameLine2 = NULL, EmployerCity = NULL, EmployerStateCode = NULL, EmployerStateID = NULL, EmployerZipCode = NULL, EmployerZipCodeExtension = NULL, IsPriorYearData = NULL, MediaID = NULL, MediaIDManifest = NULL, OriginalReceiptIdentification = NULL, Status = NULL, TransmissionType = NULL, TransmitterBusinessNameLine1 = NULL, TransmitterBusinessNameLine2 = NULL, TransmitterControlCode = NULL, TransmitterEmployerIdentificationNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAYearExportElectronicFile", body = list(DataObject = body), searchFields = append("ACAYearExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAYearExportElectronicFile
	#'
	#' This function modifies an ACAYearExportElectronicFile
	#' @param fieldNames The field values to give the modified ACAYearExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAYearExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAYearExportElectronicFile <- function(ACAYearExportElectronicFileID, ACAYearID = NULL, CompanyAddressLine1 = NULL, CompanyAddressLine2 = NULL, CompanyCity = NULL, CompanyContactPersonFirstName = NULL, CompanyContactPersonLastName = NULL, CompanyContactPersonMiddleName = NULL, CompanyContactPersonSuffix = NULL, CompanyContactPhoneNumber = NULL, CompanyName = NULL, CompanyStateCode = NULL, CompanyStateID = NULL, CompanyZipCode = NULL, CompanyZipCodeExtension = NULL, EmployerAddressLine1 = NULL, EmployerAddressLine2 = NULL, EmployerBusinessNameLine1 = NULL, EmployerBusinessNameLine2 = NULL, EmployerCity = NULL, EmployerStateCode = NULL, EmployerStateID = NULL, EmployerZipCode = NULL, EmployerZipCodeExtension = NULL, IsPriorYearData = NULL, MediaID = NULL, MediaIDManifest = NULL, OriginalReceiptIdentification = NULL, Status = NULL, TransmissionType = NULL, TransmitterBusinessNameLine1 = NULL, TransmitterBusinessNameLine2 = NULL, TransmitterControlCode = NULL, TransmitterEmployerIdentificationNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAYearExportElectronicFile", objectId = ACAYearExportElectronicFileID, body = list(DataObject = body), searchFields = append("ACAYearExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalW2KindOfEmployers
	#'
	#' This function returns a dataframe or json object of FederalW2KindOfEmployers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalW2KindOfEmployers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalW2KindOfEmployers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalW2KindOfEmployer') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalW2KindOfEmployers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalW2KindOfEmployers <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, FederalW2KindOfEmployerID = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Year = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalW2KindOfEmployer", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalW2KindOfEmployer
	#'
	#' This function returns a dataframe or json object of a FederalW2KindOfEmployer
	#' @param FederalW2KindOfEmployerID The ID of the FederalW2KindOfEmployer to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalW2KindOfEmployer. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalW2KindOfEmployer.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalW2KindOfEmployer') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalW2KindOfEmployer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalW2KindOfEmployer <- function(FederalW2KindOfEmployerID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Year = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalW2KindOfEmployerID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalW2KindOfEmployer", objectId = FederalW2KindOfEmployerID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalW2KindOfEmployer
	#'
	#' This function deletes a FederalW2KindOfEmployer
	#' @param FederalW2KindOfEmployerID The ID of the FederalW2KindOfEmployer to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalW2KindOfEmployerID of the deleted FederalW2KindOfEmployer.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalW2KindOfEmployer <- function(FederalW2KindOfEmployerID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalW2KindOfEmployer", objectId = FederalW2KindOfEmployerID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalW2KindOfEmployer
	#'
	#' This function creates a FederalW2KindOfEmployer
	#' @param fieldNames The field values to give the created FederalW2KindOfEmployer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalW2KindOfEmployer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalW2KindOfEmployer <- function(Code = NULL, Description = NULL, Year = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalW2KindOfEmployer", body = list(DataObject = body), searchFields = append("FederalW2KindOfEmployerID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalW2KindOfEmployer
	#'
	#' This function modifies a FederalW2KindOfEmployer
	#' @param fieldNames The field values to give the modified FederalW2KindOfEmployer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalW2KindOfEmployer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalW2KindOfEmployer <- function(FederalW2KindOfEmployerID, Code = NULL, Description = NULL, Year = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalW2KindOfEmployer", objectId = FederalW2KindOfEmployerID, body = list(DataObject = body), searchFields = append("FederalW2KindOfEmployerID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2ExportElectronicFiles
	#'
	#' This function returns a dataframe or json object of W2ExportElectronicFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2ExportElectronicFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2ExportElectronicFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2ExportElectronicFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2ExportElectronicFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2ExportElectronicFiles <- function(searchConditionsList = NULL, CompanyAddressType = F, CompanyCity = F, CompanyCountryCode = F, CompanyDeliveryAddress = F, CompanyInternationalProvinceID = F, CompanyInternationalStateOrProvinceCode = F, CompanyLocationAddress = F, CompanyName = F, CompanyPostalCode = F, CompanyStateCode = F, CompanyStateID = F, CompanyW2CountryID = F, CompanyZipCode = F, CompanyZipCodeExtension = F, CreatedTime = F, EmployerAddressType = F, EmployerCity = F, EmployerContactEmailAddress = F, EmployerContactFax = F, EmployerContactName = F, EmployerContactPhoneExtension = F, EmployerContactPhoneNumber = F, EmployerCountryCode = F, EmployerDeliveryAddress = F, EmployerInternationalProvinceID = F, EmployerInternationalStateOrProvinceCode = F, EmployerLocationAddress = F, EmployerName = F, EmployerPostalCode = F, EmployerStateCode = F, EmployerStateID = F, EmployerW2CountryID = F, EmployerZipCode = F, EmployerZipCodeExtension = F, HasEmployerThirdPartySickPay = F, IncomeTaxWithheldByPayerOfThirdPartySickPay = F, IowaBusinessEFileNumber = F, IsResubmission = F, LocalityName = F, MediaID = F, ModifiedTime = F, ResubmissionWageFileIdentifier = F, Status = F, SubmitterAddressType = F, SubmitterCity = F, SubmitterContactEmailAddress = F, SubmitterContactFax = F, SubmitterContactName = F, SubmitterContactPhoneExtension = F, SubmitterContactPhoneNumber = F, SubmitterCountryCode = F, SubmitterDeliveryAddress = F, SubmitterEmployerIdentificationNumber = F, SubmitterInternationalProvinceID = F, SubmitterInternationalStateOrProvinceCode = F, SubmitterLocationAddress = F, SubmitterName = F, SubmitterPostalCode = F, SubmitterStateCode = F, SubmitterStateID = F, SubmitterUserIdentification = F, SubmitterW2CountryID = F, SubmitterZipCode = F, SubmitterZipCodeExtension = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2ExportElectronicFileID = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2ExportElectronicFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2ExportElectronicFile
	#'
	#' This function returns a dataframe or json object of a W2ExportElectronicFile
	#' @param W2ExportElectronicFileID The ID of the W2ExportElectronicFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2ExportElectronicFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2ExportElectronicFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2ExportElectronicFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2ExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2ExportElectronicFile <- function(W2ExportElectronicFileID, CompanyAddressType = F, CompanyCity = F, CompanyCountryCode = F, CompanyDeliveryAddress = F, CompanyInternationalProvinceID = F, CompanyInternationalStateOrProvinceCode = F, CompanyLocationAddress = F, CompanyName = F, CompanyPostalCode = F, CompanyStateCode = F, CompanyStateID = F, CompanyW2CountryID = F, CompanyZipCode = F, CompanyZipCodeExtension = F, CreatedTime = F, EmployerAddressType = F, EmployerCity = F, EmployerContactEmailAddress = F, EmployerContactFax = F, EmployerContactName = F, EmployerContactPhoneExtension = F, EmployerContactPhoneNumber = F, EmployerCountryCode = F, EmployerDeliveryAddress = F, EmployerInternationalProvinceID = F, EmployerInternationalStateOrProvinceCode = F, EmployerLocationAddress = F, EmployerName = F, EmployerPostalCode = F, EmployerStateCode = F, EmployerStateID = F, EmployerW2CountryID = F, EmployerZipCode = F, EmployerZipCodeExtension = F, HasEmployerThirdPartySickPay = F, IncomeTaxWithheldByPayerOfThirdPartySickPay = F, IowaBusinessEFileNumber = F, IsResubmission = F, LocalityName = F, MediaID = F, ModifiedTime = F, ResubmissionWageFileIdentifier = F, Status = F, SubmitterAddressType = F, SubmitterCity = F, SubmitterContactEmailAddress = F, SubmitterContactFax = F, SubmitterContactName = F, SubmitterContactPhoneExtension = F, SubmitterContactPhoneNumber = F, SubmitterCountryCode = F, SubmitterDeliveryAddress = F, SubmitterEmployerIdentificationNumber = F, SubmitterInternationalProvinceID = F, SubmitterInternationalStateOrProvinceCode = F, SubmitterLocationAddress = F, SubmitterName = F, SubmitterPostalCode = F, SubmitterStateCode = F, SubmitterStateID = F, SubmitterUserIdentification = F, SubmitterW2CountryID = F, SubmitterZipCode = F, SubmitterZipCodeExtension = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2ExportElectronicFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2ExportElectronicFile", objectId = W2ExportElectronicFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2ExportElectronicFile
	#'
	#' This function deletes a W2ExportElectronicFile
	#' @param W2ExportElectronicFileID The ID of the W2ExportElectronicFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2ExportElectronicFileID of the deleted W2ExportElectronicFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2ExportElectronicFile <- function(W2ExportElectronicFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2ExportElectronicFile", objectId = W2ExportElectronicFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2ExportElectronicFile
	#'
	#' This function creates a W2ExportElectronicFile
	#' @param fieldNames The field values to give the created W2ExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2ExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2ExportElectronicFile <- function(CompanyAddressType = NULL, CompanyCity = NULL, CompanyCountryCode = NULL, CompanyDeliveryAddress = NULL, CompanyInternationalProvinceID = NULL, CompanyInternationalStateOrProvinceCode = NULL, CompanyLocationAddress = NULL, CompanyName = NULL, CompanyPostalCode = NULL, CompanyStateCode = NULL, CompanyStateID = NULL, CompanyW2CountryID = NULL, CompanyZipCode = NULL, CompanyZipCodeExtension = NULL, EmployerAddressType = NULL, EmployerCity = NULL, EmployerContactEmailAddress = NULL, EmployerContactFax = NULL, EmployerContactName = NULL, EmployerContactPhoneExtension = NULL, EmployerContactPhoneNumber = NULL, EmployerCountryCode = NULL, EmployerDeliveryAddress = NULL, EmployerInternationalProvinceID = NULL, EmployerInternationalStateOrProvinceCode = NULL, EmployerLocationAddress = NULL, EmployerName = NULL, EmployerPostalCode = NULL, EmployerStateCode = NULL, EmployerStateID = NULL, EmployerW2CountryID = NULL, EmployerZipCode = NULL, EmployerZipCodeExtension = NULL, HasEmployerThirdPartySickPay = NULL, IncomeTaxWithheldByPayerOfThirdPartySickPay = NULL, IowaBusinessEFileNumber = NULL, IsResubmission = NULL, LocalityName = NULL, MediaID = NULL, ResubmissionWageFileIdentifier = NULL, Status = NULL, SubmitterAddressType = NULL, SubmitterCity = NULL, SubmitterContactEmailAddress = NULL, SubmitterContactFax = NULL, SubmitterContactName = NULL, SubmitterContactPhoneExtension = NULL, SubmitterContactPhoneNumber = NULL, SubmitterCountryCode = NULL, SubmitterDeliveryAddress = NULL, SubmitterEmployerIdentificationNumber = NULL, SubmitterInternationalProvinceID = NULL, SubmitterInternationalStateOrProvinceCode = NULL, SubmitterLocationAddress = NULL, SubmitterName = NULL, SubmitterPostalCode = NULL, SubmitterStateCode = NULL, SubmitterStateID = NULL, SubmitterUserIdentification = NULL, SubmitterW2CountryID = NULL, SubmitterZipCode = NULL, SubmitterZipCodeExtension = NULL, Type = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2ExportElectronicFile", body = list(DataObject = body), searchFields = append("W2ExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2ExportElectronicFile
	#'
	#' This function modifies a W2ExportElectronicFile
	#' @param fieldNames The field values to give the modified W2ExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2ExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2ExportElectronicFile <- function(W2ExportElectronicFileID, CompanyAddressType = NULL, CompanyCity = NULL, CompanyCountryCode = NULL, CompanyDeliveryAddress = NULL, CompanyInternationalProvinceID = NULL, CompanyInternationalStateOrProvinceCode = NULL, CompanyLocationAddress = NULL, CompanyName = NULL, CompanyPostalCode = NULL, CompanyStateCode = NULL, CompanyStateID = NULL, CompanyW2CountryID = NULL, CompanyZipCode = NULL, CompanyZipCodeExtension = NULL, EmployerAddressType = NULL, EmployerCity = NULL, EmployerContactEmailAddress = NULL, EmployerContactFax = NULL, EmployerContactName = NULL, EmployerContactPhoneExtension = NULL, EmployerContactPhoneNumber = NULL, EmployerCountryCode = NULL, EmployerDeliveryAddress = NULL, EmployerInternationalProvinceID = NULL, EmployerInternationalStateOrProvinceCode = NULL, EmployerLocationAddress = NULL, EmployerName = NULL, EmployerPostalCode = NULL, EmployerStateCode = NULL, EmployerStateID = NULL, EmployerW2CountryID = NULL, EmployerZipCode = NULL, EmployerZipCodeExtension = NULL, HasEmployerThirdPartySickPay = NULL, IncomeTaxWithheldByPayerOfThirdPartySickPay = NULL, IowaBusinessEFileNumber = NULL, IsResubmission = NULL, LocalityName = NULL, MediaID = NULL, ResubmissionWageFileIdentifier = NULL, Status = NULL, SubmitterAddressType = NULL, SubmitterCity = NULL, SubmitterContactEmailAddress = NULL, SubmitterContactFax = NULL, SubmitterContactName = NULL, SubmitterContactPhoneExtension = NULL, SubmitterContactPhoneNumber = NULL, SubmitterCountryCode = NULL, SubmitterDeliveryAddress = NULL, SubmitterEmployerIdentificationNumber = NULL, SubmitterInternationalProvinceID = NULL, SubmitterInternationalStateOrProvinceCode = NULL, SubmitterLocationAddress = NULL, SubmitterName = NULL, SubmitterPostalCode = NULL, SubmitterStateCode = NULL, SubmitterStateID = NULL, SubmitterUserIdentification = NULL, SubmitterW2CountryID = NULL, SubmitterZipCode = NULL, SubmitterZipCodeExtension = NULL, Type = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2ExportElectronicFile", objectId = W2ExportElectronicFileID, body = list(DataObject = body), searchFields = append("W2ExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearEmployeeStateV1s
	#'
	#' This function returns a dataframe or json object of W2YearEmployeeStateV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeStateV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeStateV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeStateV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearEmployeeStateV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearEmployeeStateV1s <- function(searchConditionsList = NULL, Box16StateWages = F, Box17StateIncomeTax = F, CreatedTime = F, IsOriginalExtractedValue = F, ModifiedTime = F, StateAllowance = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeStateV1ID = F, W2YearEmployeeV1ID = F, W2YearTaxStateIDBox15 = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearEmployeeStateV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearEmployeeStateV1
	#'
	#' This function returns a dataframe or json object of a W2YearEmployeeStateV1
	#' @param W2YearEmployeeStateV1ID The ID of the W2YearEmployeeStateV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeStateV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeStateV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeStateV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearEmployeeStateV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearEmployeeStateV1 <- function(W2YearEmployeeStateV1ID, Box16StateWages = F, Box17StateIncomeTax = F, CreatedTime = F, IsOriginalExtractedValue = F, ModifiedTime = F, StateAllowance = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeV1ID = F, W2YearTaxStateIDBox15 = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearEmployeeStateV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeStateV1", objectId = W2YearEmployeeStateV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearEmployeeStateV1
	#'
	#' This function deletes a W2YearEmployeeStateV1
	#' @param W2YearEmployeeStateV1ID The ID of the W2YearEmployeeStateV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearEmployeeStateV1ID of the deleted W2YearEmployeeStateV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearEmployeeStateV1 <- function(W2YearEmployeeStateV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeStateV1", objectId = W2YearEmployeeStateV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearEmployeeStateV1
	#'
	#' This function creates a W2YearEmployeeStateV1
	#' @param fieldNames The field values to give the created W2YearEmployeeStateV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearEmployeeStateV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearEmployeeStateV1 <- function(Box16StateWages = NULL, Box17StateIncomeTax = NULL, IsOriginalExtractedValue = NULL, StateAllowance = NULL, W2YearEmployeeV1ID = NULL, W2YearTaxStateIDBox15 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeStateV1", body = list(DataObject = body), searchFields = append("W2YearEmployeeStateV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearEmployeeStateV1
	#'
	#' This function modifies a W2YearEmployeeStateV1
	#' @param fieldNames The field values to give the modified W2YearEmployeeStateV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearEmployeeStateV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearEmployeeStateV1 <- function(W2YearEmployeeStateV1ID, Box16StateWages = NULL, Box17StateIncomeTax = NULL, IsOriginalExtractedValue = NULL, StateAllowance = NULL, W2YearEmployeeV1ID = NULL, W2YearTaxStateIDBox15 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeStateV1", objectId = W2YearEmployeeStateV1ID, body = list(DataObject = body), searchFields = append("W2YearEmployeeStateV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearEmployeeBox14V1s
	#'
	#' This function returns a dataframe or json object of W2YearEmployeeBox14V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeBox14V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeBox14V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeBox14V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearEmployeeBox14V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearEmployeeBox14V1s <- function(searchConditionsList = NULL, Amount = F, Code = F, CreatedTime = F, Description = F, FederalW2BoxID = F, IsOriginalExtractedValue = F, ModifiedTime = F, OregonTransitTaxWages = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearEmployeeBox14V1ID = F, W2YearEmployeeV1ID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearEmployeeBox14V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearEmployeeBox14V1
	#'
	#' This function returns a dataframe or json object of a W2YearEmployeeBox14V1
	#' @param W2YearEmployeeBox14V1ID The ID of the W2YearEmployeeBox14V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeBox14V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeBox14V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeBox14V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearEmployeeBox14V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearEmployeeBox14V1 <- function(W2YearEmployeeBox14V1ID, Amount = F, Code = F, CreatedTime = F, Description = F, FederalW2BoxID = F, IsOriginalExtractedValue = F, ModifiedTime = F, OregonTransitTaxWages = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearEmployeeV1ID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearEmployeeBox14V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox14V1", objectId = W2YearEmployeeBox14V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearEmployeeBox14V1
	#'
	#' This function deletes a W2YearEmployeeBox14V1
	#' @param W2YearEmployeeBox14V1ID The ID of the W2YearEmployeeBox14V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearEmployeeBox14V1ID of the deleted W2YearEmployeeBox14V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearEmployeeBox14V1 <- function(W2YearEmployeeBox14V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox14V1", objectId = W2YearEmployeeBox14V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearEmployeeBox14V1
	#'
	#' This function creates a W2YearEmployeeBox14V1
	#' @param fieldNames The field values to give the created W2YearEmployeeBox14V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearEmployeeBox14V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearEmployeeBox14V1 <- function(Amount = NULL, Code = NULL, Description = NULL, FederalW2BoxID = NULL, IsOriginalExtractedValue = NULL, OregonTransitTaxWages = NULL, W2YearBox14SetID = NULL, W2YearEmployeeV1ID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox14V1", body = list(DataObject = body), searchFields = append("W2YearEmployeeBox14V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearEmployeeBox14V1
	#'
	#' This function modifies a W2YearEmployeeBox14V1
	#' @param fieldNames The field values to give the modified W2YearEmployeeBox14V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearEmployeeBox14V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearEmployeeBox14V1 <- function(W2YearEmployeeBox14V1ID, Amount = NULL, Code = NULL, Description = NULL, FederalW2BoxID = NULL, IsOriginalExtractedValue = NULL, OregonTransitTaxWages = NULL, W2YearBox14SetID = NULL, W2YearEmployeeV1ID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox14V1", objectId = W2YearEmployeeBox14V1ID, body = list(DataObject = body), searchFields = append("W2YearEmployeeBox14V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearEmployeeBox12V1s
	#'
	#' This function returns a dataframe or json object of W2YearEmployeeBox12V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeBox12V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeBox12V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeBox12V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearEmployeeBox12V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearEmployeeBox12V1s <- function(searchConditionsList = NULL, Amount = F, Code = F, CreatedTime = F, FederalW2BoxID = F, IsOriginalExtractedValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeBox12V1ID = F, W2YearEmployeeV1ID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearEmployeeBox12V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearEmployeeBox12V1
	#'
	#' This function returns a dataframe or json object of a W2YearEmployeeBox12V1
	#' @param W2YearEmployeeBox12V1ID The ID of the W2YearEmployeeBox12V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeBox12V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeBox12V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeBox12V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearEmployeeBox12V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearEmployeeBox12V1 <- function(W2YearEmployeeBox12V1ID, Amount = F, Code = F, CreatedTime = F, FederalW2BoxID = F, IsOriginalExtractedValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeV1ID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearEmployeeBox12V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox12V1", objectId = W2YearEmployeeBox12V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearEmployeeBox12V1
	#'
	#' This function deletes a W2YearEmployeeBox12V1
	#' @param W2YearEmployeeBox12V1ID The ID of the W2YearEmployeeBox12V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearEmployeeBox12V1ID of the deleted W2YearEmployeeBox12V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearEmployeeBox12V1 <- function(W2YearEmployeeBox12V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox12V1", objectId = W2YearEmployeeBox12V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearEmployeeBox12V1
	#'
	#' This function creates a W2YearEmployeeBox12V1
	#' @param fieldNames The field values to give the created W2YearEmployeeBox12V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearEmployeeBox12V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearEmployeeBox12V1 <- function(Amount = NULL, Code = NULL, FederalW2BoxID = NULL, IsOriginalExtractedValue = NULL, W2YearEmployeeV1ID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox12V1", body = list(DataObject = body), searchFields = append("W2YearEmployeeBox12V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearEmployeeBox12V1
	#'
	#' This function modifies a W2YearEmployeeBox12V1
	#' @param fieldNames The field values to give the modified W2YearEmployeeBox12V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearEmployeeBox12V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearEmployeeBox12V1 <- function(W2YearEmployeeBox12V1ID, Amount = NULL, Code = NULL, FederalW2BoxID = NULL, IsOriginalExtractedValue = NULL, W2YearEmployeeV1ID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeBox12V1", objectId = W2YearEmployeeBox12V1ID, body = list(DataObject = body), searchFields = append("W2YearEmployeeBox12V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearEmployeeV1s
	#'
	#' This function returns a dataframe or json object of W2YearEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearEmployeeV1s <- function(searchConditionsList = NULL, AddressID = F, AddressType = F, Box10DependentCareBenefits = F, Box10DependentCareBenefitsExtracted = F, Box11NonQualifiedPlansNonSection457b = F, Box11NonQualifiedPlansNonSection457bExtracted = F, Box11NonQualifiedPlansSection457b = F, Box11NonQualifiedPlansSection457bExtracted = F, Box1FederalWages = F, Box1FederalWagesExtracted = F, Box2FederalIncomeTaxWithheld = F, Box2FederalIncomeTaxWithheldExtracted = F, Box3SocialSecurityWages = F, Box3SocialSecurityWagesExtracted = F, Box4SocialSecurityTaxWithheld = F, Box4SocialSecurityTaxWithheldExtracted = F, Box5MedicareWages = F, Box5MedicareWagesExtracted = F, Box6MedicareTaxWithheld = F, Box6MedicareTaxWithheldExtracted = F, Box7SocialSecurityTips = F, Box9AdvanceEmployeeIncomeCredit = F, Box9VerificationCode = F, CityElectronicFile = F, CityPrinted = F, CountryCode = F, CreatedTime = F, DeliveryAddressElectronicFile = F, DeliveryAddressPrinted = F, EmployeeID = F, EmployeeNamePrinted = F, EmployeeSocialSecurityNumber = F, FatalExceptionCount = F, FirstNameElectronicFile = F, HasZeroWages = F, InternationalProvinceID = F, InternationalStateOrProvinceCode = F, IsBox13RetirementPlan = F, IsBox13StatutoryEmployee = F, IsBox13ThirdPartySickPay = F, LastNameElectronicFile = F, LocationAddressElectronicFile = F, LocationAddressPrinted = F, MaskedEmployeeSocialSecurityNumber = F, MediaIDFullSSN = F, MediaIDMaskedSSN = F, MiddleNameElectronicFile = F, ModifiedTime = F, NonFatalExceptionCount = F, PostalCode = F, StateCode = F, StateID = F, SuffixElectronicFile = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryID = F, W2YearEmployeeV1ID = F, W2YearID = F, XMLData = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearEmployeeV1
	#'
	#' This function returns a dataframe or json object of a W2YearEmployeeV1
	#' @param W2YearEmployeeV1ID The ID of the W2YearEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearEmployeeV1 <- function(W2YearEmployeeV1ID, AddressID = F, AddressType = F, Box10DependentCareBenefits = F, Box10DependentCareBenefitsExtracted = F, Box11NonQualifiedPlansNonSection457b = F, Box11NonQualifiedPlansNonSection457bExtracted = F, Box11NonQualifiedPlansSection457b = F, Box11NonQualifiedPlansSection457bExtracted = F, Box1FederalWages = F, Box1FederalWagesExtracted = F, Box2FederalIncomeTaxWithheld = F, Box2FederalIncomeTaxWithheldExtracted = F, Box3SocialSecurityWages = F, Box3SocialSecurityWagesExtracted = F, Box4SocialSecurityTaxWithheld = F, Box4SocialSecurityTaxWithheldExtracted = F, Box5MedicareWages = F, Box5MedicareWagesExtracted = F, Box6MedicareTaxWithheld = F, Box6MedicareTaxWithheldExtracted = F, Box7SocialSecurityTips = F, Box9AdvanceEmployeeIncomeCredit = F, Box9VerificationCode = F, CityElectronicFile = F, CityPrinted = F, CountryCode = F, CreatedTime = F, DeliveryAddressElectronicFile = F, DeliveryAddressPrinted = F, EmployeeID = F, EmployeeNamePrinted = F, EmployeeSocialSecurityNumber = F, FatalExceptionCount = F, FirstNameElectronicFile = F, HasZeroWages = F, InternationalProvinceID = F, InternationalStateOrProvinceCode = F, IsBox13RetirementPlan = F, IsBox13StatutoryEmployee = F, IsBox13ThirdPartySickPay = F, LastNameElectronicFile = F, LocationAddressElectronicFile = F, LocationAddressPrinted = F, MaskedEmployeeSocialSecurityNumber = F, MediaIDFullSSN = F, MediaIDMaskedSSN = F, MiddleNameElectronicFile = F, ModifiedTime = F, NonFatalExceptionCount = F, PostalCode = F, StateCode = F, StateID = F, SuffixElectronicFile = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryID = F, W2YearID = F, XMLData = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeV1", objectId = W2YearEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearEmployeeV1
	#'
	#' This function deletes a W2YearEmployeeV1
	#' @param W2YearEmployeeV1ID The ID of the W2YearEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearEmployeeV1ID of the deleted W2YearEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearEmployeeV1 <- function(W2YearEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeV1", objectId = W2YearEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearEmployeeV1
	#'
	#' This function creates a W2YearEmployeeV1
	#' @param fieldNames The field values to give the created W2YearEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearEmployeeV1 <- function(AddressID = NULL, AddressType = NULL, Box10DependentCareBenefits = NULL, Box10DependentCareBenefitsExtracted = NULL, Box11NonQualifiedPlansNonSection457b = NULL, Box11NonQualifiedPlansNonSection457bExtracted = NULL, Box11NonQualifiedPlansSection457b = NULL, Box11NonQualifiedPlansSection457bExtracted = NULL, Box1FederalWages = NULL, Box1FederalWagesExtracted = NULL, Box2FederalIncomeTaxWithheld = NULL, Box2FederalIncomeTaxWithheldExtracted = NULL, Box3SocialSecurityWages = NULL, Box3SocialSecurityWagesExtracted = NULL, Box4SocialSecurityTaxWithheld = NULL, Box4SocialSecurityTaxWithheldExtracted = NULL, Box5MedicareWages = NULL, Box5MedicareWagesExtracted = NULL, Box6MedicareTaxWithheld = NULL, Box6MedicareTaxWithheldExtracted = NULL, Box7SocialSecurityTips = NULL, Box9AdvanceEmployeeIncomeCredit = NULL, Box9VerificationCode = NULL, CityElectronicFile = NULL, CityPrinted = NULL, CountryCode = NULL, DeliveryAddressElectronicFile = NULL, DeliveryAddressPrinted = NULL, EmployeeID = NULL, EmployeeNamePrinted = NULL, EmployeeSocialSecurityNumber = NULL, FirstNameElectronicFile = NULL, InternationalProvinceID = NULL, InternationalStateOrProvinceCode = NULL, IsBox13RetirementPlan = NULL, IsBox13StatutoryEmployee = NULL, IsBox13ThirdPartySickPay = NULL, LastNameElectronicFile = NULL, LocationAddressElectronicFile = NULL, LocationAddressPrinted = NULL, MediaIDFullSSN = NULL, MediaIDMaskedSSN = NULL, MiddleNameElectronicFile = NULL, PostalCode = NULL, StateCode = NULL, StateID = NULL, SuffixElectronicFile = NULL, W2CountryID = NULL, W2YearID = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeV1", body = list(DataObject = body), searchFields = append("W2YearEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearEmployeeV1
	#'
	#' This function modifies a W2YearEmployeeV1
	#' @param fieldNames The field values to give the modified W2YearEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearEmployeeV1 <- function(W2YearEmployeeV1ID, AddressID = NULL, AddressType = NULL, Box10DependentCareBenefits = NULL, Box10DependentCareBenefitsExtracted = NULL, Box11NonQualifiedPlansNonSection457b = NULL, Box11NonQualifiedPlansNonSection457bExtracted = NULL, Box11NonQualifiedPlansSection457b = NULL, Box11NonQualifiedPlansSection457bExtracted = NULL, Box1FederalWages = NULL, Box1FederalWagesExtracted = NULL, Box2FederalIncomeTaxWithheld = NULL, Box2FederalIncomeTaxWithheldExtracted = NULL, Box3SocialSecurityWages = NULL, Box3SocialSecurityWagesExtracted = NULL, Box4SocialSecurityTaxWithheld = NULL, Box4SocialSecurityTaxWithheldExtracted = NULL, Box5MedicareWages = NULL, Box5MedicareWagesExtracted = NULL, Box6MedicareTaxWithheld = NULL, Box6MedicareTaxWithheldExtracted = NULL, Box7SocialSecurityTips = NULL, Box9AdvanceEmployeeIncomeCredit = NULL, Box9VerificationCode = NULL, CityElectronicFile = NULL, CityPrinted = NULL, CountryCode = NULL, DeliveryAddressElectronicFile = NULL, DeliveryAddressPrinted = NULL, EmployeeID = NULL, EmployeeNamePrinted = NULL, EmployeeSocialSecurityNumber = NULL, FirstNameElectronicFile = NULL, InternationalProvinceID = NULL, InternationalStateOrProvinceCode = NULL, IsBox13RetirementPlan = NULL, IsBox13StatutoryEmployee = NULL, IsBox13ThirdPartySickPay = NULL, LastNameElectronicFile = NULL, LocationAddressElectronicFile = NULL, LocationAddressPrinted = NULL, MediaIDFullSSN = NULL, MediaIDMaskedSSN = NULL, MiddleNameElectronicFile = NULL, PostalCode = NULL, StateCode = NULL, StateID = NULL, SuffixElectronicFile = NULL, W2CountryID = NULL, W2YearID = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeV1", objectId = W2YearEmployeeV1ID, body = list(DataObject = body), searchFields = append("W2YearEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MYearVendors
	#'
	#' This function returns a dataframe or json object of Form1099MYearVendors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearVendors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearVendors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearVendor') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MYearVendors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MYearVendors <- function(searchConditionsList = NULL, AddressID = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, CityElectronicFile = F, CityPrinted = F, CreatedTime = F, Form1099MYearID = F, Form1099MYearVendorID = F, HasErrors = F, HasPayerMade5000DirectSales = F, MediaID = F, ModifiedTime = F, RecipientIdentificationNumber = F, StateCode = F, StateID = F, TaxIdentificationType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VendorID = F, VendorNameElectronicFileLine1 = F, VendorNameElectronicFileLine2 = F, VendorNamePrintedLine1 = F, VendorNamePrintedLine2 = F, XMLData = F, ZipCode = F, ZipCodeAddOn = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MYearVendor", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MYearVendor
	#'
	#' This function returns a dataframe or json object of a Form1099MYearVendor
	#' @param Form1099MYearVendorID The ID of the Form1099MYearVendor to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearVendor. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearVendor.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearVendor') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MYearVendor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MYearVendor <- function(Form1099MYearVendorID, AddressID = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, CityElectronicFile = F, CityPrinted = F, CreatedTime = F, Form1099MYearID = F, HasErrors = F, HasPayerMade5000DirectSales = F, MediaID = F, ModifiedTime = F, RecipientIdentificationNumber = F, StateCode = F, StateID = F, TaxIdentificationType = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VendorID = F, VendorNameElectronicFileLine1 = F, VendorNameElectronicFileLine2 = F, VendorNamePrintedLine1 = F, VendorNamePrintedLine2 = F, XMLData = F, ZipCode = F, ZipCodeAddOn = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MYearVendorID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendor", objectId = Form1099MYearVendorID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MYearVendor
	#'
	#' This function deletes a Form1099MYearVendor
	#' @param Form1099MYearVendorID The ID of the Form1099MYearVendor to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MYearVendorID of the deleted Form1099MYearVendor.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MYearVendor <- function(Form1099MYearVendorID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendor", objectId = Form1099MYearVendorID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MYearVendor
	#'
	#' This function creates a Form1099MYearVendor
	#' @param fieldNames The field values to give the created Form1099MYearVendor. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MYearVendor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MYearVendor <- function(AddressID = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, CityElectronicFile = NULL, CityPrinted = NULL, Form1099MYearID = NULL, HasPayerMade5000DirectSales = NULL, MediaID = NULL, RecipientIdentificationNumber = NULL, StateCode = NULL, StateID = NULL, TaxIdentificationType = NULL, VendorID = NULL, VendorNameElectronicFileLine1 = NULL, VendorNameElectronicFileLine2 = NULL, VendorNamePrintedLine1 = NULL, VendorNamePrintedLine2 = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeAddOn = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendor", body = list(DataObject = body), searchFields = append("Form1099MYearVendorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MYearVendor
	#'
	#' This function modifies a Form1099MYearVendor
	#' @param fieldNames The field values to give the modified Form1099MYearVendor. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MYearVendor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MYearVendor <- function(Form1099MYearVendorID, AddressID = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, CityElectronicFile = NULL, CityPrinted = NULL, Form1099MYearID = NULL, HasPayerMade5000DirectSales = NULL, MediaID = NULL, RecipientIdentificationNumber = NULL, StateCode = NULL, StateID = NULL, TaxIdentificationType = NULL, VendorID = NULL, VendorNameElectronicFileLine1 = NULL, VendorNameElectronicFileLine2 = NULL, VendorNamePrintedLine1 = NULL, VendorNamePrintedLine2 = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeAddOn = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendor", objectId = Form1099MYearVendorID, body = list(DataObject = body), searchFields = append("Form1099MYearVendorID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MYears
	#'
	#' This function returns a dataframe or json object of Form1099MYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MYears <- function(searchConditionsList = NULL, AddressIDEmployer = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, BusinessIdentificationNumberOR = F, CalendarYear = F, CentralRegistrationNumberMD = F, CityElectronicFile = F, CityPrinted = F, CombinedReportingSystemIdentificationNumberNM = F, ContactPersonElectronicFile = F, CreatedTime = F, DistrictID = F, EmployerID = F, EmployerNameElectronicFile = F, EmployerNamePrinted = F, ExtractedVendorCount = F, FatalExceptionCount = F, FederalIDNumberEmployer = F, FilingCycle = F, Form1099MYearID = F, FormattedPhoneNumber = F, IdentificationNumberNE = F, IsLocked = F, LatestExport = F, ModifiedTime = F, NonFatalExceptionCount = F, PhoneExtensionElectronicFile = F, PhoneExtensionPrinted = F, PhoneNumberElectronicFile = F, PhoneNumberIsInternational = F, PhoneNumberPrinted = F, ReportsCAIncome = F, ReportsDEIncome = F, ReportsGAIncome = F, ReportsIAIncome = F, ReportsIDIncome = F, ReportsILIncome = F, ReportsKYIncome = F, ReportsMDIncome = F, ReportsNCIncome = F, ReportsNEIncome = F, ReportsNMIncome = F, ReportsORIncome = F, ReportsPAIncome = F, ReportsSCIncome = F, ReportsUTIncome = F, ReportsWIIncome = F, ReportsWVIncome = F, StateCode = F, StateID = F, StateIdentificationNumberDE = F, TaxPeriodMonth = F, TaxPeriodYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WithholdingAccountNumberGA = F, WithholdingAccountNumberID = F, WithholdingAccountNumberNC = F, WithholdingAccountNumberUT = F, WithholdingFileNumberSC = F, WithholdingPermitNumberIA = F, WithholdingTaxAccountNumberKY = F, WithholdingTaxAccountNumberPA = F, WithholdingTaxNumberWI = F, ZipCode = F, ZipCodeAddOn = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MYear
	#'
	#' This function returns a dataframe or json object of a Form1099MYear
	#' @param Form1099MYearID The ID of the Form1099MYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MYear <- function(Form1099MYearID, AddressIDEmployer = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, BusinessIdentificationNumberOR = F, CalendarYear = F, CentralRegistrationNumberMD = F, CityElectronicFile = F, CityPrinted = F, CombinedReportingSystemIdentificationNumberNM = F, ContactPersonElectronicFile = F, CreatedTime = F, DistrictID = F, EmployerID = F, EmployerNameElectronicFile = F, EmployerNamePrinted = F, ExtractedVendorCount = F, FatalExceptionCount = F, FederalIDNumberEmployer = F, FilingCycle = F, FormattedPhoneNumber = F, IdentificationNumberNE = F, IsLocked = F, LatestExport = F, ModifiedTime = F, NonFatalExceptionCount = F, PhoneExtensionElectronicFile = F, PhoneExtensionPrinted = F, PhoneNumberElectronicFile = F, PhoneNumberIsInternational = F, PhoneNumberPrinted = F, ReportsCAIncome = F, ReportsDEIncome = F, ReportsGAIncome = F, ReportsIAIncome = F, ReportsIDIncome = F, ReportsILIncome = F, ReportsKYIncome = F, ReportsMDIncome = F, ReportsNCIncome = F, ReportsNEIncome = F, ReportsNMIncome = F, ReportsORIncome = F, ReportsPAIncome = F, ReportsSCIncome = F, ReportsUTIncome = F, ReportsWIIncome = F, ReportsWVIncome = F, StateCode = F, StateID = F, StateIdentificationNumberDE = F, TaxPeriodMonth = F, TaxPeriodYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WithholdingAccountNumberGA = F, WithholdingAccountNumberID = F, WithholdingAccountNumberNC = F, WithholdingAccountNumberUT = F, WithholdingFileNumberSC = F, WithholdingPermitNumberIA = F, WithholdingTaxAccountNumberKY = F, WithholdingTaxAccountNumberPA = F, WithholdingTaxNumberWI = F, ZipCode = F, ZipCodeAddOn = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MYear", objectId = Form1099MYearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MYear
	#'
	#' This function deletes a Form1099MYear
	#' @param Form1099MYearID The ID of the Form1099MYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MYearID of the deleted Form1099MYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MYear <- function(Form1099MYearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MYear", objectId = Form1099MYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MYear
	#'
	#' This function creates a Form1099MYear
	#' @param fieldNames The field values to give the created Form1099MYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MYear <- function(AddressIDEmployer = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, BusinessIdentificationNumberOR = NULL, CalendarYear = NULL, CentralRegistrationNumberMD = NULL, CityElectronicFile = NULL, CityPrinted = NULL, CombinedReportingSystemIdentificationNumberNM = NULL, ContactPersonElectronicFile = NULL, DistrictID = NULL, EmployerID = NULL, EmployerNameElectronicFile = NULL, EmployerNamePrinted = NULL, FederalIDNumberEmployer = NULL, FilingCycle = NULL, IdentificationNumberNE = NULL, PhoneExtensionElectronicFile = NULL, PhoneExtensionPrinted = NULL, PhoneNumberElectronicFile = NULL, PhoneNumberIsInternational = NULL, PhoneNumberPrinted = NULL, StateCode = NULL, StateID = NULL, StateIdentificationNumberDE = NULL, TaxPeriodMonth = NULL, TaxPeriodYear = NULL, WithholdingAccountNumberGA = NULL, WithholdingAccountNumberID = NULL, WithholdingAccountNumberNC = NULL, WithholdingAccountNumberUT = NULL, WithholdingFileNumberSC = NULL, WithholdingPermitNumberIA = NULL, WithholdingTaxAccountNumberKY = NULL, WithholdingTaxAccountNumberPA = NULL, WithholdingTaxNumberWI = NULL, ZipCode = NULL, ZipCodeAddOn = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MYear", body = list(DataObject = body), searchFields = append("Form1099MYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MYear
	#'
	#' This function modifies a Form1099MYear
	#' @param fieldNames The field values to give the modified Form1099MYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MYear <- function(Form1099MYearID, AddressIDEmployer = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, BusinessIdentificationNumberOR = NULL, CalendarYear = NULL, CentralRegistrationNumberMD = NULL, CityElectronicFile = NULL, CityPrinted = NULL, CombinedReportingSystemIdentificationNumberNM = NULL, ContactPersonElectronicFile = NULL, DistrictID = NULL, EmployerID = NULL, EmployerNameElectronicFile = NULL, EmployerNamePrinted = NULL, FederalIDNumberEmployer = NULL, FilingCycle = NULL, IdentificationNumberNE = NULL, PhoneExtensionElectronicFile = NULL, PhoneExtensionPrinted = NULL, PhoneNumberElectronicFile = NULL, PhoneNumberIsInternational = NULL, PhoneNumberPrinted = NULL, StateCode = NULL, StateID = NULL, StateIdentificationNumberDE = NULL, TaxPeriodMonth = NULL, TaxPeriodYear = NULL, WithholdingAccountNumberGA = NULL, WithholdingAccountNumberID = NULL, WithholdingAccountNumberNC = NULL, WithholdingAccountNumberUT = NULL, WithholdingFileNumberSC = NULL, WithholdingPermitNumberIA = NULL, WithholdingTaxAccountNumberKY = NULL, WithholdingTaxAccountNumberPA = NULL, WithholdingTaxNumberWI = NULL, ZipCode = NULL, ZipCodeAddOn = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MYear", objectId = Form1099MYearID, body = list(DataObject = body), searchFields = append("Form1099MYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearPayTypes
	#'
	#' This function returns a dataframe or json object of W2YearPayTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearPayTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearPayTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearPayType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearPayTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearPayTypes <- function(searchConditionsList = NULL, CreatedTime = F, FederalW2BoxID = F, ModifiedTime = F, PayTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearID = F, W2YearPayTypeID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearPayType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearPayType
	#'
	#' This function returns a dataframe or json object of a W2YearPayType
	#' @param W2YearPayTypeID The ID of the W2YearPayType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearPayType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearPayType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearPayType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearPayType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearPayType <- function(W2YearPayTypeID, CreatedTime = F, FederalW2BoxID = F, ModifiedTime = F, PayTypeID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearPayTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearPayType", objectId = W2YearPayTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearPayType
	#'
	#' This function deletes a W2YearPayType
	#' @param W2YearPayTypeID The ID of the W2YearPayType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearPayTypeID of the deleted W2YearPayType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearPayType <- function(W2YearPayTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearPayType", objectId = W2YearPayTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearPayType
	#'
	#' This function creates a W2YearPayType
	#' @param fieldNames The field values to give the created W2YearPayType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearPayType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearPayType <- function(FederalW2BoxID = NULL, PayTypeID = NULL, W2YearBox14SetID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearPayType", body = list(DataObject = body), searchFields = append("W2YearPayTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearPayType
	#'
	#' This function modifies a W2YearPayType
	#' @param fieldNames The field values to give the modified W2YearPayType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearPayType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearPayType <- function(W2YearPayTypeID, FederalW2BoxID = NULL, PayTypeID = NULL, W2YearBox14SetID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearPayType", objectId = W2YearPayTypeID, body = list(DataObject = body), searchFields = append("W2YearPayTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearBenefits
	#'
	#' This function returns a dataframe or json object of W2YearBenefits
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearBenefits. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearBenefits.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearBenefit') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearBenefits
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearBenefits <- function(searchConditionsList = NULL, BenefitID = F, CreatedTime = F, FederalW2BoxID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBenefitID = F, W2YearBox14SetID = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearBenefit", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearBenefit
	#'
	#' This function returns a dataframe or json object of a W2YearBenefit
	#' @param W2YearBenefitID The ID of the W2YearBenefit to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearBenefit. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearBenefit.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearBenefit') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearBenefit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearBenefit <- function(W2YearBenefitID, BenefitID = F, CreatedTime = F, FederalW2BoxID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearBenefitID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearBenefit", objectId = W2YearBenefitID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearBenefit
	#'
	#' This function deletes a W2YearBenefit
	#' @param W2YearBenefitID The ID of the W2YearBenefit to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearBenefitID of the deleted W2YearBenefit.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearBenefit <- function(W2YearBenefitID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearBenefit", objectId = W2YearBenefitID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearBenefit
	#'
	#' This function creates a W2YearBenefit
	#' @param fieldNames The field values to give the created W2YearBenefit. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearBenefit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearBenefit <- function(BenefitID = NULL, FederalW2BoxID = NULL, W2YearBox14SetID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearBenefit", body = list(DataObject = body), searchFields = append("W2YearBenefitID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearBenefit
	#'
	#' This function modifies a W2YearBenefit
	#' @param fieldNames The field values to give the modified W2YearBenefit. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearBenefit
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearBenefit <- function(W2YearBenefitID, BenefitID = NULL, FederalW2BoxID = NULL, W2YearBox14SetID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearBenefit", objectId = W2YearBenefitID, body = list(DataObject = body), searchFields = append("W2YearBenefitID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearDeductions
	#'
	#' This function returns a dataframe or json object of W2YearDeductions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearDeductions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearDeductions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearDeduction') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearDeductions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearDeductions <- function(searchConditionsList = NULL, CreatedTime = F, DeductionID = F, FederalW2BoxID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearDeductionID = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearDeduction", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearDeduction
	#'
	#' This function returns a dataframe or json object of a W2YearDeduction
	#' @param W2YearDeductionID The ID of the W2YearDeduction to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearDeduction. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearDeduction.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearDeduction') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearDeduction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearDeduction <- function(W2YearDeductionID, CreatedTime = F, DeductionID = F, FederalW2BoxID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearBox14SetID = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearDeductionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearDeduction", objectId = W2YearDeductionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearDeduction
	#'
	#' This function deletes a W2YearDeduction
	#' @param W2YearDeductionID The ID of the W2YearDeduction to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearDeductionID of the deleted W2YearDeduction.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearDeduction <- function(W2YearDeductionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearDeduction", objectId = W2YearDeductionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearDeduction
	#'
	#' This function creates a W2YearDeduction
	#' @param fieldNames The field values to give the created W2YearDeduction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearDeduction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearDeduction <- function(DeductionID = NULL, FederalW2BoxID = NULL, W2YearBox14SetID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearDeduction", body = list(DataObject = body), searchFields = append("W2YearDeductionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearDeduction
	#'
	#' This function modifies a W2YearDeduction
	#' @param fieldNames The field values to give the modified W2YearDeduction. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearDeduction
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearDeduction <- function(W2YearDeductionID, DeductionID = NULL, FederalW2BoxID = NULL, W2YearBox14SetID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearDeduction", objectId = W2YearDeductionID, body = list(DataObject = body), searchFields = append("W2YearDeductionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearTaxStates
	#'
	#' This function returns a dataframe or json object of W2YearTaxStates
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearTaxStates. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearTaxStates.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearTaxState') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearTaxStates
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearTaxStates <- function(searchConditionsList = NULL, CreatedTime = F, EmployerIdentificationNumberState = F, ModifiedTime = F, State = F, TaxStateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, W2YearTaxStateID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearTaxState", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearTaxState
	#'
	#' This function returns a dataframe or json object of a W2YearTaxState
	#' @param W2YearTaxStateID The ID of the W2YearTaxState to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearTaxState. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearTaxState.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearTaxState') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearTaxState
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearTaxState <- function(W2YearTaxStateID, CreatedTime = F, EmployerIdentificationNumberState = F, ModifiedTime = F, State = F, TaxStateID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearTaxStateID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearTaxState", objectId = W2YearTaxStateID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearTaxState
	#'
	#' This function deletes a W2YearTaxState
	#' @param W2YearTaxStateID The ID of the W2YearTaxState to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearTaxStateID of the deleted W2YearTaxState.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearTaxState <- function(W2YearTaxStateID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearTaxState", objectId = W2YearTaxStateID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearTaxState
	#'
	#' This function creates a W2YearTaxState
	#' @param fieldNames The field values to give the created W2YearTaxState. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearTaxState
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearTaxState <- function(EmployerIdentificationNumberState = NULL, State = NULL, TaxStateID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearTaxState", body = list(DataObject = body), searchFields = append("W2YearTaxStateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearTaxState
	#'
	#' This function modifies a W2YearTaxState
	#' @param fieldNames The field values to give the modified W2YearTaxState. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearTaxState
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearTaxState <- function(W2YearTaxStateID, EmployerIdentificationNumberState = NULL, State = NULL, TaxStateID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearTaxState", objectId = W2YearTaxStateID, body = list(DataObject = body), searchFields = append("W2YearTaxStateID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearBox14Sets
	#'
	#' This function returns a dataframe or json object of W2YearBox14Sets
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearBox14Sets. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearBox14Sets.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearBox14Set') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearBox14Sets
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearBox14Sets <- function(searchConditionsList = NULL, CreatedTime = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2Box14SetID = F, W2Description = F, W2YearBox14SetID = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearBox14Set", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearBox14Set
	#'
	#' This function returns a dataframe or json object of a W2YearBox14Set
	#' @param W2YearBox14SetID The ID of the W2YearBox14Set to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearBox14Set. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearBox14Set.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearBox14Set') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearBox14Set
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearBox14Set <- function(W2YearBox14SetID, CreatedTime = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2Box14SetID = F, W2Description = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearBox14SetID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearBox14Set", objectId = W2YearBox14SetID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearBox14Set
	#'
	#' This function deletes a W2YearBox14Set
	#' @param W2YearBox14SetID The ID of the W2YearBox14Set to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearBox14SetID of the deleted W2YearBox14Set.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearBox14Set <- function(W2YearBox14SetID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearBox14Set", objectId = W2YearBox14SetID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearBox14Set
	#'
	#' This function creates a W2YearBox14Set
	#' @param fieldNames The field values to give the created W2YearBox14Set. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearBox14Set
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearBox14Set <- function(W2Box14SetID = NULL, W2Description = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearBox14Set", body = list(DataObject = body), searchFields = append("W2YearBox14SetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearBox14Set
	#'
	#' This function modifies a W2YearBox14Set
	#' @param fieldNames The field values to give the modified W2YearBox14Set. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearBox14Set
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearBox14Set <- function(W2YearBox14SetID, W2Box14SetID = NULL, W2Description = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearBox14Set", objectId = W2YearBox14SetID, body = list(DataObject = body), searchFields = append("W2YearBox14SetID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2Years
	#'
	#' This function returns a dataframe or json object of W2Years
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2Years. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2Years.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2Year') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2Years
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2Years <- function(searchConditionsList = NULL, Box13RetirementOption = F, CalendarYear = F, CreatedTime = F, DistrictID = F, EmployerAddressType = F, EmployerCityPrinted = F, EmployerCountryCodePrinted = F, EmployerDeliveryAddressPrinted = F, EmployerID = F, EmployerIdentificationNumber = F, EmployerInternationalProvinceIDPrinted = F, EmployerInternationalStateOrProvinceCodePrinted = F, EmployerLocationAddressPrinted = F, EmployerNamePrinted = F, EmployerPostalCodePrinted = F, EmployerStateCodePrinted = F, EmployerStateIDPrinted = F, EmployerW2CountryIDPrinted = F, EmployerZipCodeExtensionPrinted = F, EmployerZipCodePrinted = F, Exclude0WageEmployeesWithOnlyEmployerSponsoredHealthCoverage = F, ExtractedEmployeeCount = F, FatalExceptionCount = F, FederalW2KindOfEmployerID = F, IsAvailableInEmployeeAccess = F, IsLocked = F, IsSubmitted = F, IsW2Audit = F, LatestExport = F, ModifiedTime = F, NonFatalExceptionCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2Year", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2Year
	#'
	#' This function returns a dataframe or json object of a W2Year
	#' @param W2YearID The ID of the W2Year to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2Year. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2Year.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2Year') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2Year
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2Year <- function(W2YearID, Box13RetirementOption = F, CalendarYear = F, CreatedTime = F, DistrictID = F, EmployerAddressType = F, EmployerCityPrinted = F, EmployerCountryCodePrinted = F, EmployerDeliveryAddressPrinted = F, EmployerID = F, EmployerIdentificationNumber = F, EmployerInternationalProvinceIDPrinted = F, EmployerInternationalStateOrProvinceCodePrinted = F, EmployerLocationAddressPrinted = F, EmployerNamePrinted = F, EmployerPostalCodePrinted = F, EmployerStateCodePrinted = F, EmployerStateIDPrinted = F, EmployerW2CountryIDPrinted = F, EmployerZipCodeExtensionPrinted = F, EmployerZipCodePrinted = F, Exclude0WageEmployeesWithOnlyEmployerSponsoredHealthCoverage = F, ExtractedEmployeeCount = F, FatalExceptionCount = F, FederalW2KindOfEmployerID = F, IsAvailableInEmployeeAccess = F, IsLocked = F, IsSubmitted = F, IsW2Audit = F, LatestExport = F, ModifiedTime = F, NonFatalExceptionCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2Year", objectId = W2YearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2Year
	#'
	#' This function deletes a W2Year
	#' @param W2YearID The ID of the W2Year to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearID of the deleted W2Year.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2Year <- function(W2YearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2Year", objectId = W2YearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2Year
	#'
	#' This function creates a W2Year
	#' @param fieldNames The field values to give the created W2Year. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2Year
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2Year <- function(Box13RetirementOption = NULL, CalendarYear = NULL, DistrictID = NULL, EmployerAddressType = NULL, EmployerCityPrinted = NULL, EmployerCountryCodePrinted = NULL, EmployerDeliveryAddressPrinted = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, EmployerInternationalProvinceIDPrinted = NULL, EmployerInternationalStateOrProvinceCodePrinted = NULL, EmployerLocationAddressPrinted = NULL, EmployerNamePrinted = NULL, EmployerPostalCodePrinted = NULL, EmployerStateCodePrinted = NULL, EmployerStateIDPrinted = NULL, EmployerW2CountryIDPrinted = NULL, EmployerZipCodeExtensionPrinted = NULL, EmployerZipCodePrinted = NULL, Exclude0WageEmployeesWithOnlyEmployerSponsoredHealthCoverage = NULL, FederalW2KindOfEmployerID = NULL, IsAvailableInEmployeeAccess = NULL, IsSubmitted = NULL, IsW2Audit = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2Year", body = list(DataObject = body), searchFields = append("W2YearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2Year
	#'
	#' This function modifies a W2Year
	#' @param fieldNames The field values to give the modified W2Year. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2Year
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2Year <- function(W2YearID, Box13RetirementOption = NULL, CalendarYear = NULL, DistrictID = NULL, EmployerAddressType = NULL, EmployerCityPrinted = NULL, EmployerCountryCodePrinted = NULL, EmployerDeliveryAddressPrinted = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, EmployerInternationalProvinceIDPrinted = NULL, EmployerInternationalStateOrProvinceCodePrinted = NULL, EmployerLocationAddressPrinted = NULL, EmployerNamePrinted = NULL, EmployerPostalCodePrinted = NULL, EmployerStateCodePrinted = NULL, EmployerStateIDPrinted = NULL, EmployerW2CountryIDPrinted = NULL, EmployerZipCodeExtensionPrinted = NULL, EmployerZipCodePrinted = NULL, Exclude0WageEmployeesWithOnlyEmployerSponsoredHealthCoverage = NULL, FederalW2KindOfEmployerID = NULL, IsAvailableInEmployeeAccess = NULL, IsSubmitted = NULL, IsW2Audit = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2Year", objectId = W2YearID, body = list(DataObject = body), searchFields = append("W2YearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AffordableCareActEmployeeWeekV1s
	#'
	#' This function returns a dataframe or json object of AffordableCareActEmployeeWeekV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AffordableCareActEmployeeWeekV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AffordableCareActEmployeeWeekV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AffordableCareActEmployeeWeekV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of AffordableCareActEmployeeWeekV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAffordableCareActEmployeeWeekV1s <- function(searchConditionsList = NULL, AffordableCareActEmployeeWeekV1ID = F, AffordableCareActExtractRunID = F, Amount = F, CreatedTime = F, EmployeeID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WeekStartDate = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "AffordableCareActEmployeeWeekV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AffordableCareActEmployeeWeekV1
	#'
	#' This function returns a dataframe or json object of an AffordableCareActEmployeeWeekV1
	#' @param AffordableCareActEmployeeWeekV1ID The ID of the AffordableCareActEmployeeWeekV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AffordableCareActEmployeeWeekV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AffordableCareActEmployeeWeekV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AffordableCareActEmployeeWeekV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of AffordableCareActEmployeeWeekV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAffordableCareActEmployeeWeekV1 <- function(AffordableCareActEmployeeWeekV1ID, AffordableCareActExtractRunID = F, Amount = F, CreatedTime = F, EmployeeID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, WeekStartDate = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AffordableCareActEmployeeWeekV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "AffordableCareActEmployeeWeekV1", objectId = AffordableCareActEmployeeWeekV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AffordableCareActEmployeeWeekV1
	#'
	#' This function deletes an AffordableCareActEmployeeWeekV1
	#' @param AffordableCareActEmployeeWeekV1ID The ID of the AffordableCareActEmployeeWeekV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The AffordableCareActEmployeeWeekV1ID of the deleted AffordableCareActEmployeeWeekV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAffordableCareActEmployeeWeekV1 <- function(AffordableCareActEmployeeWeekV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "AffordableCareActEmployeeWeekV1", objectId = AffordableCareActEmployeeWeekV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AffordableCareActEmployeeWeekV1
	#'
	#' This function creates an AffordableCareActEmployeeWeekV1
	#' @param fieldNames The field values to give the created AffordableCareActEmployeeWeekV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created AffordableCareActEmployeeWeekV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAffordableCareActEmployeeWeekV1 <- function(AffordableCareActExtractRunID = NULL, Amount = NULL, EmployeeID = NULL, WeekStartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "AffordableCareActEmployeeWeekV1", body = list(DataObject = body), searchFields = append("AffordableCareActEmployeeWeekV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AffordableCareActEmployeeWeekV1
	#'
	#' This function modifies an AffordableCareActEmployeeWeekV1
	#' @param fieldNames The field values to give the modified AffordableCareActEmployeeWeekV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified AffordableCareActEmployeeWeekV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAffordableCareActEmployeeWeekV1 <- function(AffordableCareActEmployeeWeekV1ID, AffordableCareActExtractRunID = NULL, Amount = NULL, EmployeeID = NULL, WeekStartDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "AffordableCareActEmployeeWeekV1", objectId = AffordableCareActEmployeeWeekV1ID, body = list(DataObject = body), searchFields = append("AffordableCareActEmployeeWeekV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List AffordableCareActExtractRuns
	#'
	#' This function returns a dataframe or json object of AffordableCareActExtractRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AffordableCareActExtractRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AffordableCareActExtractRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AffordableCareActExtractRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of AffordableCareActExtractRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listAffordableCareActExtractRuns <- function(searchConditionsList = NULL, AffordableCareActExtractRunID = F, CreatedTime = F, Description = F, DistrictID = F, EndDate = F, ModifiedTime = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, XMLFilter = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "AffordableCareActExtractRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an AffordableCareActExtractRun
	#'
	#' This function returns a dataframe or json object of an AffordableCareActExtractRun
	#' @param AffordableCareActExtractRunID The ID of the AffordableCareActExtractRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given AffordableCareActExtractRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the AffordableCareActExtractRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('AffordableCareActExtractRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of AffordableCareActExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getAffordableCareActExtractRun <- function(AffordableCareActExtractRunID, CreatedTime = F, Description = F, DistrictID = F, EndDate = F, ModifiedTime = F, StartDate = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, XMLFilter = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "AffordableCareActExtractRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "AffordableCareActExtractRun", objectId = AffordableCareActExtractRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an AffordableCareActExtractRun
	#'
	#' This function deletes an AffordableCareActExtractRun
	#' @param AffordableCareActExtractRunID The ID of the AffordableCareActExtractRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The AffordableCareActExtractRunID of the deleted AffordableCareActExtractRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteAffordableCareActExtractRun <- function(AffordableCareActExtractRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "AffordableCareActExtractRun", objectId = AffordableCareActExtractRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an AffordableCareActExtractRun
	#'
	#' This function creates an AffordableCareActExtractRun
	#' @param fieldNames The field values to give the created AffordableCareActExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created AffordableCareActExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createAffordableCareActExtractRun <- function(Description = NULL, DistrictID = NULL, EndDate = NULL, StartDate = NULL, Version = NULL, XMLFilter = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "AffordableCareActExtractRun", body = list(DataObject = body), searchFields = append("AffordableCareActExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an AffordableCareActExtractRun
	#'
	#' This function modifies an AffordableCareActExtractRun
	#' @param fieldNames The field values to give the modified AffordableCareActExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified AffordableCareActExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyAffordableCareActExtractRun <- function(AffordableCareActExtractRunID, Description = NULL, DistrictID = NULL, EndDate = NULL, StartDate = NULL, Version = NULL, XMLFilter = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "AffordableCareActExtractRun", objectId = AffordableCareActExtractRunID, body = list(DataObject = body), searchFields = append("AffordableCareActExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form941Exports
	#'
	#' This function returns a dataframe or json object of Form941Exports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941Exports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941Exports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941Export') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form941Exports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm941Exports <- function(searchConditionsList = NULL, CreatedTime = F, Form = F, Form941ExportID = F, Form941ExtractRunID = F, MediaID = F, ModifiedTime = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form941Export", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form941Export
	#'
	#' This function returns a dataframe or json object of a Form941Export
	#' @param Form941ExportID The ID of the Form941Export to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941Export. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941Export.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941Export') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form941Export
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm941Export <- function(Form941ExportID, CreatedTime = F, Form = F, Form941ExtractRunID = F, MediaID = F, ModifiedTime = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form941ExportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form941Export", objectId = Form941ExportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form941Export
	#'
	#' This function deletes a Form941Export
	#' @param Form941ExportID The ID of the Form941Export to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form941ExportID of the deleted Form941Export.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm941Export <- function(Form941ExportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form941Export", objectId = Form941ExportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form941Export
	#'
	#' This function creates a Form941Export
	#' @param fieldNames The field values to give the created Form941Export. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form941Export
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm941Export <- function(Form = NULL, Form941ExtractRunID = NULL, MediaID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form941Export", body = list(DataObject = body), searchFields = append("Form941ExportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form941Export
	#'
	#' This function modifies a Form941Export
	#' @param fieldNames The field values to give the modified Form941Export. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form941Export
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm941Export <- function(Form941ExportID, Form = NULL, Form941ExtractRunID = NULL, MediaID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form941Export", objectId = Form941ExportID, body = list(DataObject = body), searchFields = append("Form941ExportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form941V1Days
	#'
	#' This function returns a dataframe or json object of Form941V1Days
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941V1Days. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941V1Days.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941V1Day') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form941V1Days
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm941V1Days <- function(searchConditionsList = NULL, CreatedTime = F, Date = F, Form941V1DayID = F, Form941V1ID = F, ModifiedTime = F, NonRefundableEmployeeRetentionCredit = F, NonRefundableQualifiedSickAndFamilyLeaveCredit = F, ReportedTaxLiability = F, TaxLiability = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form941V1Day", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form941V1Day
	#'
	#' This function returns a dataframe or json object of a Form941V1Day
	#' @param Form941V1DayID The ID of the Form941V1Day to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941V1Day. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941V1Day.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941V1Day') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form941V1Day
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm941V1Day <- function(Form941V1DayID, CreatedTime = F, Date = F, Form941V1ID = F, ModifiedTime = F, NonRefundableEmployeeRetentionCredit = F, NonRefundableQualifiedSickAndFamilyLeaveCredit = F, ReportedTaxLiability = F, TaxLiability = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form941V1DayID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form941V1Day", objectId = Form941V1DayID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form941V1Day
	#'
	#' This function deletes a Form941V1Day
	#' @param Form941V1DayID The ID of the Form941V1Day to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form941V1DayID of the deleted Form941V1Day.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm941V1Day <- function(Form941V1DayID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form941V1Day", objectId = Form941V1DayID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form941V1Day
	#'
	#' This function creates a Form941V1Day
	#' @param fieldNames The field values to give the created Form941V1Day. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form941V1Day
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm941V1Day <- function(Date = NULL, Form941V1ID = NULL, NonRefundableEmployeeRetentionCredit = NULL, NonRefundableQualifiedSickAndFamilyLeaveCredit = NULL, TaxLiability = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form941V1Day", body = list(DataObject = body), searchFields = append("Form941V1DayID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form941V1Day
	#'
	#' This function modifies a Form941V1Day
	#' @param fieldNames The field values to give the modified Form941V1Day. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form941V1Day
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm941V1Day <- function(Form941V1DayID, Date = NULL, Form941V1ID = NULL, NonRefundableEmployeeRetentionCredit = NULL, NonRefundableQualifiedSickAndFamilyLeaveCredit = NULL, TaxLiability = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form941V1Day", objectId = Form941V1DayID, body = list(DataObject = body), searchFields = append("Form941V1DayID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form941ExtractRunIncludedPayrolls
	#'
	#' This function returns a dataframe or json object of Form941ExtractRunIncludedPayrolls
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941ExtractRunIncludedPayrolls. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941ExtractRunIncludedPayrolls.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941ExtractRunIncludedPayroll') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form941ExtractRunIncludedPayrolls
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm941ExtractRunIncludedPayrolls <- function(searchConditionsList = NULL, CreatedTime = F, Form941ExtractRunID = F, Form941ExtractRunIncludedPayrollID = F, ModifiedTime = F, PayrollRunID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form941ExtractRunIncludedPayroll", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form941ExtractRunIncludedPayroll
	#'
	#' This function returns a dataframe or json object of a Form941ExtractRunIncludedPayroll
	#' @param Form941ExtractRunIncludedPayrollID The ID of the Form941ExtractRunIncludedPayroll to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941ExtractRunIncludedPayroll. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941ExtractRunIncludedPayroll.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941ExtractRunIncludedPayroll') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form941ExtractRunIncludedPayroll
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm941ExtractRunIncludedPayroll <- function(Form941ExtractRunIncludedPayrollID, CreatedTime = F, Form941ExtractRunID = F, ModifiedTime = F, PayrollRunID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form941ExtractRunIncludedPayrollID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRunIncludedPayroll", objectId = Form941ExtractRunIncludedPayrollID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form941ExtractRunIncludedPayroll
	#'
	#' This function deletes a Form941ExtractRunIncludedPayroll
	#' @param Form941ExtractRunIncludedPayrollID The ID of the Form941ExtractRunIncludedPayroll to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form941ExtractRunIncludedPayrollID of the deleted Form941ExtractRunIncludedPayroll.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm941ExtractRunIncludedPayroll <- function(Form941ExtractRunIncludedPayrollID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRunIncludedPayroll", objectId = Form941ExtractRunIncludedPayrollID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form941ExtractRunIncludedPayroll
	#'
	#' This function creates a Form941ExtractRunIncludedPayroll
	#' @param fieldNames The field values to give the created Form941ExtractRunIncludedPayroll. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form941ExtractRunIncludedPayroll
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm941ExtractRunIncludedPayroll <- function(Form941ExtractRunID = NULL, PayrollRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRunIncludedPayroll", body = list(DataObject = body), searchFields = append("Form941ExtractRunIncludedPayrollID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form941ExtractRunIncludedPayroll
	#'
	#' This function modifies a Form941ExtractRunIncludedPayroll
	#' @param fieldNames The field values to give the modified Form941ExtractRunIncludedPayroll. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form941ExtractRunIncludedPayroll
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm941ExtractRunIncludedPayroll <- function(Form941ExtractRunIncludedPayrollID, Form941ExtractRunID = NULL, PayrollRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRunIncludedPayroll", objectId = Form941ExtractRunIncludedPayrollID, body = list(DataObject = body), searchFields = append("Form941ExtractRunIncludedPayrollID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form941ExtractRuns
	#'
	#' This function returns a dataframe or json object of Form941ExtractRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941ExtractRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941ExtractRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941ExtractRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form941ExtractRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm941ExtractRuns <- function(searchConditionsList = NULL, CreatedTime = F, DistrictIDExtractedFor = F, FatalExceptionCount = F, Form941ExtractRunID = F, LatestExport = F, LatestForm941Export = F, LatestForm941ScheduleBExport = F, LatestSentOrAcceptedExportTime = F, ModifiedTime = F, NonFatalExceptionCount = F, PostingQuarter = F, PostingQuarterYear = F, PostingYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, XMLFilter = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form941ExtractRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form941ExtractRun
	#'
	#' This function returns a dataframe or json object of a Form941ExtractRun
	#' @param Form941ExtractRunID The ID of the Form941ExtractRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941ExtractRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941ExtractRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941ExtractRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form941ExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm941ExtractRun <- function(Form941ExtractRunID, CreatedTime = F, DistrictIDExtractedFor = F, FatalExceptionCount = F, LatestExport = F, LatestForm941Export = F, LatestForm941ScheduleBExport = F, LatestSentOrAcceptedExportTime = F, ModifiedTime = F, NonFatalExceptionCount = F, PostingQuarter = F, PostingQuarterYear = F, PostingYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, XMLFilter = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form941ExtractRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRun", objectId = Form941ExtractRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form941ExtractRun
	#'
	#' This function deletes a Form941ExtractRun
	#' @param Form941ExtractRunID The ID of the Form941ExtractRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form941ExtractRunID of the deleted Form941ExtractRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm941ExtractRun <- function(Form941ExtractRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRun", objectId = Form941ExtractRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form941ExtractRun
	#'
	#' This function creates a Form941ExtractRun
	#' @param fieldNames The field values to give the created Form941ExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form941ExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm941ExtractRun <- function(DistrictIDExtractedFor = NULL, PostingQuarter = NULL, PostingYear = NULL, Version = NULL, XMLFilter = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRun", body = list(DataObject = body), searchFields = append("Form941ExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form941ExtractRun
	#'
	#' This function modifies a Form941ExtractRun
	#' @param fieldNames The field values to give the modified Form941ExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form941ExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm941ExtractRun <- function(Form941ExtractRunID, DistrictIDExtractedFor = NULL, PostingQuarter = NULL, PostingYear = NULL, Version = NULL, XMLFilter = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form941ExtractRun", objectId = Form941ExtractRunID, body = list(DataObject = body), searchFields = append("Form941ExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form941V1s
	#'
	#' This function returns a dataframe or json object of Form941V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form941V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm941V1s <- function(searchConditionsList = NULL, AdditionalTaxableMedicareWages = F, CreatedTime = F, EmployeesPaidOn12th = F, EmployerCity = F, EmployerIdentificationNumber = F, EmployerName = F, EmployerPrimaryAddressLine = F, EmployerState = F, EmployerZip = F, FederalTaxWithheld = F, Form941ExtractRunID = F, Form941V1ID = F, MedicareTaxLiability = F, ModifiedTime = F, NonRefundableEmployeeRetentionCreditTotal = F, NonRefundableQualifiedSickAndFamilyLeaveCreditTotal = F, Quarter = F, SocialSecurityTaxLiability = F, Status = F, TaxableFederalWages = F, TaxableMedicareWages = F, TaxableSocialSecurityWages = F, TaxLiabilityMonth1 = F, TaxLiabilityMonth2 = F, TaxLiabilityMonth3 = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form941V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form941V1
	#'
	#' This function returns a dataframe or json object of a Form941V1
	#' @param Form941V1ID The ID of the Form941V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form941V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm941V1 <- function(Form941V1ID, AdditionalTaxableMedicareWages = F, CreatedTime = F, EmployeesPaidOn12th = F, EmployerCity = F, EmployerIdentificationNumber = F, EmployerName = F, EmployerPrimaryAddressLine = F, EmployerState = F, EmployerZip = F, FederalTaxWithheld = F, Form941ExtractRunID = F, MedicareTaxLiability = F, ModifiedTime = F, NonRefundableEmployeeRetentionCreditTotal = F, NonRefundableQualifiedSickAndFamilyLeaveCreditTotal = F, Quarter = F, SocialSecurityTaxLiability = F, Status = F, TaxableFederalWages = F, TaxableMedicareWages = F, TaxableSocialSecurityWages = F, TaxLiabilityMonth1 = F, TaxLiabilityMonth2 = F, TaxLiabilityMonth3 = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form941V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form941V1", objectId = Form941V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form941V1
	#'
	#' This function deletes a Form941V1
	#' @param Form941V1ID The ID of the Form941V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form941V1ID of the deleted Form941V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm941V1 <- function(Form941V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form941V1", objectId = Form941V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form941V1
	#'
	#' This function creates a Form941V1
	#' @param fieldNames The field values to give the created Form941V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form941V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm941V1 <- function(AdditionalTaxableMedicareWages = NULL, EmployeesPaidOn12th = NULL, EmployerCity = NULL, EmployerIdentificationNumber = NULL, EmployerName = NULL, EmployerPrimaryAddressLine = NULL, EmployerState = NULL, EmployerZip = NULL, FederalTaxWithheld = NULL, Form941ExtractRunID = NULL, MedicareTaxLiability = NULL, Quarter = NULL, SocialSecurityTaxLiability = NULL, Status = NULL, TaxableFederalWages = NULL, TaxableMedicareWages = NULL, TaxableSocialSecurityWages = NULL, TaxLiabilityMonth1 = NULL, TaxLiabilityMonth2 = NULL, TaxLiabilityMonth3 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form941V1", body = list(DataObject = body), searchFields = append("Form941V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form941V1
	#'
	#' This function modifies a Form941V1
	#' @param fieldNames The field values to give the modified Form941V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form941V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm941V1 <- function(Form941V1ID, AdditionalTaxableMedicareWages = NULL, EmployeesPaidOn12th = NULL, EmployerCity = NULL, EmployerIdentificationNumber = NULL, EmployerName = NULL, EmployerPrimaryAddressLine = NULL, EmployerState = NULL, EmployerZip = NULL, FederalTaxWithheld = NULL, Form941ExtractRunID = NULL, MedicareTaxLiability = NULL, Quarter = NULL, SocialSecurityTaxLiability = NULL, Status = NULL, TaxableFederalWages = NULL, TaxableMedicareWages = NULL, TaxableSocialSecurityWages = NULL, TaxLiabilityMonth1 = NULL, TaxLiabilityMonth2 = NULL, TaxLiabilityMonth3 = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form941V1", objectId = Form941V1ID, body = list(DataObject = body), searchFields = append("Form941V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalEEO4Functions
	#'
	#' This function returns a dataframe or json object of FederalEEO4Functions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalEEO4Functions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalEEO4Functions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalEEO4Function') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalEEO4Functions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalEEO4Functions <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, FederalEEO4FunctionID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalEEO4Function", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalEEO4Function
	#'
	#' This function returns a dataframe or json object of a FederalEEO4Function
	#' @param FederalEEO4FunctionID The ID of the FederalEEO4Function to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalEEO4Function. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalEEO4Function.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalEEO4Function') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalEEO4Function
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalEEO4Function <- function(FederalEEO4FunctionID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalEEO4FunctionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalEEO4Function", objectId = FederalEEO4FunctionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalEEO4Function
	#'
	#' This function deletes a FederalEEO4Function
	#' @param FederalEEO4FunctionID The ID of the FederalEEO4Function to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalEEO4FunctionID of the deleted FederalEEO4Function.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalEEO4Function <- function(FederalEEO4FunctionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalEEO4Function", objectId = FederalEEO4FunctionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalEEO4Function
	#'
	#' This function creates a FederalEEO4Function
	#' @param fieldNames The field values to give the created FederalEEO4Function. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalEEO4Function
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalEEO4Function <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalEEO4Function", body = list(DataObject = body), searchFields = append("FederalEEO4FunctionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalEEO4Function
	#'
	#' This function modifies a FederalEEO4Function
	#' @param fieldNames The field values to give the modified FederalEEO4Function. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalEEO4Function
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalEEO4Function <- function(FederalEEO4FunctionID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalEEO4Function", objectId = FederalEEO4FunctionID, body = list(DataObject = body), searchFields = append("FederalEEO4FunctionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalEEOCJobCategories
	#'
	#' This function returns a dataframe or json object of FederalEEOCJobCategories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalEEOCJobCategories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalEEOCJobCategories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalEEOCJobCategory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalEEOCJobCategories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalEEOCJobCategories <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EEOType = F, FederalEEOCJobCategoryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalEEOCJobCategory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalEEOCJobCategory
	#'
	#' This function returns a dataframe or json object of a FederalEEOCJobCategory
	#' @param FederalEEOCJobCategoryID The ID of the FederalEEOCJobCategory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalEEOCJobCategory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalEEOCJobCategory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalEEOCJobCategory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalEEOCJobCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalEEOCJobCategory <- function(FederalEEOCJobCategoryID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, EEOType = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalEEOCJobCategoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalEEOCJobCategory", objectId = FederalEEOCJobCategoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalEEOCJobCategory
	#'
	#' This function deletes a FederalEEOCJobCategory
	#' @param FederalEEOCJobCategoryID The ID of the FederalEEOCJobCategory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalEEOCJobCategoryID of the deleted FederalEEOCJobCategory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalEEOCJobCategory <- function(FederalEEOCJobCategoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalEEOCJobCategory", objectId = FederalEEOCJobCategoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalEEOCJobCategory
	#'
	#' This function creates a FederalEEOCJobCategory
	#' @param fieldNames The field values to give the created FederalEEOCJobCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalEEOCJobCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalEEOCJobCategory <- function(Code = NULL, Description = NULL, EEOType = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalEEOCJobCategory", body = list(DataObject = body), searchFields = append("FederalEEOCJobCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalEEOCJobCategory
	#'
	#' This function modifies a FederalEEOCJobCategory
	#' @param fieldNames The field values to give the modified FederalEEOCJobCategory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalEEOCJobCategory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalEEOCJobCategory <- function(FederalEEOCJobCategoryID, Code = NULL, Description = NULL, EEOType = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalEEOCJobCategory", objectId = FederalEEOCJobCategoryID, body = list(DataObject = body), searchFields = append("FederalEEOCJobCategoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCEmployeeV1s
	#'
	#' This function returns a dataframe or json object of EEOCEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCEmployeeV1s <- function(searchConditionsList = NULL, CreatedTime = F, EEOCEmployeeV1ID = F, EEOCExtractRunID = F, EmployeeID = F, FirstNameLegal = F, Gender = F, IsAmericanIndianOrAlaskanNative = F, IsAsian = F, IsBlackOrAfricanAmerican = F, IsHispanicOrLatino = F, IsMultipleRaces = F, IsNativeHawaiianOrPacificIslander = F, IsWhite = F, LastNameLegal = F, MiddleNameLegal = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCEmployeeV1
	#'
	#' This function returns a dataframe or json object of an EEOCEmployeeV1
	#' @param EEOCEmployeeV1ID The ID of the EEOCEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCEmployeeV1 <- function(EEOCEmployeeV1ID, CreatedTime = F, EEOCExtractRunID = F, EmployeeID = F, FirstNameLegal = F, Gender = F, IsAmericanIndianOrAlaskanNative = F, IsAsian = F, IsBlackOrAfricanAmerican = F, IsHispanicOrLatino = F, IsMultipleRaces = F, IsNativeHawaiianOrPacificIslander = F, IsWhite = F, LastNameLegal = F, MiddleNameLegal = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCEmployeeV1", objectId = EEOCEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCEmployeeV1
	#'
	#' This function deletes an EEOCEmployeeV1
	#' @param EEOCEmployeeV1ID The ID of the EEOCEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCEmployeeV1ID of the deleted EEOCEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCEmployeeV1 <- function(EEOCEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCEmployeeV1", objectId = EEOCEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCEmployeeV1
	#'
	#' This function creates an EEOCEmployeeV1
	#' @param fieldNames The field values to give the created EEOCEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCEmployeeV1 <- function(EEOCExtractRunID = NULL, EmployeeID = NULL, FirstNameLegal = NULL, Gender = NULL, IsAmericanIndianOrAlaskanNative = NULL, IsAsian = NULL, IsBlackOrAfricanAmerican = NULL, IsHispanicOrLatino = NULL, IsMultipleRaces = NULL, IsNativeHawaiianOrPacificIslander = NULL, IsWhite = NULL, LastNameLegal = NULL, MiddleNameLegal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCEmployeeV1", body = list(DataObject = body), searchFields = append("EEOCEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCEmployeeV1
	#'
	#' This function modifies an EEOCEmployeeV1
	#' @param fieldNames The field values to give the modified EEOCEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCEmployeeV1 <- function(EEOCEmployeeV1ID, EEOCExtractRunID = NULL, EmployeeID = NULL, FirstNameLegal = NULL, Gender = NULL, IsAmericanIndianOrAlaskanNative = NULL, IsAsian = NULL, IsBlackOrAfricanAmerican = NULL, IsHispanicOrLatino = NULL, IsMultipleRaces = NULL, IsNativeHawaiianOrPacificIslander = NULL, IsWhite = NULL, LastNameLegal = NULL, MiddleNameLegal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCEmployeeV1", objectId = EEOCEmployeeV1ID, body = list(DataObject = body), searchFields = append("EEOCEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCEmploymentV1s
	#'
	#' This function returns a dataframe or json object of EEOCEmploymentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCEmploymentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCEmploymentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCEmploymentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCEmploymentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCEmploymentV1s <- function(searchConditionsList = NULL, AnnualSalary = F, CreatedTime = F, EEOCEmployeeV1ID = F, EEOCEmploymentV1ID = F, FederalEEOCJobCategoryID = F, FederalEEOCJobCategoryValue = F, IsFullTime = F, IsNewHire = F, ModifiedTime = F, PositionTypeDescription = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCEmploymentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCEmploymentV1
	#'
	#' This function returns a dataframe or json object of an EEOCEmploymentV1
	#' @param EEOCEmploymentV1ID The ID of the EEOCEmploymentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCEmploymentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCEmploymentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCEmploymentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCEmploymentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCEmploymentV1 <- function(EEOCEmploymentV1ID, AnnualSalary = F, CreatedTime = F, EEOCEmployeeV1ID = F, FederalEEOCJobCategoryID = F, FederalEEOCJobCategoryValue = F, IsFullTime = F, IsNewHire = F, ModifiedTime = F, PositionTypeDescription = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCEmploymentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCEmploymentV1", objectId = EEOCEmploymentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCEmploymentV1
	#'
	#' This function deletes an EEOCEmploymentV1
	#' @param EEOCEmploymentV1ID The ID of the EEOCEmploymentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCEmploymentV1ID of the deleted EEOCEmploymentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCEmploymentV1 <- function(EEOCEmploymentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCEmploymentV1", objectId = EEOCEmploymentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCEmploymentV1
	#'
	#' This function creates an EEOCEmploymentV1
	#' @param fieldNames The field values to give the created EEOCEmploymentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCEmploymentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCEmploymentV1 <- function(AnnualSalary = NULL, EEOCEmployeeV1ID = NULL, FederalEEOCJobCategoryID = NULL, FederalEEOCJobCategoryValue = NULL, IsFullTime = NULL, IsNewHire = NULL, PositionTypeDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCEmploymentV1", body = list(DataObject = body), searchFields = append("EEOCEmploymentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCEmploymentV1
	#'
	#' This function modifies an EEOCEmploymentV1
	#' @param fieldNames The field values to give the modified EEOCEmploymentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCEmploymentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCEmploymentV1 <- function(EEOCEmploymentV1ID, AnnualSalary = NULL, EEOCEmployeeV1ID = NULL, FederalEEOCJobCategoryID = NULL, FederalEEOCJobCategoryValue = NULL, IsFullTime = NULL, IsNewHire = NULL, PositionTypeDescription = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCEmploymentV1", objectId = EEOCEmploymentV1ID, body = list(DataObject = body), searchFields = append("EEOCEmploymentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCExtractRunIncludedPayrollRuns
	#'
	#' This function returns a dataframe or json object of EEOCExtractRunIncludedPayrollRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExtractRunIncludedPayrollRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExtractRunIncludedPayrollRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCExtractRunIncludedPayrollRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCExtractRunIncludedPayrollRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCExtractRunIncludedPayrollRuns <- function(searchConditionsList = NULL, CreatedTime = F, EEOCExtractRunID = F, EEOCExtractRunIncludedPayrollRunID = F, ModifiedTime = F, PayrollRunID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCExtractRunIncludedPayrollRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCExtractRunIncludedPayrollRun
	#'
	#' This function returns a dataframe or json object of an EEOCExtractRunIncludedPayrollRun
	#' @param EEOCExtractRunIncludedPayrollRunID The ID of the EEOCExtractRunIncludedPayrollRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExtractRunIncludedPayrollRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExtractRunIncludedPayrollRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCExtractRunIncludedPayrollRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCExtractRunIncludedPayrollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCExtractRunIncludedPayrollRun <- function(EEOCExtractRunIncludedPayrollRunID, CreatedTime = F, EEOCExtractRunID = F, ModifiedTime = F, PayrollRunID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCExtractRunIncludedPayrollRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRunIncludedPayrollRun", objectId = EEOCExtractRunIncludedPayrollRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCExtractRunIncludedPayrollRun
	#'
	#' This function deletes an EEOCExtractRunIncludedPayrollRun
	#' @param EEOCExtractRunIncludedPayrollRunID The ID of the EEOCExtractRunIncludedPayrollRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCExtractRunIncludedPayrollRunID of the deleted EEOCExtractRunIncludedPayrollRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCExtractRunIncludedPayrollRun <- function(EEOCExtractRunIncludedPayrollRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRunIncludedPayrollRun", objectId = EEOCExtractRunIncludedPayrollRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCExtractRunIncludedPayrollRun
	#'
	#' This function creates an EEOCExtractRunIncludedPayrollRun
	#' @param fieldNames The field values to give the created EEOCExtractRunIncludedPayrollRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCExtractRunIncludedPayrollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCExtractRunIncludedPayrollRun <- function(EEOCExtractRunID = NULL, PayrollRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRunIncludedPayrollRun", body = list(DataObject = body), searchFields = append("EEOCExtractRunIncludedPayrollRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCExtractRunIncludedPayrollRun
	#'
	#' This function modifies an EEOCExtractRunIncludedPayrollRun
	#' @param fieldNames The field values to give the modified EEOCExtractRunIncludedPayrollRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCExtractRunIncludedPayrollRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCExtractRunIncludedPayrollRun <- function(EEOCExtractRunIncludedPayrollRunID, EEOCExtractRunID = NULL, PayrollRunID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRunIncludedPayrollRun", objectId = EEOCExtractRunIncludedPayrollRunID, body = list(DataObject = body), searchFields = append("EEOCExtractRunIncludedPayrollRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCExtractRuns
	#'
	#' This function returns a dataframe or json object of EEOCExtractRuns
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExtractRuns. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExtractRuns.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCExtractRun') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCExtractRuns
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCExtractRuns <- function(searchConditionsList = NULL, AddressID = F, AuthorityWithheld = F, City = F, ControlNumber = F, County = F, CreatedTime = F, EEOCExtractRunID = F, EmailAddress = F, EmployerID = F, Enrollment = F, ExtractedEmployeeCount = F, FatalExceptionCount = F, FederalEEO4FunctionID = F, FederalEEO4FunctionValue = F, FiscalYearID = F, JurisdictionName = F, LatestExport = F, LatestSentOrAcceptedExportTime = F, ModifiedTime = F, NameOfCertifyingOfficial = F, NonFatalExceptionCount = F, NumberOfAnnexes = F, OfficeOfSchoolNumber = F, POBox = F, SchoolDistrictName = F, State = F, Street = F, TelephoneNumber = F, TitleOfCertifyingOfficial = F, TotalOfSchools = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, ZipCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCExtractRun", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCExtractRun
	#'
	#' This function returns a dataframe or json object of an EEOCExtractRun
	#' @param EEOCExtractRunID The ID of the EEOCExtractRun to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExtractRun. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExtractRun.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCExtractRun') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCExtractRun <- function(EEOCExtractRunID, AddressID = F, AuthorityWithheld = F, City = F, ControlNumber = F, County = F, CreatedTime = F, EmailAddress = F, EmployerID = F, Enrollment = F, ExtractedEmployeeCount = F, FatalExceptionCount = F, FederalEEO4FunctionID = F, FederalEEO4FunctionValue = F, FiscalYearID = F, JurisdictionName = F, LatestExport = F, LatestSentOrAcceptedExportTime = F, ModifiedTime = F, NameOfCertifyingOfficial = F, NonFatalExceptionCount = F, NumberOfAnnexes = F, OfficeOfSchoolNumber = F, POBox = F, SchoolDistrictName = F, State = F, Street = F, TelephoneNumber = F, TitleOfCertifyingOfficial = F, TotalOfSchools = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, Version = F, ZipCode = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCExtractRunID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRun", objectId = EEOCExtractRunID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCExtractRun
	#'
	#' This function deletes an EEOCExtractRun
	#' @param EEOCExtractRunID The ID of the EEOCExtractRun to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCExtractRunID of the deleted EEOCExtractRun.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCExtractRun <- function(EEOCExtractRunID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRun", objectId = EEOCExtractRunID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCExtractRun
	#'
	#' This function creates an EEOCExtractRun
	#' @param fieldNames The field values to give the created EEOCExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCExtractRun <- function(AddressID = NULL, AuthorityWithheld = NULL, City = NULL, ControlNumber = NULL, County = NULL, EmailAddress = NULL, EmployerID = NULL, Enrollment = NULL, FederalEEO4FunctionID = NULL, FederalEEO4FunctionValue = NULL, FiscalYearID = NULL, JurisdictionName = NULL, NameOfCertifyingOfficial = NULL, NumberOfAnnexes = NULL, OfficeOfSchoolNumber = NULL, POBox = NULL, SchoolDistrictName = NULL, State = NULL, Street = NULL, TelephoneNumber = NULL, TitleOfCertifyingOfficial = NULL, TotalOfSchools = NULL, Type = NULL, Version = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRun", body = list(DataObject = body), searchFields = append("EEOCExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCExtractRun
	#'
	#' This function modifies an EEOCExtractRun
	#' @param fieldNames The field values to give the modified EEOCExtractRun. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCExtractRun
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCExtractRun <- function(EEOCExtractRunID, AddressID = NULL, AuthorityWithheld = NULL, City = NULL, ControlNumber = NULL, County = NULL, EmailAddress = NULL, EmployerID = NULL, Enrollment = NULL, FederalEEO4FunctionID = NULL, FederalEEO4FunctionValue = NULL, FiscalYearID = NULL, JurisdictionName = NULL, NameOfCertifyingOfficial = NULL, NumberOfAnnexes = NULL, OfficeOfSchoolNumber = NULL, POBox = NULL, SchoolDistrictName = NULL, State = NULL, Street = NULL, TelephoneNumber = NULL, TitleOfCertifyingOfficial = NULL, TotalOfSchools = NULL, Type = NULL, Version = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCExtractRun", objectId = EEOCExtractRunID, body = list(DataObject = body), searchFields = append("EEOCExtractRunID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCExceptions
	#'
	#' This function returns a dataframe or json object of EEOCExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCExceptions <- function(searchConditionsList = NULL, CreatedTime = F, EEOCEmployeeV1ID = F, EEOCEmploymentV1ID = F, EEOCExceptionID = F, EEOCExtractRunID = F, IsFatal = F, Message = F, ModifiedTime = F, PreserveException = F, Source = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCException
	#'
	#' This function returns a dataframe or json object of an EEOCException
	#' @param EEOCExceptionID The ID of the EEOCException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCException <- function(EEOCExceptionID, CreatedTime = F, EEOCEmployeeV1ID = F, EEOCEmploymentV1ID = F, EEOCExtractRunID = F, IsFatal = F, Message = F, ModifiedTime = F, PreserveException = F, Source = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCException", objectId = EEOCExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCException
	#'
	#' This function deletes an EEOCException
	#' @param EEOCExceptionID The ID of the EEOCException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCExceptionID of the deleted EEOCException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCException <- function(EEOCExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCException", objectId = EEOCExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCException
	#'
	#' This function creates an EEOCException
	#' @param fieldNames The field values to give the created EEOCException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCException <- function(EEOCEmployeeV1ID = NULL, EEOCEmploymentV1ID = NULL, EEOCExtractRunID = NULL, IsFatal = NULL, Message = NULL, PreserveException = NULL, Source = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCException", body = list(DataObject = body), searchFields = append("EEOCExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCException
	#'
	#' This function modifies an EEOCException
	#' @param fieldNames The field values to give the modified EEOCException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCException <- function(EEOCExceptionID, EEOCEmployeeV1ID = NULL, EEOCEmploymentV1ID = NULL, EEOCExtractRunID = NULL, IsFatal = NULL, Message = NULL, PreserveException = NULL, Source = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCException", objectId = EEOCExceptionID, body = list(DataObject = body), searchFields = append("EEOCExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCExports
	#'
	#' This function returns a dataframe or json object of EEOCExports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCExport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCExports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCExports <- function(searchConditionsList = NULL, CreatedTime = F, EEOCExportID = F, EEOCExtractRunID = F, MediaID = F, ModifiedTime = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCExport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCExport
	#'
	#' This function returns a dataframe or json object of an EEOCExport
	#' @param EEOCExportID The ID of the EEOCExport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCExport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCExport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCExport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCExport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCExport <- function(EEOCExportID, CreatedTime = F, EEOCExtractRunID = F, MediaID = F, ModifiedTime = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCExportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCExport", objectId = EEOCExportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCExport
	#'
	#' This function deletes an EEOCExport
	#' @param EEOCExportID The ID of the EEOCExport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCExportID of the deleted EEOCExport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCExport <- function(EEOCExportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCExport", objectId = EEOCExportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCExport
	#'
	#' This function creates an EEOCExport
	#' @param fieldNames The field values to give the created EEOCExport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCExport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCExport <- function(EEOCExtractRunID = NULL, MediaID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCExport", body = list(DataObject = body), searchFields = append("EEOCExportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCExport
	#'
	#' This function modifies an EEOCExport
	#' @param fieldNames The field values to give the modified EEOCExport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCExport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCExport <- function(EEOCExportID, EEOCExtractRunID = NULL, MediaID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCExport", objectId = EEOCExportID, body = list(DataObject = body), searchFields = append("EEOCExportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of VCRSubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSubmissionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of a VCRSubmissionRunHistory
	#' @param VCRSubmissionRunHistoryID The ID of the VCRSubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSubmissionRunHistory <- function(VCRSubmissionRunHistoryID, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSubmissionRunHistory", objectId = VCRSubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSubmissionRunHistory
	#'
	#' This function deletes a VCRSubmissionRunHistory
	#' @param VCRSubmissionRunHistoryID The ID of the VCRSubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSubmissionRunHistoryID of the deleted VCRSubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSubmissionRunHistory <- function(VCRSubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSubmissionRunHistory", objectId = VCRSubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSubmissionRunHistory
	#'
	#' This function creates a VCRSubmissionRunHistory
	#' @param fieldNames The field values to give the created VCRSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSubmissionRunHistory <- function(EndDateTime = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, VCRSubmissionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSubmissionRunHistory", body = list(DataObject = body), searchFields = append("VCRSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSubmissionRunHistory
	#'
	#' This function modifies a VCRSubmissionRunHistory
	#' @param fieldNames The field values to give the modified VCRSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSubmissionRunHistory <- function(VCRSubmissionRunHistoryID, EndDateTime = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, VCRSubmissionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSubmissionRunHistory", objectId = VCRSubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("VCRSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSubmissions
	#'
	#' This function returns a dataframe or json object of VCRSubmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSubmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSubmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSubmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSubmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSubmissions <- function(searchConditionsList = NULL, ApplicationFreeCategoricalCount = F, ApplicationFreeCount = F, ApplicationReducedCount = F, ApplicationSelectedCount = F, CategoricalNoChangeApplicationCount = F, CategoricalNoChangeStudentCount = F, CategoricalNoResponseApplicationCount = F, CategoricalNoResponseStudentCount = F, CategoricalPaidApplicationCount = F, CategoricalPaidStudentCount = F, CategoricalReducedApplicationCount = F, CategoricalReducedStudentCount = F, CauseVerifiedApplicationCount = F, CreatedTime = F, DirectVerificationApplicationCount = F, DirectVerificationStudentCount = F, DistrictID = F, IncomeNoChangeApplicationCount = F, IncomeNoChangeStudentCount = F, IncomeNoResponseApplicationCount = F, IncomeNoResponseStudentCount = F, IncomePaidApplicationCount = F, IncomePaidStudentCount = F, IncomeReducedApplicationCount = F, IncomeReducedStudentCount = F, ModifiedTime = F, ReducedFreeApplicationCount = F, ReducedFreeStudentCount = F, ReducedNoChangeApplicationCount = F, ReducedNoChangeStudentCount = F, ReducedNoResponseApplicationCount = F, ReducedNoResponseStudentCount = F, ReducedPaidApplicationCount = F, ReducedPaidStudentCount = F, SchoolYearID = F, StudentCertifiedFreeMedicaidCount = F, StudentCertifiedReducedMedicaidCount = F, StudentFreeCategoricalCount = F, StudentFreeCount = F, StudentOtherCertifiedCount = F, StudentReducedCount = F, StudentSNAPCertifiedCount = F, StudentSNAPLetterCertifiedCount = F, TotalErrorProneApplicationCount = F, TotalStudentFreeCount = F, TotalStudentReducedCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSubmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSubmission
	#'
	#' This function returns a dataframe or json object of a VCRSubmission
	#' @param VCRSubmissionID The ID of the VCRSubmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSubmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSubmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSubmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSubmission <- function(VCRSubmissionID, ApplicationFreeCategoricalCount = F, ApplicationFreeCount = F, ApplicationReducedCount = F, ApplicationSelectedCount = F, CategoricalNoChangeApplicationCount = F, CategoricalNoChangeStudentCount = F, CategoricalNoResponseApplicationCount = F, CategoricalNoResponseStudentCount = F, CategoricalPaidApplicationCount = F, CategoricalPaidStudentCount = F, CategoricalReducedApplicationCount = F, CategoricalReducedStudentCount = F, CauseVerifiedApplicationCount = F, CreatedTime = F, DirectVerificationApplicationCount = F, DirectVerificationStudentCount = F, DistrictID = F, IncomeNoChangeApplicationCount = F, IncomeNoChangeStudentCount = F, IncomeNoResponseApplicationCount = F, IncomeNoResponseStudentCount = F, IncomePaidApplicationCount = F, IncomePaidStudentCount = F, IncomeReducedApplicationCount = F, IncomeReducedStudentCount = F, ModifiedTime = F, ReducedFreeApplicationCount = F, ReducedFreeStudentCount = F, ReducedNoChangeApplicationCount = F, ReducedNoChangeStudentCount = F, ReducedNoResponseApplicationCount = F, ReducedNoResponseStudentCount = F, ReducedPaidApplicationCount = F, ReducedPaidStudentCount = F, SchoolYearID = F, StudentCertifiedFreeMedicaidCount = F, StudentCertifiedReducedMedicaidCount = F, StudentFreeCategoricalCount = F, StudentFreeCount = F, StudentOtherCertifiedCount = F, StudentReducedCount = F, StudentSNAPCertifiedCount = F, StudentSNAPLetterCertifiedCount = F, TotalErrorProneApplicationCount = F, TotalStudentFreeCount = F, TotalStudentReducedCount = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSubmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSubmission", objectId = VCRSubmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSubmission
	#'
	#' This function deletes a VCRSubmission
	#' @param VCRSubmissionID The ID of the VCRSubmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSubmissionID of the deleted VCRSubmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSubmission <- function(VCRSubmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSubmission", objectId = VCRSubmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSubmission
	#'
	#' This function creates a VCRSubmission
	#' @param fieldNames The field values to give the created VCRSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSubmission <- function(DistrictID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSubmission", body = list(DataObject = body), searchFields = append("VCRSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSubmission
	#'
	#' This function modifies a VCRSubmission
	#' @param fieldNames The field values to give the modified VCRSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSubmission <- function(VCRSubmissionID, DistrictID = NULL, SchoolYearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSubmission", objectId = VCRSubmissionID, body = list(DataObject = body), searchFields = append("VCRSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSection4Exceptions
	#'
	#' This function returns a dataframe or json object of VCRSection4Exceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection4Exceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection4Exceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection4Exception') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSection4Exceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSection4Exceptions <- function(searchConditionsList = NULL, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection4ExceptionID = F, VCRSection4V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSection4Exception", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSection4Exception
	#'
	#' This function returns a dataframe or json object of a VCRSection4Exception
	#' @param VCRSection4ExceptionID The ID of the VCRSection4Exception to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection4Exception. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection4Exception.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection4Exception') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSection4Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSection4Exception <- function(VCRSection4ExceptionID, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection4V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSection4ExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSection4Exception", objectId = VCRSection4ExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSection4Exception
	#'
	#' This function deletes a VCRSection4Exception
	#' @param VCRSection4ExceptionID The ID of the VCRSection4Exception to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSection4ExceptionID of the deleted VCRSection4Exception.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSection4Exception <- function(VCRSection4ExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSection4Exception", objectId = VCRSection4ExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSection4Exception
	#'
	#' This function creates a VCRSection4Exception
	#' @param fieldNames The field values to give the created VCRSection4Exception. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSection4Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSection4Exception <- function(DistrictID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRSection4V1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSection4Exception", body = list(DataObject = body), searchFields = append("VCRSection4ExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSection4Exception
	#'
	#' This function modifies a VCRSection4Exception
	#' @param fieldNames The field values to give the modified VCRSection4Exception. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSection4Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSection4Exception <- function(VCRSection4ExceptionID, DistrictID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRSection4V1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSection4Exception", objectId = VCRSection4ExceptionID, body = list(DataObject = body), searchFields = append("VCRSection4ExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSection3Exceptions
	#'
	#' This function returns a dataframe or json object of VCRSection3Exceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection3Exceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection3Exceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection3Exception') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSection3Exceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSection3Exceptions <- function(searchConditionsList = NULL, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection3ExceptionID = F, VCRSection3V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSection3Exception", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSection3Exception
	#'
	#' This function returns a dataframe or json object of a VCRSection3Exception
	#' @param VCRSection3ExceptionID The ID of the VCRSection3Exception to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection3Exception. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection3Exception.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection3Exception') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSection3Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSection3Exception <- function(VCRSection3ExceptionID, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection3V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSection3ExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSection3Exception", objectId = VCRSection3ExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSection3Exception
	#'
	#' This function deletes a VCRSection3Exception
	#' @param VCRSection3ExceptionID The ID of the VCRSection3Exception to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSection3ExceptionID of the deleted VCRSection3Exception.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSection3Exception <- function(VCRSection3ExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSection3Exception", objectId = VCRSection3ExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSection3Exception
	#'
	#' This function creates a VCRSection3Exception
	#' @param fieldNames The field values to give the created VCRSection3Exception. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSection3Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSection3Exception <- function(DistrictID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRSection3V1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSection3Exception", body = list(DataObject = body), searchFields = append("VCRSection3ExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSection3Exception
	#'
	#' This function modifies a VCRSection3Exception
	#' @param fieldNames The field values to give the modified VCRSection3Exception. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSection3Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSection3Exception <- function(VCRSection3ExceptionID, DistrictID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRSection3V1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSection3Exception", objectId = VCRSection3ExceptionID, body = list(DataObject = body), searchFields = append("VCRSection3ExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSection4V1s
	#'
	#' This function returns a dataframe or json object of VCRSection4V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection4V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection4V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection4V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSection4V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSection4V1s <- function(searchConditionsList = NULL, ApplicationCategoricalFreeCount = F, ApplicationFreeCount = F, ApplicationReducedCount = F, CreatedTime = F, DistrictID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StudentCategoricalFreeCount = F, StudentFreeCount = F, StudentReducedCount = F, TotalFreeCount = F, TotalReducedCount = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection4V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSection4V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSection4V1
	#'
	#' This function returns a dataframe or json object of a VCRSection4V1
	#' @param VCRSection4V1ID The ID of the VCRSection4V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection4V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection4V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection4V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSection4V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSection4V1 <- function(VCRSection4V1ID, ApplicationCategoricalFreeCount = F, ApplicationFreeCount = F, ApplicationReducedCount = F, CreatedTime = F, DistrictID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, SchoolYearID = F, StudentCategoricalFreeCount = F, StudentFreeCount = F, StudentReducedCount = F, TotalFreeCount = F, TotalReducedCount = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSection4V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSection4V1", objectId = VCRSection4V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSection4V1
	#'
	#' This function deletes a VCRSection4V1
	#' @param VCRSection4V1ID The ID of the VCRSection4V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSection4V1ID of the deleted VCRSection4V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSection4V1 <- function(VCRSection4V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSection4V1", objectId = VCRSection4V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSection4V1
	#'
	#' This function creates a VCRSection4V1
	#' @param fieldNames The field values to give the created VCRSection4V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSection4V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSection4V1 <- function(ApplicationCategoricalFreeCount = NULL, ApplicationFreeCount = NULL, ApplicationReducedCount = NULL, DistrictID = NULL, SchoolYearID = NULL, StudentCategoricalFreeCount = NULL, StudentFreeCount = NULL, StudentReducedCount = NULL, TotalFreeCount = NULL, TotalReducedCount = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSection4V1", body = list(DataObject = body), searchFields = append("VCRSection4V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSection4V1
	#'
	#' This function modifies a VCRSection4V1
	#' @param fieldNames The field values to give the modified VCRSection4V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSection4V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSection4V1 <- function(VCRSection4V1ID, ApplicationCategoricalFreeCount = NULL, ApplicationFreeCount = NULL, ApplicationReducedCount = NULL, DistrictID = NULL, SchoolYearID = NULL, StudentCategoricalFreeCount = NULL, StudentFreeCount = NULL, StudentReducedCount = NULL, TotalFreeCount = NULL, TotalReducedCount = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSection4V1", objectId = VCRSection4V1ID, body = list(DataObject = body), searchFields = append("VCRSection4V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSection3V1s
	#'
	#' This function returns a dataframe or json object of VCRSection3V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection3V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection3V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection3V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSection3V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSection3V1s <- function(searchConditionsList = NULL, CertifiedFreeMedicaidCount = F, CertifiedReducedMedicaidCount = F, CreatedTime = F, DistrictID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, OtherCertifiedCount = F, SchoolYearID = F, SNAPCertifiedCount = F, SNAPLetterCertifiedCount = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection3V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSection3V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSection3V1
	#'
	#' This function returns a dataframe or json object of a VCRSection3V1
	#' @param VCRSection3V1ID The ID of the VCRSection3V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection3V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection3V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection3V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSection3V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSection3V1 <- function(VCRSection3V1ID, CertifiedFreeMedicaidCount = F, CertifiedReducedMedicaidCount = F, CreatedTime = F, DistrictID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, OtherCertifiedCount = F, SchoolYearID = F, SNAPCertifiedCount = F, SNAPLetterCertifiedCount = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSection3V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSection3V1", objectId = VCRSection3V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSection3V1
	#'
	#' This function deletes a VCRSection3V1
	#' @param VCRSection3V1ID The ID of the VCRSection3V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSection3V1ID of the deleted VCRSection3V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSection3V1 <- function(VCRSection3V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSection3V1", objectId = VCRSection3V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSection3V1
	#'
	#' This function creates a VCRSection3V1
	#' @param fieldNames The field values to give the created VCRSection3V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSection3V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSection3V1 <- function(CertifiedFreeMedicaidCount = NULL, CertifiedReducedMedicaidCount = NULL, DistrictID = NULL, OtherCertifiedCount = NULL, SchoolYearID = NULL, SNAPCertifiedCount = NULL, SNAPLetterCertifiedCount = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSection3V1", body = list(DataObject = body), searchFields = append("VCRSection3V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSection3V1
	#'
	#' This function modifies a VCRSection3V1
	#' @param fieldNames The field values to give the modified VCRSection3V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSection3V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSection3V1 <- function(VCRSection3V1ID, CertifiedFreeMedicaidCount = NULL, CertifiedReducedMedicaidCount = NULL, DistrictID = NULL, OtherCertifiedCount = NULL, SchoolYearID = NULL, SNAPCertifiedCount = NULL, SNAPLetterCertifiedCount = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSection3V1", objectId = VCRSection3V1ID, body = list(DataObject = body), searchFields = append("VCRSection3V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACATransmissions
	#'
	#' This function returns a dataframe or json object of ACATransmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACATransmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACATransmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACATransmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACATransmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACATransmissions <- function(searchConditionsList = NULL, ACATransmissionID = F, ACATransmissionIDOriginal = F, ACATransmissionIDPrevious = F, ACATransmissionIDReceiptProvider = F, AllowEmployeeAccess = F, AllowEmployeeAddOrDelete = F, AllowEmployeeEdit = F, AllowEmployerAddOrDelete = F, AllowEmployerEdit = F, CalculatedTransmissionIdentifier = F, CalculationDay = F, CalendarYear = F, CreatedTime = F, DistrictID = F, EmploymentStartDate = F, FederalACASoftwareIdentifierID = F, HasAnExport = F, IncludedACAEmployeeCount = F, IsAvailableInEmployeeAccess = F, IsFinalized = F, IsTestTransmission = F, ModifiedTime = F, ReceiptIdentifier = F, Status = F, TransmissionNumber = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACATransmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACATransmission
	#'
	#' This function returns a dataframe or json object of an ACATransmission
	#' @param ACATransmissionID The ID of the ACATransmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACATransmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACATransmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACATransmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACATransmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACATransmission <- function(ACATransmissionID, ACATransmissionIDOriginal = F, ACATransmissionIDPrevious = F, ACATransmissionIDReceiptProvider = F, AllowEmployeeAccess = F, AllowEmployeeAddOrDelete = F, AllowEmployeeEdit = F, AllowEmployerAddOrDelete = F, AllowEmployerEdit = F, CalculatedTransmissionIdentifier = F, CalculationDay = F, CalendarYear = F, CreatedTime = F, DistrictID = F, EmploymentStartDate = F, FederalACASoftwareIdentifierID = F, HasAnExport = F, IncludedACAEmployeeCount = F, IsAvailableInEmployeeAccess = F, IsFinalized = F, IsTestTransmission = F, ModifiedTime = F, ReceiptIdentifier = F, Status = F, TransmissionNumber = F, Type = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACATransmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACATransmission", objectId = ACATransmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACATransmission
	#'
	#' This function deletes an ACATransmission
	#' @param ACATransmissionID The ID of the ACATransmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACATransmissionID of the deleted ACATransmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACATransmission <- function(ACATransmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACATransmission", objectId = ACATransmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACATransmission
	#'
	#' This function creates an ACATransmission
	#' @param fieldNames The field values to give the created ACATransmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACATransmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACATransmission <- function(ACATransmissionIDOriginal = NULL, ACATransmissionIDPrevious = NULL, ACATransmissionIDReceiptProvider = NULL, CalculationDay = NULL, CalendarYear = NULL, DistrictID = NULL, EmploymentStartDate = NULL, FederalACASoftwareIdentifierID = NULL, IsAvailableInEmployeeAccess = NULL, IsTestTransmission = NULL, ReceiptIdentifier = NULL, Status = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACATransmission", body = list(DataObject = body), searchFields = append("ACATransmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACATransmission
	#'
	#' This function modifies an ACATransmission
	#' @param fieldNames The field values to give the modified ACATransmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACATransmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACATransmission <- function(ACATransmissionID, ACATransmissionIDOriginal = NULL, ACATransmissionIDPrevious = NULL, ACATransmissionIDReceiptProvider = NULL, CalculationDay = NULL, CalendarYear = NULL, DistrictID = NULL, EmploymentStartDate = NULL, FederalACASoftwareIdentifierID = NULL, IsAvailableInEmployeeAccess = NULL, IsTestTransmission = NULL, ReceiptIdentifier = NULL, Status = NULL, Type = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACATransmission", objectId = ACATransmissionID, body = list(DataObject = body), searchFields = append("ACATransmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACASubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of ACASubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACASubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACASubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACASubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACASubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACASubmissionRunHistories <- function(searchConditionsList = NULL, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportFormat = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACASubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACASubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of an ACASubmissionRunHistory
	#' @param ACASubmissionRunHistoryID The ID of the ACASubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACASubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACASubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACASubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACASubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACASubmissionRunHistory <- function(ACASubmissionRunHistoryID, ACATransmissionID = F, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportFormat = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACASubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACASubmissionRunHistory", objectId = ACASubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACASubmissionRunHistory
	#'
	#' This function deletes an ACASubmissionRunHistory
	#' @param ACASubmissionRunHistoryID The ID of the ACASubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACASubmissionRunHistoryID of the deleted ACASubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACASubmissionRunHistory <- function(ACASubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACASubmissionRunHistory", objectId = ACASubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACASubmissionRunHistory
	#'
	#' This function creates an ACASubmissionRunHistory
	#' @param fieldNames The field values to give the created ACASubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACASubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACASubmissionRunHistory <- function(ACATransmissionID = NULL, EndDateTime = NULL, ExportFormat = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACASubmissionRunHistory", body = list(DataObject = body), searchFields = append("ACASubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACASubmissionRunHistory
	#'
	#' This function modifies an ACASubmissionRunHistory
	#' @param fieldNames The field values to give the modified ACASubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACASubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACASubmissionRunHistory <- function(ACASubmissionRunHistoryID, ACATransmissionID = NULL, EndDateTime = NULL, ExportFormat = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACASubmissionRunHistory", objectId = ACASubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("ACASubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACACoveredIndividualExceptions
	#'
	#' This function returns a dataframe or json object of ACACoveredIndividualExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACACoveredIndividualExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACACoveredIndividualExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACACoveredIndividualException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACACoveredIndividualExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACACoveredIndividualExceptions <- function(searchConditionsList = NULL, ACACoveredIndividualExceptionID = F, ACACoveredIndividualV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, Message = F, MessageType = F, ModifiedTime = F, NameID = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACACoveredIndividualException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACACoveredIndividualException
	#'
	#' This function returns a dataframe or json object of an ACACoveredIndividualException
	#' @param ACACoveredIndividualExceptionID The ID of the ACACoveredIndividualException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACACoveredIndividualException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACACoveredIndividualException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACACoveredIndividualException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACACoveredIndividualException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACACoveredIndividualException <- function(ACACoveredIndividualExceptionID, ACACoveredIndividualV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, Message = F, MessageType = F, ModifiedTime = F, NameID = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACACoveredIndividualExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualException", objectId = ACACoveredIndividualExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACACoveredIndividualException
	#'
	#' This function deletes an ACACoveredIndividualException
	#' @param ACACoveredIndividualExceptionID The ID of the ACACoveredIndividualException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACACoveredIndividualExceptionID of the deleted ACACoveredIndividualException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACACoveredIndividualException <- function(ACACoveredIndividualExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualException", objectId = ACACoveredIndividualExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACACoveredIndividualException
	#'
	#' This function creates an ACACoveredIndividualException
	#' @param fieldNames The field values to give the created ACACoveredIndividualException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACACoveredIndividualException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACACoveredIndividualException <- function(ACACoveredIndividualV2ID = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, Message = NULL, MessageType = NULL, NameID = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualException", body = list(DataObject = body), searchFields = append("ACACoveredIndividualExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACACoveredIndividualException
	#'
	#' This function modifies an ACACoveredIndividualException
	#' @param fieldNames The field values to give the modified ACACoveredIndividualException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACACoveredIndividualException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACACoveredIndividualException <- function(ACACoveredIndividualExceptionID, ACACoveredIndividualV2ID = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, Message = NULL, MessageType = NULL, NameID = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualException", objectId = ACACoveredIndividualExceptionID, body = list(DataObject = body), searchFields = append("ACACoveredIndividualExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployeeExceptions
	#'
	#' This function returns a dataframe or json object of ACAEmployeeExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployeeExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployeeExceptions <- function(searchConditionsList = NULL, ACAEmployeeExceptionID = F, ACAEmployeeV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployeeException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployeeException
	#'
	#' This function returns a dataframe or json object of an ACAEmployeeException
	#' @param ACAEmployeeExceptionID The ID of the ACAEmployeeException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployeeException <- function(ACAEmployeeExceptionID, ACAEmployeeV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployeeExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeException", objectId = ACAEmployeeExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployeeException
	#'
	#' This function deletes an ACAEmployeeException
	#' @param ACAEmployeeExceptionID The ID of the ACAEmployeeException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployeeExceptionID of the deleted ACAEmployeeException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployeeException <- function(ACAEmployeeExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeException", objectId = ACAEmployeeExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployeeException
	#'
	#' This function creates an ACAEmployeeException
	#' @param fieldNames The field values to give the created ACAEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployeeException <- function(ACAEmployeeV2ID = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeException", body = list(DataObject = body), searchFields = append("ACAEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployeeException
	#'
	#' This function modifies an ACAEmployeeException
	#' @param fieldNames The field values to give the modified ACAEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployeeException <- function(ACAEmployeeExceptionID, ACAEmployeeV2ID = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeException", objectId = ACAEmployeeExceptionID, body = list(DataObject = body), searchFields = append("ACAEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployerMonthExceptions
	#'
	#' This function returns a dataframe or json object of ACAEmployerMonthExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerMonthExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerMonthExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerMonthException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployerMonthExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployerMonthExceptions <- function(searchConditionsList = NULL, ACAEmployerMonthExceptionID = F, ACAEmployerMonthV2ID = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployerID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployerMonthException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployerMonthException
	#'
	#' This function returns a dataframe or json object of an ACAEmployerMonthException
	#' @param ACAEmployerMonthExceptionID The ID of the ACAEmployerMonthException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerMonthException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerMonthException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerMonthException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployerMonthException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployerMonthException <- function(ACAEmployerMonthExceptionID, ACAEmployerMonthV2ID = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployerID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployerMonthExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthException", objectId = ACAEmployerMonthExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployerMonthException
	#'
	#' This function deletes an ACAEmployerMonthException
	#' @param ACAEmployerMonthExceptionID The ID of the ACAEmployerMonthException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployerMonthExceptionID of the deleted ACAEmployerMonthException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployerMonthException <- function(ACAEmployerMonthExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthException", objectId = ACAEmployerMonthExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployerMonthException
	#'
	#' This function creates an ACAEmployerMonthException
	#' @param fieldNames The field values to give the created ACAEmployerMonthException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployerMonthException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployerMonthException <- function(ACAEmployerMonthV2ID = NULL, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployerID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthException", body = list(DataObject = body), searchFields = append("ACAEmployerMonthExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployerMonthException
	#'
	#' This function modifies an ACAEmployerMonthException
	#' @param fieldNames The field values to give the modified ACAEmployerMonthException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployerMonthException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployerMonthException <- function(ACAEmployerMonthExceptionID, ACAEmployerMonthV2ID = NULL, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployerID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthException", objectId = ACAEmployerMonthExceptionID, body = list(DataObject = body), searchFields = append("ACAEmployerMonthExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployerExceptions
	#'
	#' This function returns a dataframe or json object of ACAEmployerExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployerExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployerExceptions <- function(searchConditionsList = NULL, ACAEmployerExceptionID = F, ACAEmployerV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployerID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployerException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployerException
	#'
	#' This function returns a dataframe or json object of an ACAEmployerException
	#' @param ACAEmployerExceptionID The ID of the ACAEmployerException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployerException <- function(ACAEmployerExceptionID, ACAEmployerV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployerID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployerExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerException", objectId = ACAEmployerExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployerException
	#'
	#' This function deletes an ACAEmployerException
	#' @param ACAEmployerExceptionID The ID of the ACAEmployerException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployerExceptionID of the deleted ACAEmployerException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployerException <- function(ACAEmployerExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerException", objectId = ACAEmployerExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployerException
	#'
	#' This function creates an ACAEmployerException
	#' @param fieldNames The field values to give the created ACAEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployerException <- function(ACAEmployerV2ID = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployerID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerException", body = list(DataObject = body), searchFields = append("ACAEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployerException
	#'
	#' This function modifies an ACAEmployerException
	#' @param fieldNames The field values to give the modified ACAEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployerException <- function(ACAEmployerExceptionID, ACAEmployerV2ID = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployerID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployerException", objectId = ACAEmployerExceptionID, body = list(DataObject = body), searchFields = append("ACAEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAIRSExceptions
	#'
	#' This function returns a dataframe or json object of ACAIRSExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAIRSExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAIRSExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAIRSException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAIRSExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAIRSExceptions <- function(searchConditionsList = NULL, ACAIRSExceptionID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAIRSException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAIRSException
	#'
	#' This function returns a dataframe or json object of an ACAIRSException
	#' @param ACAIRSExceptionID The ID of the ACAIRSException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAIRSException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAIRSException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAIRSException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAIRSException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAIRSException <- function(ACAIRSExceptionID, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAIRSExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAIRSException", objectId = ACAIRSExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAIRSException
	#'
	#' This function deletes an ACAIRSException
	#' @param ACAIRSExceptionID The ID of the ACAIRSException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAIRSExceptionID of the deleted ACAIRSException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAIRSException <- function(ACAIRSExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAIRSException", objectId = ACAIRSExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAIRSException
	#'
	#' This function creates an ACAIRSException
	#' @param fieldNames The field values to give the created ACAIRSException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAIRSException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAIRSException <- function(ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, Message = NULL, MessageType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAIRSException", body = list(DataObject = body), searchFields = append("ACAIRSExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAIRSException
	#'
	#' This function modifies an ACAIRSException
	#' @param fieldNames The field values to give the modified ACAIRSException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAIRSException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAIRSException <- function(ACAIRSExceptionID, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, Message = NULL, MessageType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAIRSException", objectId = ACAIRSExceptionID, body = list(DataObject = body), searchFields = append("ACAIRSExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployeeMonthExceptions
	#'
	#' This function returns a dataframe or json object of ACAEmployeeMonthExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeMonthExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeMonthExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeMonthException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployeeMonthExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployeeMonthExceptions <- function(searchConditionsList = NULL, ACAEmployeeMonthExceptionID = F, ACAEmployeeMonthV2ID = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployeeMonthException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployeeMonthException
	#'
	#' This function returns a dataframe or json object of an ACAEmployeeMonthException
	#' @param ACAEmployeeMonthExceptionID The ID of the ACAEmployeeMonthException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeMonthException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeMonthException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeMonthException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployeeMonthException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployeeMonthException <- function(ACAEmployeeMonthExceptionID, ACAEmployeeMonthV2ID = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployeeMonthExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthException", objectId = ACAEmployeeMonthExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployeeMonthException
	#'
	#' This function deletes an ACAEmployeeMonthException
	#' @param ACAEmployeeMonthExceptionID The ID of the ACAEmployeeMonthException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployeeMonthExceptionID of the deleted ACAEmployeeMonthException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployeeMonthException <- function(ACAEmployeeMonthExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthException", objectId = ACAEmployeeMonthExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployeeMonthException
	#'
	#' This function creates an ACAEmployeeMonthException
	#' @param fieldNames The field values to give the created ACAEmployeeMonthException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployeeMonthException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployeeMonthException <- function(ACAEmployeeMonthV2ID = NULL, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthException", body = list(DataObject = body), searchFields = append("ACAEmployeeMonthExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployeeMonthException
	#'
	#' This function modifies an ACAEmployeeMonthException
	#' @param fieldNames The field values to give the modified ACAEmployeeMonthException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployeeMonthException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployeeMonthException <- function(ACAEmployeeMonthExceptionID, ACAEmployeeMonthV2ID = NULL, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthException", objectId = ACAEmployeeMonthExceptionID, body = list(DataObject = body), searchFields = append("ACAEmployeeMonthExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployerMonthV2s
	#'
	#' This function returns a dataframe or json object of ACAEmployerMonthV2s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerMonthV2s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerMonthV2s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerMonthV2') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployerMonthV2s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployerMonthV2s <- function(searchConditionsList = NULL, ACAEmployerMonthV2ID = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployerID = F, FullTimeEmployeeCount = F, HasErrors = F, HasSkywardFatalError = F, IsAggregatedGroup = F, KeyHash = F, ModifiedTime = F, OffersMinimumEssentialCoverage = F, TotalEmployeeCount = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployerMonthV2", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployerMonthV2
	#'
	#' This function returns a dataframe or json object of an ACAEmployerMonthV2
	#' @param ACAEmployerMonthV2ID The ID of the ACAEmployerMonthV2 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerMonthV2. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerMonthV2.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerMonthV2') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployerMonthV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployerMonthV2 <- function(ACAEmployerMonthV2ID, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployerID = F, FullTimeEmployeeCount = F, HasErrors = F, HasSkywardFatalError = F, IsAggregatedGroup = F, KeyHash = F, ModifiedTime = F, OffersMinimumEssentialCoverage = F, TotalEmployeeCount = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployerMonthV2ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthV2", objectId = ACAEmployerMonthV2ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployerMonthV2
	#'
	#' This function deletes an ACAEmployerMonthV2
	#' @param ACAEmployerMonthV2ID The ID of the ACAEmployerMonthV2 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployerMonthV2ID of the deleted ACAEmployerMonthV2.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployerMonthV2 <- function(ACAEmployerMonthV2ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthV2", objectId = ACAEmployerMonthV2ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployerMonthV2
	#'
	#' This function creates an ACAEmployerMonthV2
	#' @param fieldNames The field values to give the created ACAEmployerMonthV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployerMonthV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployerMonthV2 <- function(ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployerID = NULL, IsAggregatedGroup = NULL, OffersMinimumEssentialCoverage = NULL, TotalEmployeeCount = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthV2", body = list(DataObject = body), searchFields = append("ACAEmployerMonthV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployerMonthV2
	#'
	#' This function modifies an ACAEmployerMonthV2
	#' @param fieldNames The field values to give the modified ACAEmployerMonthV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployerMonthV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployerMonthV2 <- function(ACAEmployerMonthV2ID, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployerID = NULL, IsAggregatedGroup = NULL, OffersMinimumEssentialCoverage = NULL, TotalEmployeeCount = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployerMonthV2", objectId = ACAEmployerMonthV2ID, body = list(DataObject = body), searchFields = append("ACAEmployerMonthV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployeeV2s
	#'
	#' This function returns a dataframe or json object of ACAEmployeeV2s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeV2s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeV2s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeV2') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployeeV2s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployeeV2s <- function(searchConditionsList = NULL, ACAEmployeeV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, ACATransmissionIDReceiptProvider = F, AcceptedByIRS = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, AddressType = F, AgeOnJanuary1 = F, CityElectronicFile = F, CityPrinted = F, CountryName = F, CreatedTime = F, DistrictID = F, EmployeeAccessPostedTime = F, EmployeeID = F, EmployerPrinted1095 = F, EmployerProvidedSelfInsuredCoverage = F, FirstNameElectronicFile = F, FullNameLFMS = F, HasErrors = F, HasSkywardFatalError = F, IsAcceptedAndLatestInTransmissionSet = F, IsActive = F, IsAvailableInEmployeeAccessAndLatest = F, KeyHash = F, LastNameElectronicFile = F, MediaIDFullSSN = F, MiddleNameElectronicFile = F, ModifiedTime = F, NamePrinted = F, PlanStartMonth = F, PostalCode = F, SocialSecurityNumber = F, StateIDOrProvince = F, StateOrProvinceCode = F, StateOrProvinceName = F, SuffixElectronicFile = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryCode = F, W2CountryID = F, XMLData = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployeeV2", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployeeV2
	#'
	#' This function returns a dataframe or json object of an ACAEmployeeV2
	#' @param ACAEmployeeV2ID The ID of the ACAEmployeeV2 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeV2. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeV2.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeV2') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployeeV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployeeV2 <- function(ACAEmployeeV2ID, ACASubmissionRunHistoryID = F, ACATransmissionID = F, ACATransmissionIDReceiptProvider = F, AcceptedByIRS = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, AddressType = F, AgeOnJanuary1 = F, CityElectronicFile = F, CityPrinted = F, CountryName = F, CreatedTime = F, DistrictID = F, EmployeeAccessPostedTime = F, EmployeeID = F, EmployerPrinted1095 = F, EmployerProvidedSelfInsuredCoverage = F, FirstNameElectronicFile = F, FullNameLFMS = F, HasErrors = F, HasSkywardFatalError = F, IsAcceptedAndLatestInTransmissionSet = F, IsActive = F, IsAvailableInEmployeeAccessAndLatest = F, KeyHash = F, LastNameElectronicFile = F, MediaIDFullSSN = F, MiddleNameElectronicFile = F, ModifiedTime = F, NamePrinted = F, PlanStartMonth = F, PostalCode = F, SocialSecurityNumber = F, StateIDOrProvince = F, StateOrProvinceCode = F, StateOrProvinceName = F, SuffixElectronicFile = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryCode = F, W2CountryID = F, XMLData = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployeeV2ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeV2", objectId = ACAEmployeeV2ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployeeV2
	#'
	#' This function deletes an ACAEmployeeV2
	#' @param ACAEmployeeV2ID The ID of the ACAEmployeeV2 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployeeV2ID of the deleted ACAEmployeeV2.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployeeV2 <- function(ACAEmployeeV2ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeV2", objectId = ACAEmployeeV2ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployeeV2
	#'
	#' This function creates an ACAEmployeeV2
	#' @param fieldNames The field values to give the created ACAEmployeeV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployeeV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployeeV2 <- function(ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, ACATransmissionIDReceiptProvider = NULL, AcceptedByIRS = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, AddressType = NULL, AgeOnJanuary1 = NULL, CityElectronicFile = NULL, CityPrinted = NULL, CountryName = NULL, DistrictID = NULL, EmployeeAccessPostedTime = NULL, EmployeeID = NULL, EmployerPrinted1095 = NULL, EmployerProvidedSelfInsuredCoverage = NULL, FirstNameElectronicFile = NULL, IsActive = NULL, LastNameElectronicFile = NULL, MediaIDFullSSN = NULL, MiddleNameElectronicFile = NULL, NamePrinted = NULL, PlanStartMonth = NULL, PostalCode = NULL, SocialSecurityNumber = NULL, StateIDOrProvince = NULL, StateOrProvinceCode = NULL, StateOrProvinceName = NULL, SuffixElectronicFile = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, W2CountryCode = NULL, W2CountryID = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeV2", body = list(DataObject = body), searchFields = append("ACAEmployeeV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployeeV2
	#'
	#' This function modifies an ACAEmployeeV2
	#' @param fieldNames The field values to give the modified ACAEmployeeV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployeeV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployeeV2 <- function(ACAEmployeeV2ID, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, ACATransmissionIDReceiptProvider = NULL, AcceptedByIRS = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, AddressType = NULL, AgeOnJanuary1 = NULL, CityElectronicFile = NULL, CityPrinted = NULL, CountryName = NULL, DistrictID = NULL, EmployeeAccessPostedTime = NULL, EmployeeID = NULL, EmployerPrinted1095 = NULL, EmployerProvidedSelfInsuredCoverage = NULL, FirstNameElectronicFile = NULL, IsActive = NULL, LastNameElectronicFile = NULL, MediaIDFullSSN = NULL, MiddleNameElectronicFile = NULL, NamePrinted = NULL, PlanStartMonth = NULL, PostalCode = NULL, SocialSecurityNumber = NULL, StateIDOrProvince = NULL, StateOrProvinceCode = NULL, StateOrProvinceName = NULL, SuffixElectronicFile = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, W2CountryCode = NULL, W2CountryID = NULL, XMLData = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeV2", objectId = ACAEmployeeV2ID, body = list(DataObject = body), searchFields = append("ACAEmployeeV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployerV2s
	#'
	#' This function returns a dataframe or json object of ACAEmployerV2s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerV2s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerV2s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerV2') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployerV2s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployerV2s <- function(searchConditionsList = NULL, ACAEmployerV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, ACATransmissionIDReceiptProvider = F, AcceptedByIRS = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, AddressType = F, CityElectronicFile = F, CityPrinted = F, ContactPersonFirstName = F, ContactPersonLastName = F, ContactPersonMiddleName = F, ContactPersonSuffix = F, ContactPhoneNumber = F, CountryName = F, CreatedTime = F, DistrictID = F, EmployerID = F, EmployerIdentificationNumber = F, GovernmentEntityAddressLine1 = F, GovernmentEntityAddressLine2 = F, GovernmentEntityCity = F, GovernmentEntityContactPersonFirstName = F, GovernmentEntityContactPersonLastName = F, GovernmentEntityContactPersonMiddleName = F, GovernmentEntityContactPersonSuffix = F, GovernmentEntityContactPhoneNumber = F, GovernmentEntityEmployerIdentificationNumber = F, GovernmentEntityNameLine1 = F, GovernmentEntityNameLine2 = F, GovernmentEntityStateCode = F, GovernmentEntityZipCode = F, GovernmentEntityZipCodeExtension = F, HasErrors = F, HasSkywardFatalError = F, Is98PercentOfferMethod = F, IsAuthoritativeTransmittal = F, IsLatestInOriginalTransmissionSet = F, IsMainEmployer = F, IsMemberOfAggregatedALEGroup = F, IsQualifyingOfferMethod = F, KeyHash = F, ModifiedTime = F, NameLine1ElectronicFile = F, NameLine2ElectronicFile = F, NamePrinted = F, PostalCode = F, StateIDOrProvince = F, StateOrProvinceCode = F, StateOrProvinceName = F, TotalForms1095CFiled = F, TransmitterControlCode = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryCode = F, W2CountryID = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployerV2", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployerV2
	#'
	#' This function returns a dataframe or json object of an ACAEmployerV2
	#' @param ACAEmployerV2ID The ID of the ACAEmployerV2 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployerV2. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployerV2.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployerV2') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployerV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployerV2 <- function(ACAEmployerV2ID, ACASubmissionRunHistoryID = F, ACATransmissionID = F, ACATransmissionIDReceiptProvider = F, AcceptedByIRS = F, AddressLine1ElectronicFile = F, AddressLine1Printed = F, AddressLine2ElectronicFile = F, AddressLine2Printed = F, AddressType = F, CityElectronicFile = F, CityPrinted = F, ContactPersonFirstName = F, ContactPersonLastName = F, ContactPersonMiddleName = F, ContactPersonSuffix = F, ContactPhoneNumber = F, CountryName = F, CreatedTime = F, DistrictID = F, EmployerID = F, EmployerIdentificationNumber = F, GovernmentEntityAddressLine1 = F, GovernmentEntityAddressLine2 = F, GovernmentEntityCity = F, GovernmentEntityContactPersonFirstName = F, GovernmentEntityContactPersonLastName = F, GovernmentEntityContactPersonMiddleName = F, GovernmentEntityContactPersonSuffix = F, GovernmentEntityContactPhoneNumber = F, GovernmentEntityEmployerIdentificationNumber = F, GovernmentEntityNameLine1 = F, GovernmentEntityNameLine2 = F, GovernmentEntityStateCode = F, GovernmentEntityZipCode = F, GovernmentEntityZipCodeExtension = F, HasErrors = F, HasSkywardFatalError = F, Is98PercentOfferMethod = F, IsAuthoritativeTransmittal = F, IsLatestInOriginalTransmissionSet = F, IsMainEmployer = F, IsMemberOfAggregatedALEGroup = F, IsQualifyingOfferMethod = F, KeyHash = F, ModifiedTime = F, NameLine1ElectronicFile = F, NameLine2ElectronicFile = F, NamePrinted = F, PostalCode = F, StateIDOrProvince = F, StateOrProvinceCode = F, StateOrProvinceName = F, TotalForms1095CFiled = F, TransmitterControlCode = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryCode = F, W2CountryID = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployerV2ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerV2", objectId = ACAEmployerV2ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployerV2
	#'
	#' This function deletes an ACAEmployerV2
	#' @param ACAEmployerV2ID The ID of the ACAEmployerV2 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployerV2ID of the deleted ACAEmployerV2.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployerV2 <- function(ACAEmployerV2ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerV2", objectId = ACAEmployerV2ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployerV2
	#'
	#' This function creates an ACAEmployerV2
	#' @param fieldNames The field values to give the created ACAEmployerV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployerV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployerV2 <- function(ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, ACATransmissionIDReceiptProvider = NULL, AcceptedByIRS = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, AddressType = NULL, CityElectronicFile = NULL, CityPrinted = NULL, ContactPersonFirstName = NULL, ContactPersonLastName = NULL, ContactPersonMiddleName = NULL, ContactPersonSuffix = NULL, ContactPhoneNumber = NULL, CountryName = NULL, DistrictID = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, Is98PercentOfferMethod = NULL, IsAuthoritativeTransmittal = NULL, IsQualifyingOfferMethod = NULL, NameLine1ElectronicFile = NULL, NameLine2ElectronicFile = NULL, NamePrinted = NULL, PostalCode = NULL, StateIDOrProvince = NULL, StateOrProvinceCode = NULL, StateOrProvinceName = NULL, TransmitterControlCode = NULL, UniqueExportIdentifierSelf = NULL, W2CountryCode = NULL, W2CountryID = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployerV2", body = list(DataObject = body), searchFields = append("ACAEmployerV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployerV2
	#'
	#' This function modifies an ACAEmployerV2
	#' @param fieldNames The field values to give the modified ACAEmployerV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployerV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployerV2 <- function(ACAEmployerV2ID, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, ACATransmissionIDReceiptProvider = NULL, AcceptedByIRS = NULL, AddressLine1ElectronicFile = NULL, AddressLine1Printed = NULL, AddressLine2ElectronicFile = NULL, AddressLine2Printed = NULL, AddressType = NULL, CityElectronicFile = NULL, CityPrinted = NULL, ContactPersonFirstName = NULL, ContactPersonLastName = NULL, ContactPersonMiddleName = NULL, ContactPersonSuffix = NULL, ContactPhoneNumber = NULL, CountryName = NULL, DistrictID = NULL, EmployerID = NULL, EmployerIdentificationNumber = NULL, Is98PercentOfferMethod = NULL, IsAuthoritativeTransmittal = NULL, IsQualifyingOfferMethod = NULL, NameLine1ElectronicFile = NULL, NameLine2ElectronicFile = NULL, NamePrinted = NULL, PostalCode = NULL, StateIDOrProvince = NULL, StateOrProvinceCode = NULL, StateOrProvinceName = NULL, TransmitterControlCode = NULL, UniqueExportIdentifierSelf = NULL, W2CountryCode = NULL, W2CountryID = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployerV2", objectId = ACAEmployerV2ID, body = list(DataObject = body), searchFields = append("ACAEmployerV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployeeMonthV2s
	#'
	#' This function returns a dataframe or json object of ACAEmployeeMonthV2s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeMonthV2s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeMonthV2s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeMonthV2') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployeeMonthV2s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployeeMonthV2s <- function(searchConditionsList = NULL, ACAEmployeeMonthV2ID = F, ACAEmployeeRequiredContribution = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, FederalACAOfferOfCoverageCode = F, FederalACAOfferOfCoverageID = F, FederalACASafeHarborCode = F, FederalACASafeHarborID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployeeMonthV2", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployeeMonthV2
	#'
	#' This function returns a dataframe or json object of an ACAEmployeeMonthV2
	#' @param ACAEmployeeMonthV2ID The ID of the ACAEmployeeMonthV2 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeMonthV2. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeMonthV2.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeMonthV2') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployeeMonthV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployeeMonthV2 <- function(ACAEmployeeMonthV2ID, ACAEmployeeRequiredContribution = F, ACAMonth = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, CreatedTime = F, DistrictID = F, EmployeeID = F, FederalACAOfferOfCoverageCode = F, FederalACAOfferOfCoverageID = F, FederalACASafeHarborCode = F, FederalACASafeHarborID = F, HasErrors = F, HasSkywardFatalError = F, KeyHash = F, ModifiedTime = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipCode = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployeeMonthV2ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthV2", objectId = ACAEmployeeMonthV2ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployeeMonthV2
	#'
	#' This function deletes an ACAEmployeeMonthV2
	#' @param ACAEmployeeMonthV2ID The ID of the ACAEmployeeMonthV2 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployeeMonthV2ID of the deleted ACAEmployeeMonthV2.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployeeMonthV2 <- function(ACAEmployeeMonthV2ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthV2", objectId = ACAEmployeeMonthV2ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployeeMonthV2
	#'
	#' This function creates an ACAEmployeeMonthV2
	#' @param fieldNames The field values to give the created ACAEmployeeMonthV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployeeMonthV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployeeMonthV2 <- function(ACAEmployeeRequiredContribution = NULL, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, FederalACAOfferOfCoverageCode = NULL, FederalACAOfferOfCoverageID = NULL, FederalACASafeHarborCode = NULL, FederalACASafeHarborID = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthV2", body = list(DataObject = body), searchFields = append("ACAEmployeeMonthV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployeeMonthV2
	#'
	#' This function modifies an ACAEmployeeMonthV2
	#' @param fieldNames The field values to give the modified ACAEmployeeMonthV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployeeMonthV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployeeMonthV2 <- function(ACAEmployeeMonthV2ID, ACAEmployeeRequiredContribution = NULL, ACAMonth = NULL, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, DistrictID = NULL, EmployeeID = NULL, FederalACAOfferOfCoverageCode = NULL, FederalACAOfferOfCoverageID = NULL, FederalACASafeHarborCode = NULL, FederalACASafeHarborID = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeMonthV2", objectId = ACAEmployeeMonthV2ID, body = list(DataObject = body), searchFields = append("ACAEmployeeMonthV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACACoveredIndividualV2s
	#'
	#' This function returns a dataframe or json object of ACACoveredIndividualV2s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACACoveredIndividualV2s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACACoveredIndividualV2s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACACoveredIndividualV2') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACACoveredIndividualV2s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACACoveredIndividualV2s <- function(searchConditionsList = NULL, ACACoveredIndividualV2ID = F, ACASubmissionRunHistoryID = F, ACATransmissionID = F, BirthDate = F, CreatedTime = F, DistrictID = F, EmployeeID = F, FirstNameElectronicFile = F, FullNameLFMS = F, HasErrors = F, HasSkywardFatalError = F, IsCoveredAll12Months = F, IsCoveredApril = F, IsCoveredAugust = F, IsCoveredDecember = F, IsCoveredFebruary = F, IsCoveredJanuary = F, IsCoveredJuly = F, IsCoveredJune = F, IsCoveredMarch = F, IsCoveredMay = F, IsCoveredNovember = F, IsCoveredOctober = F, IsCoveredSeptember = F, KeyHash = F, LastNameElectronicFile = F, MiddleNameElectronicFile = F, ModifiedTime = F, NameID = F, NamePrinted = F, SocialSecurityNumber = F, SuffixElectronicFile = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACACoveredIndividualV2", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACACoveredIndividualV2
	#'
	#' This function returns a dataframe or json object of an ACACoveredIndividualV2
	#' @param ACACoveredIndividualV2ID The ID of the ACACoveredIndividualV2 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACACoveredIndividualV2. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACACoveredIndividualV2.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACACoveredIndividualV2') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACACoveredIndividualV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACACoveredIndividualV2 <- function(ACACoveredIndividualV2ID, ACASubmissionRunHistoryID = F, ACATransmissionID = F, BirthDate = F, CreatedTime = F, DistrictID = F, EmployeeID = F, FirstNameElectronicFile = F, FullNameLFMS = F, HasErrors = F, HasSkywardFatalError = F, IsCoveredAll12Months = F, IsCoveredApril = F, IsCoveredAugust = F, IsCoveredDecember = F, IsCoveredFebruary = F, IsCoveredJanuary = F, IsCoveredJuly = F, IsCoveredJune = F, IsCoveredMarch = F, IsCoveredMay = F, IsCoveredNovember = F, IsCoveredOctober = F, IsCoveredSeptember = F, KeyHash = F, LastNameElectronicFile = F, MiddleNameElectronicFile = F, ModifiedTime = F, NameID = F, NamePrinted = F, SocialSecurityNumber = F, SuffixElectronicFile = F, UniqueExportIdentifierParent = F, UniqueExportIdentifierSelf = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACACoveredIndividualV2ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualV2", objectId = ACACoveredIndividualV2ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACACoveredIndividualV2
	#'
	#' This function deletes an ACACoveredIndividualV2
	#' @param ACACoveredIndividualV2ID The ID of the ACACoveredIndividualV2 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACACoveredIndividualV2ID of the deleted ACACoveredIndividualV2.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACACoveredIndividualV2 <- function(ACACoveredIndividualV2ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualV2", objectId = ACACoveredIndividualV2ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACACoveredIndividualV2
	#'
	#' This function creates an ACACoveredIndividualV2
	#' @param fieldNames The field values to give the created ACACoveredIndividualV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACACoveredIndividualV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACACoveredIndividualV2 <- function(ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, BirthDate = NULL, DistrictID = NULL, EmployeeID = NULL, FirstNameElectronicFile = NULL, IsCoveredAll12Months = NULL, IsCoveredApril = NULL, IsCoveredAugust = NULL, IsCoveredDecember = NULL, IsCoveredFebruary = NULL, IsCoveredJanuary = NULL, IsCoveredJuly = NULL, IsCoveredJune = NULL, IsCoveredMarch = NULL, IsCoveredMay = NULL, IsCoveredNovember = NULL, IsCoveredOctober = NULL, IsCoveredSeptember = NULL, LastNameElectronicFile = NULL, MiddleNameElectronicFile = NULL, NameID = NULL, NamePrinted = NULL, SocialSecurityNumber = NULL, SuffixElectronicFile = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualV2", body = list(DataObject = body), searchFields = append("ACACoveredIndividualV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACACoveredIndividualV2
	#'
	#' This function modifies an ACACoveredIndividualV2
	#' @param fieldNames The field values to give the modified ACACoveredIndividualV2. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACACoveredIndividualV2
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACACoveredIndividualV2 <- function(ACACoveredIndividualV2ID, ACASubmissionRunHistoryID = NULL, ACATransmissionID = NULL, BirthDate = NULL, DistrictID = NULL, EmployeeID = NULL, FirstNameElectronicFile = NULL, IsCoveredAll12Months = NULL, IsCoveredApril = NULL, IsCoveredAugust = NULL, IsCoveredDecember = NULL, IsCoveredFebruary = NULL, IsCoveredJanuary = NULL, IsCoveredJuly = NULL, IsCoveredJune = NULL, IsCoveredMarch = NULL, IsCoveredMay = NULL, IsCoveredNovember = NULL, IsCoveredOctober = NULL, IsCoveredSeptember = NULL, LastNameElectronicFile = NULL, MiddleNameElectronicFile = NULL, NameID = NULL, NamePrinted = NULL, SocialSecurityNumber = NULL, SuffixElectronicFile = NULL, UniqueExportIdentifierParent = NULL, UniqueExportIdentifierSelf = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACACoveredIndividualV2", objectId = ACACoveredIndividualV2ID, body = list(DataObject = body), searchFields = append("ACACoveredIndividualV2ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSection5Exceptions
	#'
	#' This function returns a dataframe or json object of VCRSection5Exceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection5Exceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection5Exceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection5Exception') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSection5Exceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSection5Exceptions <- function(searchConditionsList = NULL, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection5ExceptionID = F, VCRSection5V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSection5Exception", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSection5Exception
	#'
	#' This function returns a dataframe or json object of a VCRSection5Exception
	#' @param VCRSection5ExceptionID The ID of the VCRSection5Exception to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection5Exception. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection5Exception.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection5Exception') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSection5Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSection5Exception <- function(VCRSection5ExceptionID, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection5V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSection5ExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSection5Exception", objectId = VCRSection5ExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSection5Exception
	#'
	#' This function deletes a VCRSection5Exception
	#' @param VCRSection5ExceptionID The ID of the VCRSection5Exception to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSection5ExceptionID of the deleted VCRSection5Exception.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSection5Exception <- function(VCRSection5ExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSection5Exception", objectId = VCRSection5ExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSection5Exception
	#'
	#' This function creates a VCRSection5Exception
	#' @param fieldNames The field values to give the created VCRSection5Exception. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSection5Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSection5Exception <- function(DistrictID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRSection5V1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSection5Exception", body = list(DataObject = body), searchFields = append("VCRSection5ExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSection5Exception
	#'
	#' This function modifies a VCRSection5Exception
	#' @param fieldNames The field values to give the modified VCRSection5Exception. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSection5Exception
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSection5Exception <- function(VCRSection5ExceptionID, DistrictID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRSection5V1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSection5Exception", objectId = VCRSection5ExceptionID, body = list(DataObject = body), searchFields = append("VCRSection5ExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRSection5V1s
	#'
	#' This function returns a dataframe or json object of VCRSection5V1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection5V1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection5V1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection5V1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRSection5V1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRSection5V1s <- function(searchConditionsList = NULL, ApplicationsSelectedCount = F, CategoricalNoChangeApplicationCount = F, CategoricalNoChangeStudentCount = F, CategoricalNoResponseApplicationCount = F, CategoricalNoResponseStudentCount = F, CategoricalPaidApplicationCount = F, CategoricalPaidStudentCount = F, CategoricalReducedApplicationCount = F, CategoricalReducedStudentCount = F, CauseVerifiedApplicationCount = F, CreatedTime = F, DirectVerificationApplicationsCount = F, DirectVerificationStudentCount = F, DistrictID = F, HasErrors = F, IncomeNoChangeApplicationCount = F, IncomeNoChangeStudentCount = F, IncomeNoResponseApplicationCount = F, IncomeNoResponseStudentCount = F, IncomePaidApplicationCount = F, IncomePaidStudentCount = F, IncomeReducedApplicationCount = F, IncomeReducedStudentCount = F, KeyHash = F, ModifiedTime = F, ReducedFreeApplicationCount = F, ReducedFreeStudentCount = F, ReducedNoChangeApplicationCount = F, ReducedNoChangeStudentCount = F, ReducedNoResponseApplicationCount = F, ReducedNoResponseStudentCount = F, ReducedPaidApplicationCount = F, ReducedPaidStudentCount = F, SchoolYearID = F, TotalErrorProneApplicationsCount = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSection5V1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRSection5V1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRSection5V1
	#'
	#' This function returns a dataframe or json object of a VCRSection5V1
	#' @param VCRSection5V1ID The ID of the VCRSection5V1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRSection5V1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRSection5V1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRSection5V1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRSection5V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRSection5V1 <- function(VCRSection5V1ID, ApplicationsSelectedCount = F, CategoricalNoChangeApplicationCount = F, CategoricalNoChangeStudentCount = F, CategoricalNoResponseApplicationCount = F, CategoricalNoResponseStudentCount = F, CategoricalPaidApplicationCount = F, CategoricalPaidStudentCount = F, CategoricalReducedApplicationCount = F, CategoricalReducedStudentCount = F, CauseVerifiedApplicationCount = F, CreatedTime = F, DirectVerificationApplicationsCount = F, DirectVerificationStudentCount = F, DistrictID = F, HasErrors = F, IncomeNoChangeApplicationCount = F, IncomeNoChangeStudentCount = F, IncomeNoResponseApplicationCount = F, IncomeNoResponseStudentCount = F, IncomePaidApplicationCount = F, IncomePaidStudentCount = F, IncomeReducedApplicationCount = F, IncomeReducedStudentCount = F, KeyHash = F, ModifiedTime = F, ReducedFreeApplicationCount = F, ReducedFreeStudentCount = F, ReducedNoChangeApplicationCount = F, ReducedNoChangeStudentCount = F, ReducedNoResponseApplicationCount = F, ReducedNoResponseStudentCount = F, ReducedPaidApplicationCount = F, ReducedPaidStudentCount = F, SchoolYearID = F, TotalErrorProneApplicationsCount = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRSection5V1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRSection5V1", objectId = VCRSection5V1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRSection5V1
	#'
	#' This function deletes a VCRSection5V1
	#' @param VCRSection5V1ID The ID of the VCRSection5V1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRSection5V1ID of the deleted VCRSection5V1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRSection5V1 <- function(VCRSection5V1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRSection5V1", objectId = VCRSection5V1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRSection5V1
	#'
	#' This function creates a VCRSection5V1
	#' @param fieldNames The field values to give the created VCRSection5V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRSection5V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRSection5V1 <- function(ApplicationsSelectedCount = NULL, CategoricalNoChangeApplicationCount = NULL, CategoricalNoChangeStudentCount = NULL, CategoricalNoResponseApplicationCount = NULL, CategoricalNoResponseStudentCount = NULL, CategoricalPaidApplicationCount = NULL, CategoricalPaidStudentCount = NULL, CategoricalReducedApplicationCount = NULL, CategoricalReducedStudentCount = NULL, CauseVerifiedApplicationCount = NULL, DirectVerificationApplicationsCount = NULL, DirectVerificationStudentCount = NULL, DistrictID = NULL, IncomeNoChangeApplicationCount = NULL, IncomeNoChangeStudentCount = NULL, IncomeNoResponseApplicationCount = NULL, IncomeNoResponseStudentCount = NULL, IncomePaidApplicationCount = NULL, IncomePaidStudentCount = NULL, IncomeReducedApplicationCount = NULL, IncomeReducedStudentCount = NULL, ReducedFreeApplicationCount = NULL, ReducedFreeStudentCount = NULL, ReducedNoChangeApplicationCount = NULL, ReducedNoChangeStudentCount = NULL, ReducedNoResponseApplicationCount = NULL, ReducedNoResponseStudentCount = NULL, ReducedPaidApplicationCount = NULL, ReducedPaidStudentCount = NULL, SchoolYearID = NULL, TotalErrorProneApplicationsCount = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRSection5V1", body = list(DataObject = body), searchFields = append("VCRSection5V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRSection5V1
	#'
	#' This function modifies a VCRSection5V1
	#' @param fieldNames The field values to give the modified VCRSection5V1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRSection5V1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRSection5V1 <- function(VCRSection5V1ID, ApplicationsSelectedCount = NULL, CategoricalNoChangeApplicationCount = NULL, CategoricalNoChangeStudentCount = NULL, CategoricalNoResponseApplicationCount = NULL, CategoricalNoResponseStudentCount = NULL, CategoricalPaidApplicationCount = NULL, CategoricalPaidStudentCount = NULL, CategoricalReducedApplicationCount = NULL, CategoricalReducedStudentCount = NULL, CauseVerifiedApplicationCount = NULL, DirectVerificationApplicationsCount = NULL, DirectVerificationStudentCount = NULL, DistrictID = NULL, IncomeNoChangeApplicationCount = NULL, IncomeNoChangeStudentCount = NULL, IncomeNoResponseApplicationCount = NULL, IncomeNoResponseStudentCount = NULL, IncomePaidApplicationCount = NULL, IncomePaidStudentCount = NULL, IncomeReducedApplicationCount = NULL, IncomeReducedStudentCount = NULL, ReducedFreeApplicationCount = NULL, ReducedFreeStudentCount = NULL, ReducedNoChangeApplicationCount = NULL, ReducedNoChangeStudentCount = NULL, ReducedNoResponseApplicationCount = NULL, ReducedNoResponseStudentCount = NULL, ReducedPaidApplicationCount = NULL, ReducedPaidStudentCount = NULL, SchoolYearID = NULL, TotalErrorProneApplicationsCount = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRSection5V1", objectId = VCRSection5V1ID, body = list(DataObject = body), searchFields = append("VCRSection5V1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRApplicationExceptions
	#'
	#' This function returns a dataframe or json object of VCRApplicationExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRApplicationExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRApplicationExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRApplicationException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRApplicationExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRApplicationExceptions <- function(searchConditionsList = NULL, ApplicationID = F, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, NameIDGuardian = F, NameIDPayor = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRApplicationExceptionID = F, VCRApplicationV1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRApplicationException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRApplicationException
	#'
	#' This function returns a dataframe or json object of a VCRApplicationException
	#' @param VCRApplicationExceptionID The ID of the VCRApplicationException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRApplicationException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRApplicationException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRApplicationException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRApplicationException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRApplicationException <- function(VCRApplicationExceptionID, ApplicationID = F, CreatedTime = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, NameIDGuardian = F, NameIDPayor = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRApplicationV1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRApplicationExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRApplicationException", objectId = VCRApplicationExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRApplicationException
	#'
	#' This function deletes a VCRApplicationException
	#' @param VCRApplicationExceptionID The ID of the VCRApplicationException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRApplicationExceptionID of the deleted VCRApplicationException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRApplicationException <- function(VCRApplicationExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRApplicationException", objectId = VCRApplicationExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRApplicationException
	#'
	#' This function creates a VCRApplicationException
	#' @param fieldNames The field values to give the created VCRApplicationException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRApplicationException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRApplicationException <- function(ApplicationID = NULL, DistrictID = NULL, Message = NULL, MessageType = NULL, NameIDGuardian = NULL, NameIDPayor = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRApplicationV1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRApplicationException", body = list(DataObject = body), searchFields = append("VCRApplicationExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRApplicationException
	#'
	#' This function modifies a VCRApplicationException
	#' @param fieldNames The field values to give the modified VCRApplicationException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRApplicationException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRApplicationException <- function(VCRApplicationExceptionID, ApplicationID = NULL, DistrictID = NULL, Message = NULL, MessageType = NULL, NameIDGuardian = NULL, NameIDPayor = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRApplicationV1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRApplicationException", objectId = VCRApplicationExceptionID, body = list(DataObject = body), searchFields = append("VCRApplicationExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRStudentExceptions
	#'
	#' This function returns a dataframe or json object of VCRStudentExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRStudentExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRStudentExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRStudentException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRStudentExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRStudentExceptions <- function(searchConditionsList = NULL, CreatedTime = F, CustomerCategoryID = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, NameIDGuardian = F, NameIDStudent = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRStudentExceptionID = F, VCRStudentV1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRStudentException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRStudentException
	#'
	#' This function returns a dataframe or json object of a VCRStudentException
	#' @param VCRStudentExceptionID The ID of the VCRStudentException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRStudentException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRStudentException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRStudentException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRStudentException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRStudentException <- function(VCRStudentExceptionID, CreatedTime = F, CustomerCategoryID = F, DistrictID = F, Message = F, MessageType = F, ModifiedTime = F, NameIDGuardian = F, NameIDStudent = F, ProcessType = F, SchoolYearID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRStudentV1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRStudentExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRStudentException", objectId = VCRStudentExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRStudentException
	#'
	#' This function deletes a VCRStudentException
	#' @param VCRStudentExceptionID The ID of the VCRStudentException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRStudentExceptionID of the deleted VCRStudentException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRStudentException <- function(VCRStudentExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRStudentException", objectId = VCRStudentExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRStudentException
	#'
	#' This function creates a VCRStudentException
	#' @param fieldNames The field values to give the created VCRStudentException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRStudentException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRStudentException <- function(CustomerCategoryID = NULL, DistrictID = NULL, Message = NULL, MessageType = NULL, NameIDGuardian = NULL, NameIDStudent = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRStudentV1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRStudentException", body = list(DataObject = body), searchFields = append("VCRStudentExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRStudentException
	#'
	#' This function modifies a VCRStudentException
	#' @param fieldNames The field values to give the modified VCRStudentException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRStudentException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRStudentException <- function(VCRStudentExceptionID, CustomerCategoryID = NULL, DistrictID = NULL, Message = NULL, MessageType = NULL, NameIDGuardian = NULL, NameIDStudent = NULL, ProcessType = NULL, SchoolYearID = NULL, VCRStudentV1ID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRStudentException", objectId = VCRStudentExceptionID, body = list(DataObject = body), searchFields = append("VCRStudentExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRApplicationV1s
	#'
	#' This function returns a dataframe or json object of VCRApplicationV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRApplicationV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRApplicationV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRApplicationV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRApplicationV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRApplicationV1s <- function(searchConditionsList = NULL, ApplicationID = F, CreatedTime = F, DistrictID = F, HasErrors = F, IsCategoricalFree = F, IsCategoricalNoChange = F, IsCategoricalNoResponse = F, IsCategoricalPaid = F, IsCategoricalReduced = F, IsCauseVerified = F, IsDirectVerification = F, IsErrorProne = F, IsFree = F, IsIncomeNoChange = F, IsIncomeNoResponse = F, IsIncomePaid = F, IsIncomeReduced = F, IsReduced = F, IsReducedFree = F, IsReducedNoChange = F, IsReducedNoResponse = F, IsReducedPaid = F, IsSelected = F, KeyHash = F, ModifiedTime = F, NameIDGuardian = F, NameIDPayor = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRApplicationV1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRApplicationV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRApplicationV1
	#'
	#' This function returns a dataframe or json object of a VCRApplicationV1
	#' @param VCRApplicationV1ID The ID of the VCRApplicationV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRApplicationV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRApplicationV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRApplicationV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRApplicationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRApplicationV1 <- function(VCRApplicationV1ID, ApplicationID = F, CreatedTime = F, DistrictID = F, HasErrors = F, IsCategoricalFree = F, IsCategoricalNoChange = F, IsCategoricalNoResponse = F, IsCategoricalPaid = F, IsCategoricalReduced = F, IsCauseVerified = F, IsDirectVerification = F, IsErrorProne = F, IsFree = F, IsIncomeNoChange = F, IsIncomeNoResponse = F, IsIncomePaid = F, IsIncomeReduced = F, IsReduced = F, IsReducedFree = F, IsReducedNoChange = F, IsReducedNoResponse = F, IsReducedPaid = F, IsSelected = F, KeyHash = F, ModifiedTime = F, NameIDGuardian = F, NameIDPayor = F, SchoolYearID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRApplicationV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRApplicationV1", objectId = VCRApplicationV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRApplicationV1
	#'
	#' This function deletes a VCRApplicationV1
	#' @param VCRApplicationV1ID The ID of the VCRApplicationV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRApplicationV1ID of the deleted VCRApplicationV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRApplicationV1 <- function(VCRApplicationV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRApplicationV1", objectId = VCRApplicationV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRApplicationV1
	#'
	#' This function creates a VCRApplicationV1
	#' @param fieldNames The field values to give the created VCRApplicationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRApplicationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRApplicationV1 <- function(ApplicationID = NULL, DistrictID = NULL, IsCategoricalFree = NULL, IsCategoricalNoChange = NULL, IsCategoricalNoResponse = NULL, IsCategoricalPaid = NULL, IsCategoricalReduced = NULL, IsCauseVerified = NULL, IsDirectVerification = NULL, IsErrorProne = NULL, IsFree = NULL, IsIncomeNoChange = NULL, IsIncomeNoResponse = NULL, IsIncomePaid = NULL, IsIncomeReduced = NULL, IsReduced = NULL, IsReducedFree = NULL, IsReducedNoChange = NULL, IsReducedNoResponse = NULL, IsReducedPaid = NULL, IsSelected = NULL, NameIDGuardian = NULL, NameIDPayor = NULL, SchoolYearID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRApplicationV1", body = list(DataObject = body), searchFields = append("VCRApplicationV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRApplicationV1
	#'
	#' This function modifies a VCRApplicationV1
	#' @param fieldNames The field values to give the modified VCRApplicationV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRApplicationV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRApplicationV1 <- function(VCRApplicationV1ID, ApplicationID = NULL, DistrictID = NULL, IsCategoricalFree = NULL, IsCategoricalNoChange = NULL, IsCategoricalNoResponse = NULL, IsCategoricalPaid = NULL, IsCategoricalReduced = NULL, IsCauseVerified = NULL, IsDirectVerification = NULL, IsErrorProne = NULL, IsFree = NULL, IsIncomeNoChange = NULL, IsIncomeNoResponse = NULL, IsIncomePaid = NULL, IsIncomeReduced = NULL, IsReduced = NULL, IsReducedFree = NULL, IsReducedNoChange = NULL, IsReducedNoResponse = NULL, IsReducedPaid = NULL, IsSelected = NULL, NameIDGuardian = NULL, NameIDPayor = NULL, SchoolYearID = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRApplicationV1", objectId = VCRApplicationV1ID, body = list(DataObject = body), searchFields = append("VCRApplicationV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List VCRStudentV1s
	#'
	#' This function returns a dataframe or json object of VCRStudentV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRStudentV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRStudentV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRStudentV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of VCRStudentV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listVCRStudentV1s <- function(searchConditionsList = NULL, AddressID = F, CreatedTime = F, CustomerCategoryID = F, DistrictID = F, HasErrors = F, IsCategoricalFree = F, IsCategoricalNoChange = F, IsCategoricalNoResponse = F, IsCategoricalPaid = F, IsCategoricalReduced = F, IsCertifiedFreeMedicaid = F, IsCertifiedReducedMedicaid = F, IsDirectVerification = F, IsFree = F, IsIncomeNoChange = F, IsIncomeNoResponse = F, IsIncomePaid = F, IsIncomeReduced = F, IsOtherCertified = F, IsReduced = F, IsReducedFree = F, IsReducedNoChange = F, IsReducedNoResponse = F, IsReducedPaid = F, IsSNAPCertified = F, IsSNAPLetterCertified = F, KeyHash = F, ModifiedTime = F, NameIDGuardian = F, NameIDStudent = F, SchoolYearID = F, Status = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRStudentV1ID = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, WithdrawalDate = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "VCRStudentV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a VCRStudentV1
	#'
	#' This function returns a dataframe or json object of a VCRStudentV1
	#' @param VCRStudentV1ID The ID of the VCRStudentV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given VCRStudentV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the VCRStudentV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('VCRStudentV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of VCRStudentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getVCRStudentV1 <- function(VCRStudentV1ID, AddressID = F, CreatedTime = F, CustomerCategoryID = F, DistrictID = F, HasErrors = F, IsCategoricalFree = F, IsCategoricalNoChange = F, IsCategoricalNoResponse = F, IsCategoricalPaid = F, IsCategoricalReduced = F, IsCertifiedFreeMedicaid = F, IsCertifiedReducedMedicaid = F, IsDirectVerification = F, IsFree = F, IsIncomeNoChange = F, IsIncomeNoResponse = F, IsIncomePaid = F, IsIncomeReduced = F, IsOtherCertified = F, IsReduced = F, IsReducedFree = F, IsReducedNoChange = F, IsReducedNoResponse = F, IsReducedPaid = F, IsSNAPCertified = F, IsSNAPLetterCertified = F, KeyHash = F, ModifiedTime = F, NameIDGuardian = F, NameIDStudent = F, SchoolYearID = F, Status = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VCRSubmissionID = F, VCRSubmissionRunHistoryID = F, WithdrawalDate = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "VCRStudentV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "VCRStudentV1", objectId = VCRStudentV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a VCRStudentV1
	#'
	#' This function deletes a VCRStudentV1
	#' @param VCRStudentV1ID The ID of the VCRStudentV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The VCRStudentV1ID of the deleted VCRStudentV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteVCRStudentV1 <- function(VCRStudentV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "VCRStudentV1", objectId = VCRStudentV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a VCRStudentV1
	#'
	#' This function creates a VCRStudentV1
	#' @param fieldNames The field values to give the created VCRStudentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created VCRStudentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createVCRStudentV1 <- function(AddressID = NULL, CustomerCategoryID = NULL, DistrictID = NULL, IsCategoricalFree = NULL, IsCategoricalNoChange = NULL, IsCategoricalNoResponse = NULL, IsCategoricalPaid = NULL, IsCategoricalReduced = NULL, IsCertifiedFreeMedicaid = NULL, IsCertifiedReducedMedicaid = NULL, IsDirectVerification = NULL, IsFree = NULL, IsIncomeNoChange = NULL, IsIncomeNoResponse = NULL, IsIncomePaid = NULL, IsIncomeReduced = NULL, IsOtherCertified = NULL, IsReduced = NULL, IsReducedFree = NULL, IsReducedNoChange = NULL, IsReducedNoResponse = NULL, IsReducedPaid = NULL, IsSNAPCertified = NULL, IsSNAPLetterCertified = NULL, NameIDGuardian = NULL, NameIDStudent = NULL, SchoolYearID = NULL, Status = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, WithdrawalDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "VCRStudentV1", body = list(DataObject = body), searchFields = append("VCRStudentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a VCRStudentV1
	#'
	#' This function modifies a VCRStudentV1
	#' @param fieldNames The field values to give the modified VCRStudentV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified VCRStudentV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyVCRStudentV1 <- function(VCRStudentV1ID, AddressID = NULL, CustomerCategoryID = NULL, DistrictID = NULL, IsCategoricalFree = NULL, IsCategoricalNoChange = NULL, IsCategoricalNoResponse = NULL, IsCategoricalPaid = NULL, IsCategoricalReduced = NULL, IsCertifiedFreeMedicaid = NULL, IsCertifiedReducedMedicaid = NULL, IsDirectVerification = NULL, IsFree = NULL, IsIncomeNoChange = NULL, IsIncomeNoResponse = NULL, IsIncomePaid = NULL, IsIncomeReduced = NULL, IsOtherCertified = NULL, IsReduced = NULL, IsReducedFree = NULL, IsReducedNoChange = NULL, IsReducedNoResponse = NULL, IsReducedPaid = NULL, IsSNAPCertified = NULL, IsSNAPLetterCertified = NULL, NameIDGuardian = NULL, NameIDStudent = NULL, SchoolYearID = NULL, Status = NULL, VCRSubmissionID = NULL, VCRSubmissionRunHistoryID = NULL, WithdrawalDate = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "VCRStudentV1", objectId = VCRStudentV1ID, body = list(DataObject = body), searchFields = append("VCRStudentV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAIndividualPDFS
	#'
	#' This function returns a dataframe or json object of ACAIndividualPDFS
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAIndividualPDFS. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAIndividualPDFS.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAIndividualPDF') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAIndividualPDFS
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAIndividualPDFS <- function(searchConditionsList = NULL, ACAEmployeeV2ID = F, ACAYearEmployeeV1ID = F, CalendarYear = F, CreatedTime = F, DistrictID = F, EmployeeID = F, EmployerName = F, MediaIDFullSSN = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAIndividualPDF", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAIndividualPDF
	#'
	#' This function returns a dataframe or json object of an ACAIndividualPDF
	#' @param ACAIndividualPDFID The ID of the ACAIndividualPDF to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAIndividualPDF. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAIndividualPDF.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAIndividualPDF') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAIndividualPDF
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAIndividualPDF <- function(ACAIndividualPDFID, ACAEmployeeV2ID = F, ACAYearEmployeeV1ID = F, CalendarYear = F, CreatedTime = F, DistrictID = F, EmployeeID = F, EmployerName = F, MediaIDFullSSN = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAIndividualPDFID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAIndividualPDF", objectId = ACAIndividualPDFID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAIndividualPDF
	#'
	#' This function deletes an ACAIndividualPDF
	#' @param ACAIndividualPDFID The ID of the ACAIndividualPDF to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAIndividualPDFID of the deleted ACAIndividualPDF.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAIndividualPDF <- function(ACAIndividualPDFID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAIndividualPDF", objectId = ACAIndividualPDFID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAIndividualPDF
	#'
	#' This function creates an ACAIndividualPDF
	#' @param fieldNames The field values to give the created ACAIndividualPDF. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAIndividualPDF
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAIndividualPDF <- function(ACAEmployeeV2ID = NULL, ACAYearEmployeeV1ID = NULL, CalendarYear = NULL, DistrictID = NULL, EmployeeID = NULL, EmployerName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAIndividualPDF", body = list(DataObject = body), searchFields = append("ACAIndividualPDFID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAIndividualPDF
	#'
	#' This function modifies an ACAIndividualPDF
	#' @param fieldNames The field values to give the modified ACAIndividualPDF. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAIndividualPDF
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAIndividualPDF <- function(ACAIndividualPDFID, ACAEmployeeV2ID = NULL, ACAYearEmployeeV1ID = NULL, CalendarYear = NULL, DistrictID = NULL, EmployeeID = NULL, EmployerName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAIndividualPDF", objectId = ACAIndividualPDFID, body = list(DataObject = body), searchFields = append("ACAIndividualPDFID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACADependentReports
	#'
	#' This function returns a dataframe or json object of ACADependentReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACADependentReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACADependentReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACADependentReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACADependentReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACADependentReports <- function(searchConditionsList = NULL, BirthDate = F, DependentID = F, DistrictID = F, EmployeeID = F, EmployeeName = F, HasAprilCoverage = F, HasAugustCoverage = F, HasDecemberCoverage = F, HasFebruaryCoverage = F, HasJanuaryCoverage = F, HasJulyCoverage = F, HasJuneCoverage = F, HasMarchCoverage = F, HasMayCoverage = F, HasNovemberCoverage = F, HasOctoberCoverage = F, HasSeptemberCoverage = F, ReportingYear = F, SocialSecurityNumber = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACADependentReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACADependentReport
	#'
	#' This function returns a dataframe or json object of an ACADependentReport
	#' @param ACADependentReportID The ID of the ACADependentReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACADependentReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACADependentReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACADependentReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACADependentReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACADependentReport <- function(ACADependentReportID, BirthDate = F, DependentID = F, DistrictID = F, EmployeeID = F, EmployeeName = F, HasAprilCoverage = F, HasAugustCoverage = F, HasDecemberCoverage = F, HasFebruaryCoverage = F, HasJanuaryCoverage = F, HasJulyCoverage = F, HasJuneCoverage = F, HasMarchCoverage = F, HasMayCoverage = F, HasNovemberCoverage = F, HasOctoberCoverage = F, HasSeptemberCoverage = F, ReportingYear = F, SocialSecurityNumber = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACADependentReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACADependentReport", objectId = ACADependentReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACADependentReport
	#'
	#' This function deletes an ACADependentReport
	#' @param ACADependentReportID The ID of the ACADependentReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACADependentReportID of the deleted ACADependentReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACADependentReport <- function(ACADependentReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACADependentReport", objectId = ACADependentReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACADependentReport
	#'
	#' This function creates an ACADependentReport
	#' @param fieldNames The field values to give the created ACADependentReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACADependentReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACADependentReport <- function(BirthDate = NULL, DependentID = NULL, DistrictID = NULL, EmployeeName = NULL, HasAprilCoverage = NULL, HasAugustCoverage = NULL, HasDecemberCoverage = NULL, HasFebruaryCoverage = NULL, HasJanuaryCoverage = NULL, HasJulyCoverage = NULL, HasJuneCoverage = NULL, HasMarchCoverage = NULL, HasMayCoverage = NULL, HasNovemberCoverage = NULL, HasOctoberCoverage = NULL, HasSeptemberCoverage = NULL, ReportingYear = NULL, SocialSecurityNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACADependentReport", body = list(DataObject = body), searchFields = append("ACADependentReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACADependentReport
	#'
	#' This function modifies an ACADependentReport
	#' @param fieldNames The field values to give the modified ACADependentReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACADependentReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACADependentReport <- function(ACADependentReportID, BirthDate = NULL, DependentID = NULL, DistrictID = NULL, EmployeeName = NULL, HasAprilCoverage = NULL, HasAugustCoverage = NULL, HasDecemberCoverage = NULL, HasFebruaryCoverage = NULL, HasJanuaryCoverage = NULL, HasJulyCoverage = NULL, HasJuneCoverage = NULL, HasMarchCoverage = NULL, HasMayCoverage = NULL, HasNovemberCoverage = NULL, HasOctoberCoverage = NULL, HasSeptemberCoverage = NULL, ReportingYear = NULL, SocialSecurityNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACADependentReport", objectId = ACADependentReportID, body = list(DataObject = body), searchFields = append("ACADependentReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List EEOCSummaries
	#'
	#' This function returns a dataframe or json object of EEOCSummaries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCSummaries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCSummaries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCSummary') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of EEOCSummaries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listEEOCSummaries <- function(searchConditionsList = NULL, AmericanIndianOrAlaskanNativeFemale = F, AmericanIndianOrAlaskanNativeMale = F, AsianFemale = F, AsianMale = F, BlackOrAfricanAmericanFemale = F, BlackOrAfricanAmericanMale = F, CategoryAnnualSalary = F, CategoryDescription = F, CategoryNumber = F, EEOCExtractRunID = F, HispanicOrLatinoFemale = F, HispanicOrLatinoMale = F, IsEEO4 = F, MultipleRacesFemale = F, MultipleRacesMale = F, NativeHawaiianOrPacificIslanderFemale = F, NativeHawaiianOrPacificIslanderMale = F, TotalColumn = F, WhiteFemale = F, WhiteMale = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "EEOCSummary", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an EEOCSummary
	#'
	#' This function returns a dataframe or json object of an EEOCSummary
	#' @param EEOCSummaryID The ID of the EEOCSummary to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given EEOCSummary. Defaults to FALSE for all return fields which, for convenience, returns all fields for the EEOCSummary.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('EEOCSummary') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of EEOCSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getEEOCSummary <- function(EEOCSummaryID, AmericanIndianOrAlaskanNativeFemale = F, AmericanIndianOrAlaskanNativeMale = F, AsianFemale = F, AsianMale = F, BlackOrAfricanAmericanFemale = F, BlackOrAfricanAmericanMale = F, CategoryAnnualSalary = F, CategoryDescription = F, CategoryNumber = F, EEOCExtractRunID = F, HispanicOrLatinoFemale = F, HispanicOrLatinoMale = F, IsEEO4 = F, MultipleRacesFemale = F, MultipleRacesMale = F, NativeHawaiianOrPacificIslanderFemale = F, NativeHawaiianOrPacificIslanderMale = F, TotalColumn = F, WhiteFemale = F, WhiteMale = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "EEOCSummaryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "EEOCSummary", objectId = EEOCSummaryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an EEOCSummary
	#'
	#' This function deletes an EEOCSummary
	#' @param EEOCSummaryID The ID of the EEOCSummary to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The EEOCSummaryID of the deleted EEOCSummary.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteEEOCSummary <- function(EEOCSummaryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "EEOCSummary", objectId = EEOCSummaryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an EEOCSummary
	#'
	#' This function creates an EEOCSummary
	#' @param fieldNames The field values to give the created EEOCSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created EEOCSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createEEOCSummary <- function(AmericanIndianOrAlaskanNativeFemale = NULL, AmericanIndianOrAlaskanNativeMale = NULL, AsianFemale = NULL, AsianMale = NULL, BlackOrAfricanAmericanFemale = NULL, BlackOrAfricanAmericanMale = NULL, CategoryAnnualSalary = NULL, CategoryDescription = NULL, CategoryNumber = NULL, HispanicOrLatinoFemale = NULL, HispanicOrLatinoMale = NULL, IsEEO4 = NULL, MultipleRacesFemale = NULL, MultipleRacesMale = NULL, NativeHawaiianOrPacificIslanderFemale = NULL, NativeHawaiianOrPacificIslanderMale = NULL, TotalColumn = NULL, WhiteFemale = NULL, WhiteMale = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "EEOCSummary", body = list(DataObject = body), searchFields = append("EEOCSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an EEOCSummary
	#'
	#' This function modifies an EEOCSummary
	#' @param fieldNames The field values to give the modified EEOCSummary. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified EEOCSummary
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyEEOCSummary <- function(EEOCSummaryID, AmericanIndianOrAlaskanNativeFemale = NULL, AmericanIndianOrAlaskanNativeMale = NULL, AsianFemale = NULL, AsianMale = NULL, BlackOrAfricanAmericanFemale = NULL, BlackOrAfricanAmericanMale = NULL, CategoryAnnualSalary = NULL, CategoryDescription = NULL, CategoryNumber = NULL, HispanicOrLatinoFemale = NULL, HispanicOrLatinoMale = NULL, IsEEO4 = NULL, MultipleRacesFemale = NULL, MultipleRacesMale = NULL, NativeHawaiianOrPacificIslanderFemale = NULL, NativeHawaiianOrPacificIslanderMale = NULL, TotalColumn = NULL, WhiteFemale = NULL, WhiteMale = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "EEOCSummary", objectId = EEOCSummaryID, body = list(DataObject = body), searchFields = append("EEOCSummaryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAEmployeeReports
	#'
	#' This function returns a dataframe or json object of ACAEmployeeReports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeReports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeReports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeReport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAEmployeeReports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAEmployeeReports <- function(searchConditionsList = NULL, ACAEmployeeRequiredContributionApril = F, ACAEmployeeRequiredContributionAugust = F, ACAEmployeeRequiredContributionDecember = F, ACAEmployeeRequiredContributionFebruary = F, ACAEmployeeRequiredContributionJanuary = F, ACAEmployeeRequiredContributionJuly = F, ACAEmployeeRequiredContributionJune = F, ACAEmployeeRequiredContributionMarch = F, ACAEmployeeRequiredContributionMay = F, ACAEmployeeRequiredContributionNovember = F, ACAEmployeeRequiredContributionOctober = F, ACAEmployeeRequiredContributionSeptember = F, ACAOfferOfCoverageCodeApril = F, ACAOfferOfCoverageCodeAugust = F, ACAOfferOfCoverageCodeDecember = F, ACAOfferOfCoverageCodeFebruary = F, ACAOfferOfCoverageCodeJanuary = F, ACAOfferOfCoverageCodeJuly = F, ACAOfferOfCoverageCodeJune = F, ACAOfferOfCoverageCodeMarch = F, ACAOfferOfCoverageCodeMay = F, ACAOfferOfCoverageCodeNovember = F, ACAOfferOfCoverageCodeOctober = F, ACAOfferOfCoverageCodeSeptember = F, ACASafeHarborCodeApril = F, ACASafeHarborCodeAugust = F, ACASafeHarborCodeDecember = F, ACASafeHarborCodeFebruary = F, ACASafeHarborCodeJanuary = F, ACASafeHarborCodeJuly = F, ACASafeHarborCodeJune = F, ACASafeHarborCodeMarch = F, ACASafeHarborCodeMay = F, ACASafeHarborCodeNovember = F, ACASafeHarborCodeOctober = F, ACASafeHarborCodeSeptember = F, DistrictID = F, EmployeeID = F, EmployeeNumber = F, FullNameLFM = F, PlanStartMonthOverrideApril = F, PlanStartMonthOverrideAugust = F, PlanStartMonthOverrideDecember = F, PlanStartMonthOverrideFebruary = F, PlanStartMonthOverrideJanuary = F, PlanStartMonthOverrideJuly = F, PlanStartMonthOverrideJune = F, PlanStartMonthOverrideMarch = F, PlanStartMonthOverrideMay = F, PlanStartMonthOverrideNovember = F, PlanStartMonthOverrideOctober = F, PlanStartMonthOverrideSeptember = F, ReportingYear = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAEmployeeReport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAEmployeeReport
	#'
	#' This function returns a dataframe or json object of an ACAEmployeeReport
	#' @param ACAEmployeeReportID The ID of the ACAEmployeeReport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAEmployeeReport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAEmployeeReport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAEmployeeReport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAEmployeeReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAEmployeeReport <- function(ACAEmployeeReportID, ACAEmployeeRequiredContributionApril = F, ACAEmployeeRequiredContributionAugust = F, ACAEmployeeRequiredContributionDecember = F, ACAEmployeeRequiredContributionFebruary = F, ACAEmployeeRequiredContributionJanuary = F, ACAEmployeeRequiredContributionJuly = F, ACAEmployeeRequiredContributionJune = F, ACAEmployeeRequiredContributionMarch = F, ACAEmployeeRequiredContributionMay = F, ACAEmployeeRequiredContributionNovember = F, ACAEmployeeRequiredContributionOctober = F, ACAEmployeeRequiredContributionSeptember = F, ACAOfferOfCoverageCodeApril = F, ACAOfferOfCoverageCodeAugust = F, ACAOfferOfCoverageCodeDecember = F, ACAOfferOfCoverageCodeFebruary = F, ACAOfferOfCoverageCodeJanuary = F, ACAOfferOfCoverageCodeJuly = F, ACAOfferOfCoverageCodeJune = F, ACAOfferOfCoverageCodeMarch = F, ACAOfferOfCoverageCodeMay = F, ACAOfferOfCoverageCodeNovember = F, ACAOfferOfCoverageCodeOctober = F, ACAOfferOfCoverageCodeSeptember = F, ACASafeHarborCodeApril = F, ACASafeHarborCodeAugust = F, ACASafeHarborCodeDecember = F, ACASafeHarborCodeFebruary = F, ACASafeHarborCodeJanuary = F, ACASafeHarborCodeJuly = F, ACASafeHarborCodeJune = F, ACASafeHarborCodeMarch = F, ACASafeHarborCodeMay = F, ACASafeHarborCodeNovember = F, ACASafeHarborCodeOctober = F, ACASafeHarborCodeSeptember = F, DistrictID = F, EmployeeID = F, EmployeeNumber = F, FullNameLFM = F, PlanStartMonthOverrideApril = F, PlanStartMonthOverrideAugust = F, PlanStartMonthOverrideDecember = F, PlanStartMonthOverrideFebruary = F, PlanStartMonthOverrideJanuary = F, PlanStartMonthOverrideJuly = F, PlanStartMonthOverrideJune = F, PlanStartMonthOverrideMarch = F, PlanStartMonthOverrideMay = F, PlanStartMonthOverrideNovember = F, PlanStartMonthOverrideOctober = F, PlanStartMonthOverrideSeptember = F, ReportingYear = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAEmployeeReportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeReport", objectId = ACAEmployeeReportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAEmployeeReport
	#'
	#' This function deletes an ACAEmployeeReport
	#' @param ACAEmployeeReportID The ID of the ACAEmployeeReport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAEmployeeReportID of the deleted ACAEmployeeReport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAEmployeeReport <- function(ACAEmployeeReportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeReport", objectId = ACAEmployeeReportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAEmployeeReport
	#'
	#' This function creates an ACAEmployeeReport
	#' @param fieldNames The field values to give the created ACAEmployeeReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAEmployeeReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAEmployeeReport <- function(ACAEmployeeRequiredContributionApril = NULL, ACAEmployeeRequiredContributionAugust = NULL, ACAEmployeeRequiredContributionDecember = NULL, ACAEmployeeRequiredContributionFebruary = NULL, ACAEmployeeRequiredContributionJanuary = NULL, ACAEmployeeRequiredContributionJuly = NULL, ACAEmployeeRequiredContributionJune = NULL, ACAEmployeeRequiredContributionMarch = NULL, ACAEmployeeRequiredContributionMay = NULL, ACAEmployeeRequiredContributionNovember = NULL, ACAEmployeeRequiredContributionOctober = NULL, ACAEmployeeRequiredContributionSeptember = NULL, ACAOfferOfCoverageCodeApril = NULL, ACAOfferOfCoverageCodeAugust = NULL, ACAOfferOfCoverageCodeDecember = NULL, ACAOfferOfCoverageCodeFebruary = NULL, ACAOfferOfCoverageCodeJanuary = NULL, ACAOfferOfCoverageCodeJuly = NULL, ACAOfferOfCoverageCodeJune = NULL, ACAOfferOfCoverageCodeMarch = NULL, ACAOfferOfCoverageCodeMay = NULL, ACAOfferOfCoverageCodeNovember = NULL, ACAOfferOfCoverageCodeOctober = NULL, ACAOfferOfCoverageCodeSeptember = NULL, ACASafeHarborCodeApril = NULL, ACASafeHarborCodeAugust = NULL, ACASafeHarborCodeDecember = NULL, ACASafeHarborCodeFebruary = NULL, ACASafeHarborCodeJanuary = NULL, ACASafeHarborCodeJuly = NULL, ACASafeHarborCodeJune = NULL, ACASafeHarborCodeMarch = NULL, ACASafeHarborCodeMay = NULL, ACASafeHarborCodeNovember = NULL, ACASafeHarborCodeOctober = NULL, ACASafeHarborCodeSeptember = NULL, DistrictID = NULL, EmployeeNumber = NULL, FullNameLFM = NULL, PlanStartMonthOverrideApril = NULL, PlanStartMonthOverrideAugust = NULL, PlanStartMonthOverrideDecember = NULL, PlanStartMonthOverrideFebruary = NULL, PlanStartMonthOverrideJanuary = NULL, PlanStartMonthOverrideJuly = NULL, PlanStartMonthOverrideJune = NULL, PlanStartMonthOverrideMarch = NULL, PlanStartMonthOverrideMay = NULL, PlanStartMonthOverrideNovember = NULL, PlanStartMonthOverrideOctober = NULL, PlanStartMonthOverrideSeptember = NULL, ReportingYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeReport", body = list(DataObject = body), searchFields = append("ACAEmployeeReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAEmployeeReport
	#'
	#' This function modifies an ACAEmployeeReport
	#' @param fieldNames The field values to give the modified ACAEmployeeReport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAEmployeeReport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAEmployeeReport <- function(ACAEmployeeReportID, ACAEmployeeRequiredContributionApril = NULL, ACAEmployeeRequiredContributionAugust = NULL, ACAEmployeeRequiredContributionDecember = NULL, ACAEmployeeRequiredContributionFebruary = NULL, ACAEmployeeRequiredContributionJanuary = NULL, ACAEmployeeRequiredContributionJuly = NULL, ACAEmployeeRequiredContributionJune = NULL, ACAEmployeeRequiredContributionMarch = NULL, ACAEmployeeRequiredContributionMay = NULL, ACAEmployeeRequiredContributionNovember = NULL, ACAEmployeeRequiredContributionOctober = NULL, ACAEmployeeRequiredContributionSeptember = NULL, ACAOfferOfCoverageCodeApril = NULL, ACAOfferOfCoverageCodeAugust = NULL, ACAOfferOfCoverageCodeDecember = NULL, ACAOfferOfCoverageCodeFebruary = NULL, ACAOfferOfCoverageCodeJanuary = NULL, ACAOfferOfCoverageCodeJuly = NULL, ACAOfferOfCoverageCodeJune = NULL, ACAOfferOfCoverageCodeMarch = NULL, ACAOfferOfCoverageCodeMay = NULL, ACAOfferOfCoverageCodeNovember = NULL, ACAOfferOfCoverageCodeOctober = NULL, ACAOfferOfCoverageCodeSeptember = NULL, ACASafeHarborCodeApril = NULL, ACASafeHarborCodeAugust = NULL, ACASafeHarborCodeDecember = NULL, ACASafeHarborCodeFebruary = NULL, ACASafeHarborCodeJanuary = NULL, ACASafeHarborCodeJuly = NULL, ACASafeHarborCodeJune = NULL, ACASafeHarborCodeMarch = NULL, ACASafeHarborCodeMay = NULL, ACASafeHarborCodeNovember = NULL, ACASafeHarborCodeOctober = NULL, ACASafeHarborCodeSeptember = NULL, DistrictID = NULL, EmployeeNumber = NULL, FullNameLFM = NULL, PlanStartMonthOverrideApril = NULL, PlanStartMonthOverrideAugust = NULL, PlanStartMonthOverrideDecember = NULL, PlanStartMonthOverrideFebruary = NULL, PlanStartMonthOverrideJanuary = NULL, PlanStartMonthOverrideJuly = NULL, PlanStartMonthOverrideJune = NULL, PlanStartMonthOverrideMarch = NULL, PlanStartMonthOverrideMay = NULL, PlanStartMonthOverrideNovember = NULL, PlanStartMonthOverrideOctober = NULL, PlanStartMonthOverrideSeptember = NULL, ReportingYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAEmployeeReport", objectId = ACAEmployeeReportID, body = list(DataObject = body), searchFields = append("ACAEmployeeReportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalNCESSchools
	#'
	#' This function returns a dataframe or json object of FederalNCESSchools
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalNCESSchools. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalNCESSchools.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalNCESSchool') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalNCESSchools
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalNCESSchools <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DistrictID = F, FederalNCESSchoolID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalNCESSchool", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalNCESSchool
	#'
	#' This function returns a dataframe or json object of a FederalNCESSchool
	#' @param FederalNCESSchoolID The ID of the FederalNCESSchool to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalNCESSchool. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalNCESSchool.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalNCESSchool') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalNCESSchool
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalNCESSchool <- function(FederalNCESSchoolID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, DistrictID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalNCESSchoolID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalNCESSchool", objectId = FederalNCESSchoolID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalNCESSchool
	#'
	#' This function deletes a FederalNCESSchool
	#' @param FederalNCESSchoolID The ID of the FederalNCESSchool to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalNCESSchoolID of the deleted FederalNCESSchool.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalNCESSchool <- function(FederalNCESSchoolID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalNCESSchool", objectId = FederalNCESSchoolID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalNCESSchool
	#'
	#' This function creates a FederalNCESSchool
	#' @param fieldNames The field values to give the created FederalNCESSchool. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalNCESSchool
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalNCESSchool <- function(Code = NULL, Description = NULL, DistrictID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalNCESSchool", body = list(DataObject = body), searchFields = append("FederalNCESSchoolID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalNCESSchool
	#'
	#' This function modifies a FederalNCESSchool
	#' @param fieldNames The field values to give the modified FederalNCESSchool. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalNCESSchool
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalNCESSchool <- function(FederalNCESSchoolID, Code = NULL, Description = NULL, DistrictID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalNCESSchool", objectId = FederalNCESSchoolID, body = list(DataObject = body), searchFields = append("FederalNCESSchoolID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalCRDCStaffTypes
	#'
	#' This function returns a dataframe or json object of FederalCRDCStaffTypes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalCRDCStaffTypes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalCRDCStaffTypes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalCRDCStaffType') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalCRDCStaffTypes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalCRDCStaffTypes <- function(searchConditionsList = NULL, Code = F, CRDCStaffCategoryType = F, CreatedTime = F, Description = F, FederalCRDCStaffTypeID = F, IsInvalid = F, IsTotalPersonnel = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalCRDCStaffType", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalCRDCStaffType
	#'
	#' This function returns a dataframe or json object of a FederalCRDCStaffType
	#' @param FederalCRDCStaffTypeID The ID of the FederalCRDCStaffType to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalCRDCStaffType. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalCRDCStaffType.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalCRDCStaffType') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalCRDCStaffType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalCRDCStaffType <- function(FederalCRDCStaffTypeID, Code = F, CRDCStaffCategoryType = F, CreatedTime = F, Description = F, IsInvalid = F, IsTotalPersonnel = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalCRDCStaffTypeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalCRDCStaffType", objectId = FederalCRDCStaffTypeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalCRDCStaffType
	#'
	#' This function deletes a FederalCRDCStaffType
	#' @param FederalCRDCStaffTypeID The ID of the FederalCRDCStaffType to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalCRDCStaffTypeID of the deleted FederalCRDCStaffType.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalCRDCStaffType <- function(FederalCRDCStaffTypeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalCRDCStaffType", objectId = FederalCRDCStaffTypeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalCRDCStaffType
	#'
	#' This function creates a FederalCRDCStaffType
	#' @param fieldNames The field values to give the created FederalCRDCStaffType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalCRDCStaffType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalCRDCStaffType <- function(CRDCStaffCategoryType = NULL, Description = NULL, IsInvalid = NULL, IsTotalPersonnel = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalCRDCStaffType", body = list(DataObject = body), searchFields = append("FederalCRDCStaffTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalCRDCStaffType
	#'
	#' This function modifies a FederalCRDCStaffType
	#' @param fieldNames The field values to give the modified FederalCRDCStaffType. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalCRDCStaffType
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalCRDCStaffType <- function(FederalCRDCStaffTypeID, CRDCStaffCategoryType = NULL, Description = NULL, IsInvalid = NULL, IsTotalPersonnel = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalCRDCStaffType", objectId = FederalCRDCStaffTypeID, body = list(DataObject = body), searchFields = append("FederalCRDCStaffTypeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCStaffDetails
	#'
	#' This function returns a dataframe or json object of CRDCStaffDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCStaffDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCStaffDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCStaffDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCStaffDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCStaffDetails <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCStaffCategoryType = F, CRDCStaffDetailID = F, CreatedTime = F, DaysAbsent = F, DistrictID = F, EmployeeID = F, FederalCRDCStaffTypeCode = F, FederalCRDCStaffTypeID = F, FederalFundsSalary = F, FederalNCESSchoolID = F, FTE = F, FullName = F, IsCurrentYear = F, IsPriorYear = F, IsTeacherCertified = F, IsTeacherFirstYear = F, IsTeacherSecondYear = F, IsTotalPersonnel = F, ModifiedTime = F, StateLocalSalary = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCStaffDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCStaffDetail
	#'
	#' This function returns a dataframe or json object of a CRDCStaffDetail
	#' @param CRDCStaffDetailID The ID of the CRDCStaffDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCStaffDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCStaffDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCStaffDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCStaffDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCStaffDetail <- function(CRDCStaffDetailID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCStaffCategoryType = F, CreatedTime = F, DaysAbsent = F, DistrictID = F, EmployeeID = F, FederalCRDCStaffTypeCode = F, FederalCRDCStaffTypeID = F, FederalFundsSalary = F, FederalNCESSchoolID = F, FTE = F, FullName = F, IsCurrentYear = F, IsPriorYear = F, IsTeacherCertified = F, IsTeacherFirstYear = F, IsTeacherSecondYear = F, IsTotalPersonnel = F, ModifiedTime = F, StateLocalSalary = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCStaffDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCStaffDetail", objectId = CRDCStaffDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCStaffDetail
	#'
	#' This function deletes a CRDCStaffDetail
	#' @param CRDCStaffDetailID The ID of the CRDCStaffDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCStaffDetailID of the deleted CRDCStaffDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCStaffDetail <- function(CRDCStaffDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCStaffDetail", objectId = CRDCStaffDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCStaffDetail
	#'
	#' This function creates a CRDCStaffDetail
	#' @param fieldNames The field values to give the created CRDCStaffDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCStaffDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCStaffDetail <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CRDCStaffCategoryType = NULL, DaysAbsent = NULL, DistrictID = NULL, EmployeeID = NULL, FederalCRDCStaffTypeCode = NULL, FederalCRDCStaffTypeID = NULL, FederalFundsSalary = NULL, FederalNCESSchoolID = NULL, FTE = NULL, FullName = NULL, IsCurrentYear = NULL, IsPriorYear = NULL, IsTeacherCertified = NULL, IsTeacherFirstYear = NULL, IsTeacherSecondYear = NULL, IsTotalPersonnel = NULL, StateLocalSalary = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCStaffDetail", body = list(DataObject = body), searchFields = append("CRDCStaffDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCStaffDetail
	#'
	#' This function modifies a CRDCStaffDetail
	#' @param fieldNames The field values to give the modified CRDCStaffDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCStaffDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCStaffDetail <- function(CRDCStaffDetailID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, CRDCStaffCategoryType = NULL, DaysAbsent = NULL, DistrictID = NULL, EmployeeID = NULL, FederalCRDCStaffTypeCode = NULL, FederalCRDCStaffTypeID = NULL, FederalFundsSalary = NULL, FederalNCESSchoolID = NULL, FTE = NULL, FullName = NULL, IsCurrentYear = NULL, IsPriorYear = NULL, IsTeacherCertified = NULL, IsTeacherFirstYear = NULL, IsTeacherSecondYear = NULL, IsTotalPersonnel = NULL, StateLocalSalary = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCStaffDetail", objectId = CRDCStaffDetailID, body = list(DataObject = body), searchFields = append("CRDCStaffDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List StateReportingFederalConfigDistricts
	#'
	#' This function returns a dataframe or json object of StateReportingFederalConfigDistricts
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingFederalConfigDistricts. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingFederalConfigDistricts.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingFederalConfigDistrict') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of StateReportingFederalConfigDistricts
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listStateReportingFederalConfigDistricts <- function(searchConditionsList = NULL, ConfigDistrictID = F, CRDCFTEType = F, CreatedTime = F, CreateLocalityFiles = F, DistrictID = F, FirstYearTeacherYearsExperienceHigh = F, FirstYearTeacherYearsExperienceLow = F, ModifiedTime = F, PlanStartMonth = F, SecondYearTeacherYearsExperienceHigh = F, SecondYearTeacherYearsExperienceLow = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ConfigDistrict", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a StateReportingFederalConfigDistrict
	#'
	#' This function returns a dataframe or json object of a StateReportingFederalConfigDistrict
	#' @param StateReportingFederalConfigDistrictID The ID of the StateReportingFederalConfigDistrict to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given StateReportingFederalConfigDistrict. Defaults to FALSE for all return fields which, for convenience, returns all fields for the StateReportingFederalConfigDistrict.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('StateReportingFederalConfigDistrict') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of StateReportingFederalConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getStateReportingFederalConfigDistrict <- function(StateReportingFederalConfigDistrictID, ConfigDistrictID = F, CRDCFTEType = F, CreatedTime = F, CreateLocalityFiles = F, DistrictID = F, FirstYearTeacherYearsExperienceHigh = F, FirstYearTeacherYearsExperienceLow = F, ModifiedTime = F, PlanStartMonth = F, SecondYearTeacherYearsExperienceHigh = F, SecondYearTeacherYearsExperienceLow = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "StateReportingFederalConfigDistrictID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ConfigDistrict", objectId = StateReportingFederalConfigDistrictID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a StateReportingFederalConfigDistrict
	#'
	#' This function deletes a StateReportingFederalConfigDistrict
	#' @param StateReportingFederalConfigDistrictID The ID of the StateReportingFederalConfigDistrict to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The StateReportingFederalConfigDistrictID of the deleted StateReportingFederalConfigDistrict.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteStateReportingFederalConfigDistrict <- function(StateReportingFederalConfigDistrictID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ConfigDistrict", objectId = StateReportingFederalConfigDistrictID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a StateReportingFederalConfigDistrict
	#'
	#' This function creates a StateReportingFederalConfigDistrict
	#' @param fieldNames The field values to give the created StateReportingFederalConfigDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created StateReportingFederalConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createStateReportingFederalConfigDistrict <- function(CRDCFTEType = NULL, CreateLocalityFiles = NULL, DistrictID = NULL, FirstYearTeacherYearsExperienceHigh = NULL, FirstYearTeacherYearsExperienceLow = NULL, PlanStartMonth = NULL, SecondYearTeacherYearsExperienceHigh = NULL, SecondYearTeacherYearsExperienceLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ConfigDistrict", body = list(DataObject = body), searchFields = append("ConfigDistrictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a StateReportingFederalConfigDistrict
	#'
	#' This function modifies a StateReportingFederalConfigDistrict
	#' @param fieldNames The field values to give the modified StateReportingFederalConfigDistrict. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified StateReportingFederalConfigDistrict
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyStateReportingFederalConfigDistrict <- function(ConfigDistrictID, CRDCFTEType = NULL, CreateLocalityFiles = NULL, DistrictID = NULL, FirstYearTeacherYearsExperienceHigh = NULL, FirstYearTeacherYearsExperienceLow = NULL, PlanStartMonth = NULL, SecondYearTeacherYearsExperienceHigh = NULL, SecondYearTeacherYearsExperienceLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ConfigDistrict", objectId = ConfigDistrictID, body = list(DataObject = body), searchFields = append("ConfigDistrictID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolAPComputerScienceV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolAPComputerScienceV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPComputerScienceV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPComputerScienceV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPComputerScienceV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolAPComputerScienceV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolAPComputerScienceV1s <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolAPComputerScienceV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolAPComputerScienceV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolAPComputerScienceV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolAPComputerScienceV1
	#' @param CRDCSchoolAPComputerScienceV1ID The ID of the CRDCSchoolAPComputerScienceV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolAPComputerScienceV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolAPComputerScienceV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolAPComputerScienceV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolAPComputerScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolAPComputerScienceV1 <- function(CRDCSchoolAPComputerScienceV1ID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, HasStudents = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolAPComputerScienceV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPComputerScienceV1", objectId = CRDCSchoolAPComputerScienceV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolAPComputerScienceV1
	#'
	#' This function deletes a CRDCSchoolAPComputerScienceV1
	#' @param CRDCSchoolAPComputerScienceV1ID The ID of the CRDCSchoolAPComputerScienceV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolAPComputerScienceV1ID of the deleted CRDCSchoolAPComputerScienceV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolAPComputerScienceV1 <- function(CRDCSchoolAPComputerScienceV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPComputerScienceV1", objectId = CRDCSchoolAPComputerScienceV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolAPComputerScienceV1
	#'
	#' This function creates a CRDCSchoolAPComputerScienceV1
	#' @param fieldNames The field values to give the created CRDCSchoolAPComputerScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolAPComputerScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolAPComputerScienceV1 <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPComputerScienceV1", body = list(DataObject = body), searchFields = append("CRDCSchoolAPComputerScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolAPComputerScienceV1
	#'
	#' This function modifies a CRDCSchoolAPComputerScienceV1
	#' @param fieldNames The field values to give the modified CRDCSchoolAPComputerScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolAPComputerScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolAPComputerScienceV1 <- function(CRDCSchoolAPComputerScienceV1ID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, HasStudents = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolAPComputerScienceV1", objectId = CRDCSchoolAPComputerScienceV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolAPComputerScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolComputerScienceV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolComputerScienceV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolComputerScienceV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolComputerScienceV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolComputerScienceV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolComputerScienceV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolComputerScienceV1s <- function(searchConditionsList = NULL, CertifiedClassCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, ClassCount = F, CRDCSchoolComputerScienceV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolComputerScienceV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolComputerScienceV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolComputerScienceV1
	#' @param CRDCSchoolComputerScienceV1ID The ID of the CRDCSchoolComputerScienceV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolComputerScienceV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolComputerScienceV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolComputerScienceV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolComputerScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolComputerScienceV1 <- function(CRDCSchoolComputerScienceV1ID, CertifiedClassCount = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, ClassCount = F, CreatedTime = F, FederalNCESSchoolID = F, FemaleAlaskanCount = F, FemaleAsianCount = F, FemaleBlackCount = F, FemaleHawaiianCount = F, FemaleHispanicCount = F, FemaleIDEACount = F, FemaleLEPCount = F, FemaleMultiRaceCount = F, FemaleWhiteCount = F, MaleAlaskanCount = F, MaleAsianCount = F, MaleBlackCount = F, MaleHawaiianCount = F, MaleHispanicCount = F, MaleIDEACount = F, MaleLEPCount = F, MaleMultiRaceCount = F, MaleWhiteCount = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolComputerScienceV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolComputerScienceV1", objectId = CRDCSchoolComputerScienceV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolComputerScienceV1
	#'
	#' This function deletes a CRDCSchoolComputerScienceV1
	#' @param CRDCSchoolComputerScienceV1ID The ID of the CRDCSchoolComputerScienceV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolComputerScienceV1ID of the deleted CRDCSchoolComputerScienceV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolComputerScienceV1 <- function(CRDCSchoolComputerScienceV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolComputerScienceV1", objectId = CRDCSchoolComputerScienceV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolComputerScienceV1
	#'
	#' This function creates a CRDCSchoolComputerScienceV1
	#' @param fieldNames The field values to give the created CRDCSchoolComputerScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolComputerScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolComputerScienceV1 <- function(CertifiedClassCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, ClassCount = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolComputerScienceV1", body = list(DataObject = body), searchFields = append("CRDCSchoolComputerScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolComputerScienceV1
	#'
	#' This function modifies a CRDCSchoolComputerScienceV1
	#' @param fieldNames The field values to give the modified CRDCSchoolComputerScienceV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolComputerScienceV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolComputerScienceV1 <- function(CRDCSchoolComputerScienceV1ID, CertifiedClassCount = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, ClassCount = NULL, FederalNCESSchoolID = NULL, FemaleAlaskanCount = NULL, FemaleAsianCount = NULL, FemaleBlackCount = NULL, FemaleHawaiianCount = NULL, FemaleHispanicCount = NULL, FemaleIDEACount = NULL, FemaleLEPCount = NULL, FemaleMultiRaceCount = NULL, FemaleWhiteCount = NULL, MaleAlaskanCount = NULL, MaleAsianCount = NULL, MaleBlackCount = NULL, MaleHawaiianCount = NULL, MaleHispanicCount = NULL, MaleIDEACount = NULL, MaleLEPCount = NULL, MaleMultiRaceCount = NULL, MaleWhiteCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolComputerScienceV1", objectId = CRDCSchoolComputerScienceV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolComputerScienceV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolInternetV1s
	#'
	#' This function returns a dataframe or json object of CRDCSchoolInternetV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolInternetV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolInternetV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolInternetV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolInternetV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolInternetV1s <- function(searchConditionsList = NULL, AllowsSchoolDevices = F, AllowsStudentDevices = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCSchoolInternetV1ID = F, CreatedTime = F, FederalNCESSchoolID = F, HasFiberOptic = F, HasWiFi = F, ModifiedTime = F, NumberWiFiDevices = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolInternetV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolInternetV1
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolInternetV1
	#' @param CRDCSchoolInternetV1ID The ID of the CRDCSchoolInternetV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolInternetV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolInternetV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolInternetV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolInternetV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolInternetV1 <- function(CRDCSchoolInternetV1ID, AllowsSchoolDevices = F, AllowsStudentDevices = F, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, FederalNCESSchoolID = F, HasFiberOptic = F, HasWiFi = F, ModifiedTime = F, NumberWiFiDevices = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolInternetV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolInternetV1", objectId = CRDCSchoolInternetV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolInternetV1
	#'
	#' This function deletes a CRDCSchoolInternetV1
	#' @param CRDCSchoolInternetV1ID The ID of the CRDCSchoolInternetV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolInternetV1ID of the deleted CRDCSchoolInternetV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolInternetV1 <- function(CRDCSchoolInternetV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolInternetV1", objectId = CRDCSchoolInternetV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolInternetV1
	#'
	#' This function creates a CRDCSchoolInternetV1
	#' @param fieldNames The field values to give the created CRDCSchoolInternetV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolInternetV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolInternetV1 <- function(AllowsSchoolDevices = NULL, AllowsStudentDevices = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasFiberOptic = NULL, HasWiFi = NULL, NumberWiFiDevices = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolInternetV1", body = list(DataObject = body), searchFields = append("CRDCSchoolInternetV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolInternetV1
	#'
	#' This function modifies a CRDCSchoolInternetV1
	#' @param fieldNames The field values to give the modified CRDCSchoolInternetV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolInternetV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolInternetV1 <- function(CRDCSchoolInternetV1ID, AllowsSchoolDevices = NULL, AllowsStudentDevices = NULL, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, FederalNCESSchoolID = NULL, HasFiberOptic = NULL, HasWiFi = NULL, NumberWiFiDevices = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolInternetV1", objectId = CRDCSchoolInternetV1ID, body = list(DataObject = body), searchFields = append("CRDCSchoolInternetV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCForms
	#'
	#' This function returns a dataframe or json object of CRDCForms
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCForms. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCForms.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCForm') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCForms
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCForms <- function(searchConditionsList = NULL, Code = F, CRDCFormID = F, CreatedTime = F, Description = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCForm", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCForm
	#'
	#' This function returns a dataframe or json object of a CRDCForm
	#' @param CRDCFormID The ID of the CRDCForm to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCForm. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCForm.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCForm') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCForm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCForm <- function(CRDCFormID, Code = F, CreatedTime = F, Description = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCFormID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCForm", objectId = CRDCFormID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCForm
	#'
	#' This function deletes a CRDCForm
	#' @param CRDCFormID The ID of the CRDCForm to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCFormID of the deleted CRDCForm.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCForm <- function(CRDCFormID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCForm", objectId = CRDCFormID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCForm
	#'
	#' This function creates a CRDCForm
	#' @param fieldNames The field values to give the created CRDCForm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCForm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCForm <- function(Code = NULL, Description = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCForm", body = list(DataObject = body), searchFields = append("CRDCFormID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCForm
	#'
	#' This function modifies a CRDCForm
	#' @param fieldNames The field values to give the modified CRDCForm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCForm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCForm <- function(CRDCFormID, Code = NULL, Description = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCForm", objectId = CRDCFormID, body = list(DataObject = body), searchFields = append("CRDCFormID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCClassDetails
	#'
	#' This function returns a dataframe or json object of CRDCClassDetails
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCClassDetails. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCClassDetails.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCClassDetail') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCClassDetails
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCClassDetails <- function(searchConditionsList = NULL, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CRDCClassDetailID = F, CreatedTime = F, EntityID = F, FederalNCESSchoolID = F, IsAdvancedMathCertifiedGrade0912 = F, IsAdvancedMathGrade0912 = F, IsAlgebraGeometryFemaleOnly = F, IsAlgebraGeometryMaleOnly = F, IsAlgebraICertifiedGrade0708 = F, IsAlgebraICertifiedGrade0912 = F, IsAlgebraIGrade0708 = F, IsAlgebraIGrade0912 = F, IsAlgebraIICertifiedGrade0912 = F, IsAlgebraIIGrade0912 = F, IsBiologyCertifiedGrade0912 = F, IsBiologyGrade0912 = F, IsCalculusCertifiedGrade0912 = F, IsCalculusGrade0912 = F, IsChemistryCertifiedGrade0912 = F, IsChemistryGrade0912 = F, IsEnglishFemaleOnly = F, IsEnglishMaleOnly = F, IsGeometryCertifiedGrade0912 = F, IsGeometryGrade0912 = F, IsOtherMathFemaleOnly = F, IsOtherMathMaleOnly = F, IsOtherSubjectsFemaleOnly = F, IsOtherSubjectsMaleOnly = F, IsPhysicsCertifiedGrade0912 = F, IsPhysicsGrade0912 = F, IsScienceFemaleOnly = F, IsScienceMaleOnly = F, ModifiedTime = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCClassDetail", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCClassDetail
	#'
	#' This function returns a dataframe or json object of a CRDCClassDetail
	#' @param CRDCClassDetailID The ID of the CRDCClassDetail to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCClassDetail. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCClassDetail.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCClassDetail') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCClassDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCClassDetail <- function(CRDCClassDetailID, CivilRightsDataCollectionID = F, CivilRightsDataCollectionRunHistoryID = F, CreatedTime = F, EntityID = F, FederalNCESSchoolID = F, IsAdvancedMathCertifiedGrade0912 = F, IsAdvancedMathGrade0912 = F, IsAlgebraGeometryFemaleOnly = F, IsAlgebraGeometryMaleOnly = F, IsAlgebraICertifiedGrade0708 = F, IsAlgebraICertifiedGrade0912 = F, IsAlgebraIGrade0708 = F, IsAlgebraIGrade0912 = F, IsAlgebraIICertifiedGrade0912 = F, IsAlgebraIIGrade0912 = F, IsBiologyCertifiedGrade0912 = F, IsBiologyGrade0912 = F, IsCalculusCertifiedGrade0912 = F, IsCalculusGrade0912 = F, IsChemistryCertifiedGrade0912 = F, IsChemistryGrade0912 = F, IsEnglishFemaleOnly = F, IsEnglishMaleOnly = F, IsGeometryCertifiedGrade0912 = F, IsGeometryGrade0912 = F, IsOtherMathFemaleOnly = F, IsOtherMathMaleOnly = F, IsOtherSubjectsFemaleOnly = F, IsOtherSubjectsMaleOnly = F, IsPhysicsCertifiedGrade0912 = F, IsPhysicsGrade0912 = F, IsScienceFemaleOnly = F, IsScienceMaleOnly = F, ModifiedTime = F, SectionID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCClassDetailID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCClassDetail", objectId = CRDCClassDetailID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCClassDetail
	#'
	#' This function deletes a CRDCClassDetail
	#' @param CRDCClassDetailID The ID of the CRDCClassDetail to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCClassDetailID of the deleted CRDCClassDetail.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCClassDetail <- function(CRDCClassDetailID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCClassDetail", objectId = CRDCClassDetailID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCClassDetail
	#'
	#' This function creates a CRDCClassDetail
	#' @param fieldNames The field values to give the created CRDCClassDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCClassDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCClassDetail <- function(CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EntityID = NULL, FederalNCESSchoolID = NULL, IsAdvancedMathCertifiedGrade0912 = NULL, IsAdvancedMathGrade0912 = NULL, IsAlgebraGeometryFemaleOnly = NULL, IsAlgebraGeometryMaleOnly = NULL, IsAlgebraICertifiedGrade0708 = NULL, IsAlgebraICertifiedGrade0912 = NULL, IsAlgebraIGrade0708 = NULL, IsAlgebraIGrade0912 = NULL, IsAlgebraIICertifiedGrade0912 = NULL, IsAlgebraIIGrade0912 = NULL, IsBiologyCertifiedGrade0912 = NULL, IsBiologyGrade0912 = NULL, IsCalculusCertifiedGrade0912 = NULL, IsCalculusGrade0912 = NULL, IsChemistryCertifiedGrade0912 = NULL, IsChemistryGrade0912 = NULL, IsEnglishFemaleOnly = NULL, IsEnglishMaleOnly = NULL, IsGeometryCertifiedGrade0912 = NULL, IsGeometryGrade0912 = NULL, IsOtherMathFemaleOnly = NULL, IsOtherMathMaleOnly = NULL, IsOtherSubjectsFemaleOnly = NULL, IsOtherSubjectsMaleOnly = NULL, IsPhysicsCertifiedGrade0912 = NULL, IsPhysicsGrade0912 = NULL, IsScienceFemaleOnly = NULL, IsScienceMaleOnly = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCClassDetail", body = list(DataObject = body), searchFields = append("CRDCClassDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCClassDetail
	#'
	#' This function modifies a CRDCClassDetail
	#' @param fieldNames The field values to give the modified CRDCClassDetail. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCClassDetail
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCClassDetail <- function(CRDCClassDetailID, CivilRightsDataCollectionID = NULL, CivilRightsDataCollectionRunHistoryID = NULL, EntityID = NULL, FederalNCESSchoolID = NULL, IsAdvancedMathCertifiedGrade0912 = NULL, IsAdvancedMathGrade0912 = NULL, IsAlgebraGeometryFemaleOnly = NULL, IsAlgebraGeometryMaleOnly = NULL, IsAlgebraICertifiedGrade0708 = NULL, IsAlgebraICertifiedGrade0912 = NULL, IsAlgebraIGrade0708 = NULL, IsAlgebraIGrade0912 = NULL, IsAlgebraIICertifiedGrade0912 = NULL, IsAlgebraIIGrade0912 = NULL, IsBiologyCertifiedGrade0912 = NULL, IsBiologyGrade0912 = NULL, IsCalculusCertifiedGrade0912 = NULL, IsCalculusGrade0912 = NULL, IsChemistryCertifiedGrade0912 = NULL, IsChemistryGrade0912 = NULL, IsEnglishFemaleOnly = NULL, IsEnglishMaleOnly = NULL, IsGeometryCertifiedGrade0912 = NULL, IsGeometryGrade0912 = NULL, IsOtherMathFemaleOnly = NULL, IsOtherMathMaleOnly = NULL, IsOtherSubjectsFemaleOnly = NULL, IsOtherSubjectsMaleOnly = NULL, IsPhysicsCertifiedGrade0912 = NULL, IsPhysicsGrade0912 = NULL, IsScienceFemaleOnly = NULL, IsScienceMaleOnly = NULL, SectionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCClassDetail", objectId = CRDCClassDetailID, body = list(DataObject = body), searchFields = append("CRDCClassDetailID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCLEAExportFields
	#'
	#' This function returns a dataframe or json object of CRDCLEAExportFields
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAExportFields. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAExportFields.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAExportField') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCLEAExportFields
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCLEAExportFields <- function(searchConditionsList = NULL, CRDCLEAExportFieldID = F, CreatedTime = F, Field = F, Header = F, IsInvalid = F, ModifiedTime = F, Order = F, Section = F, SkywardHash = F, SkywardID = F, Table = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCLEAExportField", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCLEAExportField
	#'
	#' This function returns a dataframe or json object of a CRDCLEAExportField
	#' @param CRDCLEAExportFieldID The ID of the CRDCLEAExportField to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCLEAExportField. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCLEAExportField.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCLEAExportField') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCLEAExportField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCLEAExportField <- function(CRDCLEAExportFieldID, CreatedTime = F, Field = F, Header = F, IsInvalid = F, ModifiedTime = F, Order = F, Section = F, SkywardHash = F, SkywardID = F, Table = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCLEAExportFieldID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAExportField", objectId = CRDCLEAExportFieldID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCLEAExportField
	#'
	#' This function deletes a CRDCLEAExportField
	#' @param CRDCLEAExportFieldID The ID of the CRDCLEAExportField to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCLEAExportFieldID of the deleted CRDCLEAExportField.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCLEAExportField <- function(CRDCLEAExportFieldID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAExportField", objectId = CRDCLEAExportFieldID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCLEAExportField
	#'
	#' This function creates a CRDCLEAExportField
	#' @param fieldNames The field values to give the created CRDCLEAExportField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCLEAExportField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCLEAExportField <- function(Field = NULL, Header = NULL, IsInvalid = NULL, Order = NULL, Section = NULL, Table = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCLEAExportField", body = list(DataObject = body), searchFields = append("CRDCLEAExportFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCLEAExportField
	#'
	#' This function modifies a CRDCLEAExportField
	#' @param fieldNames The field values to give the modified CRDCLEAExportField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCLEAExportField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCLEAExportField <- function(CRDCLEAExportFieldID, Field = NULL, Header = NULL, IsInvalid = NULL, Order = NULL, Section = NULL, Table = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCLEAExportField", objectId = CRDCLEAExportFieldID, body = list(DataObject = body), searchFields = append("CRDCLEAExportFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List CRDCSchoolExportFields
	#'
	#' This function returns a dataframe or json object of CRDCSchoolExportFields
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolExportFields. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolExportFields.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolExportField') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of CRDCSchoolExportFields
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listCRDCSchoolExportFields <- function(searchConditionsList = NULL, CRDCSchoolExportFieldID = F, CreatedTime = F, Field = F, Header = F, IsInvalid = F, ModifiedTime = F, Order = F, Section = F, SkywardHash = F, SkywardID = F, Table = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "CRDCSchoolExportField", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a CRDCSchoolExportField
	#'
	#' This function returns a dataframe or json object of a CRDCSchoolExportField
	#' @param CRDCSchoolExportFieldID The ID of the CRDCSchoolExportField to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given CRDCSchoolExportField. Defaults to FALSE for all return fields which, for convenience, returns all fields for the CRDCSchoolExportField.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('CRDCSchoolExportField') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of CRDCSchoolExportField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getCRDCSchoolExportField <- function(CRDCSchoolExportFieldID, CreatedTime = F, Field = F, Header = F, IsInvalid = F, ModifiedTime = F, Order = F, Section = F, SkywardHash = F, SkywardID = F, Table = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "CRDCSchoolExportFieldID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolExportField", objectId = CRDCSchoolExportFieldID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a CRDCSchoolExportField
	#'
	#' This function deletes a CRDCSchoolExportField
	#' @param CRDCSchoolExportFieldID The ID of the CRDCSchoolExportField to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The CRDCSchoolExportFieldID of the deleted CRDCSchoolExportField.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteCRDCSchoolExportField <- function(CRDCSchoolExportFieldID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolExportField", objectId = CRDCSchoolExportFieldID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a CRDCSchoolExportField
	#'
	#' This function creates a CRDCSchoolExportField
	#' @param fieldNames The field values to give the created CRDCSchoolExportField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created CRDCSchoolExportField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createCRDCSchoolExportField <- function(Field = NULL, Header = NULL, IsInvalid = NULL, Order = NULL, Section = NULL, Table = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolExportField", body = list(DataObject = body), searchFields = append("CRDCSchoolExportFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a CRDCSchoolExportField
	#'
	#' This function modifies a CRDCSchoolExportField
	#' @param fieldNames The field values to give the modified CRDCSchoolExportField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified CRDCSchoolExportField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyCRDCSchoolExportField <- function(CRDCSchoolExportFieldID, Field = NULL, Header = NULL, IsInvalid = NULL, Order = NULL, Section = NULL, Table = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "CRDCSchoolExportField", objectId = CRDCSchoolExportFieldID, body = list(DataObject = body), searchFields = append("CRDCSchoolExportFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2Countries
	#'
	#' This function returns a dataframe or json object of W2Countries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2Countries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2Countries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2Country') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2Countries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2Countries <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, DisplayName = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2Country", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2Country
	#'
	#' This function returns a dataframe or json object of a W2Country
	#' @param W2CountryID The ID of the W2Country to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2Country. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2Country.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2Country') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2Country
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2Country <- function(W2CountryID, Code = F, CreatedTime = F, DisplayName = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CountryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2Country", objectId = W2CountryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2Country
	#'
	#' This function deletes a W2Country
	#' @param W2CountryID The ID of the W2Country to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CountryID of the deleted W2Country.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2Country <- function(W2CountryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2Country", objectId = W2CountryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2Country
	#'
	#' This function creates a W2Country
	#' @param fieldNames The field values to give the created W2Country. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2Country
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2Country <- function(Code = NULL, DisplayName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2Country", body = list(DataObject = body), searchFields = append("W2CountryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2Country
	#'
	#' This function modifies a W2Country
	#' @param fieldNames The field values to give the modified W2Country. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2Country
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2Country <- function(W2CountryID, Code = NULL, DisplayName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2Country", objectId = W2CountryID, body = list(DataObject = body), searchFields = append("W2CountryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalACASoftwareIdentifiers
	#'
	#' This function returns a dataframe or json object of FederalACASoftwareIdentifiers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalACASoftwareIdentifiers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalACASoftwareIdentifiers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalACASoftwareIdentifier') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalACASoftwareIdentifiers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalACASoftwareIdentifiers <- function(searchConditionsList = NULL, CreatedTime = F, FederalACASoftwareIdentifierID = F, IsInvalid = F, ModifiedTime = F, NumericYear = F, SkywardHash = F, SkywardID = F, SoftwareIdentifier = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalACASoftwareIdentifier", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalACASoftwareIdentifier
	#'
	#' This function returns a dataframe or json object of a FederalACASoftwareIdentifier
	#' @param FederalACASoftwareIdentifierID The ID of the FederalACASoftwareIdentifier to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalACASoftwareIdentifier. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalACASoftwareIdentifier.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalACASoftwareIdentifier') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalACASoftwareIdentifier
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalACASoftwareIdentifier <- function(FederalACASoftwareIdentifierID, CreatedTime = F, IsInvalid = F, ModifiedTime = F, NumericYear = F, SkywardHash = F, SkywardID = F, SoftwareIdentifier = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalACASoftwareIdentifierID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalACASoftwareIdentifier", objectId = FederalACASoftwareIdentifierID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalACASoftwareIdentifier
	#'
	#' This function deletes a FederalACASoftwareIdentifier
	#' @param FederalACASoftwareIdentifierID The ID of the FederalACASoftwareIdentifier to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalACASoftwareIdentifierID of the deleted FederalACASoftwareIdentifier.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalACASoftwareIdentifier <- function(FederalACASoftwareIdentifierID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalACASoftwareIdentifier", objectId = FederalACASoftwareIdentifierID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalACASoftwareIdentifier
	#'
	#' This function creates a FederalACASoftwareIdentifier
	#' @param fieldNames The field values to give the created FederalACASoftwareIdentifier. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalACASoftwareIdentifier
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalACASoftwareIdentifier <- function(IsInvalid = NULL, NumericYear = NULL, SoftwareIdentifier = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalACASoftwareIdentifier", body = list(DataObject = body), searchFields = append("FederalACASoftwareIdentifierID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalACASoftwareIdentifier
	#'
	#' This function modifies a FederalACASoftwareIdentifier
	#' @param fieldNames The field values to give the modified FederalACASoftwareIdentifier. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalACASoftwareIdentifier
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalACASoftwareIdentifier <- function(FederalACASoftwareIdentifierID, IsInvalid = NULL, NumericYear = NULL, SoftwareIdentifier = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalACASoftwareIdentifier", objectId = FederalACASoftwareIdentifierID, body = list(DataObject = body), searchFields = append("FederalACASoftwareIdentifierID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NelcoForms
	#'
	#' This function returns a dataframe or json object of NelcoForms
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NelcoForms. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NelcoForms.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NelcoForm') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of NelcoForms
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNelcoForms <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, Description = F, FormShape = F, ModifiedTime = F, NelcoFormID = F, ProcessType = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "NelcoForm", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NelcoForm
	#'
	#' This function returns a dataframe or json object of a NelcoForm
	#' @param NelcoFormID The ID of the NelcoForm to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NelcoForm. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NelcoForm.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NelcoForm') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of NelcoForm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNelcoForm <- function(NelcoFormID, Code = F, CreatedTime = F, Description = F, FormShape = F, ModifiedTime = F, ProcessType = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NelcoFormID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "NelcoForm", objectId = NelcoFormID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NelcoForm
	#'
	#' This function deletes a NelcoForm
	#' @param NelcoFormID The ID of the NelcoForm to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The NelcoFormID of the deleted NelcoForm.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNelcoForm <- function(NelcoFormID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "NelcoForm", objectId = NelcoFormID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NelcoForm
	#'
	#' This function creates a NelcoForm
	#' @param fieldNames The field values to give the created NelcoForm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created NelcoForm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNelcoForm <- function(Code = NULL, Description = NULL, FormShape = NULL, ProcessType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "NelcoForm", body = list(DataObject = body), searchFields = append("NelcoFormID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NelcoForm
	#'
	#' This function modifies a NelcoForm
	#' @param fieldNames The field values to give the modified NelcoForm. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified NelcoForm
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNelcoForm <- function(NelcoFormID, Code = NULL, Description = NULL, FormShape = NULL, ProcessType = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "NelcoForm", objectId = NelcoFormID, body = list(DataObject = body), searchFields = append("NelcoFormID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NelcoFormFields
	#'
	#' This function returns a dataframe or json object of NelcoFormFields
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NelcoFormFields. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NelcoFormFields.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NelcoFormField') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of NelcoFormFields
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNelcoFormFields <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, Description = F, ModifiedTime = F, NelcoFormFieldID = F, NelcoFormID = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "NelcoFormField", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NelcoFormField
	#'
	#' This function returns a dataframe or json object of a NelcoFormField
	#' @param NelcoFormFieldID The ID of the NelcoFormField to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NelcoFormField. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NelcoFormField.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NelcoFormField') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of NelcoFormField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNelcoFormField <- function(NelcoFormFieldID, Code = F, CreatedTime = F, Description = F, ModifiedTime = F, NelcoFormID = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NelcoFormFieldID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "NelcoFormField", objectId = NelcoFormFieldID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NelcoFormField
	#'
	#' This function deletes a NelcoFormField
	#' @param NelcoFormFieldID The ID of the NelcoFormField to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The NelcoFormFieldID of the deleted NelcoFormField.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNelcoFormField <- function(NelcoFormFieldID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "NelcoFormField", objectId = NelcoFormFieldID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NelcoFormField
	#'
	#' This function creates a NelcoFormField
	#' @param fieldNames The field values to give the created NelcoFormField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created NelcoFormField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNelcoFormField <- function(Code = NULL, Description = NULL, NelcoFormID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "NelcoFormField", body = list(DataObject = body), searchFields = append("NelcoFormFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NelcoFormField
	#'
	#' This function modifies a NelcoFormField
	#' @param fieldNames The field values to give the modified NelcoFormField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified NelcoFormField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNelcoFormField <- function(NelcoFormFieldID, Code = NULL, Description = NULL, NelcoFormID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "NelcoFormField", objectId = NelcoFormFieldID, body = list(DataObject = body), searchFields = append("NelcoFormFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List NelcoFormFieldPositions
	#'
	#' This function returns a dataframe or json object of NelcoFormFieldPositions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NelcoFormFieldPositions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NelcoFormFieldPositions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NelcoFormFieldPosition') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of NelcoFormFieldPositions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listNelcoFormFieldPositions <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, Description = F, ModifiedTime = F, NelcoFormFieldID = F, NelcoFormFieldPositionID = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "NelcoFormFieldPosition", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a NelcoFormFieldPosition
	#'
	#' This function returns a dataframe or json object of a NelcoFormFieldPosition
	#' @param NelcoFormFieldPositionID The ID of the NelcoFormFieldPosition to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given NelcoFormFieldPosition. Defaults to FALSE for all return fields which, for convenience, returns all fields for the NelcoFormFieldPosition.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('NelcoFormFieldPosition') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of NelcoFormFieldPosition
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getNelcoFormFieldPosition <- function(NelcoFormFieldPositionID, Code = F, CreatedTime = F, Description = F, ModifiedTime = F, NelcoFormFieldID = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "NelcoFormFieldPositionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "NelcoFormFieldPosition", objectId = NelcoFormFieldPositionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a NelcoFormFieldPosition
	#'
	#' This function deletes a NelcoFormFieldPosition
	#' @param NelcoFormFieldPositionID The ID of the NelcoFormFieldPosition to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The NelcoFormFieldPositionID of the deleted NelcoFormFieldPosition.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteNelcoFormFieldPosition <- function(NelcoFormFieldPositionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "NelcoFormFieldPosition", objectId = NelcoFormFieldPositionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a NelcoFormFieldPosition
	#'
	#' This function creates a NelcoFormFieldPosition
	#' @param fieldNames The field values to give the created NelcoFormFieldPosition. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created NelcoFormFieldPosition
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createNelcoFormFieldPosition <- function(Code = NULL, Description = NULL, NelcoFormFieldID = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "NelcoFormFieldPosition", body = list(DataObject = body), searchFields = append("NelcoFormFieldPositionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a NelcoFormFieldPosition
	#'
	#' This function modifies a NelcoFormFieldPosition
	#' @param fieldNames The field values to give the modified NelcoFormFieldPosition. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified NelcoFormFieldPosition
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyNelcoFormFieldPosition <- function(NelcoFormFieldPositionID, Code = NULL, Description = NULL, NelcoFormFieldID = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "NelcoFormFieldPosition", objectId = NelcoFormFieldPositionID, body = list(DataObject = body), searchFields = append("NelcoFormFieldPositionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form941EmployeeV1s
	#'
	#' This function returns a dataframe or json object of Form941EmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941EmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941EmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941EmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form941EmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm941EmployeeV1s <- function(searchConditionsList = NULL, AdditionalTaxableMedicareWages = F, CreatedTime = F, EmployeeID = F, FirstName = F, Form941EmployeeV1ID = F, Form941ExtractRunID = F, IsPaidOn12th = F, LastName = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form941EmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form941EmployeeV1
	#'
	#' This function returns a dataframe or json object of a Form941EmployeeV1
	#' @param Form941EmployeeV1ID The ID of the Form941EmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form941EmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form941EmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form941EmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form941EmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm941EmployeeV1 <- function(Form941EmployeeV1ID, AdditionalTaxableMedicareWages = F, CreatedTime = F, EmployeeID = F, FirstName = F, Form941ExtractRunID = F, IsPaidOn12th = F, LastName = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form941EmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form941EmployeeV1", objectId = Form941EmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form941EmployeeV1
	#'
	#' This function deletes a Form941EmployeeV1
	#' @param Form941EmployeeV1ID The ID of the Form941EmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form941EmployeeV1ID of the deleted Form941EmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm941EmployeeV1 <- function(Form941EmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form941EmployeeV1", objectId = Form941EmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form941EmployeeV1
	#'
	#' This function creates a Form941EmployeeV1
	#' @param fieldNames The field values to give the created Form941EmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form941EmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm941EmployeeV1 <- function(AdditionalTaxableMedicareWages = NULL, EmployeeID = NULL, FirstName = NULL, Form941ExtractRunID = NULL, IsPaidOn12th = NULL, LastName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form941EmployeeV1", body = list(DataObject = body), searchFields = append("Form941EmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form941EmployeeV1
	#'
	#' This function modifies a Form941EmployeeV1
	#' @param fieldNames The field values to give the modified Form941EmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form941EmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm941EmployeeV1 <- function(Form941EmployeeV1ID, AdditionalTaxableMedicareWages = NULL, EmployeeID = NULL, FirstName = NULL, Form941ExtractRunID = NULL, IsPaidOn12th = NULL, LastName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form941EmployeeV1", objectId = Form941EmployeeV1ID, body = list(DataObject = body), searchFields = append("Form941EmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACADelimitedFileFormats
	#'
	#' This function returns a dataframe or json object of ACADelimitedFileFormats
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACADelimitedFileFormats. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACADelimitedFileFormats.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACADelimitedFileFormat') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACADelimitedFileFormats
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACADelimitedFileFormats <- function(searchConditionsList = NULL, ACADelimitedFileFormatID = F, ACAThirdPartyFormatID = F, BirthDateColumnNumber = F, CommentColumnNumber = F, CreatedTime = F, DelimiterCharacter = F, DelimiterType = F, EmployeeIdentifierColumnNumber = F, EmployeeRequiredContributionColumnNumber = F, EmployerOfferedSelfInsuredCoverageColumnNumber = F, FirstNameColumnNumber = F, LastNameColumnNumber = F, MiddleNameColumnNumber = F, ModifiedTime = F, NumberOfHeaderRows = F, OfferOfCoverageCodeColumnNumber = F, PlanStartMonthOverrideColumnNumber = F, ReportEmployeeAsCoveredIndividualColumnNumber = F, SafeHarborColumnNumber = F, SkywardHash = F, SkywardID = F, SSNColumnNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACADelimitedFileFormat", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACADelimitedFileFormat
	#'
	#' This function returns a dataframe or json object of an ACADelimitedFileFormat
	#' @param ACADelimitedFileFormatID The ID of the ACADelimitedFileFormat to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACADelimitedFileFormat. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACADelimitedFileFormat.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACADelimitedFileFormat') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACADelimitedFileFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACADelimitedFileFormat <- function(ACADelimitedFileFormatID, ACAThirdPartyFormatID = F, BirthDateColumnNumber = F, CommentColumnNumber = F, CreatedTime = F, DelimiterCharacter = F, DelimiterType = F, EmployeeIdentifierColumnNumber = F, EmployeeRequiredContributionColumnNumber = F, EmployerOfferedSelfInsuredCoverageColumnNumber = F, FirstNameColumnNumber = F, LastNameColumnNumber = F, MiddleNameColumnNumber = F, ModifiedTime = F, NumberOfHeaderRows = F, OfferOfCoverageCodeColumnNumber = F, PlanStartMonthOverrideColumnNumber = F, ReportEmployeeAsCoveredIndividualColumnNumber = F, SafeHarborColumnNumber = F, SkywardHash = F, SkywardID = F, SSNColumnNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACADelimitedFileFormatID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACADelimitedFileFormat", objectId = ACADelimitedFileFormatID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACADelimitedFileFormat
	#'
	#' This function deletes an ACADelimitedFileFormat
	#' @param ACADelimitedFileFormatID The ID of the ACADelimitedFileFormat to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACADelimitedFileFormatID of the deleted ACADelimitedFileFormat.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACADelimitedFileFormat <- function(ACADelimitedFileFormatID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACADelimitedFileFormat", objectId = ACADelimitedFileFormatID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACADelimitedFileFormat
	#'
	#' This function creates an ACADelimitedFileFormat
	#' @param fieldNames The field values to give the created ACADelimitedFileFormat. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACADelimitedFileFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACADelimitedFileFormat <- function(ACAThirdPartyFormatID = NULL, BirthDateColumnNumber = NULL, CommentColumnNumber = NULL, DelimiterCharacter = NULL, DelimiterType = NULL, EmployeeIdentifierColumnNumber = NULL, EmployeeRequiredContributionColumnNumber = NULL, EmployerOfferedSelfInsuredCoverageColumnNumber = NULL, FirstNameColumnNumber = NULL, LastNameColumnNumber = NULL, MiddleNameColumnNumber = NULL, NumberOfHeaderRows = NULL, OfferOfCoverageCodeColumnNumber = NULL, PlanStartMonthOverrideColumnNumber = NULL, ReportEmployeeAsCoveredIndividualColumnNumber = NULL, SafeHarborColumnNumber = NULL, SSNColumnNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACADelimitedFileFormat", body = list(DataObject = body), searchFields = append("ACADelimitedFileFormatID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACADelimitedFileFormat
	#'
	#' This function modifies an ACADelimitedFileFormat
	#' @param fieldNames The field values to give the modified ACADelimitedFileFormat. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACADelimitedFileFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACADelimitedFileFormat <- function(ACADelimitedFileFormatID, ACAThirdPartyFormatID = NULL, BirthDateColumnNumber = NULL, CommentColumnNumber = NULL, DelimiterCharacter = NULL, DelimiterType = NULL, EmployeeIdentifierColumnNumber = NULL, EmployeeRequiredContributionColumnNumber = NULL, EmployerOfferedSelfInsuredCoverageColumnNumber = NULL, FirstNameColumnNumber = NULL, LastNameColumnNumber = NULL, MiddleNameColumnNumber = NULL, NumberOfHeaderRows = NULL, OfferOfCoverageCodeColumnNumber = NULL, PlanStartMonthOverrideColumnNumber = NULL, ReportEmployeeAsCoveredIndividualColumnNumber = NULL, SafeHarborColumnNumber = NULL, SSNColumnNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACADelimitedFileFormat", objectId = ACADelimitedFileFormatID, body = list(DataObject = body), searchFields = append("ACADelimitedFileFormatID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAFixedLengthFileFormats
	#'
	#' This function returns a dataframe or json object of ACAFixedLengthFileFormats
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAFixedLengthFileFormats. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAFixedLengthFileFormats.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAFixedLengthFileFormat') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAFixedLengthFileFormats
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAFixedLengthFileFormats <- function(searchConditionsList = NULL, ACAFixedLengthFileFormatID = F, ACAThirdPartyFormatID = F, BirthDateLength = F, BirthDateStartPosition = F, CommentLength = F, CommentStartPosition = F, CreatedTime = F, EmployeeIdentifierLength = F, EmployeeIdentifierStartPosition = F, EmployeeRequiredContributionLength = F, EmployeeRequiredContributionStartPosition = F, EmployerOfferedSelfInsuredCoverageLength = F, EmployerOfferedSelfInsuredCoverageStartPosition = F, FirstNameLength = F, FirstNameStartPosition = F, LastNameLength = F, LastNameStartPosition = F, MiddleNameLength = F, MiddleNameStartPosition = F, ModifiedTime = F, NumberOfHeaderRows = F, OfferOfCoverageCodeLength = F, OfferOfCoverageCodeStartPosition = F, PlanStartMonthOverrideLength = F, PlanStartMonthOverrideStartPosition = F, ReportEmployeeAsCoveredIndividualLength = F, ReportEmployeeAsCoveredIndividualStartPosition = F, SafeHarborLength = F, SafeHarborStartPosition = F, SkywardHash = F, SkywardID = F, SSNLength = F, SSNStartPosition = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAFixedLengthFileFormat", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAFixedLengthFileFormat
	#'
	#' This function returns a dataframe or json object of an ACAFixedLengthFileFormat
	#' @param ACAFixedLengthFileFormatID The ID of the ACAFixedLengthFileFormat to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAFixedLengthFileFormat. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAFixedLengthFileFormat.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAFixedLengthFileFormat') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAFixedLengthFileFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAFixedLengthFileFormat <- function(ACAFixedLengthFileFormatID, ACAThirdPartyFormatID = F, BirthDateLength = F, BirthDateStartPosition = F, CommentLength = F, CommentStartPosition = F, CreatedTime = F, EmployeeIdentifierLength = F, EmployeeIdentifierStartPosition = F, EmployeeRequiredContributionLength = F, EmployeeRequiredContributionStartPosition = F, EmployerOfferedSelfInsuredCoverageLength = F, EmployerOfferedSelfInsuredCoverageStartPosition = F, FirstNameLength = F, FirstNameStartPosition = F, LastNameLength = F, LastNameStartPosition = F, MiddleNameLength = F, MiddleNameStartPosition = F, ModifiedTime = F, NumberOfHeaderRows = F, OfferOfCoverageCodeLength = F, OfferOfCoverageCodeStartPosition = F, PlanStartMonthOverrideLength = F, PlanStartMonthOverrideStartPosition = F, ReportEmployeeAsCoveredIndividualLength = F, ReportEmployeeAsCoveredIndividualStartPosition = F, SafeHarborLength = F, SafeHarborStartPosition = F, SkywardHash = F, SkywardID = F, SSNLength = F, SSNStartPosition = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAFixedLengthFileFormatID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAFixedLengthFileFormat", objectId = ACAFixedLengthFileFormatID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAFixedLengthFileFormat
	#'
	#' This function deletes an ACAFixedLengthFileFormat
	#' @param ACAFixedLengthFileFormatID The ID of the ACAFixedLengthFileFormat to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAFixedLengthFileFormatID of the deleted ACAFixedLengthFileFormat.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAFixedLengthFileFormat <- function(ACAFixedLengthFileFormatID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAFixedLengthFileFormat", objectId = ACAFixedLengthFileFormatID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAFixedLengthFileFormat
	#'
	#' This function creates an ACAFixedLengthFileFormat
	#' @param fieldNames The field values to give the created ACAFixedLengthFileFormat. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAFixedLengthFileFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAFixedLengthFileFormat <- function(ACAThirdPartyFormatID = NULL, BirthDateLength = NULL, BirthDateStartPosition = NULL, CommentLength = NULL, CommentStartPosition = NULL, EmployeeIdentifierLength = NULL, EmployeeIdentifierStartPosition = NULL, EmployeeRequiredContributionLength = NULL, EmployeeRequiredContributionStartPosition = NULL, EmployerOfferedSelfInsuredCoverageLength = NULL, EmployerOfferedSelfInsuredCoverageStartPosition = NULL, FirstNameLength = NULL, FirstNameStartPosition = NULL, LastNameLength = NULL, LastNameStartPosition = NULL, MiddleNameLength = NULL, MiddleNameStartPosition = NULL, NumberOfHeaderRows = NULL, OfferOfCoverageCodeLength = NULL, OfferOfCoverageCodeStartPosition = NULL, PlanStartMonthOverrideLength = NULL, PlanStartMonthOverrideStartPosition = NULL, ReportEmployeeAsCoveredIndividualLength = NULL, ReportEmployeeAsCoveredIndividualStartPosition = NULL, SafeHarborLength = NULL, SafeHarborStartPosition = NULL, SSNLength = NULL, SSNStartPosition = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAFixedLengthFileFormat", body = list(DataObject = body), searchFields = append("ACAFixedLengthFileFormatID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAFixedLengthFileFormat
	#'
	#' This function modifies an ACAFixedLengthFileFormat
	#' @param fieldNames The field values to give the modified ACAFixedLengthFileFormat. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAFixedLengthFileFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAFixedLengthFileFormat <- function(ACAFixedLengthFileFormatID, ACAThirdPartyFormatID = NULL, BirthDateLength = NULL, BirthDateStartPosition = NULL, CommentLength = NULL, CommentStartPosition = NULL, EmployeeIdentifierLength = NULL, EmployeeIdentifierStartPosition = NULL, EmployeeRequiredContributionLength = NULL, EmployeeRequiredContributionStartPosition = NULL, EmployerOfferedSelfInsuredCoverageLength = NULL, EmployerOfferedSelfInsuredCoverageStartPosition = NULL, FirstNameLength = NULL, FirstNameStartPosition = NULL, LastNameLength = NULL, LastNameStartPosition = NULL, MiddleNameLength = NULL, MiddleNameStartPosition = NULL, NumberOfHeaderRows = NULL, OfferOfCoverageCodeLength = NULL, OfferOfCoverageCodeStartPosition = NULL, PlanStartMonthOverrideLength = NULL, PlanStartMonthOverrideStartPosition = NULL, ReportEmployeeAsCoveredIndividualLength = NULL, ReportEmployeeAsCoveredIndividualStartPosition = NULL, SafeHarborLength = NULL, SafeHarborStartPosition = NULL, SSNLength = NULL, SSNStartPosition = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAFixedLengthFileFormat", objectId = ACAFixedLengthFileFormatID, body = list(DataObject = body), searchFields = append("ACAFixedLengthFileFormatID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAThirdPartyFormats
	#'
	#' This function returns a dataframe or json object of ACAThirdPartyFormats
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyFormats. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyFormats.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyFormat') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAThirdPartyFormats
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAThirdPartyFormats <- function(searchConditionsList = NULL, ACAThirdPartyFormatID = F, CalendarYearID = F, Code = F, CodeDescription = F, CreatedTime = F, DateFormat = F, Description = F, DistrictID = F, EmployeeIdentification = F, ExistingCoveredIndividualHandlingType = F, ExistingRecordOverride = F, ImportRecordType = F, ImportType = F, IsSystemLoaded = F, MarkAllAsEmployerOfferedSelfInsuredCoverage = F, MarkAllAsReportEmployeeAsCoveredIndividual = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, SkywardIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAThirdPartyFormat", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAThirdPartyFormat
	#'
	#' This function returns a dataframe or json object of an ACAThirdPartyFormat
	#' @param ACAThirdPartyFormatID The ID of the ACAThirdPartyFormat to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyFormat. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyFormat.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyFormat') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAThirdPartyFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAThirdPartyFormat <- function(ACAThirdPartyFormatID, CalendarYearID = F, Code = F, CodeDescription = F, CreatedTime = F, DateFormat = F, Description = F, DistrictID = F, EmployeeIdentification = F, ExistingCoveredIndividualHandlingType = F, ExistingRecordOverride = F, ImportRecordType = F, ImportType = F, IsSystemLoaded = F, MarkAllAsEmployerOfferedSelfInsuredCoverage = F, MarkAllAsReportEmployeeAsCoveredIndividual = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, SkywardIDClonedFrom = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAThirdPartyFormatID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormat", objectId = ACAThirdPartyFormatID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAThirdPartyFormat
	#'
	#' This function deletes an ACAThirdPartyFormat
	#' @param ACAThirdPartyFormatID The ID of the ACAThirdPartyFormat to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAThirdPartyFormatID of the deleted ACAThirdPartyFormat.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAThirdPartyFormat <- function(ACAThirdPartyFormatID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormat", objectId = ACAThirdPartyFormatID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAThirdPartyFormat
	#'
	#' This function creates an ACAThirdPartyFormat
	#' @param fieldNames The field values to give the created ACAThirdPartyFormat. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAThirdPartyFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAThirdPartyFormat <- function(CalendarYearID = NULL, Code = NULL, DateFormat = NULL, Description = NULL, DistrictID = NULL, EmployeeIdentification = NULL, ExistingCoveredIndividualHandlingType = NULL, ExistingRecordOverride = NULL, ImportRecordType = NULL, ImportType = NULL, MarkAllAsEmployerOfferedSelfInsuredCoverage = NULL, MarkAllAsReportEmployeeAsCoveredIndividual = NULL, SkywardIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormat", body = list(DataObject = body), searchFields = append("ACAThirdPartyFormatID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAThirdPartyFormat
	#'
	#' This function modifies an ACAThirdPartyFormat
	#' @param fieldNames The field values to give the modified ACAThirdPartyFormat. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAThirdPartyFormat
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAThirdPartyFormat <- function(ACAThirdPartyFormatID, CalendarYearID = NULL, Code = NULL, DateFormat = NULL, Description = NULL, DistrictID = NULL, EmployeeIdentification = NULL, ExistingCoveredIndividualHandlingType = NULL, ExistingRecordOverride = NULL, ImportRecordType = NULL, ImportType = NULL, MarkAllAsEmployerOfferedSelfInsuredCoverage = NULL, MarkAllAsReportEmployeeAsCoveredIndividual = NULL, SkywardIDClonedFrom = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormat", objectId = ACAThirdPartyFormatID, body = list(DataObject = body), searchFields = append("ACAThirdPartyFormatID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAThirdPartyFormatOfferOfCoverages
	#'
	#' This function returns a dataframe or json object of ACAThirdPartyFormatOfferOfCoverages
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyFormatOfferOfCoverages. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyFormatOfferOfCoverages.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyFormatOfferOfCoverage') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAThirdPartyFormatOfferOfCoverages
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAThirdPartyFormatOfferOfCoverages <- function(searchConditionsList = NULL, ACAThirdPartyFormatID = F, ACAThirdPartyFormatOfferOfCoverageID = F, CreatedTime = F, FederalACAOfferAndCoverageID = F, ImportValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatOfferOfCoverage", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAThirdPartyFormatOfferOfCoverage
	#'
	#' This function returns a dataframe or json object of an ACAThirdPartyFormatOfferOfCoverage
	#' @param ACAThirdPartyFormatOfferOfCoverageID The ID of the ACAThirdPartyFormatOfferOfCoverage to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyFormatOfferOfCoverage. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyFormatOfferOfCoverage.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyFormatOfferOfCoverage') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAThirdPartyFormatOfferOfCoverage
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAThirdPartyFormatOfferOfCoverage <- function(ACAThirdPartyFormatOfferOfCoverageID, ACAThirdPartyFormatID = F, CreatedTime = F, FederalACAOfferAndCoverageID = F, ImportValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAThirdPartyFormatOfferOfCoverageID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatOfferOfCoverage", objectId = ACAThirdPartyFormatOfferOfCoverageID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAThirdPartyFormatOfferOfCoverage
	#'
	#' This function deletes an ACAThirdPartyFormatOfferOfCoverage
	#' @param ACAThirdPartyFormatOfferOfCoverageID The ID of the ACAThirdPartyFormatOfferOfCoverage to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAThirdPartyFormatOfferOfCoverageID of the deleted ACAThirdPartyFormatOfferOfCoverage.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAThirdPartyFormatOfferOfCoverage <- function(ACAThirdPartyFormatOfferOfCoverageID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatOfferOfCoverage", objectId = ACAThirdPartyFormatOfferOfCoverageID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAThirdPartyFormatOfferOfCoverage
	#'
	#' This function creates an ACAThirdPartyFormatOfferOfCoverage
	#' @param fieldNames The field values to give the created ACAThirdPartyFormatOfferOfCoverage. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAThirdPartyFormatOfferOfCoverage
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAThirdPartyFormatOfferOfCoverage <- function(ACAThirdPartyFormatID = NULL, FederalACAOfferAndCoverageID = NULL, ImportValue = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatOfferOfCoverage", body = list(DataObject = body), searchFields = append("ACAThirdPartyFormatOfferOfCoverageID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAThirdPartyFormatOfferOfCoverage
	#'
	#' This function modifies an ACAThirdPartyFormatOfferOfCoverage
	#' @param fieldNames The field values to give the modified ACAThirdPartyFormatOfferOfCoverage. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAThirdPartyFormatOfferOfCoverage
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAThirdPartyFormatOfferOfCoverage <- function(ACAThirdPartyFormatOfferOfCoverageID, ACAThirdPartyFormatID = NULL, FederalACAOfferAndCoverageID = NULL, ImportValue = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatOfferOfCoverage", objectId = ACAThirdPartyFormatOfferOfCoverageID, body = list(DataObject = body), searchFields = append("ACAThirdPartyFormatOfferOfCoverageID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAThirdPartyFormatSafeHarbors
	#'
	#' This function returns a dataframe or json object of ACAThirdPartyFormatSafeHarbors
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyFormatSafeHarbors. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyFormatSafeHarbors.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyFormatSafeHarbor') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAThirdPartyFormatSafeHarbors
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAThirdPartyFormatSafeHarbors <- function(searchConditionsList = NULL, ACAThirdPartyFormatID = F, ACAThirdPartyFormatSafeHarborID = F, CreatedTime = F, FederalACASafeHarborID = F, ImportValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatSafeHarbor", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAThirdPartyFormatSafeHarbor
	#'
	#' This function returns a dataframe or json object of an ACAThirdPartyFormatSafeHarbor
	#' @param ACAThirdPartyFormatSafeHarborID The ID of the ACAThirdPartyFormatSafeHarbor to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyFormatSafeHarbor. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyFormatSafeHarbor.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyFormatSafeHarbor') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAThirdPartyFormatSafeHarbor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAThirdPartyFormatSafeHarbor <- function(ACAThirdPartyFormatSafeHarborID, ACAThirdPartyFormatID = F, CreatedTime = F, FederalACASafeHarborID = F, ImportValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAThirdPartyFormatSafeHarborID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatSafeHarbor", objectId = ACAThirdPartyFormatSafeHarborID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAThirdPartyFormatSafeHarbor
	#'
	#' This function deletes an ACAThirdPartyFormatSafeHarbor
	#' @param ACAThirdPartyFormatSafeHarborID The ID of the ACAThirdPartyFormatSafeHarbor to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAThirdPartyFormatSafeHarborID of the deleted ACAThirdPartyFormatSafeHarbor.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAThirdPartyFormatSafeHarbor <- function(ACAThirdPartyFormatSafeHarborID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatSafeHarbor", objectId = ACAThirdPartyFormatSafeHarborID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAThirdPartyFormatSafeHarbor
	#'
	#' This function creates an ACAThirdPartyFormatSafeHarbor
	#' @param fieldNames The field values to give the created ACAThirdPartyFormatSafeHarbor. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAThirdPartyFormatSafeHarbor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAThirdPartyFormatSafeHarbor <- function(ACAThirdPartyFormatID = NULL, FederalACASafeHarborID = NULL, ImportValue = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatSafeHarbor", body = list(DataObject = body), searchFields = append("ACAThirdPartyFormatSafeHarborID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAThirdPartyFormatSafeHarbor
	#'
	#' This function modifies an ACAThirdPartyFormatSafeHarbor
	#' @param fieldNames The field values to give the modified ACAThirdPartyFormatSafeHarbor. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAThirdPartyFormatSafeHarbor
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAThirdPartyFormatSafeHarbor <- function(ACAThirdPartyFormatSafeHarborID, ACAThirdPartyFormatID = NULL, FederalACASafeHarborID = NULL, ImportValue = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyFormatSafeHarbor", objectId = ACAThirdPartyFormatSafeHarborID, body = list(DataObject = body), searchFields = append("ACAThirdPartyFormatSafeHarborID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ACAThirdPartyImports
	#'
	#' This function returns a dataframe or json object of ACAThirdPartyImports
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyImports. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyImports.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyImport') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ACAThirdPartyImports
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listACAThirdPartyImports <- function(searchConditionsList = NULL, ACAThirdPartyFormatID = F, ACAThirdPartyImportID = F, CalendarYear = F, CreatedTime = F, ImportData = F, ImportTime = F, MediaID = F, ModifiedTime = F, Month = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ACAThirdPartyImport", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get an ACAThirdPartyImport
	#'
	#' This function returns a dataframe or json object of an ACAThirdPartyImport
	#' @param ACAThirdPartyImportID The ID of the ACAThirdPartyImport to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ACAThirdPartyImport. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ACAThirdPartyImport.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ACAThirdPartyImport') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ACAThirdPartyImport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getACAThirdPartyImport <- function(ACAThirdPartyImportID, ACAThirdPartyFormatID = F, CalendarYear = F, CreatedTime = F, ImportData = F, ImportTime = F, MediaID = F, ModifiedTime = F, Month = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ACAThirdPartyImportID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyImport", objectId = ACAThirdPartyImportID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete an ACAThirdPartyImport
	#'
	#' This function deletes an ACAThirdPartyImport
	#' @param ACAThirdPartyImportID The ID of the ACAThirdPartyImport to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ACAThirdPartyImportID of the deleted ACAThirdPartyImport.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteACAThirdPartyImport <- function(ACAThirdPartyImportID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyImport", objectId = ACAThirdPartyImportID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create an ACAThirdPartyImport
	#'
	#' This function creates an ACAThirdPartyImport
	#' @param fieldNames The field values to give the created ACAThirdPartyImport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ACAThirdPartyImport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createACAThirdPartyImport <- function(ACAThirdPartyFormatID = NULL, CalendarYear = NULL, ImportTime = NULL, MediaID = NULL, Month = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyImport", body = list(DataObject = body), searchFields = append("ACAThirdPartyImportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify an ACAThirdPartyImport
	#'
	#' This function modifies an ACAThirdPartyImport
	#' @param fieldNames The field values to give the modified ACAThirdPartyImport. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ACAThirdPartyImport
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyACAThirdPartyImport <- function(ACAThirdPartyImportID, ACAThirdPartyFormatID = NULL, CalendarYear = NULL, ImportTime = NULL, MediaID = NULL, Month = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ACAThirdPartyImport", objectId = ACAThirdPartyImportID, body = list(DataObject = body), searchFields = append("ACAThirdPartyImportID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalEEOCSalaryRanges
	#'
	#' This function returns a dataframe or json object of FederalEEOCSalaryRanges
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalEEOCSalaryRanges. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalEEOCSalaryRanges.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalEEOCSalaryRange') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalEEOCSalaryRanges
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalEEOCSalaryRanges <- function(searchConditionsList = NULL, Code = F, CreatedTime = F, FederalEEOCSalaryRangeID = F, IsInvalid = F, ModifiedTime = F, SalaryHigh = F, SalaryLow = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalEEOCSalaryRange", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalEEOCSalaryRange
	#'
	#' This function returns a dataframe or json object of a FederalEEOCSalaryRange
	#' @param FederalEEOCSalaryRangeID The ID of the FederalEEOCSalaryRange to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalEEOCSalaryRange. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalEEOCSalaryRange.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalEEOCSalaryRange') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalEEOCSalaryRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalEEOCSalaryRange <- function(FederalEEOCSalaryRangeID, Code = F, CreatedTime = F, IsInvalid = F, ModifiedTime = F, SalaryHigh = F, SalaryLow = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalEEOCSalaryRangeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalEEOCSalaryRange", objectId = FederalEEOCSalaryRangeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalEEOCSalaryRange
	#'
	#' This function deletes a FederalEEOCSalaryRange
	#' @param FederalEEOCSalaryRangeID The ID of the FederalEEOCSalaryRange to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalEEOCSalaryRangeID of the deleted FederalEEOCSalaryRange.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalEEOCSalaryRange <- function(FederalEEOCSalaryRangeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalEEOCSalaryRange", objectId = FederalEEOCSalaryRangeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalEEOCSalaryRange
	#'
	#' This function creates a FederalEEOCSalaryRange
	#' @param fieldNames The field values to give the created FederalEEOCSalaryRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalEEOCSalaryRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalEEOCSalaryRange <- function(Code = NULL, IsInvalid = NULL, SalaryHigh = NULL, SalaryLow = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalEEOCSalaryRange", body = list(DataObject = body), searchFields = append("FederalEEOCSalaryRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalEEOCSalaryRange
	#'
	#' This function modifies a FederalEEOCSalaryRange
	#' @param fieldNames The field values to give the modified FederalEEOCSalaryRange. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalEEOCSalaryRange
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalEEOCSalaryRange <- function(FederalEEOCSalaryRangeID, Code = NULL, IsInvalid = NULL, SalaryHigh = NULL, SalaryLow = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalEEOCSalaryRange", objectId = FederalEEOCSalaryRangeID, body = list(DataObject = body), searchFields = append("FederalEEOCSalaryRangeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MPermittedVendorFields
	#'
	#' This function returns a dataframe or json object of Form1099MPermittedVendorFields
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MPermittedVendorFields. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MPermittedVendorFields.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MPermittedVendorField') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MPermittedVendorFields
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MPermittedVendorFields <- function(searchConditionsList = NULL, CreatedTime = F, FieldName = F, Form1099MPermittedVendorFieldID = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MPermittedVendorField", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MPermittedVendorField
	#'
	#' This function returns a dataframe or json object of a Form1099MPermittedVendorField
	#' @param Form1099MPermittedVendorFieldID The ID of the Form1099MPermittedVendorField to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MPermittedVendorField. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MPermittedVendorField.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MPermittedVendorField') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MPermittedVendorField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MPermittedVendorField <- function(Form1099MPermittedVendorFieldID, CreatedTime = F, FieldName = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MPermittedVendorFieldID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MPermittedVendorField", objectId = Form1099MPermittedVendorFieldID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MPermittedVendorField
	#'
	#' This function deletes a Form1099MPermittedVendorField
	#' @param Form1099MPermittedVendorFieldID The ID of the Form1099MPermittedVendorField to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MPermittedVendorFieldID of the deleted Form1099MPermittedVendorField.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MPermittedVendorField <- function(Form1099MPermittedVendorFieldID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MPermittedVendorField", objectId = Form1099MPermittedVendorFieldID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MPermittedVendorField
	#'
	#' This function creates a Form1099MPermittedVendorField
	#' @param fieldNames The field values to give the created Form1099MPermittedVendorField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MPermittedVendorField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MPermittedVendorField <- function(FieldName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MPermittedVendorField", body = list(DataObject = body), searchFields = append("Form1099MPermittedVendorFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MPermittedVendorField
	#'
	#' This function modifies a Form1099MPermittedVendorField
	#' @param fieldNames The field values to give the modified Form1099MPermittedVendorField. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MPermittedVendorField
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MPermittedVendorField <- function(Form1099MPermittedVendorFieldID, FieldName = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MPermittedVendorField", objectId = Form1099MPermittedVendorFieldID, body = list(DataObject = body), searchFields = append("Form1099MPermittedVendorFieldID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearEmployeeLocalityV1s
	#'
	#' This function returns a dataframe or json object of W2YearEmployeeLocalityV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeLocalityV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeLocalityV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeLocalityV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearEmployeeLocalityV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearEmployeeLocalityV1s <- function(searchConditionsList = NULL, Box18LocalWages = F, Box19LocalIncomeTax = F, CreatedTime = F, IsOriginalExtractedValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeLocalityV1ID = F, W2YearEmployeeV1ID = F, W2YearLocalityID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearEmployeeLocalityV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearEmployeeLocalityV1
	#'
	#' This function returns a dataframe or json object of a W2YearEmployeeLocalityV1
	#' @param W2YearEmployeeLocalityV1ID The ID of the W2YearEmployeeLocalityV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearEmployeeLocalityV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearEmployeeLocalityV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearEmployeeLocalityV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearEmployeeLocalityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearEmployeeLocalityV1 <- function(W2YearEmployeeLocalityV1ID, Box18LocalWages = F, Box19LocalIncomeTax = F, CreatedTime = F, IsOriginalExtractedValue = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearEmployeeV1ID = F, W2YearLocalityID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearEmployeeLocalityV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeLocalityV1", objectId = W2YearEmployeeLocalityV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearEmployeeLocalityV1
	#'
	#' This function deletes a W2YearEmployeeLocalityV1
	#' @param W2YearEmployeeLocalityV1ID The ID of the W2YearEmployeeLocalityV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearEmployeeLocalityV1ID of the deleted W2YearEmployeeLocalityV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearEmployeeLocalityV1 <- function(W2YearEmployeeLocalityV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeLocalityV1", objectId = W2YearEmployeeLocalityV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearEmployeeLocalityV1
	#'
	#' This function creates a W2YearEmployeeLocalityV1
	#' @param fieldNames The field values to give the created W2YearEmployeeLocalityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearEmployeeLocalityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearEmployeeLocalityV1 <- function(Box18LocalWages = NULL, Box19LocalIncomeTax = NULL, IsOriginalExtractedValue = NULL, W2YearEmployeeV1ID = NULL, W2YearLocalityID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeLocalityV1", body = list(DataObject = body), searchFields = append("W2YearEmployeeLocalityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearEmployeeLocalityV1
	#'
	#' This function modifies a W2YearEmployeeLocalityV1
	#' @param fieldNames The field values to give the modified W2YearEmployeeLocalityV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearEmployeeLocalityV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearEmployeeLocalityV1 <- function(W2YearEmployeeLocalityV1ID, Box18LocalWages = NULL, Box19LocalIncomeTax = NULL, IsOriginalExtractedValue = NULL, W2YearEmployeeV1ID = NULL, W2YearLocalityID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearEmployeeLocalityV1", objectId = W2YearEmployeeLocalityV1ID, body = list(DataObject = body), searchFields = append("W2YearEmployeeLocalityV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2YearLocalities
	#'
	#' This function returns a dataframe or json object of W2YearLocalities
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearLocalities. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearLocalities.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearLocality') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2YearLocalities
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2YearLocalities <- function(searchConditionsList = NULL, Box20LocalityName = F, Code = F, CodeDescription = F, CreatedTime = F, Description = F, LocalTaxID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, W2YearLocalityID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2YearLocality", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2YearLocality
	#'
	#' This function returns a dataframe or json object of a W2YearLocality
	#' @param W2YearLocalityID The ID of the W2YearLocality to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2YearLocality. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2YearLocality.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2YearLocality') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2YearLocality
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2YearLocality <- function(W2YearLocalityID, Box20LocalityName = F, Code = F, CodeDescription = F, CreatedTime = F, Description = F, LocalTaxID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2YearID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2YearLocalityID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2YearLocality", objectId = W2YearLocalityID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2YearLocality
	#'
	#' This function deletes a W2YearLocality
	#' @param W2YearLocalityID The ID of the W2YearLocality to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2YearLocalityID of the deleted W2YearLocality.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2YearLocality <- function(W2YearLocalityID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2YearLocality", objectId = W2YearLocalityID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2YearLocality
	#'
	#' This function creates a W2YearLocality
	#' @param fieldNames The field values to give the created W2YearLocality. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2YearLocality
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2YearLocality <- function(Box20LocalityName = NULL, Code = NULL, Description = NULL, LocalTaxID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2YearLocality", body = list(DataObject = body), searchFields = append("W2YearLocalityID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2YearLocality
	#'
	#' This function modifies a W2YearLocality
	#' @param fieldNames The field values to give the modified W2YearLocality. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2YearLocality
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2YearLocality <- function(W2YearLocalityID, Box20LocalityName = NULL, Code = NULL, Description = NULL, LocalTaxID = NULL, W2YearID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2YearLocality", objectId = W2YearLocalityID, body = list(DataObject = body), searchFields = append("W2YearLocalityID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MExportElectronicFileMedias
	#'
	#' This function returns a dataframe or json object of Form1099MExportElectronicFileMedias
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MExportElectronicFileMedias. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MExportElectronicFileMedias.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MExportElectronicFileMedia') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MExportElectronicFileMedias
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MExportElectronicFileMedias <- function(searchConditionsList = NULL, CreatedTime = F, Form1099MExportElectronicFileID = F, Form1099MExportElectronicFileMediaID = F, MediaID = F, ModifiedTime = F, StateID = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFileMedia", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MExportElectronicFileMedia
	#'
	#' This function returns a dataframe or json object of a Form1099MExportElectronicFileMedia
	#' @param Form1099MExportElectronicFileMediaID The ID of the Form1099MExportElectronicFileMedia to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MExportElectronicFileMedia. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MExportElectronicFileMedia.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MExportElectronicFileMedia') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MExportElectronicFileMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MExportElectronicFileMedia <- function(Form1099MExportElectronicFileMediaID, CreatedTime = F, Form1099MExportElectronicFileID = F, MediaID = F, ModifiedTime = F, StateID = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MExportElectronicFileMediaID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFileMedia", objectId = Form1099MExportElectronicFileMediaID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MExportElectronicFileMedia
	#'
	#' This function deletes a Form1099MExportElectronicFileMedia
	#' @param Form1099MExportElectronicFileMediaID The ID of the Form1099MExportElectronicFileMedia to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MExportElectronicFileMediaID of the deleted Form1099MExportElectronicFileMedia.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MExportElectronicFileMedia <- function(Form1099MExportElectronicFileMediaID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFileMedia", objectId = Form1099MExportElectronicFileMediaID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MExportElectronicFileMedia
	#'
	#' This function creates a Form1099MExportElectronicFileMedia
	#' @param fieldNames The field values to give the created Form1099MExportElectronicFileMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MExportElectronicFileMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MExportElectronicFileMedia <- function(Form1099MExportElectronicFileID = NULL, MediaID = NULL, StateID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFileMedia", body = list(DataObject = body), searchFields = append("Form1099MExportElectronicFileMediaID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MExportElectronicFileMedia
	#'
	#' This function modifies a Form1099MExportElectronicFileMedia
	#' @param fieldNames The field values to give the modified Form1099MExportElectronicFileMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MExportElectronicFileMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MExportElectronicFileMedia <- function(Form1099MExportElectronicFileMediaID, Form1099MExportElectronicFileID = NULL, MediaID = NULL, StateID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MExportElectronicFileMedia", objectId = Form1099MExportElectronicFileMediaID, body = list(DataObject = body), searchFields = append("Form1099MExportElectronicFileMediaID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MYearStateCalendarYears
	#'
	#' This function returns a dataframe or json object of Form1099MYearStateCalendarYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearStateCalendarYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearStateCalendarYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearStateCalendarYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MYearStateCalendarYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MYearStateCalendarYears <- function(searchConditionsList = NULL, Form1099MYearID = F, Form1099MYearStateCalendarYearID = F, StateCalendarYearID = F, StateID = F, TaxStateID = F, Vendors = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MYearStateCalendarYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MYearStateCalendarYear
	#'
	#' This function returns a dataframe or json object of a Form1099MYearStateCalendarYear
	#' @param Form1099MYearStateCalendarYearID The ID of the Form1099MYearStateCalendarYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearStateCalendarYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearStateCalendarYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearStateCalendarYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MYearStateCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MYearStateCalendarYear <- function(Form1099MYearStateCalendarYearID, Form1099MYearID = F, StateCalendarYearID = F, StateID = F, TaxStateID = F, Vendors = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MYearStateCalendarYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateCalendarYear", objectId = Form1099MYearStateCalendarYearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MYearStateCalendarYear
	#'
	#' This function deletes a Form1099MYearStateCalendarYear
	#' @param Form1099MYearStateCalendarYearID The ID of the Form1099MYearStateCalendarYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MYearStateCalendarYearID of the deleted Form1099MYearStateCalendarYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MYearStateCalendarYear <- function(Form1099MYearStateCalendarYearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateCalendarYear", objectId = Form1099MYearStateCalendarYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MYearStateCalendarYear
	#'
	#' This function creates a Form1099MYearStateCalendarYear
	#' @param fieldNames The field values to give the created Form1099MYearStateCalendarYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MYearStateCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MYearStateCalendarYear <- function(Form1099MYearID = NULL, StateCalendarYearID = NULL, StateID = NULL, TaxStateID = NULL, Vendors = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateCalendarYear", body = list(DataObject = body), searchFields = append("Form1099MYearStateCalendarYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MYearStateCalendarYear
	#'
	#' This function modifies a Form1099MYearStateCalendarYear
	#' @param fieldNames The field values to give the modified Form1099MYearStateCalendarYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MYearStateCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MYearStateCalendarYear <- function(Form1099MYearStateCalendarYearID, Form1099MYearID = NULL, StateCalendarYearID = NULL, StateID = NULL, TaxStateID = NULL, Vendors = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateCalendarYear", objectId = Form1099MYearStateCalendarYearID, body = list(DataObject = body), searchFields = append("Form1099MYearStateCalendarYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List GECCountries
	#'
	#' This function returns a dataframe or json object of GECCountries
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GECCountries. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GECCountries.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GECCountry') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of GECCountries
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listGECCountries <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, GECCountryID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "GECCountry", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a GECCountry
	#'
	#' This function returns a dataframe or json object of a GECCountry
	#' @param GECCountryID The ID of the GECCountry to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given GECCountry. Defaults to FALSE for all return fields which, for convenience, returns all fields for the GECCountry.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('GECCountry') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of GECCountry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getGECCountry <- function(GECCountryID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "GECCountryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "GECCountry", objectId = GECCountryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a GECCountry
	#'
	#' This function deletes a GECCountry
	#' @param GECCountryID The ID of the GECCountry to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The GECCountryID of the deleted GECCountry.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteGECCountry <- function(GECCountryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "GECCountry", objectId = GECCountryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a GECCountry
	#'
	#' This function creates a GECCountry
	#' @param fieldNames The field values to give the created GECCountry. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created GECCountry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createGECCountry <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "GECCountry", body = list(DataObject = body), searchFields = append("GECCountryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a GECCountry
	#'
	#' This function modifies a GECCountry
	#' @param fieldNames The field values to give the modified GECCountry. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified GECCountry
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyGECCountry <- function(GECCountryID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "GECCountry", objectId = GECCountryID, body = list(DataObject = body), searchFields = append("GECCountryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099BoxThresholds
	#'
	#' This function returns a dataframe or json object of Form1099BoxThresholds
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099BoxThresholds. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099BoxThresholds.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099BoxThreshold') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099BoxThresholds
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099BoxThresholds <- function(searchConditionsList = NULL, Amount = F, ConfigCalendarYearID = F, CreatedTime = F, Form1099BoxThresholdID = F, Form1099TypeID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099BoxThreshold", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099BoxThreshold
	#'
	#' This function returns a dataframe or json object of a Form1099BoxThreshold
	#' @param Form1099BoxThresholdID The ID of the Form1099BoxThreshold to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099BoxThreshold. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099BoxThreshold.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099BoxThreshold') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099BoxThreshold
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099BoxThreshold <- function(Form1099BoxThresholdID, Amount = F, ConfigCalendarYearID = F, CreatedTime = F, Form1099TypeID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099BoxThresholdID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099BoxThreshold", objectId = Form1099BoxThresholdID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099BoxThreshold
	#'
	#' This function deletes a Form1099BoxThreshold
	#' @param Form1099BoxThresholdID The ID of the Form1099BoxThreshold to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099BoxThresholdID of the deleted Form1099BoxThreshold.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099BoxThreshold <- function(Form1099BoxThresholdID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099BoxThreshold", objectId = Form1099BoxThresholdID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099BoxThreshold
	#'
	#' This function creates a Form1099BoxThreshold
	#' @param fieldNames The field values to give the created Form1099BoxThreshold. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099BoxThreshold
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099BoxThreshold <- function(Amount = NULL, ConfigCalendarYearID = NULL, Form1099TypeID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099BoxThreshold", body = list(DataObject = body), searchFields = append("Form1099BoxThresholdID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099BoxThreshold
	#'
	#' This function modifies a Form1099BoxThreshold
	#' @param fieldNames The field values to give the modified Form1099BoxThreshold. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099BoxThreshold
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099BoxThreshold <- function(Form1099BoxThresholdID, Amount = NULL, ConfigCalendarYearID = NULL, Form1099TypeID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099BoxThreshold", objectId = Form1099BoxThresholdID, body = list(DataObject = body), searchFields = append("Form1099BoxThresholdID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MYearStateBoxValues
	#'
	#' This function returns a dataframe or json object of Form1099MYearStateBoxValues
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearStateBoxValues. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearStateBoxValues.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearStateBoxValue') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MYearStateBoxValues
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MYearStateBoxValues <- function(searchConditionsList = NULL, Amount = F, BoxNumber = F, Description = F, Form1099MYearID = F, Form1099MYearStateBoxValueID = F, StateID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MYearStateBoxValue", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MYearStateBoxValue
	#'
	#' This function returns a dataframe or json object of a Form1099MYearStateBoxValue
	#' @param Form1099MYearStateBoxValueID The ID of the Form1099MYearStateBoxValue to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearStateBoxValue. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearStateBoxValue.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearStateBoxValue') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MYearStateBoxValue
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MYearStateBoxValue <- function(Form1099MYearStateBoxValueID, Amount = F, BoxNumber = F, Description = F, Form1099MYearID = F, StateID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MYearStateBoxValueID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateBoxValue", objectId = Form1099MYearStateBoxValueID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MYearStateBoxValue
	#'
	#' This function deletes a Form1099MYearStateBoxValue
	#' @param Form1099MYearStateBoxValueID The ID of the Form1099MYearStateBoxValue to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MYearStateBoxValueID of the deleted Form1099MYearStateBoxValue.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MYearStateBoxValue <- function(Form1099MYearStateBoxValueID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateBoxValue", objectId = Form1099MYearStateBoxValueID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MYearStateBoxValue
	#'
	#' This function creates a Form1099MYearStateBoxValue
	#' @param fieldNames The field values to give the created Form1099MYearStateBoxValue. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MYearStateBoxValue
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MYearStateBoxValue <- function(Amount = NULL, BoxNumber = NULL, Description = NULL, Form1099MYearID = NULL, StateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateBoxValue", body = list(DataObject = body), searchFields = append("Form1099MYearStateBoxValueID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MYearStateBoxValue
	#'
	#' This function modifies a Form1099MYearStateBoxValue
	#' @param fieldNames The field values to give the modified Form1099MYearStateBoxValue. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MYearStateBoxValue
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MYearStateBoxValue <- function(Form1099MYearStateBoxValueID, Amount = NULL, BoxNumber = NULL, Description = NULL, Form1099MYearID = NULL, StateID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MYearStateBoxValue", objectId = Form1099MYearStateBoxValueID, body = list(DataObject = body), searchFields = append("Form1099MYearStateBoxValueID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099MYearVendorBoxes
	#'
	#' This function returns a dataframe or json object of Form1099MYearVendorBoxes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearVendorBoxes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearVendorBoxes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearVendorBox') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099MYearVendorBoxes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099MYearVendorBoxes <- function(searchConditionsList = NULL, Amount = F, CreatedTime = F, Form1099MYearVendorBoxID = F, Form1099MYearVendorID = F, Form1099TypeID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099MYearVendorBox", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099MYearVendorBox
	#'
	#' This function returns a dataframe or json object of a Form1099MYearVendorBox
	#' @param Form1099MYearVendorBoxID The ID of the Form1099MYearVendorBox to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099MYearVendorBox. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099MYearVendorBox.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099MYearVendorBox') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099MYearVendorBox
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099MYearVendorBox <- function(Form1099MYearVendorBoxID, Amount = F, CreatedTime = F, Form1099MYearVendorID = F, Form1099TypeID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099MYearVendorBoxID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendorBox", objectId = Form1099MYearVendorBoxID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099MYearVendorBox
	#'
	#' This function deletes a Form1099MYearVendorBox
	#' @param Form1099MYearVendorBoxID The ID of the Form1099MYearVendorBox to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099MYearVendorBoxID of the deleted Form1099MYearVendorBox.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099MYearVendorBox <- function(Form1099MYearVendorBoxID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendorBox", objectId = Form1099MYearVendorBoxID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099MYearVendorBox
	#'
	#' This function creates a Form1099MYearVendorBox
	#' @param fieldNames The field values to give the created Form1099MYearVendorBox. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099MYearVendorBox
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099MYearVendorBox <- function(Amount = NULL, Form1099MYearVendorID = NULL, Form1099TypeID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendorBox", body = list(DataObject = body), searchFields = append("Form1099MYearVendorBoxID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099MYearVendorBox
	#'
	#' This function modifies a Form1099MYearVendorBox
	#' @param fieldNames The field values to give the modified Form1099MYearVendorBox. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099MYearVendorBox
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099MYearVendorBox <- function(Form1099MYearVendorBoxID, Amount = NULL, Form1099MYearVendorID = NULL, Form1099TypeID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099MYearVendorBox", objectId = Form1099MYearVendorBoxID, body = list(DataObject = body), searchFields = append("Form1099MYearVendorBoxID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List ConfigCalendarYears
	#'
	#' This function returns a dataframe or json object of ConfigCalendarYears
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ConfigCalendarYears. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ConfigCalendarYears.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ConfigCalendarYear') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of ConfigCalendarYears
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listConfigCalendarYears <- function(searchConditionsList = NULL, CalendarYearID = F, ConfigCalendarYearID = F, CreatedTime = F, DistrictID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "ConfigCalendarYear", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a ConfigCalendarYear
	#'
	#' This function returns a dataframe or json object of a ConfigCalendarYear
	#' @param ConfigCalendarYearID The ID of the ConfigCalendarYear to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given ConfigCalendarYear. Defaults to FALSE for all return fields which, for convenience, returns all fields for the ConfigCalendarYear.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('ConfigCalendarYear') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of ConfigCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getConfigCalendarYear <- function(ConfigCalendarYearID, CalendarYearID = F, CreatedTime = F, DistrictID = F, ModifiedTime = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "ConfigCalendarYearID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "ConfigCalendarYear", objectId = ConfigCalendarYearID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a ConfigCalendarYear
	#'
	#' This function deletes a ConfigCalendarYear
	#' @param ConfigCalendarYearID The ID of the ConfigCalendarYear to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The ConfigCalendarYearID of the deleted ConfigCalendarYear.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteConfigCalendarYear <- function(ConfigCalendarYearID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "ConfigCalendarYear", objectId = ConfigCalendarYearID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a ConfigCalendarYear
	#'
	#' This function creates a ConfigCalendarYear
	#' @param fieldNames The field values to give the created ConfigCalendarYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created ConfigCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createConfigCalendarYear <- function(CalendarYearID = NULL, DistrictID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "ConfigCalendarYear", body = list(DataObject = body), searchFields = append("ConfigCalendarYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a ConfigCalendarYear
	#'
	#' This function modifies a ConfigCalendarYear
	#' @param fieldNames The field values to give the modified ConfigCalendarYear. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified ConfigCalendarYear
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyConfigCalendarYear <- function(ConfigCalendarYearID, CalendarYearID = NULL, DistrictID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "ConfigCalendarYear", objectId = ConfigCalendarYearID, body = list(DataObject = body), searchFields = append("ConfigCalendarYearID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List FederalW2Box12Codes
	#'
	#' This function returns a dataframe or json object of FederalW2Box12Codes
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalW2Box12Codes. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalW2Box12Codes.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalW2Box12Code') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of FederalW2Box12Codes
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listFederalW2Box12Codes <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, FederalW2Box12CodeID = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "FederalW2Box12Code", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a FederalW2Box12Code
	#'
	#' This function returns a dataframe or json object of a FederalW2Box12Code
	#' @param FederalW2Box12CodeID The ID of the FederalW2Box12Code to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given FederalW2Box12Code. Defaults to FALSE for all return fields which, for convenience, returns all fields for the FederalW2Box12Code.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('FederalW2Box12Code') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of FederalW2Box12Code
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getFederalW2Box12Code <- function(FederalW2Box12CodeID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "FederalW2Box12CodeID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "FederalW2Box12Code", objectId = FederalW2Box12CodeID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a FederalW2Box12Code
	#'
	#' This function deletes a FederalW2Box12Code
	#' @param FederalW2Box12CodeID The ID of the FederalW2Box12Code to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The FederalW2Box12CodeID of the deleted FederalW2Box12Code.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteFederalW2Box12Code <- function(FederalW2Box12CodeID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "FederalW2Box12Code", objectId = FederalW2Box12CodeID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a FederalW2Box12Code
	#'
	#' This function creates a FederalW2Box12Code
	#' @param fieldNames The field values to give the created FederalW2Box12Code. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created FederalW2Box12Code
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createFederalW2Box12Code <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "FederalW2Box12Code", body = list(DataObject = body), searchFields = append("FederalW2Box12CodeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a FederalW2Box12Code
	#'
	#' This function modifies a FederalW2Box12Code
	#' @param fieldNames The field values to give the modified FederalW2Box12Code. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified FederalW2Box12Code
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyFederalW2Box12Code <- function(FederalW2Box12CodeID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "FederalW2Box12Code", objectId = FederalW2Box12CodeID, body = list(DataObject = body), searchFields = append("FederalW2Box12CodeID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2Employments
	#'
	#' This function returns a dataframe or json object of W2Employments
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2Employments. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2Employments.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2Employment') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2Employments
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2Employments <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, W2EmploymentID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2Employment", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2Employment
	#'
	#' This function returns a dataframe or json object of a W2Employment
	#' @param W2EmploymentID The ID of the W2Employment to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2Employment. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2Employment.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2Employment') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2Employment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2Employment <- function(W2EmploymentID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2EmploymentID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2Employment", objectId = W2EmploymentID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2Employment
	#'
	#' This function deletes a W2Employment
	#' @param W2EmploymentID The ID of the W2Employment to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2EmploymentID of the deleted W2Employment.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2Employment <- function(W2EmploymentID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2Employment", objectId = W2EmploymentID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2Employment
	#'
	#' This function creates a W2Employment
	#' @param fieldNames The field values to give the created W2Employment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2Employment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2Employment <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2Employment", body = list(DataObject = body), searchFields = append("W2EmploymentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2Employment
	#'
	#' This function modifies a W2Employment
	#' @param fieldNames The field values to give the modified W2Employment. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2Employment
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2Employment <- function(W2EmploymentID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2Employment", objectId = W2EmploymentID, body = list(DataObject = body), searchFields = append("W2EmploymentID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2KindOfEmployers
	#'
	#' This function returns a dataframe or json object of W2KindOfEmployers
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2KindOfEmployers. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2KindOfEmployers.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2KindOfEmployer') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2KindOfEmployers
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2KindOfEmployers <- function(searchConditionsList = NULL, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, W2KindOfEmployerID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2KindOfEmployer", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2KindOfEmployer
	#'
	#' This function returns a dataframe or json object of a W2KindOfEmployer
	#' @param W2KindOfEmployerID The ID of the W2KindOfEmployer to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2KindOfEmployer. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2KindOfEmployer.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2KindOfEmployer') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2KindOfEmployer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2KindOfEmployer <- function(W2KindOfEmployerID, Code = F, CodeDescription = F, CreatedTime = F, Description = F, IsInvalid = F, ModifiedTime = F, SkywardHash = F, SkywardID = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ValidYearHigh = F, ValidYearLow = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2KindOfEmployerID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2KindOfEmployer", objectId = W2KindOfEmployerID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2KindOfEmployer
	#'
	#' This function deletes a W2KindOfEmployer
	#' @param W2KindOfEmployerID The ID of the W2KindOfEmployer to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2KindOfEmployerID of the deleted W2KindOfEmployer.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2KindOfEmployer <- function(W2KindOfEmployerID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2KindOfEmployer", objectId = W2KindOfEmployerID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2KindOfEmployer
	#'
	#' This function creates a W2KindOfEmployer
	#' @param fieldNames The field values to give the created W2KindOfEmployer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2KindOfEmployer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2KindOfEmployer <- function(Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2KindOfEmployer", body = list(DataObject = body), searchFields = append("W2KindOfEmployerID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2KindOfEmployer
	#'
	#' This function modifies a W2KindOfEmployer
	#' @param fieldNames The field values to give the modified W2KindOfEmployer. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2KindOfEmployer
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2KindOfEmployer <- function(W2KindOfEmployerID, Code = NULL, Description = NULL, IsInvalid = NULL, ValidYearHigh = NULL, ValidYearLow = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2KindOfEmployer", objectId = W2KindOfEmployerID, body = list(DataObject = body), searchFields = append("W2KindOfEmployerID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CEmployeeExceptions
	#'
	#' This function returns a dataframe or json object of W2CEmployeeExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployeeExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployeeExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployeeException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CEmployeeExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CEmployeeExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CEmployeeExceptionID = F, W2CEmployeeV1ID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CEmployeeException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CEmployeeException
	#'
	#' This function returns a dataframe or json object of a W2CEmployeeException
	#' @param W2CEmployeeExceptionID The ID of the W2CEmployeeException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployeeException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployeeException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployeeException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CEmployeeException <- function(W2CEmployeeExceptionID, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CEmployeeV1ID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CEmployeeExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeException", objectId = W2CEmployeeExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CEmployeeException
	#'
	#' This function deletes a W2CEmployeeException
	#' @param W2CEmployeeExceptionID The ID of the W2CEmployeeException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CEmployeeExceptionID of the deleted W2CEmployeeException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CEmployeeException <- function(W2CEmployeeExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeException", objectId = W2CEmployeeExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CEmployeeException
	#'
	#' This function creates a W2CEmployeeException
	#' @param fieldNames The field values to give the created W2CEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CEmployeeException <- function(Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, W2CEmployeeV1ID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeException", body = list(DataObject = body), searchFields = append("W2CEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CEmployeeException
	#'
	#' This function modifies a W2CEmployeeException
	#' @param fieldNames The field values to give the modified W2CEmployeeException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CEmployeeException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CEmployeeException <- function(W2CEmployeeExceptionID, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, W2CEmployeeV1ID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeException", objectId = W2CEmployeeExceptionID, body = list(DataObject = body), searchFields = append("W2CEmployeeExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CEmployerExceptions
	#'
	#' This function returns a dataframe or json object of W2CEmployerExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployerExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployerExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployerException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CEmployerExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CEmployerExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CEmployerExceptionID = F, W2CEmployerV1ID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CEmployerException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CEmployerException
	#'
	#' This function returns a dataframe or json object of a W2CEmployerException
	#' @param W2CEmployerExceptionID The ID of the W2CEmployerException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployerException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployerException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployerException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CEmployerException <- function(W2CEmployerExceptionID, CreatedTime = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CEmployerV1ID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CEmployerExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CEmployerException", objectId = W2CEmployerExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CEmployerException
	#'
	#' This function deletes a W2CEmployerException
	#' @param W2CEmployerExceptionID The ID of the W2CEmployerException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CEmployerExceptionID of the deleted W2CEmployerException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CEmployerException <- function(W2CEmployerExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CEmployerException", objectId = W2CEmployerExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CEmployerException
	#'
	#' This function creates a W2CEmployerException
	#' @param fieldNames The field values to give the created W2CEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CEmployerException <- function(Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, W2CEmployerV1ID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CEmployerException", body = list(DataObject = body), searchFields = append("W2CEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CEmployerException
	#'
	#' This function modifies a W2CEmployerException
	#' @param fieldNames The field values to give the modified W2CEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CEmployerException <- function(W2CEmployerExceptionID, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, W2CEmployerV1ID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CEmployerException", objectId = W2CEmployerExceptionID, body = list(DataObject = body), searchFields = append("W2CEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CEmployeeV1s
	#'
	#' This function returns a dataframe or json object of W2CEmployeeV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployeeV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployeeV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployeeV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CEmployeeV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CEmployeeV1s <- function(searchConditionsList = NULL, Box12aCode = F, Box12bCode = F, Box12cCode = F, Box12dCode = F, City = F, CorrectBox10DependentCareBenefits = F, CorrectBox11NonQualifiedPlansNonSection457b = F, CorrectBox11NonQualifiedPlansSection457b = F, CorrectBox12aValue = F, CorrectBox12bValue = F, CorrectBox12cValue = F, CorrectBox12dValue = F, CorrectBox13IsRetirementPlan = F, CorrectBox13IsStatutoryEmployee = F, CorrectBox13IsThirdPartySickPay = F, CorrectBox1FederalWages = F, CorrectBox2FederalIncomeTaxWithheld = F, CorrectBox3SocialSecurityWages = F, CorrectBox4SocialSecurityTaxWithheld = F, CorrectBox5MedicareWages = F, CorrectBox6MedicareTaxWithheld = F, CorrectBox7SocialSecurityTips = F, CorrectFirstName = F, CorrectLastName = F, CorrectMiddleName = F, CorrectSSN = F, CountryCode = F, CreatedTime = F, DeliveryAddress = F, EmployeeAddressType = F, EmployeeID = F, FederalW2Box12aCodeID = F, FederalW2Box12bCodeID = F, FederalW2Box12cCodeID = F, FederalW2Box12dCodeID = F, ForeignPostalCode = F, ForeignStateProvince = F, HasErrors = F, IsCorrected = F, IsCorrectedName = F, IsCorrectedSSN = F, KeyHash = F, LocationAddress = F, MediaIDIndividualPDF = F, ModifiedTime = F, OriginallyReportedBox10DependentCareBenefits = F, OriginallyReportedBox11NonQualifiedPlansNonSection457b = F, OriginallyReportedBox11NonQualifiedPlansSection457b = F, OriginallyReportedBox12aValue = F, OriginallyReportedBox12bValue = F, OriginallyReportedBox12cValue = F, OriginallyReportedBox12dValue = F, OriginallyReportedBox13IsRetirementPlan = F, OriginallyReportedBox13IsStatutoryEmployee = F, OriginallyReportedBox13IsThirdPartySickPay = F, OriginallyReportedBox1FederalWages = F, OriginallyReportedBox2FederalIncomeTaxWithheld = F, OriginallyReportedBox3SocialSecurityWages = F, OriginallyReportedBox4SocialSecurityTaxWithheld = F, OriginallyReportedBox5MedicareWages = F, OriginallyReportedBox6MedicareTaxWithheld = F, OriginallyReportedBox7SocialSecurityTips = F, OriginallyReportedFirstName = F, OriginallyReportedLastName = F, OriginallyReportedMiddleName = F, OriginallyReportedSSN = F, StateCode = F, StateID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CEmployeeV1ID = F, W2CountryID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CEmployeeV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CEmployeeV1
	#'
	#' This function returns a dataframe or json object of a W2CEmployeeV1
	#' @param W2CEmployeeV1ID The ID of the W2CEmployeeV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployeeV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployeeV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployeeV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CEmployeeV1 <- function(W2CEmployeeV1ID, Box12aCode = F, Box12bCode = F, Box12cCode = F, Box12dCode = F, City = F, CorrectBox10DependentCareBenefits = F, CorrectBox11NonQualifiedPlansNonSection457b = F, CorrectBox11NonQualifiedPlansSection457b = F, CorrectBox12aValue = F, CorrectBox12bValue = F, CorrectBox12cValue = F, CorrectBox12dValue = F, CorrectBox13IsRetirementPlan = F, CorrectBox13IsStatutoryEmployee = F, CorrectBox13IsThirdPartySickPay = F, CorrectBox1FederalWages = F, CorrectBox2FederalIncomeTaxWithheld = F, CorrectBox3SocialSecurityWages = F, CorrectBox4SocialSecurityTaxWithheld = F, CorrectBox5MedicareWages = F, CorrectBox6MedicareTaxWithheld = F, CorrectBox7SocialSecurityTips = F, CorrectFirstName = F, CorrectLastName = F, CorrectMiddleName = F, CorrectSSN = F, CountryCode = F, CreatedTime = F, DeliveryAddress = F, EmployeeAddressType = F, EmployeeID = F, FederalW2Box12aCodeID = F, FederalW2Box12bCodeID = F, FederalW2Box12cCodeID = F, FederalW2Box12dCodeID = F, ForeignPostalCode = F, ForeignStateProvince = F, HasErrors = F, IsCorrected = F, IsCorrectedName = F, IsCorrectedSSN = F, KeyHash = F, LocationAddress = F, MediaIDIndividualPDF = F, ModifiedTime = F, OriginallyReportedBox10DependentCareBenefits = F, OriginallyReportedBox11NonQualifiedPlansNonSection457b = F, OriginallyReportedBox11NonQualifiedPlansSection457b = F, OriginallyReportedBox12aValue = F, OriginallyReportedBox12bValue = F, OriginallyReportedBox12cValue = F, OriginallyReportedBox12dValue = F, OriginallyReportedBox13IsRetirementPlan = F, OriginallyReportedBox13IsStatutoryEmployee = F, OriginallyReportedBox13IsThirdPartySickPay = F, OriginallyReportedBox1FederalWages = F, OriginallyReportedBox2FederalIncomeTaxWithheld = F, OriginallyReportedBox3SocialSecurityWages = F, OriginallyReportedBox4SocialSecurityTaxWithheld = F, OriginallyReportedBox5MedicareWages = F, OriginallyReportedBox6MedicareTaxWithheld = F, OriginallyReportedBox7SocialSecurityTips = F, OriginallyReportedFirstName = F, OriginallyReportedLastName = F, OriginallyReportedMiddleName = F, OriginallyReportedSSN = F, StateCode = F, StateID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CEmployeeV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeV1", objectId = W2CEmployeeV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CEmployeeV1
	#'
	#' This function deletes a W2CEmployeeV1
	#' @param W2CEmployeeV1ID The ID of the W2CEmployeeV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CEmployeeV1ID of the deleted W2CEmployeeV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CEmployeeV1 <- function(W2CEmployeeV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeV1", objectId = W2CEmployeeV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CEmployeeV1
	#'
	#' This function creates a W2CEmployeeV1
	#' @param fieldNames The field values to give the created W2CEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CEmployeeV1 <- function(Box12aCode = NULL, Box12bCode = NULL, Box12cCode = NULL, Box12dCode = NULL, City = NULL, CorrectBox10DependentCareBenefits = NULL, CorrectBox11NonQualifiedPlansNonSection457b = NULL, CorrectBox11NonQualifiedPlansSection457b = NULL, CorrectBox12aValue = NULL, CorrectBox12bValue = NULL, CorrectBox12cValue = NULL, CorrectBox12dValue = NULL, CorrectBox13IsRetirementPlan = NULL, CorrectBox13IsStatutoryEmployee = NULL, CorrectBox13IsThirdPartySickPay = NULL, CorrectBox1FederalWages = NULL, CorrectBox2FederalIncomeTaxWithheld = NULL, CorrectBox3SocialSecurityWages = NULL, CorrectBox4SocialSecurityTaxWithheld = NULL, CorrectBox5MedicareWages = NULL, CorrectBox6MedicareTaxWithheld = NULL, CorrectBox7SocialSecurityTips = NULL, CorrectFirstName = NULL, CorrectLastName = NULL, CorrectMiddleName = NULL, CorrectSSN = NULL, CountryCode = NULL, DeliveryAddress = NULL, EmployeeAddressType = NULL, EmployeeID = NULL, FederalW2Box12aCodeID = NULL, FederalW2Box12bCodeID = NULL, FederalW2Box12cCodeID = NULL, FederalW2Box12dCodeID = NULL, ForeignPostalCode = NULL, ForeignStateProvince = NULL, LocationAddress = NULL, MediaIDIndividualPDF = NULL, OriginallyReportedBox10DependentCareBenefits = NULL, OriginallyReportedBox11NonQualifiedPlansNonSection457b = NULL, OriginallyReportedBox11NonQualifiedPlansSection457b = NULL, OriginallyReportedBox12aValue = NULL, OriginallyReportedBox12bValue = NULL, OriginallyReportedBox12cValue = NULL, OriginallyReportedBox12dValue = NULL, OriginallyReportedBox13IsRetirementPlan = NULL, OriginallyReportedBox13IsStatutoryEmployee = NULL, OriginallyReportedBox13IsThirdPartySickPay = NULL, OriginallyReportedBox1FederalWages = NULL, OriginallyReportedBox2FederalIncomeTaxWithheld = NULL, OriginallyReportedBox3SocialSecurityWages = NULL, OriginallyReportedBox4SocialSecurityTaxWithheld = NULL, OriginallyReportedBox5MedicareWages = NULL, OriginallyReportedBox6MedicareTaxWithheld = NULL, OriginallyReportedBox7SocialSecurityTips = NULL, OriginallyReportedFirstName = NULL, OriginallyReportedLastName = NULL, OriginallyReportedMiddleName = NULL, OriginallyReportedSSN = NULL, StateCode = NULL, StateID = NULL, W2CountryID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeV1", body = list(DataObject = body), searchFields = append("W2CEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CEmployeeV1
	#'
	#' This function modifies a W2CEmployeeV1
	#' @param fieldNames The field values to give the modified W2CEmployeeV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CEmployeeV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CEmployeeV1 <- function(W2CEmployeeV1ID, Box12aCode = NULL, Box12bCode = NULL, Box12cCode = NULL, Box12dCode = NULL, City = NULL, CorrectBox10DependentCareBenefits = NULL, CorrectBox11NonQualifiedPlansNonSection457b = NULL, CorrectBox11NonQualifiedPlansSection457b = NULL, CorrectBox12aValue = NULL, CorrectBox12bValue = NULL, CorrectBox12cValue = NULL, CorrectBox12dValue = NULL, CorrectBox13IsRetirementPlan = NULL, CorrectBox13IsStatutoryEmployee = NULL, CorrectBox13IsThirdPartySickPay = NULL, CorrectBox1FederalWages = NULL, CorrectBox2FederalIncomeTaxWithheld = NULL, CorrectBox3SocialSecurityWages = NULL, CorrectBox4SocialSecurityTaxWithheld = NULL, CorrectBox5MedicareWages = NULL, CorrectBox6MedicareTaxWithheld = NULL, CorrectBox7SocialSecurityTips = NULL, CorrectFirstName = NULL, CorrectLastName = NULL, CorrectMiddleName = NULL, CorrectSSN = NULL, CountryCode = NULL, DeliveryAddress = NULL, EmployeeAddressType = NULL, EmployeeID = NULL, FederalW2Box12aCodeID = NULL, FederalW2Box12bCodeID = NULL, FederalW2Box12cCodeID = NULL, FederalW2Box12dCodeID = NULL, ForeignPostalCode = NULL, ForeignStateProvince = NULL, LocationAddress = NULL, MediaIDIndividualPDF = NULL, OriginallyReportedBox10DependentCareBenefits = NULL, OriginallyReportedBox11NonQualifiedPlansNonSection457b = NULL, OriginallyReportedBox11NonQualifiedPlansSection457b = NULL, OriginallyReportedBox12aValue = NULL, OriginallyReportedBox12bValue = NULL, OriginallyReportedBox12cValue = NULL, OriginallyReportedBox12dValue = NULL, OriginallyReportedBox13IsRetirementPlan = NULL, OriginallyReportedBox13IsStatutoryEmployee = NULL, OriginallyReportedBox13IsThirdPartySickPay = NULL, OriginallyReportedBox1FederalWages = NULL, OriginallyReportedBox2FederalIncomeTaxWithheld = NULL, OriginallyReportedBox3SocialSecurityWages = NULL, OriginallyReportedBox4SocialSecurityTaxWithheld = NULL, OriginallyReportedBox5MedicareWages = NULL, OriginallyReportedBox6MedicareTaxWithheld = NULL, OriginallyReportedBox7SocialSecurityTips = NULL, OriginallyReportedFirstName = NULL, OriginallyReportedLastName = NULL, OriginallyReportedMiddleName = NULL, OriginallyReportedSSN = NULL, StateCode = NULL, StateID = NULL, W2CountryID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CEmployeeV1", objectId = W2CEmployeeV1ID, body = list(DataObject = body), searchFields = append("W2CEmployeeV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CEmployerV1s
	#'
	#' This function returns a dataframe or json object of W2CEmployerV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployerV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployerV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployerV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CEmployerV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CEmployerV1s <- function(searchConditionsList = NULL, City = F, CountryCode = F, CreatedTime = F, DeliveryAddress = F, EmployerAddressType = F, EmployerContactEmail = F, EmployerContactFaxNumber = F, EmployerContactName = F, EmployerContactPhoneExtension = F, EmployerContactPhoneNumber = F, EmployerID = F, EmployerName = F, FederalEIN = F, ForeignPostalCode = F, ForeignStateProvince = F, HasErrors = F, KeyHash = F, LocationAddress = F, ModifiedTime = F, StateCode = F, StateID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CEmployerV1ID = F, W2CountryID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, W2EmploymentCode = F, W2EmploymentID = F, W2KindOfEmployerCode = F, W2KindOfEmployerID = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CEmployerV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CEmployerV1
	#'
	#' This function returns a dataframe or json object of a W2CEmployerV1
	#' @param W2CEmployerV1ID The ID of the W2CEmployerV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CEmployerV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CEmployerV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CEmployerV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CEmployerV1 <- function(W2CEmployerV1ID, City = F, CountryCode = F, CreatedTime = F, DeliveryAddress = F, EmployerAddressType = F, EmployerContactEmail = F, EmployerContactFaxNumber = F, EmployerContactName = F, EmployerContactPhoneExtension = F, EmployerContactPhoneNumber = F, EmployerID = F, EmployerName = F, FederalEIN = F, ForeignPostalCode = F, ForeignStateProvince = F, HasErrors = F, KeyHash = F, LocationAddress = F, ModifiedTime = F, StateCode = F, StateID = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CountryID = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, W2EmploymentCode = F, W2EmploymentID = F, W2KindOfEmployerCode = F, W2KindOfEmployerID = F, ZipCode = F, ZipCodeExtension = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CEmployerV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CEmployerV1", objectId = W2CEmployerV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CEmployerV1
	#'
	#' This function deletes a W2CEmployerV1
	#' @param W2CEmployerV1ID The ID of the W2CEmployerV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CEmployerV1ID of the deleted W2CEmployerV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CEmployerV1 <- function(W2CEmployerV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CEmployerV1", objectId = W2CEmployerV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CEmployerV1
	#'
	#' This function creates a W2CEmployerV1
	#' @param fieldNames The field values to give the created W2CEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CEmployerV1 <- function(City = NULL, CountryCode = NULL, DeliveryAddress = NULL, EmployerAddressType = NULL, EmployerContactEmail = NULL, EmployerContactFaxNumber = NULL, EmployerContactName = NULL, EmployerContactPhoneExtension = NULL, EmployerContactPhoneNumber = NULL, EmployerID = NULL, EmployerName = NULL, FederalEIN = NULL, ForeignPostalCode = NULL, ForeignStateProvince = NULL, LocationAddress = NULL, StateCode = NULL, StateID = NULL, W2CountryID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, W2EmploymentCode = NULL, W2EmploymentID = NULL, W2KindOfEmployerCode = NULL, W2KindOfEmployerID = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CEmployerV1", body = list(DataObject = body), searchFields = append("W2CEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CEmployerV1
	#'
	#' This function modifies a W2CEmployerV1
	#' @param fieldNames The field values to give the modified W2CEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CEmployerV1 <- function(W2CEmployerV1ID, City = NULL, CountryCode = NULL, DeliveryAddress = NULL, EmployerAddressType = NULL, EmployerContactEmail = NULL, EmployerContactFaxNumber = NULL, EmployerContactName = NULL, EmployerContactPhoneExtension = NULL, EmployerContactPhoneNumber = NULL, EmployerID = NULL, EmployerName = NULL, FederalEIN = NULL, ForeignPostalCode = NULL, ForeignStateProvince = NULL, LocationAddress = NULL, StateCode = NULL, StateID = NULL, W2CountryID = NULL, W2CSubmissionID = NULL, W2CSubmissionRunHistoryID = NULL, W2EmploymentCode = NULL, W2EmploymentID = NULL, W2KindOfEmployerCode = NULL, W2KindOfEmployerID = NULL, ZipCode = NULL, ZipCodeExtension = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CEmployerV1", objectId = W2CEmployerV1ID, body = list(DataObject = body), searchFields = append("W2CEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CSubmissions
	#'
	#' This function returns a dataframe or json object of W2CSubmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CSubmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CSubmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CSubmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CSubmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CSubmissions <- function(searchConditionsList = NULL, AllowEmployeeAccess = F, CalendarYearID = F, CreatedTime = F, Description = F, DistrictID = F, EmployeeCount = F, FinalizedTime = F, HasAnExport = F, HasAnExtract = F, IsAvailableInEmployeeAccess = F, IsFinalized = F, ModifiedTime = F, MostRecentExportDate = F, MostRecentExtractDate = F, NumericYear = F, TransmissionNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CSubmissionID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CSubmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CSubmission
	#'
	#' This function returns a dataframe or json object of a W2CSubmission
	#' @param W2CSubmissionID The ID of the W2CSubmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CSubmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CSubmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CSubmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CSubmission <- function(W2CSubmissionID, AllowEmployeeAccess = F, CalendarYearID = F, CreatedTime = F, Description = F, DistrictID = F, EmployeeCount = F, FinalizedTime = F, HasAnExport = F, HasAnExtract = F, IsAvailableInEmployeeAccess = F, IsFinalized = F, ModifiedTime = F, MostRecentExportDate = F, MostRecentExtractDate = F, NumericYear = F, TransmissionNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CSubmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CSubmission", objectId = W2CSubmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CSubmission
	#'
	#' This function deletes a W2CSubmission
	#' @param W2CSubmissionID The ID of the W2CSubmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CSubmissionID of the deleted W2CSubmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CSubmission <- function(W2CSubmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CSubmission", objectId = W2CSubmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CSubmission
	#'
	#' This function creates a W2CSubmission
	#' @param fieldNames The field values to give the created W2CSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CSubmission <- function(CalendarYearID = NULL, Description = NULL, DistrictID = NULL, FinalizedTime = NULL, IsAvailableInEmployeeAccess = NULL, IsFinalized = NULL, NumericYear = NULL, TransmissionNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CSubmission", body = list(DataObject = body), searchFields = append("W2CSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CSubmission
	#'
	#' This function modifies a W2CSubmission
	#' @param fieldNames The field values to give the modified W2CSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CSubmission <- function(W2CSubmissionID, CalendarYearID = NULL, Description = NULL, DistrictID = NULL, FinalizedTime = NULL, IsAvailableInEmployeeAccess = NULL, IsFinalized = NULL, NumericYear = NULL, TransmissionNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CSubmission", objectId = W2CSubmissionID, body = list(DataObject = body), searchFields = append("W2CSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CSubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of W2CSubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CSubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CSubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CSubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CSubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CSubmissionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportFormat = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CSubmissionID = F, W2CSubmissionRunHistoryID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CSubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CSubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of a W2CSubmissionRunHistory
	#' @param W2CSubmissionRunHistoryID The ID of the W2CSubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CSubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CSubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CSubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CSubmissionRunHistory <- function(W2CSubmissionRunHistoryID, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportFormat = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, W2CSubmissionID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CSubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CSubmissionRunHistory", objectId = W2CSubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CSubmissionRunHistory
	#'
	#' This function deletes a W2CSubmissionRunHistory
	#' @param W2CSubmissionRunHistoryID The ID of the W2CSubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CSubmissionRunHistoryID of the deleted W2CSubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CSubmissionRunHistory <- function(W2CSubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CSubmissionRunHistory", objectId = W2CSubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CSubmissionRunHistory
	#'
	#' This function creates a W2CSubmissionRunHistory
	#' @param fieldNames The field values to give the created W2CSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CSubmissionRunHistory <- function(EndDateTime = NULL, ExportFormat = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, W2CSubmissionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CSubmissionRunHistory", body = list(DataObject = body), searchFields = append("W2CSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CSubmissionRunHistory
	#'
	#' This function modifies a W2CSubmissionRunHistory
	#' @param fieldNames The field values to give the modified W2CSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CSubmissionRunHistory <- function(W2CSubmissionRunHistoryID, EndDateTime = NULL, ExportFormat = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, W2CSubmissionID = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CSubmissionRunHistory", objectId = W2CSubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("W2CSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List W2CSummaryViews
	#'
	#' This function returns a dataframe or json object of W2CSummaryViews
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CSummaryViews. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CSummaryViews.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CSummaryView') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of W2CSummaryViews
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listW2CSummaryViews <- function(searchConditionsList = NULL, CorrectBox10DependentCareBenefitsTotal = F, CorrectBox11NonQualifiedPlansNonSection457bTotal = F, CorrectBox11NonQualifiedPlansSection457bTotal = F, CorrectBox12AATotal = F, CorrectBox12BBTotal = F, CorrectBox12CTotal = F, CorrectBox12DDTotal = F, CorrectBox12DTotal = F, CorrectBox12ETotal = F, CorrectBox12ffTotal = F, CorrectBox12FTotal = F, CorrectBox12GTotal = F, CorrectBox12HTotal = F, CorrectBox12VTotal = F, CorrectBox12WTotal = F, CorrectBox12YTotal = F, CorrectBox1FederalWagesTotal = F, CorrectBox2FederalIncomeTaxWithheldTotal = F, CorrectBox3SocialSecurityWagesTotal = F, CorrectBox4SocialSecurityTaxWithheldTotal = F, CorrectBox5MedicareWagesTotal = F, CorrectBox6MedicareTaxWithheldTotal = F, CorrectBox7SocialSecurityTipsTotal = F, OriginallyReportedBox10DependentCareBenefitsTotal = F, OriginallyReportedBox11NonQualifiedPlansNonSection457bTotal = F, OriginallyReportedBox11NonQualifiedPlansSection457bTotal = F, OriginallyReportedBox12AATotal = F, OriginallyReportedBox12BBTotal = F, OriginallyReportedBox12CTotal = F, OriginallyReportedBox12DDTotal = F, OriginallyReportedBox12DTotal = F, OriginallyReportedBox12ETotal = F, OriginallyReportedBox12ffTotal = F, OriginallyReportedBox12FTotal = F, OriginallyReportedBox12GTotal = F, OriginallyReportedBox12HTotal = F, OriginallyReportedBox12VTotal = F, OriginallyReportedBox12WTotal = F, OriginallyReportedBox12YTotal = F, OriginallyReportedBox1FederalWagesTotal = F, OriginallyReportedBox2FederalIncomeTaxWithheldTotal = F, OriginallyReportedBox3SocialSecurityWagesTotal = F, OriginallyReportedBox4SocialSecurityTaxWithheldTotal = F, OriginallyReportedBox5MedicareWagesTotal = F, OriginallyReportedBox6MedicareTaxWithheldTotal = F, OriginallyReportedBox7SocialSecurityTipsTotal = F, W2CSubmissionID = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "W2CSummaryView", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a W2CSummaryView
	#'
	#' This function returns a dataframe or json object of a W2CSummaryView
	#' @param W2CSummaryViewID The ID of the W2CSummaryView to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given W2CSummaryView. Defaults to FALSE for all return fields which, for convenience, returns all fields for the W2CSummaryView.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('W2CSummaryView') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of W2CSummaryView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getW2CSummaryView <- function(W2CSummaryViewID, CorrectBox10DependentCareBenefitsTotal = F, CorrectBox11NonQualifiedPlansNonSection457bTotal = F, CorrectBox11NonQualifiedPlansSection457bTotal = F, CorrectBox12AATotal = F, CorrectBox12BBTotal = F, CorrectBox12CTotal = F, CorrectBox12DDTotal = F, CorrectBox12DTotal = F, CorrectBox12ETotal = F, CorrectBox12ffTotal = F, CorrectBox12FTotal = F, CorrectBox12GTotal = F, CorrectBox12HTotal = F, CorrectBox12VTotal = F, CorrectBox12WTotal = F, CorrectBox12YTotal = F, CorrectBox1FederalWagesTotal = F, CorrectBox2FederalIncomeTaxWithheldTotal = F, CorrectBox3SocialSecurityWagesTotal = F, CorrectBox4SocialSecurityTaxWithheldTotal = F, CorrectBox5MedicareWagesTotal = F, CorrectBox6MedicareTaxWithheldTotal = F, CorrectBox7SocialSecurityTipsTotal = F, OriginallyReportedBox10DependentCareBenefitsTotal = F, OriginallyReportedBox11NonQualifiedPlansNonSection457bTotal = F, OriginallyReportedBox11NonQualifiedPlansSection457bTotal = F, OriginallyReportedBox12AATotal = F, OriginallyReportedBox12BBTotal = F, OriginallyReportedBox12CTotal = F, OriginallyReportedBox12DDTotal = F, OriginallyReportedBox12DTotal = F, OriginallyReportedBox12ETotal = F, OriginallyReportedBox12ffTotal = F, OriginallyReportedBox12FTotal = F, OriginallyReportedBox12GTotal = F, OriginallyReportedBox12HTotal = F, OriginallyReportedBox12VTotal = F, OriginallyReportedBox12WTotal = F, OriginallyReportedBox12YTotal = F, OriginallyReportedBox1FederalWagesTotal = F, OriginallyReportedBox2FederalIncomeTaxWithheldTotal = F, OriginallyReportedBox3SocialSecurityWagesTotal = F, OriginallyReportedBox4SocialSecurityTaxWithheldTotal = F, OriginallyReportedBox5MedicareWagesTotal = F, OriginallyReportedBox6MedicareTaxWithheldTotal = F, OriginallyReportedBox7SocialSecurityTipsTotal = F, W2CSubmissionID = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "W2CSummaryViewID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "W2CSummaryView", objectId = W2CSummaryViewID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a W2CSummaryView
	#'
	#' This function deletes a W2CSummaryView
	#' @param W2CSummaryViewID The ID of the W2CSummaryView to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The W2CSummaryViewID of the deleted W2CSummaryView.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteW2CSummaryView <- function(W2CSummaryViewID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "W2CSummaryView", objectId = W2CSummaryViewID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a W2CSummaryView
	#'
	#' This function creates a W2CSummaryView
	#' @param fieldNames The field values to give the created W2CSummaryView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created W2CSummaryView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createW2CSummaryView <- function(CorrectBox10DependentCareBenefitsTotal = NULL, CorrectBox11NonQualifiedPlansNonSection457bTotal = NULL, CorrectBox11NonQualifiedPlansSection457bTotal = NULL, CorrectBox12AATotal = NULL, CorrectBox12BBTotal = NULL, CorrectBox12CTotal = NULL, CorrectBox12DDTotal = NULL, CorrectBox12DTotal = NULL, CorrectBox12ETotal = NULL, CorrectBox12ffTotal = NULL, CorrectBox12FTotal = NULL, CorrectBox12GTotal = NULL, CorrectBox12HTotal = NULL, CorrectBox12VTotal = NULL, CorrectBox12WTotal = NULL, CorrectBox12YTotal = NULL, CorrectBox1FederalWagesTotal = NULL, CorrectBox2FederalIncomeTaxWithheldTotal = NULL, CorrectBox3SocialSecurityWagesTotal = NULL, CorrectBox4SocialSecurityTaxWithheldTotal = NULL, CorrectBox5MedicareWagesTotal = NULL, CorrectBox6MedicareTaxWithheldTotal = NULL, CorrectBox7SocialSecurityTipsTotal = NULL, OriginallyReportedBox10DependentCareBenefitsTotal = NULL, OriginallyReportedBox11NonQualifiedPlansNonSection457bTotal = NULL, OriginallyReportedBox11NonQualifiedPlansSection457bTotal = NULL, OriginallyReportedBox12AATotal = NULL, OriginallyReportedBox12BBTotal = NULL, OriginallyReportedBox12CTotal = NULL, OriginallyReportedBox12DDTotal = NULL, OriginallyReportedBox12DTotal = NULL, OriginallyReportedBox12ETotal = NULL, OriginallyReportedBox12ffTotal = NULL, OriginallyReportedBox12FTotal = NULL, OriginallyReportedBox12GTotal = NULL, OriginallyReportedBox12HTotal = NULL, OriginallyReportedBox12VTotal = NULL, OriginallyReportedBox12WTotal = NULL, OriginallyReportedBox12YTotal = NULL, OriginallyReportedBox1FederalWagesTotal = NULL, OriginallyReportedBox2FederalIncomeTaxWithheldTotal = NULL, OriginallyReportedBox3SocialSecurityWagesTotal = NULL, OriginallyReportedBox4SocialSecurityTaxWithheldTotal = NULL, OriginallyReportedBox5MedicareWagesTotal = NULL, OriginallyReportedBox6MedicareTaxWithheldTotal = NULL, OriginallyReportedBox7SocialSecurityTipsTotal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "W2CSummaryView", body = list(DataObject = body), searchFields = append("W2CSummaryViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a W2CSummaryView
	#'
	#' This function modifies a W2CSummaryView
	#' @param fieldNames The field values to give the modified W2CSummaryView. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified W2CSummaryView
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyW2CSummaryView <- function(W2CSummaryViewID, CorrectBox10DependentCareBenefitsTotal = NULL, CorrectBox11NonQualifiedPlansNonSection457bTotal = NULL, CorrectBox11NonQualifiedPlansSection457bTotal = NULL, CorrectBox12AATotal = NULL, CorrectBox12BBTotal = NULL, CorrectBox12CTotal = NULL, CorrectBox12DDTotal = NULL, CorrectBox12DTotal = NULL, CorrectBox12ETotal = NULL, CorrectBox12ffTotal = NULL, CorrectBox12FTotal = NULL, CorrectBox12GTotal = NULL, CorrectBox12HTotal = NULL, CorrectBox12VTotal = NULL, CorrectBox12WTotal = NULL, CorrectBox12YTotal = NULL, CorrectBox1FederalWagesTotal = NULL, CorrectBox2FederalIncomeTaxWithheldTotal = NULL, CorrectBox3SocialSecurityWagesTotal = NULL, CorrectBox4SocialSecurityTaxWithheldTotal = NULL, CorrectBox5MedicareWagesTotal = NULL, CorrectBox6MedicareTaxWithheldTotal = NULL, CorrectBox7SocialSecurityTipsTotal = NULL, OriginallyReportedBox10DependentCareBenefitsTotal = NULL, OriginallyReportedBox11NonQualifiedPlansNonSection457bTotal = NULL, OriginallyReportedBox11NonQualifiedPlansSection457bTotal = NULL, OriginallyReportedBox12AATotal = NULL, OriginallyReportedBox12BBTotal = NULL, OriginallyReportedBox12CTotal = NULL, OriginallyReportedBox12DDTotal = NULL, OriginallyReportedBox12DTotal = NULL, OriginallyReportedBox12ETotal = NULL, OriginallyReportedBox12ffTotal = NULL, OriginallyReportedBox12FTotal = NULL, OriginallyReportedBox12GTotal = NULL, OriginallyReportedBox12HTotal = NULL, OriginallyReportedBox12VTotal = NULL, OriginallyReportedBox12WTotal = NULL, OriginallyReportedBox12YTotal = NULL, OriginallyReportedBox1FederalWagesTotal = NULL, OriginallyReportedBox2FederalIncomeTaxWithheldTotal = NULL, OriginallyReportedBox3SocialSecurityWagesTotal = NULL, OriginallyReportedBox4SocialSecurityTaxWithheldTotal = NULL, OriginallyReportedBox5MedicareWagesTotal = NULL, OriginallyReportedBox6MedicareTaxWithheldTotal = NULL, OriginallyReportedBox7SocialSecurityTipsTotal = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "W2CSummaryView", objectId = W2CSummaryViewID, body = list(DataObject = body), searchFields = append("W2CSummaryViewID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECEmployerExceptions
	#'
	#' This function returns a dataframe or json object of Form1099NECEmployerExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECEmployerExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECEmployerExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECEmployerException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECEmployerExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECEmployerExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Form1099NECEmployerExceptionID = F, Form1099NECEmployerV1ID = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECEmployerException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECEmployerException
	#'
	#' This function returns a dataframe or json object of a Form1099NECEmployerException
	#' @param Form1099NECEmployerExceptionID The ID of the Form1099NECEmployerException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECEmployerException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECEmployerException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECEmployerException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECEmployerException <- function(Form1099NECEmployerExceptionID, CreatedTime = F, Form1099NECEmployerV1ID = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECEmployerExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerException", objectId = Form1099NECEmployerExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECEmployerException
	#'
	#' This function deletes a Form1099NECEmployerException
	#' @param Form1099NECEmployerExceptionID The ID of the Form1099NECEmployerException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECEmployerExceptionID of the deleted Form1099NECEmployerException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECEmployerException <- function(Form1099NECEmployerExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerException", objectId = Form1099NECEmployerExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECEmployerException
	#'
	#' This function creates a Form1099NECEmployerException
	#' @param fieldNames The field values to give the created Form1099NECEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECEmployerException <- function(Form1099NECEmployerV1ID = NULL, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerException", body = list(DataObject = body), searchFields = append("Form1099NECEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECEmployerException
	#'
	#' This function modifies a Form1099NECEmployerException
	#' @param fieldNames The field values to give the modified Form1099NECEmployerException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECEmployerException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECEmployerException <- function(Form1099NECEmployerExceptionID, Form1099NECEmployerV1ID = NULL, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerException", objectId = Form1099NECEmployerExceptionID, body = list(DataObject = body), searchFields = append("Form1099NECEmployerExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECVendorExceptions
	#'
	#' This function returns a dataframe or json object of Form1099NECVendorExceptions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECVendorExceptions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECVendorExceptions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECVendorException') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECVendorExceptions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECVendorExceptions <- function(searchConditionsList = NULL, CreatedTime = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, Form1099NECVendorExceptionID = F, Form1099NECVendorV1ID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECVendorException", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECVendorException
	#'
	#' This function returns a dataframe or json object of a Form1099NECVendorException
	#' @param Form1099NECVendorExceptionID The ID of the Form1099NECVendorException to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECVendorException. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECVendorException.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECVendorException') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECVendorException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECVendorException <- function(Form1099NECVendorExceptionID, CreatedTime = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, Form1099NECVendorV1ID = F, Message = F, MessageType = F, ModifiedTime = F, ProcessType = F, RuleNumber = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECVendorExceptionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorException", objectId = Form1099NECVendorExceptionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECVendorException
	#'
	#' This function deletes a Form1099NECVendorException
	#' @param Form1099NECVendorExceptionID The ID of the Form1099NECVendorException to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECVendorExceptionID of the deleted Form1099NECVendorException.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECVendorException <- function(Form1099NECVendorExceptionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorException", objectId = Form1099NECVendorExceptionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECVendorException
	#'
	#' This function creates a Form1099NECVendorException
	#' @param fieldNames The field values to give the created Form1099NECVendorException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECVendorException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECVendorException <- function(Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, Form1099NECVendorV1ID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorException", body = list(DataObject = body), searchFields = append("Form1099NECVendorExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECVendorException
	#'
	#' This function modifies a Form1099NECVendorException
	#' @param fieldNames The field values to give the modified Form1099NECVendorException. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECVendorException
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECVendorException <- function(Form1099NECVendorExceptionID, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, Form1099NECVendorV1ID = NULL, Message = NULL, MessageType = NULL, ProcessType = NULL, RuleNumber = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorException", objectId = Form1099NECVendorExceptionID, body = list(DataObject = body), searchFields = append("Form1099NECVendorExceptionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECExportElectronicFiles
	#'
	#' This function returns a dataframe or json object of Form1099NECExportElectronicFiles
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECExportElectronicFiles. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECExportElectronicFiles.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECExportElectronicFile') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECExportElectronicFiles
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECExportElectronicFiles <- function(searchConditionsList = NULL, CreatedTime = F, Form1099NECExportElectronicFileID = F, Form1099NECSubmissionID = F, IsCombinedStateFederal = F, IsTestFile = F, ModifiedTime = F, TransmitterControlCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFile", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECExportElectronicFile
	#'
	#' This function returns a dataframe or json object of a Form1099NECExportElectronicFile
	#' @param Form1099NECExportElectronicFileID The ID of the Form1099NECExportElectronicFile to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECExportElectronicFile. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECExportElectronicFile.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECExportElectronicFile') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECExportElectronicFile <- function(Form1099NECExportElectronicFileID, CreatedTime = F, Form1099NECSubmissionID = F, IsCombinedStateFederal = F, IsTestFile = F, ModifiedTime = F, TransmitterControlCode = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECExportElectronicFileID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFile", objectId = Form1099NECExportElectronicFileID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECExportElectronicFile
	#'
	#' This function deletes a Form1099NECExportElectronicFile
	#' @param Form1099NECExportElectronicFileID The ID of the Form1099NECExportElectronicFile to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECExportElectronicFileID of the deleted Form1099NECExportElectronicFile.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECExportElectronicFile <- function(Form1099NECExportElectronicFileID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFile", objectId = Form1099NECExportElectronicFileID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECExportElectronicFile
	#'
	#' This function creates a Form1099NECExportElectronicFile
	#' @param fieldNames The field values to give the created Form1099NECExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECExportElectronicFile <- function(Form1099NECSubmissionID = NULL, IsCombinedStateFederal = NULL, IsTestFile = NULL, TransmitterControlCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFile", body = list(DataObject = body), searchFields = append("Form1099NECExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECExportElectronicFile
	#'
	#' This function modifies a Form1099NECExportElectronicFile
	#' @param fieldNames The field values to give the modified Form1099NECExportElectronicFile. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECExportElectronicFile
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECExportElectronicFile <- function(Form1099NECExportElectronicFileID, Form1099NECSubmissionID = NULL, IsCombinedStateFederal = NULL, IsTestFile = NULL, TransmitterControlCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFile", objectId = Form1099NECExportElectronicFileID, body = list(DataObject = body), searchFields = append("Form1099NECExportElectronicFileID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECExportElectronicFileMedias
	#'
	#' This function returns a dataframe or json object of Form1099NECExportElectronicFileMedias
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECExportElectronicFileMedias. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECExportElectronicFileMedias.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECExportElectronicFileMedia') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECExportElectronicFileMedias
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECExportElectronicFileMedias <- function(searchConditionsList = NULL, CreatedTime = F, Form1099NECExportElectronicFileID = F, Form1099NECExportElectronicFileMediaID = F, MediaID = F, ModifiedTime = F, StateID = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFileMedia", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECExportElectronicFileMedia
	#'
	#' This function returns a dataframe or json object of a Form1099NECExportElectronicFileMedia
	#' @param Form1099NECExportElectronicFileMediaID The ID of the Form1099NECExportElectronicFileMedia to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECExportElectronicFileMedia. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECExportElectronicFileMedia.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECExportElectronicFileMedia') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECExportElectronicFileMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECExportElectronicFileMedia <- function(Form1099NECExportElectronicFileMediaID, CreatedTime = F, Form1099NECExportElectronicFileID = F, MediaID = F, ModifiedTime = F, StateID = F, Status = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECExportElectronicFileMediaID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFileMedia", objectId = Form1099NECExportElectronicFileMediaID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECExportElectronicFileMedia
	#'
	#' This function deletes a Form1099NECExportElectronicFileMedia
	#' @param Form1099NECExportElectronicFileMediaID The ID of the Form1099NECExportElectronicFileMedia to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECExportElectronicFileMediaID of the deleted Form1099NECExportElectronicFileMedia.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECExportElectronicFileMedia <- function(Form1099NECExportElectronicFileMediaID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFileMedia", objectId = Form1099NECExportElectronicFileMediaID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECExportElectronicFileMedia
	#'
	#' This function creates a Form1099NECExportElectronicFileMedia
	#' @param fieldNames The field values to give the created Form1099NECExportElectronicFileMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECExportElectronicFileMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECExportElectronicFileMedia <- function(Form1099NECExportElectronicFileID = NULL, MediaID = NULL, StateID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFileMedia", body = list(DataObject = body), searchFields = append("Form1099NECExportElectronicFileMediaID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECExportElectronicFileMedia
	#'
	#' This function modifies a Form1099NECExportElectronicFileMedia
	#' @param fieldNames The field values to give the modified Form1099NECExportElectronicFileMedia. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECExportElectronicFileMedia
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECExportElectronicFileMedia <- function(Form1099NECExportElectronicFileMediaID, Form1099NECExportElectronicFileID = NULL, MediaID = NULL, StateID = NULL, Status = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECExportElectronicFileMedia", objectId = Form1099NECExportElectronicFileMediaID, body = list(DataObject = body), searchFields = append("Form1099NECExportElectronicFileMediaID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECSubmissions
	#'
	#' This function returns a dataframe or json object of Form1099NECSubmissions
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECSubmissions. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECSubmissions.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECSubmission') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECSubmissions
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECSubmissions <- function(searchConditionsList = NULL, CalendarYearID = F, CreatedTime = F, Description = F, DistrictID = F, Form1099NECSubmissionID = F, HasAnExport = F, HasAnExtract = F, ModifiedTime = F, MostRecentExportDate = F, MostRecentExtractDate = F, NumericYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECSubmission", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECSubmission
	#'
	#' This function returns a dataframe or json object of a Form1099NECSubmission
	#' @param Form1099NECSubmissionID The ID of the Form1099NECSubmission to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECSubmission. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECSubmission.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECSubmission') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECSubmission <- function(Form1099NECSubmissionID, CalendarYearID = F, CreatedTime = F, Description = F, DistrictID = F, HasAnExport = F, HasAnExtract = F, ModifiedTime = F, MostRecentExportDate = F, MostRecentExtractDate = F, NumericYear = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECSubmissionID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmission", objectId = Form1099NECSubmissionID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECSubmission
	#'
	#' This function deletes a Form1099NECSubmission
	#' @param Form1099NECSubmissionID The ID of the Form1099NECSubmission to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECSubmissionID of the deleted Form1099NECSubmission.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECSubmission <- function(Form1099NECSubmissionID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmission", objectId = Form1099NECSubmissionID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECSubmission
	#'
	#' This function creates a Form1099NECSubmission
	#' @param fieldNames The field values to give the created Form1099NECSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECSubmission <- function(CalendarYearID = NULL, Description = NULL, DistrictID = NULL, NumericYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmission", body = list(DataObject = body), searchFields = append("Form1099NECSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECSubmission
	#'
	#' This function modifies a Form1099NECSubmission
	#' @param fieldNames The field values to give the modified Form1099NECSubmission. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECSubmission
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECSubmission <- function(Form1099NECSubmissionID, CalendarYearID = NULL, Description = NULL, DistrictID = NULL, NumericYear = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmission", objectId = Form1099NECSubmissionID, body = list(DataObject = body), searchFields = append("Form1099NECSubmissionID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECSubmissionRunHistories
	#'
	#' This function returns a dataframe or json object of Form1099NECSubmissionRunHistories
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECSubmissionRunHistories. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECSubmissionRunHistories.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECSubmissionRunHistory') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECSubmissionRunHistories
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECSubmissionRunHistories <- function(searchConditionsList = NULL, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportFormat = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECSubmissionRunHistory", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECSubmissionRunHistory
	#'
	#' This function returns a dataframe or json object of a Form1099NECSubmissionRunHistory
	#' @param Form1099NECSubmissionRunHistoryID The ID of the Form1099NECSubmissionRunHistory to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECSubmissionRunHistory. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECSubmissionRunHistory.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECSubmissionRunHistory') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECSubmissionRunHistory <- function(Form1099NECSubmissionRunHistoryID, CanAccessMedia = F, CreatedTime = F, EndDateTime = F, ExportFormat = F, Form1099NECSubmissionID = F, HasValidMedia = F, IsExport = F, IsLocked = F, MediaID = F, ModifiedTime = F, RunData = F, RunParameters = F, ScopeAccessAllowed = F, StartDateTime = F, Status = F, Type = F, UserIDCanceledBy = F, UserIDCreatedBy = F, UserIDModifiedBy = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECSubmissionRunHistoryID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionRunHistory", objectId = Form1099NECSubmissionRunHistoryID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECSubmissionRunHistory
	#'
	#' This function deletes a Form1099NECSubmissionRunHistory
	#' @param Form1099NECSubmissionRunHistoryID The ID of the Form1099NECSubmissionRunHistory to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECSubmissionRunHistoryID of the deleted Form1099NECSubmissionRunHistory.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECSubmissionRunHistory <- function(Form1099NECSubmissionRunHistoryID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionRunHistory", objectId = Form1099NECSubmissionRunHistoryID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECSubmissionRunHistory
	#'
	#' This function creates a Form1099NECSubmissionRunHistory
	#' @param fieldNames The field values to give the created Form1099NECSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECSubmissionRunHistory <- function(EndDateTime = NULL, ExportFormat = NULL, Form1099NECSubmissionID = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionRunHistory", body = list(DataObject = body), searchFields = append("Form1099NECSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECSubmissionRunHistory
	#'
	#' This function modifies a Form1099NECSubmissionRunHistory
	#' @param fieldNames The field values to give the modified Form1099NECSubmissionRunHistory. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECSubmissionRunHistory
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECSubmissionRunHistory <- function(Form1099NECSubmissionRunHistoryID, EndDateTime = NULL, ExportFormat = NULL, Form1099NECSubmissionID = NULL, MediaID = NULL, StartDateTime = NULL, Status = NULL, Type = NULL, UserIDCanceledBy = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionRunHistory", objectId = Form1099NECSubmissionRunHistoryID, body = list(DataObject = body), searchFields = append("Form1099NECSubmissionRunHistoryID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECSubmissionStateTotals
	#'
	#' This function returns a dataframe or json object of Form1099NECSubmissionStateTotals
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECSubmissionStateTotals. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECSubmissionStateTotals.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECSubmissionStateTotal') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECSubmissionStateTotals
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECSubmissionStateTotals <- function(searchConditionsList = NULL, FederalIncomeTaxWithheldTotal = F, Form1099NECSubmissionID = F, Form1099NECSubmissionStateTotalID = F, NonEmployeeCompensationTotal = F, StateCode = F, TaxStateID = F, VendorCount = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECSubmissionStateTotal", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECSubmissionStateTotal
	#'
	#' This function returns a dataframe or json object of a Form1099NECSubmissionStateTotal
	#' @param Form1099NECSubmissionStateTotalID The ID of the Form1099NECSubmissionStateTotal to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECSubmissionStateTotal. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECSubmissionStateTotal.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECSubmissionStateTotal') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECSubmissionStateTotal
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECSubmissionStateTotal <- function(Form1099NECSubmissionStateTotalID, FederalIncomeTaxWithheldTotal = F, Form1099NECSubmissionID = F, NonEmployeeCompensationTotal = F, StateCode = F, TaxStateID = F, VendorCount = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECSubmissionStateTotalID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionStateTotal", objectId = Form1099NECSubmissionStateTotalID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECSubmissionStateTotal
	#'
	#' This function deletes a Form1099NECSubmissionStateTotal
	#' @param Form1099NECSubmissionStateTotalID The ID of the Form1099NECSubmissionStateTotal to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECSubmissionStateTotalID of the deleted Form1099NECSubmissionStateTotal.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECSubmissionStateTotal <- function(Form1099NECSubmissionStateTotalID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionStateTotal", objectId = Form1099NECSubmissionStateTotalID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECSubmissionStateTotal
	#'
	#' This function creates a Form1099NECSubmissionStateTotal
	#' @param fieldNames The field values to give the created Form1099NECSubmissionStateTotal. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECSubmissionStateTotal
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECSubmissionStateTotal <- function(FederalIncomeTaxWithheldTotal = NULL, Form1099NECSubmissionID = NULL, NonEmployeeCompensationTotal = NULL, StateCode = NULL, TaxStateID = NULL, VendorCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionStateTotal", body = list(DataObject = body), searchFields = append("Form1099NECSubmissionStateTotalID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECSubmissionStateTotal
	#'
	#' This function modifies a Form1099NECSubmissionStateTotal
	#' @param fieldNames The field values to give the modified Form1099NECSubmissionStateTotal. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECSubmissionStateTotal
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECSubmissionStateTotal <- function(Form1099NECSubmissionStateTotalID, FederalIncomeTaxWithheldTotal = NULL, Form1099NECSubmissionID = NULL, NonEmployeeCompensationTotal = NULL, StateCode = NULL, TaxStateID = NULL, VendorCount = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECSubmissionStateTotal", objectId = Form1099NECSubmissionStateTotalID, body = list(DataObject = body), searchFields = append("Form1099NECSubmissionStateTotalID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECEmployerV1s
	#'
	#' This function returns a dataframe or json object of Form1099NECEmployerV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECEmployerV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECEmployerV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECEmployerV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECEmployerV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECEmployerV1s <- function(searchConditionsList = NULL, Address = F, City = F, ContactName = F, CreatedTime = F, EmailAddress = F, EmployerID = F, FederalEIN = F, Form1099NECEmployerV1ID = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, Name = F, PhoneNumber = F, StateCode = F, StateID = F, TransmitterControlCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECEmployerV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECEmployerV1
	#'
	#' This function returns a dataframe or json object of a Form1099NECEmployerV1
	#' @param Form1099NECEmployerV1ID The ID of the Form1099NECEmployerV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECEmployerV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECEmployerV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECEmployerV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECEmployerV1 <- function(Form1099NECEmployerV1ID, Address = F, City = F, ContactName = F, CreatedTime = F, EmailAddress = F, EmployerID = F, FederalEIN = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, HasErrors = F, KeyHash = F, ModifiedTime = F, Name = F, PhoneNumber = F, StateCode = F, StateID = F, TransmitterControlCode = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, ZipCode = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECEmployerV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerV1", objectId = Form1099NECEmployerV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECEmployerV1
	#'
	#' This function deletes a Form1099NECEmployerV1
	#' @param Form1099NECEmployerV1ID The ID of the Form1099NECEmployerV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECEmployerV1ID of the deleted Form1099NECEmployerV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECEmployerV1 <- function(Form1099NECEmployerV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerV1", objectId = Form1099NECEmployerV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECEmployerV1
	#'
	#' This function creates a Form1099NECEmployerV1
	#' @param fieldNames The field values to give the created Form1099NECEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECEmployerV1 <- function(Address = NULL, City = NULL, ContactName = NULL, EmailAddress = NULL, EmployerID = NULL, FederalEIN = NULL, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, Name = NULL, PhoneNumber = NULL, StateCode = NULL, StateID = NULL, TransmitterControlCode = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerV1", body = list(DataObject = body), searchFields = append("Form1099NECEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECEmployerV1
	#'
	#' This function modifies a Form1099NECEmployerV1
	#' @param fieldNames The field values to give the modified Form1099NECEmployerV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECEmployerV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECEmployerV1 <- function(Form1099NECEmployerV1ID, Address = NULL, City = NULL, ContactName = NULL, EmailAddress = NULL, EmployerID = NULL, FederalEIN = NULL, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, Name = NULL, PhoneNumber = NULL, StateCode = NULL, StateID = NULL, TransmitterControlCode = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECEmployerV1", objectId = Form1099NECEmployerV1ID, body = list(DataObject = body), searchFields = append("Form1099NECEmployerV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' List Form1099NECVendorV1s
	#'
	#' This function returns a dataframe or json object of Form1099NECVendorV1s
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECVendorV1s. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECVendorV1s.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECVendorV1') to get more field paths.
	#' @param searchConditionsList A list of search conditions to filter results which are joined by the searchConditionsGroupType. Of the form {FieldName} {ConditionType} {SearchCondition}. For example, c('StudentID LessEqual 500', 'LastName Like Ander\%'). Run \code{\link{listSearchConditionTypes}} for a list of ConditionTypes. Defaults to NULL (unfiltered).
	#' @param searchConditionsGroupType The conjunction which joins multiple searchConditions in the searchConditionsList. Either 'Or' or 'And'. Defaults to 'And'.
	#' @param searchSortFieldNamesList The list of fields sort results by. Defaults to NULL (unsorted).
	#' @param searchSortFieldNamesDescendingList A list of T/F values corresponding to whether to sort each field in searchSortFieldNamesList in descending order. Defaults to F for each FieldName in searchSortFieldNamesList.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param page Results are paginated. The page of results to return. Default is 1.
	#' @param pageSize Results are paginated. The number of records per page to return. Default is 100,000 (essentially all records for most objects).
	#' @param flatten Whether to flatten results into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A list of Form1099NECVendorV1s
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	listForm1099NECVendorV1s <- function(searchConditionsList = NULL, AccountNumber = F, Address = F, City = F, CreatedTime = F, FederalIncomeTaxWithheld = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, Form1099NECVendorV1ID = F, HasErrors = F, KeyHash = F, MediaIDIndividualPDF = F, ModifiedTime = F, NameControl = F, NameLine1 = F, NameLine2 = F, NonEmployeeCompensation = F, StateCode = F, StateID = F, TaxpayerIdentificationNumber = F, TaxpayerIdentificationType = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VendorID = F, ZipCode = F, fieldPaths = NULL, searchConditionsGroupType = "And", searchSortFieldNamesList = NULL, searchSortFieldNamesDescendingList = NULL, entityId = 1, query = NULL, page = 1, pageSize = 100000, flatten = T, returnResponse = F){

		params <- as.list(environment())

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		listSkyObjects(module = "StateReportingFederal", objectName = "Form1099NECVendorV1", searchFields = searchFields %>% append(fieldPaths), page = page, pageSize = pageSize, SearchConditionsList = searchConditionsList, SearchConditionsGroupType = searchConditionsGroupType, SearchSortFieldNamesList = searchSortFieldNamesList, SearchSortFieldNamesDescendingList = searchSortFieldNamesDescendingList, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Get a Form1099NECVendorV1
	#'
	#' This function returns a dataframe or json object of a Form1099NECVendorV1
	#' @param Form1099NECVendorV1ID The ID of the Form1099NECVendorV1 to return.
	#' @param fieldNames A TRUE or FALSE value determining whether or not to return the field for the given Form1099NECVendorV1. Defaults to FALSE for all return fields which, for convenience, returns all fields for the Form1099NECVendorV1.
	#' @param fieldPaths Fields from other objects with 'Many to One' or 'One to One' relationships to the given object listed as text. Run \code{\link{getSchemaForObjects}}('Form1099NECVendorV1') to get more field paths.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A dataframe or of Form1099NECVendorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	getForm1099NECVendorV1 <- function(Form1099NECVendorV1ID, AccountNumber = F, Address = F, City = F, CreatedTime = F, FederalIncomeTaxWithheld = F, Form1099NECSubmissionID = F, Form1099NECSubmissionRunHistoryID = F, HasErrors = F, KeyHash = F, MediaIDIndividualPDF = F, ModifiedTime = F, NameControl = F, NameLine1 = F, NameLine2 = F, NonEmployeeCompensation = F, StateCode = F, StateID = F, TaxpayerIdentificationNumber = F, TaxpayerIdentificationType = F, UpdateHash = F, UserIDCreatedBy = F, UserIDModifiedBy = F, VendorID = F, ZipCode = F, fieldPaths = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment()) %>% purrr::keep(names(.) != "Form1099NECVendorV1ID")

		searchFields <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper())

		ifelse(!any(searchFields %>% unlist()), searchFields <- searchFields %>% names(), searchFields <- searchFields %>% purrr::keep(~.x) %>% names())

		getSkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorV1", objectId = Form1099NECVendorV1ID, searchFields = searchFields, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Delete a Form1099NECVendorV1
	#'
	#' This function deletes a Form1099NECVendorV1
	#' @param Form1099NECVendorV1ID The ID of the Form1099NECVendorV1 to delete
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The Form1099NECVendorV1ID of the deleted Form1099NECVendorV1.
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	deleteForm1099NECVendorV1 <- function(Form1099NECVendorV1ID, ignoreWarnings = F, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		deleteSkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorV1", objectId = Form1099NECVendorV1ID, ignoreWarnings = ignoreWarnings, entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Create a Form1099NECVendorV1
	#'
	#' This function creates a Form1099NECVendorV1
	#' @param fieldNames The field values to give the created Form1099NECVendorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return A newly created Form1099NECVendorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	createForm1099NECVendorV1 <- function(AccountNumber = NULL, Address = NULL, City = NULL, FederalIncomeTaxWithheld = NULL, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, MediaIDIndividualPDF = NULL, NameControl = NULL, NameLine1 = NULL, NameLine2 = NULL, NonEmployeeCompensation = NULL, StateCode = NULL, StateID = NULL, TaxpayerIdentificationNumber = NULL, TaxpayerIdentificationType = NULL, VendorID = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		createSkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorV1", body = list(DataObject = body), searchFields = append("Form1099NECVendorV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}

	#' Modify a Form1099NECVendorV1
	#'
	#' This function modifies a Form1099NECVendorV1
	#' @param fieldNames The field values to give the modified Form1099NECVendorV1. Each defaults to NULL.
	#' @param entityId The id of the entity (school). Run \code{\link{listEntities}} for a list of entities. Defaults to 1 (district).
	#' @param query Queries to be added to the request. Ex. list(SchoolYearID = 1). Defaults to NULL.
	#' @param flatten Whether to flatten result into a dataframe or return the json object. Default is TRUE (flatten to dataframe).
	#' @param returnResponse Whether to return the server response instead of the results. Useful for debugging. Default is FALSE.
	#' @concept StateReportingFederal
	#' @return The modified Form1099NECVendorV1
	#' \href{https://help.skyward.com/}{Skyward's Knowledge Hub}
	#' @export
	modifyForm1099NECVendorV1 <- function(Form1099NECVendorV1ID, AccountNumber = NULL, Address = NULL, City = NULL, FederalIncomeTaxWithheld = NULL, Form1099NECSubmissionID = NULL, Form1099NECSubmissionRunHistoryID = NULL, MediaIDIndividualPDF = NULL, NameControl = NULL, NameLine1 = NULL, NameLine2 = NULL, NonEmployeeCompensation = NULL, StateCode = NULL, StateID = NULL, TaxpayerIdentificationNumber = NULL, TaxpayerIdentificationType = NULL, VendorID = NULL, ZipCode = NULL, entityId = 1, query = NULL, flatten = T, returnResponse = F){

		params <- as.list(environment())

		body <- params %>% purrr::keep(names(params) %>% stringr::str_sub(1,1) == names(params) %>% stringr::str_sub(1,1) %>% stringr::str_to_upper()) %>% purrr::compact()

		modifySkyObject(module = "StateReportingFederal", objectName = "Form1099NECVendorV1", objectId = Form1099NECVendorV1ID, body = list(DataObject = body), searchFields = append("Form1099NECVendorV1ID", body %>% names()), entityId = entityId, query = query, flatten = flatten, returnResponse = returnResponse)
	}
